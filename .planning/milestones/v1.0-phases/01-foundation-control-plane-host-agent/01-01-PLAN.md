---
phase: 01-foundation-control-plane-host-agent
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/control-plane/main.go
  - internal/controlplane/app/app.go
  - internal/controlplane/http/router.go
  - internal/store/migrator/migrator.go
  - internal/store/migrations/0001_initial.sql
  - internal/store/repository/models.go
  - internal/store/repository/queries.go
  - deploy/docker/control-plane/Dockerfile
  - deploy/compose/control-plane.dev.yml
autonomous: true
requirements:
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - "运维方可以在单宿主机开发环境中启动 `control-plane` 与 PostgreSQL 基础服务。"
    - "控制面已经具备健康检查入口、任务导向的 HTTP 骨架，以及 Phase 1 核心数据表。"
  artifacts:
    - path: "cmd/control-plane/main.go"
      provides: "控制面启动入口与配置装配"
      min_lines: 30
    - path: "internal/store/migrations/0001_initial.sql"
      provides: "用户、主机、出口 IP、绑定、任务、事件的初始 schema"
      contains: "CREATE TABLE tasks"
    - path: "internal/store/migrator/migrator.go"
      provides: "控制面启动时执行 migration 的逻辑"
      contains: "RunMigrations"
    - path: "deploy/compose/control-plane.dev.yml"
      provides: "本地单宿主机开发编排"
      contains: "control-plane:"
  key_links:
    - from: "cmd/control-plane/main.go"
      to: "internal/controlplane/http/router.go"
      via: "HTTP 路由注册"
      pattern: "NewRouter"
    - from: "internal/controlplane/app/app.go"
      to: "internal/store/repository/queries.go"
      via: "数据库依赖注入"
      pattern: "Repository"
---

<objective>
交付 Phase 1 的控制面基础盘：Go 服务骨架、PostgreSQL 核心 schema，以及单宿主机开发部署布局。

Purpose: 先把控制面与持久化真实状态立住，让后续主机代理、受管镜像与生命周期任务都有稳定落点。
Output: `control-plane` 启动入口、HTTP 路由骨架、可执行的 Phase 1 初始 migration、仓储接口草图、control-plane 容器镜像定义，以及 `deploy/compose/control-plane.dev.yml`。
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md
@.planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Go 控制面服务骨架</name>
  <files>go.mod, cmd/control-plane/main.go, internal/controlplane/app/app.go, internal/controlplane/http/router.go, internal/store/migrator/migrator.go</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md, CLAUDE.md</read_first>
  <action>创建 `module github.com/zaneliu/cloud-cli-proxy` 的 Go 工程；新增 `cmd/control-plane/main.go` 作为唯一启动入口，默认监听 `:8080`，读取 `CONTROL_PLANE_ADDR` 与 `DATABASE_URL`，并在 HTTP 服务监听前调用 `internal/store/migrator/migrator.go` 中的 `RunMigrations(ctx, db, "internal/store/migrations")`；在 `internal/controlplane/app/app.go` 里装配配置、日志、repository 与 migrator 依赖；在 `internal/controlplane/http/router.go` 注册 `GET /healthz`、`GET /v1/users`、`GET /v1/hosts`、`GET /v1/tasks` 四个 JSON 路由，其中 `GET /v1/tasks` 必须先保留真实 handler 接口位而不是匿名占位。所有生命周期相关路由禁止在控制面内直接调用 Docker、`ip`、`iptables` 或其他宿主机特权命令。</action>
  <verify>rg -n "module github.com/zaneliu/cloud-cli-proxy|CONTROL_PLANE_ADDR|DATABASE_URL|/healthz|/v1/tasks|RunMigrations|internal/store/migrations" go.mod cmd/control-plane/main.go internal/controlplane/app/app.go internal/controlplane/http/router.go internal/store/migrator/migrator.go</verify>
  <acceptance_criteria>
    - `go.mod` 包含 `module github.com/zaneliu/cloud-cli-proxy`
    - `cmd/control-plane/main.go` 包含 `:8080` 与 `DATABASE_URL`
    - `internal/controlplane/http/router.go` 包含 `/healthz`、`/v1/users`、`/v1/hosts`、`/v1/tasks`
    - `internal/store/migrator/migrator.go` 包含 `RunMigrations`
    - `cmd/control-plane/main.go` 或 `internal/controlplane/app/app.go` 包含 `RunMigrations`
    - 上述文件中不出现 `docker run`、`iptables`、`ip netns` 这类直接宿主机操作命令
  </acceptance_criteria>
  <done>Go 控制面已经能以配置驱动方式启动、先执行 migration、再提供 HTTP 入口，并把宿主机特权边界明确切开。</done>
</task>

<task type="auto">
  <name>Task 2: 定义 PostgreSQL 核心 schema 与仓储契约</name>
  <files>internal/store/migrations/0001_initial.sql, internal/store/repository/models.go, internal/store/repository/queries.go</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md</read_first>
  <action>新增 `internal/store/migrations/0001_initial.sql`，创建 `users`、`hosts`、`egress_ips`、`host_egress_bindings`、`tasks`、`events` 六张表，并为 `tasks.status` 限定 `pending|running|succeeded|failed|canceled`；在 `hosts` 中加入 `template_image_ref`、`home_volume_name`、`slot_key`，同时通过 `UNIQUE (user_id, slot_key)` 保留未来多主机扩展；在 `tasks` 中加入 `kind`、`error_code`、`error_message`、`requested_by`、`last_error_summary`；建立 `idx_tasks_status_updated_at`、`idx_hosts_user_id`、`idx_host_egress_bindings_host_id` 索引。随后在 `models.go` 与 `queries.go` 中定义与 Phase 1 对齐的 repository 结构和方法：至少包含 `CreateTask`、`ListPendingTasks`、`ListTasksWithLastErrorSummary`、`UpsertHost`、`ListHostBindings`、`RecordEvent`。</action>
  <verify>rg -n "CREATE TABLE users|CREATE TABLE hosts|CREATE TABLE egress_ips|CREATE TABLE host_egress_bindings|CREATE TABLE tasks|CREATE TABLE events|pending|running|succeeded|failed|canceled|last_error_summary|CreateTask|ListPendingTasks|ListTasksWithLastErrorSummary|UpsertHost|RecordEvent" internal/store/migrations/0001_initial.sql internal/store/repository/models.go internal/store/repository/queries.go</verify>
  <acceptance_criteria>
    - `internal/store/migrations/0001_initial.sql` 同时包含 `CREATE TABLE users`、`hosts`、`egress_ips`、`host_egress_bindings`、`tasks`、`events`
    - `internal/store/migrations/0001_initial.sql` 包含 `pending`、`running`、`succeeded`、`failed`、`canceled`
    - `internal/store/migrations/0001_initial.sql` 包含 `template_image_ref`、`home_volume_name`、`slot_key`、`last_error_summary`
    - `internal/store/repository/queries.go` 包含 `CreateTask`、`ListPendingTasks`、`ListTasksWithLastErrorSummary`、`UpsertHost`、`ListHostBindings`、`RecordEvent`
  </acceptance_criteria>
  <done>Phase 1 成功标准里的核心业务实体已经持久化建模，后续 agent 与控制面都能围绕同一份真实状态推进。</done>
</task>

<task type="auto">
  <name>Task 3: 补齐单宿主机开发部署布局</name>
  <files>deploy/docker/control-plane/Dockerfile, deploy/compose/control-plane.dev.yml</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>新增 `deploy/docker/control-plane/Dockerfile`，以 `golang:1.26.1` 为基础镜像，`WORKDIR /app`，拷贝 `go.mod`、源码目录与 `internal/store/migrations`，执行 `go build -o /usr/local/bin/control-plane ./cmd/control-plane`；新增 `deploy/compose/control-plane.dev.yml`，只编排 `postgres` 与 `control-plane` 两个服务；`postgres` 使用 `postgres:18.3`，挂载持久卷并以 `pg_isready` 做 healthcheck；`control-plane` 使用 `build.context: .` + `build.dockerfile: deploy/docker/control-plane/Dockerfile`，注入 `DATABASE_URL=postgres://cloudproxy:cloudproxy@postgres:5432/cloudproxy?sslmode=disable` 与 `CONTROL_PLANE_ADDR=:8080`，映射 `8080:8080`，并以 `curl --fail http://127.0.0.1:8080/healthz` 做 healthcheck。执行验证时必须运行 `docker compose -f deploy/compose/control-plane.dev.yml up -d postgres control-plane`，然后用 `docker compose -f deploy/compose/control-plane.dev.yml exec -T postgres psql -U cloudproxy -d cloudproxy -Atc "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('users','hosts','egress_ips','host_egress_bindings','tasks','events') ORDER BY tablename"` 确认六张表真实存在并全部出现在输出里。不要在这个 compose 文件中加入 `host-agent`、WireGuard、隧道容器或任何 SSH 接入流程。</action>
  <verify>docker compose -f deploy/compose/control-plane.dev.yml up -d postgres control-plane && docker compose -f deploy/compose/control-plane.dev.yml exec -T postgres psql -U cloudproxy -d cloudproxy -Atc "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('users','hosts','egress_ips','host_egress_bindings','tasks','events') ORDER BY tablename" && curl --fail http://127.0.0.1:8080/healthz</verify>
  <acceptance_criteria>
    - `deploy/docker/control-plane/Dockerfile` 包含 `FROM golang:1.26.1`、`WORKDIR /app`、`go build -o /usr/local/bin/control-plane ./cmd/control-plane`
    - `deploy/compose/control-plane.dev.yml` 包含 `postgres:18.3`
    - `deploy/compose/control-plane.dev.yml` 包含 `control-plane:` 与 `8080:8080`
    - `deploy/compose/control-plane.dev.yml` 包含完整的 `DATABASE_URL=postgres://cloudproxy:cloudproxy@postgres:5432/cloudproxy?sslmode=disable`
    - `docker compose -f deploy/compose/control-plane.dev.yml exec -T postgres ... SELECT tablename ...` 的输出能看到 `users`、`hosts`、`egress_ips`、`host_egress_bindings`、`tasks`、`events`
    - `deploy/compose/control-plane.dev.yml` 不包含 `host-agent`、`wireguard`、`ssh handoff`
  </acceptance_criteria>
  <done>单宿主机最小开发部署布局已落地，执行者可以先把控制面和数据库跑起来，再继续完成后续计划。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "module github.com/zaneliu/cloud-cli-proxy|/healthz|/v1/tasks|RunMigrations" go.mod cmd/control-plane/main.go internal/controlplane/http/router.go internal/store/migrator/migrator.go`
- [ ] `rg -n "CREATE TABLE users|CREATE TABLE hosts|CREATE TABLE tasks|CREATE TABLE events|last_error_summary|ListTasksWithLastErrorSummary" internal/store/migrations/0001_initial.sql internal/store/repository/queries.go`
- [ ] `docker compose -f deploy/compose/control-plane.dev.yml up -d postgres control-plane && docker compose -f deploy/compose/control-plane.dev.yml exec -T postgres psql -U cloudproxy -d cloudproxy -Atc "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('users','hosts','egress_ips','host_egress_bindings','tasks','events') ORDER BY tablename" && curl --fail http://127.0.0.1:8080/healthz`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 控制面、PostgreSQL schema 与开发编排文件都已经可执行落地
- 没有把 Docker/网络特权直接塞回控制面进程
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-control-plane-host-agent/01-01-SUMMARY.md`
</output>
