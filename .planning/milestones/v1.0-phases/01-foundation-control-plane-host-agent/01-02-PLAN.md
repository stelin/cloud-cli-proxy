---
phase: 01-foundation-control-plane-host-agent
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/docker/managed-user/Dockerfile
  - deploy/docker/managed-user/sshd_config
  - deploy/docker/managed-user/entrypoint.sh
  - deploy/docker/managed-user/image.lock
  - deploy/docker/managed-user/README.md
  - deploy/docker/managed-user/build-managed-image.sh
  - scripts/verify-managed-image.sh
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
user_setup: []
must_haves:
  truths:
    - "项目有一份可重复构建的受管用户镜像模板，默认提供 SSH 工作环境。"
    - "镜像模板内预装 OpenSSH Server、基础 Shell 工具和 `claude code`，并明确主目录持久化约定。"
  artifacts:
    - path: "deploy/docker/managed-user/Dockerfile"
      provides: "受管用户镜像定义"
      min_lines: 40
    - path: "deploy/docker/managed-user/image.lock"
      provides: "镜像 tag/digest pin 与运行时契约"
      contains: "pull_policy: never-implicit-latest"
    - path: "scripts/verify-managed-image.sh"
      provides: "镜像冒烟校验脚本"
      contains: "command -v claude"
  key_links:
    - from: "deploy/docker/managed-user/Dockerfile"
      to: "deploy/docker/managed-user/sshd_config"
      via: "COPY SSH 配置"
      pattern: "sshd_config"
    - from: "deploy/docker/managed-user/entrypoint.sh"
      to: "scripts/verify-managed-image.sh"
      via: "共用 SSH 启动契约"
      pattern: "sshd"
---

<objective>
交付 Phase 1 的受管用户镜像模板，明确镜像内容、版本锁定方式与基础验收脚本。

Purpose: 把 `RUNT-02` 固化为真实镜像产物，并为后续主机代理创建/重建容器提供稳定模板。
Output: 受管镜像 `Dockerfile`、SSH 配置与启动脚本、镜像锁文件、构建脚本、冒烟验证脚本与运行约定文档。
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md
@.planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建受管用户镜像与 SSH 启动脚本</name>
  <files>deploy/docker/managed-user/Dockerfile, deploy/docker/managed-user/sshd_config, deploy/docker/managed-user/entrypoint.sh</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>新增 `deploy/docker/managed-user/Dockerfile`，以 `ubuntu:24.04` 为基础镜像，安装 `openssh-server`、`bash`、`zsh`、`curl`、`git`、`tmux`、`sudo`、`ca-certificates`、`jq`、`procps`、`iproute2` 与 `nodejs`；通过 `npm install -g @anthropic-ai/claude-code` 提供 `claude` 命令；创建固定用户 `workspace`（UID/GID 1000），主目录与工作目录都设为 `/workspace`；新增 `sshd_config`，固定 `Port 22`、禁用密码为空登录、开启 `AuthorizedKeysFile .ssh/authorized_keys`；新增 `entrypoint.sh`，启动时补齐 `/var/run/sshd`、在缺失时生成 host keys，并以前台方式执行 `sshd -D -e`。不要在镜像内实现 WireGuard、隧道接线或终端 handoff。</action>
  <verify>rg -n "FROM ubuntu:24.04|openssh-server|@anthropic-ai/claude-code|workspace|/workspace|Port 22|AuthorizedKeysFile|sshd -D -e" deploy/docker/managed-user/Dockerfile deploy/docker/managed-user/sshd_config deploy/docker/managed-user/entrypoint.sh</verify>
  <acceptance_criteria>
    - `deploy/docker/managed-user/Dockerfile` 包含 `FROM ubuntu:24.04`
    - `deploy/docker/managed-user/Dockerfile` 同时包含 `openssh-server` 与 `@anthropic-ai/claude-code`
    - `deploy/docker/managed-user/sshd_config` 包含 `Port 22` 与 `AuthorizedKeysFile .ssh/authorized_keys`
    - `deploy/docker/managed-user/entrypoint.sh` 包含 `sshd -D -e`
  </acceptance_criteria>
  <done>受管镜像已经把 SSH 工作环境与 `claude code` 固化为可构建的模板，并保持 Phase 1 不越界到网络强约束实现。</done>
</task>

<task type="auto">
  <name>Task 2: 锁定镜像版本并写清运行时契约</name>
  <files>deploy/docker/managed-user/image.lock, deploy/docker/managed-user/README.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md</read_first>
  <action>新增 `deploy/docker/managed-user/image.lock`，用明确字段锁定镜像元数据：`image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1`、`local_dev_image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1`、`base_image: ubuntu:24.04`、`pull_policy: never-implicit-latest`、`ssh_port: 22`、`home_mount: /workspace`、`default_user: workspace`、`rebuild_mode_default: preserve-home`、`factory_reset_mode: wipe-/workspace`。同时新增 `deploy/docker/managed-user/README.md`，说明该镜像用于“一用户一主机”的模板容器，主目录持久化挂载点固定为 `/workspace`，控制面与 host-agent 必须统一从 `image.lock` 读取同一个镜像全名，Phase 2 只允许新增网络准备钩子接口，不在本计划内落地隧道。</action>
  <verify>rg -n "image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1|local_dev_image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1|pull_policy: never-implicit-latest|ssh_port: 22|home_mount: /workspace|default_user: workspace|rebuild_mode_default: preserve-home|factory_reset_mode: wipe-/workspace|同一个镜像全名|Phase 2" deploy/docker/managed-user/image.lock deploy/docker/managed-user/README.md</verify>
  <acceptance_criteria>
    - `deploy/docker/managed-user/image.lock` 包含 `pull_policy: never-implicit-latest`
    - `deploy/docker/managed-user/image.lock` 包含 `image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1`
    - `deploy/docker/managed-user/image.lock` 包含 `home_mount: /workspace` 与 `default_user: workspace`
    - `deploy/docker/managed-user/image.lock` 包含 `rebuild_mode_default: preserve-home`
    - `deploy/docker/managed-user/README.md` 明确写出控制面与 host-agent 必须统一读取 `image.lock` 中的同一个镜像全名
  </acceptance_criteria>
  <done>镜像升级与重建行为已经有可审计的锁定来源，后续 agent 不需要猜测该用哪个模板与持久化模式。</done>
</task>

<task type="auto">
  <name>Task 3: 提供构建与冒烟校验脚本</name>
  <files>deploy/docker/managed-user/build-managed-image.sh, scripts/verify-managed-image.sh</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>新增 `deploy/docker/managed-user/build-managed-image.sh`，固定用 `docker build -f deploy/docker/managed-user/Dockerfile -t ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1 .` 构建镜像；新增 `scripts/verify-managed-image.sh`，顺序执行以下校验：`docker image inspect ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1`、`docker run --rm ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1 sh -lc 'sshd -T >/dev/null && command -v claude && getent passwd workspace && test -d /workspace'`，并检查 `deploy/docker/managed-user/sshd_config` 中仍为 `Port 22`。脚本只验证镜像内容与基础 SSH 能力，不尝试建立真实 SSH 会话，也不验证 Phase 2 的出口 IP/隧道逻辑。</action>
  <verify>rg -n "docker build -f deploy/docker/managed-user/Dockerfile -t ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1 \\.|docker image inspect ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1|sshd -T >/dev/null|command -v claude|getent passwd workspace|test -d /workspace|Port 22" deploy/docker/managed-user/build-managed-image.sh scripts/verify-managed-image.sh</verify>
  <acceptance_criteria>
    - `deploy/docker/managed-user/build-managed-image.sh` 包含 `ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1`
    - `scripts/verify-managed-image.sh` 包含 `command -v claude`
    - `scripts/verify-managed-image.sh` 包含 `getent passwd workspace` 与 `test -d /workspace`
    - `scripts/verify-managed-image.sh` 与 `deploy/docker/managed-user/image.lock` 使用同一个镜像全名
    - `scripts/verify-managed-image.sh` 不包含 `ssh user@`、`wireguard`、`wg-quick`
  </acceptance_criteria>
  <done>执行者可以独立构建并验证受管镜像内容，确保 Phase 1 的镜像模板具备可重复构建和基础验收能力。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "FROM ubuntu:24.04|@anthropic-ai/claude-code|Port 22|sshd -D -e" deploy/docker/managed-user/Dockerfile deploy/docker/managed-user/sshd_config deploy/docker/managed-user/entrypoint.sh`
- [ ] `rg -n "image_name: ghcr.io/zaneliu/cloud-cli-proxy/managed-user:v0.1.0-phase1|pull_policy: never-implicit-latest|home_mount: /workspace|rebuild_mode_default: preserve-home" deploy/docker/managed-user/image.lock`
- [ ] `rg -n "command -v claude|getent passwd workspace|test -d /workspace" scripts/verify-managed-image.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 受管镜像、镜像锁和冒烟脚本都已落地，能够支撑后续主机生命周期实现
- 本计划没有提前实现 Phase 2 隧道或 Phase 3 SSH 接入体验
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-control-plane-host-agent/01-02-SUMMARY.md`
</output>
