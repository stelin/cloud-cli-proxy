---
phase: 01-foundation-control-plane-host-agent
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - cmd/host-agent/main.go
  - internal/agent/server.go
  - internal/agentapi/contracts.go
  - internal/agentapi/client.go
  - internal/runtime/tasks/dispatcher.go
  - internal/runtime/tasks/worker.go
  - internal/runtime/runtime_service.go
  - internal/controlplane/http/router.go
  - internal/controlplane/http/hosts.go
  - internal/controlplane/http/tasks.go
  - internal/store/repository/queries.go
  - internal/network/provider.go
  - deploy/systemd/cloud-cli-proxy-control-plane.service
  - deploy/systemd/cloud-cli-proxy-host-agent.service
  - deploy/scripts/host-preflight.sh
autonomous: true
requirements:
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - "控制面通过私有本机接口驱动主机代理执行容器生命周期任务，而不是直接持有宿主机特权。"
    - "主机代理已经具备 Phase 1 所需的 create/start/stop/rebuild 任务执行骨架，并为 Phase 2 预留网络 provider 接口。"
  artifacts:
    - path: "cmd/host-agent/main.go"
      provides: "宿主机代理启动入口"
      min_lines: 30
    - path: "internal/agentapi/contracts.go"
      provides: "控制面与宿主机代理的私有契约"
      contains: "HostActionRequest"
    - path: "deploy/systemd/cloud-cli-proxy-host-agent.service"
      provides: "宿主机代理的 systemd 边界"
      contains: "AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_ADMIN"
    - path: "internal/network/provider.go"
      provides: "Phase 2 网络 provider 预留接口"
      contains: "type Provider interface"
  key_links:
    - from: "internal/runtime/tasks/dispatcher.go"
      to: "internal/agentapi/client.go"
      via: "私有 API 调用"
      pattern: "RunHostAction"
    - from: "internal/agent/server.go"
      to: "internal/runtime/tasks/worker.go"
      via: "任务执行回写"
      pattern: "UpdateTaskStatus"
---

<objective>
引入宿主机代理与运行时特权边界，把控制面的生命周期请求真正落到受控的本机代理上。

Purpose: 完成 Phase 1 最关键的职责切分，让控制面负责状态与任务，主机代理负责 Docker/宿主机动作，并为 Phase 2 网络层留出明确扩展点。
Output: `host-agent` 启动入口、私有 API 契约与客户端、生命周期任务调度骨架、宿主机预检脚本，以及控制面/代理的 systemd 单元文件。
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md
@.planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义宿主机代理私有 API 与任务执行骨架</name>
  <files>cmd/host-agent/main.go, internal/agent/server.go, internal/agentapi/contracts.go, internal/runtime/tasks/worker.go</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md, .planning/phases/01-foundation-control-plane-host-agent/01-01-PLAN.md, .planning/phases/01-foundation-control-plane-host-agent/01-02-PLAN.md, internal/controlplane/http/router.go, deploy/docker/managed-user/image.lock, internal/store/repository/queries.go</read_first>
  <action>新增 `cmd/host-agent/main.go` 与 `internal/agent/server.go`，让宿主机代理通过 Unix socket `/run/cloud-cli-proxy/host-agent.sock` 提供只限本机访问的私有 API；在 `internal/agentapi/contracts.go` 定义 `HostActionRequest`、`HostActionResponse`、`TaskStatusUpdate`，动作枚举固定为 `create_host`、`start_host`、`stop_host`、`rebuild_host`、`prepare_host`；在 `internal/runtime/tasks/worker.go` 实现任务执行骨架，按 `pending -> running -> succeeded|failed|canceled` 回写状态，并强制写入 `error_code`/`error_message` 与 `last_error_summary`。这里不允许 stub-only 交付：`create_host` 与 `start_host` 必须调用真实 Docker 生命周期入口，至少生成前缀为 `cloudproxy-` 的容器名、写入标签 `cloud-cli-proxy.managed=true` / `cloud-cli-proxy.host_id`，并把宿主目录 `/var/lib/cloud-cli-proxy/hosts/<hostID>/home` 绑定到 `/workspace`；`stop_host` 与 `rebuild_host` 也必须落到真实 `docker stop`/`docker rm` + recreate 路径。禁止让控制面直接导入 Docker SDK。</action>
  <verify>rg -n "/run/cloud-cli-proxy/host-agent.sock|HostActionRequest|create_host|start_host|stop_host|rebuild_host|prepare_host|pending|running|succeeded|failed|canceled|error_code|error_message|last_error_summary|cloudproxy-|cloud-cli-proxy.managed=true|/var/lib/cloud-cli-proxy/hosts/|/workspace" cmd/host-agent/main.go internal/agent/server.go internal/agentapi/contracts.go internal/runtime/tasks/worker.go</verify>
  <acceptance_criteria>
    - `internal/agentapi/contracts.go` 包含 `HostActionRequest` 与 `HostActionResponse`
    - `internal/runtime/tasks/worker.go` 包含 `pending`、`running`、`succeeded`、`failed`、`canceled`
    - `cmd/host-agent/main.go` 或 `internal/agent/server.go` 包含 `/run/cloud-cli-proxy/host-agent.sock`
    - `internal/runtime/tasks/worker.go` 包含真实的 `create_host` / `start_host` 执行分支，以及 `cloudproxy-`、`/var/lib/cloud-cli-proxy/hosts/`、`/workspace`
    - 上述文件不直接修改 HTTP handler，也不在控制面包内导入 Docker SDK
  </acceptance_criteria>
  <done>控制面与宿主机代理之间的私有接口和异步任务状态机已经落地，Phase 1 的特权边界变成真实代码结构。</done>
</task>

<task type="auto">
  <name>Task 2: 连接控制面生命周期端点、任务调度、任务列表与镜像锁</name>
  <files>internal/agentapi/client.go, internal/runtime/tasks/dispatcher.go, internal/runtime/runtime_service.go, internal/controlplane/http/router.go, internal/controlplane/http/hosts.go, internal/controlplane/http/tasks.go, internal/store/repository/queries.go</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/phases/01-foundation-control-plane-host-agent/01-01-PLAN.md, .planning/phases/01-foundation-control-plane-host-agent/01-02-PLAN.md, internal/controlplane/http/router.go, deploy/docker/managed-user/image.lock, internal/store/repository/queries.go</read_first>
  <action>新增 `internal/agentapi/client.go` 与 `internal/runtime/tasks/dispatcher.go`，把控制面侧生命周期流程固定为“HTTP handler 创建任务 -> runtime service 读取 `deploy/docker/managed-user/image.lock` -> 通过 Unix socket 调用 host-agent”；在 `internal/controlplane/http/hosts.go` 暴露 `POST /v1/hosts/{hostID}/create`、`/start`、`/stop`、`/rebuild` 四个端点，并返回 `202 Accepted` + `task_id`；在 `internal/runtime/runtime_service.go` 中把 `image_name`、`default_user`、`home_mount`、`rebuild_mode_default` 从 `image.lock` 解析为运行时规格，要求 `create_host` / `start_host` 真正消费同一个镜像全名，`rebuild` 默认走 `preserve-home`。同时新增 `internal/controlplane/http/tasks.go`，把 `GET /v1/tasks` 从占位路由升级为真实查询，调用 `ListTasksWithLastErrorSummary` 返回 `task_id`、`kind`、`status`、`last_error_summary`、`updated_at`。不要在这里实现真实 SSH ready、隧道路由或登录体验；只为后续阶段留下 `prepare_host` 调用点。</action>
  <verify>rg -n "POST /v1/hosts/\\{hostID\\}/create|POST /v1/hosts/\\{hostID\\}/start|POST /v1/hosts/\\{hostID\\}/stop|POST /v1/hosts/\\{hostID\\}/rebuild|202 Accepted|task_id|image.lock|image_name|preserve-home|RunHostAction|prepare_host|GET /v1/tasks|last_error_summary|ListTasksWithLastErrorSummary" internal/agentapi/client.go internal/runtime/tasks/dispatcher.go internal/runtime/runtime_service.go internal/controlplane/http/router.go internal/controlplane/http/hosts.go internal/controlplane/http/tasks.go internal/store/repository/queries.go</verify>
  <acceptance_criteria>
    - `internal/controlplane/http/hosts.go` 包含四个生命周期端点：`create`、`start`、`stop`、`rebuild`
    - `internal/runtime/runtime_service.go` 包含 `image.lock`、`image_name` 与 `preserve-home`
    - `internal/agentapi/client.go` 或 `internal/runtime/tasks/dispatcher.go` 包含 `RunHostAction`
    - `internal/controlplane/http/tasks.go` 包含 `GET /v1/tasks` 的真实查询处理，并返回 `last_error_summary`
    - 以上文件不包含 `ssh user@`、`wg-quick`、`docker exec -it`
  </acceptance_criteria>
  <done>控制面已经能把生命周期请求转换成任务和私有 API 调用，真正依赖受管镜像锁而不是散落的硬编码参数。</done>
</task>

<task type="auto">
  <name>Task 3: 交付宿主机预检与 systemd 特权边界</name>
  <files>internal/network/provider.go, deploy/systemd/cloud-cli-proxy-control-plane.service, deploy/systemd/cloud-cli-proxy-host-agent.service, deploy/scripts/host-preflight.sh</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-control-plane-host-agent/01-CONTEXT.md, .planning/phases/01-foundation-control-plane-host-agent/01-RESEARCH.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>新增 `internal/network/provider.go`，定义 `type Provider interface` 与默认实现 `NoopProvider`，只暴露 `PrepareHost` / `CleanupHost` 占位方法并写清 `TODO(phase-2)`；新增 `deploy/scripts/host-preflight.sh`，检查 `docker`、`ip`、`nft` 或 `iptables`、`systemctl`、`/var/lib/cloud-cli-proxy`、`/run/cloud-cli-proxy` 是否可用；新增两个 systemd unit：`cloud-cli-proxy-control-plane.service` 以 `User=cloudproxy` 运行、`WorkingDirectory=/opt/cloud-cli-proxy`、不授予额外 capabilities；`cloud-cli-proxy-host-agent.service` 同样以 `cloudproxy` 运行，显式声明 `WorkingDirectory=/opt/cloud-cli-proxy`、`AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_ADMIN`、`RuntimeDirectory=cloud-cli-proxy`、`ExecStartPre=/usr/bin/env bash /opt/cloud-cli-proxy/deploy/scripts/host-preflight.sh`。验证时必须执行 `systemd-analyze verify deploy/systemd/cloud-cli-proxy-control-plane.service deploy/systemd/cloud-cli-proxy-host-agent.service`。不要在此计划中把 WireGuard、DNS 接管或默认拒绝规则真正实现出来。</action>
  <verify>rg -n "type Provider interface|NoopProvider|TODO\\(phase-2\\)|docker|ip|nft|iptables|systemctl|/var/lib/cloud-cli-proxy|/run/cloud-cli-proxy|User=cloudproxy|WorkingDirectory=/opt/cloud-cli-proxy|AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_ADMIN|ExecStartPre=/usr/bin/env bash /opt/cloud-cli-proxy/deploy/scripts/host-preflight.sh" internal/network/provider.go deploy/systemd/cloud-cli-proxy-control-plane.service deploy/systemd/cloud-cli-proxy-host-agent.service deploy/scripts/host-preflight.sh && systemd-analyze verify deploy/systemd/cloud-cli-proxy-control-plane.service deploy/systemd/cloud-cli-proxy-host-agent.service</verify>
  <acceptance_criteria>
    - `internal/network/provider.go` 包含 `type Provider interface` 与 `NoopProvider`
    - `deploy/systemd/cloud-cli-proxy-control-plane.service` 包含 `User=cloudproxy` 与 `WorkingDirectory=/opt/cloud-cli-proxy`
    - `deploy/systemd/cloud-cli-proxy-host-agent.service` 包含 `AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_ADMIN`
    - `deploy/systemd/cloud-cli-proxy-host-agent.service` 包含绝对路径的 `ExecStartPre=/usr/bin/env bash /opt/cloud-cli-proxy/deploy/scripts/host-preflight.sh`
    - `deploy/scripts/host-preflight.sh` 同时检查 `docker`、`ip`、`systemctl`
  </acceptance_criteria>
  <done>宿主机运行边界、启动前检查与 Phase 2 的网络扩展点都已就位，同时没有把后续阶段的隧道实现提前塞进本阶段。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "HostActionRequest|/run/cloud-cli-proxy/host-agent.sock|pending|running|succeeded|failed|canceled|cloudproxy-|/var/lib/cloud-cli-proxy/hosts/|/workspace" cmd/host-agent/main.go internal/agentapi/contracts.go internal/runtime/tasks/worker.go`
- [ ] `rg -n "POST /v1/hosts/\\{hostID\\}/create|task_id|image.lock|image_name|preserve-home|GET /v1/tasks|last_error_summary|ListTasksWithLastErrorSummary" internal/controlplane/http/hosts.go internal/controlplane/http/tasks.go internal/runtime/runtime_service.go internal/store/repository/queries.go`
- [ ] `rg -n "type Provider interface|NoopProvider|WorkingDirectory=/opt/cloud-cli-proxy|AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_ADMIN|ExecStartPre=/usr/bin/env bash /opt/cloud-cli-proxy/deploy/scripts/host-preflight.sh" internal/network/provider.go deploy/systemd/cloud-cli-proxy-control-plane.service deploy/systemd/cloud-cli-proxy-host-agent.service deploy/scripts/host-preflight.sh && systemd-analyze verify deploy/systemd/cloud-cli-proxy-control-plane.service deploy/systemd/cloud-cli-proxy-host-agent.service`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 控制面与宿主机代理的特权边界已经以私有 API、任务流与 systemd 边界方式落地
- 本计划只预留 Phase 2 网络 provider 接口，不提前实现隧道与 SSH 接入体验
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-control-plane-host-agent/01-03-SUMMARY.md`
</output>
