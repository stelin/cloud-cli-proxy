---
phase: 03-ssh
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - internal/network/ssh_access.go
  - internal/runtime/tasks/ssh_handoff.go
  - internal/runtime/tasks/ssh_handoff_test.go
  - internal/runtime/tasks/worker.go
  - internal/controlplane/http/bootstrap_errors.go
  - internal/controlplane/http/bootstrap_handoff.go
  - internal/controlplane/http/bootstrap_handoff_test.go
  - internal/controlplane/http/router.go
  - deploy/bootstrap/cloud-bootstrap.sh
  - test/bootstrap/e2e_bootstrap_ssh.sh
autonomous: true
requirements:
  - ACCS-03
  - ACCS-04
must_haves:
  truths:
    - "当任务成功且 SSH 已就绪后，终端脚本会自动切换到可用 SSH 会话，无需用户手工找 host/port。"
    - "启动失败相关场景会输出稳定中文提示和确定退出码，便于脚本化调用。"
    - "失败后只给显式重试建议，不进行自动重试或隐藏恢复。"
  artifacts:
    - path: "internal/controlplane/http/bootstrap_handoff.go"
      provides: "GET /v1/bootstrap/tasks/{taskID}/handoff 接口"
      exports: ["NewBootstrapHandoffHandler"]
    - path: "internal/controlplane/http/bootstrap_errors.go"
      provides: "error_code -> 中文提示 -> 退出码 稳定映射"
    - path: "deploy/bootstrap/cloud-bootstrap.sh"
      provides: "完整终端流：轮询状态 -> 拉取 handoff -> exec ssh"
    - path: "internal/runtime/tasks/ssh_handoff.go"
      provides: "host-agent 侧 handoff metadata 生成与事件记录"
      exports: ["BuildSSHHandoffMetadata"]
  key_links:
    - from: "internal/runtime/tasks/worker.go"
      to: "internal/runtime/tasks/ssh_handoff.go"
      via: "ssh.ready 后写入 ssh.handoff.ready 事件元数据"
      pattern: "ssh\\.handoff\\.ready"
    - from: "internal/controlplane/http/bootstrap_handoff.go"
      to: "internal/controlplane/http/bootstrap_status.go"
      via: "仅对 succeeded + ssh.ready 的任务返回 handoff"
      pattern: "succeeded|ssh.ready"
    - from: "deploy/bootstrap/cloud-bootstrap.sh"
      to: "internal/controlplane/http/bootstrap_errors.go"
      via: "按 error_code 映射退出码并打印中文重试提示"
      pattern: "error_code|exit"
---

<objective>
完成 Phase 3 最后一段：SSH handoff、显式重试机制和失败体验收口，交付从 curl 到自动进入 SSH 的闭环。

Purpose: 满足 ACCS-03 的“自动接入可用 SSH 会话”和 ACCS-04 的“清晰失败提示”，并把 D-08/D-09/D-11/D-13 固化到接口与脚本行为。
Output: handoff API、失败码映射、脚本最终流（poll + handoff + exec ssh）、host-agent handoff 元数据事件、e2e 脚本测试。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ssh/03-CONTEXT.md
@.planning/phases/03-ssh/03-RESEARCH.md
@.planning/phases/03-ssh/03-VALIDATION.md
@.planning/phases/03-ssh/03-02-SUMMARY.md
@.planning/phases/02-tunnel-egress-enforcement/02-03-SUMMARY.md

<interfaces>
From internal/store/repository/models.go:
```go
type Event struct {
    TaskID   *string
    HostID   *string
    Type     string
    Message  string
    Metadata map[string]any
}
```

From internal/runtime/tasks/worker.go (after 03-02):
```go
// startHost / rebuildHost will already emit:
// - runtime.validating
// - ssh.ready
```

From .planning/phases/03-ssh/03-CONTEXT.md:
```text
D-08: 控制面返回标准 SSH handoff 载荷，bootstrap 脚本直接 exec ssh
D-09: v1 保持 SSH-only，不提供 Web Terminal
D-11: 失败不自动重试
D-13: 失败类型返回确定非零退出码
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 host-agent 产出可消费的 SSH handoff 元数据（覆盖 D-08、D-12）</name>
  <files>internal/network/ssh_access.go, internal/runtime/tasks/ssh_handoff.go, internal/runtime/tasks/ssh_handoff_test.go, internal/runtime/tasks/worker.go</files>
  <read_first>
    - internal/network/namespace.go（复用管理网段/地址推导逻辑，避免重复实现）
    - internal/runtime/tasks/worker.go（ssh.ready 事件写入位置）
    - .planning/phases/03-ssh/03-CONTEXT.md（锁定 D-08、D-12）
    - .planning/phases/02-tunnel-egress-enforcement/02-03-SUMMARY.md（确认现有 net.ready 与 ssh.ready 链路）
  </read_first>
  <action>
    1) 新建 `internal/network/ssh_access.go`，提供可复用 helper（如 `DeriveManagementSSHAccess(hostID string)`），输出 host 侧管理 IP 与固定端口 22，避免在多处复制地址推导算法。
    2) 新建 `internal/runtime/tasks/ssh_handoff.go`：
       - 实现 `BuildSSHHandoffMetadata(hostID, defaultUser string) map[string]any`。
       - 元数据至少包含 `ssh_host`、`ssh_port`、`ssh_user`、`host_id`。
    3) 修改 `worker.go`：
       - 在 `ssh.ready` 成功后追加 `ssh.handoff.ready` 事件，写入上述 metadata（per D-08, D-12）。
       - 失败路径保持显式失败，不新增自动重试（per D-11）。
    4) 新建 `ssh_handoff_test.go`：
       - 覆盖 metadata 生成稳定性（同 hostID 重复调用结果一致）。
       - 覆盖 worker 在 ssh.ready 后会写入 `ssh.handoff.ready` 事件。
  </action>
  <verify>
    <automated>go test ./internal/runtime/tasks -run TestBuildSSHHandoffMetadata -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/network/ssh_access.go` 存在，且提供 hostID->SSH 接入参数推导 helper。
    - `worker.go` 在 `ssh.ready` 成功后写入 `ssh.handoff.ready` 事件。
    - `ssh.handoff.ready` metadata 至少包含 `ssh_host/ssh_port/ssh_user`。
    - 失败路径不引入自动重试逻辑（per D-11）。
    - `go test ./internal/runtime/tasks -run TestBuildSSHHandoffMetadata -count=1` 通过。
  </acceptance_criteria>
  <done>控制面可从事件中拿到标准 handoff 元数据，具备自动 ssh 接入所需的 machine-readable 参数。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 handoff API、失败码映射与脚本最终接入流（覆盖 D-08、D-09、D-10、D-11、D-13）</name>
  <files>internal/controlplane/http/bootstrap_errors.go, internal/controlplane/http/bootstrap_handoff.go, internal/controlplane/http/bootstrap_handoff_test.go, internal/controlplane/http/router.go, deploy/bootstrap/cloud-bootstrap.sh, test/bootstrap/e2e_bootstrap_ssh.sh</files>
  <read_first>
    - internal/controlplane/http/bootstrap_status.go（复用 03-02 的状态接口契约）
    - internal/controlplane/http/bootstrap_auth.go（复用 03-01 的 session/task 契约）
    - deploy/bootstrap/cloud-bootstrap.sh（在现有脚本基础上扩展，不重写入口）
    - .planning/phases/03-ssh/03-CONTEXT.md（锁定 D-08、D-09、D-10、D-11、D-13）
  </read_first>
  <action>
    1) 新建 `bootstrap_errors.go`，定义稳定映射：
       - `error_code -> 中文提示 -> 非零退出码`（覆盖凭证错误、账号过期、绑定缺失、启动失败、ssh_not_ready）。
       - 该映射由 API 和脚本共同复用，避免文案/退出码漂移（per D-10, D-13）。
    2) 新建 `bootstrap_handoff.go`，实现 `GET /v1/bootstrap/tasks/{taskID}/handoff`：
       - 仅当任务 `succeeded` 且存在 `ssh.handoff.ready` 事件时返回 handoff。
       - 返回标准载荷：`host`、`port`、`user`、`ssh_options`、`ready=true`（per D-08）。
       - 若未就绪或失败，返回结构化错误码与中文提示，不返回 Web Terminal 替代路径（per D-09）。
    3) 在 `router.go` 注册 handoff 路由。
    4) 更新 `deploy/bootstrap/cloud-bootstrap.sh`：
       - 轮询 `/v1/bootstrap/tasks/{taskID}`，展示中文阶段提示（承接 03-02）。
       - 成功后调用 handoff API，并 `exec ssh` 完成会话接管（per D-08）。
       - 失败时按 `bootstrap_errors.go` 映射输出中文与非零退出码。
       - 只输出显式“请重试命令 …”建议，不自动重试（per D-11）。
       - 仅使用 SSH 流程，不增加 Web Terminal 分支（per D-09）。
    5) 新建 `test/bootstrap/e2e_bootstrap_ssh.sh`：
       - 用 mock 响应验证：成功路径会执行 `ssh` 命令；失败路径返回预期退出码与中文提示。
       - 验证脚本不会在失败后自动重试。
  </action>
  <verify>
    <automated>go test ./internal/controlplane/http -run 'TestBootstrapHandoffHandler|TestBootstrapErrorMapping' -count=1 && bash -n deploy/bootstrap/cloud-bootstrap.sh && bash test/bootstrap/e2e_bootstrap_ssh.sh</automated>
  </verify>
  <acceptance_criteria>
    - `internal/controlplane/http/bootstrap_handoff.go` 存在并提供 handoff 路由。
    - handoff 响应包含 `host/port/user`，且仅在任务就绪时返回（ACCS-03）。
    - `internal/controlplane/http/bootstrap_errors.go` 存在，定义稳定 `error_code -> 退出码` 映射（ACCS-04, D-13）。
    - `deploy/bootstrap/cloud-bootstrap.sh` 成功路径使用 `exec ssh`，失败路径输出中文可执行建议。
    - 脚本不包含自动重试循环（per D-11）。
    - 脚本不包含 Web Terminal fallback（per D-09）。
    - `go test ...`、`bash -n ...`、`bash test/bootstrap/e2e_bootstrap_ssh.sh` 全部通过。
  </acceptance_criteria>
  <done>从 curl 到自动 ssh 接入闭环完成：就绪即自动进入会话，失败即返回可脚本化处理的稳定中文错误体验。</done>
</task>

</tasks>

<verification>
- `go test ./internal/controlplane/http -run 'TestBootstrapHandoffHandler|TestBootstrapErrorMapping' -count=1` 通过。
- `go test ./internal/runtime/tasks -run TestBuildSSHHandoffMetadata -count=1` 通过。
- `bash -n deploy/bootstrap/cloud-bootstrap.sh` 与 `bash test/bootstrap/e2e_bootstrap_ssh.sh` 通过。
</verification>

<success_criteria>
1. 用户无需手工查找主机端口，脚本可自动完成 SSH 接管（ACCS-03）。
2. 失败场景具备稳定错误码、中文提示和确定退出码（ACCS-04）。
3. 重试策略为显式人工触发，不存在隐式自动重试（ACCS-04, D-11）。
</success_criteria>

<output>
After completion, create `.planning/phases/03-ssh/03-03-SUMMARY.md`
</output>
