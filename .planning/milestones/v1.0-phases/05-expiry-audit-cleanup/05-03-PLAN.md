---
phase: 05-expiry-audit-cleanup
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - web/admin/src/hooks/use-users.ts
  - web/admin/src/hooks/use-events.ts
  - web/admin/src/routes/_dashboard/users/index.tsx
  - web/admin/src/routes/_dashboard/users/$userId.tsx
  - web/admin/src/routes/_dashboard/events/index.tsx
  - web/admin/src/components/layout/sidebar.tsx
  - web/admin/src/routes/_dashboard/index.tsx
autonomous: true
requirements: [LIFE-04, LIFE-05, ADMN-04]

must_haves:
  truths:
    - "用户列表页展示到期时间列和 expired 状态 Badge"
    - "用户详情页展示到期时间并提供设置/修改/清除到期时间的操作"
    - "用户详情页允许将已过期用户重新激活为 active"
    - "侧栏包含事件日志导航入口"
    - "事件日志页面展示全局事件时间线并支持筛选"
    - "仪表板概览包含最近事件摘要卡片"
  artifacts:
    - path: "web/admin/src/hooks/use-events.ts"
      provides: "事件查询 hook"
      exports: ["useEvents", "EventItem"]
    - path: "web/admin/src/routes/_dashboard/events/index.tsx"
      provides: "事件日志页面"
    - path: "web/admin/src/components/layout/sidebar.tsx"
      provides: "包含事件日志入口的侧栏"
  key_links:
    - from: "web/admin/src/hooks/use-events.ts"
      to: "GET /v1/admin/events"
      via: "apiFetch"
      pattern: "apiFetch.*events"
    - from: "web/admin/src/routes/_dashboard/events/index.tsx"
      to: "web/admin/src/hooks/use-events.ts"
      via: "useEvents hook"
      pattern: "useEvents"
    - from: "web/admin/src/routes/_dashboard/users/$userId.tsx"
      to: "PUT /v1/admin/users/{userID}/expiry"
      via: "useUpdateUserExpiry mutation"
      pattern: "useUpdateUserExpiry"
---

<objective>
在后台管理界面中展示到期时间和事件日志，完成 Phase 5 的用户可见功能。

Purpose: 本计划完成 Phase 5 的前端交付——用户列表/详情页展示和管理到期时间、事件日志页面、仪表板事件摘要卡片。
Output: 用户 expiry UI 扩展、事件日志页面、侧栏更新、仪表板事件卡片。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-expiry-audit-cleanup/05-CONTEXT.md
@.planning/phases/05-expiry-audit-cleanup/05-01-SUMMARY.md
@.planning/phases/05-expiry-audit-cleanup/05-02-SUMMARY.md

<interfaces>
From web/admin/src/hooks/use-users.ts:
```typescript
export interface User {
  id: string;
  username: string;
  status: string;
  created_at: string;
  updated_at: string;
}

export function useUsers()
export function useUser(userId: string)
export function useUpdateUserStatus()
```

From web/admin/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
```

From web/admin/src/hooks/use-tasks.ts (pattern reference):
```typescript
export function useTasks() {
  return useQuery({
    queryKey: ["tasks"],
    queryFn: () => apiFetch<{ tasks: Task[] }>("/tasks"),
    refetchInterval: 5000,
  });
}
```

Backend API contracts (from Plan 01 & 02):
- GET /v1/admin/users → { users: User[] }  // User now has expires_at
- GET /v1/admin/users/{id} → { user: User, hosts: Host[] }
- PATCH /v1/admin/users/{id} → { user: User }  // status: active/disabled
- PUT /v1/admin/users/{id}/expiry → { user: User }  // body: { expires_at: string|null }
- GET /v1/admin/events?type=&user_id=&host_id=&since=&until=&limit=&offset= → { events: Event[], total: number, limit: number, offset: number }

Sidebar nav items (current):
```typescript
const navItems = [
  { label: "仪表板", to: "/", icon: LayoutDashboard },
  { label: "用户管理", to: "/users", icon: Users },
  { label: "出口 IP", to: "/egress-ips", icon: Globe },
  { label: "主机管理", to: "/hosts", icon: Server },
  { label: "任务列表", to: "/tasks", icon: ListTodo },
] as const;
```

Dashboard stats API: GET /v1/admin/dashboard/stats → { active_users, running_hosts, available_ips }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用户到期时间展示与管理</name>
  <files>
    web/admin/src/hooks/use-users.ts
    web/admin/src/routes/_dashboard/users/index.tsx
    web/admin/src/routes/_dashboard/users/$userId.tsx
  </files>
  <read_first>
    web/admin/src/hooks/use-users.ts
    web/admin/src/routes/_dashboard/users/index.tsx
    web/admin/src/routes/_dashboard/users/$userId.tsx
    web/admin/src/lib/api.ts
    web/admin/src/components/layout/sidebar.tsx
  </read_first>
  <action>
**1. 修改 `web/admin/src/hooks/use-users.ts`：**

**1a.** User interface 增加 `expires_at` 字段：
```typescript
export interface User {
  id: string;
  username: string;
  status: string;
  expires_at: string | null;
  created_at: string;
  updated_at: string;
}
```

**1b.** 新增 `useUpdateUserExpiry` mutation hook：
```typescript
export function useUpdateUserExpiry() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ userId, expiresAt }: { userId: string; expiresAt: string | null }) =>
      apiFetch<{ user: User }>(`/users/${userId}/expiry`, {
        method: "PUT",
        body: JSON.stringify({ expires_at: expiresAt }),
      }),
    onSuccess: (_data, variables) => {
      qc.invalidateQueries({ queryKey: ["users"] });
      qc.invalidateQueries({ queryKey: ["users", variables.userId] });
    },
  });
}
```

**2. 修改 `web/admin/src/routes/_dashboard/users/index.tsx`：**

**2a.** 在表头中增加「到期时间」列（在「状态」列之后）：
```tsx
<TableHead>到期时间</TableHead>
```

**2b.** 在 TableBody 中每行对应位置增加到期时间单元格：
```tsx
<TableCell className="text-muted-foreground">
  {user.expires_at ? formatDate(user.expires_at) : "永不过期"}
</TableCell>
```

**2c.** 修改状态 Badge 的显示逻辑，支持 `expired` 状态：
将现有的二元判断：
```tsx
<Badge variant={user.status === "active" ? "default" : "secondary"}>
  {user.status === "active" ? "活跃" : "已禁用"}
</Badge>
```
改为三态判断：
```tsx
<Badge variant={
  user.status === "active" ? "default" :
  user.status === "expired" ? "destructive" : "secondary"
}>
  {user.status === "active" ? "活跃" :
   user.status === "expired" ? "已过期" : "已禁用"}
</Badge>
```

**2d.** 修改 `handleToggleStatus` 函数，支持 expired 用户的重新激活：
当前逻辑是 `active ↔ disabled` 切换。改为：
- `active` → `disabled`
- `disabled` → `active`
- `expired` → `active`（重新激活）

```typescript
function handleToggleStatus(user: User) {
  const newStatus = user.status === "active" ? "disabled" : "active";
  // ...
}
```

**2e.** 在 DropdownMenu 中，expired 用户的操作按钮显示「重新激活」（使用 `CheckCircle` 图标），与 disabled 用户的「启用用户」视觉一致。

**2f.** 表格的 colSpan（空状态行）和 skeleton 行的列数需要从 4 更新为 5（加了到期时间列）。

**3. 修改 `web/admin/src/routes/_dashboard/users/$userId.tsx`：**

**3a.** 修改状态 Badge 的显示逻辑，支持 `expired` 状态（同 index.tsx 的三态判断）。

**3b.** 在「用户信息」Card 中增加到期时间行（在「更新时间」之后）：
```tsx
<Separator />
<div className="flex justify-between text-sm">
  <span className="text-muted-foreground">到期时间</span>
  <span>{user.expires_at ? formatDate(user.expires_at) : "永不过期"}</span>
</div>
```

**3c.** 在「操作」Card 中增加「设置到期时间」操作按钮（使用 `Calendar` 图标，从 lucide-react 导入）：
```tsx
<Button
  variant="outline"
  className="w-full justify-start"
  onClick={() => setExpiryOpen(true)}
>
  <Calendar className="mr-2 h-4 w-4" />
  设置到期时间
</Button>
```

**3d.** 修改 `handleToggleStatus` 函数，与 index.tsx 保持一致，支持 expired → active 重新激活。按钮文案：expired 状态时显示「重新激活」（`CheckCircle` 图标）。

**3e.** 新增到期时间设置 Dialog（内联或独立组件均可）：
- 包含一个 `<input type="datetime-local">` 输入框用于选择到期时间
- 包含一个「清除到期时间」按钮（设为永不过期）
- 包含「确认」按钮，调用 `useUpdateUserExpiry` mutation
- 成功后 toast 提示 `"到期时间已更新"` 或 `"已设为永不过期"`

到期时间 Dialog 的交互流程：
1. 打开时，如果用户已有 expires_at，输入框预填充当前值
2. 用户选择新时间 → 点确认 → 调用 PUT /v1/admin/users/{id}/expiry，body: `{"expires_at": "ISO8601"}`
3. 用户点「清除到期时间」→ 调用 PUT /v1/admin/users/{id}/expiry，body: `{"expires_at": null}`
4. 成功后关闭 Dialog 并刷新用户数据
  </action>
  <verify>
    <automated>cd web/admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - use-users.ts 中 User interface 包含 `expires_at: string | null`
    - use-users.ts 中存在 `useUpdateUserExpiry` hook，调用 `PUT /users/${userId}/expiry`
    - users/index.tsx 的表头包含「到期时间」列
    - users/index.tsx 的 Badge 组件支持 `expired` 状态，显示「已过期」且 variant 为 `destructive`
    - users/index.tsx 中 expired 用户的操作菜单包含「重新激活」选项
    - users/$userId.tsx 的用户信息卡片包含到期时间行
    - users/$userId.tsx 中存在设置到期时间的 Dialog 或交互组件
    - users/$userId.tsx 中 expired 用户的状态按钮显示「重新激活」
    - `npx tsc --noEmit` 退出码 0
  </acceptance_criteria>
  <done>用户列表展示到期时间和过期状态，用户详情页支持设置/修改/清除到期时间，过期用户可通过 UI 重新激活</done>
</task>

<task type="auto">
  <name>Task 2: 事件日志页面与仪表板事件摘要</name>
  <files>
    web/admin/src/hooks/use-events.ts
    web/admin/src/routes/_dashboard/events/index.tsx
    web/admin/src/components/layout/sidebar.tsx
    web/admin/src/routes/_dashboard/index.tsx
  </files>
  <read_first>
    web/admin/src/hooks/use-tasks.ts
    web/admin/src/routes/_dashboard/tasks/index.tsx
    web/admin/src/components/layout/sidebar.tsx
    web/admin/src/routes/_dashboard/index.tsx
    web/admin/src/lib/api.ts
  </read_first>
  <action>
**1. 创建 `web/admin/src/hooks/use-events.ts`（per D-11, D-12）：**

```typescript
import { useQuery } from "@tanstack/react-query";
import { apiFetch } from "@/lib/api";

export interface EventItem {
  id: string;
  type: string;
  level: string;
  message: string;
  user_id: string | null;
  host_id: string | null;
  task_id: string | null;
  metadata: Record<string, unknown>;
  created_at: string;
}

export interface EventsResponse {
  events: EventItem[];
  total: number;
  limit: number;
  offset: number;
}

export interface EventsParams {
  type?: string;
  userId?: string;
  hostId?: string;
  limit?: number;
  offset?: number;
}

export function useEvents(params: EventsParams = {}) {
  const searchParams = new URLSearchParams();
  if (params.type) searchParams.set("type", params.type);
  if (params.userId) searchParams.set("user_id", params.userId);
  if (params.hostId) searchParams.set("host_id", params.hostId);
  if (params.limit) searchParams.set("limit", String(params.limit));
  if (params.offset) searchParams.set("offset", String(params.offset));

  const qs = searchParams.toString();
  return useQuery({
    queryKey: ["events", params],
    queryFn: () => apiFetch<EventsResponse>(`/events${qs ? `?${qs}` : ""}`),
    refetchInterval: 15000,
  });
}
```

**2. 创建 `web/admin/src/routes/_dashboard/events/index.tsx`（per D-11, D-12, D-13）：**

页面结构参考现有 `tasks/index.tsx` 的布局模式，但增加筛选和详情展示。

**2a.** 文件路由声明：
```typescript
import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/_dashboard/events/")({ component: EventsPage });
```

**2b.** 页面布局：
- 标题：「事件日志」
- 副标题：「每 15 秒自动刷新」
- 筛选栏：包含事件类型下拉选择器和重置按钮
- 事件表格

**2c.** 筛选栏实现：
使用 React state 管理筛选参数：
```typescript
const [typeFilter, setTypeFilter] = useState<string>("");
```

事件类型下拉选项（按 D-08 定义的事件类型分组）：
```typescript
const eventTypes = [
  { value: "", label: "全部类型" },
  { value: "auth.success", label: "认证成功" },
  { value: "auth.failed", label: "认证失败" },
  { value: "user.expired", label: "用户过期" },
  { value: "host.stop.expired", label: "过期主机停止" },
  { value: "admin.user.created", label: "创建用户" },
  { value: "admin.user.updated", label: "修改用户" },
  { value: "admin.user.deleted", label: "删除用户" },
  { value: "admin.user.password_rotated", label: "轮换密码" },
  { value: "admin.binding.created", label: "创建绑定" },
  { value: "admin.binding.deleted", label: "删除绑定" },
  { value: "admin.host.action", label: "主机操作" },
  { value: "reconcile.host.drift", label: "主机漂移" },
  { value: "reconcile.task.stale", label: "陈旧任务" },
];
```

使用 `<select>` 元素（styled with Tailwind）或 shadcn/ui 的 Select 组件。

**2d.** 事件表格列：
| 列名 | 字段 | 说明 |
|------|------|------|
| 时间 | created_at | formatDate 格式化 |
| 类型 | type | Badge 展示，按 level 着色（info=default, warn=destructive, error=destructive） |
| 消息 | message | 文本 |
| 用户 | user_id | 显示前 8 字符或 "—" |
| 主机 | host_id | 显示前 8 字符或 "—" |
| 详情 | metadata | 可展开的 metadata JSON |

**2e.** 事件详情展示（per D-13）：
使用可展开行或点击展开方式。展示 metadata 中的键值对：
```tsx
{Object.entries(event.metadata).map(([key, value]) => (
  <div key={key} className="flex gap-2 text-sm">
    <span className="font-mono text-muted-foreground">{key}:</span>
    <span>{String(value)}</span>
  </div>
))}
```

**2f.** 分页：
- 使用 offset/limit 分页
- 底部显示 `第 {offset+1}-{offset+events.length} 条，共 {total} 条`
- 上一页/下一页按钮，用 shadcn/ui 的 Button
- 默认每页 50 条

**2g.** 空状态和 loading 状态沿用 tasks/index.tsx 的 skeleton 和 empty 模式。

**3. 修改 `web/admin/src/components/layout/sidebar.tsx`：**

**3a.** 在 import 中增加 `ScrollText` 图标（从 lucide-react）。

**3b.** 在 navItems 数组中，在「任务列表」之后增加事件日志入口：
```typescript
const navItems = [
  { label: "仪表板", to: "/", icon: LayoutDashboard },
  { label: "用户管理", to: "/users", icon: Users },
  { label: "出口 IP", to: "/egress-ips", icon: Globe },
  { label: "主机管理", to: "/hosts", icon: Server },
  { label: "任务列表", to: "/tasks", icon: ListTodo },
  { label: "事件日志", to: "/events", icon: ScrollText },
] as const;
```

**4. 修改 `web/admin/src/routes/_dashboard/index.tsx`（per D-14）：**

**4a.** 在现有 3 个统计卡片之后，增加「最近事件」卡片区域。

**4b.** 使用 `useEvents({ limit: 5 })` hook 获取最近 5 条事件。

**4c.** 卡片布局：
```tsx
<Card className="md:col-span-3">
  <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
    <CardTitle className="text-sm font-medium">最近事件</CardTitle>
    <Link to="/events" className="text-xs text-muted-foreground hover:underline">
      查看全部
    </Link>
  </CardHeader>
  <CardContent>
    {/* 事件列表，每条显示：时间、类型Badge、消息 */}
    {eventsLoading ? (
      <div className="space-y-3">
        {Array.from({ length: 3 }).map((_, i) => (
          <div key={i} className="h-4 animate-pulse rounded bg-muted" />
        ))}
      </div>
    ) : events.length === 0 ? (
      <p className="text-sm text-muted-foreground">暂无事件</p>
    ) : (
      <div className="space-y-3">
        {events.map((event) => (
          <div key={event.id} className="flex items-center gap-3 text-sm">
            <span className="text-xs text-muted-foreground whitespace-nowrap">
              {formatTime(event.created_at)}
            </span>
            <Badge variant={event.level === "info" ? "secondary" : "destructive"} className="text-xs">
              {eventTypeLabel(event.type)}
            </Badge>
            <span className="truncate">{event.message}</span>
          </div>
        ))}
      </div>
    )}
  </CardContent>
</Card>
```

**4d.** 新增 `eventTypeLabel` 辅助函数，将事件 type 映射为中文标签（如 `auth.success` → `认证成功`、`admin.user.created` → `创建用户` 等）。使用与事件日志页面相同的映射表。

**4e.** 新增 `formatTime` 辅助函数，只显示时分（适合仪表板摘要）：
```typescript
function formatTime(dateStr: string) {
  return new Date(dateStr).toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" });
}
```

**4f.** 在文件顶部增加必要的 import：
```typescript
import { Link } from "@tanstack/react-router";
import { useEvents, type EventItem } from "@/hooks/use-events";
import { Badge } from "@/components/ui/badge";
```

**4g.** 仪表板的 grid 布局，统计卡片保持 `md:grid-cols-3`，最近事件卡片占满 3 列宽度（`md:col-span-3`），出现在统计卡片下方。
  </action>
  <verify>
    <automated>cd web/admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - hooks/use-events.ts 存在且导出 `useEvents` hook 和 `EventItem` 类型
    - use-events.ts 中 `useEvents` 调用 `apiFetch<EventsResponse>("/events...")`，refetchInterval 为 15000
    - routes/_dashboard/events/index.tsx 存在且包含 `createFileRoute("/_dashboard/events/")`
    - events/index.tsx 包含事件类型下拉筛选器，选项覆盖 D-08 定义的所有事件类型
    - events/index.tsx 包含分页控件（上一页/下一页按钮和条目计数）
    - events/index.tsx 展示 metadata 内容（per D-13）
    - sidebar.tsx 的 navItems 包含 `{ label: "事件日志", to: "/events", icon: ScrollText }`
    - routes/_dashboard/index.tsx 使用 `useEvents({ limit: 5 })` 展示最近事件
    - routes/_dashboard/index.tsx 包含 `Link to="/events"` 的「查看全部」链接
    - `npx tsc --noEmit` 退出码 0
  </acceptance_criteria>
  <done>事件日志页面支持筛选和分页，侧栏包含事件日志入口，仪表板展示最近事件摘要</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` TypeScript 类型检查通过
2. 侧栏导航包含 6 个条目（仪表板、用户管理、出口 IP、主机管理、任务列表、事件日志）
3. 用户列表的 expired 状态使用 destructive Badge
4. 事件日志页面至少包含一个筛选维度（事件类型）
5. 仪表板最近事件卡片展示最多 5 条事件
</verification>

<success_criteria>
- 用户管理页面完整支持到期时间展示和管理
- 过期用户在列表和详情页都有清晰的状态标识
- 管理员可以通过 UI 设置/修改/清除到期时间
- 管理员可以通过 UI 重新激活过期用户
- 事件日志页面展示全局事件时间线
- 事件日志支持按类型筛选和分页
- 事件详情的 metadata 可查看
- 仪表板包含最近事件摘要卡片
</success_criteria>

<output>
After completion, create `.planning/phases/05-expiry-audit-cleanup/05-03-SUMMARY.md`
</output>
