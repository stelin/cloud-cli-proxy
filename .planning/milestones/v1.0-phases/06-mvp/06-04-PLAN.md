---
phase: 06-mvp
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controlplane/app/app.go
  - cmd/control-plane/main.go
  - cmd/host-agent/main.go
  - internal/controlplane/http/router.go
  - internal/agentapi/client.go
  - internal/agent/server.go
  - internal/store/repository/models.go
  - internal/controlplane/http/admin_egress_ips.go
  - deploy/bootstrap/cloud-bootstrap.sh
  - web/admin/src/components/egress-ips/egress-ip-drawer.tsx
autonomous: true
requirements: [ACCS-01, ADMN-03, ADMN-04]

must_haves:
  truths:
    - "控制面和 host-agent 在 LOG_FORMAT=json 时输出结构化 JSON 日志"
    - "LOG_LEVEL 环境变量可控制日志级别（DEBUG/INFO/WARN/ERROR）"
    - "/healthz 端点返回数据库连接和 agent 可达性的分组状态"
    - "PostgreSQL 连接池有显式 max/idle 配置，控制面关闭时释放连接"
    - "EgressIP 的 WgPresharedKey 不在任何 API 响应中泄露"
    - "bootstrap 脚本对 HTTP 4xx/5xx 错误能解析 error_code 并给出对应退出码和中文提示"
    - "后台出口 IP 表单对 IP 地址做格式校验"
  artifacts:
    - path: "internal/controlplane/app/app.go"
      provides: "可配置日志格式/级别 + 显式连接池配置"
      contains: "LOG_FORMAT"
    - path: "internal/controlplane/http/router.go"
      provides: "增强型 /healthz 含 DB + agent 检查"
      contains: "AgentHealth"
    - path: "deploy/bootstrap/cloud-bootstrap.sh"
      provides: "完善的 HTTP 错误码解析"
      contains: "http_code"
  key_links:
    - from: "app.go newLogger()"
      to: "slog.NewJSONHandler"
      via: "LOG_FORMAT env var"
      pattern: "LOG_FORMAT.*json"
    - from: "router.go /healthz"
      to: "AgentHealth.Ping"
      via: "dependency injection"
      pattern: "AgentHealth"
    - from: "admin_egress_ips.go"
      to: "EgressIP response"
      via: "sanitizeEgressIP"
      pattern: "WgPresharedKey.*nil"
---

<objective>
终端/后台体验打磨与上线前安全、稳定性、可运维检查。

Purpose: 覆盖 D-10~D-15 锁定决策，确保 MVP 满足成功标准 #2（清晰失败反馈）和 #4（不依赖隐藏人工步骤的生产就绪）。
Output: 后端运维加固代码 + 安全修复 + 脚本/UI 体验打磨。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mvp/06-CONTEXT.md

<interfaces>
<!-- 涉及的关键代码锚点。 -->

From internal/controlplane/app/app.go (line 46):
```go
logger := slog.New(slog.NewTextHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo}))
```

From internal/controlplane/app/app.go (line 48):
```go
db, err := pgxpool.New(ctx, cfg.DatabaseURL)
```

From internal/controlplane/http/router.go (line 22-47):
```go
type Dependencies struct {
    Logger          *slog.Logger
    Health          HealthChecker
    // ... 其他字段
}
type HealthChecker interface {
    Health(context.Context) error
}
```

From internal/controlplane/http/router.go (line 76-88):
```go
mux.HandleFunc("GET /healthz", func(w nethttp.ResponseWriter, r *nethttp.Request) {
    if deps.Health != nil {
        if err := deps.Health.Health(r.Context()); err != nil {
            writeJSON(w, nethttp.StatusServiceUnavailable, map[string]any{"status": "error", "error": err.Error()})
            return
        }
    }
    writeJSON(w, nethttp.StatusOK, map[string]string{"status": "ok"})
})
```

From internal/store/repository/models.go (line 68-80, EgressIP):
```go
type EgressIP struct {
    // ... 其他字段
    WgPresharedKey *string   `json:"wg_preshared_key,omitempty"` // ⚠️ 敏感字段，不应出现在 API 响应中
    // ...
}
```

From internal/controlplane/http/admin_egress_ips.go:
```go
// List 返回: writeJSON(w, 200, map[string]any{"egress_ips": ips})
// Get 返回:  writeJSON(w, 200, map[string]any{"egress_ip": ip})
// Create/Update 返回完整 EgressIP 对象
```

From deploy/bootstrap/cloud-bootstrap.sh (line 16-22):
```bash
response=$(curl --fail --show-error --silent \
  -X POST "${BOOTSTRAP_API}/v1/bootstrap/sessions" \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"${username}\",\"password\":\"${password}\"}" 2>&1) || {
  printf "\n连接控制面失败，请检查网络或稍后重试\n"
  exit 2
}
```

From cmd/host-agent/main.go (line 20):
```go
var logger = slog.Default()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端运维加固 — 结构化日志 + healthz 增强 + 连接池 + 优雅关闭 (D-12, D-14, D-15)</name>
  <files>internal/controlplane/app/app.go, cmd/control-plane/main.go, cmd/host-agent/main.go, internal/controlplane/http/router.go</files>
  <read_first>
    - internal/controlplane/app/app.go
    - cmd/control-plane/main.go
    - cmd/host-agent/main.go
    - internal/controlplane/http/router.go
    - internal/agent/server.go (确认 agent 健康检查接口)
    - internal/agentapi/client.go (确认 AgentClient 能否做 ping)
  </read_first>
  <action>
实现 D-12（结构化日志 + healthz 增强）、D-14（优雅关闭 + 连接池）和 D-15（日志级别可调）。

**1. 提取 newLogger() 函数到 app.go（per D-12 + D-15）：**

替换 app.go 第 46 行的硬编码 logger 为：
```go
func newLogger() *slog.Logger {
    level := slog.LevelInfo
    if lvl := os.Getenv("LOG_LEVEL"); lvl != "" {
        var l slog.Level
        if err := l.UnmarshalText([]byte(lvl)); err == nil {
            level = l
        }
    }
    opts := &slog.HandlerOptions{Level: level}
    if os.Getenv("LOG_FORMAT") == "json" {
        return slog.New(slog.NewJSONHandler(os.Stdout, opts))
    }
    return slog.New(slog.NewTextHandler(os.Stdout, opts))
}
```

在 `New()` 中调用 `logger := newLogger()`。

**2. host-agent 同步日志改造（per D-12 + D-15）：**

在 cmd/host-agent/main.go 中：
- 将 `var logger = slog.Default()` 替换为与 app.go 相同的 `newLogger()` 逻辑
- 用 `slog.SetDefault(logger)` 使全局 slog 也使用此 logger

**3. 显式 pgxpool 连接池配置（per D-14）：**

替换 app.go 第 48 行的 `pgxpool.New(ctx, cfg.DatabaseURL)` 为：
```go
poolCfg, err := pgxpool.ParseConfig(cfg.DatabaseURL)
if err != nil {
    return nil, fmt.Errorf("parse database url: %w", err)
}
poolCfg.MaxConns = 10
poolCfg.MinConns = 2
poolCfg.MaxConnLifetime = 30 * time.Minute
poolCfg.MaxConnIdleTime = 5 * time.Minute
db, err := pgxpool.NewWithConfig(ctx, poolCfg)
```

**4. 确保优雅关闭释放连接池（per D-14）：**

在 app.go 的 `Run()` 方法中，在函数开头加 `defer a.db.Close()`，确保无论哪条退出路径都释放连接池。注意：当前 host-agent main.go 已有 `defer db.Close()`（第 39 行），只需在控制面补齐。

**5. /healthz 增强 — 添加 agent 可达性检查（per D-12）：**

在 router.go Dependencies 中添加字段：
```go
AgentHealth AgentHealthChecker
```

添加接口：
```go
type AgentHealthChecker interface {
    Ping(context.Context) error
}
```

修改 /healthz handler 为分组检查（参考 RESEARCH.md Pattern 5）：
```go
mux.HandleFunc("GET /healthz", func(w nethttp.ResponseWriter, r *nethttp.Request) {
    ctx, cancel := context.WithTimeout(r.Context(), 3*time.Second)
    defer cancel()

    checks := map[string]string{}
    status := nethttp.StatusOK

    if deps.Health != nil {
        if err := deps.Health.Health(ctx); err != nil {
            checks["database"] = err.Error()
            status = nethttp.StatusServiceUnavailable
        } else {
            checks["database"] = "ok"
        }
    }

    if deps.AgentHealth != nil {
        if err := deps.AgentHealth.Ping(ctx); err != nil {
            checks["agent"] = err.Error()
            status = nethttp.StatusServiceUnavailable
        } else {
            checks["agent"] = "ok"
        }
    }

    writeJSON(w, status, map[string]any{
        "status": map[bool]string{true: "ok", false: "degraded"}[status == nethttp.StatusOK],
        "checks": checks,
    })
})
```

在 app.go 中，将 agentClient（已有的 `agentapi.NewClient`）作为 `AgentHealth` 传入 Dependencies。agentClient 需要实现 Ping 方法——检查 agentapi.Client 是否已有类似方法；如果没有，添加一个 Ping 方法，向 agent 发送 GET /healthz 或简单连接检查。

**6. 在 app.go 中注入 AgentHealth（per D-12）：**

在构建 Dependencies 时添加：
```go
AgentHealth: agentClient,
```

在 agentapi.Client 上实现 `Ping(ctx) error` 方法：
```go
func (c *Client) Ping(ctx context.Context) error {
    resp, err := c.do(ctx, "GET", "/healthz", nil)
    if err != nil {
        return fmt.Errorf("agent unreachable: %w", err)
    }
    defer resp.Body.Close()
    if resp.StatusCode != http.StatusOK {
        return fmt.Errorf("agent unhealthy: status %d", resp.StatusCode)
    }
    return nil
}
```

如果 agent server 没有 /healthz 端点，在 agent/server.go 中添加一个简单的：
```go
mux.HandleFunc("GET /healthz", func(w http.ResponseWriter, r *http.Request) {
    writeJSON(w, http.StatusOK, map[string]string{"status": "ok"})
})
```
  </action>
  <verify>
    <automated>go build ./cmd/control-plane/ && go build ./cmd/host-agent/ && go vet ./internal/controlplane/... ./cmd/... ./internal/agent/... ./internal/agentapi/...</automated>
  </verify>
  <done>
    - LOG_FORMAT=json 时控制面和 host-agent 输出 JSON 日志
    - LOG_LEVEL 环境变量可控制日志级别
    - /healthz 返回 database 和 agent 分组检查结果
    - pgxpool 有显式 MaxConns=10, MinConns=2 等配置
    - 控制面 Run() 退出时释放连接池
    - go build 和 go vet 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 安全审查与终端体验打磨 (D-10, D-13)</name>
  <files>deploy/bootstrap/cloud-bootstrap.sh, internal/store/repository/models.go, internal/controlplane/http/admin_egress_ips.go</files>
  <read_first>
    - deploy/bootstrap/cloud-bootstrap.sh (完整脚本)
    - internal/controlplane/http/bootstrap_errors.go (错误码契约)
    - internal/controlplane/http/admin_egress_ips.go (响应构造)
    - internal/controlplane/http/admin_users.go (确认 User 无敏感字段泄露)
    - internal/store/repository/models.go (确认 JSON 标签)
    - internal/controlplane/http/router.go (确认端点权限边界)
  </read_first>
  <action>
实现 D-10（bootstrap 脚本失败路径打磨）和 D-13（安全审查 + 敏感字段泄露修复）。

**1. 修复 bootstrap 脚本 HTTP 错误码解析（per D-10）：**

当前问题：`curl --fail` 对 4xx/5xx 响应返回非零退出码且不输出 body，导致所有 HTTP 错误都走 `exit 2`（"连接控制面失败"），无法区分 401（密码错误）和网络故障。

修复方案 — 改用 `-w '%{http_code}'` 替代 `--fail`：
```bash
http_code=$(curl --silent --show-error -o /tmp/cloud-bootstrap-resp.$$ \
  -w '%{http_code}' \
  -X POST "${BOOTSTRAP_API}/v1/bootstrap/sessions" \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"${username}\",\"password\":\"${password}\"}" 2>/dev/null) || {
  printf "\n连接控制面失败，请检查网络或稍后重试\n"
  printf "重试命令: curl -sSL %s/v1/bootstrap/script | bash\n" "$BOOTSTRAP_API"
  rm -f /tmp/cloud-bootstrap-resp.$$
  exit 2
}
response=$(cat /tmp/cloud-bootstrap-resp.$$)
rm -f /tmp/cloud-bootstrap-resp.$$
```

然后对 http_code 做分支：
```bash
if [ "$http_code" -ge 400 ]; then
  error_code=$(echo "$response" | grep -o '"error_code":"[^"]*"' | cut -d'"' -f4)
  message=$(echo "$response" | grep -o '"message":"[^"]*"' | cut -d'"' -f4)
  if [ -n "$error_code" ]; then
    printf "\n错误: %s\n" "$message"
    case "$error_code" in
      auth_invalid)           exit 10 ;;
      account_disabled)       exit 11 ;;
      account_expired)        exit 12 ;;
      host_not_found)         exit 13 ;;
      start_failed)           exit 14 ;;
      ssh_not_ready)          exit 15 ;;
      egress_binding_missing) exit 16 ;;
      *)                      exit 1 ;;
    esac
  fi
  printf "\n服务器返回错误（HTTP %s），请稍后重试或联系管理员\n" "$http_code"
  exit 2
fi
```

注意保留正常路径（http_code 2xx）的 error_code 解析逻辑不变（处理 200 + error_code 的边缘情况）。

同样修复轮询阶段和 handoff 阶段的 curl 调用——将 `--fail` 替换为 `-w '%{http_code}' -o tempfile` 模式，使超时和 5xx 都能给出清晰提示。

**2. 补齐边缘场景的中文提示（per D-10）：**

- 轮询超时（第 81-84 行）：在"等待主机就绪超时"后补充实际超时时间和建议："等待 ${POLL_TIMEOUT} 秒后主机仍未就绪，请稍后重试"
- Handoff 阶段网络失败：从通用的 exit 2 改为 "获取 SSH 接入信息失败，请检查网络后重试命令: curl ..."
- 所有 exit 路径确保有明确的"下一步建议"（重试命令或联系管理员）

**3. 修复 EgressIP WgPresharedKey 泄露（per D-13）：**

在 admin_egress_ips.go 中，List、Get、Create、Update handler 在调用 writeJSON 之前，清除 WgPresharedKey：

添加辅助函数：
```go
func sanitizeEgressIP(ip *repository.EgressIP) {
    ip.WgPresharedKey = nil
}
```

在 List handler 中：
```go
for i := range ips {
    sanitizeEgressIP(&ips[i])
}
writeJSON(w, nethttp.StatusOK, map[string]any{"egress_ips": ips})
```

在 Get/Create/Update handler 中：
```go
sanitizeEgressIP(&ip)
writeJSON(w, nethttp.StatusOK, map[string]any{"egress_ip": ip})
```

**4. 确认其他 API 端点无敏感字段泄露（per D-13）：**

审查确认：
- ✅ User 结构体不包含 PasswordHash（PasswordHash 仅在 BootstrapUserAuth 中）
- ✅ AdminConfig.JWTSecret 不在任何 API 响应中
- ⚠️ EgressIP.WgPresharedKey — 本任务修复
- ✅ 日志中不打印密码（admin_users.go 只 log error，不 log 请求体）

在 task action 注释中记录审查结果，代码改动只需要 sanitizeEgressIP。

**5. 确认 API 权限边界正确（per D-13）：**

基于 router.go 审查：
- GET /healthz — 公开 ✅
- GET /v1/bootstrap/script — 公开 ✅
- POST /v1/bootstrap/sessions — 用户凭证认证 ✅
- GET /v1/bootstrap/tasks/{taskID} — 通过 taskID 隐式鉴权（UUID 足够随机）✅
- POST /v1/admin/login — 管理员凭证 ✅
- 所有 /v1/admin/* — AdminAuthMiddleware(JWT) ✅

权限边界正确，无需代码改动。记录审查结论即可。
  </action>
  <verify>
    <automated>bash -n deploy/bootstrap/cloud-bootstrap.sh && go vet ./internal/controlplane/http/...</automated>
  </verify>
  <done>
    - bootstrap 脚本对 HTTP 401/403/404 返回对应的退出码和中文提示，不再一律 exit 2
    - bootstrap 脚本所有 exit 路径都有"下一步建议"
    - EgressIP API 响应中 WgPresharedKey 字段被清除
    - API 权限边界和敏感字段泄露审查完成并记录
    - bash -n 和 go vet 通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 后台 UI 体验打磨 (D-11)</name>
  <files>web/admin/src/components/egress-ips/egress-ip-drawer.tsx</files>
  <read_first>
    - web/admin/src/components/egress-ips/egress-ip-drawer.tsx
    - web/admin/src/components/users/create-user-dialog.tsx (参考已有校验模式)
    - web/admin/src/routes/_dashboard/users/index.tsx (确认 loading/empty 状态)
    - web/admin/src/routes/_dashboard/hosts/index.tsx (确认 loading/empty 状态)
    - web/admin/src/routes/_dashboard/egress-ips/index.tsx (确认 loading/empty 状态)
    - web/admin/src/routes/_dashboard/events/index.tsx (确认 loading/empty 状态)
    - web/admin/src/routes/_dashboard/tasks/index.tsx (确认 loading/empty 状态)
  </read_first>
  <action>
补齐后台表单校验和体验一致性（per D-11）。

**现有基线审查：**

已到位的能力（不需要修改）：
- ✅ 所有 5 个列表页面（用户、主机、出口 IP、事件、任务）都有 skeleton loading
- ✅ 所有 5 个列表页面都有"暂无..."空状态展示
- ✅ CreateUserDialog 有 zod schema 校验（用户名 3-50 字符、密码 ≥ 8 字符）
- ✅ EgressIPDrawer 有 zod schema 基础校验（label/ip_address 非空）
- ✅ 表单提交按钮有 isPending 加载态（"创建中…"、"保存中..."）
- ✅ 操作成功/失败有 sonner toast 反馈

**需要补齐的体验差距：**

1. **出口 IP 表单 — IP 地址格式校验（D-11 格式校验）：**

当前 egress-ip-drawer.tsx 的 zod schema 中 `ip_address` 只有 `z.string().min(1)`。后端 admin_egress_ips.go 使用 `net.ParseIP()` 做了服务端校验，但前端没有对应的格式校验提示。

将 zod schema 中的 ip_address 改为：
```typescript
ip_address: z
  .string()
  .min(1, "IP 地址不能为空")
  .regex(
    /^(\d{1,3}\.){3}\d{1,3}$/,
    "请输入有效的 IPv4 地址格式（如 1.2.3.4）",
  ),
```

2. **出口 IP 表单 — WireGuard endpoint 格式提示（D-11 格式校验）：**

wg_endpoint 当前无校验。添加条件校验：当填写了 endpoint 时，格式应为 `host:port`：
```typescript
wg_endpoint: z
  .string()
  .optional()
  .refine(
    (v) => !v || /^.+:\d+$/.test(v),
    "格式应为 host:port（如 vpn.example.com:51820）",
  ),
```

3. **出口 IP 表单 — WireGuard peer address CIDR 格式提示（D-11 格式校验）：**

wg_peer_address 当前无校验。添加条件校验：
```typescript
wg_peer_address: z
  .string()
  .optional()
  .refine(
    (v) => !v || /^(\d{1,3}\.){3}\d{1,3}\/\d{1,2}$/.test(v),
    "格式应为 CIDR（如 10.0.0.2/32）",
  ),
```

4. **为有条件校验的字段显示错误提示（D-11 校验反馈）：**

在 egress-ip-drawer.tsx 中，为 wg_endpoint 和 wg_peer_address 字段的 Input 下方添加错误提示显示：
```tsx
{form.formState.errors.wg_endpoint && (
  <p className="text-sm text-destructive">
    {form.formState.errors.wg_endpoint.message}
  </p>
)}
```

**不需要改动的部分（确认并记录在 SUMMARY 中）：**
- 用户列表：skeleton + 空状态 + dropdown 操作 ✅
- 主机列表：skeleton + 空状态 + 详情链接 ✅
- 出口 IP 列表：skeleton + 空状态 + 编辑/删除 ✅
- 事件列表：skeleton + 空状态 + 分页 + 筛选 ✅
- 任务列表：skeleton + 空状态 + 自动刷新 ✅
- 创建用户对话框：zod 校验 + 错误提示 + loading 按钮 ✅
  </action>
  <verify>
    <automated>cd web/admin && npx tsc --noEmit</automated>
  </verify>
  <done>
    - 出口 IP 表单的 IP 地址有前端格式校验和中文错误提示
    - WireGuard endpoint 和 peer address 有格式校验
    - 所有列表页面的 loading/空状态/操作反馈一致性已确认
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<verification>
```bash
# 后端编译和检查
go build ./cmd/control-plane/ && go build ./cmd/host-agent/ && go vet ./...

# 脚本语法检查
bash -n deploy/bootstrap/cloud-bootstrap.sh

# 前端类型检查
cd web/admin && npx tsc --noEmit

# 验证 healthz 响应格式（在运行实例上）
# curl http://127.0.0.1:8080/healthz | jq .
# 期望: {"status":"ok","checks":{"database":"ok","agent":"ok"}}
```
</verification>

<success_criteria>
- LOG_FORMAT=json 和 LOG_LEVEL 环境变量在控制面和 host-agent 中都已实现
- /healthz 返回分组检查结果（database + agent）
- pgxpool 有显式连接池配置
- 控制面优雅关闭时释放 DB 连接池
- bootstrap 脚本 HTTP 4xx/5xx 能解析 error_code 并返回对应退出码
- EgressIP.WgPresharedKey 不在 API 响应中泄露
- 后台出口 IP 表单有 IP 格式校验
- go build + go vet + bash -n + tsc --noEmit 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-mvp/06-04-SUMMARY.md`
</output>
