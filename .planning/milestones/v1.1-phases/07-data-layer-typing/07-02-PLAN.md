---
phase: 07-data-layer-typing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/network/types.go
  - internal/network/validate.go
  - internal/network/validate_test.go
  - internal/network/tunnel_provider_linux.go
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "EgressConfig 包含 TunnelType 字段区分 wireguard 和 proxy"
    - "EgressConfig.Tunnel 是 *TunnelSpec 指针类型，proxy 模式下为 nil"
    - "EgressConfig.Proxy 是 *ProxySpec 指针类型，wireguard 模式下为 nil"
    - "ValidateEgressBinding 对 wireguard 类型走现有校验路径，对 proxy 类型走 proxy_config 校验路径"
    - "TunnelProvider.PrepareHost 对 Tunnel==nil 的情况返回错误而非 panic"
  artifacts:
    - path: "internal/network/types.go"
      provides: "TunnelType 常量、ProxySpec 结构体、EgressConfig 重构"
      contains: "TunnelTypeWireGuard"
    - path: "internal/network/validate.go"
      provides: "EgressIPRecord 扩展 + ValidateEgressBinding 分支逻辑"
      contains: "TunnelTypeProxy"
    - path: "internal/network/validate_test.go"
      provides: "proxy 类型绑定校验测试"
      contains: "TestValidateEgressBinding_ProxySuccess"
  key_links:
    - from: "internal/network/validate.go"
      to: "internal/network/types.go"
      via: "ValidateEgressBinding 返回 EgressConfig，使用 TunnelType 常量和 ProxySpec"
      pattern: "TunnelTypeProxy.*ProxySpec"
    - from: "internal/network/tunnel_provider_linux.go"
      to: "internal/network/types.go"
      via: "PrepareHost 读取 spec.Egress.Tunnel（现在是指针）"
      pattern: "spec\\.Egress\\.Tunnel"
---

<objective>
扩展 network 包的类型系统和校验逻辑，使 EgressConfig 能够表达 wireguard 和 proxy 两种隧道模式，ValidateEgressBinding 按 tunnel_type 分支构建不同的配置。

Purpose: 为 Phase 8 的 SingBoxProvider 和 Provider 工厂提供类型基础，同时确保 Phase 7 内 WireGuard 路径不受影响。
Output: 扩展后的 network 类型定义、分支校验逻辑和对应测试。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-layer-typing/07-CONTEXT.md
@.planning/phases/07-data-layer-typing/07-RESEARCH.md

<interfaces>
From internal/network/types.go:
```go
type TunnelSpec struct {
    InterfaceName string
    PrivateKey    string
    PeerPublicKey string
    PeerEndpoint  *net.UDPAddr
    PresharedKey  string
    AllowedIPs    string
    TunnelAddress *net.IPNet
    DNSServer     string
}

type EgressConfig struct {
    EgressIPID string
    ExpectedIP string
    Tunnel     TunnelSpec  // 将改为 *TunnelSpec
}

type HostNetworkSpec struct {
    HostID       string
    ContainerPID uint32
    Egress       *EgressConfig
}
```

From internal/network/validate.go:
```go
type EgressIPRecord struct {
    ID, IPAddress string
    WgEndpoint, WgPublicKey, WgPresharedKey *string
    WgAllowedIPs string
    WgDNSServer, WgPeerAddress *string
}

type EgressValidator interface {
    GetEgressIPByHost(ctx context.Context, hostID string) (EgressIPRecord, error)
    GetHostWgKeys(ctx context.Context, hostID string) (wgPrivateKey string, wgPublicKey string, err error)
}

func ValidateEgressBinding(ctx context.Context, v EgressValidator, hostID string) (EgressConfig, error)
```

From internal/network/tunnel_provider_linux.go (line 29-48):
```go
func (tp *TunnelProvider) PrepareHost(ctx context.Context, spec HostNetworkSpec) error {
    if spec.Egress == nil { ... }
    if spec.Egress.Tunnel.PrivateKey == "" { // 改为指针后需要 nil guard
        ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 network 类型和 ValidateEgressBinding 分支逻辑</name>
  <files>internal/network/types.go, internal/network/validate.go, internal/network/tunnel_provider_linux.go</files>
  <read_first>
    - internal/network/types.go
    - internal/network/validate.go
    - internal/network/tunnel_provider_linux.go
    - internal/network/errors.go
  </read_first>
  <action>
**types.go 变更（per D-07, D-08, D-10）：**

1. 在文件顶部 import 中添加 `"encoding/json"`（现有 import 只有 `"net"`）。

2. 在 `TunnelSpec` 结构体定义之前，添加隧道类型常量（per D-23 第二层）：
   ```go
   const (
       TunnelTypeWireGuard = "wireguard"
       TunnelTypeProxy     = "proxy"
   )
   ```

3. 在 `TunnelSpec` 结构体之后（TunnelSpec 本身不变，per D-10），添加 `ProxySpec`（per D-08）：
   ```go
   type ProxySpec struct {
       OutboundConfig json.RawMessage
       DNSServer      string
   }
   ```

4. 修改 `EgressConfig` 结构体（per D-07）：
   ```go
   type EgressConfig struct {
       EgressIPID string
       ExpectedIP string
       TunnelType string
       Tunnel     *TunnelSpec
       Proxy      *ProxySpec
   }
   ```
   变更点：新增 `TunnelType string`，`Tunnel` 从值类型 `TunnelSpec` 改为指针 `*TunnelSpec`，新增 `Proxy *ProxySpec`。

**validate.go 变更（per D-09, D-11, D-12, D-13）：**

1. 在文件顶部 import 中添加 `"encoding/json"`。

2. `EgressIPRecord` 结构体新增两个字段（per D-09）：
   ```go
   TunnelType  string
   ProxyConfig json.RawMessage
   ```
   放在 `IPAddress` 字段之后。

3. 重构 `ValidateEgressBinding` 函数。获取 record 后按 `record.TunnelType` 分支：

   ```go
   func ValidateEgressBinding(ctx context.Context, v EgressValidator, hostID string) (EgressConfig, error) {
       record, err := v.GetEgressIPByHost(ctx, hostID)
       if err != nil {
           return EgressConfig{}, &NetworkError{
               Type:    ErrBindingMissing,
               Message: "no egress IP bound to host",
               HostID:  hostID,
           }
       }

       switch record.TunnelType {
       case TunnelTypeProxy:
           return validateProxyBinding(record, hostID)
       default:
           return validateWireGuardBinding(ctx, v, record, hostID)
       }
   }
   ```

4. 提取现有 WireGuard 校验逻辑为 `validateWireGuardBinding`：
   ```go
   func validateWireGuardBinding(ctx context.Context, v EgressValidator, record EgressIPRecord, hostID string) (EgressConfig, error) {
       // 现有 WgEndpoint/WgPublicKey 非空检查
       // GetHostWgKeys 调用
       // 构建 TunnelSpec
       // 返回 EgressConfig，TunnelType = TunnelTypeWireGuard，Tunnel = &TunnelSpec{...}，Proxy = nil
   }
   ```
   关键变更：返回的 `EgressConfig` 中 `Tunnel` 现在是 `&TunnelSpec{...}`（指针取址），新增 `TunnelType: TunnelTypeWireGuard`。

5. 添加 proxy 校验逻辑（per D-12, D-13）：
   ```go
   func validateProxyBinding(record EgressIPRecord, hostID string) (EgressConfig, error) {
       if record.ProxyConfig == nil || len(record.ProxyConfig) == 0 {
           return EgressConfig{}, &NetworkError{
               Type:    ErrTunnelSetupFailed,
               Message: "proxy type requires non-empty proxy_config",
               HostID:  hostID,
               Metadata: map[string]any{"egress_ip_id": record.ID},
           }
       }

       var parsed map[string]any
       if err := json.Unmarshal(record.ProxyConfig, &parsed); err != nil {
           return EgressConfig{}, &NetworkError{
               Type:    ErrTunnelSetupFailed,
               Message: fmt.Sprintf("proxy_config is not valid JSON: %v", err),
               HostID:  hostID,
               Metadata: map[string]any{"egress_ip_id": record.ID},
           }
       }

       dnsServer, _ := parsed["dns_server"].(string)

       return EgressConfig{
           EgressIPID: record.ID,
           ExpectedIP: record.IPAddress,
           TunnelType: TunnelTypeProxy,
           Tunnel:     nil,
           Proxy: &ProxySpec{
               OutboundConfig: record.ProxyConfig,
               DNSServer:      dnsServer,
           },
       }, nil
   }
   ```

**tunnel_provider_linux.go 变更（防御性 nil guard）：**

在 `PrepareHost` 方法中，现有 `if spec.Egress == nil` 检查之后、`if spec.Egress.Tunnel.PrivateKey == ""` 之前，添加 Tunnel 指针 nil 检查：
```go
if spec.Egress.Tunnel == nil {
    return &NetworkError{
        Type:    ErrTunnelSetupFailed,
        Message: "TunnelProvider requires WireGuard tunnel config",
        HostID:  spec.HostID,
        Metadata: map[string]any{"tunnel_type": spec.Egress.TunnelType},
    }
}
```
  </action>
  <verify>
    <automated>go build ./internal/network/...</automated>
  </verify>
  <acceptance_criteria>
    - `types.go` 包含 `TunnelTypeWireGuard = "wireguard"` 和 `TunnelTypeProxy = "proxy"` 常量
    - `types.go` 包含 `ProxySpec` 结构体，含 `OutboundConfig json.RawMessage` 和 `DNSServer string`
    - `types.go` 中 `EgressConfig.Tunnel` 类型为 `*TunnelSpec`（指针）
    - `types.go` 中 `EgressConfig` 包含 `TunnelType string` 和 `Proxy *ProxySpec` 字段
    - `validate.go` 中 `EgressIPRecord` 包含 `TunnelType string` 和 `ProxyConfig json.RawMessage`
    - `validate.go` 中 `ValidateEgressBinding` 包含 `switch record.TunnelType` 分支
    - `validate.go` 中存在 `validateProxyBinding` 函数
    - `validate.go` 中存在 `validateWireGuardBinding` 函数
    - `tunnel_provider_linux.go` 中 `PrepareHost` 包含 `spec.Egress.Tunnel == nil` 检查
    - `go build ./internal/network/...` 编译通过（在非 Linux 上可能只编译非 _linux 文件，但 types.go 和 validate.go 应通过）
  </acceptance_criteria>
  <done>network 包的类型系统支持 wireguard/proxy 双模式，校验逻辑按 tunnel_type 正确分支，TunnelProvider 有 nil guard 防护</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 ValidateEgressBinding 测试覆盖 proxy 分支</name>
  <files>internal/network/validate_test.go</files>
  <read_first>
    - internal/network/validate_test.go
    - internal/network/validate.go
    - internal/network/types.go
  </read_first>
  <action>
更新 `internal/network/validate_test.go`：

1. 在文件顶部 import 中添加 `"encoding/json"`。

2. 更新 `mockValidator` 的 `egressIP` 字段类型为 `EgressIPRecord`（已是，确认不变）。

3. 更新所有现有测试中的 `EgressIPRecord` 字面量，添加 `TunnelType: ""` 或 `TunnelType: "wireguard"`。对于已有测试，`TunnelType` 为空字符串应走 `default` 分支（即 WireGuard），保持现有行为。

4. 更新 `TestValidateEgressBinding_Success` 测试：在断言部分增加 `cfg.TunnelType` 和 `cfg.Tunnel` 指针检查：
   ```go
   if cfg.TunnelType != TunnelTypeWireGuard && cfg.TunnelType != "" {
       t.Errorf("TunnelType: got %q, want %q or empty", cfg.TunnelType, TunnelTypeWireGuard)
   }
   if cfg.Tunnel == nil {
       t.Fatal("Tunnel should not be nil for WireGuard type")
   }
   if cfg.Proxy != nil {
       t.Error("Proxy should be nil for WireGuard type")
   }
   ```

5. 新增 `TestValidateEgressBinding_ProxySuccess` 测试：
   ```go
   func TestValidateEgressBinding_ProxySuccess(t *testing.T) {
       proxyConfig := json.RawMessage(`{"type":"socks","server":"proxy.example.com","server_port":1080}`)

       v := &mockValidator{
           egressIP: EgressIPRecord{
               ID:          "eip-proxy-1",
               IPAddress:   "5.6.7.8",
               TunnelType:  TunnelTypeProxy,
               ProxyConfig: proxyConfig,
               WgAllowedIPs: "0.0.0.0/0",
           },
       }

       cfg, err := ValidateEgressBinding(context.Background(), v, "host-proxy-1")
       if err != nil {
           t.Fatalf("unexpected error: %v", err)
       }

       if cfg.TunnelType != TunnelTypeProxy {
           t.Errorf("TunnelType: got %q, want %q", cfg.TunnelType, TunnelTypeProxy)
       }
       if cfg.EgressIPID != "eip-proxy-1" {
           t.Errorf("EgressIPID: got %q, want %q", cfg.EgressIPID, "eip-proxy-1")
       }
       if cfg.ExpectedIP != "5.6.7.8" {
           t.Errorf("ExpectedIP: got %q, want %q", cfg.ExpectedIP, "5.6.7.8")
       }
       if cfg.Tunnel != nil {
           t.Error("Tunnel should be nil for proxy type")
       }
       if cfg.Proxy == nil {
           t.Fatal("Proxy should not be nil for proxy type")
       }
       if cfg.Proxy.OutboundConfig == nil {
           t.Error("Proxy.OutboundConfig should not be nil")
       }
   }
   ```

6. 新增 `TestValidateEgressBinding_ProxyMissingConfig` 测试：
   ```go
   func TestValidateEgressBinding_ProxyMissingConfig(t *testing.T) {
       v := &mockValidator{
           egressIP: EgressIPRecord{
               ID:          "eip-proxy-2",
               IPAddress:   "5.6.7.8",
               TunnelType:  TunnelTypeProxy,
               ProxyConfig: nil,
               WgAllowedIPs: "0.0.0.0/0",
           },
       }

       _, err := ValidateEgressBinding(context.Background(), v, "host-proxy-2")
       if err == nil {
           t.Fatal("expected error for proxy type with nil proxy_config")
       }

       var netErr *NetworkError
       if !errors.As(err, &netErr) {
           t.Fatalf("expected *NetworkError, got %T", err)
       }
       if netErr.Type != ErrTunnelSetupFailed {
           t.Errorf("expected ErrTunnelSetupFailed, got %s", netErr.Type)
       }
   }
   ```

7. 新增 `TestValidateEgressBinding_ProxyInvalidJSON` 测试：
   ```go
   func TestValidateEgressBinding_ProxyInvalidJSON(t *testing.T) {
       v := &mockValidator{
           egressIP: EgressIPRecord{
               ID:          "eip-proxy-3",
               IPAddress:   "5.6.7.8",
               TunnelType:  TunnelTypeProxy,
               ProxyConfig: json.RawMessage(`{invalid json`),
               WgAllowedIPs: "0.0.0.0/0",
           },
       }

       _, err := ValidateEgressBinding(context.Background(), v, "host-proxy-3")
       if err == nil {
           t.Fatal("expected error for invalid proxy_config JSON")
       }
   }
   ```
  </action>
  <verify>
    <automated>go test ./internal/network/ -run TestValidateEgressBinding -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `validate_test.go` 包含 `TestValidateEgressBinding_ProxySuccess` 测试
    - `validate_test.go` 包含 `TestValidateEgressBinding_ProxyMissingConfig` 测试
    - `validate_test.go` 包含 `TestValidateEgressBinding_ProxyInvalidJSON` 测试
    - 所有现有 WireGuard 测试（MissingBinding, IncompleteConfig, Success, KeysError）继续通过
    - 所有新增 proxy 测试通过
    - `go test ./internal/network/ -run TestValidateEgressBinding` 全部 PASS
  </acceptance_criteria>
  <done>ValidateEgressBinding 的 WireGuard 和 proxy 分支都有测试覆盖，共 7 个测试用例全部通过</done>
</task>

</tasks>

<verification>
- `go build ./internal/network/...` 编译通过
- `go test ./internal/network/ -run TestValidateEgressBinding -v` 全部 PASS
- WireGuard 路径行为不变（现有 4 个测试通过）
- proxy 路径校验正确（3 个新测试通过）
</verification>

<success_criteria>
- `TunnelTypeWireGuard` 和 `TunnelTypeProxy` 常量存在于 types.go
- `ProxySpec` 结构体存在于 types.go
- `EgressConfig` 包含 `TunnelType`、`Tunnel *TunnelSpec`、`Proxy *ProxySpec`
- `EgressIPRecord` 包含 `TunnelType` 和 `ProxyConfig`
- `ValidateEgressBinding` 按 tunnel_type 分支（switch 语句）
- `TunnelProvider.PrepareHost` 有 `Tunnel == nil` 防护
- 7 个 ValidateEgressBinding 测试全部 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-layer-typing/07-02-SUMMARY.md`
</output>
