---
phase: 07-data-layer-typing
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - internal/controlplane/http/admin_egress_ips.go
  - internal/controlplane/http/admin_egress_ips_test.go
  - internal/runtime/tasks/worker.go
autonomous: true
requirements: [DATA-01, DATA-04, DATA-05]

must_haves:
  truths:
    - "管理员创建出口 IP 时可以指定 tunnel_type 为 wireguard 或 proxy"
    - "tunnel_type 未提供时默认为 wireguard（向后兼容现有前端）"
    - "tunnel_type=proxy 时 proxy_config 必填且通过白名单+必需字段校验"
    - "tunnel_type=wireguard 时 proxy_config 被忽略，tunnel_type=proxy 时 wg_* 字段被忽略"
    - "API 响应中 proxy_config 的 password 字段被脱敏为 '***'"
    - "repoValidator 正确映射 TunnelType 和 ProxyConfig 到 network.EgressIPRecord"
  artifacts:
    - path: "internal/controlplane/http/admin_egress_ips.go"
      provides: "tunnel_type/proxy_config 请求字段、validateProxyConfig、sanitizeProxyConfig、handler 分支逻辑"
      contains: "validateProxyConfig"
    - path: "internal/controlplane/http/admin_egress_ips_test.go"
      provides: "tunnel_type 和 proxy_config 校验测试覆盖"
      contains: "proxy_config"
    - path: "internal/runtime/tasks/worker.go"
      provides: "repoValidator 适配新字段"
      contains: "TunnelType"
  key_links:
    - from: "internal/controlplane/http/admin_egress_ips.go"
      to: "internal/store/repository/models.go"
      via: "CreateEgressIPParams/UpdateEgressIPParams 传递 TunnelType 和 ProxyConfig"
      pattern: "TunnelType.*ProxyConfig"
    - from: "internal/runtime/tasks/worker.go"
      to: "internal/network/validate.go"
      via: "repoValidator 映射 repository.EgressIP → network.EgressIPRecord 包含新字段"
      pattern: "TunnelType.*ProxyConfig"
---

<objective>
适配 Admin API handler 支持 tunnel_type 和 proxy_config 字段，实现 proxy_config 白名单校验和响应脱敏，并更新 worker.go 中的 repoValidator 适配层。

Purpose: 让管理员能通过 API 创建和管理 proxy 类型的出口 IP，同时保持 WireGuard 类型的完全向后兼容。
Output: 完整适配的 API handler、proxy_config 校验函数、脱敏逻辑和 worker 适配层。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-layer-typing/07-CONTEXT.md
@.planning/phases/07-data-layer-typing/07-RESEARCH.md
@.planning/phases/07-data-layer-typing/07-01-SUMMARY.md
@.planning/phases/07-data-layer-typing/07-02-SUMMARY.md

<interfaces>
From internal/store/repository/models.go (after Plan 01):
```go
type EgressIP struct {
    ID             string          `json:"id"`
    Label          string          `json:"label"`
    IPAddress      string          `json:"ip_address"`
    Provider       string          `json:"provider"`
    Status         string          `json:"status"`
    TunnelType     string          `json:"tunnel_type"`
    WgEndpoint     *string         `json:"wg_endpoint,omitempty"`
    // ... wg fields ...
    ProxyConfig    json.RawMessage `json:"proxy_config,omitempty"`
    CreatedAt      time.Time       `json:"created_at"`
    UpdatedAt      time.Time       `json:"updated_at"`
}

type CreateEgressIPParams struct {
    // ... existing fields ...
    TunnelType  string
    ProxyConfig json.RawMessage
}

type UpdateEgressIPParams struct {
    // ... existing fields ...
    TunnelType  string
    ProxyConfig json.RawMessage
}
```

From internal/network/types.go (after Plan 02):
```go
const (
    TunnelTypeWireGuard = "wireguard"
    TunnelTypeProxy     = "proxy"
)
```

From internal/network/validate.go (after Plan 02):
```go
type EgressIPRecord struct {
    ID          string
    IPAddress   string
    TunnelType  string
    ProxyConfig json.RawMessage
    WgEndpoint  *string
    // ... wg fields ...
}
```

From internal/controlplane/http/admin_egress_ips.go (current):
```go
type createEgressIPRequest struct {
    Label      string  `json:"label"`
    IPAddress  string  `json:"ip_address"`
    Provider   string  `json:"provider"`
    WgEndpoint *string `json:"wg_endpoint"`
    // ... wg fields ...
}

func sanitizeEgressIP(ip *repository.EgressIP) {
    ip.WgPresharedKey = nil
}
```

From internal/runtime/tasks/worker.go (current):
```go
func (rv *repoValidator) GetEgressIPByHost(ctx context.Context, hostID string) (network.EgressIPRecord, error) {
    eip, err := rv.repo.GetEgressIPByHost(ctx, hostID)
    // ... maps to EgressIPRecord without TunnelType/ProxyConfig ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin API handler 适配 + validateProxyConfig + Worker 适配</name>
  <files>internal/controlplane/http/admin_egress_ips.go, internal/runtime/tasks/worker.go</files>
  <read_first>
    - internal/controlplane/http/admin_egress_ips.go
    - internal/runtime/tasks/worker.go
    - internal/store/repository/models.go
    - internal/network/types.go
    - internal/network/validate.go
    - .planning/phases/07-data-layer-typing/07-CONTEXT.md
  </read_first>
  <action>
**admin_egress_ips.go 变更（per D-14 ~ D-22）：**

1. 在文件顶部 import 中添加 `"fmt"`（如未存在）。添加 `"github.com/zaneliu/cloud-cli-proxy/internal/network"`（用于 TunnelType 常量）。

2. `createEgressIPRequest` 结构体新增字段（per D-18）：
   ```go
   TunnelType  string          `json:"tunnel_type"`
   ProxyConfig json.RawMessage `json:"proxy_config,omitempty"`
   ```

3. `updateEgressIPRequest` 结构体新增字段（per D-18）：
   ```go
   TunnelType  string          `json:"tunnel_type"`
   ProxyConfig json.RawMessage `json:"proxy_config,omitempty"`
   ```

4. 添加 `validateProxyConfig` 函数（per D-14, D-15, D-16, D-17）：
   ```go
   var allowedOutboundTypes = map[string]bool{
       "socks": true, "vmess": true, "shadowsocks": true, "trojan": true, "http": true,
   }

   func validateProxyConfig(raw json.RawMessage) error {
       if len(raw) == 0 {
           return fmt.Errorf("proxy_config is required for proxy tunnel type")
       }

       var parsed map[string]any
       if err := json.Unmarshal(raw, &parsed); err != nil {
           return fmt.Errorf("proxy_config is not valid JSON: %w", err)
       }

       outboundType, _ := parsed["type"].(string)
       if !allowedOutboundTypes[outboundType] {
           return fmt.Errorf("unsupported outbound type %q, allowed: socks, vmess, shadowsocks, trojan, http", outboundType)
       }

       server, _ := parsed["server"].(string)
       if server == "" {
           return fmt.Errorf("proxy_config.server is required")
       }

       port, _ := parsed["server_port"].(float64)
       if port <= 0 || port > 65535 {
           return fmt.Errorf("proxy_config.server_port must be a positive integer (1-65535)")
       }

       switch outboundType {
       case "vmess":
           if uuid, _ := parsed["uuid"].(string); uuid == "" {
               return fmt.Errorf("proxy_config.uuid is required for vmess")
           }
       case "shadowsocks":
           if method, _ := parsed["method"].(string); method == "" {
               return fmt.Errorf("proxy_config.method is required for shadowsocks")
           }
           if password, _ := parsed["password"].(string); password == "" {
               return fmt.Errorf("proxy_config.password is required for shadowsocks")
           }
       case "trojan":
           if password, _ := parsed["password"].(string); password == "" {
               return fmt.Errorf("proxy_config.password is required for trojan")
           }
       }

       return nil
   }
   ```

5. 添加 `sanitizeProxyConfig` 函数（per D-22）：
   ```go
   func sanitizeProxyConfig(raw json.RawMessage) json.RawMessage {
       if raw == nil {
           return nil
       }
       var parsed map[string]any
       if err := json.Unmarshal(raw, &parsed); err != nil {
           return nil
       }
       if _, ok := parsed["password"]; ok {
           parsed["password"] = "***"
       }
       sanitized, _ := json.Marshal(parsed)
       return sanitized
   }
   ```

6. 修改 `sanitizeEgressIP` 函数，扩展脱敏逻辑（per D-22）：
   ```go
   func sanitizeEgressIP(ip *repository.EgressIP) {
       ip.WgPresharedKey = nil
       ip.ProxyConfig = sanitizeProxyConfig(ip.ProxyConfig)
   }
   ```

7. 修改 `Create()` handler（per D-19, D-20, D-21）：

   在现有校验逻辑（label、IP、provider 检查）之后，添加 tunnel_type 处理：
   ```go
   if req.TunnelType == "" {
       req.TunnelType = network.TunnelTypeWireGuard
   }
   if req.TunnelType != network.TunnelTypeWireGuard && req.TunnelType != network.TunnelTypeProxy {
       writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "tunnel_type must be wireguard or proxy"})
       return
   }

   if req.TunnelType == network.TunnelTypeProxy {
       if err := validateProxyConfig(req.ProxyConfig); err != nil {
           writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": err.Error()})
           return
       }
       req.WgEndpoint = nil
       req.WgPublicKey = nil
       req.WgPresharedKey = nil
       req.WgDNSServer = nil
       req.WgPeerAddress = nil
       req.WgAllowedIPs = "0.0.0.0/0"
   } else {
       req.ProxyConfig = nil
   }
   ```

   在 `h.store.CreateEgressIP` 调用的 `repository.CreateEgressIPParams` 中添加：
   ```go
   TunnelType:  req.TunnelType,
   ProxyConfig: req.ProxyConfig,
   ```

   在事件记录的 Metadata 中添加 `"tunnel_type": ip.TunnelType`。

8. 修改 `Update()` handler（per D-19, D-20, D-21）：

   在现有 status 校验之后，添加 tunnel_type 处理：
   ```go
   if req.TunnelType == "" {
       req.TunnelType = network.TunnelTypeWireGuard
   }
   if req.TunnelType != network.TunnelTypeWireGuard && req.TunnelType != network.TunnelTypeProxy {
       writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "tunnel_type must be wireguard or proxy"})
       return
   }

   if req.TunnelType == network.TunnelTypeProxy {
       if err := validateProxyConfig(req.ProxyConfig); err != nil {
           writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": err.Error()})
           return
       }
       req.WgEndpoint = nil
       req.WgPublicKey = nil
       req.WgPresharedKey = nil
       req.WgDNSServer = nil
       req.WgPeerAddress = nil
       req.WgAllowedIPs = "0.0.0.0/0"
   } else {
       req.ProxyConfig = nil
   }
   ```

   在 `repository.UpdateEgressIPParams` 中添加：
   ```go
   TunnelType:  req.TunnelType,
   ProxyConfig: req.ProxyConfig,
   ```

**worker.go 变更：**

更新 `repoValidator.GetEgressIPByHost` 方法，在返回的 `network.EgressIPRecord` 中添加新字段映射：
```go
return network.EgressIPRecord{
    ID:             eip.ID,
    IPAddress:      eip.IPAddress,
    TunnelType:     eip.TunnelType,
    WgEndpoint:     eip.WgEndpoint,
    WgPublicKey:    eip.WgPublicKey,
    WgPresharedKey: eip.WgPresharedKey,
    WgAllowedIPs:   eip.WgAllowedIPs,
    WgDNSServer:    eip.WgDNSServer,
    WgPeerAddress:  eip.WgPeerAddress,
    ProxyConfig:    eip.ProxyConfig,
}, nil
```
  </action>
  <verify>
    <automated>go build ./internal/controlplane/... ./internal/runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - `admin_egress_ips.go` 中 `createEgressIPRequest` 包含 `TunnelType string` 和 `ProxyConfig json.RawMessage`
    - `admin_egress_ips.go` 中 `updateEgressIPRequest` 包含 `TunnelType string` 和 `ProxyConfig json.RawMessage`
    - `admin_egress_ips.go` 中存在 `validateProxyConfig` 函数
    - `admin_egress_ips.go` 中存在 `sanitizeProxyConfig` 函数
    - `admin_egress_ips.go` 中存在 `allowedOutboundTypes` map，包含 "socks", "vmess", "shadowsocks", "trojan", "http"
    - `admin_egress_ips.go` 中 Create handler 包含 `req.TunnelType == ""` 默认值设置
    - `admin_egress_ips.go` 中 Create handler 包含 `validateProxyConfig` 调用
    - `admin_egress_ips.go` 中 `sanitizeEgressIP` 调用 `sanitizeProxyConfig`
    - `worker.go` 中 `repoValidator.GetEgressIPByHost` 返回值包含 `TunnelType: eip.TunnelType`
    - `worker.go` 中 `repoValidator.GetEgressIPByHost` 返回值包含 `ProxyConfig: eip.ProxyConfig`
    - `go build ./internal/controlplane/... ./internal/runtime/...` 编译通过
  </acceptance_criteria>
  <done>Admin API 完整支持 tunnel_type 和 proxy_config 字段的创建/更新/校验/脱敏，worker 适配层正确映射新字段</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 Admin EgressIP API 测试覆盖 tunnel_type 和 proxy_config</name>
  <files>internal/controlplane/http/admin_egress_ips_test.go</files>
  <read_first>
    - internal/controlplane/http/admin_egress_ips_test.go
    - internal/controlplane/http/admin_egress_ips.go
    - internal/store/repository/models.go
  </read_first>
  <action>
更新 `internal/controlplane/http/admin_egress_ips_test.go`：

1. 在 import 中确认包含 `"encoding/json"`。

2. 更新 `sampleIP` 的 `EgressIP` 字面量，添加 `TunnelType: "wireguard"`（使现有测试与新模型对齐）。

3. 在 `TestAdminEgressIPsHandler` 的 tests slice 中，追加以下测试用例：

   **proxy 类型创建成功（DATA-01）：**
   ```go
   {
       name:   "Create proxy egress IP 201",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label":      "proxy-ip",
           "ip_address": "5.6.7.8",
           "provider":   "manual",
           "tunnel_type": "proxy",
           "proxy_config": map[string]any{
               "type": "socks", "server": "proxy.example.com", "server_port": 1080,
           },
       },
       store:      &stubEgressIPStore{createIP: repository.EgressIP{
           ID: "ip2", Label: "proxy-ip", IPAddress: "5.6.7.8",
           Provider: "manual", Status: "available", TunnelType: "proxy",
           WgAllowedIPs: "0.0.0.0/0", CreatedAt: now, UpdatedAt: now,
       }},
       wantStatus: 201,
       wantField:  "egress_ip",
   },
   ```

   **无效 tunnel_type（DATA-01, D-23）：**
   ```go
   {
       name:   "Create invalid tunnel_type 400",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label": "bad", "ip_address": "1.2.3.4", "tunnel_type": "invalid",
       },
       store:      &stubEgressIPStore{},
       wantStatus: 400,
   },
   ```

   **proxy 类型缺少 proxy_config（DATA-04）：**
   ```go
   {
       name:   "Create proxy missing proxy_config 400",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label": "no-config", "ip_address": "1.2.3.4", "tunnel_type": "proxy",
       },
       store:      &stubEgressIPStore{},
       wantStatus: 400,
   },
   ```

   **proxy_config 不合法 outbound type（DATA-04, D-14）：**
   ```go
   {
       name:   "Create proxy unsupported outbound type 400",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label": "bad-type", "ip_address": "1.2.3.4", "tunnel_type": "proxy",
           "proxy_config": map[string]any{
               "type": "wireguard", "server": "example.com", "server_port": 1080,
           },
       },
       store:      &stubEgressIPStore{},
       wantStatus: 400,
   },
   ```

   **proxy_config 缺少 server（DATA-04, D-15）：**
   ```go
   {
       name:   "Create proxy missing server 400",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label": "no-server", "ip_address": "1.2.3.4", "tunnel_type": "proxy",
           "proxy_config": map[string]any{
               "type": "socks", "server_port": 1080,
           },
       },
       store:      &stubEgressIPStore{},
       wantStatus: 400,
   },
   ```

   **vmess 缺少 uuid（DATA-04, D-16）：**
   ```go
   {
       name:   "Create vmess missing uuid 400",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]any{
           "label": "no-uuid", "ip_address": "1.2.3.4", "tunnel_type": "proxy",
           "proxy_config": map[string]any{
               "type": "vmess", "server": "proxy.example.com", "server_port": 443,
           },
       },
       store:      &stubEgressIPStore{},
       wantStatus: 400,
   },
   ```

   **默认 tunnel_type 向后兼容（DATA-05, D-19）：**
   ```go
   {
       name:   "Create without tunnel_type defaults to wireguard 201",
       method: "POST",
       path:   "/v1/admin/egress-ips",
       body: map[string]string{
           "label": "wg-default", "ip_address": "9.8.7.6", "provider": "manual",
       },
       store:      &stubEgressIPStore{createIP: sampleIP},
       wantStatus: 201,
       wantField:  "egress_ip",
   },
   ```

4. 确保所有现有测试用例继续通过（sampleIP 新增 TunnelType 字段不影响现有断言）。
  </action>
  <verify>
    <automated>go test ./internal/controlplane/http/ -run TestAdminEgressIPsHandler -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `admin_egress_ips_test.go` 包含 "Create proxy egress IP 201" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create invalid tunnel_type 400" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create proxy missing proxy_config 400" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create proxy unsupported outbound type 400" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create proxy missing server 400" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create vmess missing uuid 400" 测试用例
    - `admin_egress_ips_test.go` 包含 "Create without tunnel_type defaults to wireguard 201" 测试用例
    - 所有现有测试用例继续通过（status 200/201/204/400/404/500）
    - `go test ./internal/controlplane/http/ -run TestAdminEgressIPsHandler -v` 全部 PASS
  </acceptance_criteria>
  <done>Admin EgressIP API 测试覆盖 tunnel_type 创建、proxy_config 校验（白名单、必需字段、协议分支）和向后兼容，共 18+ 个测试用例全部通过</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` 全量编译通过
- `go test ./internal/controlplane/http/ -run TestAdminEgressIPsHandler -v` 全部 PASS
- `go test ./internal/network/ -run TestValidateEgressBinding -v` 全部 PASS
- 现有前端不传 tunnel_type 时 Create API 返回 201（向后兼容）
</verification>

<success_criteria>
- API handler 支持 tunnel_type 和 proxy_config 字段的创建和更新
- proxy_config 校验覆盖白名单（5 种协议）和必需字段（server、server_port、协议专有字段）
- 响应脱敏覆盖 proxy_config 中的 password 字段
- tunnel_type 未提供时默认 wireguard
- repoValidator 正确映射新字段
- 全部测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-layer-typing/07-03-SUMMARY.md`
</output>
