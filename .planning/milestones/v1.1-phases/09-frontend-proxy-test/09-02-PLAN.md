---
phase: 09-frontend-proxy-test
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - web/admin/src/components/egress-ips/egress-ip-drawer.tsx
  - web/admin/src/components/egress-ips/proxy-fields.tsx
  - internal/controlplane/http/admin_egress_ips.go
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "创建/编辑出口 IP 时，表单根据隧道类型动态切换 WireGuard 字段或代理协议字段"
    - "选择代理协议后，按协议类型（socks/vmess/ss/trojan/http）动态渲染对应字段"
    - "代理协议配置区域支持表单模式和 JSON 模式双向切换"
    - "编辑代理类型出口 IP 时，密码字段不会被 *** 覆盖原值"
  artifacts:
    - path: "web/admin/src/components/egress-ips/egress-ip-drawer.tsx"
      provides: "Dynamic form with tunnel_type switch and conditional rendering"
      contains: "watch(\"tunnel_type\")"
    - path: "web/admin/src/components/egress-ips/proxy-fields.tsx"
      provides: "Protocol-specific fields, JSON mode, form↔JSON conversion"
      exports: ["ProxyFields"]
  key_links:
    - from: "egress-ip-drawer.tsx"
      to: "proxy-fields.tsx"
      via: "import and render ProxyFields when tunnel_type=proxy"
      pattern: "import.*ProxyFields"
    - from: "egress-ip-drawer.tsx"
      to: "use-egress-ips.ts"
      via: "import EgressIP type with tunnel_type and proxy_config"
      pattern: "tunnel_type.*proxy_config"
    - from: "admin_egress_ips.go"
      to: "store.GetEgressIP"
      via: "password merge reads existing record before update"
      pattern: "GetEgressIP.*password"
---

<objective>
将出口 IP 表单从静态 WireGuard 字段改造为根据隧道类型动态切换的表单，支持 5 种代理协议的字段渲染和 JSON 编辑模式。

Purpose: 管理员需要通过表单界面配置代理类型出口 IP，而不是手工编写 JSON，同时保留高级用户直接编辑 sing-box outbound JSON 的能力。
Output: 改造后的 EgressIPDrawer 组件 + 新建的 ProxyFields 组件 + 后端密码合并逻辑。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-proxy-test/09-CONTEXT.md
@.planning/phases/09-frontend-proxy-test/09-RESEARCH.md

@web/admin/src/components/egress-ips/egress-ip-drawer.tsx
@web/admin/src/hooks/use-egress-ips.ts
@web/admin/src/lib/api.ts
@web/admin/src/components/ui/select.tsx
@web/admin/src/components/ui/input.tsx
@web/admin/src/components/ui/separator.tsx
@internal/controlplane/http/admin_egress_ips.go

<interfaces>
<!-- Key types and contracts the executor needs. -->

From web/admin/src/hooks/use-egress-ips.ts (after Plan 01):
```typescript
export interface EgressIP {
  id: string;
  label: string;
  ip_address: string;
  provider: string;
  status: string;
  wg_endpoint: string | null;
  wg_public_key: string | null;
  wg_preshared_key: string | null;
  wg_allowed_ips: string;
  wg_dns_server: string | null;
  wg_peer_address: string | null;
  tunnel_type: string;
  proxy_config: Record<string, unknown> | null;
  created_at: string;
  updated_at: string;
}
```

From internal/controlplane/http/admin_egress_ips.go:
```go
func sanitizeProxyConfig(raw json.RawMessage) json.RawMessage {
    // replaces "password" field with "***"
}

type updateEgressIPRequest struct {
    TunnelType  string          `json:"tunnel_type"`
    ProxyConfig json.RawMessage `json:"proxy_config,omitempty"`
    // ... other fields
}

// Update handler at line 256-332 — needs password merge logic
```

Decision D-06 field mappings (from 09-CONTEXT.md):
- socks: server, server_port, username(optional), password(optional), version="5"
- vmess: server, server_port, uuid, security(default "auto"), alter_id(default 0), tls{enabled, server_name}
- shadowsocks: server, server_port, method, password
- trojan: server, server_port, password, tls{enabled, server_name}
- http: server, server_port, username(optional), password(optional), tls{enabled}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ProxyFields 组件（协议字段 + JSON 模式）</name>
  <files>web/admin/src/components/egress-ips/proxy-fields.tsx</files>
  <read_first>
    - web/admin/src/components/egress-ips/egress-ip-drawer.tsx (current form — Task 2 will refactor to import ProxyFields)
    - web/admin/src/hooks/use-egress-ips.ts (EgressIP type for proxy_config structure)
    - web/admin/src/components/ui/select.tsx (Select component)
    - web/admin/src/components/ui/input.tsx (Input component)
    - .planning/phases/09-frontend-proxy-test/09-CONTEXT.md (D-05 to D-13 locked decisions on field layout and JSON mode)
  </read_first>
  <action>
创建 `web/admin/src/components/egress-ips/proxy-fields.tsx`：

**1. 组件签名：**
```typescript
import { useState } from "react";
import { type UseFormReturn } from "react-hook-form";
import { Eye, EyeOff } from "lucide-react";
import { toast } from "sonner";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
import {
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from "@/components/ui/select";

interface ProxyFieldsProps {
  form: UseFormReturn<any>;
}

export function ProxyFields({ form }: ProxyFieldsProps) { ... }
```

**2. 表单/JSON 模式切换** (per D-09)：
```tsx
const editMode = form.watch("edit_mode");

<div className="flex gap-2">
  <Button
    type="button"
    variant={editMode === "form" ? "default" : "outline"}
    size="sm"
    onClick={() => {
      if (editMode === "json") {
        // JSON → 表单 (per D-12)
        const jsonStr = form.getValues("proxy_config_json");
        if (jsonStr) {
          try {
            const parsed = JSON.parse(jsonStr);
            const formVals = proxyConfigToFormValues(parsed);
            Object.entries(formVals).forEach(([k, v]) => form.setValue(k as any, v));
          } catch {
            toast.error("JSON 格式不正确，无法切换到表单模式");
            return;
          }
        }
      }
      form.setValue("edit_mode", "form");
    }}
  >
    表单模式
  </Button>
  <Button
    type="button"
    variant={editMode === "json" ? "default" : "outline"}
    size="sm"
    onClick={() => {
      // 表单 → JSON (per D-11)
      const values = form.getValues();
      const config = formValuesToProxyConfig(values);
      form.setValue("proxy_config_json", JSON.stringify(config, null, 2));
      form.setValue("edit_mode", "json");
    }}
  >
    JSON 模式
  </Button>
</div>
```

**3. JSON 模式渲染** (per D-10)：
```tsx
{editMode === "json" && (
  <div className="space-y-2">
    <Label>sing-box outbound JSON</Label>
    <textarea
      className="min-h-[200px] w-full rounded-md border border-input bg-background px-3 py-2 font-mono text-sm ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
      value={form.watch("proxy_config_json") || ""}
      onChange={(e) => form.setValue("proxy_config_json", e.target.value)}
    />
    {form.formState.errors.proxy_config_json && (
      <p className="text-sm text-destructive">
        {form.formState.errors.proxy_config_json.message as string}
      </p>
    )}
  </div>
)}
```

**4. 表单模式 — 协议选择器** (per D-05)：
```tsx
{editMode === "form" && (
  <>
    <div className="space-y-2">
      <Label>协议类型</Label>
      <Select
        value={form.watch("proxy_protocol") || "socks"}
        onValueChange={(val) => form.setValue("proxy_protocol", val)}
      >
        <SelectTrigger><SelectValue /></SelectTrigger>
        <SelectContent>
          <SelectItem value="socks">SOCKS5</SelectItem>
          <SelectItem value="vmess">VMess</SelectItem>
          <SelectItem value="shadowsocks">Shadowsocks</SelectItem>
          <SelectItem value="trojan">Trojan</SelectItem>
          <SelectItem value="http">HTTP</SelectItem>
        </SelectContent>
      </Select>
    </div>

    {/* 通用字段：server 和 port (per D-07) */}
    <div className="grid grid-cols-3 gap-2">
      <div className="col-span-2 space-y-2">
        <Label htmlFor="proxy_server">服务器地址 *</Label>
        <Input id="proxy_server" placeholder="例如 proxy.example.com" {...form.register("proxy_server")} />
        {form.formState.errors.proxy_server && (
          <p className="text-sm text-destructive">{form.formState.errors.proxy_server.message as string}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="proxy_port">端口 *</Label>
        <Input id="proxy_port" type="number" placeholder="1080" {...form.register("proxy_port")} />
        {form.formState.errors.proxy_port && (
          <p className="text-sm text-destructive">{form.formState.errors.proxy_port.message as string}</p>
        )}
      </div>
    </div>

    {/* 协议专有字段 */}
    {protocol === "socks" && <SocksFields form={form} />}
    {protocol === "vmess" && <VmessFields form={form} />}
    {protocol === "shadowsocks" && <ShadowsocksFields form={form} />}
    {protocol === "trojan" && <TrojanFields form={form} />}
    {protocol === "http" && <HttpFields form={form} />}
  </>
)}
```
其中 `const protocol = form.watch("proxy_protocol");`

**5. 各协议字段子组件**（文件内的函数组件，不需要单独文件）：

**SocksFields** (per D-06 socks):
```tsx
function SocksFields({ form }: { form: UseFormReturn<any> }) {
  return (
    <>
      <div className="space-y-2">
        <Label htmlFor="proxy_username">用户名（选填）</Label>
        <Input id="proxy_username" {...form.register("proxy_username")} />
      </div>
      <PasswordField form={form} name="proxy_password" label="密码（选填）" />
    </>
  );
}
```

**VmessFields** (per D-06 vmess):
```tsx
function VmessFields({ form }: { form: UseFormReturn<any> }) {
  const tlsEnabled = form.watch("proxy_tls");
  return (
    <>
      <div className="space-y-2">
        <Label htmlFor="proxy_uuid">UUID *</Label>
        <Input id="proxy_uuid" className="font-mono" {...form.register("proxy_uuid")} />
        {form.formState.errors.proxy_uuid && (
          <p className="text-sm text-destructive">{form.formState.errors.proxy_uuid.message as string}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label>加密方式</Label>
        <Select value={form.watch("proxy_security") || "auto"} onValueChange={(v) => form.setValue("proxy_security", v)}>
          <SelectTrigger><SelectValue /></SelectTrigger>
          <SelectContent>
            <SelectItem value="auto">auto</SelectItem>
            <SelectItem value="none">none</SelectItem>
            <SelectItem value="zero">zero</SelectItem>
            <SelectItem value="aes-128-gcm">aes-128-gcm</SelectItem>
            <SelectItem value="chacha20-poly1305">chacha20-poly1305</SelectItem>
          </SelectContent>
        </Select>
      </div>
      <div className="space-y-2">
        <Label htmlFor="proxy_alter_id">Alter ID</Label>
        <Input id="proxy_alter_id" type="number" defaultValue={0} {...form.register("proxy_alter_id")} />
      </div>
      <div className="flex items-center gap-2">
        <input type="checkbox" id="proxy_tls" checked={!!tlsEnabled} onChange={(e) => form.setValue("proxy_tls", e.target.checked)} className="rounded" />
        <Label htmlFor="proxy_tls">启用 TLS</Label>
      </div>
      {tlsEnabled && (
        <div className="space-y-2">
          <Label htmlFor="proxy_server_name">SNI (Server Name)</Label>
          <Input id="proxy_server_name" placeholder="默认使用服务器地址" {...form.register("proxy_server_name")} />
        </div>
      )}
    </>
  );
}
```

**ShadowsocksFields** (per D-06 shadowsocks):
```tsx
function ShadowsocksFields({ form }: { form: UseFormReturn<any> }) {
  return (
    <>
      <div className="space-y-2">
        <Label>加密方式 *</Label>
        <Select value={form.watch("proxy_method") || ""} onValueChange={(v) => form.setValue("proxy_method", v)}>
          <SelectTrigger><SelectValue placeholder="选择加密方式" /></SelectTrigger>
          <SelectContent>
            <SelectItem value="2022-blake3-aes-128-gcm">2022-blake3-aes-128-gcm</SelectItem>
            <SelectItem value="2022-blake3-aes-256-gcm">2022-blake3-aes-256-gcm</SelectItem>
            <SelectItem value="aes-128-gcm">aes-128-gcm</SelectItem>
            <SelectItem value="aes-256-gcm">aes-256-gcm</SelectItem>
            <SelectItem value="chacha20-ietf-poly1305">chacha20-ietf-poly1305</SelectItem>
          </SelectContent>
        </Select>
        {form.formState.errors.proxy_method && (
          <p className="text-sm text-destructive">{form.formState.errors.proxy_method.message as string}</p>
        )}
      </div>
      <PasswordField form={form} name="proxy_password" label="密码 *" required />
    </>
  );
}
```

**TrojanFields** (per D-06 trojan):
```tsx
function TrojanFields({ form }: { form: UseFormReturn<any> }) {
  const tlsEnabled = form.watch("proxy_tls");
  return (
    <>
      <PasswordField form={form} name="proxy_password" label="密码 *" required />
      <div className="flex items-center gap-2">
        <input type="checkbox" id="proxy_tls_trojan" checked={!!tlsEnabled} onChange={(e) => form.setValue("proxy_tls", e.target.checked)} className="rounded" />
        <Label htmlFor="proxy_tls_trojan">启用 TLS</Label>
      </div>
      {tlsEnabled && (
        <div className="space-y-2">
          <Label htmlFor="proxy_server_name">SNI (Server Name)</Label>
          <Input id="proxy_server_name" placeholder="默认使用服务器地址" {...form.register("proxy_server_name")} />
        </div>
      )}
    </>
  );
}
```

**HttpFields** (per D-06 http):
```tsx
function HttpFields({ form }: { form: UseFormReturn<any> }) {
  const tlsEnabled = form.watch("proxy_tls");
  return (
    <>
      <div className="space-y-2">
        <Label htmlFor="proxy_username">用户名（选填）</Label>
        <Input id="proxy_username" {...form.register("proxy_username")} />
      </div>
      <PasswordField form={form} name="proxy_password" label="密码（选填）" />
      <div className="flex items-center gap-2">
        <input type="checkbox" id="proxy_tls_http" checked={!!tlsEnabled} onChange={(e) => form.setValue("proxy_tls", e.target.checked)} className="rounded" />
        <Label htmlFor="proxy_tls_http">启用 TLS</Label>
      </div>
    </>
  );
}
```

**6. PasswordField 可复用组件** (per D-08)：
```tsx
function PasswordField({ form, name, label, required }: {
  form: UseFormReturn<any>;
  name: string;
  label: string;
  required?: boolean;
}) {
  const [visible, setVisible] = useState(false);
  return (
    <div className="space-y-2">
      <Label htmlFor={name}>{label}</Label>
      <div className="relative">
        <Input
          id={name}
          type={visible ? "text" : "password"}
          placeholder={required ? undefined : "不修改则留空"}
          {...form.register(name)}
        />
        <Button
          type="button"
          variant="ghost"
          size="icon"
          className="absolute right-0 top-0 h-full px-3"
          onClick={() => setVisible(!visible)}
        >
          {visible ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}
        </Button>
      </div>
      {form.formState.errors[name] && (
        <p className="text-sm text-destructive">{form.formState.errors[name]?.message as string}</p>
      )}
    </div>
  );
}
```

**7. formValuesToProxyConfig 导出函数** (per D-11, D-13)：
```typescript
export function formValuesToProxyConfig(values: Record<string, any>): Record<string, unknown> {
  const config: Record<string, unknown> = {
    type: values.proxy_protocol,
    server: values.proxy_server,
    server_port: Number(values.proxy_port),
  };

  switch (values.proxy_protocol) {
    case "socks":
      config.version = "5";
      if (values.proxy_username) config.username = values.proxy_username;
      if (values.proxy_password && values.proxy_password !== "***") config.password = values.proxy_password;
      break;
    case "vmess":
      config.uuid = values.proxy_uuid;
      config.security = values.proxy_security || "auto";
      config.alter_id = Number(values.proxy_alter_id) || 0;
      if (values.proxy_tls) {
        config.tls = { enabled: true, server_name: values.proxy_server_name || values.proxy_server };
      }
      break;
    case "shadowsocks":
      config.method = values.proxy_method;
      if (values.proxy_password && values.proxy_password !== "***") config.password = values.proxy_password;
      break;
    case "trojan":
      if (values.proxy_password && values.proxy_password !== "***") config.password = values.proxy_password;
      if (values.proxy_tls) {
        config.tls = { enabled: true, server_name: values.proxy_server_name || values.proxy_server };
      }
      break;
    case "http":
      if (values.proxy_username) config.username = values.proxy_username;
      if (values.proxy_password && values.proxy_password !== "***") config.password = values.proxy_password;
      if (values.proxy_tls) {
        config.tls = { enabled: true };
      }
      break;
  }

  return config;
}
```

**8. proxyConfigToFormValues 导出函数** (per D-12)：
```typescript
export function proxyConfigToFormValues(config: Record<string, unknown>): Record<string, unknown> {
  const protocol = config.type as string;
  const values: Record<string, unknown> = {
    proxy_protocol: protocol,
    proxy_server: config.server as string,
    proxy_port: config.server_port as number,
    proxy_username: (config.username as string) || "",
    proxy_password: (config.password as string) || "",
  };

  switch (protocol) {
    case "vmess":
      values.proxy_uuid = config.uuid as string;
      values.proxy_security = (config.security as string) || "auto";
      values.proxy_alter_id = (config.alter_id as number) || 0;
      if (config.tls && typeof config.tls === "object") {
        const tls = config.tls as Record<string, unknown>;
        values.proxy_tls = !!tls.enabled;
        values.proxy_server_name = (tls.server_name as string) || "";
      }
      break;
    case "shadowsocks":
      values.proxy_method = config.method as string;
      break;
    case "trojan":
      if (config.tls && typeof config.tls === "object") {
        const tls = config.tls as Record<string, unknown>;
        values.proxy_tls = !!tls.enabled;
        values.proxy_server_name = (tls.server_name as string) || "";
      }
      break;
  }

  return values;
}
```
  </action>
  <verify>
    <automated>cd web/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - proxy-fields.tsx exists at web/admin/src/components/egress-ips/proxy-fields.tsx
    - proxy-fields.tsx contains `export function ProxyFields`
    - proxy-fields.tsx contains `export function formValuesToProxyConfig`
    - proxy-fields.tsx contains `export function proxyConfigToFormValues`
    - proxy-fields.tsx contains `proxy_protocol`
    - proxy-fields.tsx contains `SocksFields`
    - proxy-fields.tsx contains `VmessFields`
    - proxy-fields.tsx contains `ShadowsocksFields`
    - proxy-fields.tsx contains `TrojanFields`
    - proxy-fields.tsx contains `HttpFields`
    - proxy-fields.tsx contains `PasswordField`
    - proxy-fields.tsx contains `Eye` and `EyeOff` (lucide-react icons per D-08)
    - proxy-fields.tsx contains `edit_mode` (form/json switch per D-09)
    - proxy-fields.tsx contains `<textarea` (monospace JSON editor per D-10)
    - proxy-fields.tsx contains `JSON.stringify` (form→JSON per D-11)
    - proxy-fields.tsx contains `JSON.parse` (JSON→form per D-12)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ProxyFields 组件按协议类型动态渲染 5 种协议的专有字段，密码字段带可见性切换，支持表单模式和 JSON 模式双向切换，formValuesToProxyConfig 和 proxyConfigToFormValues 实现完整的数据转换</done>
</task>

<task type="auto">
  <name>Task 2: 重构 EgressIPDrawer 表单 + 后端密码合并</name>
  <files>web/admin/src/components/egress-ips/egress-ip-drawer.tsx, internal/controlplane/http/admin_egress_ips.go</files>
  <read_first>
    - web/admin/src/components/egress-ips/egress-ip-drawer.tsx (current full form implementation, 291 lines)
    - web/admin/src/components/egress-ips/proxy-fields.tsx (created in Task 1 — ProxyFields, formValuesToProxyConfig, proxyConfigToFormValues)
    - web/admin/src/hooks/use-egress-ips.ts (updated EgressIP interface with tunnel_type and proxy_config)
    - internal/controlplane/http/admin_egress_ips.go (Update handler at L256-332, sanitizeProxyConfig at L87-100)
    - web/admin/src/components/ui/select.tsx (Select component API)
  </read_first>
  <action>
**前端 — 重构 egress-ip-drawer.tsx：**

**1. 替换 formSchema**，用包含所有字段的 z.object + superRefine：
```typescript
const formSchema = z.object({
  label: z.string().min(1, "标签不能为空"),
  ip_address: z.string().min(1, "IP 地址不能为空")
    .regex(/^(\d{1,3}\.){3}\d{1,3}$/, "请输入有效的 IPv4 地址格式"),
  provider: z.string().default("manual"),
  status: z.string().optional(),
  tunnel_type: z.enum(["wireguard", "proxy"]),
  // WireGuard fields
  wg_endpoint: z.string().optional()
    .refine((v) => !v || /^.+:\d+$/.test(v), "格式应为 host:port"),
  wg_public_key: z.string().optional(),
  wg_preshared_key: z.string().optional(),
  wg_allowed_ips: z.string().default("0.0.0.0/0"),
  wg_dns_server: z.string().optional(),
  wg_peer_address: z.string().optional()
    .refine((v) => !v || /^(\d{1,3}\.){3}\d{1,3}\/\d{1,2}$/.test(v), "格式应为 CIDR"),
  // Proxy fields
  proxy_protocol: z.enum(["socks", "vmess", "shadowsocks", "trojan", "http"]).optional(),
  proxy_server: z.string().optional(),
  proxy_port: z.coerce.number().int().min(1).max(65535).optional(),
  proxy_username: z.string().optional(),
  proxy_password: z.string().optional(),
  proxy_uuid: z.string().optional(),
  proxy_security: z.string().optional(),
  proxy_alter_id: z.coerce.number().int().min(0).optional(),
  proxy_tls: z.boolean().optional(),
  proxy_server_name: z.string().optional(),
  proxy_method: z.string().optional(),
  edit_mode: z.enum(["form", "json"]).default("form"),
  proxy_config_json: z.string().optional(),
}).superRefine((data, ctx) => {
  if (data.tunnel_type === "proxy" && data.edit_mode === "form") {
    if (!data.proxy_server) {
      ctx.addIssue({ code: "custom", path: ["proxy_server"], message: "服务器地址不能为空" });
    }
    if (!data.proxy_port) {
      ctx.addIssue({ code: "custom", path: ["proxy_port"], message: "端口不能为空" });
    }
    const proto = data.proxy_protocol;
    if (proto === "vmess" && !data.proxy_uuid) {
      ctx.addIssue({ code: "custom", path: ["proxy_uuid"], message: "UUID 不能为空" });
    }
    if (proto === "shadowsocks") {
      if (!data.proxy_method) ctx.addIssue({ code: "custom", path: ["proxy_method"], message: "加密方式不能为空" });
      if (!data.proxy_password) ctx.addIssue({ code: "custom", path: ["proxy_password"], message: "密码不能为空" });
    }
    if (proto === "trojan" && !data.proxy_password) {
      ctx.addIssue({ code: "custom", path: ["proxy_password"], message: "密码不能为空" });
    }
  }
  if (data.tunnel_type === "proxy" && data.edit_mode === "json") {
    if (!data.proxy_config_json) {
      ctx.addIssue({ code: "custom", path: ["proxy_config_json"], message: "JSON 配置不能为空" });
    } else {
      try { JSON.parse(data.proxy_config_json); }
      catch { ctx.addIssue({ code: "custom", path: ["proxy_config_json"], message: "JSON 格式不正确" }); }
    }
  }
});
```

**2. 更新 defaultValues**，添加：
```typescript
tunnel_type: "wireguard",  // per D-03
proxy_protocol: "socks",   // per D-05
proxy_server: "",
proxy_port: undefined,
proxy_username: "",
proxy_password: "",
proxy_uuid: "",
proxy_security: "auto",
proxy_alter_id: 0,
proxy_tls: false,
proxy_server_name: "",
proxy_method: "",
edit_mode: "form",
proxy_config_json: "",
```

**3. 更新编辑模式的 useEffect**，当 `mode === "edit" && ipData?.egress_ip` 时：
- 设置 `tunnel_type: ip.tunnel_type || "wireguard"` (per D-04)
- 如果 `ip.tunnel_type === "proxy" && ip.proxy_config`：调用 `proxyConfigToFormValues(ip.proxy_config)` 填充代理字段；同时设置 `proxy_config_json: JSON.stringify(ip.proxy_config, null, 2)`
- 如果密码字段值为 `"***"`，替换为空字符串（placeholder 提示用户）

**4. 在表单 JSX 中**，在 `{mode === "edit" && ...状态选择...}` 之后、`<Separator />` 之前，添加隧道类型选择器 (per D-01)：
```tsx
<div className="space-y-2">
  <Label>隧道类型</Label>
  <Select
    value={form.watch("tunnel_type")}
    onValueChange={(val) => form.setValue("tunnel_type", val as "wireguard" | "proxy")}
  >
    <SelectTrigger>
      <SelectValue />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value="wireguard">WireGuard</SelectItem>
      <SelectItem value="proxy">代理协议</SelectItem>
    </SelectContent>
  </Select>
</div>
```

**5. 用条件渲染替换固定的 WG 字段块** (per D-02)：
```tsx
{tunnelType === "wireguard" && (
  <>
    <Separator />
    <p className="text-sm font-medium text-muted-foreground">WireGuard 配置（可选）</p>
    {/* 保留现有的 6 个 WG 字段 */}
  </>
)}
{tunnelType === "proxy" && (
  <>
    <Separator />
    <p className="text-sm font-medium text-muted-foreground">代理协议配置</p>
    <ProxyFields form={form} />
  </>
)}
```
其中 `tunnelType` 通过 `const tunnelType = form.watch("tunnel_type");` 获得。

**6. 更新 onSubmit 函数**：
```typescript
function onSubmit(values: FormValues) {
  let payload: Record<string, unknown> = {
    label: values.label,
    ip_address: values.ip_address,
    provider: values.provider || "manual",
    tunnel_type: values.tunnel_type,
  };

  if (mode === "edit") {
    payload.status = values.status;
  }

  if (values.tunnel_type === "wireguard") {
    payload = {
      ...payload,
      wg_endpoint: values.wg_endpoint || null,
      wg_public_key: values.wg_public_key || null,
      wg_preshared_key: values.wg_preshared_key || null,
      wg_allowed_ips: values.wg_allowed_ips || "0.0.0.0/0",
      wg_dns_server: values.wg_dns_server || null,
      wg_peer_address: values.wg_peer_address || null,
      proxy_config: null,
    };
  } else {
    // Proxy mode
    let proxyConfig: Record<string, unknown>;
    if (values.edit_mode === "json") {
      proxyConfig = JSON.parse(values.proxy_config_json!);
    } else {
      proxyConfig = formValuesToProxyConfig(values);
    }
    // Strip "***" passwords — backend will merge with original
    if (proxyConfig.password === "***" || proxyConfig.password === "") {
      delete proxyConfig.password;
    }
    payload = {
      ...payload,
      proxy_config: proxyConfig,
      wg_endpoint: null,
      wg_public_key: null,
      wg_preshared_key: null,
      wg_allowed_ips: "0.0.0.0/0",
      wg_dns_server: null,
      wg_peer_address: null,
    };
  }
  // ... existing create/update mutation calls with payload
}
```

**7. 添加 import：** `import { ProxyFields, formValuesToProxyConfig, proxyConfigToFormValues } from "./proxy-fields";`

**后端 — 密码合并逻辑：**

**8. 在 admin_egress_ips.go 的 Update() handler 中**，在 `validateProxyConfig(req.ProxyConfig)` 调用之前，添加密码合并：
```go
if req.TunnelType == network.TunnelTypeProxy {
    // Merge password if masked
    req.ProxyConfig = mergeProxyPassword(r.Context(), h.store, ipID, req.ProxyConfig)
    if err := validateProxyConfig(req.ProxyConfig); err != nil {
        // ...
    }
}
```

**9. 添加 mergeProxyPassword 函数**（在 admin_egress_ips.go 中，validateProxyConfig 之后）：
```go
func mergeProxyPassword(ctx context.Context, store AdminEgressIPStore, ipID string, incoming json.RawMessage) json.RawMessage {
    var newCfg map[string]any
    if err := json.Unmarshal(incoming, &newCfg); err != nil {
        return incoming
    }

    pwd, hasPwd := newCfg["password"]
    if !hasPwd {
        // No password field in request — fetch original and merge
        existing, err := store.GetEgressIP(ctx, ipID)
        if err != nil || existing.ProxyConfig == nil {
            return incoming
        }
        var oldCfg map[string]any
        if err := json.Unmarshal(existing.ProxyConfig, &oldCfg); err != nil {
            return incoming
        }
        if origPwd, ok := oldCfg["password"]; ok {
            newCfg["password"] = origPwd
            merged, _ := json.Marshal(newCfg)
            return merged
        }
    } else if pwd == "***" {
        // Masked password — replace with original
        existing, err := store.GetEgressIP(ctx, ipID)
        if err != nil || existing.ProxyConfig == nil {
            return incoming
        }
        var oldCfg map[string]any
        if err := json.Unmarshal(existing.ProxyConfig, &oldCfg); err != nil {
            return incoming
        }
        if origPwd, ok := oldCfg["password"]; ok {
            newCfg["password"] = origPwd
            merged, _ := json.Marshal(newCfg)
            return merged
        }
    }
    return incoming
}
```
  </action>
  <verify>
    <automated>go build ./internal/controlplane/... && cd web/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - egress-ip-drawer.tsx contains `tunnel_type: z.enum(["wireguard", "proxy"])`
    - egress-ip-drawer.tsx contains `superRefine`
    - egress-ip-drawer.tsx contains `form.watch("tunnel_type")`
    - egress-ip-drawer.tsx contains `tunnelType === "wireguard"`
    - egress-ip-drawer.tsx contains `tunnelType === "proxy"`
    - egress-ip-drawer.tsx contains `<ProxyFields`
    - egress-ip-drawer.tsx contains `import.*ProxyFields.*from.*proxy-fields`
    - egress-ip-drawer.tsx contains `formValuesToProxyConfig`
    - egress-ip-drawer.tsx contains `proxy_config: null` (WG branch clears proxy_config)
    - admin_egress_ips.go contains `func mergeProxyPassword`
    - admin_egress_ips.go contains `mergeProxyPassword` called in Update handler
    - `go build ./internal/controlplane/...` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>EgressIPDrawer 表单根据 tunnel_type 动态切换 WG/代理字段组，编辑模式正确回填代理配置，后端 Update handler 在密码为 "***" 或缺失时自动保留原密码</done>
</task>

</tasks>

<verification>
- `go build ./internal/controlplane/...` 编译通过
- `cd web/admin && npx tsc --noEmit` 类型检查通过
- egress-ip-drawer.tsx 使用 superRefine 实现条件校验
- egress-ip-drawer.tsx 根据 tunnel_type watch 条件渲染 WG/Proxy 字段
- proxy-fields.tsx 包含 5 种协议的字段渲染组件
- proxy-fields.tsx 包含表单/JSON 模式切换和双向转换
- admin_egress_ips.go Update handler 包含密码合并逻辑
</verification>

<success_criteria>
1. 创建模式默认选中 WireGuard（per D-03），显示现有 WG 字段
2. 切换到"代理协议"后显示协议选择器和对应字段（per D-01, D-02）
3. 协议选择器默认 SOCKS5（per D-05），包含全部 5 种协议
4. 各协议字段完整实现 D-06 字段映射表
5. server 和 port 始终在协议专有字段之前显示（per D-07）
6. 密码字段使用 password 类型 + Eye/EyeOff 切换（per D-08）
7. 表单/JSON 模式切换按钮工作正常（per D-09）
8. JSON 模式使用 monospace textarea（per D-10）
9. 表单→JSON 和 JSON→表单双向转换正确（per D-11, D-12）
10. 提交时统一用 JSON 格式 proxy_config（per D-13）
11. 编辑模式下密码 "***" 不会覆盖原值（后端合并）
12. 编辑模式根据已保存 tunnel_type 回填对应字段（per D-04）
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-proxy-test/09-02-SUMMARY.md`
</output>
