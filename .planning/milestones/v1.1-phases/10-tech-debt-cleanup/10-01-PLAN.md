---
phase: 10-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/tasks/worker.go
  - internal/controlplane/http/admin_egress_ip_probe.go
autonomous: true
requirements: [SC-1, SC-2]

must_haves:
  truths:
    - "stopHost 成功后宿主机侧不再遗留 mgmt-{hostID[:8]} veth 对"
    - "vmess/ss/trojan 代理测试在 sing-box 不可用时返回明确的中文提示而非模糊错误"
  artifacts:
    - path: "internal/runtime/tasks/worker.go"
      provides: "stopHost 在 docker stop 后调用 CleanupHost"
      contains: "w.provider.CleanupHost"
    - path: "internal/controlplane/http/admin_egress_ip_probe.go"
      provides: "getProxyDialer 中 vmess/ss/trojan 分支入口的 LookPath 预检"
      contains: "exec.LookPath"
  key_links:
    - from: "internal/runtime/tasks/worker.go:stopHost"
      to: "RoutingProvider.CleanupHost"
      via: "w.provider.CleanupHost(ctx, network.HostNetworkSpec{HostID: request.HostID})"
      pattern: "CleanupHost.*HostNetworkSpec"
    - from: "internal/controlplane/http/admin_egress_ip_probe.go:getProxyDialer"
      to: "exec.LookPath"
      via: "LookPath(\"sing-box\") 预检"
      pattern: "exec\\.LookPath.*sing-box"
---

<objective>
修复后端两项技术债务：(1) stopHost 路径遗漏 CleanupHost 导致宿主机侧 mgmt veth 残留；(2) 代理测试 API 中 vmess/ss/trojan 路径依赖 sing-box 二进制但控制面未安装时错误提示不明确。

Purpose: 提升运行时健壮性，消除网络资源泄漏和排障成本。
Output: 修补后的 worker.go 和 admin_egress_ip_probe.go。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-RESEARCH.md

<interfaces>
From internal/runtime/tasks/worker.go:
```go
type Worker struct {
    // ...
    provider network.NetworkProvider
    // ...
}

func (w *Worker) stopHost(ctx context.Context, request agentapi.HostActionRequest) error
func (w *Worker) rebuildHost(ctx context.Context, request agentapi.HostActionRequest) error
```

From internal/network (provider interface):
```go
type NetworkProvider interface {
    PrepareHost(ctx context.Context, spec HostNetworkSpec) error
    CleanupHost(ctx context.Context, spec HostNetworkSpec) error
}
type HostNetworkSpec struct {
    HostID string
    // ...
}
```

From internal/controlplane/http/admin_egress_ip_probe.go:
```go
func getProxyDialer(ctx context.Context, proxyConfig json.RawMessage) (dialer contextDialer, cleanup func(), err error)
func startLocalSingBox(ctx context.Context, proxyConfig json.RawMessage) (port int, cleanup func(), err error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: stopHost 追加 CleanupHost 调用</name>
  <files>internal/runtime/tasks/worker.go</files>
  <read_first>
    - internal/runtime/tasks/worker.go
  </read_first>
  <action>
修改 `stopHost` 函数（约第 195-204 行），在 `w.runDocker(ctx, "stop", containerName)` 成功返回后追加 `CleanupHost` 调用。

当前代码：
```go
func (w *Worker) stopHost(ctx context.Context, request agentapi.HostActionRequest) error {
    containerName := firstNonEmpty(request.ContainerName, containerNameForHost(request.HostID))
    if exists, err := w.containerExists(ctx, containerName); err != nil {
        return err
    } else if !exists {
        return nil
    }
    return w.runDocker(ctx, "stop", containerName)
}
```

修改为：
```go
func (w *Worker) stopHost(ctx context.Context, request agentapi.HostActionRequest) error {
    containerName := firstNonEmpty(request.ContainerName, containerNameForHost(request.HostID))
    if exists, err := w.containerExists(ctx, containerName); err != nil {
        return err
    } else if !exists {
        return nil
    }

    if err := w.runDocker(ctx, "stop", containerName); err != nil {
        return err
    }

    if err := w.provider.CleanupHost(ctx, network.HostNetworkSpec{HostID: request.HostID}); err != nil {
        return fmt.Errorf("cleanup host network after stop: %w", err)
    }

    return nil
}
```

关键点：
- 参照 `rebuildHost`（第 229 行）中已有的 `w.provider.CleanupHost` 调用模式
- `RoutingProvider.CleanupHost` 内部对 TunnelProvider 和 SingBoxProvider 都做防御性清理
- 容器已停时 sing-box pkill 会无效但不报错（PID 为 0 的守卫检查已存在），mgmt veth 清理是关键目标
- 不需要额外 import，`network` 和 `fmt` 已在文件顶部导入
  </action>
  <verify>
    <automated>go build ./internal/runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - worker.go 中 `stopHost` 函数包含 `w.provider.CleanupHost(ctx, network.HostNetworkSpec{HostID: request.HostID})`
    - worker.go 中 `stopHost` 函数包含 `fmt.Errorf("cleanup host network after stop: %w", err)`
    - `go build ./internal/runtime/...` 编译通过
  </acceptance_criteria>
  <done>stopHost 在 docker stop 成功后调用 CleanupHost 清理宿主机侧网络残留</done>
</task>

<task type="auto">
  <name>Task 2: getProxyDialer 添加 sing-box LookPath 预检</name>
  <files>internal/controlplane/http/admin_egress_ip_probe.go</files>
  <read_first>
    - internal/controlplane/http/admin_egress_ip_probe.go
  </read_first>
  <action>
修改 `getProxyDialer` 函数中 `case "vmess", "shadowsocks", "trojan":` 分支（约第 106 行），在调用 `startLocalSingBox` 之前添加 `exec.LookPath("sing-box")` 预检。

当前代码：
```go
case "vmess", "shadowsocks", "trojan":
    localPort, singboxCleanup, startErr := startLocalSingBox(ctx, proxyConfig)
    if startErr != nil {
        return nil, nil, startErr
    }
```

修改为：
```go
case "vmess", "shadowsocks", "trojan":
    if _, lookErr := exec.LookPath("sing-box"); lookErr != nil {
        return nil, nil, fmt.Errorf("sing-box 未安装，无法测试 %s 协议（需在控制面环境安装 sing-box）", outboundType)
    }
    localPort, singboxCleanup, startErr := startLocalSingBox(ctx, proxyConfig)
    if startErr != nil {
        return nil, nil, startErr
    }
```

关键点：
- `exec` 和 `fmt` 已在文件顶部导入（第 14 行 `"os/exec"`，第 9 行 `"fmt"`）
- 当 sing-box 不在 PATH 中时，原代码在 `cmd.Start()` 失败后返回 `"sing-box binary not found, cannot test %s protocol: %w"` 错误，新代码在进入 `startLocalSingBox` 之前即返回更明确的中文错误信息
- 错误消息使用中文，明确告知"控制面环境未安装 sing-box"，降低排障成本
  </action>
  <verify>
    <automated>go build ./internal/controlplane/...</automated>
  </verify>
  <acceptance_criteria>
    - admin_egress_ip_probe.go 中 `case "vmess", "shadowsocks", "trojan":` 分支包含 `exec.LookPath("sing-box")`
    - admin_egress_ip_probe.go 中包含错误信息字符串 `"sing-box 未安装，无法测试 %s 协议（需在控制面环境安装 sing-box）"`
    - `go build ./internal/controlplane/...` 编译通过
  </acceptance_criteria>
  <done>vmess/ss/trojan 代理测试在 sing-box 不可用时返回结构化中文错误提示，而非 cmd.Start 的模糊错误</done>
</task>

</tasks>

<verification>
1. `go build ./...` 全项目编译通过
2. `go vet ./internal/runtime/... ./internal/controlplane/...` 无警告
3. stopHost 函数中 CleanupHost 调用与 rebuildHost 中的模式一致
4. getProxyDialer 中 LookPath 预检在 startLocalSingBox 之前执行
</verification>

<success_criteria>
- stopHost 在 docker stop 后执行 CleanupHost，与 rebuildHost 行为对齐
- sing-box 不可用时代理测试返回明确中文提示而非底层错误
- 全项目编译通过（`go build ./...`）
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md`
</output>
