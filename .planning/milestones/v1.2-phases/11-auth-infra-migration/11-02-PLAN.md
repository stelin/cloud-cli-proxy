---
phase: 11-auth-infra-migration
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - internal/controlplane/http/auth_handler.go
  - internal/controlplane/http/auth_middleware.go
  - internal/controlplane/http/admin_auth.go
  - internal/controlplane/http/entry.go
  - internal/controlplane/http/router.go
  - internal/controlplane/app/app.go
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "POST /v1/auth/login 接受 short_id + password，返回带 role 的 JWT"
    - "旧 POST /v1/admin/login 继续可用（兼容）"
    - "AuthMiddleware 从 JWT 提取 user_id 和 role 注入 context"
    - "RequireRole 中间件可限制只有指定角色访问"
    - "entry.go 使用 bcrypt 比对 password_hash 而非明文"
    - "应用启动时自动创建种子管理员（从环境变量读取）"
  artifacts:
    - path: "internal/controlplane/http/auth_handler.go"
      provides: "UnifiedLoginHandler 统一登录端点"
      exports: ["NewUnifiedLoginHandler"]
    - path: "internal/controlplane/http/auth_middleware.go"
      provides: "AuthMiddleware + RequireRole + context 辅助函数"
      exports: ["AuthMiddleware", "RequireRole", "UserIDFromContext", "RoleFromContext"]
    - path: "internal/controlplane/http/router.go"
      provides: "/v1/auth/login 路由注册"
      contains: "/v1/auth/login"
    - path: "internal/controlplane/app/app.go"
      provides: "ensureSeedAdmin 启动逻辑"
      contains: "ensureSeedAdmin"
  key_links:
    - from: "internal/controlplane/http/auth_handler.go"
      to: "internal/store/repository/queries.go"
      via: "GetUserByShortIDForAuth"
      pattern: "GetUserByShortIDForAuth"
    - from: "internal/controlplane/http/auth_middleware.go"
      to: "internal/controlplane/http/auth.go"
      via: "AuthClaims 结构体解析 JWT"
      pattern: "AuthClaims"
    - from: "internal/controlplane/http/router.go"
      to: "internal/controlplane/http/auth_handler.go"
      via: "注册统一登录端点"
      pattern: "/v1/auth/login"
    - from: "internal/controlplane/app/app.go"
      to: "internal/store/repository/queries.go"
      via: "CreateUserWithRole 创建种子管理员"
      pattern: "CreateUserWithRole"
---

<objective>
统一登录端点、认证中间件和种子管理员：实现 /v1/auth/login 端点，创建通用 AuthMiddleware 替代 AdminAuthMiddleware，改造 entry.go 废弃明文密码，应用启动时自动种子管理员。

Purpose: 完成后端认证统一，所有角色通过同一端点登录获取带 role 的 JWT，资源隔离中间件就绪。
Output: auth_handler.go, auth_middleware.go，改造后的 admin_auth.go/entry.go/router.go/app.go
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-auth-infra-migration/11-CONTEXT.md
@.planning/phases/11-auth-infra-migration/11-RESEARCH.md
@.planning/phases/11-auth-infra-migration/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的类型，本 Plan 直接使用 -->

From internal/controlplane/http/auth.go (Plan 01 创建):
```go
const BcryptCost = 10

type AuthClaims struct {
    jwt.RegisteredClaims
    UserID string `json:"user_id"`
    Role   string `json:"role"`
}

func GenerateAuthToken(secret []byte, userID, role string, expiry time.Duration) (string, error)
```

From internal/store/repository/models.go (Plan 01 扩展):
```go
type User struct {
    ID            string     `json:"id"`
    Username      string     `json:"username"`
    Status        string     `json:"status"`
    Role          string     `json:"role"`
    ShortID       string     `json:"short_id"`
    PasswordHash  string     `json:"password_hash,omitempty"`
    EntryPassword string     `json:"entry_password,omitempty"`
    ExpiresAt     *time.Time `json:"expires_at,omitempty"`
    CreatedAt     time.Time  `json:"created_at"`
    UpdatedAt     time.Time  `json:"updated_at"`
}

type CreateUserWithRoleParams struct {
    Username     string
    PasswordHash string
    ShortID      string
    Role         string
}
```

From internal/store/repository/queries.go (Plan 01 新增):
```go
func (r *Repository) GetUserByShortIDForAuth(ctx context.Context, shortID string) (User, error)
func (r *Repository) CreateUserWithRole(ctx context.Context, params CreateUserWithRoleParams) (User, error)
```

From internal/controlplane/http/router.go (现有):
```go
type Dependencies struct { ... }
func NewRouter(deps Dependencies) nethttp.Handler
```

From internal/controlplane/app/app.go (现有):
```go
type Config struct {
    AdminUsername  string
    AdminPassword  string
    AdminJWTSecret string
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一登录 Handler + 认证中间件</name>
  <files>
    internal/controlplane/http/auth_handler.go,
    internal/controlplane/http/auth_middleware.go
  </files>
  <read_first>
    internal/controlplane/http/auth.go,
    internal/controlplane/http/admin_auth.go,
    internal/controlplane/http/bootstrap_auth.go,
    internal/store/repository/models.go,
    internal/store/repository/queries.go
  </read_first>
  <action>
1. 创建 `internal/controlplane/http/auth_handler.go`（per D-01, D-02）：

```go
package http

import (
    "context"
    "encoding/json"
    "errors"
    "log/slog"
    nethttp "net/http"
    "time"

    "github.com/jackc/pgx/v5"
    "golang.org/x/crypto/bcrypt"

    "github.com/zaneliu/cloud-cli-proxy/internal/store/repository"
)

// AuthUserStore 统一登录所需的 store 接口
type AuthUserStore interface {
    GetUserByShortIDForAuth(context.Context, string) (repository.User, error)
}

type UnifiedLoginHandler struct {
    store     AuthUserStore
    jwtSecret []byte
    logger    *slog.Logger
}

func NewUnifiedLoginHandler(logger *slog.Logger, store AuthUserStore, jwtSecret []byte) *UnifiedLoginHandler {
    return &UnifiedLoginHandler{store: store, jwtSecret: jwtSecret, logger: logger}
}

type unifiedLoginRequest struct {
    ShortID  string `json:"short_id"`
    Password string `json:"password"`
    // 兼容旧前端发送 username 字段
    Username string `json:"username"`
}

func (h *UnifiedLoginHandler) ServeHTTP(w nethttp.ResponseWriter, r *nethttp.Request) {
    if r.Method != nethttp.MethodPost {
        writeJSON(w, nethttp.StatusMethodNotAllowed, map[string]string{"error": "method not allowed"})
        return
    }

    var req unifiedLoginRequest
    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
        writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "invalid request body"})
        return
    }

    // 兼容：如果 short_id 为空但 username 有值，用 username 作为 short_id
    shortID := req.ShortID
    if shortID == "" {
        shortID = req.Username
    }
    if shortID == "" || req.Password == "" {
        writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "short_id and password are required"})
        return
    }

    user, err := h.store.GetUserByShortIDForAuth(r.Context(), shortID)
    if err != nil {
        if errors.Is(err, pgx.ErrNoRows) {
            writeJSON(w, nethttp.StatusUnauthorized, map[string]string{"error": "invalid credentials"})
            return
        }
        h.logger.Error("unified login: lookup user failed", "short_id", shortID, "error", err)
        writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "internal error"})
        return
    }

    if user.Status != "active" {
        writeJSON(w, nethttp.StatusForbidden, map[string]string{"error": "account is not active"})
        return
    }

    if err := bcrypt.CompareHashAndPassword([]byte(user.PasswordHash), []byte(req.Password)); err != nil {
        writeJSON(w, nethttp.StatusUnauthorized, map[string]string{"error": "invalid credentials"})
        return
    }

    tokenStr, err := GenerateAuthToken(h.jwtSecret, user.ID, user.Role, 24*time.Hour)
    if err != nil {
        h.logger.Error("unified login: sign jwt failed", "error", err)
        writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "token generation failed"})
        return
    }

    writeJSON(w, nethttp.StatusOK, map[string]any{
        "token":      tokenStr,
        "role":       user.Role,
        "expires_in": 86400,
    })
}
```

2. 创建 `internal/controlplane/http/auth_middleware.go`（per D-06, D-13, D-14）：

```go
package http

import (
    "context"
    nethttp "net/http"
    "strings"

    "github.com/golang-jwt/jwt/v5"
)

type contextKey string

const (
    ctxKeyUserID contextKey = "user_id"
    ctxKeyRole   contextKey = "role"
)

// UserIDFromContext 从请求 context 获取当前用户 ID
func UserIDFromContext(ctx context.Context) string {
    v, _ := ctx.Value(ctxKeyUserID).(string)
    return v
}

// RoleFromContext 从请求 context 获取当前用户角色
func RoleFromContext(ctx context.Context) string {
    v, _ := ctx.Value(ctxKeyRole).(string)
    return v
}

// AuthMiddleware 通用 JWT 认证中间件，从 token 提取 user_id 和 role 注入 context
func AuthMiddleware(secret []byte) func(nethttp.Handler) nethttp.Handler {
    return func(next nethttp.Handler) nethttp.Handler {
        return nethttp.HandlerFunc(func(w nethttp.ResponseWriter, r *nethttp.Request) {
            tokenStr := extractToken(r)
            if tokenStr == "" {
                writeJSON(w, nethttp.StatusUnauthorized, map[string]string{"error": "missing or invalid authorization header"})
                return
            }

            claims := &AuthClaims{}
            token, err := jwt.ParseWithClaims(tokenStr, claims, func(t *jwt.Token) (any, error) {
                if _, ok := t.Method.(*jwt.SigningMethodHMAC); !ok {
                    return nil, jwt.ErrSignatureInvalid
                }
                return secret, nil
            }, jwt.WithValidMethods([]string{"HS256"}))

            if err != nil || !token.Valid {
                writeJSON(w, nethttp.StatusUnauthorized, map[string]string{"error": "invalid token"})
                return
            }

            // 防御性检查：旧 token 可能没有 role claim
            if claims.Role == "" {
                writeJSON(w, nethttp.StatusUnauthorized, map[string]string{"error": "token missing role claim, please re-login"})
                return
            }

            ctx := context.WithValue(r.Context(), ctxKeyUserID, claims.UserID)
            ctx = context.WithValue(ctx, ctxKeyRole, claims.Role)

            // 当通过 query param 认证时，设置 cookie（兼容 VNC 代理等场景）
            if r.URL.Query().Get("token") != "" {
                nethttp.SetCookie(w, &nethttp.Cookie{
                    Name:     "admin_token",
                    Value:    tokenStr,
                    Path:     "/v1/",
                    HttpOnly: true,
                    SameSite: nethttp.SameSiteLaxMode,
                    MaxAge:   86400,
                })
            }

            next.ServeHTTP(w, r.WithContext(ctx))
        })
    }
}

// RequireRole 角色限制中间件，只允许指定角色通过
func RequireRole(roles ...string) func(nethttp.Handler) nethttp.Handler {
    return func(next nethttp.Handler) nethttp.Handler {
        return nethttp.HandlerFunc(func(w nethttp.ResponseWriter, r *nethttp.Request) {
            role := RoleFromContext(r.Context())
            for _, allowed := range roles {
                if role == allowed {
                    next.ServeHTTP(w, r)
                    return
                }
            }
            writeJSON(w, nethttp.StatusForbidden, map[string]string{"error": "insufficient permissions"})
        })
    }
}

// extractToken 从 Authorization header / query param / cookie 中提取 token
func extractToken(r *nethttp.Request) string {
    if auth := r.Header.Get("Authorization"); strings.HasPrefix(auth, "Bearer ") {
        return strings.TrimPrefix(auth, "Bearer ")
    }
    if t := r.URL.Query().Get("token"); t != "" {
        return t
    }
    if c, err := r.Cookie("admin_token"); err == nil && c.Value != "" {
        return c.Value
    }
    return ""
}
```
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy && go build ./internal/controlplane/http/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "type UnifiedLoginHandler struct" internal/controlplane/http/auth_handler.go
    - grep -q "NewUnifiedLoginHandler" internal/controlplane/http/auth_handler.go
    - grep -q "GetUserByShortIDForAuth" internal/controlplane/http/auth_handler.go
    - grep -q "GenerateAuthToken" internal/controlplane/http/auth_handler.go
    - grep -q '"role":.*user.Role' internal/controlplane/http/auth_handler.go
    - grep -q "func AuthMiddleware" internal/controlplane/http/auth_middleware.go
    - grep -q "func RequireRole" internal/controlplane/http/auth_middleware.go
    - grep -q "func UserIDFromContext" internal/controlplane/http/auth_middleware.go
    - grep -q "func RoleFromContext" internal/controlplane/http/auth_middleware.go
    - grep -q "ctxKeyUserID" internal/controlplane/http/auth_middleware.go
    - grep -q "ctxKeyRole" internal/controlplane/http/auth_middleware.go
    - go build ./internal/controlplane/http/... 成功
  </acceptance_criteria>
  <done>UnifiedLoginHandler 接受 short_id + password 返回带 role 的 JWT；AuthMiddleware 解析 JWT 注入 context；RequireRole 可限制角色访问</done>
</task>

<task type="auto">
  <name>Task 2: 路由注册 + AdminAuth 替换 + entry.go bcrypt 改造 + 种子管理员</name>
  <files>
    internal/controlplane/http/router.go,
    internal/controlplane/http/admin_auth.go,
    internal/controlplane/http/entry.go,
    internal/controlplane/app/app.go
  </files>
  <read_first>
    internal/controlplane/http/router.go,
    internal/controlplane/http/admin_auth.go,
    internal/controlplane/http/entry.go,
    internal/controlplane/app/app.go,
    internal/controlplane/http/auth_handler.go,
    internal/controlplane/http/auth_middleware.go
  </read_first>
  <action>
1. **router.go 改造**（per D-01, D-06）：
   - Dependencies 结构体新增 `AuthStore AuthUserStore` 字段
   - 在 `if deps.Admin != nil` 块内：
     a. 创建统一登录 handler：`loginHandler := NewUnifiedLoginHandler(deps.Logger, deps.AuthStore, deps.Admin.JWTSecret)`
     b. 注册新端点：`mux.Handle("POST /v1/auth/login", loginHandler)`
     c. 保留 `/v1/admin/login` 指向同一 handler 做兼容：`mux.Handle("POST /v1/admin/login", loginHandler)`
     d. 将 `adminAuth := AdminAuthMiddleware(deps.Admin.JWTSecret)` 替换为 `adminAuth := AuthMiddleware(deps.Admin.JWTSecret)`（用新的通用中间件替代旧的）
     e. 在所有 admin 路由前再套一层 RequireRole("admin")，即 `adminAuth` 之后再用 `adminRequire := RequireRole("admin")`，路由注册改为 `adminRequire(adminAuth(handler))`。或者更简洁的方式：创建一个组合函数 `adminGuard` 先 AuthMiddleware 再 RequireRole("admin")
   - 删除原来的 `NewAdminLoginHandler` 调用

2. **admin_auth.go 改造**：
   - 删除 `AdminLoginHandler`、`NewAdminLoginHandler`、`adminLoginRequest` 和 `AdminLoginHandler.ServeHTTP` 方法（登录逻辑已移到 auth_handler.go）
   - 删除 `AdminAuthMiddleware` 函数（认证逻辑已移到 auth_middleware.go 的 AuthMiddleware）
   - 文件可以完全清空或只保留 package 声明（如果没有其他内容）；如果觉得删文件更干净，直接删除文件（但需确认没有其他代码引用此文件的导出符号）

3. **entry.go 改造**（per D-07, D-08）：
   - `EntryStore` 接口：将 `GetUserByShortID` 改为返回 `repository.User`（已经是，确认 User 现在包含 PasswordHash）
   - `Auth()` 方法中：
     a. 将 `if user.EntryPassword != body.Password` 替换为 `if err := bcrypt.CompareHashAndPassword([]byte(user.PasswordHash), []byte(body.Password)); err != nil`
     b. 添加 `"golang.org/x/crypto/bcrypt"` import
     c. 不再读取 user.EntryPassword

4. **app.go 改造**（per D-03, D-05）：
   - 新增 `ensureSeedAdmin` 方法：
     ```go
     func (a *App) ensureSeedAdmin(ctx context.Context) error {
         if a.cfg.AdminUsername == "" || a.cfg.AdminPassword == "" {
             a.logger.Warn("seed admin: ADMIN_USERNAME or ADMIN_PASSWORD not set, skipping")
             return nil
         }
         _, err := a.repo.GetUserByShortIDForAuth(ctx, a.cfg.AdminUsername)
         if err == nil {
             return nil // 已存在
         }
         hash, err := bcrypt.GenerateFromPassword([]byte(a.cfg.AdminPassword), BcryptCost)
         if err != nil {
             return fmt.Errorf("hash admin password: %w", err)
         }
         _, err = a.repo.CreateUserWithRole(ctx, repository.CreateUserWithRoleParams{
             Username:     a.cfg.AdminUsername,
             PasswordHash: string(hash),
             ShortID:      a.cfg.AdminUsername,
             Role:         "admin",
         })
         if err != nil {
             return fmt.Errorf("create seed admin: %w", err)
         }
         a.logger.Info("seed admin created", "short_id", a.cfg.AdminUsername)
         return nil
     }
     ```
   - 在 `Run()` 方法中迁移完成后、启动 HTTP 服务前调用 `a.ensureSeedAdmin(ctx)`
   - 添加必要的 imports：`"golang.org/x/crypto/bcrypt"` 和对 `BcryptCost` 的引用（来自 http 包：`cphttp.BcryptCost`，或者直接用 `bcrypt.DefaultCost`）
   - Dependencies 构造时新增 `AuthStore: a.repo`

注意 BcryptCost 常量在 http 包中定义。app.go 在 app 包中，跨包引用需要用 `cphttp.BcryptCost`。如果觉得不合适，可以将常量移到 repository 包或直接在 app.go 中使用 `bcrypt.DefaultCost`。
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy && go build ./internal/... && go vet ./internal/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "/v1/auth/login" internal/controlplane/http/router.go
    - grep -q "AuthMiddleware" internal/controlplane/http/router.go
    - grep -q "RequireRole" internal/controlplane/http/router.go
    - grep -q "AuthStore" internal/controlplane/http/router.go
    - grep -rL "AdminAuthMiddleware" internal/controlplane/http/ | grep -v _test（确认 AdminAuthMiddleware 不再被任何非测试文件引用）
    - grep -q "bcrypt.CompareHashAndPassword" internal/controlplane/http/entry.go
    - grep -vq "EntryPassword.*body.Password" internal/controlplane/http/entry.go（确认明文比对已删除）
    - grep -q "ensureSeedAdmin" internal/controlplane/app/app.go
    - grep -q "CreateUserWithRole" internal/controlplane/app/app.go
    - go build ./internal/... 成功
    - go vet ./internal/... 无错误
  </acceptance_criteria>
  <done>统一登录端点 /v1/auth/login 注册并兼容旧 /v1/admin/login；AdminAuthMiddleware 被 AuthMiddleware + RequireRole("admin") 替代；entry.go 使用 bcrypt；种子管理员在启动时自动创建</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` 编译通过
- `go vet ./internal/...` 无错误
- /v1/auth/login 端点在 router.go 中注册
- AdminAuthMiddleware 已从路由中移除，替换为 AuthMiddleware
- entry.go 不再使用明文密码比对
- app.go 包含 ensureSeedAdmin 逻辑
</verification>

<success_criteria>
- 统一登录端点接受 short_id + password，查 users 表 bcrypt 比对，返回 {token, role, expires_in}
- 通用 AuthMiddleware 从 JWT 提取 user_id + role 注入 context
- RequireRole 中间件限制管理员 API 只允许 admin 角色
- entry.go 使用 bcrypt 比对 password_hash（per D-07, D-08）
- 应用启动时从环境变量创建种子管理员（per D-03, D-05）
- 整个项目 `go build ./internal/...` 和 `go vet ./internal/...` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infra-migration/11-02-SUMMARY.md`
</output>
