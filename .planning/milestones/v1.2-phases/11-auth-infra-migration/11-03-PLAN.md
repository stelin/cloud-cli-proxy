---
phase: 11-auth-infra-migration
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - web/admin/src/lib/auth.ts
  - web/admin/src/routes/login.tsx
  - web/admin/src/routes/_dashboard.tsx
  - web/admin/src/routes/_portal.tsx
  - web/admin/src/routes/_portal/index.tsx
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "登录页发送 short_id + password 到 /v1/auth/login"
    - "登录成功后 admin 角色跳转 /dashboard，user 角色跳转 /portal"
    - "管理员面板路由守卫拒绝 user 角色访问"
    - "用户面板骨架路由 /portal 存在且受认证保护"
    - "auth.ts 提供 getRole() 和 parseTokenPayload() 函数"
  artifacts:
    - path: "web/admin/src/lib/auth.ts"
      provides: "parseTokenPayload, getRole 函数"
      exports: ["parseTokenPayload", "getRole", "isAuthenticated", "getToken", "setToken", "clearToken", "logout"]
    - path: "web/admin/src/routes/login.tsx"
      provides: "统一登录页，按角色跳转"
      contains: "/v1/auth/login"
    - path: "web/admin/src/routes/_dashboard.tsx"
      provides: "管理员路由守卫（admin only）"
      contains: "getRole"
    - path: "web/admin/src/routes/_portal.tsx"
      provides: "用户面板布局骨架"
      contains: "_portal"
    - path: "web/admin/src/routes/_portal/index.tsx"
      provides: "用户面板首页占位"
      contains: "portal"
  key_links:
    - from: "web/admin/src/routes/login.tsx"
      to: "/v1/auth/login"
      via: "fetch POST"
      pattern: "fetch.*v1/auth/login"
    - from: "web/admin/src/routes/login.tsx"
      to: "web/admin/src/lib/auth.ts"
      via: "setToken + role 跳转"
      pattern: "setToken.*role"
    - from: "web/admin/src/routes/_dashboard.tsx"
      to: "web/admin/src/lib/auth.ts"
      via: "getRole 角色检查"
      pattern: "getRole"
---

<objective>
前端统一登录与角色路由：改造登录页发送 short_id 到统一端点，根据返回的 role 跳转不同面板，管理员路由守卫增加角色校验，创建用户面板骨架路由。

Purpose: 管理员和用户共用同一登录页，登录后自动跳转到对应面板（per D-11, D-12）。
Output: 改造后的 auth.ts/login.tsx/_dashboard.tsx，新增 _portal.tsx 和 _portal/index.tsx
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-auth-infra-migration/11-CONTEXT.md
@.planning/phases/11-auth-infra-migration/11-RESEARCH.md

<interfaces>
<!-- 现有前端代码 -->

From web/admin/src/lib/auth.ts (现有):
```typescript
export function isAuthenticated(): boolean {
  return !!localStorage.getItem("admin_token");
}
export function getToken(): string | null {
  return localStorage.getItem("admin_token");
}
export function setToken(token: string): void {
  localStorage.setItem("admin_token", token);
}
export function clearToken(): void {
  localStorage.removeItem("admin_token");
}
export function logout(): void {
  clearToken();
  window.location.href = "/login";
}
```

From web/admin/src/routes/login.tsx (现有):
```typescript
// 当前发送到 /v1/admin/login 带 username + password
// 成功后 navigate({ to: "/" })
const loginSchema = z.object({
  username: z.string().min(1, "用户名不能为空"),
  password: z.string().min(1, "密码不能为空"),
});
```

From web/admin/src/routes/_dashboard.tsx (现有):
```typescript
export const Route = createFileRoute("/_dashboard")({
  beforeLoad: () => {
    if (!isAuthenticated()) {
      throw redirect({ to: "/login" });
    }
  },
  component: DashboardLayout,
});
```

<!-- 后端统一登录响应格式（Plan 02 实现） -->
POST /v1/auth/login 响应:
```json
{ "token": "eyJ...", "role": "admin", "expires_in": 86400 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth.ts 扩展 + 登录页改造</name>
  <files>
    web/admin/src/lib/auth.ts,
    web/admin/src/routes/login.tsx
  </files>
  <read_first>
    web/admin/src/lib/auth.ts,
    web/admin/src/routes/login.tsx
  </read_first>
  <action>
1. **auth.ts** 扩展（保持现有函数不变，新增角色相关函数）：

在文件末尾新增：
```typescript
export function parseTokenPayload(): { user_id: string; role: string; sub: string; exp: number } | null {
  const token = getToken();
  if (!token) return null;
  try {
    const parts = token.split('.');
    if (parts.length !== 3) return null;
    const payload = JSON.parse(atob(parts[1]));
    return {
      user_id: payload.user_id || '',
      role: payload.role || '',
      sub: payload.sub || '',
      exp: payload.exp || 0,
    };
  } catch {
    return null;
  }
}

export function getRole(): string | null {
  return parseTokenPayload()?.role ?? null;
}

export function isAdmin(): boolean {
  return getRole() === 'admin';
}
```

2. **login.tsx** 改造（per D-11, D-12）：
   - schema 字段从 `username` 改为 `short_id`：
     ```typescript
     const loginSchema = z.object({
       short_id: z.string().min(1, "用户 ID 不能为空"),
       password: z.string().min(1, "密码不能为空"),
     });
     type LoginForm = z.infer<typeof loginSchema>;
     ```
   - fetch URL 从 `/v1/admin/login` 改为 `/v1/auth/login`
   - 请求 body 发送 `{ short_id, password }`（而非 username + password）
   - 返回类型扩展为 `Promise<{ token: string; role: string; expires_in: number }>`
   - onSuccess 回调：
     ```typescript
     onSuccess: (data) => {
       setToken(data.token);
       if (data.role === 'admin') {
         navigate({ to: '/' });
       } else {
         navigate({ to: '/portal' });
       }
     },
     ```
   - 表单 UI：Label 从 "用户名" 改为 "用户 ID"，Input id 从 "username" 改为 "short_id"，placeholder 从 "admin" 改为 "输入 Short ID"，register 从 `register("username")` 改为 `register("short_id")`
   - CardDescription 从 "管理后台" 改为 "登录"（因为现在管理员和用户共用）
   - import 中新增 `getRole`（虽然本文件暂不直接用，但保持一致性）
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy/web/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "parseTokenPayload" web/admin/src/lib/auth.ts
    - grep -q "getRole" web/admin/src/lib/auth.ts
    - grep -q "isAdmin" web/admin/src/lib/auth.ts
    - grep -q "/v1/auth/login" web/admin/src/routes/login.tsx
    - grep -q "short_id" web/admin/src/routes/login.tsx
    - grep -q "data.role" web/admin/src/routes/login.tsx
    - grep -q "/portal" web/admin/src/routes/login.tsx
    - grep -vq "/v1/admin/login" web/admin/src/routes/login.tsx（确认旧端点已移除）
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>auth.ts 提供 parseTokenPayload/getRole/isAdmin；登录页发送 short_id 到 /v1/auth/login 并按 role 跳转</done>
</task>

<task type="auto">
  <name>Task 2: 管理员路由守卫角色校验 + 用户面板骨架</name>
  <files>
    web/admin/src/routes/_dashboard.tsx,
    web/admin/src/routes/_portal.tsx,
    web/admin/src/routes/_portal/index.tsx
  </files>
  <read_first>
    web/admin/src/routes/_dashboard.tsx,
    web/admin/src/lib/auth.ts,
    web/admin/src/routes/__root.tsx,
    web/admin/src/components/layout/sidebar.tsx,
    web/admin/src/components/layout/topbar.tsx
  </read_first>
  <action>
1. **_dashboard.tsx** 改造（per D-12 — admin 跳转 /dashboard）：
   - import 中新增 `getRole`（从 `@/lib/auth`）
   - beforeLoad 中增加角色校验：
     ```typescript
     beforeLoad: () => {
       if (!isAuthenticated()) {
         throw redirect({ to: '/login' });
       }
       const role = getRole();
       if (role !== 'admin') {
         // 非管理员被重定向到用户面板
         throw redirect({ to: '/portal' });
       }
     },
     ```

2. 创建 `web/admin/src/routes/_portal.tsx`（用户面板布局骨架）：
   ```typescript
   import { createFileRoute, Outlet, redirect } from "@tanstack/react-router";
   import { isAuthenticated, getRole } from "@/lib/auth";
   import { Topbar } from "@/components/layout/topbar";

   export const Route = createFileRoute("/_portal")({
     beforeLoad: () => {
       if (!isAuthenticated()) {
         throw redirect({ to: "/login" });
       }
       // admin 也可以访问 portal（可选），但默认 admin 会被路由到 dashboard
     },
     component: PortalLayout,
   });

   function PortalLayout() {
     return (
       <div className="flex h-screen flex-col">
         <Topbar />
         <main className="flex-1 overflow-y-auto bg-muted/40 p-6">
           <Outlet />
         </main>
       </div>
     );
   }
   ```

3. 创建 `web/admin/src/routes/_portal/index.tsx`（用户面板首页占位，Phase 12 填充内容）：
   ```typescript
   import { createFileRoute } from "@tanstack/react-router";

   export const Route = createFileRoute("/_portal/")({
     component: PortalIndex,
   });

   function PortalIndex() {
     return (
       <div className="space-y-4">
         <h1 className="text-2xl font-bold">我的面板</h1>
         <p className="text-muted-foreground">
           欢迎使用 Cloud CLI Proxy。您的主机和资源信息将在后续版本中显示。
         </p>
       </div>
     );
   }
   ```

注意：TanStack Router 的文件路由约定要求 `_portal` 作为布局路由前缀，`_portal/index.tsx` 作为默认子路由。确认项目中 TanStack Router 的 `routeTree.gen.ts` 会自动生成（如果使用 `@tanstack/router-plugin`），或者需要手动更新路由树。检查现有 `_dashboard` 目录结构作为参考。
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy/web/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getRole" web/admin/src/routes/_dashboard.tsx
    - grep -q "role !== 'admin'" web/admin/src/routes/_dashboard.tsx（或等效的角色检查）
    - grep -q "/portal" web/admin/src/routes/_dashboard.tsx
    - test -f web/admin/src/routes/_portal.tsx
    - grep -q "isAuthenticated" web/admin/src/routes/_portal.tsx
    - grep -q "PortalLayout" web/admin/src/routes/_portal.tsx
    - test -f web/admin/src/routes/_portal/index.tsx
    - grep -q "我的面板" web/admin/src/routes/_portal/index.tsx
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>管理员路由守卫拒绝非 admin 角色并重定向到 /portal；用户面板骨架路由 _portal 和 _portal/index 存在且受认证保护；TypeScript 编译通过</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 前端类型检查通过
- 登录页发送 short_id 到 /v1/auth/login
- 登录成功后按 role 跳转不同路由
- _dashboard 守卫拒绝非 admin 角色
- _portal 路由存在且受认证保护
</verification>

<success_criteria>
- auth.ts 导出 parseTokenPayload、getRole、isAdmin 函数
- login.tsx 发送 short_id + password 到 /v1/auth/login，按 role 跳转 / 或 /portal
- _dashboard.tsx 仅允许 admin 角色访问
- _portal.tsx 作为用户面板布局存在，_portal/index.tsx 显示占位内容
- 前端 TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infra-migration/11-03-SUMMARY.md`
</output>
