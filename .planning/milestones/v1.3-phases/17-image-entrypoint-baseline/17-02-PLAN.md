---
phase: 17-image-entrypoint-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-shell/docker/Dockerfile
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "docker build 成功产出镜像，无报错退出"
    - "容器内 sing-box version 输出 1.13.x 版本号"
    - "容器内 claude --version 可执行并输出版本信息"
    - "容器内 echo $DISABLE_AUTOUPDATER 输出 1"
  artifacts:
    - path: "claude-shell/docker/Dockerfile"
      provides: "claude-shell 专用容器镜像构建定义"
      contains: "FROM ubuntu:24.04"
  key_links:
    - from: "claude-shell/docker/Dockerfile"
      to: "claude-shell/docker/entrypoint.sh"
      via: "COPY entrypoint.sh /usr/local/bin/entrypoint.sh"
      pattern: "COPY entrypoint\\.sh"
---

<objective>
修复 Phase 17 gap：Claude Code 安装脚本 CDN 返回 HTTP 403，导致 docker build 失败。

Purpose: 在 Dockerfile 中为 Claude Code 安装步骤增加网络容错（重试 + 直接下载回退），使镜像在 CDN 临时不可达时仍能构建成功。

Output: 更新后的 `claude-shell/docker/Dockerfile`，`docker build` 可成功产出完整镜像。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-image-entrypoint-baseline/17-CONTEXT.md
@.planning/phases/17-image-entrypoint-baseline/17-RESEARCH.md

<gap_analysis>
## Gap Source

17-VERIFICATION.md (gaps_found): 4/6 truths failed，全部根因是 Claude Code 安装脚本 `https://claude.ai/install.sh` 在构建环境返回 HTTP 403。

## Failed Truths

| # | Truth | Root Cause |
|---|-------|------------|
| 1 | docker build 成功产出镜像 | install.sh 返回 403，RUN 步骤非零退出 |
| 2 | sing-box version 输出 1.13.x | 构建失败，无镜像可运行 |
| 3 | claude --version 可执行 | Claude Code 未安装到镜像 |
| 6 | DISABLE_AUTOUPDATER=1 | ENV 声明正确但无镜像可运行 |

## Verified Truths (No Change Needed)

| # | Truth | Status |
|---|-------|--------|
| 4 | entrypoint 按正确顺序执行 | ✓ VERIFIED |
| 5 | 步骤失败时输出错误信息和退出码 | ✓ VERIFIED |
</gap_analysis>

<interfaces>
<!-- 当前 Dockerfile 第 47 行（需修改部分） -->
```dockerfile
USER ${CLAUDE_USER}
RUN curl -fsSL https://claude.ai/install.sh | bash
```

<!-- Claude Code 安装后的 PATH 和 ENV（不修改） -->
```dockerfile
ENV PATH="/home/claude/.local/bin:${PATH}"
ENV DISABLE_AUTOUPDATER=1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 Dockerfile Claude Code 安装步骤</name>
  <files>claude-shell/docker/Dockerfile</files>
  <read_first>
    - claude-shell/docker/Dockerfile (当前版本)
    - .planning/phases/17-image-entrypoint-baseline/17-RESEARCH.md (Claude Code 安装模式)
    - .planning/phases/17-image-entrypoint-baseline/17-CONTEXT.md (D-04, D-05 决策)
  </read_first>
  <action>
修改 `claude-shell/docker/Dockerfile`，将第 47 行的单次 `curl install.sh` 替换为带重试和回退的安装逻辑。

**修改方案：** 替换 Dockerfile 第 44-47 行（Claude Code 安装注释块 + RUN 指令）

替换前（当前代码）：
```dockerfile
# ---------------------------------------------------------------------------
# 4. Claude Code 安装（D-04, D-05 — 必须在 USER claude 之后执行）
# ---------------------------------------------------------------------------
USER ${CLAUDE_USER}
RUN curl -fsSL https://claude.ai/install.sh | bash
```

替换后：
```dockerfile
# ---------------------------------------------------------------------------
# 4. Claude Code 安装（D-04, D-05 — 必须在 USER claude 之后执行）
#    重试策略：官方脚本最多尝试 3 次，失败后直接下载 binary 回退
# ---------------------------------------------------------------------------
USER ${CLAUDE_USER}

ARG CLAUDE_CODE_VERSION=latest
ARG INSTALL_MAX_RETRIES=3

RUN set -eux; \
    installed=false; \
    for attempt in $(seq 1 ${INSTALL_MAX_RETRIES}); do \
      echo "Attempt ${attempt}/${INSTALL_MAX_RETRIES}: installing Claude Code via official script..."; \
      if curl -fsSL https://claude.ai/install.sh | bash; then \
        installed=true; \
        break; \
      fi; \
      echo "Attempt ${attempt} failed, waiting before retry..."; \
      sleep $((attempt * 2)); \
    done; \
    if [ "$installed" != "true" ]; then \
      echo "Official install script failed after ${INSTALL_MAX_RETRIES} attempts, falling back to GitHub release..."; \
      ARCH="$(dpkg --print-architecture)"; \
      if [ "${CLAUDE_CODE_VERSION}" = "latest" ]; then \
        CLAUDE_CODE_VERSION="$(curl -fsSL https://api.github.com/repos/anthropics/claude-code/releases/latest | jq -r '.tag_name' | sed 's/^v//')"; \
      fi; \
      curl -fsSL -o /tmp/claude.tar.gz \
        "https://github.com/anthropics/claude-code/releases/download/v${CLAUDE_CODE_VERSION}/claude-linux-${ARCH}.tar.gz"; \
      mkdir -p "${HOME}/.local/bin"; \
      tar -xzf /tmp/claude.tar.gz -C "${HOME}/.local/bin" claude; \
      chmod 0755 "${HOME}/.local/bin/claude"; \
      rm -f /tmp/claude.tar.gz; \
    fi; \
    claude --version
```

**修改要点：**
1. 保留官方 install.sh 作为首选安装方式（符合 D-04）
2. 增加 3 次重试，每次间隔递增（2s, 4s, 6s）
3. 全部重试失败后，通过 GitHub API 获取最新版本号，直接下载 binary 回退
4. 回退逻辑保持与官方安装相同的安装路径（`$HOME/.local/bin/claude`）
5. 最终 `claude --version` 验证安装成功
6. 不修改后续的 ENV PATH 和 DISABLE_AUTOUPDATER 设置
  </action>
  <verify>
    <automated>cd claude-shell/docker && docker build -t claude-shell-test . && docker run --rm claude-shell-test bash -c "claude --version && sing-box version && echo DISABLE_AUTOUPDATER=$DISABLE_AUTOUPDATER"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile 第 47 行的 `RUN curl -fsSL https://claude.ai/install.sh | bash` 已被替换为带重试和回退的安装逻辑
    - 安装逻辑优先尝试官方 install.sh（最多 3 次重试）
    - 安装逻辑包含直接下载 binary 的回退路径
    - 回退路径使用 `jq` 解析 GitHub API 获取最新版本号
    - 安装完成后 `claude --version` 验证成功
    - docker build 成功退出（exit code 0）
    - docker run 输出 sing-box 1.13.x 版本号
    - docker run 输出 claude 版本信息
    - docker run 输出 DISABLE_AUTOUPDATER=1
  </acceptance_criteria>
  <done>Dockerfile Claude Code 安装步骤增加重试和回退策略，docker build 成功产出包含 Claude Code 的完整镜像</done>
</task>

</tasks>

<verification>
Phase 17 gap closure 验证：

1. 镜像构建：`cd claude-shell/docker && docker build -t claude-shell-test .` 成功退出
2. Claude Code 验证：`docker run --rm claude-shell-test bash -c "claude --version"` 输出版本信息
3. sing-box 验证：`docker run --rm claude-shell-test bash -c "sing-box version"` 输出 1.13.x
4. 自动更新禁用：`docker run --rm claude-shell-test bash -c "echo $DISABLE_AUTOUPDATER"` 输出 1
5. entrypoint 编排（已验证，无需重测）：`entrypoint.sh` 语法正确，步骤顺序正确
</verification>

<success_criteria>
1. docker build 成功退出（exit code 0），产出完整镜像
2. 镜像内 `claude --version` 可执行并输出版本信息
3. 镜像内 `sing-box version` 输出 1.13.x
4. 镜像内 `echo $DISABLE_AUTOUPDATER` 输出 1
5. 官方 install.sh 仍为首选安装方式，直接下载为回退
</success_criteria>

<output>
After completion, create `.planning/phases/17-image-entrypoint-baseline/17-02-SUMMARY.md`
</output>
