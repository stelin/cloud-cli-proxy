---
phase: "29.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/repository/queries.go
  - internal/store/repository/queries_host_entry_password_test.go
autonomous: true
requirements_addressed:
  - P0-HOTFIX-29.1

must_haves:
  truths:
    - "GetHost 及 5 个同类 List 函数返回的 Host.EntryPassword 与 DB hosts.entry_password 一致"
    - "仓储层存在回归测试，一旦任何 Host 读 SQL 漏 entry_password 列，go test 立即失败"
    - "下游 runtime_service.QueueHostAction 首次读到非空 EntryPassword 时不再进入 warn 分支"
  artifacts:
    - path: "internal/store/repository/queries.go"
      provides: "6 个 Host 读查询的 package-level SQL 常量 + SELECT/Scan 含 entry_password"
      contains: "getHostSQL"
    - path: "internal/store/repository/queries.go"
      provides: "ListHosts / ListHostsByUserID / ListHostsWithUsername / ListRunningHosts / ListRunningHostsByUserID SQL 常量"
      contains: "listRunningHostsSQL"
    - path: "internal/store/repository/queries_host_entry_password_test.go"
      provides: "6 条 SQL 常量对 entry_password 的 strings.Contains 契约断言"
      contains: "TestAllHostReadQueriesIncludeEntryPassword"
  key_links:
    - from: "internal/store/repository/queries.go::GetHost"
      to: "internal/store/repository/models.go::Host.EntryPassword"
      via: "SELECT COALESCE(entry_password, '') 映射 &item.EntryPassword"
      pattern: "entry_password"
    - from: "internal/store/repository/queries_host_entry_password_test.go"
      to: "6 条 *SQL 常量"
      via: "package repository 内直接引用常量"
      pattern: "strings\\.Contains\\(.+,\\s*\"entry_password\"\\)"

threat_model:
  boundaries:
    - "仓储层 ↔ 上游 HTTP handler / runtime service：HTTP / service 消费方依赖返回的 Host.EntryPassword 真实可信"
    - "仓储层 ↔ DB：SQL 列顺序与 Scan 列顺序必须严格一一对齐，否则类型错位"
  threats:
    - id: T-29.1-01
      category: Tampering
      component: "internal/store/repository/queries.go::GetHost"
      disposition: mitigate
      mitigation: "SELECT 中显式 COALESCE(entry_password, '')，Scan 对齐 &item.EntryPassword；并由回归测试锁定 SQL 必含该列，防止后续提交回归"
    - id: T-29.1-02
      category: Repudiation
      component: "仓储层 5 个 List* 函数"
      disposition: mitigate
      mitigation: "一次性整批补齐 6 处，避免某一路径继续返回空密码而被误当合法 — 见 RESEARCH §1.3 覆盖矩阵"
    - id: T-29.1-05-log
      category: "Information disclosure"
      component: "仓储层测试输出"
      disposition: mitigate
      mitigation: "契约测试仅断言列名字符串，t.Errorf 只打印 SQL 文本（无行数据）；禁止在测试输出或日志中打印任何明文 entry_password 样本"
---

<objective>
把仓储层 6 个读 Host 的 SQL 与 Scan 一次性修正为包含 `entry_password` 列，并把 SQL 提升为包级常量以便可断言式回归。这是整个 P0 修复链路的根节点 —— 没有这一步，worker/runtime 的任何 fail-fast 都会把所有主机打进 failed。

Purpose: 消灭"容器密码退化为 workspace"的数据源漏水点；同时为同类回归建立机器可测的契约。
Output: `queries.go` 内 6 条 SQL 常量 + Scan 正确对齐；`queries_host_entry_password_test.go` 新测试文件 + `go test ./internal/store/repository/...` 通过。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-CONTEXT.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md

<interfaces>
<!-- 来自 internal/store/repository/models.go:34-50 —— Host 结构体 EntryPassword 字段已存在 -->

```go
// internal/store/repository/models.go
type Host struct {
    ID               string
    UserID           string
    Status           string
    ShortID          string
    EntryPassword    string    `json:"entry_password,omitempty"`  // 已存在，位置固定在 ShortID 之后
    TemplateImageRef string
    HomeVolumeName   string
    SlotKey          string
    Timezone         string
    Hostname         string
    MemoryLimitMB    int
    CPULimit         float64
    DiskLimitGB      int
    CreatedAt        time.Time
    UpdatedAt        time.Time
}
```

<!-- 来自 internal/store/repository/queries.go:1152-1173 —— 既有"SQL 提常量"模板 -->

```go
// internal/store/repository/queries.go
const getHostByShortIDSQL = `
	SELECT h.id::text, h.short_id, h.entry_password, h.status,
	       h.user_id::text, u.status, u.username,
	       COALESCE(h.template_image_ref, '')
	FROM hosts h
	JOIN users u ON u.id = h.user_id
	WHERE h.short_id = $1
`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: 把 6 个 Host 读函数的内联 SQL 抽为包级常量并补 entry_password 列</name>
  <files>internal/store/repository/queries.go</files>
  <read_first>
    - internal/store/repository/queries.go （必读全文，确认 6 个目标函数当前行号与 SELECT/Scan 列序；注意 Phase 30 可能已把 GetHostByShortID 抽为 getHostByShortIDSQL，延用同一模式）
    - internal/store/repository/models.go （Host 结构体 EntryPassword 字段位置）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md §1.1 / §1.3 / §8
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §1
  </read_first>
  <behavior>
    - 6 条 SQL 常量（getHostSQL / listHostsSQL / listHostsByUserIDSQL / listHostsWithUsernameSQL / listRunningHostsSQL / listRunningHostsByUserIDSQL）均包含子串 "entry_password"
    - 6 个函数的 Scan() 目标列表在 &item.ShortID 之后紧跟 &item.EntryPassword，其余列顺序严格保持不变
    - WHERE / JOIN / ORDER BY 子句字面零改动
    - go build ./... 编译通过
  </behavior>
  <action>
    对 `queries.go` 中以下 6 个函数做**仅此**的两类改动：

    1. 把每个函数原来内联的 SELECT SQL 提升为紧邻函数定义之上的 package-level `const` 字符串，命名严格遵循：
       - `GetHost`                        → `const getHostSQL`
       - `ListHosts`                      → `const listHostsSQL`
       - `ListHostsByUserID`              → `const listHostsByUserIDSQL`
       - `ListHostsWithUsername`          → `const listHostsWithUsernameSQL`
       - `ListRunningHosts`               → `const listRunningHostsSQL`
       - `ListRunningHostsByUserID`       → `const listRunningHostsByUserIDSQL`
       风格对齐既有 `getHostByShortIDSQL`（1152-1160）：反引号 raw string、首行换行、列出完整 SELECT 文本。

    2. 在每条 SELECT 的列序中，把原 `COALESCE(short_id, '')` 之后紧跟插入一个新列 `COALESCE(entry_password, '')`。相应 Scan() 在 `&item.ShortID` 之后紧跟 `&item.EntryPassword`。**其余列顺序严格零改动**。

    取消所有相关函数体里原来的内联反引号 SQL 字符串，改为 `r.db.Query(ctx, listHostsSQL, ...)` / `r.db.QueryRow(ctx, getHostSQL, hostID)` 等形式。

    严禁在本任务中：
    - 调整任何 WHERE / ORDER BY / JOIN；
    - 调整返回值结构 / 签名；
    - 把 `%w` 错误包装替换成别的格式；
    - 触碰 `GetHostByShortID` / `UpsertHost` / `GetPrimaryHostByUserID`（这三处 SELECT 本就已含 `entry_password`，保留现状）。
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/store/repository/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '^const getHostSQL = `' internal/store/repository/queries.go 必须 == 1
    - grep -c '^const listHostsSQL = `' internal/store/repository/queries.go 必须 == 1
    - grep -c '^const listHostsByUserIDSQL = `' internal/store/repository/queries.go 必须 == 1
    - grep -c '^const listHostsWithUsernameSQL = `' internal/store/repository/queries.go 必须 == 1
    - grep -c '^const listRunningHostsSQL = `' internal/store/repository/queries.go 必须 == 1
    - grep -c '^const listRunningHostsByUserIDSQL = `' internal/store/repository/queries.go 必须 == 1
    - 以下命令输出必须 == 6：rg -c 'entry_password' <(grep -E '^const (getHostSQL|listHostsSQL|listHostsByUserIDSQL|listHostsWithUsernameSQL|listRunningHostsSQL|listRunningHostsByUserIDSQL)' -A 30 internal/store/repository/queries.go) —— 即 6 条常量文本每条至少出现 1 次 entry_password
    - rg -n '&item\.EntryPassword' internal/store/repository/queries.go 必须在 ≥ 6 个不同行（GetHost + 5 个 List\*）出现
    - go build ./... 成功（exit 0）
    - go vet ./internal/store/repository/... 成功（exit 0）
    - git diff --stat 仅涉及 internal/store/repository/queries.go
  </acceptance_criteria>
  <done>
    6 个 SQL 常量就位并各含 entry_password 列；Scan 对齐；构建通过；没有引入非本任务的重构。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: 新增 queries_host_entry_password_test.go 契约测试</name>
  <files>internal/store/repository/queries_host_entry_password_test.go</files>
  <read_first>
    - internal/store/repository/queries.go （Task 1.1 修改后的版本 —— 需要 import 6 个常量名）
    - internal/store/repository/migration_0014_test.go （契约测试模板，参考 TestResolveClaudeAccountQueries_MatchD05 / TestGetHostByShortID_QueryIncludesTemplateImageRef）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §2
  </read_first>
  <behavior>
    - 运行 `go test -run TestAllHostReadQueriesIncludeEntryPassword ./internal/store/repository/...` 必须 PASS
    - 手动把 Task 1.1 任一常量里的 entry_password 删除后重跑测试，必须 FAIL（该反证不在 PLAN 执行内；测试自身足以在未来检测到回归即可）
  </behavior>
  <action>
    新建文件 `internal/store/repository/queries_host_entry_password_test.go`，package 声明为 `package repository`（与被测代码同包，可直接引用包级常量）。

    写一个测试函数 `TestAllHostReadQueriesIncludeEntryPassword`：

    ```go
    package repository

    import (
        "strings"
        "testing"
    )

    // TestAllHostReadQueriesIncludeEntryPassword 锁定 Phase 29.1 的根因修复：
    // 任何读 Host 的 SQL 必须把 entry_password 列纳入 SELECT，
    // 否则下游 runtime / worker / admin 链路会把容器密码降级为 "workspace"。
    func TestAllHostReadQueriesIncludeEntryPassword(t *testing.T) {
        queries := map[string]string{
            "getHostSQL":                  getHostSQL,
            "listHostsSQL":                listHostsSQL,
            "listHostsByUserIDSQL":        listHostsByUserIDSQL,
            "listHostsWithUsernameSQL":    listHostsWithUsernameSQL,
            "listRunningHostsSQL":         listRunningHostsSQL,
            "listRunningHostsByUserIDSQL": listRunningHostsByUserIDSQL,
        }
        for name, q := range queries {
            if !strings.Contains(q, "entry_password") {
                t.Errorf("%s 必须包含 entry_password 列（Phase 29.1 根因修复）；实际 SQL:\n%s", name, q)
            }
        }
    }
    ```

    严禁：
    - 引入 DB 连接 / testcontainers / dockertest（本仓库无此基础设施）；
    - 断言任何密码明文或 host 具体数据；只做字符串契约校验；
    - 扩大到其他 SQL 常量断言（scope 严控在本阶段 6 条）。
  </action>
  <verify>
    <automated>go test ./internal/store/repository/... -run TestAllHostReadQueriesIncludeEntryPassword -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 文件 internal/store/repository/queries_host_entry_password_test.go 存在
    - 文件首行 package 声明为 `package repository`
    - grep -c 'TestAllHostReadQueriesIncludeEntryPassword' internal/store/repository/queries_host_entry_password_test.go 必须 == 1
    - rg -c '"entry_password"' internal/store/repository/queries_host_entry_password_test.go 必须 == 1
    - go test ./internal/store/repository/... -run TestAllHostReadQueriesIncludeEntryPassword -count=1 exit 0
    - go test ./internal/store/repository/... -count=1 exit 0（整包回归未被破坏）
    - 测试输出中不包含任何密码明文样本（仅 SQL 文本；SQL 文本中只有列名 "entry_password"，不含具体值）
  </acceptance_criteria>
  <done>
    契约测试单独 PASS；整个 repository 包测试 PASS；6 条 SQL 常量均被覆盖。
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 成功
- `go vet ./internal/store/repository/...` 成功
- `go test ./internal/store/repository/... -count=1` 全部 PASS（含新增契约测试）
- `git diff --stat` 仅涉及 `internal/store/repository/queries.go` 与 `internal/store/repository/queries_host_entry_password_test.go` 两个文件
- 人工抽查 Host 结构体 Scan 列序：`&item.ShortID` 紧跟 `&item.EntryPassword`，其余列未变
</verification>

<success_criteria>
- 6 个 Host 读 SQL 常量全部存在且均含 `entry_password` 列；Scan 对齐
- `TestAllHostReadQueriesIncludeEntryPassword` PASS，未来任一路径漏列会被即时发现
- 消费方 `runtime_service.QueueHostAction` 再读 `host.EntryPassword` 时得到真实随机密码（由 Plan 02 在本 wave 并行改造消费端的 fail-fast 路径后联合闭环）
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-gethost-entry-password-workspace/29.1-01-SUMMARY.md` 汇报：
- 实际改动的函数清单与行号
- 6 条 SQL 常量名最终列表
- 新测试文件运行结果
- 任何遇到的意外（如既有代码已部分重构的情况）
</output>
