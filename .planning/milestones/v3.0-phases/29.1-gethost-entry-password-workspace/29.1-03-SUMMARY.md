---
phase: 29.1-gethost-entry-password-workspace
plan: 3
subsystem: runtime
tags: [entrypoint, passwd-S, fail-fast, image, hotfix, p0]

# Dependency graph
requires:
  - phase: 29.1-01
    provides: "queries.go 6 个 Host 读 SQL 已含 entry_password 列；下游 host.EntryPassword 现在能拿到真实值"
  - phase: 29.1-02
    provides: "runtime_service / worker 三处空 entry_password 已 fail-fast；CONTAINER_SSH_PASSWORD 不会再被静默替换为 'workspace'"
provides:
  - "受管镜像 entrypoint.sh 在 chpasswd 之后立即执行 passwd -S 自检：状态非 P|PS 时容器 exit 1，让重启循环成为可观测信号"
  - "FATAL 消息只输出 RUN_USER + 状态字串（P/PS/L/LK/NP/UNSET 等枚举），绝不回显 CONTAINER_PASSWORD / CONTAINER_SSH_PASSWORD"
  - "镜像层最后一道防线：即便上游回归再次注入空密码，容器宁可起不来，也不起一个 SSH 登不进去的'伪成功'实例"
affects:
  - "29.1-04 admin 批量 resync / UAT：UAT 步骤可在 docker run 时通过 -e CONTAINER_SSH_PASSWORD=<空> / 容器内 passwd -l 触发自检失败路径，验证 exit 1 与 stderr FATAL 行落地"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "shadow-utils passwd -S 第 2 列状态码白名单（P|PS）+ case 默认分支 fail-fast：bash 原生工具，不引入新依赖"
    - "FATAL 只回显非敏感枚举字段（用户名 + 状态字串），明确把密码值排除在 stderr 之外，遵守 T-29.1-05-log-entrypoint mitigation"
    - "entrypoint 不做 retry，重启循环交给外层 docker restart policy / host-agent，符合 T-29.1-04 accept disposition"

key-files:
  created: []
  modified:
    - "deploy/docker/managed-user/entrypoint.sh"

key-decisions:
  - "[Phase 29.1-03] 自检失败走 exit 1 而非 set 默认密码：让容器进入 exit/restart 循环，比偷偷给一个 'workspace' fallback 更易被运维通过 docker ps / 重启次数告警发现"
  - "[Phase 29.1-03] passwd -S 解析使用 awk '{print $2}' + 命令链 || echo UNSET 兜底：当 passwd 失败 / 用户不存在时，status 落到 UNSET 分支同样 fail-fast，不会出现解析失败被当成 P 通过"
  - "[Phase 29.1-03] FATAL 消息只含 RUN_USER + status：绝不在 stderr 出现 CONTAINER_PASSWORD / CONTAINER_SSH_PASSWORD 任一变量值，符合 T-29.1-05-log-entrypoint mitigation"

patterns-established:
  - "entrypoint fail-fast：镜像内任何 'silent fallback to workspace 密码' 改造为 status 校验 + exit 1，P0 级别故障可通过 restart 循环立刻被告警/巡检捕获"

requirements-completed: [P0-HOTFIX-29.1]

# Metrics
duration: ~3min
completed: 2026-04-20
---

# Phase 29.1 Plan 03: entrypoint passwd -S 自检 Summary

**受管镜像 entrypoint.sh 在 chpasswd 之后追加 passwd -S "${RUN_USER}" 自检：状态非 P|PS 时立即 exit 1，让"密码退化为 workspace"类故障从用户登不进去前移到容器启动失败。**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-20T18:06:13Z
- **Completed:** 2026-04-20T18:08:00Z（约）
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- `deploy/docker/managed-user/entrypoint.sh` 在原 `chpasswd` 行（第 150 行，未改动）之后、IPv6 disable 段（已下移到第 164 行起）之前，新增 11 行（含上下空行 12 行）自检块（第 152–162 行）
- 状态白名单仅 `P|PS`；其余值（`L` / `LK` / `NP` / `UNSET` / 空）走 case `*)` 分支 → stderr 输出 `[entrypoint] FATAL: password status for ${RUN_USER} is '${status}', refusing to start` → `exit 1`
- 不引入任何新工具依赖；passwd / awk 都来自镜像既有 util-linux / shadow-utils
- 脚本其它段落（sudoers / mkdir / chown / IPv6 / KasmVNC / fluxbox / pcmanfm / prepare_v3_* / sshd）git diff 0 行

## Task Commits

1. **Task 3.1: entrypoint.sh 在 chpasswd 之后加 passwd -S 自检块** — `40e9710` (feat)

_注：本 plan 单 task，无 TDD RED/GREEN 分裂；脚本类微改 plan 直接 feat 落地。_

## Files Created/Modified

- `deploy/docker/managed-user/entrypoint.sh` — chpasswd 行不变；其后插入 12 行（含尾部空行）自检块；FATAL 仅含 RUN_USER + status，不含密码

## Static Verification

### bash -n 解析

```text
$ bash -n deploy/docker/managed-user/entrypoint.sh
（无任何输出，exit 0）
```

`syntax OK`，脚本可被 bash 完整解析。

### shellcheck

宿主机未安装 shellcheck（`command -v shellcheck` exit 1，"no shellcheck"），故未运行 shellcheck。新增块仅使用既有同名风格（`"$status"` 引号、`case`/`esac`、`>&2` 重定向、`2>/dev/null`），与脚本其它段落一致；ERROR 级别人工扫一遍亦为空。

### 顺序与变量泄漏断言

```text
# 行号顺序断言
chpasswd@150 < passwd-S@154 < disable_ipv6@165   ORDER OK

# 自检块新增行（154 / 159 / 160）grep CONTAINER_PASSWORD|CONTAINER_SSH_PASSWORD
（无命中；命中只在 124 / 150 两处既有行）
```

### git diff --stat

```text
 deploy/docker/managed-user/entrypoint.sh | 12 ++++++++++++
 1 file changed, 12 insertions(+)
```

精确 12 行 insertion，无 deletion，无其它文件涉及。

## Decisions Made

详见 frontmatter `key-decisions`。简记：fail-fast 走 exit 1（让 restart 循环成为告警信号）/ awk + ||echo UNSET 兜底解析失败 / FATAL 只回显非敏感枚举。

## Deviations from Plan

None - plan executed exactly as written.（Plan 03 极小，单 task，无任何 Rule 1/2/3 触发，也无 Rule 4 架构问询。）

## Issues Encountered

无。

## Threat Surface Scan

未引入计划之外的安全相关 surface。所有改动严格落在 PLAN frontmatter `threat_model` 既定的 `entrypoint.sh chpasswd` 边界内：T-29.1-01b（mitigate）、T-29.1-04（accept，重启交给外层）、T-29.1-05-log-entrypoint（mitigate，FATAL 不回显密码）三条 disposition 均与本次实现一致。

## Self-Check: PASSED

- `[ -f deploy/docker/managed-user/entrypoint.sh ]` → FOUND
- `git log --oneline | grep 40e9710` → FOUND（commit 40e9710 "feat(29.1-03): entrypoint chpasswd 后追加 passwd -S 自检"）
- bash -n exit 0 → 通过
- rg `passwd -S "\$\{RUN_USER\}"` → 1 行命中（line 154）
- rg `FATAL: password status for \$\{RUN_USER\} is` → 1 行命中（line 159）
- 顺序断言（chpasswd → passwd -S → disable_ipv6）→ ORDER OK
- 自检块新增行不含 CONTAINER_PASSWORD / CONTAINER_SSH_PASSWORD → 通过

## User Setup Required

None — 镜像层自检纯 in-script，宿主无需任何额外配置。镜像下次 build & rollout 自动生效。

## Next Phase Readiness

- 仓储 / runtime / 镜像三层防线（Plan 01 / 02 / 03）全部就绪。Plan 04 可以接 admin 批量 resync 与端到端 UAT，触发空密码场景验证 entrypoint exit 1 + docker restart 循环 + 控制面 runtime.entry_password_missing 事件三条审计线全部点亮。
- 无新增 blocker。

---
*Phase: 29.1-gethost-entry-password-workspace*
*Completed: 2026-04-20*
