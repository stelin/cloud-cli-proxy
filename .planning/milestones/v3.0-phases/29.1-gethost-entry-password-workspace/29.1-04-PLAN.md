---
phase: "29.1"
plan: 4
type: execute
wave: 2
depends_on:
  - 1
  - 2
files_modified:
  - internal/controlplane/http/admin_hosts.go
  - internal/controlplane/http/admin_hosts_test.go
  - internal/controlplane/http/router.go
autonomous: false
requirements_addressed:
  - P0-HOTFIX-29.1

must_haves:
  truths:
    - "admin 可以一键调用 POST /v1/admin/hosts/resync-passwords，对所有 running host 按 DB hosts.entry_password 批量 docker exec chpasswd"
    - "端点对每个 host 的成功 / 失败 / 跳过（空密码）聚合返回；单 host 失败不导致整体 500"
    - "每次批量操作写 admin.hosts.password_resync_triggered 事件；单 host 结果分别写 runtime.entry_password_resynced / runtime.entry_password_resync_failed / runtime.entry_password_missing 事件"
    - "端点必须经过现有 adminGuard 鉴权中间件；无其他 IAM 改造"
    - "syncContainerPassword 从 func 提升为 package-level var，便于单测注入 fake 实现"
    - "人工 UAT：新建 host → 从 API 取得 entry_password → 用该密码 SSH 登录容器成功 → 容器内 $CONTAINER_SSH_PASSWORD 与 API 返回值一致 → passwd -S workspace 第 2 列为 P 或 PS"
  artifacts:
    - path: "internal/controlplane/http/admin_hosts.go"
      provides: "ResyncPasswords 批量 handler + syncContainerPassword 改为 var"
      contains: "ResyncPasswords"
    - path: "internal/controlplane/http/router.go"
      provides: "POST /v1/admin/hosts/resync-passwords 路由，套 adminGuard"
      contains: "resync-passwords"
    - path: "internal/controlplane/http/admin_hosts_test.go"
      provides: "stubHostStore 增加 ListRunningHosts + 3 条 TestResyncPasswords_* 用例"
      contains: "TestResyncPasswords_"
  key_links:
    - from: "POST /v1/admin/hosts/resync-passwords"
      to: "h.store.ListRunningHosts → syncContainerPassword → h.events.RecordEvent"
      via: "adminGuard 包裹的 hostsHandler.ResyncPasswords()"
      pattern: "resync-passwords"
    - from: "admin handler 批量循环"
      to: "AdminHostStore.ListRunningHosts(ctx) ([]repository.Host, error)"
      via: "接口扩展 + stubHostStore 同步实现"
      pattern: "ListRunningHosts"

threat_model:
  boundaries:
    - "未授权调用方 ↔ admin API：端点必须强制 adminGuard，不得对 user 自助 API 暴露"
    - "admin endpoint ↔ host-agent / docker daemon：端点通过本地 docker exec 直接触达所有 running 容器，权限面广，需严格鉴权与事件留痕"
  threats:
    - id: T-29.1-03
      category: "Elevation of privilege / Tampering"
      component: "POST /v1/admin/hosts/resync-passwords"
      disposition: mitigate
      mitigation: "路由直接包裹既有 adminGuard（与 List / Create / Rotate 同级），复用既有鉴权；不新增 IAM 接口；端点只按 DB 现存 entry_password 同步，**不轮换不生成新密码**（避免被滥用做密码重置攻击面）"
    - id: T-29.1-01c
      category: Tampering
      component: "批量循环中的单 host 错误处理"
      disposition: mitigate
      mitigation: "单 host `syncContainerPassword` 失败仅记 warn 事件并计入结果列表；整体 HTTP 200 返回聚合结果；空 EntryPassword 一律走 `runtime.entry_password_missing` 事件 + skipped_empty_password 计数，不触发 chpasswd，杜绝再次把 workspace 写入容器"
    - id: T-29.1-05-log-admin
      category: "Information disclosure"
      component: "RecordEvent / HTTP 响应 / logger"
      disposition: mitigate
      mitigation: "事件 Metadata 与响应体仅记 host_id / container_name / operator / source；**绝不**包含 h.EntryPassword 明文或派生哈希；响应体 results 条目不回填密码"
    - id: T-29.1-04b
      category: "Denial of Service"
      component: "批量 chpasswd 对数百个 running 容器的影响"
      disposition: accept
      mitigation: "当前单宿主环境下 running host 数量预期 O(100) 以下；逐个同步、每次 docker exec 本身已有超时；实施时若发现容器数量过大，后续迭代可加限速，本阶段不做预优化"
---

<objective>
交付存量修复链路：新建 admin endpoint `POST /v1/admin/hosts/resync-passwords`，根据 DB `hosts.entry_password` 对所有 running 容器批量执行 `docker exec chpasswd` 同步，并把每个 host 的结果聚合返回；附带 3 条 handler 单测与端到端人工 UAT 检查点。

该 plan 放在 wave 2 是因为：
- 必须依赖 Plan 01（仓储层补列）—— `ListRunningHosts` 返回的 Host.EntryPassword 此时才会是真实密码；
- 必须依赖 Plan 02（worker fail-fast）—— 否则批量同步过程中若有 host 密码字段异常，旧逻辑仍可能把 "workspace" 写进容器。

Purpose: 把"存量容器密码已被退化为 workspace"的历史脏数据清理掉；同时为未来类似 P0 事故保留一个受控的 admin 工具。
Output: admin_hosts.go 新增 handler + var 化微重构；router.go 路由注册；admin_hosts_test.go 3 条新用例；UAT 通过。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-CONTEXT.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-01-PLAN.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-02-PLAN.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-03-PLAN.md

<interfaces>
<!-- 来自 internal/controlplane/http/admin_hosts.go:25-34 —— AdminHostStore 接口扩点 -->

```go
// internal/controlplane/http/admin_hosts.go (既有接口，需追加 ListRunningHosts)
type AdminHostStore interface {
    ListHostsWithUsername(ctx context.Context) ([]repository.HostWithUsername, error)
    GetHostDetail(ctx context.Context, hostID string) (repository.HostDetail, error)
    GetHost(ctx context.Context, hostID string) (repository.Host, error)
    UpsertHost(ctx context.Context, p repository.UpsertHostParams) (repository.Host, error)
    GetUser(ctx context.Context, userID string) (repository.User, error)
    UpdateHostEntryPassword(ctx context.Context, hostID, pw string) error
    // 本 plan 新增:
    // ListRunningHosts(ctx context.Context) ([]repository.Host, error)
}
```

<!-- 来自 internal/controlplane/http/admin_hosts.go:915-925 —— syncContainerPassword 现为 func，本 plan 改 var -->

```go
// internal/controlplane/http/admin_hosts.go (改造前)
func syncContainerPassword(containerName, user, password string) error { ... }

// 改造后（仅 func 头改成 var 赋值；函数体零改动）：
var syncContainerPassword = func(containerName, user, password string) error { ... }
```

<!-- 来自 internal/controlplane/http/router.go:232-249 —— 既有 host 路由家族，本 plan 追加一行 -->

```go
// internal/controlplane/http/router.go
if deps.AdminHosts != nil {
    hostsHandler := NewAdminHostsHandler(deps.Logger, deps.AdminHosts, deps.HostActions, deps.EventRecorder)
    mux.Handle("GET /v1/admin/hosts", adminGuard(hostsHandler.List()))
    mux.Handle("POST /v1/admin/hosts", adminGuard(hostsHandler.Create()))
    // 本 plan 追加：
    // mux.Handle("POST /v1/admin/hosts/resync-passwords", adminGuard(hostsHandler.ResyncPasswords()))
    mux.Handle("GET /v1/admin/hosts/{hostID}", adminGuard(hostsHandler.Get()))
    ...
}
```

<!-- 来自 internal/runtime/tasks/worker.go::waitForSSH —— RecordEvent 调用模板 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: AdminHostStore 接口扩 ListRunningHosts + syncContainerPassword 提升为 var</name>
  <files>internal/controlplane/http/admin_hosts.go</files>
  <read_first>
    - internal/controlplane/http/admin_hosts.go （必读：第 25-34 行 AdminHostStore 接口定义；第 915-925 行 syncContainerPassword 定义；以及 Get / List / RotateSSHPassword 三个现有 handler 的 writeJSON / errors.Is(pgx.ErrNoRows, ...) / RecordEvent 调用格式）
    - internal/store/repository/queries.go （Plan 01 后已含 listRunningHostsSQL + ListRunningHosts 返回 []Host）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §8 / §9
  </read_first>
  <behavior>
    - AdminHostStore 接口新增方法 `ListRunningHosts(ctx context.Context) ([]repository.Host, error)`
    - 既有 `syncContainerPassword` 从 func 改为 package-level `var syncContainerPassword = func(...) error { ... }`，函数体字面 0 改动，所有既有调用点自动兼容
    - 包编译通过；既有测试仍全部 PASS
  </behavior>
  <action>
    1. 在 `internal/controlplane/http/admin_hosts.go` 接口定义处（~25-34 行），追加方法签名：

       ```go
       ListRunningHosts(ctx context.Context) ([]repository.Host, error)
       ```

       保持既有方法排序不变；追加在 `UpdateHostEntryPassword` 之后。

    2. 把第 ~915 行：

       ```go
       // syncContainerPassword updates the Linux user password inside a running container via docker exec.
       func syncContainerPassword(containerName, user, password string) error {
           cmd := exec.CommandContext(context.Background(), "docker", "exec", "-i", containerName,
               "chpasswd")
           cmd.Stdin = strings.NewReader(fmt.Sprintf("%s:%s\n", user, password))
           output, err := cmd.CombinedOutput()
           if err != nil {
               return fmt.Errorf("docker exec chpasswd: %w (%s)", err, strings.TrimSpace(string(output)))
           }
           return nil
       }
       ```

       改为：

       ```go
       // syncContainerPassword updates the Linux user password inside a running container via docker exec.
       // Exposed as a package-level var so unit tests can inject a fake implementation (Phase 29.1).
       var syncContainerPassword = func(containerName, user, password string) error {
           cmd := exec.CommandContext(context.Background(), "docker", "exec", "-i", containerName,
               "chpasswd")
           cmd.Stdin = strings.NewReader(fmt.Sprintf("%s:%s\n", user, password))
           output, err := cmd.CombinedOutput()
           if err != nil {
               return fmt.Errorf("docker exec chpasswd: %w (%s)", err, strings.TrimSpace(string(output)))
           }
           return nil
       }
       ```

       函数体字面 0 修改，只改 `func` → `var ... = func`。

    3. 严禁在本任务中做任何其他源文件改动；禁止改已有 `RotateSSHPassword` / `ChangeRootPassword` 的调用点（var 赋值对调用方透明）。
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/controlplane/...</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'ListRunningHosts\(ctx context\.Context\) \(\[\]repository\.Host, error\)' internal/controlplane/http/admin_hosts.go 必须命中 1 行（接口方法签名）
    - rg -n '^var syncContainerPassword = func\(containerName, user, password string\) error' internal/controlplane/http/admin_hosts.go 必须命中 1 行
    - rg -n '^func syncContainerPassword\(' internal/controlplane/http/admin_hosts.go 必须命中 0 行
    - go build ./... exit 0
    - go vet ./internal/controlplane/... exit 0
    - go test ./internal/controlplane/http/... -count=1 exit 0（既有测试 stub 未实现 ListRunningHosts 会导致编译失败时，本任务负责只扩接口 + 在 Task 4.3 同一 wave 中补 stub；为避免临时失败可与 Task 4.3 在同一 commit 内落地）
  </acceptance_criteria>
  <done>
    AdminHostStore 接口已扩 ListRunningHosts；syncContainerPassword 已 var 化；包构建/vet 通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: 新增 ResyncPasswords handler + 路由注册</name>
  <files>internal/controlplane/http/admin_hosts.go,internal/controlplane/http/router.go</files>
  <read_first>
    - internal/controlplane/http/admin_hosts.go （RotateSSHPassword / Get / List 三个 handler 作为 writeJSON / logger / events.RecordEvent 模板；注意 AdminHostsHandler 构造里已持有 h.events / h.logger / h.store）
    - internal/controlplane/http/router.go （第 232-249 行 host 路由家族，整条 adminGuard 块的写法）
    - internal/runtime/tasks/worker.go waitForSSH 片段（RecordEvent 参数 shape）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md §4.2 / §5
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §8 / §10 / Shared Patterns A/C
  </read_first>
  <behavior>
    - `POST /v1/admin/hosts/resync-passwords` 端点在 `router.go` adminGuard 包裹下挂载
    - handler 流程：
      1. `hosts, err := h.store.ListRunningHosts(r.Context())`；err != nil → 500 + 错误日志；
      2. 初始化计数器 `succeeded, failed, skippedEmpty`；`results := []map[string]any{}`；
      3. 逐 host：
         - 若 `host.EntryPassword == ""`：记 `runtime.entry_password_missing`（level=error, metadata: host_id, container, source="resync"），`skippedEmpty++`，results 追加 `{"host_id": h.ID, "status": "skipped_empty_password"}`；
         - 否则调用 `syncContainerPassword("cloudproxy-"+h.ID, "workspace", h.EntryPassword)`；
           - 成功 → 记 `runtime.entry_password_resynced`（level=info, metadata: host_id, container, source="admin_batch"），`succeeded++`，results 追加 `{"host_id": h.ID, "status": "ok"}`；
           - 失败 → 记 `runtime.entry_password_resync_failed`（level=warn, metadata: host_id, container, error=err.Error()），`failed++`，results 追加 `{"host_id": h.ID, "status": "failed", "error": err.Error()}`；
      4. 全量结束后记 `admin.hosts.password_resync_triggered`（level=info, metadata: operator="admin", target_count=len(hosts)）；
      5. `writeJSON(w, 200, map[string]any{"total": len(hosts), "succeeded": succeeded, "failed": failed, "skipped_empty_password": skippedEmpty, "results": results})`。
    - 任何单 host 错误都不应导致端点返回 500。
    - 事件 Metadata 与 HTTP 响应里均**绝不**出现 `host.EntryPassword` 明文。
  </behavior>
  <action>
    **A) internal/controlplane/http/admin_hosts.go**

    在 `RotateSSHPassword()` handler 之后追加 `ResyncPasswords()` handler，完整骨架如下（按既有 handler 缩进 / 命名风格）：

    ```go
    // ResyncPasswords 遍历所有 running host，把容器内 Linux 用户密码按 DB
    // hosts.entry_password 同步一遍；单 host 失败不影响整体返回。
    // Phase 29.1 存量修复入口。
    func (h *AdminHostsHandler) ResyncPasswords() nethttp.Handler {
        return nethttp.HandlerFunc(func(w nethttp.ResponseWriter, r *nethttp.Request) {
            hosts, err := h.store.ListRunningHosts(r.Context())
            if err != nil {
                h.logger.Error("list running hosts failed", "error", err)
                writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "list running hosts failed"})
                return
            }

            var (
                succeeded     int
                failed        int
                skippedEmpty  int
                results       = make([]map[string]any, 0, len(hosts))
            )
            const containerUser = "workspace"

            for i := range hosts {
                hostID := hosts[i].ID
                containerName := "cloudproxy-" + hostID

                if hosts[i].EntryPassword == "" {
                    if h.events != nil {
                        if _, evErr := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                            HostID:   &hostID,
                            Level:    "error",
                            Type:     "runtime.entry_password_missing",
                            Message:  "host entry_password is empty during resync; skipping",
                            Metadata: map[string]any{"host_id": hostID, "container": containerName, "source": "resync"},
                        }); evErr != nil {
                            h.logger.Error("record event failed", "type", "runtime.entry_password_missing", "error", evErr)
                        }
                    }
                    skippedEmpty++
                    results = append(results, map[string]any{"host_id": hostID, "status": "skipped_empty_password"})
                    continue
                }

                if syncErr := syncContainerPassword(containerName, containerUser, hosts[i].EntryPassword); syncErr != nil {
                    h.logger.Warn("resync password to running container failed",
                        "host_id", hostID, "container", containerName, "error", syncErr)
                    if h.events != nil {
                        if _, evErr := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                            HostID:   &hostID,
                            Level:    "warn",
                            Type:     "runtime.entry_password_resync_failed",
                            Message:  "docker exec chpasswd failed during admin batch resync",
                            Metadata: map[string]any{"host_id": hostID, "container": containerName, "error": syncErr.Error()},
                        }); evErr != nil {
                            h.logger.Error("record event failed", "type", "runtime.entry_password_resync_failed", "error", evErr)
                        }
                    }
                    failed++
                    results = append(results, map[string]any{"host_id": hostID, "status": "failed", "error": syncErr.Error()})
                    continue
                }

                if h.events != nil {
                    if _, evErr := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                        HostID:   &hostID,
                        Level:    "info",
                        Type:     "runtime.entry_password_resynced",
                        Message:  "container entry password resynced from DB",
                        Metadata: map[string]any{"host_id": hostID, "container": containerName, "source": "admin_batch"},
                    }); evErr != nil {
                        h.logger.Error("record event failed", "type", "runtime.entry_password_resynced", "error", evErr)
                    }
                }
                succeeded++
                results = append(results, map[string]any{"host_id": hostID, "status": "ok"})
            }

            if h.events != nil {
                if _, evErr := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                    Level:    "info",
                    Type:     "admin.hosts.password_resync_triggered",
                    Message:  "管理员批量同步主机 SSH 密码",
                    Metadata: map[string]any{"operator": "admin", "target_count": len(hosts)},
                }); evErr != nil {
                    h.logger.Error("record event failed", "type", "admin.hosts.password_resync_triggered", "error", evErr)
                }
            }

            writeJSON(w, nethttp.StatusOK, map[string]any{
                "total":                  len(hosts),
                "succeeded":              succeeded,
                "failed":                 failed,
                "skipped_empty_password": skippedEmpty,
                "results":                results,
            })
        })
    }
    ```

    **B) internal/controlplane/http/router.go**

    在现有 `if deps.AdminHosts != nil { ... }` 块内、紧邻 `rotate-ssh-password` 那一行（或其他既有 `POST /v1/admin/hosts/{hostID}/...` 之后、`GET /v1/admin/hosts/{hostID}` 之前亦可），追加：

    ```go
    mux.Handle("POST /v1/admin/hosts/resync-passwords", adminGuard(hostsHandler.ResyncPasswords()))
    ```

    不要修改块内其他任何路由行。

    严禁：
    - 在 HTTP 响应或事件 metadata 中回填任何密码值 / 哈希；
    - 为该端点新建独立的 AuthN/AuthZ 中间件（复用 adminGuard）；
    - 在 handler 内调用 RotateSSHPassword 或生成新密码；本端点**不轮换**，只同步 DB 现存值；
    - 当 `h.events == nil` 时 panic（保持 nil-safe，跳过记录但继续执行）。
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/controlplane/...</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'func \(h \*AdminHostsHandler\) ResyncPasswords\(\) nethttp\.Handler' internal/controlplane/http/admin_hosts.go 命中 1 行
    - rg -n 'runtime\.entry_password_missing' internal/controlplane/http/admin_hosts.go 命中 1 行
    - rg -n 'runtime\.entry_password_resynced' internal/controlplane/http/admin_hosts.go 命中 1 行
    - rg -n 'runtime\.entry_password_resync_failed' internal/controlplane/http/admin_hosts.go 命中 1 行
    - rg -n 'admin\.hosts\.password_resync_triggered' internal/controlplane/http/admin_hosts.go 命中 1 行
    - rg -n 'POST /v1/admin/hosts/resync-passwords' internal/controlplane/http/router.go 命中 1 行
    - rg -n 'adminGuard\(hostsHandler\.ResyncPasswords\(\)\)' internal/controlplane/http/router.go 命中 1 行
    - rg -n 'EntryPassword' internal/controlplane/http/admin_hosts.go 的所有命中行中不得出现把 `hosts[i].EntryPassword` 当作 map value / response field / event Message 参数值的代码（仅允许作为 `syncContainerPassword(...)` 第 3 形参 与 `== ""` 判断条件）
    - go build ./... exit 0
    - go vet ./internal/controlplane/... exit 0
  </acceptance_criteria>
  <done>
    handler 实现完成；路由挂载于 adminGuard 之下；构建/vet 通过；明文密码未泄漏到响应 / 事件 / 日志。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.3: stubHostStore 扩 ListRunningHosts + 3 条 TestResyncPasswords_* 单测</name>
  <files>internal/controlplane/http/admin_hosts_test.go</files>
  <read_first>
    - internal/controlplane/http/admin_hosts_test.go （必读全文：复用 stubHostStore / stubQueuer / NewAdminHostsHandler 构造 / writeJSON 响应解析）
    - internal/controlplane/http/admin_hosts.go （Task 4.1 / 4.2 修改后的版本）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §11
  </read_first>
  <behavior>
    - stubHostStore 新增字段 `runningHosts []repository.Host` 与 `runningErr error`；新增方法 `ListRunningHosts(ctx) ([]repository.Host, error)`。
    - 新增 3 条 `TestResyncPasswords_*` 用例，分别覆盖：
      - 无 running host：响应 total=0，succeeded=0，failed=0，skipped_empty_password=0；
      - 空 EntryPassword：skipped_empty_password=1，且 stubEvents 收到 type == "runtime.entry_password_missing"；syncContainerPassword 未被调用；
      - 正常成功：succeeded=1；stubEvents 至少收到 "runtime.entry_password_resynced" 与 "admin.hosts.password_resync_triggered" 两类事件；syncContainerPassword 被调用一次，接收到的 password 参数等于测试预设值（仅测试本地内存，不暴露到响应）。
    - 测试使用 `syncContainerPassword = func(...) error { ... }` 在测试内做临时替换，`t.Cleanup` 还原。
  </behavior>
  <action>
    1. 在 `stubHostStore` 结构体定义里新增字段：

       ```go
       runningHosts []repository.Host
       runningErr   error
       ```

       在方法集里新增：

       ```go
       func (s *stubHostStore) ListRunningHosts(_ context.Context) ([]repository.Host, error) {
           return s.runningHosts, s.runningErr
       }
       ```

    2. 确认测试文件里已有可复用的 stubEventRecorder（记录 RecordEvent 参数）；若没有，仿照 `internal/runtime/tasks/ssh_inject_test.go::fakeWorkerRepo` 的 RecordEvent 记录模式，在 admin_hosts_test.go 内新增一个仅实现 `RecordEvent` 的最小 stub，并把 `NewAdminHostsHandler` 构造注入到该 stub。（若现存代码已有等价 stub，直接复用，不要重复定义。）

    3. 新增 3 条 `t.Run` 风格或独立函数的测试（推荐独立函数，命名如下）：

       ```go
       func TestResyncPasswords_NoRunningHosts(t *testing.T) { ... }
       func TestResyncPasswords_SkipsEmptyEntryPassword(t *testing.T) { ... }
       func TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword(t *testing.T) { ... }
       ```

       每条用例的骨架：

       ```go
       // 公共 helper（可放 test 文件底部）
       func newTestAdminHostsHandler(t *testing.T, store AdminHostStore, events eventRecorder, queue HostActionQueuer) *AdminHostsHandler {
           t.Helper()
           return NewAdminHostsHandler(slog.New(slog.NewTextHandler(io.Discard, nil)), store, queue, events)
       }

       func TestResyncPasswords_NoRunningHosts(t *testing.T) {
           store := &stubHostStore{runningHosts: nil}
           events := newStubEvents()
           h := newTestAdminHostsHandler(t, store, events, &stubQueuer{})

           req := httptest.NewRequest("POST", "/v1/admin/hosts/resync-passwords", nil)
           rec := httptest.NewRecorder()
           h.ResyncPasswords().ServeHTTP(rec, req)

           if rec.Code != 200 { t.Fatalf("status=%d", rec.Code) }
           var body map[string]any
           if err := json.Unmarshal(rec.Body.Bytes(), &body); err != nil { t.Fatal(err) }
           if v, _ := body["total"].(float64); v != 0 { t.Errorf("total=%v, want 0", v) }
           if v, _ := body["succeeded"].(float64); v != 0 { t.Errorf("succeeded=%v, want 0", v) }
       }

       func TestResyncPasswords_SkipsEmptyEntryPassword(t *testing.T) {
           // 临时替换 syncContainerPassword，断言它不被调用
           orig := syncContainerPassword
           called := 0
           syncContainerPassword = func(containerName, user, password string) error {
               called++
               return nil
           }
           t.Cleanup(func() { syncContainerPassword = orig })

           store := &stubHostStore{runningHosts: []repository.Host{{ID: "h-empty", EntryPassword: ""}}}
           events := newStubEvents()
           h := newTestAdminHostsHandler(t, store, events, &stubQueuer{})

           req := httptest.NewRequest("POST", "/v1/admin/hosts/resync-passwords", nil)
           rec := httptest.NewRecorder()
           h.ResyncPasswords().ServeHTTP(rec, req)

           if rec.Code != 200 { t.Fatalf("status=%d", rec.Code) }
           if called != 0 {
               t.Errorf("syncContainerPassword must NOT be called for empty EntryPassword; called=%d", called)
           }
           if !events.hasType("runtime.entry_password_missing") {
               t.Error("expected runtime.entry_password_missing event")
           }
       }

       func TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword(t *testing.T) {
           orig := syncContainerPassword
           var gotPassword string
           var gotUser string
           var gotContainer string
           syncContainerPassword = func(containerName, user, password string) error {
               gotContainer = containerName
               gotUser = user
               gotPassword = password
               return nil
           }
           t.Cleanup(func() { syncContainerPassword = orig })

           const samplePassword = "PLACEHOLDER-8C"
           store := &stubHostStore{runningHosts: []repository.Host{{ID: "h-ok", EntryPassword: samplePassword}}}
           events := newStubEvents()
           h := newTestAdminHostsHandler(t, store, events, &stubQueuer{})

           req := httptest.NewRequest("POST", "/v1/admin/hosts/resync-passwords", nil)
           rec := httptest.NewRecorder()
           h.ResyncPasswords().ServeHTTP(rec, req)

           if rec.Code != 200 { t.Fatalf("status=%d body=%s", rec.Code, rec.Body.String()) }
           if gotContainer != "cloudproxy-h-ok" { t.Errorf("container=%q", gotContainer) }
           if gotUser != "workspace" { t.Errorf("user=%q", gotUser) }
           if gotPassword != samplePassword { t.Errorf("password argument not forwarded correctly") }
           if !events.hasType("runtime.entry_password_resynced") {
               t.Error("expected runtime.entry_password_resynced event")
           }
           if !events.hasType("admin.hosts.password_resync_triggered") {
               t.Error("expected admin.hosts.password_resync_triggered event")
           }
           // 断言响应体 results 不泄漏密码
           if strings.Contains(rec.Body.String(), samplePassword) {
               t.Error("response body must not contain the entry_password sample")
           }
       }
       ```

       注意事项：
       - `samplePassword` 使用占位符字符串（如 `PLACEHOLDER-8C`）；禁止使用类似真实的 8 位随机密码；
       - 若现有测试文件已有 eventRecorder stub，直接复用，不要重命名；
       - `AdminHostsHandler` 构造签名以当前代码为准：若 events 是 interface 则直接传 stub，若要 nil 也允许（但本测试必须传非 nil 以断言事件）。
       - `stubQueuer` 本测试不触发入队，传零值即可。

    4. 严禁：
       - 引入 httptest + 实 docker 的集成框架；
       - 引入 testcontainers；
       - 在测试文件里硬编码真实密码；
       - 触碰 `TestAdminHostsHandler` 既有表格，若需新增用例，放在独立 Test 函数。
  </action>
  <verify>
    <automated>go test ./internal/controlplane/http/... -run 'TestResyncPasswords_' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'TestResyncPasswords_NoRunningHosts' internal/controlplane/http/admin_hosts_test.go 命中 1 行
    - rg -n 'TestResyncPasswords_SkipsEmptyEntryPassword' internal/controlplane/http/admin_hosts_test.go 命中 1 行
    - rg -n 'TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword' internal/controlplane/http/admin_hosts_test.go 命中 1 行
    - rg -n 'func \(s \*stubHostStore\) ListRunningHosts' internal/controlplane/http/admin_hosts_test.go 命中 1 行
    - go test ./internal/controlplane/http/... -count=1 exit 0（全包回归未破坏）
    - go test ./internal/controlplane/http/... -run TestResyncPasswords_ -v 的三条用例全部 PASS
    - rg -E '"[0-9a-zA-Z]{8}"' internal/controlplane/http/admin_hosts_test.go 的命中行中不得出现形如真实随机密码的样本（本测试用固定占位符 `PLACEHOLDER-8C`）
  </acceptance_criteria>
  <done>
    stub 已扩；3 条 handler 单测全部 PASS；syncContainerPassword 调用与事件触发被精准断言；响应体不泄漏密码样本。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.4: 人工 UAT —— 创建主机 + SSH 登录 + 容器内自检 + 批量 resync 冒烟</name>
  <files>(manual verification; no source files modified in this task)</files>
  <read_first>
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-01-PLAN.md / 29.1-02-PLAN.md / 29.1-03-PLAN.md（确认前三 plan 已全部 merge / running）
    - Plan 04 Task 4.1 / 4.2 / 4.3 已完成并 commit
  </read_first>
  <what-built>
    - Plan 01 仓储层修复 + Plan 02 worker fail-fast + Plan 03 entrypoint 自检 + Plan 04 批量 resync 端点
    - 端到端：新建 host → 后台返回 entry_password 与 DB 一致 → SSH 登录容器成功 → 容器内 `$CONTAINER_SSH_PASSWORD` 与 API 返回值一致 → `passwd -S workspace` 正常
    - 批量 resync 端点对至少 1 个 running host 能触发事件 + 返回 succeeded ≥ 1
  </what-built>
  <action>
    人工 UAT（不可自动化；需真实环境 Docker daemon、SSH proxy 与 admin token）。执行者必须严格遵循 `how-to-verify` 所列 7 个步骤，任何步骤失败即停止并在 resume-signal 中报告。禁止在任何记录（commit message / SUMMARY / event / log）中粘贴真实 entry_password 明文，统一用占位符 `<entry_password>`。
  </action>
  <how-to-verify>
    以下步骤在预生产 / staging 环境执行；生产环境建议先对 1 台 host 做单独验证后再跑全量 resync。

    1. 重新构建镜像并部署：
       - 后端：`go build ./... && ./bin/control-plane &`；host-agent 同步部署。
       - 镜像：`docker build -f deploy/docker/managed-user/Dockerfile -t cloudproxy/managed-user:phase-29.1-test .`（或按项目既有 build 流程）。

    2. 通过 admin API 或后台 UI 创建一个新 host：
       - 记录返回体中的 `host_id`、`short_id`、`entry_password`（占位符记录为 `<entry_password>`）。
       - 等待 host 状态变为 `running`。

    3. 在宿主机上：
       ```
       docker exec cloudproxy-<host_id> env | grep CONTAINER_
       ```
       断言：`CONTAINER_SSH_PASSWORD` 的值与步骤 2 API 返回的 `<entry_password>` **一致**；不是 `workspace`。

    4. 再执行：
       ```
       docker exec cloudproxy-<host_id> passwd -S workspace
       ```
       断言：第 2 列为 `P` 或 `PS`。

    5. 从外部（你的工作机）用 short_id 登录：
       ```
       ssh <short_id>@<proxy-host> -p 2222
       ```
       输入步骤 2 的 `<entry_password>`；预期：成功登录到容器 shell。

    6. 故障注入验证（可选但强烈建议）：
       - 在宿主机手动执行 `docker exec -i cloudproxy-<host_id> chpasswd <<< 'workspace:workspace'` 把密码改回 `workspace`；
       - 以步骤 5 的 entry_password 再次 `ssh` 应失败；
       - 调用 `POST /v1/admin/hosts/resync-passwords`（携带 admin 鉴权 header）；响应应为 JSON `{"total": N, "succeeded": ≥ 1, ...}`；
       - 再次 `ssh` 应成功；
       - 检查 control-plane 日志或事件表：应出现 `admin.hosts.password_resync_triggered` 与 `runtime.entry_password_resynced` 事件。

    7. 故障注入 entrypoint 自检（强烈建议，若有 sandbox 镜像）：
       - 手动 `docker run --rm -e CONTAINER_SSH_PASSWORD="" cloudproxy/managed-user:phase-29.1-test` 观察；
       - 或者进入容器后 `passwd -l workspace` 锁定密码再重启容器；
       - 预期：容器启动时 stderr 出现 `[entrypoint] FATAL: password status for workspace is '...', refusing to start` 且进程以非 0 退出。

    记录验证结果（成功的步骤编号 + 任何异常）到 `29.1-04-SUMMARY.md` 或本 checkpoint 的 resume 回执中；**绝不**粘贴真实 entry_password，统一用 `<entry_password>` 占位符。
  </how-to-verify>
  <verify>
    <automated>MISSING — 端到端 SSH 登录与 Docker 容器密码状态校验无法自动化，必须由人工按 how-to-verify 7 步执行后在 resume-signal 处回执 "approved"</automated>
  </verify>
  <acceptance_criteria>
    - how-to-verify 步骤 1-5 全部观察到预期结果（新建 host / docker env 与 API 一致 / passwd -S 为 P|PS / SSH 登录成功）
    - how-to-verify 步骤 6 批量 resync：HTTP 200 + JSON succeeded ≥ 1；事件表新增 `admin.hosts.password_resync_triggered` 与 `runtime.entry_password_resynced`
    - how-to-verify 步骤 7 entrypoint 自检：锁定密码的容器启动失败、退出码非 0、stderr 出现 `[entrypoint] FATAL: password status for workspace is`
    - resume-signal 回执中不包含任何真实 entry_password 明文字符串（仅占位符 `<entry_password>`）
    - SUMMARY 中记录的步骤观测 ≥ 前 5 条必检 + 步骤 6 必检；步骤 7 若环境不具备可标注 "skipped with reason" 但需给出理由
  </acceptance_criteria>
  <done>
    人工 UAT 至少步骤 1-6 全部通过；步骤 7 通过或有明确跳过理由；回执不泄漏密码；SUMMARY 已记录完整验证结果。
  </done>
  <resume-signal>
    回复 "approved" 表示所有步骤符合预期；否则列出具体失败步骤与观察到的实际行为（并用占位符替换任何密码样本）。
  </resume-signal>
</task>

</tasks>

<verification>
- `go build ./...` 成功
- `go vet ./internal/controlplane/...` 成功
- `go test ./internal/controlplane/http/... -count=1` 全部 PASS
- 人工 UAT 步骤 1-5 全绿；步骤 6-7 故障注入验证若条件允许也应全绿
- `rg '"entry_password"' internal/controlplane/http/admin_hosts.go` —— 命中仅限于既有脱敏/置空的调用（如 List / Get 显式置空），不应有新增把密码写入 HTTP 响应或事件的行
</verification>

<success_criteria>
- admin 可以通过 `POST /v1/admin/hosts/resync-passwords` 一次性同步所有 running 容器密码
- 端点复用 adminGuard；响应体 / 事件 Metadata 不含密码明文
- 3 条 handler 单测稳定 PASS
- 端到端人工 UAT 通过：新建 host → API 返回 entry_password → SSH 登录成功 → 容器内 env 与 API 一致 → passwd -S 正常
- 故障注入回路验证：把容器内密码人为改回 "workspace" 后，resync 能恢复；entrypoint 自检对锁定状态容器能 exit 1
- 汇总来看：Phase 29.1 goal 全 8 条 must-haves 闭环（仓储 / 测试 / worker fail-fast / worker 单测 / entrypoint 自检 / 批量同步 / e2e / 日志不泄漏）
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-gethost-entry-password-workspace/29.1-04-SUMMARY.md` 汇报：
- AdminHostStore 接口最终方法集与 syncContainerPassword var 化差异
- ResyncPasswords 新增行数 / 路由变更
- 3 条单测运行结果（`go test -v -run TestResyncPasswords_ ./internal/controlplane/http/...`）
- 人工 UAT 每步实际结果（用 `<entry_password>` 占位符）
- 任何故障注入验证发现的弱点（若有）
</output>
