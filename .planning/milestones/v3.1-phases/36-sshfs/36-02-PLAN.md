---
phase: 36-sshfs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloudclaude/config.go
  - internal/cloudclaude/last_session.go
  - internal/cloudclaude/last_session_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-02
  - REQ-MOUNT-V31-03

must_haves:
  truths:
    - "Config struct 有 HotSyncMaxFileMB int 字段，yaml tag hot_sync_max_file_mb,omitempty"
    - "Config.EffectiveHotSyncMaxFileMB() 零值/负值时返回 50，正值返回自身"
    - "LastSessionSnapshot.OversizedFiles []OversizedFile 字段存在，omitempty"
    - "OversizedFile struct 有 Path string + SizeBytes int64 两个字段"
    - "schema_version 序列化为 1 不变（OversizedFiles 空时 omitempty 省略）"
  artifacts:
    - path: "internal/cloudclaude/config.go"
      provides: "HotSyncMaxFileMB 字段 + EffectiveHotSyncMaxFileMB() accessor"
      contains: "HotSyncMaxFileMB"
    - path: "internal/cloudclaude/last_session.go"
      provides: "OversizedFiles 字段 + OversizedFile struct"
      contains: "OversizedFile"
  key_links:
    - from: "internal/cloudclaude/config.go"
      to: "internal/cloudclaude/hot_sync.go"
      via: "cfg.EffectiveHotSyncMaxFileMB() * 1024 * 1024 → HotSyncConfig.MaxFileBytes（Plan 03 注入）"
      pattern: "EffectiveHotSyncMaxFileMB"
    - from: "internal/cloudclaude/last_session.go"
      to: "internal/cloudclaude/mount_strategy.go"
      via: "snapshot.OversizedFiles = hotStatus.OversizedFiles（Plan 03 赋值）"
      pattern: "OversizedFiles"
---

<objective>
建立 Phase 36 单文件熔断所需的两个数据契约：(1) Config 新增 HotSyncMaxFileMB 配置字段和 EffectiveHotSyncMaxFileMB() accessor；(2) LastSessionSnapshot 新增 OversizedFiles 数组字段和 OversizedFile struct。

Purpose: 本 plan 与 Plan 01 并行（Wave 1），为 Plan 03（hot_sync 熔断逻辑）提供输入类型和输出类型。两个 struct 改动均为纯加法（omitempty），不影响任何既有测试或已落地数据。
Output: 已可编译的 HotSyncMaxFileMB 配置接口 + OversizedFile/OversizedFiles 数据契约 + 序列化/反序列化单测通过
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-sshfs/36-CONTEXT.md
@.planning/phases/36-sshfs/36-RESEARCH.md
@.planning/phases/36-sshfs/36-PATTERNS.md

<interfaces>
<!-- 当前 Config struct（config.go lines 20-25），executor 在此基础上追加 -->
From internal/cloudclaude/config.go:
```go
type Config struct {
    Gateway       string   `yaml:"gateway"`
    ShortID       string   `yaml:"short_id"`
    Password      string   `yaml:"password"`
    ProxyCommands []string `yaml:"proxy_commands,omitempty"`
}

// 现有 accessor 模式（EffectiveProxyCommands）：
func (c *Config) EffectiveProxyCommands() []string {
    if len(c.ProxyCommands) > 0 {
        return c.ProxyCommands
    }
    return DefaultProxyCommands
}
```

<!-- 当前 LastSessionSnapshot struct（last_session.go 约 lines 10-38），executor 在末尾追加 -->
From internal/cloudclaude/last_session.go (Phase 32 D-27 omitempty 追加模式):
```go
type LastSessionSnapshot struct {
    SchemaVersion  int    `json:"schema_version"`
    // ...既有字段（含 Phase 32 D-27 的 TmuxSession/ClientRole/ReconnectCount omitempty）...
    // [Phase 36 D-09] OversizedFiles 追加在末尾
}

// Phase 32 D-27 追加示范：
TmuxSession    string `json:"tmux_session,omitempty"`
ClientRole     string `json:"client_role,omitempty"`
ReconnectCount int    `json:"reconnect_count,omitempty"`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config.HotSyncMaxFileMB 字段 + EffectiveHotSyncMaxFileMB() accessor</name>
  <read_first>
    - internal/cloudclaude/config.go（全文，103 行，确认 Config struct 末尾 + EffectiveProxyCommands 位置）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-04（accessor 字面实现 + 零值语义）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/config.go"（精确 copy-from 代码块）
  </read_first>
  <action>
**Step 1 — Config struct 末尾追加字段：**

在 `ProxyCommands []string \`yaml:"proxy_commands,omitempty"\`` 行之后追加：
```go
HotSyncMaxFileMB int `yaml:"hot_sync_max_file_mb,omitempty"`
```

**Step 2 — 新增 const + accessor（紧跟 EffectiveProxyCommands 方法之后）：**

```go
const defaultHotSyncMaxFileMB = 50

// EffectiveHotSyncMaxFileMB 返回有效的单文件热同步大小上限（MB）。
// 零值或负值时返回默认值 50MB（D-04）。
func (c *Config) EffectiveHotSyncMaxFileMB() int {
    if c.HotSyncMaxFileMB <= 0 {
        return defaultHotSyncMaxFileMB
    }
    return c.HotSyncMaxFileMB
}
```

不改动 Validate() 函数（D-04：不强校验上限）。
  </action>
  <acceptance_criteria>
    - `grep -c "HotSyncMaxFileMB" internal/cloudclaude/config.go` 输出 `3`（struct 字段 1 + accessor 方法签名 1 + if 判断 1）
    - `grep "hot_sync_max_file_mb" internal/cloudclaude/config.go` 含 omitempty
    - `grep "defaultHotSyncMaxFileMB = 50" internal/cloudclaude/config.go` 存在
    - `go build ./internal/cloudclaude/...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/... && grep -c "HotSyncMaxFileMB" internal/cloudclaude/config.go</automated>
  </verify>
  <done>Config 有 HotSyncMaxFileMB 字段和 EffectiveHotSyncMaxFileMB() accessor，零值返回 50，go build 干净</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LastSessionSnapshot.OversizedFiles 字段 + OversizedFile struct + 序列化测试</name>
  <read_first>
    - internal/cloudclaude/last_session.go（全文，125 行，确认 LastSessionSnapshot struct 末尾 + DowngradeStep struct 位置）
    - internal/cloudclaude/last_session_test.go（全文，确认现有序列化测试模式 + 文件末尾）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-09（OversizedFile struct 字面定义 + schema_version=1 不变）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/last_session.go"（Phase 32 omitempty 追加模式）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/last_session_test.go"（roundtrip 测试模板）
  </read_first>
  <behavior>
    - Test 1: snapshot 含 OversizedFiles，序列化后 json 含 "oversized_files" 键，反序列化后字段正确
    - Test 2: snapshot OversizedFiles 为 nil（空），序列化后 json 不含 "oversized_files" 键（omitempty）
    - Test 3: 序列化后 schema_version 为 1
  </behavior>
  <action>
**Step 1 — last_session.go：在文件中 DowngradeStep struct 附近（或与 LastSessionSnapshot 同位置）追加 OversizedFile struct：**

```go
// OversizedFile 描述热同步阶段因超过大小阈值而被跳过的单个文件。
// 对应 REQUIREMENTS.md REQ-MOUNT-V31-03 / Phase 36 D-09。
type OversizedFile struct {
    Path      string `json:"path"`        // cwd 相对路径
    SizeBytes int64  `json:"size_bytes"`
}
```

**Step 2 — LastSessionSnapshot struct 末尾追加字段（在既有 Phase 32 omitempty 字段之后）：**

```go
// [Phase 36 D-09] omitempty + schema_version=1 不变（向后兼容）
OversizedFiles []OversizedFile `json:"oversized_files,omitempty"`
```

LoadLastSession() 和 WriteLastSession() 函数无需改动——json.Marshal/Unmarshal 自动处理新字段。

**Step 3 — last_session_test.go 末尾追加 3 条测试：**

```go
func TestLastSession_OversizedFiles_Roundtrip(t *testing.T) {
    snap := LastSessionSnapshot{
        SchemaVersion: 1,
        OversizedFiles: []OversizedFile{
            {Path: "assets/video.mp4", SizeBytes: 60 * 1024 * 1024},
        },
    }
    data, err := json.Marshal(snap)
    if err != nil {
        t.Fatalf("Marshal 失败: %v", err)
    }
    var got LastSessionSnapshot
    if err := json.Unmarshal(data, &got); err != nil {
        t.Fatalf("Unmarshal 失败: %v", err)
    }
    if got.SchemaVersion != 1 {
        t.Errorf("schema_version 应为 1，got %d", got.SchemaVersion)
    }
    if len(got.OversizedFiles) != 1 {
        t.Fatalf("OversizedFiles 长度应为 1，got %d", len(got.OversizedFiles))
    }
    if got.OversizedFiles[0].Path != "assets/video.mp4" {
        t.Errorf("Path 不匹配: %q", got.OversizedFiles[0].Path)
    }
    if got.OversizedFiles[0].SizeBytes != 60*1024*1024 {
        t.Errorf("SizeBytes 不匹配: %d", got.OversizedFiles[0].SizeBytes)
    }
}

func TestLastSession_OversizedFiles_OmitemptyEmpty(t *testing.T) {
    snap := LastSessionSnapshot{SchemaVersion: 1}
    data, _ := json.Marshal(snap)
    if strings.Contains(string(data), "oversized_files") {
        t.Error("空 OversizedFiles 应被 omitempty 省略，但 JSON 中出现了 oversized_files 键")
    }
}

func TestLastSession_OversizedFiles_OmitemptyNil(t *testing.T) {
    snap := LastSessionSnapshot{SchemaVersion: 1, OversizedFiles: nil}
    data, _ := json.Marshal(snap)
    if strings.Contains(string(data), "oversized_files") {
        t.Error("nil OversizedFiles 应被 omitempty 省略")
    }
}
```

import 段确认已有 `"encoding/json"` 和 `"strings"`（若无则追加）。
  </action>
  <acceptance_criteria>
    - `grep -c "OversizedFile" internal/cloudclaude/last_session.go` ≥ 3（struct 定义 1 + 字段类型 1 + json tag 相关）
    - `grep "oversized_files,omitempty" internal/cloudclaude/last_session.go` 存在
    - `grep "schema_version" internal/cloudclaude/last_session.go` 仍存在且 SchemaVersion=1 不变
    - `go test ./internal/cloudclaude/... -run TestLastSession_OversizedFiles -v` 输出 3 条 PASS
    - `go test ./internal/cloudclaude/... -v` 全包无 FAIL（包括既有 Phase 32 序列化测试）
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/cloudclaude/... -run TestLastSession -v</automated>
  </verify>
  <done>last_session.go 含 OversizedFile struct + OversizedFiles []OversizedFile omitempty 字段，序列化测试 3 条全 PASS，既有测试无回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| config.yaml → Config struct | 用户可编辑；HotSyncMaxFileMB 零/负值走默认，不校验上限（D-04 决策） |
| last-session.json → LastSessionSnapshot | CLI 本地写入；OversizedFiles 仅含相对路径，不含认证信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering | HotSyncMaxFileMB=0 | accept | 零值走 defaultHotSyncMaxFileMB=50，不产生安全漏洞；超大值（如 1000）用户自负风险，doctor disk 维度会间接发现 |
| T-36-02-02 | Information Disclosure | last-session.json OversizedFiles.Path | mitigate | Path 使用 cwd 相对路径（非绝对路径），不暴露家目录结构；doctor oversized_files_count check 的 Details 也只列相对路径 |
| T-36-02-03 | Denial of Service | oversized_files 数组无限增长 | accept | 每次 mount 启动重新写入（不累积）；Plan 03 初始扫描结果覆盖上次，本次会话最大 N 个文件 |
</threat_model>

<verification>
```bash
go test ./internal/cloudclaude/... -v
go build ./...
```
期望：全 PASS，无编译错误。
</verification>

<success_criteria>
- Config 新增 HotSyncMaxFileMB int yaml字段和 EffectiveHotSyncMaxFileMB() accessor（零值返回 50）
- last_session.go 新增 OversizedFile struct 和 OversizedFiles []OversizedFile omitempty 字段
- schema_version=1 不变，WriteLastSession/LoadLastSession 无需改动
- last_session_test.go 新增 3 条序列化/omitempty 测试全 PASS
- go test ./internal/cloudclaude/... 全包无 FAIL
</success_criteria>

<output>
完成后创建 `.planning/phases/36-sshfs/36-02-SUMMARY.md`，记录：
- Config.HotSyncMaxFileMB yaml tag 精确字面量
- OversizedFile struct 字段列表
- 任何偏差说明
</output>
