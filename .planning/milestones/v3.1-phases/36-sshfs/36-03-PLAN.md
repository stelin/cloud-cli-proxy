---
phase: 36-sshfs
plan: "03"
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - internal/cloudclaude/hot_sync.go
  - internal/cloudclaude/mount_strategy.go
  - internal/cloudclaude/hot_sync_oversized_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-02
  - REQ-MOUNT-V31-03

must_haves:
  truths:
    - "60MB 未 ignore 文件进入 HotSyncStatus.OversizedFiles 列表，不复制到 hot 分支"
    - "30MB 未 ignore 文件不进入 OversizedFiles，正常进入 hot 分支"
    - "ignore 命中的 50MB+ 文件不进入 OversizedFiles（第一层过滤优先）"
    - "syncOnce 轮询阶段也跳过大文件（L3 地雷已修复）"
    - "last-session.json::oversized_files 在会话结束后可读取"
    - "启动后 stderr 一次性输出 [!] 跳过大文件 N 个 提示（N>0 时）"
  artifacts:
    - path: "internal/cloudclaude/hot_sync.go"
      provides: "HotSyncConfig.MaxFileBytes + HotSyncEngine.maxFileBytes/oversized + HotSyncStatus.OversizedFiles + initialSync/syncOnce 过滤"
      contains: "MaxFileBytes"
    - path: "internal/cloudclaude/mount_strategy.go"
      provides: "MaxFileBytes 注入 + OversizedFiles 写入 last-session + D-08 stderr 提示"
      contains: "OversizedFiles"
    - path: "internal/cloudclaude/hot_sync_oversized_test.go"
      provides: "三场景过滤断言"
      contains: "TestHotSyncOversized"
  key_links:
    - from: "internal/cloudclaude/config.go"
      to: "internal/cloudclaude/hot_sync.go"
      via: "mount_strategy 注入 cfg.EffectiveHotSyncMaxFileMB()*1024*1024 → HotSyncConfig.MaxFileBytes"
      pattern: "MaxFileBytes.*1024.*1024"
    - from: "internal/cloudclaude/hot_sync.go"
      to: "internal/cloudclaude/mount_strategy.go"
      via: "StartHotSync 返回 HotSyncStatus{OversizedFiles: engine.oversized}"
      pattern: "OversizedFiles.*engine\\.oversized"
---

<objective>
在 HotSyncEngine 的初始扫描（initialSync）和轮询同步（syncOnce）两处注入单文件大小熔断逻辑，将超阈值且未被 ignore 的文件排除出热同步范围，并通过 HotSyncStatus 将列表传递给 mount_strategy，最终写入 last-session.json 并在 stderr 输出一次性提示。

Purpose: 本 plan 实现 SC#2「60MB fixture 不出现在 hot tree」的核心逻辑。关键地雷（L3）：仅在 initialSync 过滤不够，syncOnce 轮询阶段也必须过滤，否则用户中途新增大文件会被上传到 hot 分支。
Output: hot_sync.go 含完整熔断过滤逻辑 + mount_strategy.go 注入传递 + hot_sync_oversized_test.go 三场景覆盖
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-sshfs/36-CONTEXT.md
@.planning/phases/36-sshfs/36-RESEARCH.md
@.planning/phases/36-sshfs/36-PATTERNS.md

<interfaces>
<!-- Plan 02 产出（依赖项） -->
From internal/cloudclaude/last_session.go (Plan 02 新增):
```go
type OversizedFile struct {
    Path      string `json:"path"`
    SizeBytes int64  `json:"size_bytes"`
}
// LastSessionSnapshot.OversizedFiles []OversizedFile `json:"oversized_files,omitempty"`
```

From internal/cloudclaude/config.go (Plan 02 新增):
```go
func (c *Config) EffectiveHotSyncMaxFileMB() int { ... } // 零值返回 50
```

<!-- hot_sync.go 现有结构（RESEARCH.md F2 锚点） -->
From internal/cloudclaude/hot_sync.go (关键现有 struct):
```go
type HotSyncConfig struct {
    LocalDir    string
    RemoteDir   string
    // ... 其他字段
    // [Phase 36 D-05] 追加：
    MaxFileBytes int64
}

type HotSyncStatus struct {
    ConflictCount int
    // [Phase 36 D-07] 追加：
    OversizedFiles []OversizedFile
}

// StartHotSync(connA, connB ssh.Conn, cfg HotSyncConfig) (cleanup func(), status HotSyncStatus, err error)
// engine.initialSync() — line ~145
// engine.syncOnce() — line ~195
// scanLocalSyncFiles(localDir string, matcher IgnoreMatcher) (map[string]syncFileState, error) — line 388
// syncFileState 含 Size int64
```

<!-- mount_strategy.go 现有注入点（RESEARCH.md F2 + PATTERNS.md） -->
From internal/cloudclaude/mount_strategy.go (tryModeReal，约 line 222-230):
```go
snapshot.ActualMode = mode.String()
snapshot.ConflictCount = hotStatus.ConflictCount
// [Phase 36 D-09 追加] snapshot.OversizedFiles = hotStatus.OversizedFiles

printBanner(cfg.Logger, mode, cfg.NoColor)
writeLastSessionWarn(cfg.LastSessionPath, snapshot, cfg.Logger)
```

MountConfig struct 需追加 HotSyncMaxFileMB int（从 main.go 注入 cfg.EffectiveHotSyncMaxFileMB()）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HotSyncEngine 熔断字段 + initialSync/syncOnce 过滤逻辑</name>
  <read_first>
    - internal/cloudclaude/hot_sync.go（全文；重点段：lines 66-83 Engine struct，88-130 StartHotSync，145-187 initialSync，195-280 syncOnce，388-428 scanLocalSyncFiles）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F2 锚点" §"F2 并发安全分析" §"L3 地雷"（syncOnce 也需过滤）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/hot_sync.go"（struct 扩展 + 方案 A 代码块 + syncOnce 过滤代码块 + 两层熔断互补注记）
    - .planning/milestones/v3.0-phases/31-cli/31-CONTEXT.md §D-11 §D-12（整目录级熔断——必须保留，不得删除或合并）
  </read_first>
  <action>
**Step 1 — HotSyncConfig struct 追加 MaxFileBytes 字段：**

在 HotSyncConfig struct 末尾追加：
```go
MaxFileBytes int64 // D-05：单文件热同步大小上限（bytes），零值表示不启用熔断
```

**Step 2 — HotSyncEngine struct 追加 2 个字段：**

在 HotSyncEngine struct 末尾追加：
```go
maxFileBytes int64          // D-05：从 HotSyncConfig 复制
oversized    []OversizedFile // D-06/07：initialSync 阶段填充，run() goroutine 只读
```

**Step 3 — StartHotSync：从 cfg.MaxFileBytes 初始化 engine 字段：**

在 `engine := &HotSyncEngine{...}` 构造后追加赋值：
```go
engine.maxFileBytes = cfg.MaxFileBytes
```

**Step 4 — initialSync()：在 scanLocalSyncFiles 返回后、resetRemote 分支之前，注入方案 A 后处理过滤：**

```go
// [Phase 36 D-06] 单文件大小熔断（第二层，ignore 过滤已在 scanLocalSyncFiles 内完成）
// 注意：不得删除 Phase 31 D-11 的整目录级 SkipDir 过滤（在 scanLocalSyncFiles 内部，不受此代码影响）
if e.maxFileBytes > 0 {
    for rel, state := range localFiles {
        if state.Size >= e.maxFileBytes {
            e.oversized = append(e.oversized, OversizedFile{Path: rel, SizeBytes: state.Size})
            delete(localFiles, rel)
        }
    }
}
```

**Step 5 — syncOnce()：在 scanLocalSyncFiles 返回后同样过滤（L3 地雷修复）：**

```go
// [Phase 36 L3] syncOnce 也跳过大文件，防止用户中途新增大文件被推到 hot
// 仅静默跳过，不更新 e.oversized（初始扫描列表已写入 last-session.json，不刷屏）
if e.maxFileBytes > 0 {
    for rel, state := range localFiles {
        if state.Size >= e.maxFileBytes {
            delete(localFiles, rel)
        }
    }
}
```

**Step 6 — StartHotSync 返回值携带 OversizedFiles：**

将原来的 `return cleanup, HotSyncStatus{}, nil`（或类似行）改为：
```go
return cleanup, HotSyncStatus{
    OversizedFiles: engine.oversized,
}, nil
```

注意：如果 HotSyncStatus 已有 ConflictCount 字段的赋值，需保留原有字段，只追加 OversizedFiles。
  </action>
  <acceptance_criteria>
    - `grep -c "MaxFileBytes" internal/cloudclaude/hot_sync.go` 输出 ≥ 3（struct 字段 + 赋值 + if 判断）
    - `grep -c "oversized" internal/cloudclaude/hot_sync.go` 输出 ≥ 4（engine 字段 + append + delete + StartHotSync 返回）
    - `grep "Phase 31 D-11" internal/cloudclaude/hot_sync.go` 存在（注释确认保留整目录级过滤）
    - `grep "syncOnce" internal/cloudclaude/hot_sync.go` 含 L3 相关过滤代码块
    - `go build ./internal/cloudclaude/...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/... && grep -c "MaxFileBytes" internal/cloudclaude/hot_sync.go</automated>
  </verify>
  <done>hot_sync.go 含完整熔断字段和过滤逻辑，initialSync + syncOnce 均跳过大文件，StartHotSync 返回 OversizedFiles，整目录级 D-11 过滤保留不受影响</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mount_strategy MountConfig 注入 + OversizedFiles 持久化 + stderr 提示 + 熔断测试</name>
  <read_first>
    - internal/cloudclaude/mount_strategy.go（全文，约 400 行；重点：MountConfig struct，tryModeReal lines ~280-320，writeLastSessionWarn 调用位置）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F2 锚点" §"D-08 stderr 一次性输出"（代码块）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/mount_strategy.go"（精确修改代码块含 D-08/D-09）
    - internal/cloudclaude/hot_sync_oversized_test.go（若文件存在则读取；否则新建）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/hot_sync_oversized_test.go"（三场景测试模板）
  </read_first>
  <behavior>
    - Test 1: 60MB 未 ignore 文件 → OversizedFiles 含该文件，delete 出 localFiles
    - Test 2: ignore 命中的 60MB 文件 → OversizedFiles 为空（第一层 ignore 已跳过）
    - Test 3: 30MB 未 ignore 文件 → OversizedFiles 为空（未超阈值 50MB），正常留在 localFiles
  </behavior>
  <action>
**Step 1 — MountConfig struct 追加 HotSyncMaxFileMB int 字段：**

在 MountConfig struct 末尾追加（不影响现有调用）：
```go
HotSyncMaxFileMB int // D-04：cfg.EffectiveHotSyncMaxFileMB() 注入，用于 D-08 stderr 提示
```

**Step 2 — MountWorkspace 或 tryModeReal 中，StartHotSync 调用处注入 MaxFileBytes：**

找到 `HotSyncConfig{...}` 构造处，追加：
```go
MaxFileBytes: int64(cfg.EffectiveHotSyncMaxFileMB()) * 1024 * 1024, // D-05
```

（注意：cfg 在此处应为 MountConfig 类型；EffectiveHotSyncMaxFileMB() 来自 Config struct，需通过 MountConfig 透传或在此处直接计算）

**Step 3 — tryModeReal 中 hotStatus 赋值处追加 D-08/D-09（在 snapshot.ConflictCount 赋值行之后）：**

```go
snapshot.ConflictCount = hotStatus.ConflictCount
snapshot.OversizedFiles = hotStatus.OversizedFiles // [Phase 36 D-09]

// [Phase 36 D-08] 一次性 stderr 提示（在 printBanner 之前）
if n := len(hotStatus.OversizedFiles); n > 0 {
    limit := n
    if limit > 5 {
        limit = 5
    }
    fmt.Fprintf(cfg.Logger, "[!] 跳过大文件 %d 个（>%dMB），由 cold 兜底:\n",
        n, cfg.HotSyncMaxFileMB)
    for _, f := range hotStatus.OversizedFiles[:limit] {
        fmt.Fprintf(cfg.Logger, "  %s (%dMB)\n", f.Path, f.SizeBytes/1024/1024)
    }
    if n > 5 {
        fmt.Fprintf(cfg.Logger, "  ... 还有 %d 个，见 ~/.cloud-claude/last-session.json\n", n-5)
    }
}
```

如果 cfg.HotSyncMaxFileMB 未在 MountConfig 中（因为 Config 和 MountConfig 是不同 struct），则替换为 `cfg.HotSyncMaxFileMB`（需确认字段是否已追加）或 `hotStatus.OversizedFiles[0].SizeBytes/1024/1024` 辅助显示。

**Step 4 — 新建 internal/cloudclaude/hot_sync_oversized_test.go：**

```go
package cloudclaude

import (
    "path/filepath"
    "os"
    "testing"
)

func createFixtureFile(t *testing.T, path string, size int64) {
    t.Helper()
    f, err := os.Create(path)
    if err != nil {
        t.Fatalf("创建 fixture 文件失败: %v", err)
    }
    defer f.Close()
    if err := f.Truncate(size); err != nil {
        t.Fatalf("Truncate 失败: %v", err)
    }
}

// TestHotSyncOversized_60MB_NotIgnored 断言 60MB 未 ignore 文件进入 OversizedFiles
func TestHotSyncOversized_60MB_NotIgnored(t *testing.T) {
    dir := t.TempDir()
    createFixtureFile(t, filepath.Join(dir, "bigfile.bin"), 60*1024*1024)

    matcher := NewIgnoreMatcher(dir, nil) // 无额外 ignore 规则
    localFiles, err := scanLocalSyncFiles(dir, matcher)
    if err != nil {
        t.Fatalf("scanLocalSyncFiles 失败: %v", err)
    }

    const maxBytes = 50 * 1024 * 1024
    var oversized []OversizedFile
    for rel, state := range localFiles {
        if state.Size >= maxBytes {
            oversized = append(oversized, OversizedFile{Path: rel, SizeBytes: state.Size})
            delete(localFiles, rel)
        }
    }

    if len(oversized) != 1 {
        t.Fatalf("oversized 应含 1 条，got %d: %+v", len(oversized), oversized)
    }
    if oversized[0].Path != "bigfile.bin" {
        t.Errorf("oversized[0].Path 应为 bigfile.bin，got %q", oversized[0].Path)
    }
    if _, ok := localFiles["bigfile.bin"]; ok {
        t.Error("bigfile.bin 应已从 localFiles 中删除")
    }
}

// TestHotSyncOversized_IgnoreHit_NotCounted 断言 ignore 命中的大文件不进入 OversizedFiles
func TestHotSyncOversized_IgnoreHit_NotCounted(t *testing.T) {
    dir := t.TempDir()
    createFixtureFile(t, filepath.Join(dir, "ignored_big.bin"), 60*1024*1024)

    // 通过 .gitignore 或 ignore 模式匹配 ignored_big.bin
    matcher := NewIgnoreMatcher(dir, []string{"ignored_big.bin"})
    localFiles, err := scanLocalSyncFiles(dir, matcher)
    if err != nil {
        t.Fatalf("scanLocalSyncFiles 失败: %v", err)
    }

    const maxBytes = 50 * 1024 * 1024
    var oversized []OversizedFile
    for rel, state := range localFiles {
        if state.Size >= maxBytes {
            oversized = append(oversized, OversizedFile{Path: rel, SizeBytes: state.Size})
        }
    }

    if len(oversized) != 0 {
        t.Errorf("ignore 命中的文件不应进入 oversized，got %+v", oversized)
    }
}

// TestHotSyncOversized_30MB_NotOversized 断言 30MB 文件不进入 OversizedFiles
func TestHotSyncOversized_30MB_NotOversized(t *testing.T) {
    dir := t.TempDir()
    createFixtureFile(t, filepath.Join(dir, "medium.bin"), 30*1024*1024)

    matcher := NewIgnoreMatcher(dir, nil)
    localFiles, err := scanLocalSyncFiles(dir, matcher)
    if err != nil {
        t.Fatalf("scanLocalSyncFiles 失败: %v", err)
    }

    const maxBytes = 50 * 1024 * 1024
    var oversized []OversizedFile
    for rel, state := range localFiles {
        if state.Size >= maxBytes {
            oversized = append(oversized, OversizedFile{Path: rel, SizeBytes: state.Size})
        }
    }

    if len(oversized) != 0 {
        t.Errorf("30MB 文件不应进入 oversized，got %+v", oversized)
    }
    if _, ok := localFiles["medium.bin"]; !ok {
        t.Error("medium.bin 应保留在 localFiles 中")
    }
}
```

注意：
- `NewIgnoreMatcher` 需确认其签名（可能是 `NewIgnoreMatcher(dir string, extraPatterns []string) IgnoreMatcher`），executor 按实际签名调整。
- `scanLocalSyncFiles` 是 package-level 函数，在 `package cloudclaude` 内可直接调用。
- 如果 `NewIgnoreMatcher` 不接受 extraPatterns 参数，可改为在 dir 内创建 `.gitignore` 文件写入规则。
  </action>
  <acceptance_criteria>
    - `grep -c "OversizedFiles" internal/cloudclaude/mount_strategy.go` 输出 ≥ 2（赋值 + 打印循环）
    - `grep "跳过大文件" internal/cloudclaude/mount_strategy.go` 存在（D-08 stderr 提示字面量）
    - `grep "HotSyncMaxFileMB\|MaxFileBytes" internal/cloudclaude/mount_strategy.go` 存在（注入链）
    - `go test ./internal/cloudclaude/... -run TestHotSyncOversized -v` 输出 3 条 PASS
    - `go test ./internal/cloudclaude/... -v` 全包无 FAIL
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/cloudclaude/... -run TestHotSyncOversized -v && go test ./internal/cloudclaude/... -count=1 2>&1 | tail -5</automated>
  </verify>
  <done>mount_strategy.go 注入 MaxFileBytes + 写入 OversizedFiles + stderr 一次性提示，hot_sync_oversized_test.go 三场景全 PASS，全包无回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| 本地文件系统 → HotSyncEngine.oversized | 文件大小来自 os.FileInfo.Size()，不信任文件名；路径用 cwd 相对路径 |
| HotSyncStatus → mount_strategy.go → last-session.json | 写入本地 ~/.cloud-claude/，无跨进程协议变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Information Disclosure | OversizedFiles.Path 写入 stderr | mitigate | 使用 cwd 相对路径（非绝对路径），不暴露家目录结构；N>5 时截断显示 |
| T-36-03-02 | Tampering | syncOnce 绕过大文件过滤 | mitigate | L3 地雷已在 syncOnce 同样注入过滤逻辑，两处保持一致 |
| T-36-03-03 | Denial of Service | 大文件 oversized 切片内存占用 | accept | 会话期间上传文件路径列表，内存级别可忽略；会话结束后 GC 回收 |
</threat_model>

<verification>
```bash
go test ./internal/cloudclaude/... -v
go build ./...
```
期望：全 PASS，含 TestHotSyncOversized* 三条新测试。
</verification>

<success_criteria>
- hot_sync.go HotSyncConfig 有 MaxFileBytes int64，HotSyncEngine 有 maxFileBytes/oversized，HotSyncStatus 有 OversizedFiles
- initialSync() 过滤大文件进 engine.oversized，syncOnce() 静默跳过大文件（L3 修复）
- Phase 31 D-11 整目录级熔断保留（注释确认）
- StartHotSync 返回 HotSyncStatus{OversizedFiles: engine.oversized}
- mount_strategy.go 注入 MaxFileBytes，写入 snapshot.OversizedFiles，输出 D-08 一次性 stderr 提示
- hot_sync_oversized_test.go 三场景 PASS
</success_criteria>

<output>
完成后创建 `.planning/phases/36-sshfs/36-03-SUMMARY.md`，记录：
- HotSyncConfig.MaxFileBytes 注入路径（从 main.go 到 mount_strategy 到 HotSyncConfig）
- D-08 stderr 提示实际格式字符串
- 任何偏差说明（特别是 NewIgnoreMatcher 签名调整）
</output>
