---
phase: 36-sshfs
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloudclaude/mount_sshfs.go
  - internal/cloudclaude/mount_sshfs_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-04

must_haves:
  truths:
    - "sshfs 命令字面量包含完整 4 个缓存参数：cache=yes,kernel_cache,auto_cache,cache_timeout=300"
    - "4 个缓存参数紧跟在 ConnectTimeout=10 之后、-f 之前（字面量顺序锁死）"
    - "fixture SFTP counting 测试：同文件 2 次 ReadFile → server-side Fileread 调用仅 1 次（FUSE page cache 命中）"
    - "sshfs 未安装时 counting 测试自动 Skip，不阻断 CI"
  artifacts:
    - path: "internal/cloudclaude/mount_sshfs.go"
      provides: "sshfsCmd 字面量含 4 个缓存参数"
      contains: "cache=yes,kernel_cache,auto_cache,cache_timeout=300"
    - path: "internal/cloudclaude/mount_sshfs_test.go"
      provides: "countingFileReader + TestSSHFSCacheHitsKernelPageCache"
      contains: "TestSSHFSCacheHitsKernelPageCache"
  key_links:
    - from: "internal/cloudclaude/mount_sshfs.go"
      to: "FUSE kernel page cache（内核透明）"
      via: "sshfsCmd -o cache=yes,kernel_cache,auto_cache,cache_timeout=300"
      pattern: "cache=yes,kernel_cache,auto_cache,cache_timeout=300"
    - from: "internal/cloudclaude/mount_sshfs_test.go"
      to: "pkg/sftp countingFileReader"
      via: "sftp.NewRequestServer + Handlers{FileGet: countingFileReader}"
      pattern: "countingFileReader"
---

<objective>
在 mount_sshfs.go::mountSSHFS 的 sshfs 命令字面量中追加 4 个 FUSE page cache 参数，并新增 fixture SFTP counting 测试以端到端验证「同会话同文件 cat 2 次 → SFTP server-side read = 1」。

Purpose: 对应 SC#3——开启 sshfs 内核 page cache 后，首次读取冷文件后由内核接管缓存，相同内容的第二次读取不回源 SFTP server，消除重复 RTT。
Output: mount_sshfs.go 字面量修改（1 处）+ mount_sshfs_test.go 新增 counting test（sshfs 可用时运行，不可用时 Skip）
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-sshfs/36-CONTEXT.md
@.planning/phases/36-sshfs/36-RESEARCH.md
@.planning/phases/36-sshfs/36-PATTERNS.md

<interfaces>
<!-- mount_sshfs.go 当前 sshfsCmd 字面量（PATTERNS.md + RESEARCH.md F3 锚点精确行号）-->
From internal/cloudclaude/mount_sshfs.go (lines 48-51):
```go
// 当前（Phase 35 基线）：
sshfsCmd := fmt.Sprintf(
    "sshfs : %s -o passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3,ConnectTimeout=10 -f",
    shellQuote(remotePath),
)

// Phase 36 D-10 目标（追加 4 个缓存参数，字面量顺序锁死）：
sshfsCmd := fmt.Sprintf(
    "sshfs : %s -o passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3,ConnectTimeout=10,cache=yes,kernel_cache,auto_cache,cache_timeout=300 -f",
    shellQuote(remotePath),
)
```

<!-- pkg/sftp API（RESEARCH.md F3 / L5 地雷规避）-->
From go.mod: github.com/pkg/sftp v1.13.10（已在 go.mod，无需 go get）

sftp.NewRequestServer 签名：
```go
// sftp.Handlers 四接口全部需要实现（L5：不能为 nil）
type Handlers struct {
    FileGet    ReadWriteAt
    FilePut    ReadWriteAt
    FileCmd    FileCmder
    FileLister ListerAt
}

// FileReader 接口（request-interfaces.go）：
type ReadWriteAt interface {
    Fileread(r *Request) (io.ReaderAt, error)
    Filewrite(r *Request) (io.WriterAt, error)
}
```

<!-- SSH server 包装方式（RESEARCH.md F3 §Open Questions）-->
使用 golang.org/x/crypto/ssh（已在 go.mod）：
```go
// 关键 API：
config := &ssh.ServerConfig{NoClientAuth: true}
hostKey, _ := rsa.GenerateKey(rand.Reader, 2048)
config.AddHostKey(mustNewSigner(hostKey))
listener, _ := net.Listen("tcp", "127.0.0.1:0")
conn, chans, reqs, _ := ssh.NewServerConn(netConn, config)
go ssh.DiscardRequests(reqs)
for newChan := range chans {
    ch, reqs, _ := newChan.Accept()
    go ssh.DiscardRequests(reqs)
    go sftp.NewRequestServer(ch, handlers).Serve()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mount_sshfs.go 追加 4 个缓存参数（D-10 字面量修改）</name>
  <read_first>
    - internal/cloudclaude/mount_sshfs.go（全文，19-95 行；重点：lines 48-51 sshfsCmd 字面量；确认函数名 mountSSHFS 与参数）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-10（4 个缓存参数字面量顺序 + 选型论据）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/mount_sshfs.go"（精确改动代码块）
  </read_first>
  <action>
**唯一修改点**：`mountSSHFS` 函数内的 sshfsCmd 字面量，在 `ConnectTimeout=10` 之后、` -f` 之前追加 `,cache=yes,kernel_cache,auto_cache,cache_timeout=300`。

将：
```go
"sshfs : %s -o passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3,ConnectTimeout=10 -f"
```
改为：
```go
"sshfs : %s -o passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3,ConnectTimeout=10,cache=yes,kernel_cache,auto_cache,cache_timeout=300 -f"
```

参数含义（D-10 + D-12）：
- `cache=yes`：显式开启 sshfs 缓存（默认值，但显式写出便于 grep / doctor sshfs_cache_args check 字符串匹配）
- `kernel_cache`：让内核接管 page cache，避免 sshfs 自己的 fragment 缓存（核心收益）
- `auto_cache`：按 mtime 自动失效（防御：容器内 claude code 改了 cold 分支的文件 → mtime 变 → cache 自动失效）
- `cache_timeout=300`：5 分钟缓存窗口，在「读多写少」场景与「外部修改感知」间取折中

**不改动**：函数签名、cleanup 结构、error 包装模式、其他参数。
  </action>
  <acceptance_criteria>
    - `grep "cache=yes,kernel_cache,auto_cache,cache_timeout=300" internal/cloudclaude/mount_sshfs.go` 存在（精确字面量）
    - `grep -c "ConnectTimeout=10,cache=yes" internal/cloudclaude/mount_sshfs.go` 输出 `1`（参数连续，无空格）
    - `grep "ConnectTimeout=10 -f" internal/cloudclaude/mount_sshfs.go` 不存在（旧字面量已被替换）
    - `go build ./internal/cloudclaude/...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/... && grep "cache=yes,kernel_cache,auto_cache,cache_timeout=300" internal/cloudclaude/mount_sshfs.go</automated>
  </verify>
  <done>mount_sshfs.go 字面量修改完成，4 个缓存参数以正确顺序追加，go build 干净</done>
</task>

<task type="auto">
  <name>Task 2: 新建 mount_sshfs_test.go — fixture SFTP counting 测试（D-11）</name>
  <read_first>
    - internal/cloudclaude/mount_sshfs.go（全文；确认 package 名为 cloudclaude + shellQuote 等辅助函数位置）
    - internal/cloudclaude/mount_sshfs_test.go（若已存在则读取；确认现有 Skip 模式）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F3 锚点" §"D-11 fixture SFTP 测试"（完整测试流程 + countingFileReader 代码块）
    - .planning/phases/36-sshfs/36-RESEARCH.md §L5（pkg/sftp v1.13.10 Handlers 四接口全实现规避）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/mount_sshfs_test.go"（Skip 判断 + Handlers 四接口示例）
  </read_first>
  <action>
在 `internal/cloudclaude/mount_sshfs_test.go` 末尾追加以下测试（若文件不存在则新建）：

```go
package cloudclaude

import (
	"crypto/rand"
	"crypto/rsa"
	"fmt"
	"io"
	"net"
	"os"
	"os/exec"
	"path/filepath"
	"sync"
	"sync/atomic"
	"testing"
	"time"

	"github.com/pkg/sftp"
	"golang.org/x/crypto/ssh"
)

// countingFileReader 实现 sftp.ReadWriteAt，统计每个文件路径被 Fileread 调用的次数。
// 同时通过 atomic.Int64 保证并发安全（FUSE 可能并发发起多个 read request）。
type countingFileReader struct {
	root string
	mu   sync.Mutex
	reads map[string]*atomic.Int64
}

func newCountingFileReader(root string) *countingFileReader {
	return &countingFileReader{
		root:  root,
		reads: make(map[string]*atomic.Int64),
	}
}

func (c *countingFileReader) counter(path string) *atomic.Int64 {
	c.mu.Lock()
	defer c.mu.Unlock()
	if _, ok := c.reads[path]; !ok {
		c.reads[path] = &atomic.Int64{}
	}
	return c.reads[path]
}

func (c *countingFileReader) ReadCount(path string) int64 {
	c.mu.Lock()
	defer c.mu.Unlock()
	if ctr, ok := c.reads[path]; ok {
		return ctr.Load()
	}
	return 0
}

// Fileread 实现 sftp.ReadWriteAt 接口（FileGet），计数并返回文件内容。
func (c *countingFileReader) Fileread(req *sftp.Request) (io.ReaderAt, error) {
	c.counter(req.Filepath).Add(1)
	return os.Open(filepath.Join(c.root, req.Filepath))
}

// Filewrite 不需要，返回不支持。
func (c *countingFileReader) Filewrite(req *sftp.Request) (io.WriterAt, error) {
	return nil, sftp.ErrSSHFxOpUnsupported
}

// noopReadWriteAt 用于 FilePut（sftp.Handlers.FilePut 不能为 nil，L5 地雷规避）。
type noopReadWriteAt struct{}
func (n *noopReadWriteAt) Fileread(req *sftp.Request) (io.ReaderAt, error)  { return nil, sftp.ErrSSHFxOpUnsupported }
func (n *noopReadWriteAt) Filewrite(req *sftp.Request) (io.WriterAt, error) { return nil, sftp.ErrSSHFxOpUnsupported }

// mustSignerFromKey 生成临时 SSH host key（测试专用）。
func mustSignerFromKey(t *testing.T) ssh.Signer {
	t.Helper()
	key, err := rsa.GenerateKey(rand.Reader, 2048)
	if err != nil {
		t.Fatalf("生成 RSA key 失败: %v", err)
	}
	signer, err := ssh.NewSignerFromKey(key)
	if err != nil {
		t.Fatalf("生成 SSH signer 失败: %v", err)
	}
	return signer
}

// TestSSHFSCacheHitsKernelPageCache 验证：同会话同文件 ReadFile 2 次 → SFTP Fileread 仅调用 1 次。
// sshfs 未安装时自动 Skip（D-11 / D-20 / D-22）。
func TestSSHFSCacheHitsKernelPageCache(t *testing.T) {
	if _, err := exec.LookPath("sshfs"); err != nil {
		t.Skip("sshfs 未安装，跳过 cache counting 测试（D-11）")
	}
	if _, err := exec.LookPath("fusermount"); err != nil {
		if _, err2 := exec.LookPath("fusermount3"); err2 != nil {
			t.Skip("fusermount/fusermount3 未安装，跳过 FUSE 测试")
		}
	}

	// Step 1: 创建 fixture 根目录和测试文件（512KB，足以触发真实 read）
	fixtureDir := t.TempDir()
	fixturePath := filepath.Join(fixtureDir, "fixture.bin")
	fixtureSz := int64(512 * 1024)
	fixFile, err := os.Create(fixturePath)
	if err != nil {
		t.Fatalf("创建 fixture 文件失败: %v", err)
	}
	if err := fixFile.Truncate(fixtureSz); err != nil {
		_ = fixFile.Close()
		t.Fatalf("Truncate 失败: %v", err)
	}
	_ = fixFile.Close()

	// Step 2: 启动 fixture SSH+SFTP server（golang.org/x/crypto/ssh + pkg/sftp）
	listener, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatalf("Listen 失败: %v", err)
	}
	t.Cleanup(func() { _ = listener.Close() })

	port := listener.Addr().(*net.TCPAddr).Port
	counting := newCountingFileReader(fixtureDir)

	sshConfig := &ssh.ServerConfig{NoClientAuth: true}
	sshConfig.AddHostKey(mustSignerFromKey(t))

	// inMemHandlers 作为 FileLister/FileCmd 的 no-op 底座（L5 规避）
	inMem := sftp.InMemHandler()
	handlers := sftp.Handlers{
		FileGet:    counting,
		FilePut:    &noopReadWriteAt{},
		FileCmd:    inMem.FileCmd,
		FileLister: inMem.FileLister,
	}

	go func() {
		for {
			netConn, err := listener.Accept()
			if err != nil {
				return // listener closed
			}
			go func(c net.Conn) {
				srvConn, chans, reqs, err := ssh.NewServerConn(c, sshConfig)
				if err != nil {
					return
				}
				go ssh.DiscardRequests(reqs)
				_ = srvConn
				for newChan := range chans {
					if newChan.ChannelType() != "session" {
						_ = newChan.Reject(ssh.UnknownChannelType, "unknown channel type")
						continue
					}
					ch, chReqs, err := newChan.Accept()
					if err != nil {
						return
					}
					go func() {
						for req := range chReqs {
							if req.Type == "subsystem" && len(req.Payload) > 4 {
								name := string(req.Payload[4:])
								if name == "sftp" {
									_ = req.Reply(true, nil)
									srv := sftp.NewRequestServer(ch, handlers)
									_ = srv.Serve()
									return
								}
							}
							if req.WantReply {
								_ = req.Reply(false, nil)
							}
						}
					}()
				}
			}(netConn)
		}
	}()

	// Step 3: sshfs 挂载到临时 mountpoint
	mountDir := t.TempDir()
	// sshfs 参数：完整参数含 cache 选项（D-10），无 SSH host key 检查（测试环境）
	sshfsArgs := []string{
		"-p", fmt.Sprintf("%d", port),
		"-o", "StrictHostKeyChecking=no,UserKnownHostsFile=/dev/null",
		"-o", fmt.Sprintf("passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3,ConnectTimeout=10,cache=yes,kernel_cache,auto_cache,cache_timeout=300"),
		"-f",
	}
	// 注意：sshfs 命令格式为 sshfs [user@]host:[dir] mountpoint [options]
	sshfsCmd := exec.Command("sshfs",
		append([]string{fmt.Sprintf("testuser@127.0.0.1:%s", fixtureDir), mountDir}, sshfsArgs...)...)
	// sshfs -f 前台运行，需要 goroutine 异步启动
	if err := sshfsCmd.Start(); err != nil {
		t.Fatalf("sshfs Start 失败: %v", err)
	}
	t.Cleanup(func() {
		_ = exec.Command("fusermount", "-u", mountDir).Run()
		_ = exec.Command("fusermount3", "-u", mountDir).Run()
		_ = sshfsCmd.Process.Kill()
	})

	// 等待 sshfs 挂载就绪（最多 5s）
	mountedFile := filepath.Join(mountDir, "fixture.bin")
	deadline := time.Now().Add(5 * time.Second)
	for time.Now().Before(deadline) {
		if _, err := os.Stat(mountedFile); err == nil {
			break
		}
		time.Sleep(100 * time.Millisecond)
	}
	if _, err := os.Stat(mountedFile); err != nil {
		t.Fatalf("sshfs 挂载超时（5s），fixture.bin 不可见: %v", err)
	}

	// Step 4: 同文件读取 2 次
	data1, err := os.ReadFile(mountedFile)
	if err != nil {
		t.Fatalf("第一次 ReadFile 失败: %v", err)
	}
	if int64(len(data1)) != fixtureSz {
		t.Fatalf("第一次读取大小 %d != %d", len(data1), fixtureSz)
	}

	data2, err := os.ReadFile(mountedFile)
	if err != nil {
		t.Fatalf("第二次 ReadFile 失败: %v", err)
	}
	if int64(len(data2)) != fixtureSz {
		t.Fatalf("第二次读取大小 %d != %d", len(data2), fixtureSz)
	}

	// Step 5: 断言 server-side Fileread 调用 == 1（FUSE page cache 命中第二次不回源）
	readCount := counting.ReadCount("/fixture.bin")
	if readCount != 1 {
		t.Errorf("期望 SFTP server-side Fileread = 1（第二次由 FUSE page cache 接管），实际 = %d", readCount)
	}
}
```

**import 注意**：
- `fmt` 包在测试中需要 `import "fmt"`（用于 `fmt.Sprintf`）
- 若文件已有 package 声明，只追加函数体（不重复 package/import），将新 import 合并到现有 import 块
- 若文件不存在，使用上述完整代码创建

**L5 地雷规避**（RESEARCH.md §L5）：
- `sftp.Handlers` 四字段全实现，`FilePut` 用 `noopReadWriteAt`，`FileCmd` 和 `FileLister` 用 `sftp.InMemHandler()` 的对应字段
- 验证 `pkg/sftp v1.13.10` 的 `sftp.InMemHandler()` API 确实存在（若不存在则各接口返回 `ErrSSHFxOpUnsupported`）
  </action>
  <acceptance_criteria>
    - `ls internal/cloudclaude/mount_sshfs_test.go` 文件存在
    - `grep -c "TestSSHFSCacheHitsKernelPageCache" internal/cloudclaude/mount_sshfs_test.go` 输出 `1`
    - `grep "countingFileReader" internal/cloudclaude/mount_sshfs_test.go` 存在
    - `grep "t.Skip.*sshfs 未安装" internal/cloudclaude/mount_sshfs_test.go` 存在（Skip 卫士）
    - `grep "ReadCount.*== 1\|readCount != 1" internal/cloudclaude/mount_sshfs_test.go` 存在（断言行）
    - `go build ./internal/cloudclaude/...` 无编译错误
    - `go test ./internal/cloudclaude/... -run TestSSHFSCacheHits -v 2>&1 | grep -E "PASS|SKIP"` 输出 PASS 或 SKIP（不得 FAIL）
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/... && go test ./internal/cloudclaude/... -run TestSSHFSCacheHits -v 2>&1 | grep -E "PASS|SKIP|FAIL"</automated>
  </verify>
  <done>mount_sshfs_test.go 含 TestSSHFSCacheHitsKernelPageCache，sshfs 可用时验证 SFTP read=1，不可用时 Skip；go build 干净</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| sshfs 进程 → FUSE 内核 page cache | 内核接管缓存，透明于用户进程；cache=yes,kernel_cache 参数在 sshfs 启动时注入 |
| fixture SSH server（测试专用） | NoClientAuth=true 仅在测试中使用；生产 sshfs 连接走 Entry API 颁发的 SSH 密钥认证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-01 | Information Disclosure | cache_timeout=300 延迟失效 | accept | auto_cache 按 mtime 自动失效；容器内 claude code 修改文件会更新 mtime，5 分钟内旧缓存仅影响读者一致性（非安全威胁） |
| T-36-05-02 | Denial of Service | kernel_cache 占用内核 page cache | accept | sshfs 只读取 cold 分支（小于 50MB 的文件热同步到 hot，cold 主要是大文件）；page cache 受内核 LRU 管理，无无限增长风险 |
| T-36-05-03 | Tampering | countingFileReader 并发 Fileread | mitigate | counter() 用 sync.Mutex 保护 map 读写；atomic.Int64 保证计数器自增原子性 |
</threat_model>

<verification>
```bash
# Task 1 验证
grep "cache=yes,kernel_cache,auto_cache,cache_timeout=300" internal/cloudclaude/mount_sshfs.go

# Task 2 验证
go test ./internal/cloudclaude/... -run TestSSHFSCacheHits -v 2>&1 | grep -E "PASS|SKIP|FAIL"

# 全包无回归
go build ./...
go test ./internal/cloudclaude/... -count=1 2>&1 | tail -10
```
期望：mount_sshfs.go grep 命中，TestSSHFSCacheHits PASS 或 SKIP，全包无 FAIL。
</verification>

<success_criteria>
- mount_sshfs.go sshfsCmd 字面量含完整 4 个缓存参数（字面量顺序锁死：cache=yes,kernel_cache,auto_cache,cache_timeout=300）
- mount_sshfs_test.go TestSSHFSCacheHitsKernelPageCache：sshfs 可用时 PASS（server-side read==1），不可用时 Skip
- go build ./... 无编译错误
- go test ./internal/cloudclaude/... 全包无 FAIL（含新测试）
</success_criteria>

<output>
完成后创建 `.planning/phases/36-sshfs/36-05-SUMMARY.md`，记录：
- mount_sshfs.go 修改后 sshfsCmd 完整字面量（用于 Plan 06 doctor sshfs_cache_args check 对比）
- TestSSHFSCacheHitsKernelPageCache 实际运行结果（PASS / SKIP + 环境说明）
- 任何偏差说明（特别是 sftp.InMemHandler() API 验证结果）
</output>
