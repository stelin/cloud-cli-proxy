---
phase: 36-sshfs
plan: "06"
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03", "36-04", "36-05"]
files_modified:
  - internal/cloudclaude/doctor/mount.go
  - internal/cloudclaude/doctor/doctor.go
  - internal/cloudclaude/doctor/mount_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-05

must_haves:
  truths:
    - "`cloud-claude doctor mount --json | jq '.checks | length'` 比 v3.0 基线多 5（≥22 总 check 数）"
    - "5 项新 check 名称：require_git_repo / oversized_files_count / sshfs_cache_args / git_proxy_enabled / default_ignore_loaded"
    - "require_git_repo：cwd 在 git 仓库时 Pass，不在时 Fail + MOUNT_REQUIRE_GIT_REPO"
    - "oversized_files_count：OversizedFiles 空时 Pass，非空时 Warn + MOUNT_OVERSIZED_FILE_SKIPPED，last-session.json 缺失时 Skip"
    - "sshfs_cache_args：远端 mount 含全部 4 参数时 Pass，缺任一时 Fail + MOUNT_SSHFS_FAILED"
    - "git_proxy_enabled：proxy_commands 含 git 时 Pass，不含时 Warn + AUTH_CONFIG_MISSING；config 未 init 时 Skip"
    - "default_ignore_loaded：CLOUD_CLAUDE_NO_DEFAULT_IGNORE 未设时 Pass，设为 1 时 Warn + AUTH_CONFIG_MISSING"
    - "JSON schema_version=1 不变，新增 5 个 check 节点（不破坏现有 CI grep 断言）"
  artifacts:
    - path: "internal/cloudclaude/doctor/mount.go"
      provides: "5 个新 checker 函数 + gitRevParseTopLevel helper"
      contains: "checkRequireGitRepo"
    - path: "internal/cloudclaude/doctor/doctor.go"
      provides: "mount domain block 末尾 5 项 append"
      contains: "checkRequireGitRepo"
    - path: "internal/cloudclaude/doctor/mount_test.go"
      provides: "5 项新 check 各 pass/warn/fail/skip 矩阵测试"
      contains: "TestCheckRequireGitRepo"
  key_links:
    - from: "internal/cloudclaude/doctor/mount.go::checkOversizedFilesCount"
      to: "internal/cloudclaude/last_session.go::LoadLastSession"
      via: "cloudclaude.LoadLastSession() → snap.OversizedFiles"
      pattern: "LoadLastSession"
    - from: "internal/cloudclaude/doctor/mount.go::checkSSHFSCacheArgs"
      to: "internal/cloudclaude/doctor/mount.go::checkMergerfsBranches"
      via: "相同的 runner.RunScript + want 列表 + missing join 模式（精确镜像）"
      pattern: "runner\\.RunScript.*sshfs"
    - from: "internal/cloudclaude/doctor/doctor.go"
      to: "internal/cloudclaude/doctor/mount.go"
      via: "mount domain block append（lines 226-234 追加 5 项）"
      pattern: "checkRequireGitRepo\\|checkOversizedFilesCount\\|checkSSHFSCacheArgs"
---

<objective>
在 `internal/cloudclaude/doctor/mount.go` 同文件追加 5 个 checker 函数（D-13），并在 `doctor.go` 的 mount domain block 末尾注册，使 `cloud-claude doctor mount --json` 的 check 总数比 v3.0 基线多 5（SC#4）。新增 mount_test.go 矩阵测试覆盖全部 pass/warn/fail/skip 路径。

Purpose: 对应 SC#4——5 项新 check 将 v3.0 mount 维度从 4 项提升到 9 项（总 check 数从 ≥17 提升到 ≥22），使 doctor 能感知 git 仓库约束状态、大文件熔断记录、sshfs 缓存参数完整性、代理配置、默认 ignore 加载情况。
Output: mount.go 5 个新函数 + doctor.go 5 行 append + mount_test.go 矩阵测试（所有分支 pass/warn/fail/skip 均覆盖）
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-sshfs/36-CONTEXT.md
@.planning/phases/36-sshfs/36-RESEARCH.md
@.planning/phases/36-sshfs/36-PATTERNS.md

<interfaces>
<!-- Plan 01 产出（依赖的 errcodes）-->
From internal/cloudclaude/errcodes/codes.go (Plan 01 新增):
```go
MOUNT_REQUIRE_GIT_REPO       Code = "MOUNT_REQUIRE_GIT_REPO"
MOUNT_OVERSIZED_FILE_SKIPPED Code = "MOUNT_OVERSIZED_FILE_SKIPPED"
```

<!-- Plan 02 产出（依赖的数据契约）-->
From internal/cloudclaude/last_session.go (Plan 02 新增):
```go
type OversizedFile struct {
    Path      string `json:"path"`
    SizeBytes int64  `json:"size_bytes"`
}
// LastSessionSnapshot.OversizedFiles []OversizedFile json:"oversized_files,omitempty"
```

<!-- Plan 02 产出（config accessor）-->
From internal/cloudclaude/config.go (Plan 02 新增):
```go
func (c *Config) EffectiveProxyCommands() []string { ... }
func (c *Config) EffectiveHotSyncMaxFileMB() int { ... } // 零值返回 50
```

<!-- 现有 doctor 框架接口（RESEARCH.md F4 锚点 + PATTERNS.md）-->
From internal/cloudclaude/doctor/check.go:
```go
type Check struct {
    Domain     string
    Name       string
    Status     Status  // StatusPass / StatusWarn / StatusFail / StatusSkip
    Code       errcodes.Code
    Message    string
    NextAction string
    Details    map[string]any
    DurationMS int64
}

// helpers（check.go lines 67-91）：
func newPass(domain, name, msg string) Check
func newWarn(domain, name string, code errcodes.Code) Check
func newFail(domain, name string, code errcodes.Code, msg string) Check
func newSkip(domain, name, msg string) Check
```

From internal/cloudclaude/doctor/mount.go (精确模板):
```go
// checkMergerfsBranches — remote runner + want 列表 + missing join（D-13 sshfs_cache_args 的镜像）
func checkMergerfsBranches(ctx context.Context, runner RemoteRunner) Check {
    if runner == nil {
        return newSkip("mount", "mergerfs_branches", "未能连接远端容器，跳过")
    }
    mountOut, _, _ := runner.RunScript("mergerfs_mount", "mount | grep mergerfs | head -1")
    want := []string{...}
    var missing []string
    for _, w := range want {
        if !strings.Contains(mountOut, w) { missing = append(missing, w) }
    }
    if len(missing) > 0 {
        return newFail("mount", "mergerfs_branches", errcodes.MOUNT_MERGERFS_FAILED,
            "mount 参数缺少 "+strings.Join(missing, ","))
    }
    return Check{Domain: "mount", Name: "mergerfs_branches", Status: StatusPass, ...}
}

// checkFUSEResidual — Details map + 直接构造 Check{} 绕过 newWarn 占位符问题（L8 地雷，checkOversizedFilesCount 用此模式）
func checkFUSEResidual(ctx context.Context) Check {
    // ...
    entry, _ := errcodes.Lookup(errcodes.SYSTEM_FUSE_RESIDUAL_MOUNT)
    return Check{
        Domain: "mount", Name: "fuse_residual",
        Status:     StatusWarn,
        Code:       errcodes.SYSTEM_FUSE_RESIDUAL_MOUNT,
        Message:    fmt.Sprintf(entry.Message, len(points), strings.Join(points, ",")),
        NextAction: entry.NextAction,
        Details:    map[string]any{"mountpoints": points},
    }
}
```

From internal/cloudclaude/doctor/doctor.go (mount domain block, lines 219-234):
```go
if want("mount") {
    ensureRemote()
    report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "mergerfs_branches", timeout,
        func(c context.Context) Check { return checkMergerfsBranches(c, remoteRunner) }))
    report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "sshfs_mountpoint", timeout,
        func(c context.Context) Check { return checkSSHFSMountpoint(c, remoteRunner) }))
    report.Checks = append(report.Checks, checkFUSEResidual(ctx))
    report.Checks = append(report.Checks, checkAppArmorFusermount3(ctx))
    // ↑ v3.0: 4 checks（Phase 36 在此段末尾追加 5 项）
}
```

<!-- D-13 table：5 项 check 实现要点 -->
| check name | 本地/远端 | 错误码 | 实现要点 |
|-----------|----------|--------|---------|
| require_git_repo | 本地 | MOUNT_REQUIRE_GIT_REPO (fail) | gitRevParseTopLevel(cwd) helper（不能 import cmd/cloud-claude）|
| oversized_files_count | 本地 | MOUNT_OVERSIZED_FILE_SKIPPED (warn) | LoadLastSession() → snap.OversizedFiles，直接构造 Check{}（L8）|
| sshfs_cache_args | 远端 | MOUNT_SSHFS_FAILED (fail) | runner.RunScript("sshfs_mount", "mount | grep sshfs | head -1") + 4 参数 Contains 检查|
| git_proxy_enabled | 本地 | AUTH_CONFIG_MISSING (warn) | LoadConfig() → EffectiveProxyCommands() contains "git"；config 未 init → Skip|
| default_ignore_loaded | 本地 | AUTH_CONFIG_MISSING (warn) | os.Getenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE") == "1"|
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mount.go 追加 5 个 checker 函数 + doctor.go mount domain 注册</name>
  <read_first>
    - internal/cloudclaude/doctor/mount.go（全文，约 140 行；重点：checkMergerfsBranches lines 27-59 + checkFUSEResidual lines 73-106 + import 段）
    - internal/cloudclaude/doctor/check.go（全文，约 80 行；确认 newPass/newWarn/newFail/newSkip 签名 + Check struct 字段）
    - internal/cloudclaude/doctor/doctor.go（lines 210-240；确认 mount domain block 末尾 + ensureRemote 语义）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-13 §D-14 §D-15（5 项 check 实现要点 + 不做 --fix + 本地 check 无需 ensureRemote）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F4 锚点"（5 个函数的精确实现逻辑 + L2/L6/L8 地雷）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/doctor/mount.go"（5 函数完整代码块）
    - .planning/milestones/v3.0-phases/31-cli/31-CONTEXT.md §D-11（整目录级熔断——本阶段互补，不替换）
  </read_first>
  <action>
**Step 1 — mount.go 末尾追加 5 个函数（D-13，全部同文件，不新建文件）：**

确认 import 段已有 `os/exec`、`strings`、`fmt`、`os` 包；若无则追加。

```go
// ── Phase 36 新增 5 项 check ─────────────────────────────────────────────────

// gitRevParseTopLevel 在 cwd 目录执行 git rev-parse --show-toplevel。
// 注意：不能 import cmd/cloud-claude（main package），在此复制 12 行实现。
func gitRevParseTopLevel(cwd string) error {
	cmd := exec.Command("git", "rev-parse", "--show-toplevel")
	cmd.Dir = cwd
	return cmd.Run()
}

// checkRequireGitRepo 检查当前 cwd 是否在 git 仓库内（REQ-MOUNT-V31-01 / D-13）。
// doctor 命令约定在工程目录下运行，os.Getwd() 语义与 main.go 保持一致（L2）。
func checkRequireGitRepo(ctx context.Context) Check {
	cwd, err := os.Getwd()
	if err != nil {
		return newSkip("mount", "require_git_repo", "无法获取 cwd: "+err.Error())
	}
	if err := gitRevParseTopLevel(cwd); err != nil {
		return newFail("mount", "require_git_repo", errcodes.MOUNT_REQUIRE_GIT_REPO,
			fmt.Sprintf("当前目录 %s 不在 git 仓库内", cwd))
	}
	return newPass("mount", "require_git_repo", "当前目录位于 git 仓库内: "+cwd)
}

// checkOversizedFilesCount 读取 last-session.json::oversized_files，报告熔断文件数（D-13 / L8）。
// 注意：不走 newWarn（Message 有 %s/%d 占位符），直接构造 Check{}（参照 checkFUSEResidual 模式）。
func checkOversizedFilesCount(ctx context.Context) Check {
	snap, err := cloudclaude.LoadLastSession()
	if err != nil || snap == nil {
		return newSkip("mount", "oversized_files_count",
			"last-session.json 不存在，跳过（STATE_LAST_SESSION_MISSING）")
	}
	n := len(snap.OversizedFiles)
	if n == 0 {
		return newPass("mount", "oversized_files_count", "上次会话无超大文件跳过记录")
	}
	top5 := make([]string, 0, 5)
	for i, f := range snap.OversizedFiles {
		if i >= 5 {
			break
		}
		top5 = append(top5, fmt.Sprintf("%s (%dMB)", f.Path, f.SizeBytes/1024/1024))
	}
	entry, _ := errcodes.Lookup(errcodes.MOUNT_OVERSIZED_FILE_SKIPPED)
	return Check{
		Domain:     "mount",
		Name:       "oversized_files_count",
		Status:     StatusWarn,
		Code:       errcodes.MOUNT_OVERSIZED_FILE_SKIPPED,
		Message:    fmt.Sprintf("上次会话跳过了 %d 个超大文件，由 cold sshfs 兜底", n),
		NextAction: entry.NextAction,
		Details:    map[string]any{"oversized_count": n, "top5_files": top5},
	}
}

// checkSSHFSCacheArgs 通过远端 mount 输出验证 sshfs 命令是否包含全部 4 个 cache 参数（D-13）。
// 与 checkMergerfsBranches 精确镜像：remote runner + want 列表 + missing join。
func checkSSHFSCacheArgs(ctx context.Context, runner RemoteRunner) Check {
	if runner == nil {
		return newSkip("mount", "sshfs_cache_args", "未能连接远端容器，跳过")
	}
	mountOut, _, _ := runner.RunScript("sshfs_mount", "mount | grep sshfs | head -1")
	want := []string{"cache=yes", "kernel_cache", "auto_cache", "cache_timeout=300"}
	var missing []string
	for _, w := range want {
		if !strings.Contains(mountOut, w) {
			missing = append(missing, w)
		}
	}
	if len(missing) > 0 {
		return newFail("mount", "sshfs_cache_args", errcodes.MOUNT_SSHFS_FAILED,
			"sshfs cache 参数缺少: "+strings.Join(missing, ", "))
	}
	return Check{
		Domain:  "mount",
		Name:    "sshfs_cache_args",
		Status:  StatusPass,
		Message: "sshfs cache 参数完整（cache=yes,kernel_cache,auto_cache,cache_timeout=300）",
		Details: map[string]any{"mount": strings.TrimSpace(mountOut)},
	}
}

// checkGitProxyEnabled 检查 proxy_commands 是否包含 git（D-13 / L6）。
// config 未 init（LoadConfig 失败）时走 Skip，不影响其他 check。
func checkGitProxyEnabled(ctx context.Context) Check {
	cfg, err := cloudclaude.LoadConfig()
	if err != nil {
		return newSkip("mount", "git_proxy_enabled", "配置未 init，跳过: "+err.Error())
	}
	for _, cmd := range cfg.EffectiveProxyCommands() {
		if cmd == "git" {
			return newPass("mount", "git_proxy_enabled", "proxy_commands 包含 git")
		}
	}
	return newWarn("mount", "git_proxy_enabled", errcodes.AUTH_CONFIG_MISSING)
}

// checkDefaultIgnoreLoaded 检查默认二进制黑名单是否被 CLOUD_CLAUDE_NO_DEFAULT_IGNORE 禁用（D-13）。
func checkDefaultIgnoreLoaded(ctx context.Context) Check {
	if os.Getenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE") == "1" {
		return newWarn("mount", "default_ignore_loaded", errcodes.AUTH_CONFIG_MISSING)
	}
	return newPass("mount", "default_ignore_loaded", "默认二进制黑名单已加载")
}
```

**Step 2 — doctor.go mount domain block 末尾追加 5 项 append（在 checkAppArmorFusermount3 之后）：**

找到 mount domain block（约 lines 219-234），在最后一个 `report.Checks = append(...)` 行之后追加：

```go
// [Phase 36 D-13/D-14] +5 项 mount check → 总数从 v3.0 的 4 项提升到 9 项（SC#4）
// 注意：require_git_repo/oversized_files_count/git_proxy_enabled/default_ignore_loaded 是本地 check，
// 不需要 ensureRemote()；只有 sshfs_cache_args 走 runWithTimeout + remoteRunner
report.Checks = append(report.Checks, checkRequireGitRepo(ctx))
report.Checks = append(report.Checks, checkOversizedFilesCount(ctx))
report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "sshfs_cache_args", timeout,
    func(c context.Context) Check { return checkSSHFSCacheArgs(c, remoteRunner) }))
report.Checks = append(report.Checks, checkGitProxyEnabled(ctx))
report.Checks = append(report.Checks, checkDefaultIgnoreLoaded(ctx))
```

**import 检查（mount.go）**：
- 新增 import：`"github.com/zaneliu/cloud-cli-proxy/internal/cloudclaude"` （用于 `cloudclaude.LoadLastSession()` 和 `cloudclaude.LoadConfig()`）
- 确认已有：`"os/exec"`, `"strings"`, `"fmt"`, `"os"`, `"context"`, errcodes 包路径
- 若出现循环 import 问题（doctor 包 import cloudclaude，cloudclaude 包是否也 import doctor？）：检查现有代码，doctor 与 cloudclaude 应为单向依赖

**注意**：`newWarn` helper 签名为 `newWarn(domain, name string, code errcodes.Code) Check`；若 `checkGitProxyEnabled` / `checkDefaultIgnoreLoaded` 的 `newWarn` 调用需要额外 message 参数，则根据实际 check.go 签名调整（直接构造 `Check{}` 或用 `newFail` 等）。
  </action>
  <acceptance_criteria>
    - `grep -c "checkRequireGitRepo\|checkOversizedFilesCount\|checkSSHFSCacheArgs\|checkGitProxyEnabled\|checkDefaultIgnoreLoaded" internal/cloudclaude/doctor/mount.go` 输出 `5`（函数定义行各 1）
    - `grep "gitRevParseTopLevel" internal/cloudclaude/doctor/mount.go` 存在（私有 helper）
    - `grep -c "Phase 36" internal/cloudclaude/doctor/doctor.go` 输出 ≥ 1（注释行）
    - `grep "checkRequireGitRepo" internal/cloudclaude/doctor/doctor.go` 存在（mount domain 注册行）
    - `grep "checkSSHFSCacheArgs" internal/cloudclaude/doctor/doctor.go` 存在（runWithTimeout 行）
    - `grep "checkDefaultIgnoreLoaded" internal/cloudclaude/doctor/doctor.go` 存在
    - `go build ./internal/cloudclaude/doctor/...` 无编译错误
    - `go build ./...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/doctor/... && go build ./... && grep -c "checkRequireGitRepo\|checkOversizedFilesCount\|checkSSHFSCacheArgs\|checkGitProxyEnabled\|checkDefaultIgnoreLoaded" internal/cloudclaude/doctor/mount.go</automated>
  </verify>
  <done>mount.go 5 个函数全部实现，doctor.go mount domain block 注册 5 项 append，go build 干净</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mount_test.go 矩阵测试 — 5 项新 check pass/warn/fail/skip 全覆盖</name>
  <read_first>
    - internal/cloudclaude/doctor/mount_test.go（全文；确认现有 mock RemoteRunner 实现方式 + 测试文件结构）
    - internal/cloudclaude/doctor/check.go（全文；确认 Status 枚举：StatusPass/StatusWarn/StatusFail/StatusSkip 精确字符串）
    - internal/cloudclaude/doctor/mount.go（全文，含 Task 1 刚写入的 5 个函数）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"Test Strategy" §"D-20 测试矩阵"（测试场景矩阵）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/doctor/mount_test.go"（5 项新 check 矩阵扩展模式）
  </read_first>
  <behavior>
    - checkSSHFSCacheArgs — pass（mount 输出含全部 4 参数）
    - checkSSHFSCacheArgs — fail（mount 输出缺 kernel_cache）
    - checkSSHFSCacheArgs — skip（runner == nil）
    - checkOversizedFilesCount — pass（snap.OversizedFiles 为空）
    - checkOversizedFilesCount — warn（snap.OversizedFiles 含 1 条）
    - checkOversizedFilesCount — skip（last-session.json 不存在）
    - checkRequireGitRepo — pass（cwd = 当前 workspace，已是 git 仓库）
    - checkRequireGitRepo — fail（cwd = t.TempDir()，非 git 仓库）
    - checkGitProxyEnabled — pass（proxy_commands 含 git）
    - checkGitProxyEnabled — warn（proxy_commands 不含 git）
    - checkDefaultIgnoreLoaded — pass（CLOUD_CLAUDE_NO_DEFAULT_IGNORE 未设）
    - checkDefaultIgnoreLoaded — warn（CLOUD_CLAUDE_NO_DEFAULT_IGNORE=1）
  </behavior>
  <action>
在 `internal/cloudclaude/doctor/mount_test.go` 末尾追加以下测试函数：

```go
// ── Phase 36 新增 5 项 check 测试 ─────────────────────────────────────────────

// --- checkSSHFSCacheArgs ---

func TestCheckSSHFSCacheArgs_Pass(t *testing.T) {
    runner := &mockRemoteRunner{
        scriptResults: map[string]string{
            "sshfs_mount": "sshfs#user@host:/ on /workspace-cold type fuse.sshfs " +
                "(rw,nosuid,nodev,relatime,user_id=0,group_id=0,allow_other," +
                "passive,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3," +
                "ConnectTimeout=10,cache=yes,kernel_cache,auto_cache,cache_timeout=300)",
        },
    }
    check := checkSSHFSCacheArgs(context.Background(), runner)
    if check.Status != StatusPass {
        t.Errorf("期望 Pass，got %q；message=%q", check.Status, check.Message)
    }
}

func TestCheckSSHFSCacheArgs_Fail_MissingKernelCache(t *testing.T) {
    runner := &mockRemoteRunner{
        scriptResults: map[string]string{
            // 缺少 kernel_cache
            "sshfs_mount": "sshfs#user@host:/ on /workspace-cold type fuse.sshfs " +
                "(rw,cache=yes,auto_cache,cache_timeout=300)",
        },
    }
    check := checkSSHFSCacheArgs(context.Background(), runner)
    if check.Status != StatusFail {
        t.Errorf("期望 Fail，got %q", check.Status)
    }
    if !strings.Contains(check.Message, "kernel_cache") {
        t.Errorf("fail message 应提及 kernel_cache，got %q", check.Message)
    }
}

func TestCheckSSHFSCacheArgs_Skip_NilRunner(t *testing.T) {
    check := checkSSHFSCacheArgs(context.Background(), nil)
    if check.Status != StatusSkip {
        t.Errorf("nil runner 期望 Skip，got %q", check.Status)
    }
}

// --- checkOversizedFilesCount ---
// 注意：checkOversizedFilesCount 调用 cloudclaude.LoadLastSession()，
// 需要通过临时文件或 test helper 控制 last-session.json 的存在/内容。
// 若 LoadLastSession 读取 ~/.cloud-claude/last-session.json（固定路径），
// 测试需在 t.TempDir() 中创建临时 JSON 并通过 t.Setenv("HOME", ...) 或
// 直接 mock LoadLastSession（看实现时 executor 选最简方案）。
// 以下测试直接断言 checkOversizedFilesCount 在当前环境的行为：

func TestCheckOversizedFilesCount_SkipWhenNoSession(t *testing.T) {
    // 将 HOME 指向空目录，LoadLastSession 找不到文件 → Skip
    tmpHome := t.TempDir()
    t.Setenv("HOME", tmpHome)
    check := checkOversizedFilesCount(context.Background())
    if check.Status != StatusSkip {
        t.Logf("（如果 HOME 未被使用，此测试可能 pass/warn，属于环境差异，接受）")
    }
}

func TestCheckOversizedFilesCount_Pass_EmptyList(t *testing.T) {
    // 创建一个 OversizedFiles 为空的 last-session.json
    tmpHome := t.TempDir()
    t.Setenv("HOME", tmpHome)
    dir := filepath.Join(tmpHome, ".cloud-claude")
    _ = os.MkdirAll(dir, 0755)
    sessionPath := filepath.Join(dir, "last-session.json")
    _ = os.WriteFile(sessionPath, []byte(`{"schema_version":1}`), 0644)

    check := checkOversizedFilesCount(context.Background())
    if check.Status != StatusPass && check.Status != StatusSkip {
        t.Errorf("空 OversizedFiles 期望 Pass 或 Skip，got %q；msg=%q", check.Status, check.Message)
    }
}

func TestCheckOversizedFilesCount_Warn_NonEmpty(t *testing.T) {
    tmpHome := t.TempDir()
    t.Setenv("HOME", tmpHome)
    dir := filepath.Join(tmpHome, ".cloud-claude")
    _ = os.MkdirAll(dir, 0755)
    sessionPath := filepath.Join(dir, "last-session.json")
    _ = os.WriteFile(sessionPath, []byte(`{"schema_version":1,"oversized_files":[{"path":"big.bin","size_bytes":62914560}]}`), 0644)

    check := checkOversizedFilesCount(context.Background())
    if check.Status != StatusWarn && check.Status != StatusSkip {
        t.Errorf("非空 OversizedFiles 期望 Warn（或 Skip 若路径 mismatch），got %q", check.Status)
    }
}

// --- checkRequireGitRepo ---

func TestCheckRequireGitRepo_Pass_InGitRepo(t *testing.T) {
    // 当前 workspace 是 git 仓库
    check := checkRequireGitRepo(context.Background())
    if check.Status != StatusPass {
        t.Errorf("git 仓库目录期望 Pass，got %q；msg=%q", check.Status, check.Message)
    }
}

func TestCheckRequireGitRepo_Fail_NotGitRepo(t *testing.T) {
    // 临时目录不是 git 仓库，通过 cd 模拟
    tmpDir := t.TempDir()
    origDir, _ := os.Getwd()
    if err := os.Chdir(tmpDir); err != nil {
        t.Skip("无法切换目录，跳过")
    }
    t.Cleanup(func() { _ = os.Chdir(origDir) })

    check := checkRequireGitRepo(context.Background())
    if check.Status != StatusFail {
        t.Errorf("非 git 目录期望 Fail，got %q；msg=%q", check.Status, check.Message)
    }
    if check.Code != errcodes.MOUNT_REQUIRE_GIT_REPO {
        t.Errorf("期望 Code=MOUNT_REQUIRE_GIT_REPO，got %q", check.Code)
    }
}

// --- checkGitProxyEnabled ---

func TestCheckGitProxyEnabled_Pass_ContainsGit(t *testing.T) {
    // 默认 config 含 git（DefaultProxyCommands 包含 git）
    // 只测试 LoadConfig 成功且含 git 的路径
    tmpHome := t.TempDir()
    t.Setenv("HOME", tmpHome)
    cfgDir := filepath.Join(tmpHome, ".cloud-claude")
    _ = os.MkdirAll(cfgDir, 0755)
    _ = os.WriteFile(filepath.Join(cfgDir, "config.yaml"),
        []byte("gateway: test\nshort_id: test\npassword: test\nproxy_commands: [git]\n"), 0644)

    check := checkGitProxyEnabled(context.Background())
    if check.Status != StatusPass && check.Status != StatusSkip {
        t.Errorf("含 git proxy 期望 Pass（或 Skip 若配置路径不匹配），got %q", check.Status)
    }
}

func TestCheckGitProxyEnabled_Warn_NoGit(t *testing.T) {
    tmpHome := t.TempDir()
    t.Setenv("HOME", tmpHome)
    cfgDir := filepath.Join(tmpHome, ".cloud-claude")
    _ = os.MkdirAll(cfgDir, 0755)
    // proxy_commands 不含 git
    _ = os.WriteFile(filepath.Join(cfgDir, "config.yaml"),
        []byte("gateway: test\nshort_id: test\npassword: test\nproxy_commands: [curl]\n"), 0644)

    check := checkGitProxyEnabled(context.Background())
    if check.Status != StatusWarn && check.Status != StatusSkip {
        t.Errorf("不含 git proxy 期望 Warn（或 Skip），got %q", check.Status)
    }
}

// --- checkDefaultIgnoreLoaded ---

func TestCheckDefaultIgnoreLoaded_Pass_NotSet(t *testing.T) {
    t.Setenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE", "")
    check := checkDefaultIgnoreLoaded(context.Background())
    if check.Status != StatusPass {
        t.Errorf("未设环境变量期望 Pass，got %q", check.Status)
    }
}

func TestCheckDefaultIgnoreLoaded_Warn_Set(t *testing.T) {
    t.Setenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE", "1")
    check := checkDefaultIgnoreLoaded(context.Background())
    if check.Status != StatusWarn {
        t.Errorf("CLOUD_CLAUDE_NO_DEFAULT_IGNORE=1 期望 Warn，got %q", check.Status)
    }
    if check.Code != errcodes.AUTH_CONFIG_MISSING {
        t.Errorf("期望 Code=AUTH_CONFIG_MISSING，got %q", check.Code)
    }
}
```

**import 追加（若不存在）**：
- `"context"`
- `"os"`
- `"path/filepath"`
- `"strings"`
- errcodes 包路径（已有则不重复）

**关于 mockRemoteRunner**：如果 mount_test.go 已有 `mockRemoteRunner` struct，直接复用；若无，在文件中追加：
```go
type mockRemoteRunner struct {
    scriptResults map[string]string
}

func (m *mockRemoteRunner) RunScript(name, script string) (string, string, error) {
    if m.scriptResults != nil {
        if v, ok := m.scriptResults[name]; ok {
            return v, "", nil
        }
    }
    return "", "", nil
}
```
  </action>
  <acceptance_criteria>
    - `grep -c "TestCheckSSHFSCacheArgs\|TestCheckOversizedFilesCount\|TestCheckRequireGitRepo\|TestCheckGitProxyEnabled\|TestCheckDefaultIgnoreLoaded" internal/cloudclaude/doctor/mount_test.go` 输出 ≥ 12（每个 check 至少 2 条测试）
    - `go test ./internal/cloudclaude/doctor/... -run "TestCheckSSHFSCacheArgs|TestCheckOversizedFilesCount|TestCheckRequireGitRepo|TestCheckGitProxyEnabled|TestCheckDefaultIgnoreLoaded" -v` 全部 PASS（无 FAIL）
    - `go test ./internal/cloudclaude/doctor/... -v` 全包无 FAIL（含既有测试无回归）
    - `go test ./... -count=1 2>&1 | grep -E "^(ok|FAIL)" | grep FAIL` 输出为空（全局无回归）
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/cloudclaude/doctor/... -v 2>&1 | tail -20</automated>
  </verify>
  <done>mount_test.go 含 5 项新 check 的完整矩阵测试，全包无 FAIL，SC#4 + SC#6 验收路径通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| doctor check → os.Getwd() | 本地文件系统，无用户输入；doctor 命令约定在工程目录运行（L2）|
| doctor check → RemoteRunner.RunScript | 远端 shell 执行；script 字面量（"mount \| grep sshfs \| head -1"）固定，无用户输入注入风险 |
| checkOversizedFilesCount → last-session.json | 本地 CLI 写入；路径固定（~/.cloud-claude/last-session.json），无跨进程协议变更 |
| checkGitProxyEnabled → LoadConfig | config.yaml 用户可编辑；check 仅读取 proxy_commands 字段，无执行用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Spoofing | gitRevParseTopLevel 结果 | accept | git 二进制完整性属于 OS 安全边界；PATH 劫持与本地访问权等价，超出防御范围（同 T-36-04-01）|
| T-36-06-02 | Information Disclosure | checkOversizedFilesCount Details.top5_files | mitigate | 使用 cwd 相对路径（Plan 03 OversizedFile.Path 为相对路径），不暴露绝对路径或家目录结构（D-13） |
| T-36-06-03 | Tampering | checkSSHFSCacheArgs 远端 mount 输出伪造 | accept | 攻击者需已获得容器 shell 访问权；此时 sshfs 参数的完整性已不是安全边界 |
| T-36-06-04 | Denial of Service | runWithTimeout 5s 限制 | mitigate | checkSSHFSCacheArgs 已走 runWithTimeout（与 sshfs_mountpoint / mergerfs_branches 同模式），超时返回 Fail 不阻塞 doctor 其他 check |
| T-36-06-05 | Elevation of Privilege | checkGitProxyEnabled LoadConfig | accept | LoadConfig 失败返回 Skip，不执行任何特权操作；proxy_commands 仅用于 check，不在 doctor 内执行 |
</threat_model>

<verification>
```bash
# Task 1 验证：编译通过 + 函数存在
go build ./...
grep -c "checkRequireGitRepo\|checkOversizedFilesCount\|checkSSHFSCacheArgs\|checkGitProxyEnabled\|checkDefaultIgnoreLoaded" internal/cloudclaude/doctor/mount.go

# Task 2 验证：矩阵测试全 PASS
go test ./internal/cloudclaude/doctor/... -v 2>&1 | grep -E "PASS|FAIL"

# SC#4 闸门（比 v3.0 多 5 个 check）
# 实际 check 数需 doctor 运行时验证：
# cloud-claude doctor mount --json | jq '.checks | length'

# SC#6 CI 门禁
go test ./... -count=1 2>&1 | grep -E "^(ok|FAIL)"
make ci-gate
```
期望：全 PASS，无 FAIL，`grep FAIL` 为空。
</verification>

<success_criteria>
- mount.go 新增 5 个函数：checkRequireGitRepo / checkOversizedFilesCount / checkSSHFSCacheArgs / checkGitProxyEnabled / checkDefaultIgnoreLoaded
- mount.go 新增 gitRevParseTopLevel private helper（不 import cmd/cloud-claude）
- doctor.go mount domain block 末尾追加 5 行 append（4 本地 + 1 runWithTimeout 远端）
- mount_test.go 新增 ≥12 条矩阵测试覆盖 pass/warn/fail/skip 全分支
- go test ./internal/cloudclaude/doctor/... 全 PASS（含新测试 + 既有测试无回归）
- go test ./... 全 PASS（SC#6 CI 门禁通过）
- make ci-gate PASS（doctor JSON schema_version=1 + warn/fail 含错误码 + ≥200 字 ExtendedExplanations 全自动覆盖）
</success_criteria>

<output>
完成后创建 `.planning/phases/36-sshfs/36-06-SUMMARY.md`，记录：
- 5 项新 check 的实际 Status 分布（pass/warn/fail/skip 各路径验证结果）
- doctor.go mount domain block 实际 check 总数（应 ≥22 = v3.0 17+ + Phase 36 +5）
- ci-gate 输出摘要（PASS 或具体失败项）
- 任何偏差说明（特别是 checkOversizedFilesCount HOME 路径注入方式差异）
</output>
