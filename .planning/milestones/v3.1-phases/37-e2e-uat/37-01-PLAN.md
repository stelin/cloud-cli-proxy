---
phase: 37-e2e-uat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloudclaude/cold_promoter.go
  - internal/cloudclaude/cold_promoter_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-07
  - REQ-MOUNT-V31-09
  - REQ-MOUNT-V31-10

must_haves:
  truths:
    - "ColdPromoter.Run(ctx) 在 ctx cancel 后 5s 内返回"
    - "PromotionEngine.Enqueue 相同 path 100ms 内 50 次调用 → 实际 SFTP 拉取仅 1 次"
    - "PromotionEngine 拉取失败按 1/2/4s 退避重试，第 3 次失败写 stderr 并加入熔断集合"
    - "ColdPromoter 启动时写 PID file ~/.cloud-claude/cold-promoter.pid"
    - "inotify 仅监听 IN_OPEN | IN_ACCESS | IN_CLOSE_NOWRITE 三个事件"
    - "单文件 500MB 晋升期间 cold 仍可读（用户 cat 不阻塞）"
  artifacts:
    - path: "internal/cloudclaude/cold_promoter.go"
      provides: "ColdPromoter（inotify watcher）+ PromotionEngine（异步 SFTP 晋升 + 去重 + 熔断）"
      contains: "NewColdPromoter"
    - path: "internal/cloudclaude/cold_promoter_test.go"
      provides: "4 条核心路径单测覆盖 dedup / retry-backoff / circuit-breaker / no-block"
      contains: "TestPromotionDedup"
  key_links:
    - from: "cold_promoter.go::PromotionEngine.Enqueue"
      to: "cold_promoter.go::PromotionEngine.promotePath"
      via: "5s debounce 窗口去重 + 异步入队"
      pattern: "PromotionDedupWindow.*=.*5.*time.Second"
    - from: "cold_promoter.go::ColdPromoter.Run"
      to: "cold_promoter.go::inotify 事件循环"
      via: "IN_OPEN/IN_ACCESS → events channel → PromotionEngine"
      pattern: "unix.InotifyAddWatch.*IN_OPEN"
---

<objective>
实现 ColdPromoter 核心引擎：inotify 冷文件读监听器 + PromotionEngine（异步 SFTP 晋升、5s 去重、指数退避重试、熔断集合）。本 plan 是 Phase 37 的核心实现，独立于 mount 集成和 doctor 可观测，Wave 1 先跑。

Purpose: 建立"冷文件读触发晋升"的完整引擎，所有行为可通过单测断言
Output: cold_promoter.go（新文件）+ cold_promoter_test.go（4 条核心单测 PASS）
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-e2e-uat/37-CONTEXT.md
@.planning/phases/37-e2e-uat/37-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
PromotionEngine 接口（需实现的 API）：

```go
// NewColdPromoter 构造 ColdPromoter，绑定 connB（SFTP 数据面）+ coldRoot（inotify 监听源）+ hotRoot（晋升目标）。
// pidFile 用于写入 PID（doctor promoter_alive check 依赖）。
func NewColdPromoter(connB *ssh.Client, coldRoot, hotRoot string, logger io.Writer, pidFile string) *ColdPromoter

// Run 阻塞运行 inotify 事件循环 + PromotionEngine；ctx cancel 后 drain 剩余事件并返回。
func (cp *ColdPromoter) Run(ctx context.Context)

// QueueDepth 返回 PromotionEngine 内部队列深度（doctor 可观测）。
func (cp *ColdPromoter) QueueDepth() int

// Stats 返回晋升统计（promotionCount, promotionBytes, promotionFailedCount）。
func (cp *ColdPromoter) Stats() (count int, bytes int64, failed int)

// Wait 等待 Run 返回（用于 cleanup LIFO）。
func (cp *ColdPromoter) Wait()
```

SFTP client 用法（复用 connB，独立 sftp session）：

```go
client, err := sftp.NewClient(cp.connB)
// ...
src, err := client.Open(remotePath)
// ...
dst, err := os.Create(localPath)
// ...
io.Copy(dst, src)
```

inotify 初始化模板（golang.org/x/sys/unix）：

```go
fd, err := unix.InotifyInit()
// ...
unix.InotifyAddWatch(fd, coldRoot, unix.IN_OPEN|unix.IN_ACCESS|unix.IN_CLOSE_NOWRITE)
// 读取事件:
buf := make([]byte, unix.SizeofInotifyEvent*1024)
n, err := unix.Read(fd, buf)
```

SSHFSWatcher 参考模式（internal/cloudclaude/sshfs_watcher.go）：
- Run(ctx) 使用 for { select { case <-ctx.Done(): return; case <-ticker.C: ... } }
- check 字段为函数注入（便于单测）
- ctx cancel 即停止

HotSyncEngine.copyRemoteToLocal 参考模式（internal/cloudclaude/hot_sync.go L383-408）：
- sftpClient.Open → os.Create → io.Copy → Close
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 cold_promoter.go — inotify watcher 基础框架</name>
  <read_first>
    - internal/cloudclaude/sshfs_watcher.go（Watcher Run(ctx) 模式参考）
    - internal/cloudclaude/hot_sync.go L383-408（copyRemoteToLocal 模式参考）
    - internal/cloudclaude/mount_strategy.go L463-488（tryModeReal Full 路径，理解 coldRoot/hotRoot/stageBase 命名）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"Watcher 策略"（inotify 监听范围 + 降级语义）
    - .planning/phases/37-e2e-uat/37-RESEARCH.md §1 §3（技术选型 + 生命周期）
  </read_first>
  <action>
新建 `internal/cloudclaude/cold_promoter.go`，实现：

**Step 1 — ColdPromoter 结构体：**

```go
type ColdPromoter struct {
    connB    *ssh.Client
    coldRoot string
    hotRoot  string
    logger   io.Writer
    pidFile  string

    ctx       context.Context
    cancel    context.CancelFunc
    doneCh    chan struct{}

    // PromotionEngine 内部状态
    mu            sync.Mutex
    queue         map[string]*promotionEntry
    events        chan string
    circuitBreaker map[string]struct{}

    // 统计（atomic）
    promotionCount       atomic.Int64
    promotionBytes       atomic.Int64
    promotionFailedCount atomic.Int64
}

type promotionEntry struct {
    path         string
    lastEnqueued time.Time
}

func NewColdPromoter(connB *ssh.Client, coldRoot, hotRoot string, logger io.Writer, pidFile string) *ColdPromoter
```

**Step 2 — inotify 初始化逻辑：**

```go
func (cp *ColdPromoter) initInotify() (fd int, err error)
```

- `unix.InotifyInit()` → fd
- `unix.InotifyAddWatch(fd, coldRoot, unix.IN_OPEN|unix.IN_ACCESS|unix.IN_CLOSE_NOWRITE)`
- 失败返回 error（调用方降级）

**Step 3 — PID file 写入：**

```go
func (cp *ColdPromoter) writePIDFile() error
```

- `os.WriteFile(cp.pidFile, []byte(fmt.Sprintf("%d\n", os.Getpid())), 0644)`
- 失败返回 error（调用方降级，不 panic）

**Step 4 — Run 主循环框架：**

```go
func (cp *ColdPromoter) Run(ctx context.Context) {
    defer close(cp.doneCh)
    defer os.Remove(cp.pidFile)

    fd, err := cp.initInotify()
    if err != nil {
        fmt.Fprintln(cp.logger, errcodes.Format(errcodes.MOUNT_PROMOTER_FAILED, err.Error()))
        return
    }
    defer unix.Close(fd)

    if err := cp.writePIDFile(); err != nil {
        fmt.Fprintln(cp.logger, errcodes.Format(errcodes.MOUNT_PROMOTER_FAILED, err.Error()))
        unix.Close(fd)
        return
    }

    // 启动 PromotionEngine 消费循环
    go cp.runPromotionLoop(ctx)

    // inotify 主循环
    buf := make([]byte, unix.SizeofInotifyEvent*1024)
    for {
        select {
        case <-ctx.Done():
            return
        default:
        }
        n, err := unix.Read(fd, buf)
        if err != nil {
            if errors.Is(err, unix.EINTR) {
                continue
            }
            return
        }
        // 解析事件、提取 path、入队
        cp.processInotifyEvents(buf[:n])
    }
}
```

**Step 5 — inotify 事件解析：**

遍历 `buf` 中的 `unix.InotifyEvent`，提取 `Name` 字段，若事件 mask 含 `IN_OPEN|IN_ACCESS|IN_CLOSE_NOWRITE` 且值非空 → `cp.enqueue(name)`。

注意：事件 `Len` 为 0 时跳过；`Name` 需要去除尾随 `\x00`。
  </action>
  <acceptance_criteria>
    - `grep -c "func NewColdPromoter" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "unix.InotifyInit" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "func (cp \*ColdPromoter) Run" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "func (cp \*ColdPromoter) Wait" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `go build ./...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./...</automated>
  </verify>
  <done>cold_promoter.go 含 ColdPromoter 结构体 + Run + Wait + initInotify + writePIDFile + inotify 事件解析</done>
</task>

<task type="auto">
  <name>Task 2: 实现 PromotionEngine（异步拉升 + 去重 + 熔断）</name>
  <read_first>
    - internal/cloudclaude/cold_promoter.go（Task 1 产物）
    - internal/cloudclaude/hot_sync.go L383-408（copyRemoteToLocal）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"PromotionEngine 行为"
    - .planning/phases/37-e2e-uat/37-RESEARCH.md §2
  </read_first>
  <action>
在 cold_promoter.go 中追加 PromotionEngine 实现：

**Step 1 — Enqueue（入队 + 去重）：**

```go
const promotionDedupWindow = 5 * time.Second

func (cp *ColdPromoter) enqueue(path string) {
    cp.mu.Lock()
    entry, exists := cp.queue[path]
    now := time.Now()
    if exists {
        if now.Sub(entry.lastEnqueued) < promotionDedupWindow {
            entry.lastEnqueued = now
            cp.mu.Unlock()
            return // 5s 窗口内重复，丢弃
        }
        entry.lastEnqueued = now
    } else {
        cp.queue[path] = &promotionEntry{path: path, lastEnqueued: now}
    }
    cp.mu.Unlock()
    // 异步入队
    select {
    case cp.events <- path:
    default:
        // events channel 满了，丢弃（buffer=1024，溢出说明晋升积压严重）
    }
}
```

**Step 2 — runPromotionLoop（消费循环）：**

```go
func (cp *ColdPromoter) runPromotionLoop(ctx context.Context) {
    for {
        select {
        case <-ctx.Done():
            return
        case path := <-cp.events:
            go cp.promotePath(path) // 异步 goroutine，不阻塞循环
        }
    }
}
```

**Step 3 — promotePath（SFTP 拉取 + 重试 + 熔断）：**

```go
func (cp *ColdPromoter) promotePath(relPath string) {
    // 熔断检查
    cp.mu.Lock()
    if _, blocked := cp.circuitBreaker[relPath]; blocked {
        cp.mu.Unlock()
        return
    }
    cp.mu.Unlock()

    remotePath := filepath.Join(cp.coldRoot, relPath)
    localPath := filepath.Join(cp.hotRoot, relPath)

    backoffs := []time.Duration{1 * time.Second, 2 * time.Second, 4 * time.Second}
    var lastErr error
    for attempt := 0; attempt < 3; attempt++ {
        if attempt > 0 {
            time.Sleep(backoffs[attempt-1])
        }
        err := cp.copyFromCold(remotePath, localPath)
        if err == nil {
            cp.promotionCount.Add(1)
            // bytes 统计（在 copyFromCold 中累加）
            return
        }
        lastErr = err
    }
    // 3 次失败：stderr + 熔断
    fmt.Fprintf(cp.logger, "[!] 晋升失败 %s: %v\n", relPath, lastErr)
    cp.promotionFailedCount.Add(1)
    cp.mu.Lock()
    cp.circuitBreaker[relPath] = struct{}{}
    cp.mu.Unlock()
}
```

**Step 4 — copyFromCold（SFTP 拉取实现）：**

- 创建独立 SFTP client（`sftp.NewClient(cp.connB)`）
- `client.Open(remotePath)` → `os.MkdirAll(filepath.Dir(localPath), 0755)` → `os.Create(localPath)` → `io.Copy(dst, src)`
- 拉取完成后 `cp.promotionBytes.Add(fileSize)`
- 复用 `io.Copy` 模式（与 `HotSyncEngine.copyRemoteToLocal` 一致）

**Step 5 — QueueDepth + Stats 暴露：**

```go
func (cp *ColdPromoter) QueueDepth() int {
    return len(cp.events)
}

func (cp *ColdPromoter) Stats() (int, int64, int) {
    return int(cp.promotionCount.Load()), cp.promotionBytes.Load(), int(cp.promotionFailedCount.Load())
}

func (cp *ColdPromoter) Wait() {
    <-cp.doneCh
}
```
  </action>
  <acceptance_criteria>
    - `grep -c "promotionDedupWindow" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "func (cp \*ColdPromoter) promotePath" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "func (cp \*ColdPromoter) QueueDepth" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `grep -c "func (cp \*ColdPromoter) Stats" internal/cloudclaude/cold_promoter.go` 输出 `1`
    - `go build ./...` 无编译错误
    - `go vet ./internal/cloudclaude/...` 无警告
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/cloudclaude/...</automated>
  </verify>
  <done>PromotionEngine 完整实现：Enqueue 含 5s 去重 + runPromotionLoop 消费 + promotePath 含 1/2/4s 退避 + copyFromCold SFTP 拉取 + QueueDepth/Stats/Wait API</done>
</task>

<task type="auto">
  <name>Task 3: 编写 cold_promoter_test.go 单测</name>
  <read_first>
    - internal/cloudclaude/cold_promoter.go（Task 1 + 2 产物）
    - internal/cloudclaude/hot_sync_test.go（现有单测模式参考）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"PromotionEngine 行为"
    - .planning/phases/37-e2e-uat/37-RESEARCH.md §11
  </read_first>
  <action>
新建 `internal/cloudclaude/cold_promoter_test.go`，编写 4 条核心单测：

**Test 1 — TestPromotionDedup：**
- 构建 countingSFTPClient（包装 fake，记录 Open 调用次数）
- 相同 path 在 100ms 内调用 50 次 Enqueue
- time.Sleep(200ms) 等待异步处理
- 断言：实际 SFTP Open 调用次数 == 1

**Test 2 — TestPromotionRetryBackoff：**
- 构建 failingSFTPClient（前 2 次 Open 返回 error，第 3 次成功）
- 调用 promotePath
- 测量总耗时应在 1s+2s 之间（退避 1/2s，第 3 次成功）
- 断言：promotionCount == 1, promotionFailedCount == 0

**Test 3 — TestPromotionCircuitBreaker：**
- 构建 alwaysFailingSFTPClient
- 调用 promotePath → 第 3 次失败后熔断
- 再次调用 promotePath（同 path）→ 立即返回（不触发拉取）
- 断言：promotionFailedCount == 1, SFTP Open 调用次数 == 3（仅第一次 promotePath 的 3 次重试）

**Test 4 — TestPromoterStartStop：**
- 构造 ColdPromoter，用 context.WithCancel
- go cp.Run(ctx)
- time.Sleep(100ms) 确认 Run 已启动
- cancel() → Wait() 5s 内返回
- 断言：pidFile 被清理（os.Stat 返回 IsNotExist）

SFTP client mock 策略：通过包级 var（如 `testSFTPClientFactory func(conn *ssh.Client) (*sftp.Client, error)`）注入 fake。Phase 36 的 strategyHooks 模式可作为参考。

race detector：`go test -race ./internal/cloudclaude/... -run TestPromotion`
  </action>
  <acceptance_criteria>
    - `go test ./internal/cloudclaude/... -run TestPromotionDedup -v` 输出 PASS
    - `go test ./internal/cloudclaude/... -run TestPromotionRetryBackoff -v` 输出 PASS
    - `go test ./internal/cloudclaude/... -run TestPromotionCircuitBreaker -v` 输出 PASS
    - `go test ./internal/cloudclaude/... -run TestPromoterStartStop -v` 输出 PASS
    - `go test -race ./internal/cloudclaude/... -run TestPromotion` 无 race 警告
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/cloudclaude/... -run "TestPromotion|TestPromoter" -v</automated>
  </verify>
  <done>4 条单测全 PASS（含 race detector），覆盖 dedup / retry-backoff / circuit-breaker / start-stop</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| SFTP client (connB) → 远端容器 | 晋升路径：从 cold 分支（sshfs）读文件写到 hot 分支；权限由容器内 uid 控制 |
| PID file → 本地文件系统 | doctor promoter_alive 读 pidfile 检查进程存活；路径权限 0644 |
| inotify 事件 → ColdPromoter | 容器内文件系统事件，不跨越信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | PID file | mitigate | pidFile 放在 ~/.cloud-claude/ 下（0700 目录），其他用户不可写；Run 退出时 os.Remove |
| T-37-01-02 | Denial of Service | events channel | mitigate | buffer=1024，溢出时静默丢弃（不阻塞 inotify 循环），stderr 输出 warning |
| T-37-01-03 | Elevation of Privilege | SFTP copy | accept | 复用现有 connB SSH 连接，权限模型与 hot_sync 一致；晋升的文件权限由 os.Create 决定（0644） |
</threat_model>

<verification>
```bash
# Wave 1 本 plan 完整验证
go test -race ./internal/cloudclaude/... -run "TestPromotion|TestPromoter" -v
go build ./...
go vet ./internal/cloudclaude/...
```
期望：全 PASS，无编译错误，无 race 警告。
</verification>

<success_criteria>
- cold_promoter.go 含 ColdPromoter + PromotionEngine 完整实现
- PromotionEngine 5s 去重窗口 + 1/2/4s 退避 + circuit breaker 全部就位
- 4 条核心单测 PASS（含 race detector）
- go build + go vet 干净
</success_criteria>

<output>
完成后创建 `.planning/phases/37-e2e-uat/37-01-SUMMARY.md`，记录：
- ColdPromoter 公开 API 签名
- PromotionEngine 关键常量（dedup window / backoff 序列）
- 4 条单测名称与通过状态
- 任何偏差说明
</output>
