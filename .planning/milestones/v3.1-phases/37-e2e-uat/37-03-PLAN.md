---
phase: 37-e2e-uat
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - internal/cloudclaude/errcodes/codes.go
  - internal/cloudclaude/errcodes/mount.go
  - internal/cloudclaude/errcodes/explanations.go
  - internal/cloudclaude/doctor/mount.go
  - cmd/cloud-claude/explain_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-14

must_haves:
  truths:
    - "errcodes.Registry() 包含 MOUNT_PROMOTER_FAILED（severity=warn）"
    - "ExtendedExplanations[MOUNT_PROMOTER_FAILED] 字符数 >= 200"
    - "cloud-claude doctor mount --json 含有 promoter_alive / promotion_queue_depth / promotion_total / promotion_failed_total 4 个新 check"
    - "CI grep 锁定新增 4 个 check（make ci-gate 通过）"
    - "cloud-claude explain MOUNT_PROMOTER_FAILED 子进程 exit 0，stdout >= 200 字"
  artifacts:
    - path: "internal/cloudclaude/errcodes/codes.go"
      provides: "MOUNT_PROMOTER_FAILED 常量声明"
      contains: "MOUNT_PROMOTER_FAILED"
    - path: "internal/cloudclaude/errcodes/mount.go"
      provides: "MOUNT_PROMOTER_FAILED MustRegister 注册"
      contains: "MOUNT_PROMOTER_FAILED"
    - path: "internal/cloudclaude/errcodes/explanations.go"
      provides: "MOUNT_PROMOTER_FAILED registerExplanation >= 200 中文字符"
      contains: "MOUNT_PROMOTER_FAILED"
    - path: "internal/cloudclaude/doctor/mount.go"
      provides: "4 项 promotion check（promoter_alive / promotion_queue_depth / promotion_total / promotion_failed_total）"
      contains: "checkPromoterAlive"
  key_links:
    - from: "errcodes/codes.go MOUNT_PROMOTER_FAILED"
      to: "errcodes/mount.go MustRegister"
      via: "const → init() 注册"
      pattern: "MustRegister.*MOUNT_PROMOTER_FAILED"
    - from: "doctor/mount.go checkPromoterAlive"
      to: "last_session.go LoadLastSession"
      via: "读取 PID file + last-session.json"
      pattern: "promoter_alive"
---

<objective>
补齐 Phase 37 的错误码基础设施和 doctor 可观测：注册 MOUNT_PROMOTER_FAILED 错误码及其 >=200 字长说明，在 doctor mount 域新增 4 项晋升指标 check，并通过 explain 子进程测试 + CI gate 验收。

Purpose: 让晋升机制具备可观测性（doctor）+ 可解释性（explain），满足 M14 四要素契约
Output: 1 条新错误码全链路 + 4 项 doctor check + explain 测试 PASS
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-uat/37-CONTEXT.md
@.planning/phases/37-e2e-uat/37-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
错误码注册模板（errcodes/mount.go init() 末尾追加，参考 MOUNT_OVERSIZED_FILE_SKIPPED）：

```go
MustRegister(Entry{
    Code:       MOUNT_PROMOTER_FAILED,
    Severity:   SeverityWarn,
    Message:    "cold-promoter 进程启动失败: %s，降级为无晋升模式（cold 分支仍可读）",
    NextAction: "检查容器内 /proc/sys/fs/inotify/max_user_watches 限制，或设置 CLOUD_CLAUDE_NO_PROMOTION=1 显式关闭",
})
```

Code 常量（codes.go MOUNT_* 块，字母序 P 在 O 之后 R 之前）：

```go
MOUNT_PROMOTER_FAILED Code = "MOUNT_PROMOTER_FAILED"
```

Doctor check 注册模板（doctor/mount.go 末尾追加，参考 Phase 36 checkRequireGitRepo）：

```go
func checkPromoterAlive(ctx context.Context) Check {
    // 读取 PID file
    // kill -0 检测进程存活
}

func checkPromotionQueueDepth(ctx context.Context) Check {
    // 读 last-session.json
}

func checkPromotionTotal(ctx context.Context) Check {
    // 读 last-session.json::promotion_count
}

func checkPromotionFailedTotal(ctx context.Context) Check {
    // 读 last-session.json::promotion_failed_count
}
```

Doctor mount 域追加位置：在 doctor.go::RunDoctor mount 块末尾（checkDefaultIgnoreLoaded 之后）追加 4 个新 check 调用。

explain 子进程测试模板（cmd/cloud-claude/explain_test.go 末尾追加，参考 TestExplain_MountRequireGitRepo_Exit0_MinLen）：

```go
func TestExplain_MountPromoterFailed_Exit0_MinLen(t *testing.T) {
    bin := buildOnceExplainBin(t)
    code, stdout, stderr := runExplainBin(t, bin, "explain", "MOUNT_PROMOTER_FAILED")
    if code != 0 {
        t.Fatalf("known code 应 exit 0，实际 %d；stderr=%q", code, stderr)
    }
    if !strings.Contains(stdout, "MOUNT_PROMOTER_FAILED") {
        t.Errorf("stdout 未包含错误码字面量")
    }
    if n := utf8.RuneCountInString(stdout); n < 200 {
        t.Errorf("stdout 字符数 %d < 200", n)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册 MOUNT_PROMOTER_FAILED 错误码 + 长说明</name>
  <read_first>
    - internal/cloudclaude/errcodes/codes.go（MOUNT_* const 块末尾，确认插入位置字母序 P 在 O 之后 R 之前）
    - internal/cloudclaude/errcodes/mount.go（init() 末尾，确认 MustRegister 追加位置）
    - internal/cloudclaude/errcodes/explanations.go（init() 末尾 MOUNT_* 注释块，确认 registerExplanation 追加位置）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"错误码"
    - .planning/phases/37-e2e-uat/37-RESEARCH.md §4
  </read_first>
  <action>
**Step 1 — codes.go MOUNT_* 块追加常量（字母序 O → P → R）：**

在 `MOUNT_OVERSIZED_FILE_SKIPPED` 行之后、`MOUNT_REQUIRE_GIT_REPO` 行之前插入：
```go
MOUNT_PROMOTER_FAILED Code = "MOUNT_PROMOTER_FAILED"
```

**Step 2 — mount.go init() 末尾追加 MustRegister：**

```go
MustRegister(Entry{
    Code:       MOUNT_PROMOTER_FAILED,
    Severity:   SeverityWarn,
    Message:    "cold-promoter 进程启动失败: %s，降级为无晋升模式（cold 分支仍可读）",
    NextAction: "检查容器内 /proc/sys/fs/inotify/max_user_watches 限制，或设置 CLOUD_CLAUDE_NO_PROMOTION=1 显式关闭",
})
```

NextAction 长度验证：
- `utf8.RuneCountInString("检查容器内 /proc/sys/fs/inotify/max_user_watches 限制，或设置 CLOUD_CLAUDE_NO_PROMOTION=1 显式关闭")` 预期 < 80 rune
- 若超过 80，压缩为："检查 inotify max_user_watches 限制，或 CLOUD_CLAUDE_NO_PROMOTION=1 显式关闭"

**Step 3 — explanations.go init() 末尾追加 registerExplanation：**

```go
registerExplanation(MOUNT_PROMOTER_FAILED, `触发场景：Full 模式 mount 就绪后，系统尝试在 cold 分支根目录启动 inotify cold-promoter watcher，但 inotify 初始化失败（如 /proc/sys/fs/inotify/max_user_watches 耗尽、PID file 目录不可写），或 PID file 写入失败（~/.cloud-claude 目录权限不足）。

根本原因：容器内 inotify watch 数量受内核参数 max_user_watches 限制（默认 8192）；大规模仓库的文件数量可能接近或超过此限制。PID file 写入要求 ~/.cloud-claude/ 目录可写（cloud-claude 默认通过 ConfigDir 管理）。任一条件不满足均会导致 watcher 无法启动。

复现方式：（1）临时降低 inotify 限制：echo 1 > /proc/sys/fs/inotify/max_user_watches && cloud-claude；（2）移除 ~/.cloud-claude/ 目录写权限：chmod 500 ~/.cloud-claude && cloud-claude。

修复路径：（1）检查并调大 max_user_watches：cat /proc/sys/fs/inotify/max_user_watches，建议设为 65536；（2）确认 ~/.cloud-claude/ 目录权限为 0700；（3）若无需晋升机制，设置 CLOUD_CLAUDE_NO_PROMOTION=1 显式关闭（cold 仍可读，功能完整）；（4）降级模式下每次文件读取都通过 sshfs 回源，无性能优化但功能不受影响。

关联文档：REQUIREMENTS.md §REQ-MOUNT-V31-07；runbooks/v31-cold-promotion.md §"晋升失败排障"。`)
```

确认总字符数 >= 200（`utf8.RuneCountInString`），五段结构（触发场景/根本原因/复现方式/修复路径/关联文档）完整。
  </action>
  <acceptance_criteria>
    - `grep -c "MOUNT_PROMOTER_FAILED" internal/cloudclaude/errcodes/codes.go` 输出 `1`
    - `grep -c "MOUNT_PROMOTER_FAILED" internal/cloudclaude/errcodes/mount.go` 输出 `1`
    - `grep -c "MOUNT_PROMOTER_FAILED" internal/cloudclaude/errcodes/explanations.go` 输出 `1`
    - `go test ./internal/cloudclaude/errcodes/... -run TestErrcodesRegistry -v` 输出 PASS
    - `go test ./internal/cloudclaude/errcodes/... -run TestAllCodesHaveExplanations -v` 输出 PASS（≥200 rune 断言通过）
    - `go build ./...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/cloudclaude/errcodes/... -v && go build ./...</automated>
  </verify>
  <done>MOUNT_PROMOTER_FAILED 全链路：codes.go const + mount.go MustRegister + explanations.go registerExplanation >= 200 字</done>
</task>

<task type="auto">
  <name>Task 2: Doctor mount 域新增 4 项 promotion check</name>
  <read_first>
    - internal/cloudclaude/doctor/mount.go（全文，确认 check 函数模式 + 末尾追加位置）
    - internal/cloudclaude/doctor/doctor.go L80-120（RunDoctor mount 域 checks 列表追加位置）
    - internal/cloudclaude/last_session.go（确认 LoadLastSession + LastSessionSnapshot 字段）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"Doctor 可观测"
    - .planning/phases/37-e2e-uat/37-RESEARCH.md §6
  </read_first>
  <action>
在 `internal/cloudclaude/doctor/mount.go` 末尾追加 4 个 check 函数：

**Step 1 — checkPromoterAlive：**

```go
func checkPromoterAlive(ctx context.Context) Check {
    home, err := os.UserHomeDir()
    if err != nil {
        return newSkip("mount", "promoter_alive", "无法定位 home 目录: "+err.Error())
    }
    pidFile := filepath.Join(home, ".cloud-claude", "cold-promoter.pid")
    data, err := os.ReadFile(pidFile)
    if err != nil {
        return newSkip("mount", "promoter_alive", "PID file 不存在（watcher 未启动或 NO_PROMOTION=1）")
    }
    pidStr := strings.TrimSpace(string(data))
    cmd := exec.Command("kill", "-0", pidStr)
    if err := cmd.Run(); err != nil {
        return newWarn("mount", "promoter_alive", errcodes.MOUNT_PROMOTER_FAILED)
    }
    return newPass("mount", "promoter_alive", "cold-promoter 进程存活（PID "+pidStr+"）")
}
```

**Step 2 — checkPromotionQueueDepth：**

读 last-session.json 的 queue_depth（当前会话的临终快照，由 promoter 在 cleanup 时写入）。若 last-session.json 不存在 → skip。

```go
func checkPromotionQueueDepth(ctx context.Context) Check {
    snap, err := cloudclaude.LoadLastSession()
    if err != nil || snap == nil {
        return newSkip("mount", "promotion_queue_depth", "last-session.json 不存在")
    }
    // 注：QueueDepth 字段尚未加入 LastSessionSnapshot；
    // 本 check 先读 promotion_count 作为替代指标（队列有积压 → count 会增长）。
    // 若后续 Plan 02 追加了 QueueDepth 字段，则改用 snap.PromotionQueueDepth。
    if snap.PromotionCount > 0 {
        return newPass("mount", "promotion_queue_depth",
            fmt.Sprintf("promotion 活跃（累计 %d 次晋升）", snap.PromotionCount))
    }
    return newSkip("mount", "promotion_queue_depth", "无晋升活动记录")
}
```

实际上 CONTEXT.md 说 promotion_queue_depth 从 PromotionEngine 内部队列获取，但 doctor 运行在本地（非容器内），无法直接访问 goroutine 内部状态。**改为从 last-session.json 或 PID file 旁的状态文件获取。**

**推荐方案：** promotion cleanup 时在 last-session.json 中追加 `promotion_queue_depth` 字段（当前深度快照）。Plan 02 的 last_session.go 追加第 4 个字段。

**若 Plan 02 未跟上，本 check 读 last-session.json 的 promotion 字段做 simplified 判断：**
- promotion_count > 0 且 promoter_alive → 当前有活跃晋升 → pass
- 否则 → skip

**Step 3 — checkPromotionTotal：**

```go
func checkPromotionTotal(ctx context.Context) Check {
    snap, err := cloudclaude.LoadLastSession()
    if err != nil || snap == nil {
        return newSkip("mount", "promotion_total", "last-session.json 不存在")
    }
    count := snap.PromotionCount
    if count > 0 {
        return newPass("mount", "promotion_total", fmt.Sprintf("累计晋升 %d 个文件", count))
    }
    return newSkip("mount", "promotion_total", "本次会话无晋升记录")
}
```

**Step 4 — checkPromotionFailedTotal：**

```go
func checkPromotionFailedTotal(ctx context.Context) Check {
    snap, err := cloudclaude.LoadLastSession()
    if err != nil || snap == nil {
        return newSkip("mount", "promotion_failed_total", "last-session.json 不存在")
    }
    failed := snap.PromotionFailedCount
    if failed > 0 {
        return newWarn("mount", "promotion_failed_total", errcodes.MOUNT_PROMOTER_FAILED)
    }
    return newPass("mount", "promotion_failed_total", "所有晋升均成功（0 次失败）")
}
```

**Step 5 — 在 doctor.go::RunDoctor mount 域追加调用：**

在 `checkDefaultIgnoreLoaded` 之后追加 4 个 check：

```go
// Phase 37: 晋升可观测
checks = append(checks, checkPromoterAlive(ctx))
checks = append(checks, checkPromotionQueueDepth(ctx))
checks = append(checks, checkPromotionTotal(ctx))
checks = append(checks, checkPromotionFailedTotal(ctx))
```

**Step 6 — doctor/mount.go import 追加（若需要）：**
```go
"os/exec"
"path/filepath"
"strings"
```
  </action>
  <acceptance_criteria>
    - `grep -c "promoter_alive" internal/cloudclaude/doctor/mount.go` 输出 `>=1`
    - `grep -c "promotion_queue_depth" internal/cloudclaude/doctor/mount.go` 输出 `>=1`
    - `grep -c "promotion_total" internal/cloudclaude/doctor/mount.go` 输出 `>=1`
    - `grep -c "promotion_failed_total" internal/cloudclaude/doctor/mount.go` 输出 `>=1`
    - `grep -c "checkPromoterAlive\|checkPromotionQueueDepth\|checkPromotionTotal\|checkPromotionFailedTotal" internal/cloudclaude/doctor/doctor.go` 输出 `4`
    - `go build ./...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/cloudclaude/doctor/... && bash scripts/ci-doctor-grep.sh ./cloud-claude</automated>
  </verify>
  <done>doctor mount 域追加 promoter_alive / promotion_queue_depth / promotion_total / promotion_failed_total 4 项 check</done>
</task>

<task type="auto">
  <name>Task 3: 追加 explain 子进程测试</name>
  <read_first>
    - cmd/cloud-claude/explain_test.go（确认 buildOnceExplainBin / runExplainBin 模式 + 现有 TestExplain_MountOversizedFileSkipped_Exit0_MinLen 为模板）
    - .planning/phases/37-e2e-uat/37-CONTEXT.md §"错误码"
  </read_first>
  <action>
在 `cmd/cloud-claude/explain_test.go` 末尾追加：

```go
func TestExplain_MountPromoterFailed_Exit0_MinLen(t *testing.T) {
    bin := buildOnceExplainBin(t)
    code, stdout, stderr := runExplainBin(t, bin, "explain", "MOUNT_PROMOTER_FAILED")
    if code != 0 {
        t.Fatalf("known code 应 exit 0，实际 %d；stderr=%q", code, stderr)
    }
    if !strings.Contains(stdout, "MOUNT_PROMOTER_FAILED") {
        t.Errorf("stdout 未包含错误码字面量")
    }
    if n := utf8.RuneCountInString(stdout); n < 200 {
        t.Errorf("stdout 字符数 %d < 200（ExtendedExplanations >=200 中文字符）", n)
    }
}
```

确认 import 段已有 `"strings"` 和 `"unicode/utf8"`（从 Phase 36 测试继承）。
  </action>
  <acceptance_criteria>
    - `grep -c "TestExplain_MountPromoterFailed_Exit0_MinLen" cmd/cloud-claude/explain_test.go` 输出 `1`
    - `go test ./cmd/cloud-claude/... -run TestExplain_MountPromoterFailed -v` 输出 PASS
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/cloud-claude/... -run TestExplain_Mount -v</automated>
  </verify>
  <done>explain 子进程测试通过，MOUNT_PROMOTER_FAILED 可被 cloud-claude explain 正确渲染</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| doctor check → PID file / last-session.json | 本地只读文件；不受外部输入影响 |
| explain stdout → 用户终端 | 长说明为纯文档文字，不含运行时数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering | PID file | mitigate | PID file 在 ~/.cloud-claude/（0700），kill -0 仅验证 PID 有效不造成副作用 |
| T-37-03-02 | Information Disclosure | doctor JSON | accept | promotion 字段仅含计数，不含文件路径或内容 |
</threat_model>

<verification>
```bash
# Wave 2 本 plan 完整验证
go test ./internal/cloudclaude/errcodes/... -v
go test ./cmd/cloud-claude/... -run TestExplain_Mount -v
go build ./... && go vet ./internal/cloudclaude/doctor/...
bash scripts/ci-doctor-grep.sh ./cloud-claude
```
期望：全 PASS，ci-doctor-grep 通过 M14 + SC#3 三断言。
</verification>

<success_criteria>
- MOUNT_PROMOTER_FAILED 全链路注册（codes.go + mount.go + explanations.go）
- doctor mount 域新增 4 项 promotion check
- explain 子进程测试 PASS
- ci-doctor-grep 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/37-e2e-uat/37-03-SUMMARY.md`，记录：
- MOUNT_PROMOTER_FAILED 注册位置 + Severity + NextAction 字面量
- 4 项 doctor check 名称与 Status 逻辑
- explain 测试实际 stdout 字符数
- 任何偏差说明
</output>
