---
phase: 038-ssh-proxy-port-forwarding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/sshproxy/forward.go
  - internal/sshproxy/forward_test.go
  - internal/sshproxy/proxy.go
autonomous: true
requirements:
  - SSH-01
  - SSH-04

must_haves:
  truths:
    - "SSH Proxy 接受 direct-tcpip channel 请求并代理到容器侧"
    - "direct-tcpip payload 被正确解析（raddr, rport, laddr, lport）"
    - "转发到管理网段（10.99.x.x）、Docker socket、metadata 端点的请求被明确拒绝"
    - "同一 SSH 连接支持多个并发 direct-tcpip channel"
    - "channel 关闭后 goroutine 不泄漏（CloseWrite 模式）"
  artifacts:
    - path: "internal/sshproxy/forward.go"
      provides: "handleDirectTCPIP + validateForwardTarget + isForbiddenTarget"
      contains: "func (s \*Server) handleDirectTCPIP"
    - path: "internal/sshproxy/forward_test.go"
      provides: "direct-tcpip 单元测试（payload 解析 + 安全校验 + 双向拷贝）"
      contains: "TestDirectTCPIP_PayloadParse"
    - path: "internal/sshproxy/proxy.go"
      provides: "handleConnection channel 分发（session / direct-tcpip）"
      contains: "case \"direct-tcpip\":"
  key_links:
    - from: "proxy.go::handleConnection"
      to: "forward.go::handleDirectTCPIP"
      via: "switch newChan.ChannelType() 分发"
      pattern: "case \"direct-tcpip\":.*handleDirectTCPIP"
    - from: "forward.go::handleDirectTCPIP"
      to: "forward.go::validateForwardTarget"
      via: "安全校验拦截"
      pattern: "validateForwardTarget.*Reject"
    - from: "forward.go::handleDirectTCPIP"
      to: "容器 sshd"
      via: "targetClient.OpenChannel(\"direct-tcpip\", ...)"
      pattern: "OpenChannel.*direct-tcpip"
---

<objective>
在 SSH Proxy 中实现 `direct-tcpip` channel 转发支持（SSH-01），并加入 forwarding 目标安全校验（SSH-04）。修改 `handleConnection` 按 channel 类型分发，新增 `forward.go` 承载 `handleDirectTCPIP` 和 `validateForwardTarget`，同时保持现有 session 代理行为不变。

Purpose: 这是 VS Code Remote-SSH 端口转发的核心路径——VS Code 通过 `direct-tcpip` 将本地端口转发到容器内远程端口，使语言服务器、调试器等功能正常工作。
Output: forward.go + forward_test.go + proxy.go 分发逻辑修改，direct-tcpip 转发 + 安全校验全部就位
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/038-ssh-proxy-port-forwarding/38-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- 关键现有接口，executor 直接使用 -->

From internal/sshproxy/proxy.go (当前 handleConnection 核心逻辑):
```go
func (s *Server) handleConnection(netConn net.Conn, config *ssh.ServerConfig) {
    // ... ssh.NewServerConn ...
    go ssh.DiscardRequests(globalReqs)
    for newChan := range chans {
        if newChan.ChannelType() != "session" {
            newChan.Reject(ssh.UnknownChannelType, "only session channels supported")
            continue
        }
        go s.handleChannel(newChan, targetAddr, targetUser, targetPassword, targetPrivateKey)
    }
}
```

From internal/sshproxy/proxy.go (Server 结构体):
```go
type Server struct {
    addr              string
    logger            *slog.Logger
    resolver          ContainerResolver
    hostKey           ssh.Signer
    containerUser     string
    containerPassword string
}
```

From internal/sshproxy/resolver.go (ContainerTarget):
```go
type ContainerTarget struct {
    Addr       string
    User       string
    Password   string
    PrivateKey string
}
```

Payload 结构（与 golang.org/x/crypto/ssh 内部一致，字段顺序必须匹配 wire format）：
```go
type channelOpenDirectMsg struct {
    raddr string
    rport uint32
    laddr string
    lport uint32
}
```

双向拷贝标准模式（现有 session 代理已使用）：
```go
var wg sync.WaitGroup
wg.Add(2)
go func() { defer wg.Done(); io.Copy(dst, src); dst.CloseWrite() }()
go func() { defer wg.Done(); io.Copy(src, dst); src.CloseWrite() }()
wg.Wait()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 forward.go — direct-tcpip 处理 + 安全校验</name>
  <files>internal/sshproxy/forward.go</files>
  <behavior>
    - Test 1: 解析合法的 direct-tcpip payload → raddr/rport/laddr/lport 全部正确
    - Test 2: 解析非法 payload（长度不足）→ 返回 error
    - Test 3: 安全校验 — 10.99.1.1:80 → forbidden=true
    - Test 4: 安全校验 — 127.0.0.1:8080 → forbidden=false
    - Test 5: 安全校验 — metadata.google.internal:80 → forbidden=true
    - Test 6: 安全校验 — 169.254.169.254:2375 → forbidden=true（Docker socket 端口）
    - Test 7: 安全校验 — 8.8.8.8:53 → forbidden=false
  </behavior>
  <action>
新建 `internal/sshproxy/forward.go`，实现：

**Step 1 — Payload 解析结构体（字段顺序 = wire format 顺序）：**

```go
type channelOpenDirectMsg struct {
    raddr string
    rport uint32
    laddr string
    lport uint32
}
```

**Step 2 — 安全校验函数：**

```go
var forbiddenCIDRs = []string{"10.99.0.0/16"}
var forbiddenHosts = []string{
    "metadata.google.internal",
    "169.254.169.254",
}
var forbiddenPorts = map[int]bool{2375: true, 2376: true}

func isForbiddenTarget(host string, port int) bool {
    if forbiddenPorts[port] {
        return true
    }
    for _, h := range forbiddenHosts {
        if strings.EqualFold(host, h) {
            return true
        }
    }
    ip := net.ParseIP(host)
    if ip != nil {
        for _, cidr := range forbiddenCIDRs {
            _, ipNet, _ := net.ParseCIDR(cidr)
            if ipNet.Contains(ip) {
                return true
            }
        }
    }
    return false
}
```

**Step 3 — handleDirectTCPIP（核心代理逻辑）：**

```go
func (s *Server) handleDirectTCPIP(newChan ssh.NewChannel, targetAddr, targetUser, targetPassword, targetPrivateKey string) {
    var msg channelOpenDirectMsg
    if err := ssh.Unmarshal(newChan.ExtraData(), &msg); err != nil {
        s.logger.Warn("direct-tcpip payload unmarshal failed", "error", err)
        newChan.Reject(ssh.ConnectionFailed, "invalid direct-tcpip payload")
        return
    }

    // SSH-04: 安全校验
    if isForbiddenTarget(msg.raddr, int(msg.rport)) {
        s.logger.Warn("forwarding to forbidden target rejected", "raddr", msg.raddr, "rport", msg.rport)
        newChan.Reject(ssh.Prohibited, "forwarding to this target is not allowed")
        return
    }

    clientChan, clientReqs, err := newChan.Accept()
    if err != nil {
        s.logger.Error("accept direct-tcpip channel failed", "error", err)
        return
    }
    defer clientChan.Close()
    go ssh.DiscardRequests(clientReqs)

    // 建立到容器侧的 SSH 连接（复用现有 dialContainer 模式）
    targetClient, err := s.dialContainer(targetAddr, targetUser, targetPassword, targetPrivateKey)
    if err != nil {
        s.logger.Error("dial container for direct-tcpip failed", "target", targetAddr, "error", err)
        fmt.Fprintf(clientChan.Stderr(), "forwarding failed: %v\r\n", err)
        return
    }
    defer targetClient.Close()

    targetChan, targetReqs, err := targetClient.OpenChannel("direct-tcpip", ssh.Marshal(&msg))
    if err != nil {
        s.logger.Error("open target direct-tcpip failed", "target", targetAddr, "error", err)
        fmt.Fprintf(clientChan.Stderr(), "forwarding failed: %v\r\n", err)
        return
    }
    defer targetChan.Close()
    go ssh.DiscardRequests(targetReqs)

    // 双向并发拷贝
    var wg sync.WaitGroup
    wg.Add(2)
    go func() {
        defer wg.Done()
        io.Copy(targetChan, clientChan)
        targetChan.CloseWrite()
    }()
    go func() {
        defer wg.Done()
        io.Copy(clientChan, targetChan)
        clientChan.CloseWrite()
    }()
    wg.Wait()

    s.logger.Debug("direct-tcpip channel closed", "target", targetAddr, "raddr", msg.raddr, "rport", msg.rport)
}
```

**Step 4 — 提取 dialContainer 公共函数：**

将 proxy.go `handleChannel` 中 dial 容器 SSH 的逻辑提取为 `Server.dialContainer(...)` 方法，供 `handleChannel` 和 `handleDirectTCPIP` 复用。保持现有认证逻辑不变（私钥优先，密码 fallback）。
  </action>
  <verify>
    <automated>go test ./internal/sshproxy/... -run "TestDirectTCPIP|TestForwardTarget" -v</automated>
  </verify>
  <done>forward.go 含 channelOpenDirectMsg + isForbiddenTarget + handleDirectTCPIP + dialContainer，forward_test.go 7 条安全校验测试 PASS</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改 proxy.go — channel 类型分发 + 提取 dialContainer</name>
  <files>internal/sshproxy/proxy.go</files>
  <behavior>
    - Test 1: handleConnection 收到 session channel → 走 handleChannel（现有行为不变）
    - Test 2: handleConnection 收到 direct-tcpip channel → 走 handleDirectTCPIP
    - Test 3: handleConnection 收到未知 channel type → Reject(UnknownChannelType)
    - Test 4: dialContainer 使用私钥认证成功
    - Test 5: dialContainer 私钥失败后密码 fallback 成功
  </behavior>
  <action>
修改 `internal/sshproxy/proxy.go`：

**Step 1 — 提取 dialContainer 公共方法：**

将 `handleChannel` 中以下代码块提取为 `func (s *Server) dialContainer(targetAddr, targetUser, targetPassword, targetPrivateKey string) (*ssh.Client, error)`：

```go
func (s *Server) dialContainer(targetAddr, targetUser, targetPassword, targetPrivateKey string) (*ssh.Client, error) {
    user := targetUser
    if user == "" {
        user = s.containerUser
    }
    pass := targetPassword
    if pass == "" {
        pass = s.containerPassword
    }

    var authMethods []ssh.AuthMethod
    if targetPrivateKey != "" && (strings.Contains(targetPrivateKey, "BEGIN OPENSSH PRIVATE KEY") || strings.Contains(targetPrivateKey, "BEGIN RSA PRIVATE KEY") || strings.Contains(targetPrivateKey, "BEGIN EC PRIVATE KEY") || strings.Contains(targetPrivateKey, "BEGIN DSA PRIVATE KEY")) {
        signer, err := ssh.ParsePrivateKey([]byte(targetPrivateKey))
        if err == nil {
            authMethods = append(authMethods, ssh.PublicKeys(signer))
            s.logger.Debug("SSH proxy using private key auth for container", "addr", targetAddr, "user", user)
        } else {
            s.logger.Warn("SSH proxy failed to parse target private key, falling back to password", "error", err)
        }
    }
    authMethods = append(authMethods, ssh.Password(pass), passwordKeyboardInteractive(pass), ssh.PublicKeys(s.hostKey))

    targetConfig := &ssh.ClientConfig{
        User:            user,
        Auth:            authMethods,
        HostKeyCallback: ssh.InsecureIgnoreHostKey(),
        Timeout:         10 * time.Second,
    }

    return ssh.Dial("tcp", targetAddr, targetConfig)
}
```

**Step 2 — handleChannel 中替换为 dialContainer 调用：**

删除 `handleChannel` 中原有的 dial 逻辑，替换为：
```go
targetClient, err := s.dialContainer(targetAddr, targetUser, targetPassword, targetPrivateKey)
if err != nil {
    s.logger.Error("dial container SSH failed", "addr", targetAddr, "user", user, "error", err)
    fmt.Fprintf(clientChan.Stderr(), "Failed to connect to container: %s\r\n", err.Error())
    return
}
defer targetClient.Close()
```

**Step 3 — handleConnection 中改为 channel 类型分发：**

将 `handleConnection` 中的 channel 循环替换为：

```go
for newChan := range chans {
    switch newChan.ChannelType() {
    case "session":
        go s.handleChannel(newChan, targetAddr, targetUser, targetPassword, targetPrivateKey)
    case "direct-tcpip":
        go s.handleDirectTCPIP(newChan, targetAddr, targetUser, targetPassword, targetPrivateKey)
    default:
        newChan.Reject(ssh.UnknownChannelType, fmt.Sprintf("channel type %s not supported", newChan.ChannelType()))
    }
}
```

**Step 4 — 移除不再需要的局部变量：**

`handleChannel` 中删除 `user`、`pass`、`authMethods`、`targetConfig` 等局部变量（已移至 dialContainer）。
  </action>
  <verify>
    <automated>go test ./internal/sshproxy/... -v</automated>
  </verify>
  <done>proxy.go handleConnection 按 channel 类型分发（session / direct-tcpip），handleChannel 复用 dialContainer，现有测试全部 PASS</done>
</task>

</tasks>

<verification>
```bash
# Wave 1 本 plan 完整验证
go test ./internal/sshproxy/... -v
go build ./...
```
期望：全 PASS，无编译错误，无 `FAIL` 行。
</verification>

<success_criteria>
- proxy.go handleConnection 支持 session 和 direct-tcpip 两种 channel 类型分发
- forward.go 含 handleDirectTCPIP 完整实现（payload 解析 → 安全校验 → 接受 channel → dial 容器 → OpenChannel → 双向拷贝）
- forward.go 含 isForbiddenTarget（管理网段 / Docker socket / metadata 端点黑名单）
- dialContainer 从 handleChannel 提取为 Server 方法，供 session 和 direct-tcpip 复用
- forward_test.go 覆盖 payload 解析、安全校验（至少 7 个用例）
- 现有 proxy_test.go 全部 PASS（无回归）
- go build ./... 干净
</success_criteria>

<output>
完成后创建 `.planning/phases/038-ssh-proxy-port-forwarding/038-01-SUMMARY.md`，记录：
- dialContainer 提取位置和签名
- handleDirectTCPIP 关键步骤
- 安全校验黑名单内容
- 测试覆盖情况
- 任何偏差说明
</output>
