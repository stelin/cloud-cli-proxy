---
phase: 038-ssh-proxy-port-forwarding
plan: "03"
type: execute
wave: 2
depends_on:
  - "038-01"
  - "038-02"
files_modified:
  - deploy/docker/managed-user/sshd_config
  - internal/sshproxy/forward_test.go
  - internal/sshproxy/proxy_test.go
autonomous: true
requirements:
  - SSH-03

must_haves:
  truths:
    - "容器内 sshd_config 显式开启 AllowTcpForwarding yes"
    - "容器内 sshd_config 显式开启 AllowStreamLocalForwarding yes"
    - "容器内 sshd_config 显式设置 GatewayPorts no"
    - "SSH Proxy 对非 session / direct-tcpip / forwarded-tcpip 的 channel 请求返回 UnknownChannelType"
    - "现有 proxy_test.go 全部 PASS"
  artifacts:
    - path: "deploy/docker/managed-user/sshd_config"
      provides: "sshd 端口转发配置（已存在，验证不变）"
      contains: "AllowTcpForwarding yes"
    - path: "internal/sshproxy/proxy_test.go"
      provides: "现有 session 代理测试 + 新增 forwarding 集成测试"
      contains: "TestHandleConnection_DirectTCPIP"
    - path: "internal/sshproxy/forward_test.go"
      provides: "forwarding 单元测试覆盖"
      contains: "TestForwardTarget_Valid"
  key_links:
    - from: "deploy/docker/managed-user/sshd_config"
      to: "容器 sshd"
      via: "sshd 启动时读取配置"
      pattern: "AllowTcpForwarding.*yes"
---

<objective>
验证 SSH-03 配置已就绪（sshd_config 已含端口转发配置），补充 forwarding 集成测试确保 proxy 的 channel 分发和拒绝逻辑正确，并验证现有测试无回归。

Purpose: SSH-03 的配置在镜像中已存在（38-RESEARCH.md 确认），本 plan 聚焦验证其正确性 + 补充测试覆盖，为 Phase 38 收尾。
Output: 验证 sshd_config 配置正确，新增/更新测试覆盖 forwarding 场景，所有测试 PASS
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/038-ssh-proxy-port-forwarding/38-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- 关键现有接口，executor 直接使用 -->

From deploy/docker/managed-user/sshd_config (当前配置):
```
Port 22
Protocol 2
AddressFamily any
ListenAddress 0.0.0.0
PermitRootLogin no
PasswordAuthentication yes
PermitEmptyPasswords no
ChallengeResponseAuthentication no
UsePAM no
X11Forwarding no
PrintMotd no
AuthorizedKeysFile .ssh/authorized_keys
PidFile /var/run/sshd.pid
ClientAliveInterval 15
ClientAliveCountMax 8
MaxSessions 30
MaxStartups 60:30:120
AllowTcpForwarding yes
AllowStreamLocalForwarding yes
GatewayPorts no
Subsystem sftp /usr/lib/openssh/sftp-server
```

From internal/sshproxy/proxy_test.go (现有测试模式):
```go
// startTestTargetSSH 启动一个接受 password 认证和 session channel 的测试 SSH server
// handleTargetConn 处理单个连接，拒绝非 session channel
```

现有 proxy.go handleConnection 分发逻辑（Plan 02 修改后）:
```go
for newChan := range chans {
    switch newChan.ChannelType() {
    case "session":
        go s.handleChannel(newChan, targetClient, targetAddr)
    case "direct-tcpip":
        go s.handleDirectTCPIP(newChan, targetClient, targetAddr)
    default:
        newChan.Reject(ssh.UnknownChannelType, fmt.Sprintf("channel type %s not supported", newChan.ChannelType()))
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 验证 sshd_config 并补充 forwarding 集成测试</name>
  <files>deploy/docker/managed-user/sshd_config, internal/sshproxy/proxy_test.go, internal/sshproxy/forward_test.go</files>
  <behavior>
    - Test 1: sshd_config 包含 AllowTcpForwarding yes
    - Test 2: sshd_config 包含 AllowStreamLocalForwarding yes
    - Test 3: sshd_config 包含 GatewayPorts no
    - Test 4: handleConnection 收到未知 channel type → Reject(UnknownChannelType)
    - Test 5: handleConnection 收到 direct-tcpip channel → 不 Reject（分发到 handleDirectTCPIP）
    - Test 6: 测试 target SSH server 支持 direct-tcpip（模拟容器 sshd）
  </behavior>
  <action>
**Step 1 — 验证 sshd_config 配置：**

确认 `deploy/docker/managed-user/sshd_config` 已包含以下三行（38-RESEARCH.md 已确认，本步为验证）：
```
AllowTcpForwarding yes
AllowStreamLocalForwarding yes
GatewayPorts no
```

若任一配置缺失或值为 no/客户端，记录偏差并修正。

**Step 2 — 扩展 proxy_test.go 的测试 target SSH server：**

修改 `handleTargetConn` 使其支持 `direct-tcpip` channel（模拟容器 sshd 的行为）：

```go
func handleTargetConn(conn net.Conn, config *ssh.ServerConfig) {
    sshConn, chans, reqs, err := ssh.NewServerConn(conn, config)
    if err != nil {
        return
    }
    defer sshConn.Close()
    go ssh.DiscardRequests(reqs)

    for newChan := range chans {
        switch newChan.ChannelType() {
        case "session":
            ch, reqs, err := newChan.Accept()
            if err != nil {
                return
            }
            go ssh.DiscardRequests(reqs)
            ch.SendRequest("exit-status", false, ssh.Marshal(&struct{ ExitStatus uint32 }{0}))
            ch.Close()
        case "direct-tcpip":
            var msg channelOpenDirectMsg
            if err := ssh.Unmarshal(newChan.ExtraData(), &msg); err != nil {
                newChan.Reject(ssh.ConnectionFailed, "invalid payload")
                continue
            }
            ch, reqs, err := newChan.Accept()
            if err != nil {
                continue
            }
            go ssh.DiscardRequests(reqs)
            // 模拟连接成功：直接关闭（真实场景会 dial 目标）
            ch.SendRequest("exit-status", false, ssh.Marshal(&struct{ ExitStatus uint32 }{0}))
            ch.Close()
        default:
            newChan.Reject(ssh.UnknownChannelType, "only session and direct-tcpip supported")
        }
    }
}
```

注意：需要在 proxy_test.go 中添加 `channelOpenDirectMsg` 结构体定义（或从 forward.go 导入，但测试文件在同一包可直接使用）。

**Step 3 — 新增 proxy_test.go 集成测试：**

```go
func TestHandleConnection_DirectTCPIPChannel(t *testing.T) {
    targetPassword := "targetpass"
    targetAddr, cleanup := startTestTargetSSH(t, targetPassword)
    defer cleanup()

    resolver := &stubResolverRepo{
        hostAuth: repository.HostSSHAuth{
            EntryPassword: targetPassword,
            ContainerUser: "workspace",
        },
        targetAddr: targetAddr,
    }
    logger := slog.New(slog.DiscardHandler)
    s, err := NewServer(":0", "workspace", "targetpass", "", resolver, logger)
    if err != nil {
        t.Fatal(err)
    }

    config := buildServerConfig(s, resolver)
    proxyAddr, done := startServerAndConnect(t, s, config)

    clientConfig := &ssh.ClientConfig{
        User:            "alice",
        Auth:            []ssh.AuthMethod{ssh.Password("proxy-entry-pass")},
        HostKeyCallback: ssh.InsecureIgnoreHostKey(),
        Timeout:         10 * time.Second,
    }

    cc, clientErr := ssh.Dial("tcp", proxyAddr, clientConfig)
    if clientErr != nil {
        t.Fatalf("client connection failed: %v", clientErr)
    }
    defer cc.Close()

    // Open a direct-tcpip channel
    msg := channelOpenDirectMsg{
        raddr: "127.0.0.1",
        rport: 8080,
        laddr: "127.0.0.1",
        lport: 12345,
    }
    ch, _, openErr := cc.OpenChannel("direct-tcpip", ssh.Marshal(&msg))
    if openErr != nil {
        t.Fatalf("open direct-tcpip channel failed: %v", openErr)
    }
    ch.Close()

    cc.Close()
    <-done
}

func TestHandleConnection_UnknownChannelType(t *testing.T) {
    targetPassword := "targetpass"
    targetAddr, cleanup := startTestTargetSSH(t, targetPassword)
    defer cleanup()

    resolver := &stubResolverRepo{
        hostAuth: repository.HostSSHAuth{
            EntryPassword: targetPassword,
            ContainerUser: "workspace",
        },
        targetAddr: targetAddr,
    }
    logger := slog.New(slog.DiscardHandler)
    s, err := NewServer(":0", "workspace", "targetpass", "", resolver, logger)
    if err != nil {
        t.Fatal(err)
    }

    config := buildServerConfig(s, resolver)
    proxyAddr, done := startServerAndConnect(t, s, config)

    clientConfig := &ssh.ClientConfig{
        User:            "alice",
        Auth:            []ssh.AuthMethod{ssh.Password("proxy-entry-pass")},
        HostKeyCallback: ssh.InsecureIgnoreHostKey(),
        Timeout:         10 * time.Second,
    }

    cc, clientErr := ssh.Dial("tcp", proxyAddr, clientConfig)
    if clientErr != nil {
        t.Fatalf("client connection failed: %v", clientErr)
    }
    defer cc.Close()

    // Try to open an unsupported channel type
    ch, _, openErr := cc.OpenChannel("x11", nil)
    if openErr == nil {
        ch.Close()
        t.Fatal("expected x11 channel to be rejected")
    }
    if !strings.Contains(openErr.Error(), "Unknown channel type") {
        t.Fatalf("expected UnknownChannelType, got: %v", openErr)
    }

    cc.Close()
    <-done
}
```

**Step 4 — 确保 proxy_test.go 导入 strings 包：**

检查并添加 `"strings"` 到 proxy_test.go 的 import 段。
  </action>
  <verify>
    <automated>go test ./internal/sshproxy/... -v</automated>
  </verify>
  <done>sshd_config 三行配置验证通过，proxy_test.go 新增 direct-tcpip 和未知 channel type 集成测试 PASS，现有测试无回归</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补充 forward_test.go 完整测试覆盖</name>
  <files>internal/sshproxy/forward_test.go</files>
  <behavior>
    - Test 1: channelOpenDirectMsg 编解码往返 → 字段值不变
    - Test 2: isForbiddenTarget — 10.99.1.1 → true
    - Test 3: isForbiddenTarget — 127.0.0.1 → false
    - Test 4: isForbiddenTarget — metadata.google.internal → true
    - Test 5: isForbiddenTarget — 169.254.169.254 → true
    - Test 6: isForbiddenTarget — 8.8.8.8 → false
    - Test 7: isForbiddenTarget — 任意 IP + 端口 2375 → true
    - Test 8: handleGlobalRequests — tcpip-forward 透传 → SendRequest 被调用
    - Test 9: handleGlobalRequests — 未知请求 → Reply(false)
  </behavior>
  <action>
新建/扩展 `internal/sshproxy/forward_test.go`，覆盖 forwarding 核心逻辑：

**Step 1 — Payload 编解码测试：**

```go
func TestChannelOpenDirectMsg_MarshalUnmarshal(t *testing.T) {
    original := channelOpenDirectMsg{
        raddr: "192.168.1.1",
        rport: 8080,
        laddr: "10.0.0.1",
        lport: 54321,
    }
    data := ssh.Marshal(&original)
    var decoded channelOpenDirectMsg
    if err := ssh.Unmarshal(data, &decoded); err != nil {
        t.Fatalf("unmarshal failed: %v", err)
    }
    if decoded.raddr != original.raddr || decoded.rport != original.rport ||
        decoded.laddr != original.laddr || decoded.lport != original.lport {
        t.Fatalf("decoded mismatch: got %+v, want %+v", decoded, original)
    }
}
```

**Step 2 — 安全校验测试：**

```go
func TestIsForbiddenTarget_ManagementCIDR(t *testing.T) {
    if !isForbiddenTarget("10.99.1.1", 80) {
        t.Fatal("10.99.1.1 should be forbidden")
    }
}

func TestIsForbiddenTarget_Localhost(t *testing.T) {
    if isForbiddenTarget("127.0.0.1", 8080) {
        t.Fatal("127.0.0.1:8080 should not be forbidden")
    }
}

func TestIsForbiddenTarget_MetadataEndpoint(t *testing.T) {
    if !isForbiddenTarget("metadata.google.internal", 80) {
        t.Fatal("metadata.google.internal should be forbidden")
    }
}

func TestIsForbiddenTarget_169_254(t *testing.T) {
    if !isForbiddenTarget("169.254.169.254", 80) {
        t.Fatal("169.254.169.254 should be forbidden")
    }
}

func TestIsForbiddenTarget_PublicIP(t *testing.T) {
    if isForbiddenTarget("8.8.8.8", 53) {
        t.Fatal("8.8.8.8:53 should not be forbidden")
    }
}

func TestIsForbiddenTarget_DockerSocketPort(t *testing.T) {
    if !isForbiddenTarget("1.2.3.4", 2375) {
        t.Fatal("port 2375 should be forbidden")
    }
    if !isForbiddenTarget("1.2.3.4", 2376) {
        t.Fatal("port 2376 should be forbidden")
    }
}
```

**Step 3 — 全局请求处理测试：**

```go
func TestHandleGlobalRequests_TCPIPForward(t *testing.T) {
    // 使用 mock target client 验证 SendRequest 被调用
    // 由于 ssh.Client 难以 mock，使用集成方式：
    // 启动一个测试 SSH server 作为 target，验证全局请求被透传
}
```

注：全局请求的完整集成测试需要较复杂的 mock 或双 SSH server 设置。如果单元测试成本过高，可在 proxy_test.go 中用集成测试覆盖，forward_test.go 聚焦纯函数测试（payload 编解码 + 安全校验）。
  </action>
  <verify>
    <automated>go test ./internal/sshproxy/... -v</automated>
  </verify>
  <done>forward_test.go 覆盖 payload 编解码 + 安全校验（6 个用例），全部 PASS</done>
</task>

</tasks>

<verification>
```bash
# Wave 2 本 plan 完整验证
go test ./internal/sshproxy/... -v
go build ./...
```
期望：全 PASS，无编译错误，无 `FAIL` 行。
</verification>

<success_criteria>
- sshd_config 确认包含 AllowTcpForwarding yes / AllowStreamLocalForwarding yes / GatewayPorts no
- proxy_test.go 新增 TestHandleConnection_DirectTCPIPChannel（direct-tcpip 集成测试 PASS）
- proxy_test.go 新增 TestHandleConnection_UnknownChannelType（未知 channel 拒绝测试 PASS）
- forward_test.go 覆盖 channelOpenDirectMsg 编解码 + isForbiddenTarget（6 个用例 PASS）
- 现有 proxy_test.go 全部 PASS（无回归）
- go build ./... 干净
</success_criteria>

<output>
完成后创建 `.planning/phases/038-ssh-proxy-port-forwarding/038-03-SUMMARY.md`，记录：
- sshd_config 验证结果
- 新增测试列表和通过状态
- 任何偏差说明
</output>
