---
phase: 42-phase39-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-dev-containers/39-VERIFICATION.md
autonomous: false
requirements:
  - LOCAL-01
  - LOCAL-02
  - LOCAL-03
  - LOCAL-04
  - UX-02

must_haves:
  truths:
    - "cloud-claude local up 一键启动容器并输出 SSH 连接信息，功能正确"
    - "cloud-claude local down 停止并清理本地容器"
    - "cloud-claude local status 显示容器运行状态和端口映射"
    - "entrypoint MODE=local 跳过 KasmVNC 和桌面栈，仍启动 sshd"
    - "entrypoint MODE=local 在有 egress 配置时启动 sing-box（tun 或 proxy 模式）"
    - ".devcontainer/devcontainer.json 包含 MODE=local、forwardPorts、SYS_ADMIN 配置"
    - "--egress-config 注入 sing-box outbound JSON，tun 和 proxy 模式均正确检测"
  artifacts:
    - path: ".planning/phases/39-dev-containers/39-VERIFICATION.md"
      provides: "Phase 39 正式验证报告，5 个需求全部 SATISFIED"
    - path: "internal/local/local.go"
      provides: "LocalManager Up/Down/Status + buildCreateArgs"
      exports: ["LocalManager", "Up", "Down", "Status", "SSHInfo", "ContainerStatus"]
    - path: "internal/local/egress.go"
      provides: "DetectEgressMode + ValidateEgressConfig"
      exports: ["EgressMode", "DetectEgressMode", "ValidateEgressConfig"]
    - path: "internal/local/container.go"
      provides: "DockerRunner 接口 + container 操作封装"
      exports: ["DockerRunner", "containerExists", "inspectSSHPort", "inspectContainerStatus"]
    - path: "internal/local/password.go"
      provides: "crypto/rand 密码生成"
      exports: ["GeneratePassword"]
    - path: "cmd/cloud-claude/local.go"
      provides: "cobra local 子命令组（up/down/status）"
      exports: ["newLocalCmd", "runLocalUp", "runLocalDown", "runLocalStatus"]
    - path: ".devcontainer/devcontainer.json"
      provides: "VS Code Dev Container 配置（MODE=local）"
    - path: "deploy/docker/managed-user/entrypoint.sh"
      provides: "MODE=local 分支 + sing-box 启动逻辑"
  key_links:
    - from: "cmd/cloud-claude/local.go::runLocalUp"
      to: "internal/local/local.go::Up"
      via: "LocalManager.Up() 调用"
      pattern: "mgr\\.Up\\("
    - from: "cmd/cloud-claude/local.go::runLocalDown"
      to: "internal/local/local.go::Down"
      via: "LocalManager.Down() 调用"
      pattern: "mgr\\.Down\\("
    - from: "cmd/cloud-claude/local.go::runLocalStatus"
      to: "internal/local/local.go::Status"
      via: "LocalManager.Status() 调用"
      pattern: "mgr\\.Status\\("
    - from: "internal/local/local.go::Up"
      to: "internal/local/egress.go::ValidateEgressConfig"
      via: "egress config 验证和注入"
      pattern: "ValidateEgressConfig"
    - from: "internal/local/local.go::Up"
      to: "internal/local/container.go::DockerRunner"
      via: "Docker 容器操作"
      pattern: "m\\.runner\\("
    - from: "deploy/docker/managed-user/entrypoint.sh::MODE=local"
      to: "deploy/docker/managed-user/entrypoint.sh::sing-box 启动段"
      via: "MODE 检测后跳过桌面栈，进入 sing-box 启动"
      pattern: "SING_BOX_MODE"
---

<objective>
为 Phase 39（本地 Dev Containers）的 5 个需求（LOCAL-01、LOCAL-02、LOCAL-03、LOCAL-04、UX-02）补充正式 VERIFICATION.md，消除 v3.4 里程碑审计中的 "unverified phase" blocker。

Purpose: 所有 Phase 39 代码已实现并通过 30 个单元测试，但缺少正式验证文档。本计划运行所有可自动化的验证命令，交叉引用代码证据，生成符合项目标准格式的 39-VERIFICATION.md，然后通过人工 Docker 集成测试确认端到端行为正确。

Output: `.planning/phases/39-dev-containers/39-VERIFICATION.md`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-phase39-verification/42-RESEARCH.md
@.planning/phases/39-dev-containers/39-01-SUMMARY.md
@.planning/phases/39-dev-containers/39-02-SUMMARY.md
@.planning/phases/39-dev-containers/39-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Phase 39 codebase. -->

From internal/local/local.go:
```go
type LocalOptions struct {
    ProjectDir  string
    Image       string
    EgressConfig string  // 可选：sing-box outbound JSON 文件路径
    Port        int     // 可选：SSH 端口映射
}

type SSHInfo struct {
    Host     string
    Port     string
    User     string
    Password string
}

type ContainerStatus struct {
    Name   string
    Status string
    Ports  string
}

type LocalManager struct { ... }

func NewLocalManager(opts LocalOptions) *LocalManager
func NewLocalManagerWithRunner(opts LocalOptions, runner DockerRunner) *LocalManager
func (m *LocalManager) Up(ctx context.Context) (SSHInfo, error)
func (m *LocalManager) Down(ctx context.Context) error
func (m *LocalManager) Status(ctx context.Context) (*ContainerStatus, error)
func (m *LocalManager) buildCreateArgs(containerName, projectDir, password string) []string
```

From internal/local/container.go:
```go
type DockerRunner func(ctx context.Context, args ...string) ([]byte, error)
func DefaultDockerRunner(ctx context.Context, args ...string) ([]byte, error)
func containerExists(ctx context.Context, runner DockerRunner, name string) (bool, error)
func inspectSSHPort(ctx context.Context, runner DockerRunner, containerName string) (string, error)
func inspectContainerStatus(ctx context.Context, runner DockerRunner, name string) (string, error)
```

From internal/local/egress.go:
```go
type EgressMode string

const (
    EgressModeTun   EgressMode = "tun"
    EgressModeProxy EgressMode = "proxy"
)

func DetectEgressMode(outboundJSON []byte) (EgressMode, error)
func ValidateEgressConfig(filePath string) ([]byte, EgressMode, error)
func egressMountArg(filePath string) string
```

From cmd/cloud-claude/local.go:
```go
func newLocalCmd() *cobra.Command
func runLocalUp(cmd *cobra.Command, args []string) error
func runLocalDown(cmd *cobra.Command, args []string) error
func runLocalStatus(cmd *cobra.Command, args []string) error
```

From internal/local/password.go:
```go
func GeneratePassword(length int) (string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行自动化验证并生成 39-VERIFICATION.md</name>
  <files>.planning/phases/39-dev-containers/39-VERIFICATION.md</files>
  <action>
按以下步骤收集证据并生成验证文档：

**Step 1 — 运行所有自动化验证命令，记录输出：**

```bash
# 1. 单元测试（30 个测试全部通过）
go test ./internal/local/... -v -count=1

# 2. 构建检查
go build ./cmd/cloud-claude/

# 3. CLI help 验证（4 个子命令均存在）
go run ./cmd/cloud-claude/ local --help
go run ./cmd/cloud-claude/ local up --help
go run ./cmd/cloud-claude/ local down --help
go run ./cmd/cloud-claude/ local status --help

# 4. entrypoint.sh 语法检查
bash -n deploy/docker/managed-user/entrypoint.sh

# 5. devcontainer.json JSON 验证
cat .devcontainer/devcontainer.json | python3 -m json.tool > /dev/null

# 6. 反模式扫描（TODO/FIXME/HACK/PLACEHOLDER）
grep -rn "TODO\|FIXME\|HACK\|PLACEHOLDER" internal/local/ cmd/cloud-claude/local.go deploy/docker/managed-user/entrypoint.sh .devcontainer/devcontainer.json || true

# 7. stub 检查
grep -rn "return null\|return {}" internal/local/ || true
```

**Step 2 — 读取以下源文件，提取行级证据：**

- `internal/local/local.go` — Up 方法完整流程（密码生成 -> 容器名 -> buildCreateArgs -> docker create/start -> inspectSSHPort -> 返回 SSHInfo）；Down 方法；Status 方法；buildCreateArgs 方法（egress 注入逻辑）
- `internal/local/container.go` — DockerRunner 接口；containerExists；inspectSSHPort；inspectContainerStatus
- `internal/local/egress.go` — DetectEgressMode（socks/http → proxy，其他 → tun）；ValidateEgressConfig（文件存在性 + JSON 合法性 + 模式检测）；egressMountArg
- `internal/local/password.go` — GeneratePassword（crypto/rand）
- `cmd/cloud-claude/local.go` — newLocalCmd 注册 local 子命令组；runLocalUp 调用 mgr.Up()；runLocalDown 调用 mgr.Down()；runLocalStatus 调用 mgr.Status()
- `.devcontainer/devcontainer.json` — containerEnv MODE=local；forwardPorts [22]；runArgs --env MODE=local --cap-add SYS_ADMIN --device /dev/fuse
- `deploy/docker/managed-user/entrypoint.sh` — 行 217-218 MODE 检测；行 220 `if [ "$MODE" != "local" ]`；行 220-313 KasmVNC + 桌面栈 + v3 stages（全部在 if 块内，local 模式跳过）；行 315-384 sing-box 启动段（MODE=local + 有 outbound JSON 时）；行 323-328 tun 模式检测 + 降级为 proxy

**Step 3 — 生成 `.planning/phases/39-dev-containers/39-VERIFICATION.md`，严格遵循以下格式：**

```yaml
---
phase: 39-dev-containers
verified: {当前 ISO 时间戳}
status: passed
score: {N}/{N} must-haves verified
re_verification: false
---
```

文档必须包含以下章节（参考 38-VERIFICATION.md 格式）：

1. **Phase Goal** — 从 ROADMAP.md Phase 39 提取
2. **Goal Achievement > Observable Truths** — 表格格式，每个 truth 对应一个具体可观察行为，Status 列标注 VERIFIED/CODE-VERIFIED/MANUAL-REQUIRED，Evidence 列引用具体 file:line
3. **Required Artifacts** — 列出所有 Phase 39 产出物，标注行数、函数数、测试状态
4. **Key Link Verification** — 列出上述 6 条 key_links 的 from→to→via→status（WIRED），引用具体代码行
5. **Behavioral Spot-Checks** — 运行上述 7 条自动化命令，记录实际输出
6. **Requirements Coverage** — 5 个需求（LOCAL-01、LOCAL-02、LOCAL-03、LOCAL-04、UX-02）全部标记 SATISFIED，每个需求引用具体代码证据
7. **Anti-Patterns Found** — 反模式扫描结果
8. **Human Verification Required** — 列出以下人工验证场景（Task 2 将执行其中关键部分）：
   - cloud-claude local up 真实启动 Docker 容器并通过 SSH 连接
   - cloud-claude local down 停止并清理容器
   - cloud-claude local status 显示运行状态
   - devcontainer.json 被 VS Code Dev Containers 正确解析（需要 VS Code 环境）
   - egress tun 模式在有真实 sing-box 配置的环境中启动成功
9. **Test Coverage** — 单元测试数量和通过状态
10. **Gaps Summary** — 总结验证覆盖情况

关键注意事项：
- 对于只通过单元测试（mock Docker）验证的 truth，使用 CODE-VERIFIED 状态，标注"单元测试验证代码逻辑，Docker 行为需人工确认"
- 对于 entrypoint.sh 的行为，使用文件行号范围作为证据，不要笼统描述
- 对于 egress tun 模式的降级逻辑（entrypoint.sh:325-328），标注为 RUNTIME-BEHAVIOR，说明这是代码审查确认的运行时行为
- 引用 Phase 40 的集成测试证据时，标注来源为 "Phase 40 手动验证"
  </action>
  <verify>
    <automated>test -f .planning/phases/39-dev-containers/39-VERIFICATION.md && grep -c "SATISFIED" .planning/phases/39-dev-containers/39-VERIFICATION.md | grep -q "5" && echo "VERIFICATION.md exists with 5 SATISFIED requirements" || echo "FAIL"</automated>
  </verify>
  <done>
    .planning/phases/39-dev-containers/39-VERIFICATION.md 存在，包含：
    - 正确的 YAML frontmatter（phase: 39-dev-containers, status: passed）
    - 10+ Observable Truths，每个有 file:line 证据
    - 8 个 Required Artifacts 全部 VERIFIED
    - 6+ Key Links 全部 WIRED
    - 5 个 Requirements（LOCAL-01~04, UX-02）全部 SATISFIED
    - Behavioral Spot-Checks 记录了自动化命令实际输出
    - Human Verification Required 列出了 5 个人工验证场景
    - 无 anti-patterns（TODO/FIXME/HACK/PLACEHOLDER）
    - 全部 30 个单元测试通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Docker 集成验证（人工确认）</name>
  <files>.planning/phases/39-dev-containers/39-VERIFICATION.md</files>
  <action>
Task 1 生成了完整的 39-VERIFICATION.md，覆盖 Phase 39 的 5 个需求，所有代码级证据已验证。本任务需要用户在 Docker 环境下执行以下集成测试，并将结果更新到 VERIFICATION.md 的 Human Verification Required 章节。

**验证 Docker 环境下的集成行为（需要 Docker Desktop 正在运行）：**

1. 运行 `cloud-claude local up`，确认容器启动并输出 SSH 连接信息（host, port, user, password）
2. 运行 `ssh {user}@127.0.0.1 -p {port}` 并用输出的密码登录，确认 SSH 连接成功
3. 运行 `cloud-claude local status`，确认输出显示容器 Running 状态和端口映射
4. 运行 `cloud-claude local down`，确认容器被停止并清理
5. 再次运行 `cloud-claude local status`，确认容器已不存在

**可选（如果有 VS Code + Dev Containers 扩展）：**
6. 在项目根目录用 VS Code 打开，选择 "Reopen in Container"，确认 devcontainer.json 被正确解析并启动容器

根据用户反馈更新 VERIFICATION.md 中的 Human Verification Required 章节状态。
  </action>
  <verify>
    <automated>grep -q "Human Verification Required" .planning/phases/39-dev-containers/39-VERIFICATION.md && echo "Human verification section exists" || echo "FAIL"</automated>
  </verify>
  <done>
    用户确认 Docker 集成测试结果，VERIFICATION.md 中 Human Verification Required 章节已更新实际测试状态
  </done>
</task>

</tasks>

<verification>
以下检查在 Task 1 完成后自动执行：

```bash
# VERIFICATION.md 存在且包含所有 5 个 SATISFIED 需求
test -f .planning/phases/39-dev-containers/39-VERIFICATION.md

# 检查 frontmatter 格式
head -6 .planning/phases/39-dev-containers/39-VERIFICATION.md | grep "status: passed"

# 检查 5 个需求全部 SATISFIED
grep -c "SATISFIED" .planning/phases/39-dev-containers/39-VERIFICATION.md

# 检查无 anti-patterns
grep -c "TODO\|FIXME\|HACK\|PLACEHOLDER" .planning/phases/39-dev-containers/39-VERIFICATION.md || echo "0 anti-patterns"

# 单元测试全部通过
go test ./internal/local/... -count=1

# 构建通过
go build ./cmd/cloud-claude/
```
</verification>

<success_criteria>
1. `.planning/phases/39-dev-containers/39-VERIFICATION.md` 存在，格式符合 38-VERIFICATION.md 标准
2. Observable Truths 部分有 10+ 条，每条有 file:line 级别的代码证据
3. Requirements Coverage 表中 LOCAL-01、LOCAL-02、LOCAL-03、LOCAL-04、UX-02 全部标记 SATISFIED
4. 全部 30 个单元测试通过，`go build` 无错误
5. Key Links 部分列出所有关键调用链并标记 WIRED
6. Human Verification Required 列出了 5 个人工验证场景
7. 人工 Docker 集成测试（Task 2）通过或问题已记录
</success_criteria>

<output>
After completion, create `.planning/phases/42-phase39-verification/42-01-SUMMARY.md`
</output>
