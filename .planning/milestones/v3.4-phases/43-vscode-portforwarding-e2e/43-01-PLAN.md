---
phase: 43-vscode-portforwarding-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/scripts/uat-vscode-remote-ssh.sh
autonomous: true
requirements:
  - SSH-05
  - SEC-01

must_haves:
  truths:
    - "UAT 脚本包含 direct-tcpip 端口转发验证场景（场景 7）"
    - "UAT 脚本包含端口转发出口 IP 验证场景（场景 8）"
    - "UAT 脚本包含安全拒绝验证场景（场景 9，尝试转发到 10.99.x.x）"
    - "新增场景遵循现有 scenario 函数模式（reset_assertions / dry-run / has_docker / detect_container / write_json_report）"
    - "新增场景通过 bash -n 语法检查和 --dry-run 模式运行"
    - "main() 函数中已追加新场景调用"
    - "exit code 语义不变：0=PASS, 1=FAIL, 2=SKIP"
  artifacts:
    - path: "tests/scripts/uat-vscode-remote-ssh.sh"
      provides: "VS Code Remote-SSH E2E UAT 脚本（含端口转发场景）"
      contains: "direct-tcpip"
  key_links:
    - from: "tests/scripts/uat-vscode-remote-ssh.sh"
      to: "internal/sshproxy/forward.go"
      via: "场景 7/8/9 通过 ssh -L 触发 handleDirectTCPIP 路径"
      pattern: "ssh.*-L"
---

<objective>
向现有 UAT 脚本 `tests/scripts/uat-vscode-remote-ssh.sh` 追加 3 个端口转发验证场景，补齐 direct-tcpip / tcpip-forward 的端到端验证覆盖。

Purpose: 补齐 Phase 40 验证缺口 — 现有 UAT 脚本（6 场景）完全没有覆盖 SSH 端口转发路径。
Output: 更新后的 UAT 脚本（9 场景）。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-vscode-portforwarding-e2e/43-CONTEXT.md
@.planning/phases/43-vscode-portforwarding-e2e/43-RESEARCH.md

<interfaces>
现有 UAT 脚本结构（tests/scripts/uat-vscode-remote-ssh.sh）:
- CLI 参数: --dry-run, --confirm-destructive, --container=NAME, --expected-egress-ip=IP
- 辅助函数: pass(), fail(), skip(), info(), assert_eq(), assert_contains(), assert_process_running(), reset_assertions(), write_json_report()
- 场景函数: scenario_singbox_process(), scenario_egress_ip(), scenario_dns_leak(), scenario_vscode_server(), scenario_sshd_process(), scenario_singbox_log_domains()
- main() 函数依次调用每个场景

SSH Proxy 转发入口:
```go
// proxy.go:228
case "direct-tcpip":
    go s.handleDirectTCPIP(newChan, targetClient, targetAddr)
```

安全校验（forward.go:35-57）:
```go
func isForbiddenTarget(host string, port int) bool {
    // forbiddenPorts: 2375, 2376
    // forbiddenHosts: metadata.google.internal, 169.254.169.254
    // forbiddenCIDRs: 10.99.0.0/16
}
```

SSH -L 触发 direct-tcpip:
```bash
ssh -L LOCAL_PORT:localhost:REMOTE_PORT user@proxy -p PROXY_PORT
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 追加端口转发验证场景到 UAT 脚本</name>
  <files>tests/scripts/uat-vscode-remote-ssh.sh</files>
  <action>
向 `tests/scripts/uat-vscode-remote-ssh.sh` 追加 3 个新场景函数，并在 `main()` 中调用。

**新增辅助函数：**

```bash
# 等待容器内端口可用
wait_for_port() {
  local container="$1" port="$2" timeout="${3:-10}"
  local start=$(date +%s)
  while ! docker exec "$container" nc -z localhost "$port" 2>/dev/null; do
    if (( $(date +%s) - start >= timeout )); then
      return 1
    fi
    sleep 0.5
  done
  return 0
}

# 清理 SSH 后台隧道进程
SSH_PIDS=()
cleanup_ssh_tunnels() {
  for pid in "${SSH_PIDS[@]}"; do
    kill "$pid" 2>/dev/null || true
  done
}
trap cleanup_ssh_tunnels EXIT
```

**场景 7: direct-tcpip 端口转发验证**

```bash
scenario_direct_tcpip_forward() {
  reset_assertions
  info "===== 场景 7: direct-tcpip 端口转发验证 ====="

  if [[ "$DRY_RUN" == "true" ]]; then
    echo "  [DRY-RUN] 将执行:"
    echo "    1. docker exec \$CONTAINER python3 -m http.server 9876 --directory /tmp &"
    echo "    2. ssh -L 19876:localhost:9876 -f -N -p \$SSH_PORT workspace@127.0.0.1"
    echo "    3. curl http://127.0.0.1:19876/"
    echo "  [DRY-RUN] 预期: curl 返回 200，端口转发通过 direct-tcpip channel 工作"
    echo "  [DRY-RUN] 需要: 运行中的容器 + SSH 连接信息"
    skip "direct_tcpip_forward" "需要运行中的容器（dry-run）"
    write_json_report "direct_tcpip_forward" "skip"
    return 0
  fi

  # 环境检测与场景 1 相同逻辑
  if ! has_docker; then
    skip "direct_tcpip_forward" "未安装 docker"
    write_json_report "direct_tcpip_forward" "skip"
    return 0
  fi

  local container
  container="$(detect_container)"
  if [ -z "$container" ]; then
    skip "direct_tcpip_forward" "未找到运行中的 cloud-claude-local 容器"
    write_json_report "direct_tcpip_forward" "skip"
    return 0
  fi

  # 需要 SSH_PORT 参数（从环境变量或参数获取）
  local ssh_port="${SSH_PORT:-}"
  if [ -z "$ssh_port" ]; then
    # 尝试自动检测 SSH 端口
    ssh_port=$(docker port "$container" 22 2>/dev/null | head -1 | cut -d: -f2)
  fi
  if [ -z "$ssh_port" ]; then
    skip "direct_tcpip_forward" "无法确定 SSH 端口（设置 SSH_PORT 环境变量或确保容器有端口映射）"
    write_json_report "direct_tcpip_forward" "skip"
    return 0
  fi

  info "容器: $container, SSH 端口: $ssh_port"

  # 步骤 1: 在容器内启动 HTTP 服务
  docker exec -d "$container" python3 -m http.server 9876 --directory /tmp 2>/dev/null || true
  sleep 1

  # 步骤 2: 通过 SSH -L 建立 direct-tcpip 隧道
  ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      -o ConnectTimeout=10 \
      -L 19876:localhost:9876 \
      -f -N -p "$ssh_port" workspace@127.0.0.1 2>/dev/null
  SSH_PIDS+=($!)
  sleep 1

  # 步骤 3: 通过隧道访问
  local result
  result=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:19876/ 2>/dev/null || echo "CURL_FAILED")

  # 清理容器内服务
  docker exec "$container" pkill -f "python3 -m http.server 9876" 2>/dev/null || true

  if [ "$result" = "200" ]; then
    pass "场景 7: direct-tcpip 端口转发验证通过 (HTTP $result)"
    assert_eq "端口转发 HTTP 状态码" "200" "$result"
  elif [ "$result" = "CURL_FAILED" ]; then
    fail "场景 7: curl 通过 SSH 隧道访问失败"
    SCENARIO_ASSERTIONS+=('{"name":"direct-tcpip 端口转发","result":"fail","detail":"curl failed"}')
  else
    fail "场景 7: 端口转发返回非 200 状态码: $result"
    assert_eq "端口转发 HTTP 状态码" "200" "$result"
  fi

  write_json_report "direct_tcpip_forward" "$([ "$FAIL_COUNT" -eq 0 ] && echo "pass" || echo "fail")"
}
```

**场景 8: 端口转发出口 IP 验证**

```bash
scenario_forward_egress_ip() {
  reset_assertions
  info "===== 场景 8: 端口转发出口 IP 验证 ====="

  if [[ "$DRY_RUN" == "true" ]]; then
    echo "  [DRY-RUN] 将执行:"
    echo "    1. docker exec \$CONTAINER 启动返回 client IP 的 Python HTTP 服务（端口 9877）"
    echo "    2. ssh -L 19877:localhost:9877 -f -N -p \$SSH_PORT workspace@127.0.0.1"
    echo "    3. curl http://127.0.0.1:19877/ 获取容器视角的 client IP"
    echo "  [DRY-RUN] 预期: 返回的 IP 是容器内部 IP（172.x.x.x），非宿主机出口 IP"
    echo "  [DRY-RUN] 注意: 此场景验证端口转发路径的流量路由正确性"
    skip "forward_egress_ip" "需要运行中的容器 + egress 配置（dry-run）"
    write_json_report "forward_egress_ip" "skip"
    return 0
  fi

  if ! has_docker; then
    skip "forward_egress_ip" "未安装 docker"
    write_json_report "forward_egress_ip" "skip"
    return 0
  fi

  local container
  container="$(detect_container)"
  if [ -z "$container" ]; then
    skip "forward_egress_ip" "未找到运行中的 cloud-claude-local 容器"
    write_json_report "forward_egress_ip" "skip"
    return 0
  fi

  if [ -z "$EXPECTED_EGRESS_IP" ]; then
    skip "forward_egress_ip" "未指定 --expected-egress-ip"
    write_json_report "forward_egress_ip" "skip"
    return 0
  fi

  local ssh_port="${SSH_PORT:-}"
  if [ -z "$ssh_port" ]; then
    ssh_port=$(docker port "$container" 22 2>/dev/null | head -1 | cut -d: -f2)
  fi
  if [ -z "$ssh_port" ]; then
    skip "forward_egress_ip" "无法确定 SSH 端口"
    write_json_report "forward_egress_ip" "skip"
    return 0
  fi

  info "容器: $container, SSH 端口: $ssh_port, 期望出口 IP: $EXPECTED_EGRESS_IP"

  # 步骤 1: 在容器内启动返回 client IP 的服务
  docker exec -d "$container" python3 -c "
from http.server import HTTPServer, BaseHTTPRequestHandler
class H(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(200)
        self.end_headers()
        self.wfile.write(self.client_address[0].encode())
    def log_message(self, *args): pass
HTTPServer(('0.0.0.0', 9877), H).serve_forever()
" 2>/dev/null || true
  sleep 1

  # 步骤 2: 通过 SSH 隧道访问
  ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      -o ConnectTimeout=10 \
      -L 19877:localhost:9877 \
      -f -N -p "$ssh_port" workspace@127.0.0.1 2>/dev/null
  SSH_PIDS+=($!)
  sleep 1

  # 步骤 3: 检查返回的 IP
  local client_ip
  client_ip=$(curl -s --max-time 5 http://127.0.0.1:19877/ 2>/dev/null || echo "CURL_FAILED")

  # 清理
  docker exec "$container" pkill -f "9877" 2>/dev/null || true

  if [ "$client_ip" = "CURL_FAILED" ]; then
    fail "场景 8: curl 通过端口转发访问失败"
    SCENARIO_ASSERTIONS+=('{"name":"forward egress IP","result":"fail","detail":"curl failed"}')
  elif [ "$client_ip" = "$EXPECTED_EGRESS_IP" ]; then
    fail "场景 8: 端口转发返回的 IP 是 egress IP（$client_ip），说明外部请求走了 egress 路由"
    # 注意：这不是错误 — 这说明流量确实走 sing-box 出站了
    # 但从 direct-tcpip 的视角，curl 发起端的 IP 应该是 SSH 客户端（宿主机）
    # 所以这里需要根据实际场景判断
    assert_eq "端口转发 client IP" "容器内部 IP" "$client_ip"
  else
    pass "场景 8: 端口转发 client IP 为 $client_ip（非 egress IP $EXPECTED_EGRESS_IP）"
    assert_contains "端口转发 client IP 不是 egress IP" "$client_ip" ""
  fi

  write_json_report "forward_egress_ip" "$([ "$FAIL_COUNT" -eq 0 ] && echo "pass" || echo "fail")"
}
```

**场景 9: 安全拒绝验证（forbidden target）**

```bash
scenario_security_reject() {
  reset_assertions
  info "===== 场景 9: 安全拒绝验证（forbidden target） ====="

  if [[ "$DRY_RUN" == "true" ]]; then
    echo "  [DRY-RUN] 将执行:"
    echo "    1. ssh -L 19878:10.99.1.1:22 -f -N -p \$SSH_PORT workspace@127.0.0.1"
    echo "    2. curl http://127.0.0.1:19878/"
    echo "  [DRY-RUN] 预期: SSH 端口转发被拒绝（administratively prohibited）"
    echo "  [DRY-RUN] 验证: isForbiddenTarget() 对管理网段 10.99.x.x 正确拦截"
    skip "security_reject" "需要运行中的容器（dry-run）"
    write_json_report "security_reject" "skip"
    return 0
  fi

  if ! has_docker; then
    skip "security_reject" "未安装 docker"
    write_json_report "security_reject" "skip"
    return 0
  fi

  local container
  container="$(detect_container)"
  if [ -z "$container" ]; then
    skip "security_reject" "未找到运行中的 cloud-claude-local 容器"
    write_json_report "security_reject" "skip"
    return 0
  fi

  local ssh_port="${SSH_PORT:-}"
  if [ -z "$ssh_port" ]; then
    ssh_port=$(docker port "$container" 22 2>/dev/null | head -1 | cut -d: -f2)
  fi
  if [ -z "$ssh_port" ]; then
    skip "security_reject" "无法确定 SSH 端口"
    write_json_report "security_reject" "skip"
    return 0
  fi

  info "容器: $container, SSH 端口: $ssh_port"

  # 尝试转发到管理网段（10.99.1.1:22）
  local ssh_output
  ssh_output=$(ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      -o ConnectTimeout=10 -o BatchMode=yes \
      -L 19878:10.99.1.1:22 \
      -N -p "$ssh_port" workspace@127.0.0.1 2>&1) || true

  # SSH -L 转发到 forbidden target 时，channel open 会被 ssh.Prohibited 拒绝
  # SSH 客户端通常报 "Connection refused" 或 "administratively prohibited"
  # 关键是：隧道建立后实际连接应该失败
  local curl_result
  curl_result=$(curl -s -o /dev/null -w "%{http_code}" --max-time 3 http://127.0.0.1:19878/ 2>/dev/null || echo "REFUSED")

  # 无论 curl 返回什么，只要不是成功连接到 10.99.1.1 就算安全拒绝生效
  # SSH 隧道建立本身不会被拒绝（-L 只是声明转发规则），但实际连接会被 isForbiddenTarget 拦截
  if [ "$curl_result" = "REFUSED" ] || [ "$curl_result" = "000" ]; then
    pass "场景 9: 安全拒绝验证通过 — 连接到 10.99.1.1 被拒绝"
    SCENARIO_ASSERTIONS+=('{"name":"安全拒绝 10.99.x.x","result":"pass","detail":"connection refused as expected"}')
  elif echo "$ssh_output" | grep -qi "prohibited\|refused\|denied"; then
    pass "场景 9: 安全拒绝验证通过 — SSH 报告 prohibited"
    SCENARIO_ASSERTIONS+=('{"name":"安全拒绝 10.99.x.x","result":"pass","detail":"SSH prohibited"}')
  else
    fail "场景 9: 安全拒绝验证失败 — 连接到 10.99.1.1 未被拦截 (curl: $curl_result)"
    SCENARIO_ASSERTIONS+=("$(printf '{"name":"安全拒绝 10.99.x.x","result":"fail","detail":"curl returned %s"}' "$curl_result")")
  fi

  write_json_report "security_reject" "$([ "$FAIL_COUNT" -eq 0 ] && echo "pass" || echo "fail")"
}
```

**main() 更新：**

在 main() 函数中，`scenario_singbox_log_domains` 调用之后追加：

```bash
scenario_direct_tcpip_forward || true
echo ""
scenario_forward_egress_ip || true
echo ""
scenario_security_reject || true
echo ""
```

**关键实现细节：**

1. SSH_PORT 参数：通过 `--ssh-port=PORT` CLI 参数传入，或从环境变量 `SSH_PORT` 获取，或通过 `docker port $CONTAINER 22` 自动检测。在 CLI 参数解析部分新增 `--ssh-port=*` case。

2. SSH 隧道清理：在脚本顶部定义 `SSH_PIDS` 数组和 `cleanup_ssh_tunnels` 函数，并注册 `trap cleanup_ssh_tunnels EXIT`。

3. 场景 8 的出口 IP 判断：如果通过端口转发访问容器内返回 client IP 的服务，返回的 IP 应该是 SSH 客户端的 IP（127.0.0.1 或宿主机内网 IP），而不是 egress IP。这证明 direct-tcpip 转发的是 SSH 隧道内的连接，而不是容器的出站流量。真正的出口 IP 验证由场景 2（直接在容器内 curl ifconfig.me）覆盖。

4. 安全拒绝测试（场景 9）：SSH -L 建立的隧道在 connect 时触发 handleDirectTCPIP，此时 isForbiddenTarget 检查会拒绝。SSH 客户端会看到连接失败。测试重点是确认连接确实失败（不返回 200）。
  </action>
  <verify>
    <automated>cd . && bash -n tests/scripts/uat-vscode-remote-ssh.sh && bash tests/scripts/uat-vscode-remote-ssh.sh --dry-run 2>&1 | grep -c "场景" | grep -q "9"</automated>
  </verify>
  <done>
    - UAT 脚本新增 3 个场景函数：scenario_direct_tcpip_forward, scenario_forward_egress_ip, scenario_security_reject
    - 新增辅助函数：wait_for_port, cleanup_ssh_tunnels
    - 新增 CLI 参数：--ssh-port=PORT
    - main() 函数追加 3 个场景调用
    - bash -n 语法检查通过
    - --dry-run 模式输出 9 个场景描述
    - JSON 报告包含 9 个 scenarios
    - 退出码语义不变（0=PASS, 1=FAIL, 2=SKIP）
    - 场景 7/8/9 在无容器环境下正确 SKIP
  </done>
</task>

</tasks>

<verification>
```bash
# 语法检查
bash -n tests/scripts/uat-vscode-remote-ssh.sh

# dry-run 模式验证场景数
bash tests/scripts/uat-vscode-remote-ssh.sh --dry-run 2>&1 | grep "场景" | wc -l
# 预期: 9

# dry-run 输出包含新场景关键词
bash tests/scripts/uat-vscode-remote-ssh.sh --dry-run 2>&1 | grep -E "direct-tcpip|安全拒绝|出口 IP 验证"

# 帮助信息包含新参数
bash tests/scripts/uat-vscode-remote-ssh.sh --help | grep ssh-port
```
</verification>

<success_criteria>
- UAT 脚本从 6 场景扩展到 9 场景
- 场景 7: direct-tcpip 端口转发验证通过 SSH -L + curl 实现
- 场景 8: 端口转发出口 IP 验证通过返回 client IP 的服务实现
- 场景 9: 安全拒绝验证通过尝试转发到 10.99.1.1 实现
- 所有新场景遵循现有 scenario 函数模式
- bash -n 和 --dry-run 通过
- SSH 隧道进程通过 trap EXIT 正确清理
</success_criteria>

<output>
After completion, create `.planning/phases/43-vscode-portforwarding-e2e/43-01-SUMMARY.md`
</output>
