# Requirements Archive: v3.5 网络白名单与 DNS 拆分解析

**Archived:** 2026-05-13
**Status:** 34/34 requirements satisfied
**Milestone Tag:** v3.5

---

# Requirements: Cloud CLI Proxy v3.5

**Defined:** 2026-05-12
**Shipped:** 2026-05-13
**Core Value:** 给每个用户提供一台开箱即用的 SSH 云主机，并且严格保证其所有出网流量都走受控的指定出口 IP

## v3.5 Requirements

### 网络配置基础（BYPASS-NET）

- [x] **BYPASS-NET-01**: sing-box 配置改造为两段式 —— 静态 config.json（每 host 一份模板渲染）+ 动态 local rule-set 文件（路径形如 `/etc/sing-box/whitelist-cidrs.json` 和 `whitelist-domains.json`），`type:"local" + format:"source"` 由 sing-box 文件 watch 自动 reload，不重启进程。 — completed 2026-05-12 (45-01)

- [x] **BYPASS-NET-02**: sing-box `route.rules` 引入 `ip_is_private:true` 内置规则做本地/链路本地/RFC1918/ULA 兜底直连；并按顺序加 `whitelist-cidrs` / `whitelist-domains` 两个 rule_set 引用，所有未命中规则的流量走 `final: "proxy-out"`。 — completed 2026-05-12 (45-01)

- [x] **BYPASS-NET-03**: sing-box `tun` inbound 启用 `strict_route:true` + `auto_route:true` + `endpoint_independent_nat:true`，禁止未支持网络回退到系统默认路由；`route.default_interface` 显式指向 `eth0`，避免 direct outbound 回环走 tun。 — completed 2026-05-12 (45-01)

- [x] **BYPASS-NET-04**: route.rules 第一条 `action:"sniff"` 配置 `sniffer: ["tls","http","quic","dns"]`，保证 IP literal 连接也能命中基于域名的白名单；`protocol:"dns"` 必须走 `hijack-dns` action（替代旧 `outbound:"dns"`）。 — completed 2026-05-12 (45-01)

### DNS 拆分与防泄漏（BYPASS-DNS）

- [x] **BYPASS-DNS-01**: sing-box 配置启用拆分 DNS —— `dns.servers` 至少包含 `dns-local`（type:local）和 `dns-proxy`（type:https，server:1.1.1.1，detour:proxy-out，domain_resolver:dns-local），且 `dns.final = "dns-proxy"`，`strategy:"ipv4_only"`。 — completed 2026-05-12 (45-01)

- [x] **BYPASS-DNS-02**: 内网域名后缀（`.lan` / `.local` / `.internal`）走 `dns-local` 解析；公网白名单域名（`whitelist-domains` rule_set）走 `dns-proxy` 解析以保护查询隐私（即使流量走 direct 也不让 LAN DNS 留痕）。 — completed 2026-05-12 (45-01)

- [x] **BYPASS-DNS-03**: 容器 `/etc/resolv.conf` 从当前 `nameserver 8.8.8.8` 占位改为只读挂载，唯一 nameserver 指向 sing-box tun IP（172.19.0.1），并配 `options ndots:0 single-request-reopen`；用户和应用无法在容器内修改。 — completed 2026-05-12 (45-02)

- [x] **BYPASS-DNS-04**: 容器 `/etc/nsswitch.conf` 设为 `hosts: files dns`，禁用 mdns/myhostname/wins 等本地发现解析路径，避免 mDNS/LLMNR/NetBIOS 旁路。 — completed 2026-05-12 (45-02)

### 数据模型与预设（BYPASS-DATA）

- [x] **BYPASS-DATA-01**: 新增 migration `0019_host_bypass_rules.sql`，建 5 张表：`host_bypass_presets`、`host_bypass_rules`、`host_bypass_bindings`、`host_bypass_snapshots`、`host_bypass_audit_log`；UUID 主键 + `gen_random_uuid()` + `TIMESTAMPTZ DEFAULT NOW()`，命名与现有 `host_egress_bindings` 风格一致。 — completed 2026-05-12 (45-03)

- [x] **BYPASS-DATA-02**: Repository 层提供 `BypassPreset` / `BypassRule` / `BypassBinding` / `BypassSnapshot` 的 19 个 CRUD 方法（`internal/store/repository/queries_bypass.go`），错误处理沿用项目现有 `NetworkError` 风格 + `ErrSystemBypassPresetImmutable` sentinel。 — completed 2026-05-12 (45-03)

- [x] **BYPASS-DATA-03**: 系统内置预设种子（migration seed）：`loopback`（`127.0.0.0/8` + `169.254.0.0/16`，`is_system=true`，`is_force_on=true`，**不可关闭**）和 `lan`（含 RFC1918 + CGNAT 100.64/10 + ULA fc00::/7，默认关闭）；用户和管理员都不能删除或修改 `is_system` 预设（Go + SQL 双层防御）。 — completed 2026-05-12 (45-03)

- [x] **BYPASS-DATA-04**: 配置快照（`host_bypass_snapshots`）记录每次 apply 的版本号、`config_hash`、渲染后的 `whitelist-cidrs.json` / `whitelist-domains.json` 内容、`applied_status`（pending/applied/failed/rolled_back）和 `created_by`，用于回滚和审计；migration 0020 追加 source 列区分 apply / rollback。 — completed 2026-05-12 (45-03 + 46-02)

### 控制面 API 与护栏（BYPASS-API）

- [x] **BYPASS-API-01**: 新增管理员 API（标准库 `net/http` + Go 1.22 mux + JWT 鉴权）：
  - `GET/POST/PATCH/DELETE /v1/admin/bypass/presets`（系统预设不可写 → 403 BYPASS_PRESET_IMMUTABLE）
  - `GET/POST/PATCH/DELETE /v1/admin/bypass/rules`
  - `POST /v1/admin/bypass/rules/validate`（dry-run 单条规则校验）
  - `GET/POST /v1/admin/hosts/{hostID}/bypass`（list / bind / unbind）
  — completed 2026-05-12 (46-01)

- [x] **BYPASS-API-02**: 提供 preview + apply + rollback + effective 四个核心接口：
  - `POST /v1/admin/hosts/{hostID}/bypass/preview` —— 渲染 rule-set 文件 + nft set diff + 风险报告，不落库
  - `POST /v1/admin/hosts/{hostID}/bypass/apply` —— 写 snapshot 并触发 agent reload（`config_hash` 作为幂等键）
  - `POST /v1/admin/hosts/{hostID}/bypass/rollback` —— 新建 source='rollback' 行回到上一个 `applied` snapshot
  - `GET /v1/admin/hosts/{hostID}/bypass/effective` —— 返回当前生效的完整规则集合
  - `GET /v1/admin/hosts/{hostID}/bypass/audit-log` —— cursor 分页（before=RFC3339 + limit clamp 默认 20 / 最大 200）
  — completed 2026-05-12 (46-02)

- [x] **BYPASS-API-03**: 护栏硬拦截（HTTP 422 错误码 `BYPASS_RULE_TOO_BROAD` / `BYPASS_RULE_CONFLICT_PROXY` / `BYPASS_LIMIT_EXCEEDED`）：
  - 拒绝 `0.0.0.0/0` / `::/0` 全量绕过
  - 拒绝 v4 CIDR < /16 且不属于私有段（防误绕公网大段）
  - 拒绝 `domain_suffix` 长度 < 4 或顶级 TLD（如 `.com`）
  - 拒绝覆盖代理服务器 IP 的规则（自我矛盾）
  - 单 host 有效规则数 > 1000 拒绝
  — completed 2026-05-12 (46-01)

- [x] **BYPASS-API-04**: 危险关键字软拦截 —— `domain_keyword` 长度 < 4 时返回 400 警告，要求请求体携带 `confirm_risky:true` 才允许保存；审计日志记录管理员的二次确认动作（note=`confirm_risky_accepted`）。 — completed 2026-05-12 (46-01)

- [x] **BYPASS-API-05**: 所有写操作（create/update/delete/bind/unbind/apply/rollback）走双轨审计 helper `writeBypassAuditLog`：写入 `host_bypass_audit_log`（actor_id、actor_ip、action、target_kind、target_id、before/after JSON、note，默认保留 90 天）+ `EventRecorder.RecordEvent("bypass.<action>", metadata)` 事件流；任一失败仅 Warn 不阻断主请求。 — completed 2026-05-12 (46-01)

### React 后台 UI（BYPASS-UI）

- [x] **BYPASS-UI-01**: host 详情页（`web/admin/src/routes/_dashboard/hosts/$hostId.tsx`）新增「代理白名单」Tab；UI 风格遵循 shadcn + Tailwind v4 + Radix UI（仿 `binding-manager.tsx` 和 `egress-ip-drawer.tsx`），含 vitest 基础设施（vitest@4 + @testing-library + jsdom）。 — completed 2026-05-12 (46-03)

- [x] **BYPASS-UI-02**: Tab 内提供预设多选卡片（loopback 强制锁定，lan 可勾选），每个卡片悬浮显示包含的规则示例；选中后实时刷新预览。 — completed 2026-05-12 (46-03)

- [x] **BYPASS-UI-03**: 自定义规则列表 CRUD（IP/CIDR/域名/域名后缀/端口五种类型）；高风险规则展示黄色徽章，触发二次确认弹窗（RiskyKeywordConfirm）。 — completed 2026-05-12 (46-03)

- [x] **BYPASS-UI-04**: 「预览生效配置」面板（PreviewSheet），展示当前 v→v+1 的 rule-set diff 和人类可读摘要；支持「查看 sing-box JSON」和「查看 nft set diff」两种视图切换（unified diff 行扫描 + 颜色 token）。 — completed 2026-05-12 (46-04)

- [x] **BYPASS-UI-05**: 应用按钮分阶段反馈进度（ApplyProgressDialog 5 阶段固定中文：生成快照 / 下发到 agent / Reload 配置 / 健康检查 / 完成）—— task.progress_percent 4 档映射（<25 dispatch / <50 reload / <75 health / ≥75 done）；失败时显示具体错误码并标注自动回滚状态；成功后 toast 提示 `白名单变更不影响现有 TCP 连接，新连接才用新规则`；含 RollbackConfirmDialog（host slug 严格输入二次确认）。 — completed 2026-05-12 (46-04)

### Agent 热更新链路（BYPASS-RELOAD）

- [x] **BYPASS-RELOAD-01**: `internal/agentapi/contracts.go` 新增 `ActionReloadHostBypass HostAction = "reload_host_bypass"`；worker dispatch（`internal/runtime/tasks/worker.go`）新增对应 case，读取最新 snapshot 并下发到 host-agent；QueueHostAction 第 5 参用专属 `bypassSnapshotID` 形参（避免 hack 借 requestedBy 传 ID）。 — completed 2026-05-12 (47-01)

- [x] **BYPASS-RELOAD-02**: host-agent 收到 reload 指令后执行：
  1. `nft -f` 事务批量更新容器 netns `@whitelist_v4` set；
  2. 写 rule-set 文件用 `tmpfile + rename` 原子语义，路径 `<GatewayConfigDir>/whitelist-{cidrs,domains}.json`，并 bind mount 到 gateway 容器 `/etc/sing-box/`；
  3. 等 1s 让 sing-box 文件 watch reload；
  4. 健康检查（容器 netns 内 `/dev/tcp/192.168.0.1/53` TCP 半握手验证 sb-tun0 路由生效）。
  — completed 2026-05-12 (47-01)

- [x] **BYPASS-RELOAD-03**: 失败自动回滚 —— reload 健康检查 3 次失败后（间隔 200ms），自动用上一个 `applied` snapshot 重新下发，并把当前 snapshot 标记为 `rolled_back`，触发事件日志告警；rollback 路径不走 admin API 重入，直接在 worker 内复用 `ApplyBypassRuleSet`。 — completed 2026-05-12 (47-01)

- [x] **BYPASS-RELOAD-04**: nft set 内容与 rule-set 文件的归一化（去重 + sort + join("\n")）SHA-256 必须 hash 一致；控制面提供 `GET /v1/admin/hosts/{hostID}/bypass/consistency` 健康检查接口（3s timeout，DeadlineExceeded → 504 BYPASS_CONSISTENCY_TIMEOUT；其他 500 BYPASS_CONSISTENCY_ERROR），定期对账。 — completed 2026-05-12 (47-01)

### fail-closed 加固（BYPASS-NFT）

- [x] **BYPASS-NFT-01**: 扩展 `internal/network/worker_firewall_linux.go` + 新增 `internal/network/bypass_firewall.go::ConfigureBypassFirewall` 的 `output` 链规则集，增加：
  - `oifname "sb-tun0" accept`（白名单流量从 tun 出，统一走 sing-box 路由判断）
  - `meta skuid singbox ip daddr <代理服务器IP> tcp dport 443 accept`（uid 锁定，proxyIP 解析失败降级为「无 uid 锁」不阻断主流程）
  - `oifname "eth0" ip daddr @whitelist_v4 accept`（白名单逃逸通道）
  - 默认末尾 `counter log prefix "sbfw-drop " drop`（计数 + 日志）
  - 创建空 `whitelist_v4` set（type=ipv4_addr, flags=interval），由 Phase 47 Plan 01 动态填充
  — completed 2026-05-12 (47-02)

- [x] **BYPASS-NFT-02**: 阻断 mDNS（5353）/ LLMNR（5355）/ NetBIOS（137）的 UDP 出向流量，无论是否在白名单中；计数器可读。 — completed 2026-05-12 (47-02)

- [x] **BYPASS-NFT-03**: 容器内全局禁用 IPv6 —— 启动参数加 `--sysctl net.ipv6.conf.all.disable_ipv6=1` 和 `--sysctl net.ipv6.conf.default.disable_ipv6=1`；IPv6 family 表 policy=drop 等价 ip6tables drop，不额外调用 ip6tables 命令。 — completed 2026-05-12 (47-02)

- [x] **BYPASS-NFT-04**: sing-box 启动失败 fail-closed —— gateway 容器健康检查不通过时，control-plane 不允许 worker 容器开放 SSH 端口（容器 unhealthy 状态由 `provider.PrepareHost` 中的现有 verify 流程负责）。 — completed 2026-05-12 (47-02)

### E2E 验证与安全不变量（BYPASS-VERIFY）

- [x] **BYPASS-VERIFY-01**: 扩展 `internal/network/verify.go` 的 `VerifyNetworkIntegrity`，新增 3 项检查（沿用旧错误码 ErrLeakNotBlocked / ErrEgressIPMismatch / ErrDNSLeak + Metadata.check 子字段区分）：
  1. 白名单 IP（如 RFC1918 192.168.0.1）`nsenter + curl` 流量从 eth0 出（源 IP = host eth0），非代理出口（`verifyBypassEgressMatchesEth0`）
  2. 非白名单域名（如 `api.example.com`）流量仍走代理出口（源 IP = egress IP）（`verifyNonBypassTraffic`）
  3. `nsenter + dig @8.8.8.8 example.com` 必超时（DNS 不能直连公网 DNS）（`verifyPublicDNSBlocked`）
  `nsenterRunner` 抽包级 var，单测旁路 exec 完全 in-process。
  — completed 2026-05-13 (47-03)

- [x] **BYPASS-VERIFY-02**: 实现 10 条安全不变量的 CI 化（含 fail-closed 场景 —— pkill sing-box 后白名单也必须断）：I1（resolv.conf 唯一指向 sing-box）/ I2（nft policy=drop）/ I3（eth0 出向 = 白名单 ∪ 代理 IP）/ I4（公网 DNS 失败）/ I5（fail-closed）/ I6（IPv6 全禁）/ I7（nft 与 rule-set hash 一致）/ I8（rule-set 文件有效）/ I9（mDNS/LLMNR/NetBIOS=0）/ I10（reload 不断 SSH）。`uat-bypass.yml` 由 fixture 自适应 preflight 守护，`scripts/uat-bypass-fixture-up.sh` 控制面 + bypass host + admin token 三件套已就位（TD-01 fixed commit 58c0a5f），下一次 PR 即触发完整 6 场景 × ubuntu-24.04 × I1–I10 CI 守护。 — completed 2026-05-13 (47-03)

- [x] **BYPASS-VERIFY-03**: 新增 `scripts/uat-bypass.sh` UAT 脚本（仿 `scripts/uat-network-resilience.sh`），覆盖 6 个场景：仅 loopback / 仅 lan / loopback+lan / 自定义 IP 规则 / 自定义域名规则 / fail-closed sing-box 崩溃。`bash -n` PASS。 — completed 2026-05-13 (47-03)

- [x] **BYPASS-VERIFY-04**: `host_bypass_snapshots.applied_status` 和 `host_bypass_audit_log` 在 e2e 测试中端到端可见 —— 测试脚本能拉到 `applied/rolled_back` 两种状态的 snapshot 且 audit log 行数正确。 — completed 2026-05-13 (47-03)

## Future Requirements (v3.5 P1 / v3.6+)

- [ ] **BYPASS-PRESET-CN-DEV**: `cn-dev` 预设（阿里云/腾讯云 metadata、mirrors.aliyun.com、mirrors.tuna、registry.npmmirror.com、goproxy.cn 等）
- [ ] **BYPASS-PRESET-OSS-DEV**: `oss-dev` 预设（github.com、registry.npmjs.org、pypi.org、registry-1.docker.io、proxy.golang.org 等）
- [ ] **BYPASS-PRESET-AI-API**: `ai-api` 预设（api.anthropic.com、api.openai.com 等代理到不了的场景）
- [ ] **BYPASS-RULESET-REMOTE**: 远程 rule-set 拉取 worker（引用 MetaCubeX/meta-rules-dat + 自维护镜像 fallback）
- [ ] **BYPASS-CANARY**: 「先在测试 host 验证」按钮，灰度下发
- [ ] **BYPASS-USER-SELF**: 用户自助配置白名单（区分管理员/用户角色权限）
- [ ] **BYPASS-HIT-STATS**: 命中统计（轮询 sing-box Clash API `/connections`）
- [ ] **BYPASS-DASHBOARD**: 流量 dashboard（bypass / proxy 流量字节占比）

## Out of Scope (v3.5)

- 复合规则编辑器（多字段 AND 组合）—— UI 复杂度高，P1 再评估
- `domain_regex` 高级规则 —— 性能和误用风险，延后
- 多租户白名单（按 tenant_id 隔离）—— v1 单宿主机单租户，未来再加
- FakeIP 模式 —— 主流量 SSH 不会被 sniff，FakeIP 会让 SSH 锁死在假 IP 段
- IPv6 双栈支持 —— v3.5 容器内全禁 IPv6，未来在能确保 ip6tables / IPv6 路由规则对称完整时再开
- 用户层 bypass 自定义 —— v3.5 仅管理员可配置
- 跨 host 批量 apply —— v3.5 单 host 维度 apply，未来再加

## Tech Debt & Follow-ups（v3.5 ship 之后跟踪）

| ID | 描述 | 优先级 | 状态 |
|----|------|--------|------|
| TD-01 | `scripts/uat-bypass-fixture-up.sh` 控制面 + bypass host + admin token 三件套 → uat-bypass.yml 真实跑通 | P1 | **fixed (commit 58c0a5f)** |
| TD-02 | I9 严格化（注入 mDNS/LLMNR/NetBIOS 请求 + 断言 nft counter ≥ N） | P2 | open |
| TD-03 | `detectHostEth0IPFallback` 真实化（EgressConfig.LANBypassProbeIP 扩展字段） | P2 | open |
| TD-04 | I3 严格化（切到 nft counter 持续观测窗口） | P2 | open |
| TD-05 | `verify.go` Linux runner 真实容器 + nsenter 集成测试 | P2 | open |
| IN-* | Phase 45 + 46 REVIEW.md 共 10 个 Info 级 finding（注释 wording / TLD 黑名单 / handler 模板 helper 等） | INFO | open |

## Traceability

| REQ-ID            | Phase    | Plans       | Status     |
|-------------------|----------|-------------|------------|
| BYPASS-NET-01     | Phase 45 | 45-01       | Complete   |
| BYPASS-NET-02     | Phase 45 | 45-01       | Complete   |
| BYPASS-NET-03     | Phase 45 | 45-01       | Complete   |
| BYPASS-NET-04     | Phase 45 | 45-01       | Complete   |
| BYPASS-DNS-01     | Phase 45 | 45-01       | Complete   |
| BYPASS-DNS-02     | Phase 45 | 45-01       | Complete   |
| BYPASS-DNS-03     | Phase 45 | 45-02       | Complete   |
| BYPASS-DNS-04     | Phase 45 | 45-02       | Complete   |
| BYPASS-DATA-01    | Phase 45 | 45-03       | Complete   |
| BYPASS-DATA-02    | Phase 45 | 45-03       | Complete   |
| BYPASS-DATA-03    | Phase 45 | 45-03       | Complete   |
| BYPASS-DATA-04    | Phase 45 | 45-03 + 46-02 | Complete |
| BYPASS-API-01     | Phase 46 | 46-01       | Complete   |
| BYPASS-API-02     | Phase 46 | 46-02       | Complete   |
| BYPASS-API-03     | Phase 46 | 46-01       | Complete   |
| BYPASS-API-04     | Phase 46 | 46-01       | Complete   |
| BYPASS-API-05     | Phase 46 | 46-01       | Complete   |
| BYPASS-UI-01      | Phase 46 | 46-03       | Complete   |
| BYPASS-UI-02      | Phase 46 | 46-03       | Complete   |
| BYPASS-UI-03      | Phase 46 | 46-03       | Complete   |
| BYPASS-UI-04      | Phase 46 | 46-04       | Complete   |
| BYPASS-UI-05      | Phase 46 | 46-04       | Complete   |
| BYPASS-RELOAD-01  | Phase 47 | 47-01       | Complete   |
| BYPASS-RELOAD-02  | Phase 47 | 47-01       | Complete   |
| BYPASS-RELOAD-03  | Phase 47 | 47-01       | Complete   |
| BYPASS-RELOAD-04  | Phase 47 | 47-01       | Complete   |
| BYPASS-NFT-01     | Phase 47 | 47-02       | Complete   |
| BYPASS-NFT-02     | Phase 47 | 47-02       | Complete   |
| BYPASS-NFT-03     | Phase 47 | 47-02       | Complete   |
| BYPASS-NFT-04     | Phase 47 | 47-02       | Complete   |
| BYPASS-VERIFY-01  | Phase 47 | 47-03       | Complete   |
| BYPASS-VERIFY-02  | Phase 47 | 47-03       | Complete   |
| BYPASS-VERIFY-03  | Phase 47 | 47-03       | Complete   |
| BYPASS-VERIFY-04  | Phase 47 | 47-03       | Complete   |

**Coverage:** 34/34 v3.5 requirements satisfied (100%) — see milestone audit at `milestones/v3.5-MILESTONE-AUDIT.md`.

---

*Archived: 2026-05-13 — milestone v3.5 shipped with 34/34 REQ satisfied, 5 tech-debt items（TD-01 fixed，TD-02..05 P2 follow-up）, 10 Info-level deferred items.*
