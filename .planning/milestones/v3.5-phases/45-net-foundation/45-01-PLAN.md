---
phase: 45-net-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/network/gateway_singbox_config.go
  - internal/network/gateway_singbox_config_test.go
  - internal/network/container_proxy_provider.go
autonomous: true
requirements:
  - BYPASS-NET-01
  - BYPASS-NET-02
  - BYPASS-NET-03
  - BYPASS-NET-04
  - BYPASS-DNS-01
  - BYPASS-DNS-02

must_haves:
  truths:
    - "新建 host 渲染出的 sing-box config.json 含 route.rule_set 数组（whitelist-cidrs / whitelist-domains，type=local，format=source）"
    - "route.rules 第一条是 action=sniff（sniffer 至少含 tls/http/quic/dns），第二条是 protocol=dns + action=hijack-dns"
    - "route.rules 含 ip_cidr=<proxy_ip>/32 → direct、ip_is_private:true → direct、两个 rule_set 引用 → direct，final=proxy-out"
    - "tun inbound 启用 strict_route=true / auto_route=true / endpoint_independent_nat=true，route.default_interface=eth0"
    - "dns.servers 同时含 dns-local（type=local）与 dns-proxy（type=https，server=1.1.1.1，detour=proxy-out，domain_resolver=dns-local），dns.final=dns-proxy，strategy=ipv4_only"
    - "dns.rules 含 .lan/.local/.internal 后缀 → dns-local，以及 whitelist-domains rule_set → dns-proxy"
    - "PrepareHost 在写 config.json 之后会在同一 gatewayConfigDir 下写 whitelist-cidrs.json / whitelist-domains.json 两个空 rule-set placeholder（{\"version\":3,\"rules\":[]}）"
    - "dockerRunGateway 在 gateway 容器启动时把两个 placeholder 文件挂入 /etc/sing-box/ 为 ro"
    - "direct outbound 沿用旧 tag `direct`（不改为 RESEARCH 中的 `direct-out`）以兼容现有 gateway_singbox_config_test.go 断言；术语漂移由 Phase 47 文档统一"
  artifacts:
    - path: "internal/network/gateway_singbox_config.go"
      provides: "扩展后的 buildGatewaySingBoxConfig（含 rule_set / 七条 route.rules / 拆分 DNS / tun 加固）+ ruleSetWhitelistCIDRsPath / ruleSetWhitelistDomainsPath / placeholder 常量"
      contains: "ruleSetWhitelistCIDRsPath"
    - path: "internal/network/gateway_singbox_config_test.go"
      provides: "至少 5 个新增测试函数覆盖 rule_set / strict_route / sniff+hijack / split-dns / route-rules-order"
      contains: "TestBuildGatewaySingBoxConfig_RouteRulesOrder"
    - path: "internal/network/container_proxy_provider.go"
      provides: "PrepareHost 写 config.json 之后写两个 rule-set placeholder + dockerRunGateway 新增两个 -v ro mount"
      contains: "whitelist-cidrs.json"
  key_links:
    - from: "internal/network/container_proxy_provider.go::PrepareHost"
      to: "internal/network/gateway_singbox_config.go::buildGatewaySingBoxConfig"
      via: "调用渲染函数生成 config.json"
      pattern: "buildGatewaySingBoxConfig\\("
    - from: "internal/network/container_proxy_provider.go::dockerRunGateway"
      to: "/etc/sing-box/whitelist-{cidrs,domains}.json"
      via: "-v <gatewayConfigDir>/whitelist-{cidrs,domains}.json:/etc/sing-box/...:ro"
      pattern: "whitelist-cidrs.json:/etc/sing-box"
---

<objective>
Phase 45 Plan 01：把 sing-box 渲染层从「两条 route.rules + 单 DNS」扩展到 v3.5 两段式配置目标 —— 加 route.rule_set 数组、七条 route.rules、拆分 DNS（dns-local + dns-proxy DoH）、tun 三参加固、显式 default_interface=eth0；同时在 PrepareHost 主流程里生成两个空 rule-set placeholder 文件并挂入 gateway 容器，让 sing-box 启动不报错、Phase 47 的动态写入有挂点。

Purpose: 这是 v3.5 整个白名单/DNS 架构的渲染骨架。没有这一步，后续 API（Phase 46）渲染的 rule-set 没有静态引用容器，Agent 热更新（Phase 47）也没有 mount 入口。
Output: 修改后的 `buildGatewaySingBoxConfig` + 扩展测试 + PrepareHost 内 placeholder 写盘与 mount 注入。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-net-foundation/45-CONTEXT.md
@.planning/phases/45-net-foundation/45-RESEARCH.md
@.planning/research/SUMMARY.md
@internal/network/gateway_singbox_config.go
@internal/network/gateway_singbox_config_test.go
@internal/network/container_proxy_provider.go

<interfaces>
<!-- 关键现状（直接引用，executor 无需再次勘察） -->

internal/network/gateway_singbox_config.go:10
  func buildGatewaySingBoxConfig(outboundRaw json.RawMessage, dnsServer, proxyServerIP string) ([]byte, error)
  当前实现：
    - tun inbound: address=["172.19.0.1/30"], auto_route=true, strict_route=FALSE, stack="mixed", sniff=true, sniff_override_destination=true
    - dns: servers=[{tag:"dns-remote", type:"tcp", server:dnsServer, detour:"proxy-out"}], strategy="ipv4_only"
    - outbounds: proxy-out + direct（注意：当前 direct outbound tag 叫 "direct"，没有 bind_interface）
    - route.rules: 仅 [{ip_cidr:[proxy_ip/32], outbound:"direct"}, {port:53, action:"hijack-dns"}]
    - route.final = "proxy-out", route.auto_detect_interface = true（**需要改为 default_interface="eth0"**）

internal/network/container_proxy_provider.go:53-78
  spec.Egress.Proxy.DNSServer 当前仍透传给 buildGatewaySingBoxConfig；本 plan 暂不改 EgressConfig.DNSServer
  默认值（这个改造归 Plan 45-02 的 verify 同步任务），本 plan 接受 dnsServer 参数但内部忽略它在 dns.servers 中
  的位置 —— 用 dns-local + dns-proxy 取代单一 dns-remote。

internal/network/container_proxy_provider.go:62-67
  configDir := gatewayConfigDir(hostID)  // <DATA_DIR>/gateway/<host_id>/
  os.MkdirAll(configDir, 0o755)
  configPath := filepath.Join(configDir, "config.json")
  os.WriteFile(configPath, configJSON, 0o644)

internal/network/container_proxy_provider.go:223-244
  func dockerRunGateway(...) error {
    args := []string{
      "run","-d","--name",gwName,"--network",netName,"--ip",gwIP,
      "--cap-add","NET_ADMIN",
      "--device","/dev/net/tun:/dev/net/tun",
      "--sysctl","net.ipv4.ip_forward=1",
      "-v", configPath + ":/etc/sing-box/config.json:ro",
      ...
    }
  }
  // 新增两个 -v 挂载：whitelist-cidrs.json / whitelist-domains.json 都用 ro

internal/network/gateway_singbox_config_test.go
  现有 table-driven 测试期望 outbound tag "direct"、dns server "dns-remote"，本 plan 需要保留
  现有测试可继续断言的字段（如 outbounds 含 proxy-out + 一个 direct outbound）；改名 / 新增字段
  时如果触发现有断言失败，必须同步更新断言。
</interfaces>

<rule_set_placeholder_format>
<!-- sing-box rule-set source format version 3，对应 sing-box 1.11+ -->
<!-- 来源：https://sing-box.sagernet.org/configuration/rule-set/source-format/ -->
{
  "version": 3,
  "rules": []
}
</rule_set_placeholder_format>

<target_config_skeleton>
<!-- 渲染输出的目标形态（必须满足的字段约束；map 拼装顺序不强制） -->
{
  "log": { "level": "info" },
  "dns": {
    "servers": [
      { "tag": "dns-local", "type": "local" },
      { "tag": "dns-proxy", "type": "https", "server": "1.1.1.1",
        "domain_resolver": "dns-local", "detour": "proxy-out" }
    ],
    "rules": [
      { "domain_suffix": [".lan", ".local", ".internal"], "action": "route", "server": "dns-local" },
      { "rule_set": ["whitelist-domains"], "action": "route", "server": "dns-proxy" }
    ],
    "final": "dns-proxy",
    "strategy": "ipv4_only"
  },
  "inbounds": [{
    "type": "tun", "tag": "tun-in",
    "address": ["172.19.0.1/30"],
    "auto_route": true,
    "strict_route": true,
    "endpoint_independent_nat": true,
    "stack": "system",
    "sniff": true
  }],
  "outbounds": [
    { "type": "<from outbound_config>", "tag": "proxy-out", "server": "<resolved_proxy_ip>" },
    { "type": "direct", "tag": "direct", "bind_interface": "eth0" }
  ],
  "route": {
    "default_interface": "eth0",
    "rule_set": [
      { "type": "local", "tag": "whitelist-cidrs",   "format": "source",
        "path": "/etc/sing-box/whitelist-cidrs.json" },
      { "type": "local", "tag": "whitelist-domains", "format": "source",
        "path": "/etc/sing-box/whitelist-domains.json" }
    ],
    "rules": [
      { "action": "sniff", "sniffer": ["tls","http","quic","dns"] },
      { "protocol": "dns", "action": "hijack-dns" },
      { "ip_cidr": ["<proxy_ip>/32"], "action": "route", "outbound": "direct" },
      { "ip_is_private": true,        "action": "route", "outbound": "direct" },
      { "rule_set": ["whitelist-cidrs"],   "action": "route", "outbound": "direct" },
      { "rule_set": ["whitelist-domains"], "action": "route", "outbound": "direct" }
    ],
    "final": "proxy-out"
  }
}
</target_config_skeleton>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gateway 镜像 sing-box 版本预检 + 常量与渲染骨架重构</name>
  <files>
    internal/network/gateway_singbox_config.go
  </files>
  <read_first>
    - internal/network/gateway_singbox_config.go（全文，1-90 行）
    - internal/network/container_proxy_provider.go:180-200（GatewayImage() 与镜像名）
    - .planning/phases/45-net-foundation/45-RESEARCH.md §Open Questions Q1 / §Assumptions A1
    - .planning/research/SUMMARY.md §1.2 + §1.3
  </read_first>
  <action>
    1. **镜像版本预检（开头先做，结果写到 SUMMARY）**：
       - 执行 `docker images cloud-cli-proxy-sing-gateway:local --format '{{.Repository}}:{{.Tag}} {{.ID}}'` 确认镜像存在；
       - 执行 `docker run --rm cloud-cli-proxy-sing-gateway:local sing-box version 2>&1 | head -3`，把版本号记录到 plan SUMMARY 的「镜像勘察」段；
       - 如果版本 < 1.11.0：**停止本任务**，在 SUMMARY 标记 Open Question Q1 = 镜像需先升级（不属本 plan 范围）。版本 ≥ 1.11.0 则继续。
       - 如果 docker 不可用（开发机非 Linux 或镜像未构建），记 `[ASSUMED]` 版本 ≥ 1.11 由 CI / 部署机验证，继续。

    2. **在 gateway_singbox_config.go 顶部新增包级常量**（紧跟 import 后）：
       ```go
       const (
           ruleSetWhitelistCIDRsName   = "whitelist-cidrs"
           ruleSetWhitelistDomainsName = "whitelist-domains"
           ruleSetWhitelistCIDRsPath   = "/etc/sing-box/whitelist-cidrs.json"
           ruleSetWhitelistDomainsPath = "/etc/sing-box/whitelist-domains.json"

           // ruleSetPlaceholder 是 sing-box rule-set source format v3 的空规则集字面量。
           // sing-box 1.11+ 支持；用作 Phase 45 placeholder，Phase 47 由 host-agent 覆盖。
           ruleSetPlaceholder = `{"version":3,"rules":[]}` + "\n"
       )
       ```

    3. **重构 buildGatewaySingBoxConfig 函数体**（保留函数签名不变）。把 `tunIn` / `routeRules` / `cfg` 三块拆成内部 helper：
       - `buildGatewayTunInbound() (json.RawMessage, error)`：返回 tun inbound 配置；字段必须满足 `auto_route:true`、`strict_route:true`、`endpoint_independent_nat:true`、`stack:"system"`、`sniff:true`，`address:["172.19.0.1/30"]`；删除 `sniff_override_destination` 字段（新版改由 `action:"sniff"` 规则承担）。
       - `buildGatewayDirectOutbound() (json.RawMessage, error)`：tag=`direct`，type=`direct`，`bind_interface:"eth0"`（双重保险，对应 RESEARCH §Pitfall 1）。
       - `buildGatewayDNS() map[string]any`：返回拆分 DNS map，servers 含 dns-local + dns-proxy，rules 含 `.lan/.local/.internal` → dns-local 与 `whitelist-domains` rule_set → dns-proxy，`final:"dns-proxy"`、`strategy:"ipv4_only"`。注意 dns-proxy 的字段顺序不强制，但必须含 `domain_resolver:"dns-local"` 与 `detour:"proxy-out"`。
       - `buildGatewayRouteRules(proxyServerIP string) []map[string]any`：返回精确 6 条规则数组，按 RESEARCH §Code Example 1 顺序：
         ```go
         []map[string]any{
             {"action": "sniff", "sniffer": []string{"tls", "http", "quic", "dns"}},
             {"protocol": "dns", "action": "hijack-dns"},
             {"ip_cidr": []string{proxyServerIP + "/32"}, "action": "route", "outbound": "direct"},
             {"ip_is_private": true, "action": "route", "outbound": "direct"},
             {"rule_set": []string{ruleSetWhitelistCIDRsName}, "action": "route", "outbound": "direct"},
             {"rule_set": []string{ruleSetWhitelistDomainsName}, "action": "route", "outbound": "direct"},
         }
         ```
       - `buildGatewayRouteRuleSet() []map[string]any`：返回 2 条 rule_set 引用（type=local, format=source, path=常量）。

    4. **顶层 cfg 组装**（buildGatewaySingBoxConfig 主体）：
       ```go
       cfg := map[string]any{
           "log":       map[string]any{"level": "info"},
           "dns":       buildGatewayDNS(),
           "inbounds":  []json.RawMessage{tunIn},
           "outbounds": []json.RawMessage{proxyOut, directOut},
           "route": map[string]any{
               "default_interface": "eth0",
               "rule_set":          buildGatewayRouteRuleSet(),
               "rules":             buildGatewayRouteRules(proxyServerIP),
               "final":             "proxy-out",
           },
       }
       ```
       **明确删除** `route.auto_detect_interface` 字段（被 `default_interface:"eth0"` 替代，对应 RESEARCH §State of the Art）。

    5. **保留 `dnsServer` 参数兼容性**：Go 函数的**未使用形参不会导致编译失败**（只有局部变量才会），因此：
       - 保留 `dnsServer` 函数形参（签名不变，调用方仍然传入），Plan 45-02 才会动签名。
       - 函数体内不再使用 `dnsServer`（拆分 DNS 用固定 `1.1.1.1` over DoH）。
       - **删除**当前函数顶部的 `if dnsServer == "" { dnsServer = "1.1.1.1" }` 兜底块（不再需要默认值）。
       - **不要**写 `_ = dnsServer`（Go 不要求消费未使用的函数参数；写了反而是噪音）。
       - 在保留参数处加注释：`// dnsServer kept for signature stability; split-DNS uses fixed 1.1.1.1 via DoH. See Plan 45-02.`

    6. 编译通过：`go build ./internal/network/...`

    7. 不要拆 dns_config.go / route_rules.go 新文件（CONTEXT 倾向单文件 + helper，且全文不会超 300 行）。
  </action>
  <verify>
    <automated>
      go build ./internal/network/... 2>&1 | tee /tmp/45-01-task1-build.log && \
      grep -v '^#' internal/network/gateway_singbox_config.go > /tmp/45-01-config-nocomment.go && \
      grep -c 'ruleSetWhitelistCIDRsName\b'   /tmp/45-01-config-nocomment.go && \
      grep -c 'ruleSetWhitelistDomainsName\b' /tmp/45-01-config-nocomment.go && \
      grep -c 'ruleSetPlaceholder\b'          /tmp/45-01-config-nocomment.go && \
      grep -c '"default_interface": "eth0"'   /tmp/45-01-config-nocomment.go && \
      grep -c '"dns-local"'                   /tmp/45-01-config-nocomment.go && \
      grep -c '"dns-proxy"'                   /tmp/45-01-config-nocomment.go && \
      grep -c '"endpoint_independent_nat"'    /tmp/45-01-config-nocomment.go && \
      grep -c '"strict_route":\s*true'        /tmp/45-01-config-nocomment.go && \
      grep -c 'hijack-dns'                    /tmp/45-01-config-nocomment.go && \
      grep -c 'ip_is_private'                 /tmp/45-01-config-nocomment.go && \
      grep -c '"sniff"'                       /tmp/45-01-config-nocomment.go && \
      ! grep -q 'auto_detect_interface'       /tmp/45-01-config-nocomment.go && \
      ! grep -q 'sniff_override_destination'  /tmp/45-01-config-nocomment.go
    </automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/network/...` 退出码 0
    - `gateway_singbox_config.go` 含 `ruleSetWhitelistCIDRsName` / `ruleSetWhitelistDomainsName` / `ruleSetPlaceholder` 三个包级常量（grep -c ≥ 1，且 `ruleSetPlaceholder` 字面量含 `"version":3,"rules":[]`）
    - 渲染输出含 `"default_interface": "eth0"`、`"dns-local"`、`"dns-proxy"`、`"endpoint_independent_nat"`、`"strict_route": true`、`"hijack-dns"`、`"ip_is_private"`
    - 渲染输出**不含** `auto_detect_interface`（grep 不命中）与 `sniff_override_destination`
    - 镜像版本预检结果（实测版本号 或 `[ASSUMED]` 标记）记录到 plan SUMMARY 的「镜像勘察」段
  </acceptance_criteria>
  <done>
    - gateway_singbox_config.go 通过编译，常量定义就绪
    - 渲染函数输出符合 target_config_skeleton 全部字段约束
    - 镜像 sing-box 版本预检结果落到 SUMMARY；如版本 < 1.11，明确标注本 plan 需在 Phase 45 内追加镜像升级任务（不在本 plan 范围）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 gateway_singbox_config_test.go 覆盖六类断言</name>
  <files>
    internal/network/gateway_singbox_config_test.go
  </files>
  <read_first>
    - internal/network/gateway_singbox_config_test.go（全文，了解现有 table-driven 模式与 helper）
    - internal/network/gateway_singbox_config.go（Task 1 输出）
  </read_first>
  <behavior>
    - Test 1（TestBuildGatewaySingBoxConfig_RuleSetReferences）：渲染后 JSON unmarshal 出 `route.rule_set` 数组长度=2，元素 0 tag=`whitelist-cidrs` type=`local` format=`source` path=`/etc/sing-box/whitelist-cidrs.json`，元素 1 同上为 `whitelist-domains`
    - Test 2（TestBuildGatewaySingBoxConfig_RouteRulesOrder）：`route.rules` 长度=6；按下标断言每条的 action / 关键字段：
        * [0] action=sniff，sniffer 切片含 "tls","http","quic","dns" 四个值
        * [1] protocol="dns"，action="hijack-dns"
        * [2] ip_cidr=[<proxy_ip>/32]，outbound="direct"
        * [3] ip_is_private=true，outbound="direct"
        * [4] rule_set=["whitelist-cidrs"]，outbound="direct"
        * [5] rule_set=["whitelist-domains"]，outbound="direct"
    - Test 3（TestBuildGatewaySingBoxConfig_TunHardening）：`inbounds[0]` type=tun，strict_route=true，auto_route=true，endpoint_independent_nat=true，stack="system"；`route.default_interface`="eth0"；**不含** auto_detect_interface
    - Test 4（TestBuildGatewaySingBoxConfig_SniffAndHijack）：第一条规则 action=sniff（不是 outbound），第二条 action=hijack-dns（不是旧 outbound="dns"）
    - Test 5（TestBuildGatewaySingBoxConfig_SplitDNS）：dns.servers 长度=2；存在 tag=dns-local type=local；存在 tag=dns-proxy type=https server=1.1.1.1 domain_resolver=dns-local detour=proxy-out；dns.rules 长度=2；dns.final="dns-proxy"；dns.strategy="ipv4_only"
    - Test 6（TestBuildGatewaySingBoxConfig_DirectOutboundBindEth0）：outbounds 中存在 tag=direct 的元素，且其 bind_interface="eth0"（对应 RESEARCH §Pitfall 1 防回环）
  </behavior>
  <action>
    1. 在 `gateway_singbox_config_test.go` 末尾新增 6 个测试函数，使用 helper：
       ```go
       func renderTestConfig(t *testing.T, proxyIP string) map[string]any {
           t.Helper()
           proxyRaw := json.RawMessage(`{"type":"vless","tag":"proxy","server":"placeholder","server_port":443}`)
           raw, err := buildGatewaySingBoxConfig(proxyRaw, "", proxyIP)
           if err != nil { t.Fatalf("buildGatewaySingBoxConfig: %v", err) }
           var cfg map[string]any
           if err := json.Unmarshal(raw, &cfg); err != nil {
               t.Fatalf("unmarshal config: %v", err)
           }
           return cfg
       }
       ```
       helper 放当前文件不另建新文件。

    2. 检查现有测试（若有断言 `dns-remote` 或 `auto_detect_interface` 或 `sniff_override_destination`）必须同步删除或重写；保留对 outbounds 含 `proxy-out` 与 `direct` 两 tag 的现有断言（仍成立）。

    3. 运行单元测试：`go test ./internal/network/ -run TestBuildGatewaySingBoxConfig -count=1 -v`，所有新测试与未被删除的现有测试全部通过。
  </action>
  <verify>
    <automated>
      go test ./internal/network/ -run TestBuildGatewaySingBoxConfig -count=1 -v 2>&1 | tee /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_RuleSetReferences\b'   /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_RouteRulesOrder\b'     /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_TunHardening\b'        /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_SniffAndHijack\b'      /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_SplitDNS\b'            /tmp/45-01-task2-test.log && \
      grep -c '^--- PASS: TestBuildGatewaySingBoxConfig_DirectOutboundBindEth0\b' /tmp/45-01-task2-test.log && \
      ! grep -E '^(FAIL|--- FAIL)' /tmp/45-01-task2-test.log
    </automated>
  </verify>
  <acceptance_criteria>
    - 6 个新增测试函数全部 PASS（每条 grep -c ≥ 1）
    - test 输出中不含 FAIL / --- FAIL（一处都不允许）
    - 现有 TestBuildGatewaySingBoxConfig_* 测试若有断言 `dns-remote` / `auto_detect_interface` / `sniff_override_destination` 已同步重写或删除
  </acceptance_criteria>
  <done>
    - 6 个新测试函数文件提交，断言覆盖 BYPASS-NET-01..04 / BYPASS-DNS-01..02
    - `go test ./internal/network/ -run TestBuildGatewaySingBoxConfig -count=1` 全部 PASS
  </done>
</task>

<task type="auto">
  <name>Task 3: PrepareHost 写 rule-set placeholder + dockerRunGateway 挂载</name>
  <files>
    internal/network/container_proxy_provider.go
  </files>
  <read_first>
    - internal/network/container_proxy_provider.go:30-180（PrepareHost 主流程）
    - internal/network/container_proxy_provider.go:223-244（dockerRunGateway）
    - internal/network/gateway_singbox_config.go（Task 1 输出，需复用常量）
    - .planning/phases/45-net-foundation/45-RESEARCH.md §Open Questions Q4（空 rule-set 是否需 stub）
  </read_first>
  <action>
    1. 在 `PrepareHost` 写完 `configPath` 之后（当前 67 行 `os.WriteFile(configPath, configJSON, 0o644)` 之后），紧接着写两个 placeholder 文件。位置选在同一 `configDir`，新增代码：
       ```go
       // Phase 45：sing-box 1.11+ 通过 route.rule_set[type=local] 引用这两个文件；
       // 内容为 v3 schema 空规则集占位，Phase 47 由 host-agent 原子替换。
       cidrsPath := filepath.Join(configDir, "whitelist-cidrs.json")
       domainsPath := filepath.Join(configDir, "whitelist-domains.json")
       if err := os.WriteFile(cidrsPath, []byte(ruleSetPlaceholder), 0o644); err != nil {
           return fmt.Errorf("gateway: write whitelist-cidrs placeholder: %w", err)
       }
       if err := os.WriteFile(domainsPath, []byte(ruleSetPlaceholder), 0o644); err != nil {
           return fmt.Errorf("gateway: write whitelist-domains placeholder: %w", err)
       }
       ```
       使用 `ruleSetPlaceholder` 包级常量（Task 1 已定义在 gateway_singbox_config.go；同包直接引用）。

    2. 修改 `dockerRunGateway` 签名增加两个参数：
       ```go
       func dockerRunGateway(ctx context.Context, gwName, netName, gwIP, proxyServerIP, configPath, cidrsPath, domainsPath, image string) error
       ```
       在 args 切片中现有 `-v configPath:/etc/sing-box/config.json:ro` 之后追加两个挂载（**严格只读**）：
       ```go
       "-v", cidrsPath + ":/etc/sing-box/whitelist-cidrs.json:ro",
       "-v", domainsPath + ":/etc/sing-box/whitelist-domains.json:ro",
       ```

    3. 同步修改 `PrepareHost` 内对 `dockerRunGateway` 的调用，把 `cidrsPath` / `domainsPath` 传进去。

    4. **不要**在本 task 改 `EgressConfig.Proxy.DNSServer` 默认值或 `verify.go` —— 那归 Plan 45-02 同步任务。本 task 改完后，现有 verify 仍按旧 DNS 期望值跑（**预期**：现有集成测试可能因 sing-box 启动失败而失败，但 unit test 不受影响；该副作用在 Plan 45-02 修复）。

    5. 编译：`go build ./internal/network/...` 必须通过。

    6. 跑 unit test 全套：`go test ./internal/network/ -count=1 -short` 必须通过（`-short` 跳过依赖 docker 的集成测试）。
  </action>
  <verify>
    <automated>
      go build ./internal/network/... 2>&1 | tee /tmp/45-01-task3-build.log && \
      grep -v '^//' internal/network/container_proxy_provider.go > /tmp/45-01-provider-nocomment.go && \
      grep -c 'whitelist-cidrs.json'                    /tmp/45-01-provider-nocomment.go && \
      grep -c 'whitelist-domains.json'                  /tmp/45-01-provider-nocomment.go && \
      grep -c 'ruleSetPlaceholder'                      /tmp/45-01-provider-nocomment.go && \
      grep -c '/etc/sing-box/whitelist-cidrs.json:ro'   /tmp/45-01-provider-nocomment.go && \
      grep -c '/etc/sing-box/whitelist-domains.json:ro' /tmp/45-01-provider-nocomment.go && \
      go test ./internal/network/ -count=1 -short 2>&1 | tee /tmp/45-01-task3-test.log && \
      ! grep -E '^(FAIL|--- FAIL)' /tmp/45-01-task3-test.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/network/...` 退出码 0
    - `container_proxy_provider.go` 含 `whitelist-cidrs.json` / `whitelist-domains.json` 两个文件名（grep -c ≥ 1 each）
    - 含 `ruleSetPlaceholder`（复用 Task 1 常量）
    - 含 `/etc/sing-box/whitelist-cidrs.json:ro` 与 `/etc/sing-box/whitelist-domains.json:ro` 两个 docker -v 字面量（容器侧路径 + :ro 后缀，grep -c ≥ 1 each）
    - `go test ./internal/network/ -count=1 -short` 退出码 0，输出不含 `^(FAIL|--- FAIL)`
  </acceptance_criteria>
  <done>
    - PrepareHost 在写完 config.json 之后立即生成两个 rule-set placeholder，dockerRunGateway 把它们挂入 gateway 容器为 ro
    - 现有 unit 测试（含调整后的 gateway 配置测试）全部 PASS
    - 集成测试（依赖 docker / sing-box 真启动）的预期副作用已在 plan SUMMARY 标注，由 Plan 45-02 修复
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 控制面 Go ↔ 宿主文件系统 | 控制面以宿主进程身份写入 `<DATA_DIR>/gateway/<host_id>/` 目录；该目录后续被 docker bind mount 到 gateway 容器 |
| 宿主文件系统 ↔ gateway 容器 | bind mount 必须 `:ro`；容器内 sing-box（root in container）若变可写则可篡改规则 |
| sing-box DNS 解析 ↔ 公网 | dns-proxy 经 detour=proxy-out 走 DoH，元数据加密；dns-local 仅解析内网域名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | rule-set 文件落盘 | mitigate | placeholder 文件以 0o644 模式写在 `<DATA_DIR>/gateway/<host_id>/`，仅控制面进程可改；bind mount `:ro` 在容器侧只读（Task 3） |
| T-45-02 | Information Disclosure | DNS 查询元数据泄漏 | mitigate | `dns-proxy` type=https + detour=proxy-out（Task 1 buildGatewayDNS），公网域名查询走加密 + 经代理隧道，不在本地 ISP 留痕 |
| T-45-03 | Tampering | sing-box 配置 fail-open | mitigate | Task 1 渲染输出确定性单元测试覆盖 6 类断言（Task 2），任何字段缺失立刻 build 失败；Task 3 placeholder 文件确保 sing-box 启动不报错；fail-closed 由 Phase 47 在 worker 层加固 |
| T-45-04 | Tampering | direct outbound 回环 | mitigate | Task 1 显式 `bind_interface:"eth0"` + `route.default_interface:"eth0"` 双重保险（对应 RESEARCH §Pitfall 1） |
| T-45-05 | Spoofing | route.final 漏配回退 direct | mitigate | Task 1 渲染 `route.final = "proxy-out"`；Task 2 Test 2 显式断言 final 字段，缺失即测试失败 |
| T-45-06 | Tampering | rule-set 文件被替换为指向用户可写路径 | accept | 路径由控制面 Go 代码常量决定（`gatewayConfigDir(hostID)`），不接受外部输入；攻击者要篡改需先获取控制面 host 权限，超出本 plan 范围 |
</threat_model>

<verification>
1. `go build ./internal/network/...` 通过
2. `go test ./internal/network/ -count=1 -short` 全部 PASS
3. 渲染输出含完整 7 条 route.rules（含 sniff 与 hijack-dns）、2 条 rule_set 引用、2 个 DNS server（dns-local + dns-proxy）、tun 三参加固、default_interface=eth0
4. PrepareHost 在写 config.json 后生成两个 rule-set placeholder，dockerRunGateway 把它们 `:ro` 挂入 gateway 容器
5. 不含 `auto_detect_interface` / `sniff_override_destination` / `dns-remote` 旧字段
</verification>

<success_criteria>
- 6 个 PASS 标记完整出现在 `go test ... -run TestBuildGatewaySingBoxConfig -v` 输出
- `container_proxy_provider.go` grep 命中 `ruleSetPlaceholder` 与两条 `/etc/sing-box/whitelist-*.json:ro` mount 字面量
- 镜像 sing-box 版本预检结果记录到 SUMMARY（实测号或 `[ASSUMED]` 标注）
- Plan 45-02 / 45-03 可以基于本 plan 输出独立进展（无新增跨 plan 依赖）
</success_criteria>

<output>
完成后写 `.planning/phases/45-net-foundation/45-01-SUMMARY.md`，必须包含：
- 镜像 sing-box 版本预检结果（实测或 `[ASSUMED]` 标注）
- 渲染输出关键字段 diff 摘要（旧 vs 新）
- 已知遗留副作用（如：现有 `TestPrepareHost*` 集成测试可能因 EgressConfig.DNSServer 未同步而失败，由 Plan 45-02 修复）
- 给 Plan 45-02 的接口约定（PrepareHost 已写出两个 placeholder 文件，但 worker 容器侧 resolv.conf/nsswitch.conf bind mount 仍未实现）
- **术语对齐说明**：本 plan 沿用旧 outbound tag `direct`（保持与现有 `gateway_singbox_config_test.go` 断言兼容，最小变更）；RESEARCH.md 中写的 `direct-out` 仅是研究稿术语，未在代码与测试中使用；Phase 47 文档若引用 `direct-out` 需统一为 `direct`
</output>
</content>
</invoke>