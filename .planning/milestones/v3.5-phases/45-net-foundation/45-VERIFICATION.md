---
phase: 45-net-foundation
verified: 2026-05-12T16:15:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
requirements_verified:
  - BYPASS-NET-01
  - BYPASS-NET-02
  - BYPASS-NET-03
  - BYPASS-NET-04
  - BYPASS-DNS-01
  - BYPASS-DNS-02
  - BYPASS-DNS-03
  - BYPASS-DNS-04
  - BYPASS-DATA-01
  - BYPASS-DATA-02
  - BYPASS-DATA-03
  - BYPASS-DATA-04
notes:
  - "outbound tag 命名为 'direct'（不是 ROADMAP 中的 'direct-out'）—— 这是 Plan 01 显式记录的术语保留决定，功能等价（route.rules 全部 outbound: 'direct' 指向同一 outbound）"
  - "Repository 实际 19 个方法（不是 PLAN frontmatter 写的 18 个）—— Plan 03 SUMMARY 已注明文档计数偏差，反射测试 expected 列表以 19 为准"
---

# Phase 45: 网络配置基础与数据模型 — 验证报告

**Phase Goal:** 让 sing-box 静态配置改造为两段式 + 拆分 DNS 模型 + 容器 resolv.conf 接管 + 白名单数据模型与系统预设全部就绪，为后续 API 和热更新打下可渲染、可入库、可挂载的基础。
**Verified:** 2026-05-12T16:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths（来源：ROADMAP Success Criteria）

| #   | Truth                                                                                                   | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | sing-box config.json 渲染含 route.rule_set 数组 + 6 条 route.rules（sniff/hijack-dns/proxy_ip/ip_is_private/2×rule_set） + tun 三参加固 + default_interface=eth0 | ✓ VERIFIED  | `internal/network/gateway_singbox_config.go:41-55` 主体 + `buildGatewayRouteRules / buildGatewayRouteRuleSet / buildGatewayTunInbound` helper；`TestBuildGatewaySingBoxConfig_RuleSetReferences / RouteRulesOrder / TunHardening / SniffAndHijack / DirectOutboundBindEth0` 全 PASS；grep 命中 `"default_interface": "eth0"` / `"strict_route"` / `"endpoint_independent_nat"` / `"hijack-dns"` / `"ip_is_private"` 全部存在，0 命中 `auto_detect_interface` / `sniff_override_destination` |
| 2   | 拆分 DNS（dns-local + dns-proxy DoH，detour=proxy-out，domain_resolver=dns-local，final=dns-proxy，strategy=ipv4_only） | ✓ VERIFIED  | `gateway_singbox_config.go:101-131::buildGatewayDNS` 含两个 server + 两条 rules（`.lan/.local/.internal → dns-local`、`whitelist-domains → dns-proxy`）；`TestBuildGatewaySingBoxConfig_SplitDNS` PASS                                                                                                                                                                                                                            |
| 3   | 容器 /etc/resolv.conf 与 /etc/nsswitch.conf 为只读 bind mount，唯一 nameserver=172.19.0.1，hosts=files dns，无 mdns/myhostname/wins | ✓ VERIFIED  | `container_proxy_provider.go:22-38` 定义 `resolvConfContent` / `nsswitchConfContent`；`WriteContainerDNSConfig` (line 305) 写盘到 `<DATA_DIR>/gateway/<host_id>/`；`worker.go:278-284` 在 egressCfg.Proxy 分支注入 `-v <gwDir>/resolv.conf:/etc/resolv.conf:ro` 与 nsswitch.conf 同样 ro mount；`createHost` (line 366-417) 调用顺序为 PrepareGateway → buildCreateArgs → docker create → docker start → PrepareHost，被 `call_order_test.go::TestWorker_CreateHost_CallOrder` 守护；`verifyDNS` 用 `containerExpectedDNS = "172.19.0.1"` 常量（verify.go:18）；`configureWorkerEgress` 已删除 `echo nameserver 8.8.8.8` 旧写盘逻辑（line 463-501） |
| 4   | migration 0019 五张表 + 两条 system seed（loopback is_force_on=true / lan is_force_on=false）              | ✓ VERIFIED  | `internal/store/migrations/0019_host_bypass_rules.sql:12-104`：BEGIN/COMMIT 包裹，五个 `CREATE TABLE IF NOT EXISTS`，含 `CHECK (scope/rule_type/source/applied_status)`、`UNIQUE (host_id, config_hash)`、`ON DELETE CASCADE/SET NULL`、`gen_random_uuid()`、JSONB 默认 `'{"version":3,"rules":[]}'`；`INSERT ... VALUES ('loopback', ..., TRUE, TRUE, ...), ('lan', ..., TRUE, FALSE, ...) ON CONFLICT (slug) DO NOTHING`；`TestMigration0019_FileContent / SystemPresetsSeed / SnapshotShape` 三测试全 PASS |
| 5   | Repository 暴露 CRUD 全套（presets/rules/bindings/snapshots/audit_log），is_system=true 行的 Update/Delete 拦截返回 ErrSystemBypassPresetImmutable，applied_status 默认 pending 可写四态 | ✓ VERIFIED  | `queries_bypass.go` 19 个 `func (r *Repository) Bypass*` 方法（grep -c 验证）+ `ErrSystemBypassPresetImmutable` sentinel（line 16）+ `updateBypassPresetSQL` / `deleteBypassPresetSQL` 含 `AND is_system = FALSE` 兜底；`DeleteBypassPreset` (line 344-366) 先 checkBypassPresetIsSystemSQL，命中返回 sentinel；`UpdateBypassPreset` (line 299-342) 影响 0 行后回查 is_system 区分 sentinel vs ErrNoRows；`nullableUUIDArg` helper (line 27-32) 把 `*string == ""` 转 NULL 修复 CR-01；`TestBypassRepository_Signatures / SQLConstants / ErrSystemPresetImmutable` 全 PASS |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                | Expected                                                                              | Status     | Details                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `internal/network/gateway_singbox_config.go`                            | 渲染骨架 + 5 个 helper + 5 个 rule-set 常量                                          | ✓ VERIFIED | 197 行；`ruleSetWhitelistCIDRsName/Path/Domains*` 常量 line 12-21；`buildGatewayTunInbound / buildGatewayDirectOutbound / buildGatewayDNS / buildGatewayRouteRules / buildGatewayRouteRuleSet` 全部存在                                                                                                                            |
| `internal/network/gateway_singbox_config_test.go`                       | 6 新增测试覆盖 rule_set / strict_route / sniff+hijack / split-dns / route-rules-order | ✓ VERIFIED | 全部 6 测试函数 PASS（`go test -run TestBuildGatewaySingBoxConfig -v`）                                                                                                                                                                                                                |
| `internal/network/container_proxy_provider.go`                          | PrepareGateway / PrepareHost / WriteContainerDNSConfig / GatewayConfigDir / teardownGateway 错误日志    | ✓ VERIFIED | `PrepareGateway` line 53-142 完成所有 gateway 启动 + DNS 源文件写盘；`PrepareHost` line 157-236 仅 connect + routes + verify；`WriteContainerDNSConfig` line 305 写两个源文件；`GatewayConfigDir` line 293 导出；`teardownGateway` line 243-280 错误 Warn 到 logger (WR-04 修复) |
| `internal/network/provider.go`                                          | Provider 接口 3 方法：PrepareGateway / PrepareHost / CleanupHost                       | ✓ VERIFIED | line 29-33 接口含三方法；docstring 明确并发语义（同 hostID 串行，WR-08 修复对齐）                                                                                                                                                                                                       |
| `internal/network/verify.go`                                            | containerExpectedDNS = "172.19.0.1" 常量 + verifyDNS 整体相等比对                       | ✓ VERIFIED | line 18 定义常量；line 123 整体 `rawContent != resolvConfContent` 比对（WR-07 修复）；不再含 `expected.Proxy.DNSServer` 引用                                                                                                                                                                              |
| `internal/runtime/tasks/worker.go`                                      | createHost 调用顺序 PrepareGateway → buildCreateArgs → docker create → docker start → PrepareHost；defer 守护 CR-02 | ✓ VERIFIED | line 366-435 顺序符合；defer CleanupHost (line 395-403) + gatewayPrepared 旗标守护失败回滚（CR-02 修复）；line 278-284 在 egressCfg.Proxy 分支注入两条 ro mount                                                                                                                                                  |
| `internal/store/migrations/0019_host_bypass_rules.sql`                  | 五张表 + 两条 system seed + 索引 + CHECK                                              | ✓ VERIFIED | 104 行；含 BEGIN/COMMIT、5×CREATE TABLE IF NOT EXISTS、4 个 CHECK 枚举、UNIQUE(host_id, config_hash)、ON DELETE CASCADE/SET NULL、loopback/lan seed                                                                                                                                                            |
| `internal/store/repository/queries_bypass.go`                           | 19 CRUD 方法 + 20+ SQL 常量 + nullableUUIDArg + ErrSystemBypassPresetImmutable        | ✓ VERIFIED | 603 行；19 方法实测；20 SQL 常量；nullableUUIDArg helper（CR-01 修复）；sentinel error 文本含 "system" 与 "cannot"                                                                                                                                                                                             |
| `internal/store/repository/models.go`                                   | 5 个 Bypass 实体 + 11 个 *Params 类型                                                | ✓ VERIFIED | line 319-498 含 `BypassPresetRule / BypassPreset / BypassRule / BypassBinding / BypassSnapshot / BypassAuditLog` + 对应 Params                                                                                                                                                          |
| `internal/network/call_order_test.go` (新增)                            | TestWorker_CreateHost_CallOrder + TestWorker_StartHost_CallOrder 守护                | ✓ VERIFIED | `TestWorker_CreateHost_CallOrder PASS`（log: `OK call order positions (createHost): [3742 4310 4418 4486 4729]`）；顶层 func 边界精确匹配（WR-02 修复）                                                                                                                                                  |
| `internal/network/container_proxy_dns_test.go` (新增)                  | TestContainerProxy_WriteContainerDNSConfig_WritesFiles + ResolvConfBindMount + NsswitchBindMount | ✓ VERIFIED | 3 测试函数全 PASS；nsswitch.conf forbidden 列表扩展含 mdns/myhostname/wins/nis_plus/resolve/dns_sd/lwres（WR-06 修复）                                                                                                                                                                |
| `internal/store/repository/migration_0019_test.go`                      | 3 个文本断言测试                                                                       | ✓ VERIFIED | TestMigration0019_FileContent / SystemPresetsSeed / SnapshotShape 全 PASS                                                                                                                                                                                                                |
| `internal/store/repository/queries_bypass_test.go`                      | 反射 + SQL 文本 + sentinel 断言                                                       | ✓ VERIFIED | TestBypassRepository_Signatures / SQLConstants / ErrSystemPresetImmutable 全 PASS（反射 expected 列表 19 项 / 21 SQL 常量 / sentinel 文案 "system" + "cannot"）                                                                                                                                  |

### Key Link Verification

| From                                                     | To                                                                                    | Via                                                              | Status   | Details                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `worker.go::createHost`                                  | `provider.PrepareGateway(ctx, spec)`                                                  | docker create 之前显式起 gateway                                | ✓ WIRED  | line 390 调用；defer line 395-403 守护回滚（CR-02）                                                                                                                                                                                                                                                                                            |
| `worker.go::buildCreateArgs`                             | `/etc/resolv.conf, /etc/nsswitch.conf` ro bind mount                                  | 在 egressCfg.Proxy != nil 时 append `-v` 参数                  | ✓ WIRED  | line 278-284 grep 命中 `gwDir+"/resolv.conf:/etc/resolv.conf:ro"` 与 `nsswitch.conf` 同模式                                                                                                                                                                                                                                                              |
| `ContainerProxyProvider.PrepareGateway`                  | `WriteContainerDNSConfig(hostID)`                                                     | gateway 准备阶段写两个源文件                                   | ✓ WIRED  | container_proxy_provider.go:110-112 调用                                                                                                                                                                                                                                                                                                            |
| `ContainerProxyProvider.PrepareGateway`                  | `dockerRunGateway` 含 3 个 ro mount（config.json + 2 个 rule-set placeholder）       | line 119 调用，gateway 容器 `-v /etc/sing-box/whitelist-*.json:ro` | ✓ WIRED  | `dockerRunGateway` line 353-376 包含 3 条 mount 字面量                                                                                                                                                                                                                                                                                          |
| `verify.go::verifyDNS`                                   | `containerExpectedDNS = "172.19.0.1"`                                                 | DNS 期望值常量化，与 EgressConfig 解耦                          | ✓ WIRED  | verify.go:57 调用 + line 123 整体内容相等比对（WR-07 修复）                                                                                                                                                                                                                                                                                                                      |
| `Repository.DeleteBypassPreset`                          | `ErrSystemBypassPresetImmutable`                                                      | 命中 is_system=true 返回 sentinel；SQL 同时 `AND is_system = FALSE` 兜底 | ✓ WIRED  | queries_bypass.go:344-366 双层防御                                                                                                                                                                                                                                                                                                              |
| `migration 0019`                                         | `host_bypass_snapshots.applied_status` 默认 pending + CHECK 四态                      | TEXT NOT NULL DEFAULT 'pending' CHECK IN (...)                  | ✓ WIRED  | 0019_host_bypass_rules.sql:68-69                                                                                                                                                                                                                                                                                                                |
| `gateway_singbox_config.go::buildGatewayRouteRuleSet`    | `/etc/sing-box/whitelist-cidrs.json` / `whitelist-domains.json`                       | type=local, format=source, path=常量                            | ✓ WIRED  | line 157-172 渲染两条 rule_set；常量 line 15-16 与 PrepareGateway 写盘路径一致                                                                                                                                                                                                                                                                                                  |

### Data-Flow Trace (Level 4)

不适用：Phase 45 主要为渲染层 + 数据 schema + Repository CRUD，输出是静态配置文件、SQL schema 与方法签名，没有需要追溯的「动态数据流」组件。所有数据流由 Phase 47（host-agent reload）和 Phase 46（admin API 数据流）承接。

### Behavioral Spot-Checks

| Behavior                                                      | Command                                                                                                                              | Result                                                                                                | Status |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------ |
| 全包构建通过                                                  | `go build ./...`                                                                                                                     | exit 0                                                                                                | ✓ PASS |
| network 包测试全绿                                            | `go test ./internal/network/ -count=1 -short`                                                                                       | ok                                                                                                    | ✓ PASS |
| Phase 45 渲染层测试 6/6                                       | `go test ./internal/network/ -run TestBuildGatewaySingBoxConfig -v`                                                                  | RuleSetReferences / RouteRulesOrder / TunHardening / SniffAndHijack / SplitDNS / DirectOutboundBindEth0 全 PASS  | ✓ PASS |
| 容器 DNS 入口锁测试 3/3                                       | `go test ./internal/network/ -run TestContainerProxy -v`                                                                             | WriteContainerDNSConfig_WritesFiles / ResolvConfBindMount / NsswitchBindMount 全 PASS                | ✓ PASS |
| 调用顺序守护                                                 | `go test ./internal/network/ -run TestWorker_CreateHost_CallOrder -v`                                                                | PASS（positions [3742 4310 4418 4486 4729] 严格升序）                                              | ✓ PASS |
| migration 文本断言 3/3                                       | `go test ./internal/store/repository/ -run TestMigration0019 -v`                                                                     | FileContent / SystemPresetsSeed / SnapshotShape 全 PASS                                              | ✓ PASS |
| Repository 签名 + SQL + sentinel 3/3                         | `go test ./internal/store/repository/ -run TestBypassRepository -v`                                                                  | Signatures (19 方法) / SQLConstants (21 SQL) / ErrSystemPresetImmutable 全 PASS                       | ✓ PASS |
| runtime/tasks 测试无回归                                     | `go test ./internal/runtime/... -count=1 -short`                                                                                    | ok runtime + ok runtime/tasks                                                                          | ✓ PASS |
| 旧 DNS 写盘字面量已删除                                       | `grep -rn "echo 'nameserver 8.8.8.8' > /etc/resolv.conf" internal/`                                                                  | 0 matches                                                                                              | ✓ PASS |
| 小写 gatewayConfigDir 已绝迹                                  | `grep -rn 'gatewayConfigDir\b' internal/`                                                                                            | 0 matches（B3 acceptance）                                                                            | ✓ PASS |
| sing-box 旧字段移除                                          | `grep -rn 'auto_detect_interface' internal/network/gateway_singbox_config.go`                                                        | 0 matches（仅 test 中作 negative assertion）                                                          | ✓ PASS |

### Requirements Coverage

| Requirement      | Source Plan      | Description                                                                                                                                       | Status      | Evidence                                                                                                                                                |
| ---------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BYPASS-NET-01    | 45-01-PLAN.md    | sing-box 配置改造为两段式（静态 config.json + 动态 local rule-set）                                                                              | ✓ SATISFIED | `gateway_singbox_config.go` 含 rule_set + ruleSetWhitelistCIDRsPath / DomainsPath；TestBuildGatewaySingBoxConfig_RuleSetReferences PASS                  |
| BYPASS-NET-02    | 45-01-PLAN.md    | route.rules 含 ip_is_private:true + rule_set 引用，final=proxy-out                                                                                | ✓ SATISFIED | 6 条规则按下标精确断言 PASS（TestBuildGatewaySingBoxConfig_RouteRulesOrder）                                                                                                |
| BYPASS-NET-03    | 45-01-PLAN.md    | tun strict_route + auto_route + endpoint_independent_nat + default_interface=eth0                                                              | ✓ SATISFIED | TestBuildGatewaySingBoxConfig_TunHardening PASS；direct outbound bind_interface=eth0 同 PASS                                                              |
| BYPASS-NET-04    | 45-01-PLAN.md    | route.rules 第一条 action=sniff，protocol=dns + hijack-dns（替代旧 outbound:"dns"）                                                              | ✓ SATISFIED | TestBuildGatewaySingBoxConfig_SniffAndHijack PASS                                                                                                                          |
| BYPASS-DNS-01    | 45-01-PLAN.md    | dns.servers 含 dns-local（type=local）+ dns-proxy（type=https，server=1.1.1.1，detour=proxy-out）；dns.final=dns-proxy；strategy=ipv4_only      | ✓ SATISFIED | TestBuildGatewaySingBoxConfig_SplitDNS PASS                                                                                                                            |
| BYPASS-DNS-02    | 45-01-PLAN.md    | .lan/.local/.internal → dns-local；whitelist-domains rule_set → dns-proxy                                                                       | ✓ SATISFIED | `buildGatewayDNS` rules 段含两条；测试断言 PASS                                                                                                                                |
| BYPASS-DNS-03    | 45-02-PLAN.md    | 容器 /etc/resolv.conf 改为只读 bind mount，唯一 nameserver 172.19.0.1，含 options ndots:0 single-request-reopen                                | ✓ SATISFIED | resolvConfContent 常量 + WriteContainerDNSConfig + worker.go ro mount + verifyDNS 整体相等比对（WR-07）；TestContainerProxy_WriteContainerDNSConfig_WritesFiles PASS  |
| BYPASS-DNS-04    | 45-02-PLAN.md    | 容器 /etc/nsswitch.conf 改为 hosts: files dns，禁用 mdns/myhostname/wins                                                                          | ✓ SATISFIED | nsswitchConfContent + ro mount + forbidden 列表扩展（WR-06 含 resolve/dns_sd/lwres）；TestContainerProxy_NsswitchBindMount PASS                                                |
| BYPASS-DATA-01   | 45-03-PLAN.md    | migration 0019 建 5 张表                                                                                                                          | ✓ SATISFIED | 0019_host_bypass_rules.sql 五个 CREATE TABLE；TestMigration0019_FileContent PASS                                                                                            |
| BYPASS-DATA-02   | 45-03-PLAN.md    | Repository BypassPreset/Rule/Binding/Snapshot CRUD                                                                                              | ✓ SATISFIED | 19 个 Repository 方法 + 反射断言 PASS（TestBypassRepository_Signatures）                                                                                                |
| BYPASS-DATA-03   | 45-03-PLAN.md    | loopback / lan 两条 system seed + is_system 不可删                                                                                              | ✓ SATISFIED | seed INSERT line 92-102；DeleteBypassPreset / UpdateBypassPreset 双层防御（Go 层 + SQL `AND is_system = FALSE`）；ErrSystemBypassPresetImmutable sentinel；TestBypassRepository_ErrSystemPresetImmutable PASS  |
| BYPASS-DATA-04   | 45-03-PLAN.md    | host_bypass_snapshots applied_status 四态 + version + config_hash + whitelist_*_json                                                          | ✓ SATISFIED | 0019 schema line 61-74 含 BIGINT version、TEXT config_hash、JSONB 默认 `{"version":3,"rules":[]}`、CHECK 四态、UNIQUE (host_id, config_hash)；TestMigration0019_SnapshotShape PASS  |

**所有 12 个 REQ-ID 均有实现证据。**

### Anti-Patterns Found

| File                                                | Line   | Pattern                                                                                                | Severity | Impact                                                                                                                                                                                                                                                                  |
| --------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gateway_singbox_config.go`                         | 81     | 注释明确 outbound tag 保留 `direct`（非 ROADMAP 的 `direct-out`）                                       | ℹ️ Info | 文档化决定，功能等价；Plan 01 SUMMARY「术语对齐说明」段已记录；Phase 47 文档若引用需统一为 `direct`                                                                                                                                                                                                  |
| `queries_bypass.go`                                 | 19 方法 | Plan frontmatter 写 18 个，实际 19 个                                                                  | ℹ️ Info | Plan 03 SUMMARY 已注明：`反射 expected 列表以 19 为准`，无功能影响；建议下一次 Plan 维护时把 PLAN 标题文案的「18」修正为「19」                                                                                                                                                                                                       |
| 整体                                                | -      | TODO / FIXME / placeholder 字符串                                                                      | ℹ️ Info | 仅在文档/注释中出现（如 `Phase 47 由 host-agent 覆盖`/`留给 Plan 45-02 修复`），均是显式的跨 phase 工作交接说明，非真实代码 stub                                                                                                                                                                                                  |

**无 Blocker / Warning 级别的 anti-pattern。** Review 阶段发现的 2 BLOCKER（CR-01 / CR-02）+ 8 WARNING（WR-01..08）已全部修复并 commit（commit hash a74d600 → 7be53d4 链）。

### Human Verification Required

无。所有 must-have truths 均可通过 grep + go test 自动验证；本 phase 不涉及视觉外观、用户操作流或外部服务联动。Phase 46 / 47 才会引入需要人工验证的 UI 与真实运行时行为。

### Gaps Summary

无 gaps。Phase 45 三个 plan 的全部 must-have truths 已通过 codebase 证据满足，12 个 REQ-ID 均有 implementation 证据，5 条 ROADMAP success criteria 全部 codebase 可验证（criterion #3 的「nsenter + mount ro 标记」属真实运行时行为，但其前置条件——源文件存在 + bind mount 注入 + verify 期望值 + 调用顺序——已全部静态验证；真实 nsenter 行为留给 Phase 47 E2E）。

CR/WR 修复链未引入新的 BLOCKER 或失败测试。STATE.md / ROADMAP.md 未被本 phase 触动（由 orchestrator 控制）。

---

_Verified: 2026-05-12T16:15:00Z_
_Verifier: Claude (gsd-verifier)_
