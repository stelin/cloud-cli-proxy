---
phase: 46-api-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - internal/agentapi/contracts.go
  - internal/runtime/tasks/worker.go
  - internal/controlplane/http/admin_bypass_snapshots.go
  - internal/controlplane/http/admin_bypass_snapshots_test.go
  - internal/controlplane/http/admin_bypass_audit_log.go
  - internal/controlplane/http/admin_bypass_audit_log_test.go
  - internal/controlplane/http/bypass_render.go
  - internal/controlplane/http/bypass_render_test.go
  - internal/controlplane/http/router.go
  - internal/controlplane/server.go
autonomous: true
requirements:
  - BYPASS-API-02
  - BYPASS-API-05
must_haves:
  truths:
    - POST /v1/admin/hosts/{hostID}/bypass/preview 返回渲染后的 whitelist-cidrs.json + whitelist-domains.json + nft set diff + 风险报告，不写库
    - POST /v1/admin/hosts/{hostID}/bypass/apply 写一行 host_bypass_snapshots，applied_status="pending"；config_hash 重复 POST 同 hash 返回 200 + 已存在 snapshot id（幂等）
    - POST /v1/admin/hosts/{hostID}/bypass/rollback 接收 target_snapshot_id，若该 snapshot.applied_status="applied" 才允许；新建 snapshot 行（version+1，复制 target config_hash + whitelist json，source='rollback'，applied_status='pending'）；不直接修改 target snapshot 状态（target 保持 applied 不变，仅 Phase 47 worker 真实下发后才切状态）；写 audit note 含 rollback_target_snapshot_id
    - GET /v1/admin/hosts/{hostID}/bypass/effective 返回 4 段：presets_active / rules_active / whitelist_cidrs_rendered / whitelist_domains_rendered
    - GET /v1/admin/hosts/{hostID}/bypass/audit-log?before=<iso>&limit=20 cursor 分页返回 audit 行；默认 limit=20，最大 200
    - worker dispatch 命中 ActionReloadHostBypass 时仅写日志 "Phase 46 placeholder; no-op until Phase 47" 并返回 nil（不真实执行；Phase 47 接管真实 reload）
    - 所有写操作（apply / rollback）通过 writeBypassAuditLog 双轨落库：host_bypass_audit_log 详细 diff + EventRecorder.RecordEvent("bypass.apply" / "bypass.rollback") 异步事件流（per CONTEXT.md L107）
  artifacts:
    - path: internal/controlplane/http/bypass_render.go
      provides: 把 binding + rule 集合渲染为 whitelist-cidrs.json / whitelist-domains.json / nft set diff
      min_lines: 200
    - path: internal/controlplane/http/admin_bypass_snapshots.go
      provides: AdminBypassSnapshotsHandler (Preview / Apply / Rollback / Effective)
      min_lines: 280
    - path: internal/controlplane/http/admin_bypass_audit_log.go
      provides: AdminBypassAuditLogHandler (ListByTarget with cursor)
      min_lines: 80
    - path: internal/agentapi/contracts.go
      provides: ActionReloadHostBypass 常量
      contains: "ActionReloadHostBypass HostAction = \"reload_host_bypass\""
    - path: internal/runtime/tasks/worker.go
      provides: dispatch case for ActionReloadHostBypass (Phase 47 占位)
      contains: ActionReloadHostBypass
  key_links:
    - from: bypass_render.go::RenderBypassConfig
      to: Phase 45 sing-box rule-set 文件格式（whitelist-cidrs.json / whitelist-domains.json）
      via: 渲染产出与 Phase 45 placeholder 完全一致格式
      pattern: "\\\"version\\\":\\s*3,\\s*\\\"rules\\\""
    - from: Apply handler
      to: Repository.CreateBypassSnapshot
      via: config_hash 作为幂等键，UNIQUE(host_id, config_hash) DB 约束兜底
      pattern: "CreateBypassSnapshot"
    - from: Apply handler
      to: worker dispatch (ActionReloadHostBypass)
      via: deps.HostActions.QueueHostAction(ctx, hostID, ActionReloadHostBypass, "")
      pattern: "ActionReloadHostBypass"
    - from: Apply/Rollback handler
      to: writeBypassAuditLog 双轨（host_bypass_audit_log + EventRecorder.RecordEvent）
      via: bypass_audit_helper.go::writeBypassAuditLog（Plan 01 Task 2 产物）
      pattern: "writeBypassAuditLog\\(.+h\\.events"
---

<objective>
落地 v3.5 后台 API 的「快照与动作半区」：preview / apply / rollback / effective / audit-log 5 个端点 + 渲染层 + worker dispatch 占位。Apply 写 snapshot 状态为 pending，dispatch 命中 ActionReloadHostBypass 时仅写日志（Phase 47 接管真实 reload）。Rollback 新建 snapshot 行（不破坏 target 历史状态）。

Purpose: 让前端 (Plan 03/04) 可以走完「勾预设 → 加规则 → 预览 → 应用 → 回滚」闭环，所有写操作落 snapshot + audit + events 三层。
Output: 3 个 handler 文件 + 1 个渲染模块 + worker dispatch 占位 + router/server 注册。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/46-api-ui/46-CONTEXT.md
@.planning/phases/46-api-ui/46-01-PLAN.md
@.planning/phases/45-net-foundation/45-03-SUMMARY.md

# 必读
@internal/controlplane/http/admin_egress_ip_probe.go
@internal/controlplane/http/admin_hosts.go
@internal/controlplane/http/router.go
@internal/store/repository/queries_bypass.go
@internal/store/repository/models.go
@internal/agentapi/contracts.go

<interfaces>
<!-- Plan 01 已就绪的契约 + Plan 02 新增的契约。直接照抄。 -->

## Plan 01 已落地的 helper（本 plan 直接复用）

```go
// internal/controlplane/http/bypass_validation.go
const (
    ErrCodeBypassSnapshotConflict  = "BYPASS_SNAPSHOT_CONFLICT"
    ErrCodeBypassSnapshotNotFound  = "BYPASS_SNAPSHOT_NOT_FOUND"
    ErrCodeBypassHostNotFound      = "BYPASS_HOST_NOT_FOUND"
)
// internal/controlplane/http/bypass_audit_helper.go（Plan 01 Task 2 修订版：双轨）
// 签名：(ctx, logger, writer, events, r, action, targetKind, *targetID, before, after any, note string)
// 内部同时调 writer.InsertBypassAuditLog + events.RecordEvent("bypass."+action, payload)
func writeBypassAuditLog(ctx, logger, writer, events, r, action, targetKind, *targetID, before, after any, note string)
func extractActorIP(r *http.Request) string
func actorIDPtr(ctx context.Context) *string
```

## Repository 快照 API（Phase 45 Plan 03 已就绪）

```go
ListBypassSnapshotsByHost(ctx, hostID string, limit int) ([]BypassSnapshot, error)
CreateBypassSnapshot(ctx, params CreateBypassSnapshotParams) (BypassSnapshot, error)   // UNIQUE(host_id, config_hash) 由 DB 约束
UpdateBypassSnapshotStatus(ctx, id, status string) (BypassSnapshot, error)             // status ∈ {pending, applied, failed, rolled_back}
GetLatestAppliedBypassSnapshot(ctx, hostID string) (BypassSnapshot, error)             // version DESC LIMIT 1, applied_status='applied'
ListBypassAuditLogByTarget(ctx, kind, id string) ([]BypassAuditLog, error)
```

## Snapshot 实体

```go
type BypassSnapshot struct {
    ID, HostID                            string
    Version                               int64
    ConfigHash                            string
    WhitelistCIDRsJSON, WhitelistDomainsJSON json.RawMessage
    AppliedStatus                         string // pending / applied / failed / rolled_back
    Source                                string // "apply" / "rollback"（CreateBypassSnapshotParams 透传，Phase 45 schema 已含 source TEXT 列）
    CreatedBy                             *string
    CreatedAt                             time.Time
}
type CreateBypassSnapshotParams struct {
    HostID, ConfigHash string
    Version            int64
    WhitelistCIDRsJSON, WhitelistDomainsJSON json.RawMessage
    Source             string  // "apply" / "rollback"
    CreatedBy *string
}
```

注：如果 Phase 45 Plan 03 实际 `CreateBypassSnapshotParams` 未含 `Source` 字段（45-03-SUMMARY 未明列），执行者在本 plan 内补字段并对齐 SQL `INSERT ... (source) VALUES ($N)`；Phase 45 schema 0019 已建 source TEXT 列（来自 45-03 schema），仅 Repository 层补 1 个字段透传。

## sing-box rule-set 文件格式（Phase 45 placeholder 已锁定）

```json
{ "version": 3, "rules": [ { "ip_cidr": ["10.0.0.0/8"] }, { "domain_suffix": ["corp.internal"] } ] }
```

## worker dispatch 接入点（internal/runtime/tasks/worker.go）

需要查找现有 switch case（如 `case agentapi.ActionStartHost:`），在最后追加 `case agentapi.ActionReloadHostBypass:`（详见 Task 5）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 加 ActionReloadHostBypass 常量 + worker dispatch 占位</name>
  <files>
    internal/agentapi/contracts.go
    internal/runtime/tasks/worker.go
  </files>
  <read_first>
    - internal/agentapi/contracts.go 前 30 行（HostAction 常量块）
    - internal/runtime/tasks/worker.go（全文，搜索 `case agentapi.Action`，定位现有 dispatch switch）
  </read_first>
  <action>
    1. 修改 `internal/agentapi/contracts.go`：在 ActionVolumeRemove 常量后面（约第 11 行后）追加：
       ```go
       ActionReloadHostBypass HostAction = "reload_host_bypass" // Phase 46/47 — Phase 46 占位，Phase 47 真实下发 rule-set
       ```

    2. 修改 `internal/runtime/tasks/worker.go`：找到处理 HostAction 的 switch 语句（应该在 dispatch / dispatchHostAction 之类函数内）。在最后一个 case 之前追加：
       ```go
       case agentapi.ActionReloadHostBypass:
           // Phase 46: 占位实现，仅写日志。Phase 47 实现真实 reload：
           //   1. 拉最新 host_bypass_snapshots 并下发到 host-agent
           //   2. host-agent 写 rule-set 文件 + nft set 更新
           //   3. 健康检查 3 次失败自动回滚
           // 注意：日志字符串必须含字面 "Phase 46 placeholder; no-op until Phase 47"（must_haves truths 校验依赖该字面）
           w.logger.Info("reload_host_bypass dispatched (Phase 46 placeholder; no-op until Phase 47)",
               "host_id", task.HostID, "task_id", task.ID)
           return nil
       ```
       （字段名 `w.logger` / `task.HostID` / `task.ID` 视 worker.go 实际结构调整；如果当前 dispatch 接的是 `(ctx, taskID)` 也用相同模式。）

    3. 不要修改任何其他 dispatch 逻辑、retry 策略、健康检查代码。本 task 只加一个 case + 一个常量。
  </action>
  <verify>
    <automated>cd . && go build ./internal/... && go test ./internal/agentapi/ ./internal/runtime/tasks/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ActionReloadHostBypass" internal/agentapi/contracts.go` ≥ 1
    - `grep -c "ActionReloadHostBypass" internal/runtime/tasks/worker.go` ≥ 1
    - `grep -c "Phase 46 placeholder; no-op until Phase 47" internal/runtime/tasks/worker.go` ≥ 1（WARN-3：truths 与代码字面一致）
    - `go build ./internal/...` 通过
    - `go test ./internal/agentapi/... ./internal/runtime/tasks/... -count=1` 通过（不引入回归）
    - dispatch 命中后只写日志，不调用任何 nft / sing-box / 文件 IO
  </acceptance_criteria>
  <done>Phase 47 可在此基础上把 case 内的 placeholder 替换为真实 reload 逻辑，无需再改契约。日志字面与 truths #6 描述一致。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 bypass_render.go（binding → whitelist-cidrs.json / whitelist-domains.json / nft diff）</name>
  <files>
    internal/controlplane/http/bypass_render.go
    internal/controlplane/http/bypass_render_test.go
  </files>
  <read_first>
    - internal/store/repository/models.go 第 318-411 行（BypassPresetRule / BypassRule / BypassSnapshot 类型）
    - .planning/phases/46-api-ui/46-CONTEXT.md `### Claude's Discretion` 节（nft diff 渲染策略）
    - 查找 Phase 45 sing-box rule-set placeholder 输出（grep `whitelist-cidrs.json` 在 internal/network/ 或 internal/gateway/ 下，对齐 JSON shape）
  </read_first>
  <action>
    创建 `internal/controlplane/http/bypass_render.go`。所有函数 unexported 或 pkg-public 按需暴露。

    1. 输入聚合类型：
       ```go
       type BypassRenderInput struct {
           HostID    string
           Presets   []repository.BypassPreset    // 仅当前 host 通过 binding 启用的 preset
           Rules     []repository.BypassRule       // host 自身规则 + binding 引用的 global rule
       }

       type BypassRenderOutput struct {
           CIDRsJSON    json.RawMessage   // 直接可写到 whitelist-cidrs.json
           DomainsJSON  json.RawMessage   // 直接可写到 whitelist-domains.json
           NftDiff      string            // unified diff text（与上一个 applied snapshot 对比）
           ConfigHash   string            // sha256(cidrsJSON + "\n" + domainsJSON) 的 hex
           RiskyCount   int               // is_risky=true 的规则数（前端用以触发二次确认）
           Summary      string            // 中文摘要，如 "覆盖 12 条规则，其中 2 条高风险"
       }
       ```

    2. 实现 `RenderBypassConfig(input BypassRenderInput, prevSnapshot *repository.BypassSnapshot) (BypassRenderOutput, error)`：
       - 收集所有规则按 rule_type 分桶：`ip`/`cidr` 进 cidrs 桶，`domain`/`domain_suffix`/`domain_keyword` 进 domains 桶
       - cidrs 桶聚合为：
         ```json
         {"version": 3, "rules": [{"ip_cidr": ["10.0.0.0/8", "192.168.0.0/16", ...]}]}
         ```
         IP 自动转 `/32`（或保留 `/32` 后缀，与 sing-box 解析对齐）
       - domains 桶按 sing-box rule-set 多键拆分：
         ```json
         {"version": 3, "rules": [
            {"domain": ["api.internal.corp"]},
            {"domain_suffix": ["corp.internal"]},
            {"domain_keyword": ["mirrors"]}
         ]}
         ```
       - JSON 输出用 `json.MarshalIndent(..., "", "  ")` 保证可读且 deterministic（关键：先按字典序排序每个数组，再序列化）—— 让 config_hash 在相同规则集合下稳定。
       - ConfigHash = `sha256(cidrsJSON + "\n" + domainsJSON)` 取 hex。
       - NftDiff：
         - 如果 prevSnapshot == nil：返回纯绿色 `+` 行（新增全部 cidrs / ips）
         - 否则做简单 set diff：列出新增 IP（`+` 绿）/ 删除 IP（`-` 红）/ unchanged context 不输出（节省篇幅）
         - 输出格式：
           ```
           # @whitelist_v4 set diff (host=<id>)
           + 10.0.0.0/8
           - 1.1.1.1
           ```
       - RiskyCount = 数 `IsRisky=true` 的 rule
       - Summary 中文文案：`fmt.Sprintf("覆盖 %d 条规则，其中 %d 条高风险", totalRules, RiskyCount)`

    3. 测试 `bypass_render_test.go` ≥ 6 sub-test：
       - empty input → cidrs/domains 都是 `{"version":3,"rules":[]}`，config_hash 是常量
       - 单条 cidr 10.0.0.0/8 → cidrs JSON 含该项，domains JSON 空 rules
       - 多条 domain + domain_suffix → domains JSON 分两个 rule 块
       - 排序稳定性：输入 `[10.0.0.0/8, 192.168.0.0/16]` 与 `[192.168.0.0/16, 10.0.0.0/8]` 产出相同 ConfigHash
       - prevSnapshot=nil 时 NftDiff 全部 `+` 行
       - prevSnapshot 含 1.1.1.1，本次缺 1.1.1.1 但加 8.8.8.8 → diff 含 `- 1.1.1.1` 和 `+ 8.8.8.8/32`
       - RiskyCount 在 rules 含 1 条 IsRisky=true 时 = 1
  </action>
  <verify>
    <automated>cd . && go test ./internal/controlplane/http/ -run TestRenderBypassConfig -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestRenderBypassConfig -count=1 -v ./internal/controlplane/http/` 通过且 ≥ 6 个子用例
    - `grep -c '"version":\s*3' internal/controlplane/http/bypass_render.go` ≥ 2（cidrs + domains 模板）
    - `grep -c "sha256\|sha2" internal/controlplane/http/bypass_render.go` ≥ 1
    - 同一规则集合在不同输入顺序下产出同一 ConfigHash（排序稳定性测试通过）
  </acceptance_criteria>
  <done>渲染层是纯函数，可被 Preview/Apply handler 直接调用，输出与 Phase 45 rule-set 文件格式 100% 兼容。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 Preview / Apply / Rollback / Effective handler + 测试（rollback 新建 snapshot，不破坏 target 状态）</name>
  <files>
    internal/controlplane/http/admin_bypass_snapshots.go
    internal/controlplane/http/admin_bypass_snapshots_test.go
  </files>
  <read_first>
    - internal/controlplane/http/admin_bypass_presets.go（Plan 01 产物，参考结构）
    - internal/controlplane/http/admin_egress_ip_probe.go（action endpoint 风格参考，尤其是 stream / single endpoint）
    - internal/controlplane/http/bypass_render.go（本 plan Task 2 产物）
    - internal/controlplane/http/bypass_audit_helper.go（Plan 01 Task 2 双轨版 — 签名含 events 参数）
    - internal/store/repository/queries_bypass.go 第 140-180 行（snapshot SQL）
  </read_first>
  <action>
    创建 `internal/controlplane/http/admin_bypass_snapshots.go`：

    1. store interface（聚合，由 server.go 注入 Repository）：
       ```go
       type AdminBypassSnapshotStore interface {
           // host validation
           GetHost(context.Context, string) (repository.Host, error)
           // collection for render
           ListBypassBindingsByHost(context.Context, string) ([]repository.BypassBinding, error)
           GetBypassPresetByID(context.Context, string) (repository.BypassPreset, error)
           ListBypassRules(context.Context, *string) ([]repository.BypassRule, error)
           // snapshot lifecycle
           ListBypassSnapshotsByHost(context.Context, string, int) ([]repository.BypassSnapshot, error)
           CreateBypassSnapshot(context.Context, repository.CreateBypassSnapshotParams) (repository.BypassSnapshot, error)
           GetBypassSnapshotByID(context.Context, string) (repository.BypassSnapshot, error)  // WARN-4 修复：rollback 需要查 target 状态
           GetLatestAppliedBypassSnapshot(context.Context, string) (repository.BypassSnapshot, error)
           // audit
           InsertBypassAuditLog(context.Context, repository.InsertBypassAuditLogParams) (string, error)
       }
       type HostActionQueuer interface {  // 复用 router.go 现有 interface
           QueueHostAction(context.Context, string, agentapi.HostAction, string) (repository.Task, error)
       }
       ```
       **GetBypassSnapshotByID 注：** Phase 45 Plan 03 Repository 19 个方法清单未直接含该方法（仅有 ListByHost + GetLatestApplied）。执行者按 WARN-5 同样的模式在 `queries_bypass.go` 末尾补一条 SQL：
       ```go
       const getBypassSnapshotByIDSQL = `SELECT id::text, host_id::text, version, config_hash, whitelist_cidrs_json, whitelist_domains_json, applied_status, source, created_by, created_at FROM host_bypass_snapshots WHERE id = $1`
       func (r *Repository) GetBypassSnapshotByID(ctx context.Context, id string) (BypassSnapshot, error) { ... }
       ```
       如果 Phase 45 已补，则直接复用。

    2. handler struct：
       ```go
       type AdminBypassSnapshotsHandler struct {
           logger    *slog.Logger
           store     AdminBypassSnapshotStore
           actions   HostActionQueuer
           events    EventRecorder
       }
       func NewAdminBypassSnapshotsHandler(logger *slog.Logger, store AdminBypassSnapshotStore, actions HostActionQueuer, events EventRecorder) *AdminBypassSnapshotsHandler
       ```

    3. 内部 helper `func (h) collectRenderInput(ctx, hostID string) (BypassRenderInput, error)`：
       - ListBypassBindingsByHost(hostID)
       - 对每个 binding.PresetID != nil 的，GetBypassPresetByID
       - 对每个 binding.RuleID != nil 的，过滤进 rules（注意：ListBypassRules 返回 global + host scope，需手动 filter 进 binding 引用的 RuleID 集合 + host 自身全部 rule）
       - 实现要求：preset 必须 `enabled=true && (is_force_on || enabled)` 才算 active；rule 必须 binding.enabled=true
       - 返回聚合 input

    4. 4 个 handler 方法：

       **Preview (`POST /v1/admin/hosts/{hostID}/bypass/preview`)**：
       - path param hostID；先 GetHost 校验存在（404 + BYPASS_HOST_NOT_FOUND）
       - request body 可选 `{"include_pending_rule_ids": []string, "include_pending_preset_ids": []string}` —— 用于前端在本地未保存时模拟（**简化决定**：本 plan 不支持 include_pending_*，只读已落库 binding，前端在 Plan 03 用 React Query 失效刷新代替模拟）。
       - 调 collectRenderInput → RenderBypassConfig（prevSnapshot=GetLatestAppliedBypassSnapshot，未找到则 nil）
       - 返回 200 + body：
         ```json
         {
           "config_hash": "...",
           "version_current": 23,
           "version_next": 24,
           "whitelist_cidrs_rendered": {...},
           "whitelist_domains_rendered": {...},
           "nft_diff": "...",
           "risky_count": 2,
           "summary": "覆盖 12 条规则..."
         }
         ```
       - **不落库，不写 audit**

       **Apply (`POST /v1/admin/hosts/{hostID}/bypass/apply`)**：
       - path param hostID；GetHost 校验
       - request body `{"note": "..."}`（可选）
       - 重新 collectRenderInput → RenderBypassConfig
       - 计算 next version：`latestSnaps, _ := store.ListBypassSnapshotsByHost(ctx, hostID, 1)`，next=latestSnaps[0].Version+1（空则 1）
       - 调 `store.CreateBypassSnapshot(...)`，params.Source = `"apply"`：
         - 如果返回错误且 `strings.Contains(err.Error(), "unique") || strings.Contains(err.Error(), "host_bypass_snapshots_host_id_config_hash_key")`：
           - 视作幂等命中：查 list 找到该 config_hash 的现有 snapshot，返回 200 + 该 snapshot id（不写 audit，不 dispatch）
       - 成功后调 `h.actions.QueueHostAction(ctx, hostID, agentapi.ActionReloadHostBypass, snapshot.ID)`（task_payload 字段塞 snapshot.ID 字符串）；失败不阻塞主请求，但 logger.Error
       - 写 audit：`writeBypassAuditLog(ctx, h.logger, h.store, h.events, r, "apply", "snapshot", &snapshot.ID, prevSnapshot, snapshot, note)`（双轨 — 同时落 audit_log + events.RecordEvent("bypass.apply")）
       - 返回 201 + body：
         ```json
         {
           "snapshot_id": "...",
           "version": 24,
           "config_hash": "...",
           "applied_status": "pending",
           "task_id": "...",
           "message": "白名单变更不影响现有 TCP 连接，新连接才用新规则"
         }
         ```

       **Rollback (`POST /v1/admin/hosts/{hostID}/bypass/rollback`)** — **WARN-4 修复：不破坏 target snapshot 历史状态，新建一行**：
       - request body `{"target_snapshot_id": "..."}`（必填）
       - 步骤：
         1. `target, err := h.store.GetBypassSnapshotByID(ctx, target_snapshot_id)`；若不存在或 `target.HostID != hostID` → 404 + BYPASS_SNAPSHOT_NOT_FOUND（不暴露存在性）
         2. 校验 `target.AppliedStatus == "applied"`；否则 → 409 + BYPASS_SNAPSHOT_CONFLICT（"只能回滚到曾经应用过的快照"）
         3. `current, _ := h.store.GetLatestAppliedBypassSnapshot(ctx, hostID)`；如果 current.ID == target_snapshot_id 表示已经在该 snapshot → 200 + 现有结果（幂等，不新建行，不 dispatch，不写 audit）
         4. 计算 next version：`latestSnaps, _ := h.store.ListBypassSnapshotsByHost(ctx, hostID, 1)`，next=latestSnaps[0].Version+1
         5. **新建 snapshot 行**：`store.CreateBypassSnapshot(ctx, repository.CreateBypassSnapshotParams{ HostID: hostID, Version: next, ConfigHash: target.ConfigHash, WhitelistCIDRsJSON: target.WhitelistCIDRsJSON, WhitelistDomainsJSON: target.WhitelistDomainsJSON, Source: "rollback", CreatedBy: actorIDPtr(ctx) })`
            - 如果该 ConfigHash 触发 UNIQUE 约束（之前同 host 已有相同 hash 的 snapshot 行），新建行无法插入；此时退而求其次：**复制 target 的 config_hash + 添加 version 后缀**到 ConfigHash 字段（即 `target.ConfigHash + ":rollback:" + nextVersionStr`）以绕开 UNIQUE 约束 — 这是已知 trade-off，确保 rollback 总能产生一条新历史行
            - 该 trade-off 的代价：rollback 产生的 snapshot ConfigHash 不严格等于 target，幂等去重靠 (host_id, config_hash) UNIQUE 约束。Phase 47 真实接管时如需严格 hash，可改用 (host_id, config_hash, source) 复合 UNIQUE。本 plan 不改 schema。
         6. **不调用** `UpdateBypassSnapshotStatus(target_id, "pending")` — target 行的 applied_status 保持 "applied" 不动（WARN-4 核心修复点）
         7. dispatch ActionReloadHostBypass：`h.actions.QueueHostAction(ctx, hostID, agentapi.ActionReloadHostBypass, newSnapshot.ID)`（task_payload=新 snapshot.ID，不是 target.ID — Phase 47 worker 读这条 pending snapshot 下发）
         8. 写 audit：`writeBypassAuditLog(ctx, h.logger, h.store, h.events, r, "rollback", "snapshot", &newSnapshot.ID, current, target, "rollback_target_snapshot_id="+target_snapshot_id)`（双轨，note 字段显式带 target_snapshot_id 便于审计追溯）
         9. 返回 200 + body：
            ```json
            {
              "snapshot_id": "<新 snapshot id>",
              "rollback_target_snapshot_id": "<target id>",
              "version": <next>,
              "applied_status": "pending",
              "task_id": "...",
              "message": "回滚请求已下发，新版本 vN 将复用 vM 的配置"
            }
            ```
       - **语义重申：rollback 不破坏 target snapshot 历史状态；产出的是一条新 pending snapshot 行，Phase 47 worker 接管后才决定 applied 状态切换。target 行的 applied_status 字段在整个 rollback 流程内保持 "applied" 不变。**

       **Effective (`GET /v1/admin/hosts/{hostID}/bypass/effective`)**：
       - GetHost 校验
       - collectRenderInput → 返回 4 段：
         ```json
         {
           "presets_active": [...],
           "rules_active": [...],
           "whitelist_cidrs_rendered": {...},
           "whitelist_domains_rendered": {...}
         }
         ```
       - 不写 audit

    5. 测试 `admin_bypass_snapshots_test.go` ≥ 12 sub-test（**WARN-4 新增 rollback 语义校验**）：
       - preview 成功（hostID 存在）
       - preview 404（hostID 不存在）
       - apply 首次成功 + audit 落库 + QueueHostAction 被调用 + events.RecordEvent("bypass.apply") 被调用
       - apply 幂等命中（stub.CreateBypassSnapshot 返回 unique violation，handler 用 ListBypassSnapshotsByHost 找回原 snapshot 返回 200）
       - apply config_hash 与 preview 一致（同一规则集合产出同一 hash）
       - **rollback 成功**：stub.GetBypassSnapshotByID 返回 target（applied_status=applied） → 测试断言：
         - **target snapshot 的 applied_status 字段从未被 UpdateBypassSnapshotStatus 调用**（stub 计数器断言）
         - 新 snapshot 行被 CreateBypassSnapshot 创建（source="rollback"）
         - audit log note 字段含 `rollback_target_snapshot_id=` 前缀
         - QueueHostAction 调用的 payload 是新 snapshot.ID 不是 target.ID
         - events.RecordEvent("bypass.rollback") 被调用
       - rollback target 不存在 → 404 + BYPASS_SNAPSHOT_NOT_FOUND
       - rollback target 跨 host（target.HostID != hostID）→ 404（不暴露存在性）
       - rollback target.applied_status != "applied" → 409 + BYPASS_SNAPSHOT_CONFLICT
       - rollback target=current latest → 幂等 200（不新建行，不 dispatch，不写 audit）
       - effective 返回 4 段且 cidrs/domains 不为空（stub binding 含 1 个 preset + 1 个 rule）
       - effective host 不存在 → 404
       - 测试用 fake QueueHostAction 计数器断言被调用次数
  </action>
  <verify>
    <automated>cd . && go test ./internal/controlplane/http/ -run TestAdminBypassSnapshotsHandler -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestAdminBypassSnapshotsHandler -count=1 -v ./internal/controlplane/http/` 通过且 ≥ 12 子用例
    - `grep -c "ActionReloadHostBypass" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 2（apply + rollback）
    - `grep -c "QueueHostAction" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 2
    - `grep -c "config_hash" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 3
    - `grep -c "rollback_target_snapshot_id" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 1（WARN-4 audit note）
    - `grep -c "GetBypassSnapshotByID" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 1（WARN-4：rollback 先 GetByID）
    - **rollback 实现内不出现 UpdateBypassSnapshotStatus 调用**（grep `-c "UpdateBypassSnapshotStatus" admin_bypass_snapshots.go` 应为 0；只有 apply / Phase 47 worker 才该调）
    - `grep -c "source.*rollback\|\"rollback\"" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 1（CreateBypassSnapshotParams.Source = "rollback"）
    - `grep -c "h\\.events" internal/controlplane/http/admin_bypass_snapshots.go` ≥ 2（apply + rollback 双轨）
    - 幂等路径不重复写 audit（测试断言 audit row count 不增长）
    - `applied_status: "pending"` 在 apply 响应中可见
  </acceptance_criteria>
  <done>4 个 action endpoints 闭环；rollback 通过新建 snapshot 实现而非破坏 target 历史，Phase 47 接管 dispatch case 后由 worker 决定 applied 状态切换。events 字段在 apply/rollback 双轨实落地。</done>
</task>

<task type="auto">
  <name>Task 4: 实现 AdminBypassAuditLogHandler + cursor 分页</name>
  <files>
    internal/controlplane/http/admin_bypass_audit_log.go
    internal/controlplane/http/admin_bypass_audit_log_test.go
  </files>
  <read_first>
    - internal/controlplane/http/admin_events.go（cursor 风格参考；如果文件无 cursor 则使用 limit + before query param 模式）
    - internal/store/repository/queries_bypass.go 第 180-220 行（ListBypassAuditLogByTarget）
    - .planning/phases/46-api-ui/46-CONTEXT.md `### 审计日志写入` 节
  </read_first>
  <action>
    创建 `internal/controlplane/http/admin_bypass_audit_log.go`：

    1. store interface：
       ```go
       type AdminBypassAuditLogStore interface {
           ListBypassAuditLogByTarget(context.Context, string, string) ([]repository.BypassAuditLog, error)
       }
       ```

    2. handler `AdminBypassAuditLogHandler` + 构造函数。

    3. 单个方法 `ListByHost() http.Handler`：
       - path param `{hostID}`（target_kind="host"，target_id=hostID）
       - query params:
         - `?limit=20` 默认 20，最大 200（clamp）
         - `?before=<iso-timestamp>` cursor，仅返回 created_at < before 的 row
         - `?target_kind=` 可选覆盖（默认 "host"）
         - `?target_id=` 可选覆盖（默认 hostID）
       - 调 Repository ListBypassAuditLogByTarget(kind, id)，应用 limit + before filter（Repository 没接 cursor 参数，handler 在内存 filter —— 数据量小，可接受）
       - 返回 `{"audit_log": [...], "next_before": "<iso>"}`，next_before 为最后一行的 created_at；如果不足 limit 则 next_before=""

    4. 测试 `admin_bypass_audit_log_test.go` ≥ 4 sub-test：
       - list 默认 limit
       - before 过滤
       - limit 超过 200 → clamp 至 200
       - 空结果 → audit_log=[]、next_before=""
  </action>
  <verify>
    <automated>cd . && go test ./internal/controlplane/http/ -run TestAdminBypassAuditLogHandler -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestAdminBypassAuditLogHandler -count=1 -v ./internal/controlplane/http/` 通过且 ≥ 4 子用例
    - `grep -c "next_before\|before=" internal/controlplane/http/admin_bypass_audit_log.go` ≥ 2
    - `grep -c "limit\b" internal/controlplane/http/admin_bypass_audit_log.go` ≥ 3
    - clamp 逻辑：`limit > 200` 时实际只返回 200 行
  </acceptance_criteria>
  <done>前端可分页拉 audit log，feed bypass Tab 的「操作历史」抽屉。</done>
</task>

<task type="auto">
  <name>Task 5: 注册路由 + wire server.go（连接到 Repository）</name>
  <files>
    internal/controlplane/http/router.go
    internal/controlplane/server.go
  </files>
  <read_first>
    - internal/controlplane/http/router.go 全文（Dependencies struct + setupRoutes）
    - internal/controlplane/server.go（关注 Dependencies wire 代码，搜索 `AdminEgressIPs:` 和 `AdminBindings:`）
    - .planning/phases/46-api-ui/46-01-PLAN.md Task 5（Plan 01 已在 router.go 增字段）
  </read_first>
  <action>
    1. 修改 `internal/controlplane/http/router.go` Dependencies struct（增字段）：
       ```go
       AdminBypassSnapshots AdminBypassSnapshotStore
       AdminBypassAuditLog  AdminBypassAuditLogStore
       ```

    2. 在 router.go 内 Plan 01 加的 bypass 路由块之后，追加：
       ```go
       if deps.AdminBypassSnapshots != nil {
           sh := NewAdminBypassSnapshotsHandler(deps.Logger, deps.AdminBypassSnapshots, deps.HostActions, deps.EventRecorder)
           mux.Handle("POST /v1/admin/hosts/{hostID}/bypass/preview", adminGuard(sh.Preview()))
           mux.Handle("POST /v1/admin/hosts/{hostID}/bypass/apply", adminGuard(sh.Apply()))
           mux.Handle("POST /v1/admin/hosts/{hostID}/bypass/rollback", adminGuard(sh.Rollback()))
           mux.Handle("GET /v1/admin/hosts/{hostID}/bypass/effective", adminGuard(sh.Effective()))
       }
       if deps.AdminBypassAuditLog != nil {
           ah := NewAdminBypassAuditLogHandler(deps.Logger, deps.AdminBypassAuditLog)
           mux.Handle("GET /v1/admin/hosts/{hostID}/bypass/audit-log", adminGuard(ah.ListByHost()))
       }
       ```

    3. 修改 `internal/controlplane/server.go`：找到 `Dependencies{...}` 字面量初始化处（搜索 `AdminEgressIPs: repo,`），把 Plan 01 + Plan 02 所有 bypass store 字段都连到 `repo`（同一个 `*repository.Repository` 已实现全部方法）：
       ```go
       AdminBypassPresets:   repo,
       AdminBypassRules:     repo,
       AdminBypassBindings:  repo,
       AdminBypassSnapshots: repo,
       AdminBypassAuditLog:  repo,
       ```
       注意：如果 server.go 的 Dependencies 初始化是分行的（逐字段赋值），按现有缩进追加。如果 Repository 还缺 GetHost 接口签名（来自 admin_bypass_snapshots.go 的 AdminBypassSnapshotStore），用 grep 确认：`grep -n "GetHost" internal/store/repository/`，如果 GetHost 存在则直接用 `repo`；否则缩小 interface 到不含 GetHost（用 hosts list filter 代替）—— **推荐做法**：复用现有的 AdminHostStore 接口（已有 GetHost），handler 接 2 个 store interface：snapshot store + host getter（接受 AdminHostStore 子集）。

    4. 编译验证：`go build ./internal/controlplane/...`。如果出现接口不满足错误，调整 store interface 方法集到当前 Repository 的实际签名。
  </action>
  <verify>
    <automated>cd . && go build ./internal/controlplane/... && go test ./internal/controlplane/http/ -count=1 -short</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/controlplane/...` 通过
    - `go test ./internal/controlplane/http/ -count=1 -short` 全包 PASS
    - `grep -c "/v1/admin/hosts/{hostID}/bypass/" internal/controlplane/http/router.go` ≥ 5
    - `grep -c "AdminBypassPresets:\|AdminBypassRules:\|AdminBypassBindings:\|AdminBypassSnapshots:\|AdminBypassAuditLog:" internal/controlplane/server.go` ≥ 5
    - 路由 `POST /v1/admin/hosts/{hostID}/bypass/preview` 在 router 注册（grep 命中）
  </acceptance_criteria>
  <done>15+ 条 bypass 路由全部注册，server 启动后 admin 客户端可立即调用 preview / apply / rollback / effective / audit-log。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → /v1/admin/hosts/{hostID}/bypass/* | adminGuard JWT 校验 |
| handler → worker dispatch | QueueHostAction 走 task queue，host-agent 才是真实执行者 |
| Apply → DB | UNIQUE(host_id, config_hash) DB 约束兜底幂等 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | S | snapshot/audit-log handler | mitigate | adminGuard 包裹；测试 1 case 401 unauthenticated |
| T-46-02 | T | Apply 数据竞争（并发 POST 同 config_hash）| mitigate | DB UNIQUE 约束兜底；handler 捕获 unique violation 转幂等返回 |
| T-46-04 | T (Tampering) | rollback 越权回滚他人 host | mitigate | path param hostID 与 target_snapshot.host_id 必须匹配，handler 严格校验，不匹配 → 404 不暴露存在性 |
| T-46-04b | T (Tampering) | rollback 破坏 target snapshot 历史 | mitigate | Task 3 改用新建 snapshot 模式（source='rollback'），target 行的 applied_status 保持 "applied" 不变；不调 UpdateBypassSnapshotStatus(target.ID, "pending") |
| T-46-06 | E (Elevation) | 跳过 Apply 直接改 audit_log 表 | mitigate | Repository 不暴露原始 SQL；handler 是唯一写 audit 的入口 |
| T-46-09 | I (Info Disclosure) | preview 泄露其他 host 数据 | mitigate | path param hostID 隔离；handler collectRenderInput 仅查询当前 hostID 的 binding |
| T-46-10 | D (DoS) | 大 host 渲染巨型 JSON | mitigate | Phase 46 单 host 规则 ≤ 1000 条（Plan 01 BYPASS_LIMIT_EXCEEDED 已护拦）；JSON marshal 内存可控 |
| T-46-11 | R (Repudiation) | apply / rollback 无溯源 | mitigate | 所有动作通过 writeBypassAuditLog 双轨：host_bypass_audit_log 行（actor_id / actor_ip / before / after / note 都落 JSONB）+ EventRecorder.RecordEvent("bypass.apply" / "bypass.rollback") 异步事件流（per CONTEXT.md L107）|
</threat_model>

<verification>
```bash
# 全包编译
cd .
go build ./internal/controlplane/... ./internal/agentapi/... ./internal/runtime/...

# Plan 02 测试套件
go test ./internal/controlplane/http/ -count=1 -run 'TestRenderBypassConfig|TestAdminBypassSnapshotsHandler|TestAdminBypassAuditLogHandler' -v

# 全包回归
go test ./internal/controlplane/http/ ./internal/agentapi/ ./internal/runtime/tasks/ -count=1 -short

# 文件/grep 完整性
grep -rn "ActionReloadHostBypass" internal/                            # ≥ 3
grep -c "Phase 46 placeholder; no-op until Phase 47" internal/runtime/tasks/worker.go  # ≥ 1（WARN-3 字面对齐）
grep -c "/v1/admin/hosts/{hostID}/bypass/" internal/controlplane/http/router.go  # ≥ 5
grep -c "BypassRender\|RenderBypassConfig" internal/controlplane/http/  # ≥ 4
grep -c "rollback_target_snapshot_id" internal/controlplane/http/admin_bypass_snapshots.go  # ≥ 1（WARN-4 audit note）
```
</verification>

<success_criteria>
1. `internal/agentapi/contracts.go` 暴露 `ActionReloadHostBypass HostAction = "reload_host_bypass"`
2. worker dispatch 命中 ActionReloadHostBypass 时仅写日志字面 "Phase 46 placeholder; no-op until Phase 47"（WARN-3 字面与 truths #6 对齐），不执行真实 reload
3. 5 个新端点全部可用：preview / apply / rollback / effective / audit-log
4. Apply config_hash 幂等：重复 POST 同 hash 返回 200 + 现有 snapshot id，不重复写库不重复 dispatch
5. Rollback 不破坏 target snapshot 历史状态（WARN-4 核心修复）：新建一条 source='rollback' 的 pending snapshot，target 行的 applied_status 保持 "applied"；audit log note 含 `rollback_target_snapshot_id=<id>` 前缀
6. Rollback 目标 snapshot 与 host 严格匹配，越权 → 404 不暴露存在性
7. RenderBypassConfig 是纯函数，输出与 Phase 45 sing-box rule-set 文件格式 100% 兼容（`{"version":3,"rules":[...]}`）
8. Audit log cursor 分页 limit 默认 20、最大 200
9. 所有写操作（apply / rollback）通过 writeBypassAuditLog 双轨：audit_log + events.RecordEvent，actor_id / actor_ip / before / after 完整
</success_criteria>

<output>
完成后写 `.planning/phases/46-api-ui/46-02-SUMMARY.md`，必含：
- 5 个新端点请求/响应 body schema（Plan 03/04 前端 client 直接参考）
- ActionReloadHostBypass dispatch 占位代码位置（行号 + Phase 47 接管点说明 + 字面日志「Phase 46 placeholder; no-op until Phase 47」）
- config_hash 幂等路径的测试 case 引用
- **Rollback 新建 snapshot 模式的语义说明**（WARN-4）：target 行 applied_status 不变 + 新行 source='rollback' applied_status='pending' + audit note 含 rollback_target_snapshot_id
- writeBypassAuditLog 双轨调用位置（apply / rollback 各 1 处）
- RenderBypassConfig 输入/输出契约（Plan 03 预览 Sheet 用同一 JSON）
</output>
</output>
