---
phase: 46-api-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 46-02
  - 46-03
files_modified:
  - web/admin/src/components/ui/progress.tsx
  - web/admin/src/components/ui/scroll-area.tsx
  - web/admin/src/hooks/use-bypass-snapshots.ts
  - web/admin/src/lib/api/bypass.ts
  - web/admin/src/lib/api/types/bypass.ts
  - web/admin/src/components/bypass/preview-sheet.tsx
  - web/admin/src/components/bypass/json-viewer.tsx
  - web/admin/src/components/bypass/nft-diff-viewer.tsx
  - web/admin/src/components/bypass/apply-progress-dialog.tsx
  - web/admin/src/components/bypass/rollback-confirm-dialog.tsx
  - web/admin/src/components/bypass/bypass-tab.tsx
  - web/admin/src/components/bypass/__tests__/preview-sheet.test.tsx
  - web/admin/src/components/bypass/__tests__/apply-progress-dialog.test.tsx
autonomous: true
requirements:
  - BYPASS-UI-04
  - BYPASS-UI-05
must_haves:
  truths:
    - BypassTab 右下角浮现 sticky "查看生效预览" 按钮，点击后右侧滑出 640px 宽 Sheet
    - Sheet 顶部显示 "v23 → v24 · 覆盖 N 条规则" 摘要，含两个 Tab："sing-box JSON" / "nft set diff"
    - JSONViewer 在内容 > 10000 行时折叠 + 显示「点击展开完整内容」按钮
    - NftDiffViewer 用 `+ 绿色` / `- 红色` / 上下文默认色渲染 unified diff
    - 主按钮「应用此配置」点击后 ApplyProgressDialog 弹出 5 阶段步骤条（生成快照 / 下发到 agent / Reload 配置 / 健康检查 / 完成）
    - apply mutation 后端返回的 task_id 经 useTaskPolling + useSSE 拉取进度（沿用 $hostId.tsx 升级镜像模式）
    - 任意步骤失败：Dialog 保持开启 + 显示错误码中文文案 + "关闭" 按钮，toast 不触发
    - rollback 触发 RollbackConfirmDialog，要求输入 host slug 严格匹配后才能确认；匹配前按钮 disabled
    - 风险摘要 > 5 条高风险时主按钮变 warning 色且文案变为「应用此配置（含 N 条高风险）」
  artifacts:
    - path: web/admin/src/components/bypass/preview-sheet.tsx
      provides: 右侧 Sheet 容器 + 双 Tab + 摘要 + 应用按钮
      min_lines: 180
    - path: web/admin/src/components/bypass/json-viewer.tsx
      provides: JSON 只读展示 + 10000 行折叠
      min_lines: 60
    - path: web/admin/src/components/bypass/nft-diff-viewer.tsx
      provides: unified diff 渲染（绿+ / 红- / context）
      min_lines: 50
    - path: web/admin/src/components/bypass/apply-progress-dialog.tsx
      provides: 5 阶段步骤条 + 任务轮询
      min_lines: 200
    - path: web/admin/src/components/bypass/rollback-confirm-dialog.tsx
      provides: host slug 输入二次确认
      min_lines: 80
    - path: web/admin/src/hooks/use-bypass-snapshots.ts
      provides: usePreviewBypass / useApplyBypass / useRollbackBypass / useEffectiveBypass / useAuditLog
      min_lines: 100
  key_links:
    - from: preview-sheet.tsx
      to: /v1/admin/hosts/{hostID}/bypass/preview
      via: usePreviewBypass mutation
      pattern: "preview"
    - from: apply-progress-dialog.tsx
      to: task progress
      via: useTaskPolling + useSSE
      pattern: "useSSE\\|useTaskPolling"
    - from: bypass-tab.tsx
      to: PreviewSheet + ApplyProgressDialog
      via: 在 Plan 03 预留的注释位置插入
      pattern: "PreviewSheet"
---

<objective>
落地 Phase 46 前端的「预览 + 应用 + 回滚」闭环。复用 Plan 02 的 preview / apply / rollback / effective API + Plan 03 的 BypassTab 容器 + $hostId.tsx 的 useSSE/useTaskPolling 模式。

Purpose: 让管理员能预览即将应用的配置（JSON + nft diff）、可视化追踪应用进度、安全回滚到历史快照。
Output: 5 个新 React 组件 + 1 个 hook 文件 + 2 个 shadcn primitive (progress / scroll-area) + BypassTab 集成 + 测试。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/46-api-ui/46-CONTEXT.md
@.planning/phases/46-api-ui/46-UI-SPEC.md
@.planning/phases/46-api-ui/46-02-PLAN.md
@.planning/phases/46-api-ui/46-03-PLAN.md

# 必读参考
@web/admin/src/routes/_dashboard/hosts/$hostId.tsx (升级镜像 Dialog 模式，行 453-620)
@web/admin/src/hooks/use-sse.ts
@web/admin/src/hooks/use-tasks.ts
@web/admin/src/components/bypass/bypass-tab.tsx (Plan 03 产物，预留 PreviewSheet 注释位置)
@web/admin/src/lib/api/bypass.ts (Plan 03 产物，本 plan 扩展)

<interfaces>
<!-- Plan 02 已落地的快照 API -->

## API

```
POST /v1/admin/hosts/{hostID}/bypass/preview     → BypassPreviewResponse
POST /v1/admin/hosts/{hostID}/bypass/apply       → BypassApplyResponse (含 task_id, snapshot_id, applied_status: "pending")
POST /v1/admin/hosts/{hostID}/bypass/rollback    → BypassRollbackResponse (含 task_id)
GET  /v1/admin/hosts/{hostID}/bypass/effective   → BypassEffectiveResponse
GET  /v1/admin/hosts/{hostID}/bypass/audit-log?limit=20&before=<iso>  → { audit_log, next_before }
```

## 响应类型（Plan 02 已定义）

```ts
interface BypassPreviewResponse {
  config_hash: string;
  version_current: number;
  version_next: number;
  whitelist_cidrs_rendered: { version: number; rules: any[] };
  whitelist_domains_rendered: { version: number; rules: any[] };
  nft_diff: string;
  risky_count: number;
  summary: string;
}
interface BypassApplyResponse {
  snapshot_id: string;
  version: number;
  config_hash: string;
  applied_status: "pending"|"applied"|"failed"|"rolled_back";
  task_id: string;
  message: string;
}
interface BypassRollbackResponse {
  snapshot_id: string;
  task_id: string;
  message: string;
}
interface BypassEffectiveResponse {
  presets_active: BypassPreset[];
  rules_active: BypassRule[];
  whitelist_cidrs_rendered: { version: number; rules: any[] };
  whitelist_domains_rendered: { version: number; rules: any[] };
}
```

## 现有 task 轮询模式（$hostId.tsx 行 113-127 升级镜像参考）

```ts
const [taskId, setTaskId] = useState<string | null>(null);
const { data: task } = useTaskPolling(taskId);
useSSE(`${window.location.origin}/v1/admin/sse?topics=tasks`, (msg) => {
  if (msg.topic === "tasks" && msg.action === "progress" && msg.id === taskId) {
    // 处理 progress
  }
});
// task.status ∈ "pending"|"running"|"succeeded"|"failed"|"canceled"
```

**注意**：Phase 46 ActionReloadHostBypass 是 placeholder（Plan 02 Task 1：worker dispatch 命中直接 return nil），即任务会立即变 `succeeded`。Apply Dialog 在 Phase 46 的体验：5 个阶段会以接近 0 延迟全部跳到 ✓，这是已知行为，Phase 47 接管后阶段切换才有真实耗时。本 plan UI 设计为「无论真实耗时如何，5 阶段都能正确渲染」。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 progress / scroll-area primitive + 扩展 API client / types / hooks</name>
  <files>
    web/admin/src/components/ui/progress.tsx
    web/admin/src/components/ui/scroll-area.tsx
    web/admin/src/lib/api/types/bypass.ts
    web/admin/src/lib/api/bypass.ts
    web/admin/src/hooks/use-bypass-snapshots.ts
  </files>
  <read_first>
    - web/admin/src/lib/api/bypass.ts (Plan 03 产物)
    - web/admin/src/lib/api/types/bypass.ts (Plan 03 产物)
    - web/admin/src/hooks/use-bypass-rules.ts (Plan 03 产物，参考 mutation hook 风格)
  </read_first>
  <action>
    1. 执行（在 `web/admin/`）：
       ```bash
       cd web/admin && npx shadcn@latest add progress scroll-area
       ```
       生成 `src/components/ui/progress.tsx` 和 `scroll-area.tsx`。失败则手动从 shadcn new-york 风格源码贴入。

    2. 扩展 `web/admin/src/lib/api/types/bypass.ts`，在文件末尾追加：
       ```ts
       export interface BypassPreviewResponse {
         config_hash: string;
         version_current: number;
         version_next: number;
         whitelist_cidrs_rendered: { version: number; rules: any[] };
         whitelist_domains_rendered: { version: number; rules: any[] };
         nft_diff: string;
         risky_count: number;
         summary: string;
       }
       export interface BypassApplyResponse {
         snapshot_id: string;
         version: number;
         config_hash: string;
         applied_status: "pending" | "applied" | "failed" | "rolled_back";
         task_id: string;
         message: string;
       }
       export interface BypassRollbackResponse {
         snapshot_id: string;
         task_id: string;
         message: string;
       }
       export interface BypassEffectiveResponse {
         presets_active: BypassPreset[];
         rules_active: BypassRule[];
         whitelist_cidrs_rendered: { version: number; rules: any[] };
         whitelist_domains_rendered: { version: number; rules: any[] };
       }
       export interface BypassAuditLog {
         id: string;
         actor_id: string | null;
         actor_ip: string;
         action: string;
         target_kind: string;
         target_id: string | null;
         before: unknown;
         after: unknown;
         note: string;
         created_at: string;
       }
       export interface BypassAuditLogResponse {
         audit_log: BypassAuditLog[];
         next_before: string;
       }
       ```

    3. 扩展 `web/admin/src/lib/api/bypass.ts`，在 `bypassApi` 对象末尾追加：
       ```ts
       preview: (hostId: string) =>
         apiFetch<BypassPreviewResponse>(`/hosts/${hostId}/bypass/preview`, { method: "POST", body: "{}" }),
       apply: (hostId: string, note?: string) =>
         apiFetch<BypassApplyResponse>(`/hosts/${hostId}/bypass/apply`, { method: "POST", body: JSON.stringify({ note: note ?? "" }) }),
       rollback: (hostId: string, targetSnapshotId: string) =>
         apiFetch<BypassRollbackResponse>(`/hosts/${hostId}/bypass/rollback`, { method: "POST", body: JSON.stringify({ target_snapshot_id: targetSnapshotId }) }),
       effective: (hostId: string) =>
         apiFetch<BypassEffectiveResponse>(`/hosts/${hostId}/bypass/effective`),
       auditLog: (hostId: string, opts?: { limit?: number; before?: string }) => {
         const q = new URLSearchParams();
         if (opts?.limit) q.set("limit", String(opts.limit));
         if (opts?.before) q.set("before", opts.before);
         const qs = q.toString();
         return apiFetch<BypassAuditLogResponse>(`/hosts/${hostId}/bypass/audit-log${qs ? "?" + qs : ""}`);
       },
       ```
       记得在文件顶部 import 新加的类型。

    4. 创建 `web/admin/src/hooks/use-bypass-snapshots.ts`：
       ```ts
       import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
       import { bypassApi } from "@/lib/api/bypass";

       export function usePreviewBypass(hostId: string) {
         return useMutation({ mutationFn: () => bypassApi.preview(hostId) });
       }

       export function useApplyBypass(hostId: string) {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: (note?: string) => bypassApi.apply(hostId, note),
           onSuccess: () => {
             qc.invalidateQueries({ queryKey: ["bypass", "bindings", hostId] });
             qc.invalidateQueries({ queryKey: ["bypass", "effective", hostId] });
           },
         });
       }

       export function useRollbackBypass(hostId: string) {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: (targetSnapshotId: string) => bypassApi.rollback(hostId, targetSnapshotId),
           onSuccess: () => qc.invalidateQueries({ queryKey: ["bypass"] }),
         });
       }

       export function useEffectiveBypass(hostId: string) {
         return useQuery({
           queryKey: ["bypass", "effective", hostId],
           queryFn: () => bypassApi.effective(hostId),
           staleTime: 30_000,
         });
       }

       export function useBypassAuditLog(hostId: string, before?: string) {
         return useQuery({
           queryKey: ["bypass", "audit-log", hostId, before ?? ""],
           queryFn: () => bypassApi.auditLog(hostId, { limit: 20, before }),
           staleTime: 30_000,
         });
       }
       ```
  </action>
  <verify>
    <automated>cd web/admin && pnpm typecheck && test -f src/components/ui/progress.tsx && test -f src/components/ui/scroll-area.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `web/admin/src/components/ui/progress.tsx` 存在且 export Progress
    - `web/admin/src/components/ui/scroll-area.tsx` 存在且 export ScrollArea
    - `bypassApi` 含 preview / apply / rollback / effective / auditLog 5 个新方法
    - use-bypass-snapshots.ts 含 5 个 hook
    - `pnpm typecheck` 通过
    - `grep -c 'preview\|apply\|rollback\|effective\|auditLog' src/lib/api/bypass.ts` ≥ 5
  </acceptance_criteria>
  <done>Plan 02 5 个新端点全部有前端调用入口。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 JSONViewer + NftDiffViewer</name>
  <files>
    web/admin/src/components/bypass/json-viewer.tsx
    web/admin/src/components/bypass/nft-diff-viewer.tsx
  </files>
  <read_first>
    - web/admin/src/components/ui/scroll-area.tsx (本 plan Task 1 产物)
    - .planning/phases/46-api-ui/46-UI-SPEC.md `### 预览面板` + nft diff 颜色规范
  </read_first>
  <action>
    1. 创建 `web/admin/src/components/bypass/json-viewer.tsx`：
       ```tsx
       import { useMemo, useState } from "react";
       import { ScrollArea } from "@/components/ui/scroll-area";
       import { Button } from "@/components/ui/button";

       interface JSONViewerProps {
         value: unknown;
       }

       export function JSONViewer({ value }: JSONViewerProps) {
         const text = useMemo(() => JSON.stringify(value, null, 2), [value]);
         const lineCount = text.split("\n").length;
         const [expanded, setExpanded] = useState(false);
         const needsFold = lineCount > 10000;

         if (needsFold && !expanded) {
           return (
             <div className="rounded-md border bg-muted/30 p-6 text-center">
               <p className="text-sm text-muted-foreground mb-3">
                 配置超过 1 万行（{lineCount.toLocaleString()} 行），点击展开完整内容（可能影响浏览器性能）
               </p>
               <Button size="sm" variant="outline" onClick={() => setExpanded(true)}>
                 展开完整内容
               </Button>
             </div>
           );
         }

         return (
           <ScrollArea className="h-[60vh] rounded-md border bg-slate-950">
             <pre className="p-4 font-mono text-xs leading-relaxed text-slate-100 whitespace-pre">
               {text}
             </pre>
           </ScrollArea>
         );
       }
       ```

    2. 创建 `web/admin/src/components/bypass/nft-diff-viewer.tsx`：
       ```tsx
       import { ScrollArea } from "@/components/ui/scroll-area";

       interface NftDiffViewerProps {
         diff: string;
       }

       export function NftDiffViewer({ diff }: NftDiffViewerProps) {
         if (!diff.trim()) {
           return (
             <div className="rounded-md border bg-muted/30 py-12 text-center text-sm text-muted-foreground">
               无差异（首次应用或规则集合未变更）
             </div>
           );
         }
         const lines = diff.split("\n");
         return (
           <ScrollArea className="h-[60vh] rounded-md border bg-slate-950">
             <pre className="p-4 font-mono text-xs leading-relaxed whitespace-pre">
               {lines.map((line, i) => {
                 let cls = "text-slate-300";
                 if (line.startsWith("+")) cls = "text-emerald-400";
                 else if (line.startsWith("-")) cls = "text-red-400";
                 else if (line.startsWith("#")) cls = "text-slate-500";
                 return (
                   <div key={i} className={cls}>{line || " "}</div>
                 );
               })}
             </pre>
           </ScrollArea>
         );
       }
       ```
  </action>
  <verify>
    <automated>cd web/admin && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - json-viewer.tsx 在 lineCount > 10000 时折叠 + 显示"展开完整内容"按钮
    - nft-diff-viewer.tsx 用 emerald-400 渲染 + 行、red-400 渲染 - 行、slate-500 渲染注释行
    - 空 diff 显示空状态文案
    - `pnpm typecheck` 通过
  </acceptance_criteria>
  <done>预览面板的两个视图组件就绪，PreviewSheet 可直接 import。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 PreviewSheet（双 Tab 切换 + 应用主按钮）+ 测试</name>
  <files>
    web/admin/src/components/bypass/preview-sheet.tsx
    web/admin/src/components/bypass/__tests__/preview-sheet.test.tsx
  </files>
  <read_first>
    - web/admin/src/components/ui/sheet.tsx
    - web/admin/src/components/ui/tabs.tsx (Plan 03 产物)
    - 本 plan Task 2 产物（json-viewer.tsx + nft-diff-viewer.tsx）
    - .planning/phases/46-api-ui/46-UI-SPEC.md `### 预览面板（Sheet）` + 状态机
  </read_first>
  <action>
    创建 `web/admin/src/components/bypass/preview-sheet.tsx`：

    ```tsx
    import { useEffect } from "react";
    import { toast } from "sonner";
    import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@/components/ui/sheet";
    import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
    import { Button } from "@/components/ui/button";
    import { Loader2 } from "lucide-react";
    import { usePreviewBypass } from "@/hooks/use-bypass-snapshots";
    import { parseBypassApiError } from "@/lib/i18n/error-codes";
    import { JSONViewer } from "./json-viewer";
    import { NftDiffViewer } from "./nft-diff-viewer";
    import type { BypassPreviewResponse } from "@/lib/api/types/bypass";

    interface PreviewSheetProps {
      hostId: string;
      open: boolean;
      onOpenChange: (open: boolean) => void;
      onApply: (preview: BypassPreviewResponse) => void;
    }

    export function PreviewSheet({ hostId, open, onOpenChange, onApply }: PreviewSheetProps) {
      const previewMutation = usePreviewBypass(hostId);

      useEffect(() => {
        if (open && !previewMutation.data && !previewMutation.isPending) {
          previewMutation.mutate(undefined, {
            onError: (err: any) => {
              const { message } = parseBypassApiError(err?.message ?? "");
              toast.error(message);
            },
          });
        }
        if (!open) {
          previewMutation.reset();
        }
      }, [open]);

      const preview = previewMutation.data;
      const isHighRisk = (preview?.risky_count ?? 0) > 5;

      return (
        <Sheet open={open} onOpenChange={onOpenChange}>
          <SheetContent className="w-[640px] sm:max-w-[640px] flex flex-col">
            <SheetHeader>
              <SheetTitle className="text-xl font-semibold tracking-tight">预览生效配置</SheetTitle>
              {preview && (
                <SheetDescription className="font-mono">
                  v{preview.version_current} → v{preview.version_next} · {preview.summary}
                </SheetDescription>
              )}
            </SheetHeader>

            {previewMutation.isPending ? (
              <div className="flex flex-1 items-center justify-center">
                <Loader2 className="h-6 w-6 animate-spin text-primary" />
                <span className="ml-2 text-sm text-muted-foreground">正在生成预览…</span>
              </div>
            ) : previewMutation.isError ? (
              <div className="flex flex-1 items-center justify-center text-destructive text-sm">
                预览生成失败，请关闭重试
              </div>
            ) : preview ? (
              <div className="flex-1 overflow-hidden flex flex-col gap-4 mt-4">
                <Tabs defaultValue="json" className="flex-1 flex flex-col">
                  <TabsList>
                    <TabsTrigger value="json">sing-box JSON</TabsTrigger>
                    <TabsTrigger value="nft">nft set diff</TabsTrigger>
                  </TabsList>
                  <TabsContent value="json" className="flex-1 mt-2">
                    <JSONViewer value={{
                      "whitelist-cidrs.json": preview.whitelist_cidrs_rendered,
                      "whitelist-domains.json": preview.whitelist_domains_rendered,
                    }} />
                  </TabsContent>
                  <TabsContent value="nft" className="flex-1 mt-2">
                    <NftDiffViewer diff={preview.nft_diff} />
                  </TabsContent>
                </Tabs>

                <div className="rounded-md border bg-muted/30 p-3">
                  <p className="text-sm font-semibold mb-1">风险报告</p>
                  <p className="text-xs text-muted-foreground">
                    {preview.risky_count === 0 ? "无风险项" : `含 ${preview.risky_count} 条高风险规则`}
                  </p>
                </div>

                <div className="flex gap-2 justify-end pt-2 border-t">
                  <Button variant="outline" onClick={() => onOpenChange(false)}>取消</Button>
                  <Button
                    className={isHighRisk ? "bg-warning text-warning-foreground hover:bg-warning/90" : ""}
                    onClick={() => onApply(preview)}
                  >
                    {isHighRisk ? `应用此配置（含 ${preview.risky_count} 条高风险）` : "应用此配置"}
                  </Button>
                </div>
              </div>
            ) : null}
          </SheetContent>
        </Sheet>
      );
    }
    ```

    测试 `preview-sheet.test.tsx` ≥ 5 case：
    - 打开 Sheet 自动调 preview mutation
    - 加载完成显示 "v23 → v24 · 覆盖 12 条规则"（mock preview return）
    - Tab 切换 "json" / "nft" 显示不同 viewer
    - risky_count > 5 时主按钮文案变 "应用此配置（含 X 条高风险）" 且带 warning 色 className
    - 点击 "应用此配置" 调 onApply(preview)
  </action>
  <verify>
    <automated>cd web/admin && pnpm typecheck && pnpm test:unit -- src/components/bypass/__tests__/preview-sheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PreviewSheet 宽 640px (`grep -c "w-\[640px\]"` ≥ 1)
    - 双 Tab 切换 "sing-box JSON" / "nft set diff"
    - risky_count > 5 时主按钮加 warning className 且文案变化
    - 风险摘要区显示「无风险项」或「含 N 条高风险规则」
    - 测试 ≥ 5 case PASS
  </acceptance_criteria>
  <done>预览面板闭环，PreviewSheet 把 onApply callback 暴露给外层调用方（BypassTab）触发 ApplyProgressDialog。</done>
</task>

<task type="auto">
  <name>Task 4: 实现 ApplyProgressDialog（5 阶段步骤条 + 任务轮询）+ 测试</name>
  <files>
    web/admin/src/components/bypass/apply-progress-dialog.tsx
    web/admin/src/components/bypass/__tests__/apply-progress-dialog.test.tsx
  </files>
  <read_first>
    - web/admin/src/routes/_dashboard/hosts/$hostId.tsx 行 453-620 （升级镜像 Dialog 完整模式）
    - web/admin/src/hooks/use-tasks.ts（useTaskPolling 签名 — **WARN-6：必须 `grep -n "progress" web/admin/src/hooks/use-tasks.ts` 确认 task 实体的进度字段实际名称：是 `task.progress_percent` 还是 `task.progress` 还是 `task.percent`？本 plan 内 stageStatuses 状态机假设字段名 `progress_percent`；若实际字段名不同，必须相应调整 stageStatuses useMemo 内的字段引用，并同步更新 ApplyProgressDialog 内任何 task.progress_percent 字面引用。如该字段不存在（task 实体仅有 status 而无进度数字），则把 stageStatuses 状态机降级为「无 taskId / running / succeeded / failed」四态映射，不依赖 percent 数值**）
    - web/admin/src/hooks/use-sse.ts (useSSE 签名)
    - .planning/phases/46-api-ui/46-UI-SPEC.md `### 应用进度 Dialog` + `### 应用进度反馈` 节
  </read_first>
  <action>
    创建 `web/admin/src/components/bypass/apply-progress-dialog.tsx`：

    ```tsx
    import { useEffect, useMemo, useState } from "react";
    import { toast } from "sonner";
    import { Loader2, CheckCircle2, XCircle, Circle } from "lucide-react";
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog";
    import { Button } from "@/components/ui/button";
    import { useApplyBypass } from "@/hooks/use-bypass-snapshots";
    import { useTaskPolling } from "@/hooks/use-tasks";
    import { useSSE } from "@/hooks/use-sse";
    import { parseBypassApiError, bypassErrorMessage } from "@/lib/i18n/error-codes";

    const STAGES = [
      { key: "snapshot", label: "生成快照" },
      { key: "dispatch", label: "下发到 agent" },
      { key: "reload",   label: "Reload 配置" },
      { key: "health",   label: "健康检查" },
      { key: "done",     label: "完成" },
    ] as const;

    type StageStatus = "pending" | "active" | "done" | "failed";

    interface ApplyProgressDialogProps {
      hostId: string;
      open: boolean;
      onOpenChange: (open: boolean) => void;
      /** PreviewSheet 计算出来的 risky_count，用于 toast 成功文案；不参与状态机 */
      riskyCount?: number;
    }

    export function ApplyProgressDialog({ hostId, open, onOpenChange, riskyCount }: ApplyProgressDialogProps) {
      const applyMutation = useApplyBypass(hostId);
      const [taskId, setTaskId] = useState<string | null>(null);
      const [errorCode, setErrorCode] = useState<string | null>(null);
      const [autoCloseScheduled, setAutoCloseScheduled] = useState(false);

      const { data: task } = useTaskPolling(taskId);

      useSSE(`${window.location.origin}/v1/admin/sse?topics=tasks`, (msg) => {
        if (msg.topic === "tasks" && msg.id === taskId) {
          // SSE 事件通知；progress 字段由 useTaskPolling 已经覆盖，本 SSE 只是更早唤醒 UI
        }
      });

      useEffect(() => {
        if (open && !applyMutation.data && !applyMutation.isPending) {
          applyMutation.mutate(undefined, {
            onSuccess: (resp) => {
              setTaskId(resp.task_id);
            },
            onError: (err: any) => {
              const { code } = parseBypassApiError(err?.message ?? "");
              setErrorCode(code);
            },
          });
        }
        if (!open) {
          applyMutation.reset();
          setTaskId(null);
          setErrorCode(null);
          setAutoCloseScheduled(false);
        }
      }, [open]);

      const taskStatus = task?.status;
      const isRunning = taskId !== null && taskStatus !== "succeeded" && taskStatus !== "failed" && taskStatus !== "canceled";
      const isDone = taskStatus === "succeeded";
      const isFailed = taskStatus === "failed" || taskStatus === "canceled" || !!errorCode;

      // 5 阶段状态映射：
      // - 无 taskId：snapshot=active，其它 pending
      // - 有 taskId 且 running：snapshot=done，dispatch=active，其它 pending
      // - succeeded：全部 done
      // - failed：当前阶段 failed，前面阶段 done
      const stageStatuses: StageStatus[] = useMemo(() => {
        if (errorCode) return ["failed", "pending", "pending", "pending", "pending"];
        if (!taskId) return ["active", "pending", "pending", "pending", "pending"];
        if (isDone) return ["done", "done", "done", "done", "done"];
        if (isFailed) {
          // 简单策略：第 2 阶段失败（dispatch 失败最常见）
          return ["done", "failed", "pending", "pending", "pending"];
        }
        if (isRunning) {
          const pct = task?.progress_percent ?? 0;
          if (pct < 25) return ["done", "active", "pending", "pending", "pending"];
          if (pct < 50) return ["done", "done", "active", "pending", "pending"];
          if (pct < 75) return ["done", "done", "done", "active", "pending"];
          return ["done", "done", "done", "done", "active"];
        }
        return ["active", "pending", "pending", "pending", "pending"];
      }, [errorCode, taskId, isDone, isFailed, isRunning, task?.progress_percent]);

      // 全部 done 时自动 500ms 关闭 + toast
      useEffect(() => {
        if (isDone && !autoCloseScheduled) {
          setAutoCloseScheduled(true);
          const timer = setTimeout(() => {
            onOpenChange(false);
            toast.success("已应用 · 白名单变更不影响现有 TCP 连接，新连接才用新规则");
          }, 500);
          return () => clearTimeout(timer);
        }
      }, [isDone, autoCloseScheduled, onOpenChange]);

      const failureMessage = errorCode
        ? bypassErrorMessage(errorCode)
        : task?.last_error_summary || "应用过程出现错误";
      const failureCodeLabel = errorCode || "TASK_FAILED";

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="sm:max-w-md">
            <DialogHeader>
              <DialogTitle>应用白名单配置</DialogTitle>
            </DialogHeader>

            <div className="space-y-3 py-4">
              {STAGES.map((stage, i) => {
                const status = stageStatuses[i];
                const Icon =
                  status === "active" ? Loader2 :
                  status === "done"   ? CheckCircle2 :
                  status === "failed" ? XCircle :
                  Circle;
                const iconCls =
                  status === "active" ? "text-primary animate-spin" :
                  status === "done"   ? "text-success" :
                  status === "failed" ? "text-destructive" :
                  "text-muted-foreground";
                const textCls =
                  status === "active" ? "text-primary font-semibold" :
                  status === "done"   ? "text-muted-foreground" :
                  status === "failed" ? "text-destructive font-semibold" :
                  "text-muted-foreground";
                return (
                  <div key={stage.key} className="flex items-center gap-3">
                    <Icon className={`h-5 w-5 ${iconCls}`} />
                    <span className={`text-sm ${textCls}`}>{stage.label}</span>
                  </div>
                );
              })}
            </div>

            {isFailed && (
              <div className="rounded-md border border-destructive/30 bg-destructive/5 p-3">
                <p className="text-sm font-semibold text-destructive">应用失败</p>
                <p className="mt-1 text-xs text-destructive/80">
                  {failureMessage} · 错误码：<span className="font-mono">{failureCodeLabel}</span>
                </p>
              </div>
            )}

            {(isFailed || isDone) && (
              <DialogFooter>
                <Button onClick={() => onOpenChange(false)}>关闭</Button>
              </DialogFooter>
            )}
          </DialogContent>
        </Dialog>
      );
    }
    ```

    测试 `apply-progress-dialog.test.tsx` ≥ 5 case：
    - 打开 Dialog 自动调 apply mutation
    - mutation onSuccess 设置 taskId
    - task.progress_percent < 25 时只有第 1/2 阶段渲染（dispatch 是 active）
    - task.status="succeeded" → 全部 done 图标 + 500ms 后 onOpenChange(false) 被调 + toast.success 命中
    - apply mutation onError BYPASS_HOST_UNREACHABLE → 显示中文文案 "host-agent 当前不可达..." + 错误码标签
  </action>
  <verify>
    <automated>cd web/admin && pnpm typecheck && pnpm test:unit -- src/components/bypass/__tests__/apply-progress-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apply-progress-dialog.tsx 渲染 5 个固定阶段：生成快照 / 下发到 agent / Reload 配置 / 健康检查 / 完成
    - 任务 succeeded → 自动 500ms 关闭 + toast.success
    - 任务 failed → Dialog 保持开启 + 错误码中文文案 + 关闭按钮
    - 用 Loader2 / CheckCircle2 / XCircle / Circle 4 个 lucide icon 区分状态
    - 测试 ≥ 5 case PASS
  </acceptance_criteria>
  <done>应用闭环：调 apply → 5 阶段进度 → 成功 toast / 失败错误码。Phase 47 接管 dispatch 后 task progress 真实驱动阶段切换。</done>
</task>

<task type="auto">
  <name>Task 5: 实现 RollbackConfirmDialog + BypassTab 集成 PreviewSheet/ApplyProgressDialog</name>
  <files>
    web/admin/src/components/bypass/rollback-confirm-dialog.tsx
    web/admin/src/components/bypass/bypass-tab.tsx
  </files>
  <read_first>
    - web/admin/src/components/ui/alert-dialog.tsx
    - web/admin/src/components/bypass/bypass-tab.tsx (Plan 03 产物)
    - .planning/phases/46-api-ui/46-UI-SPEC.md `### Rollback host slug 二次确认`
  </read_first>
  <action>
    1. 创建 `web/admin/src/components/bypass/rollback-confirm-dialog.tsx`：
       ```tsx
       import { useState } from "react";
       import { toast } from "sonner";
       import {
         AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent,
         AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle,
       } from "@/components/ui/alert-dialog";
       import { Input } from "@/components/ui/input";
       import { useRollbackBypass } from "@/hooks/use-bypass-snapshots";
       import { parseBypassApiError } from "@/lib/i18n/error-codes";

       interface RollbackConfirmDialogProps {
         hostId: string;
         hostSlug: string;
         targetSnapshotId: string;
         targetVersion: number;
         currentVersion: number;
         open: boolean;
         onOpenChange: (open: boolean) => void;
         onSuccess?: () => void;
       }

       export function RollbackConfirmDialog(props: RollbackConfirmDialogProps) {
         const { hostId, hostSlug, targetSnapshotId, targetVersion, currentVersion, open, onOpenChange, onSuccess } = props;
         const [input, setInput] = useState("");
         const rollbackMutation = useRollbackBypass(hostId);
         const matched = input.trim() === hostSlug;

         return (
           <AlertDialog open={open} onOpenChange={(o) => { if (!o) setInput(""); onOpenChange(o); }}>
             <AlertDialogContent>
               <AlertDialogHeader>
                 <AlertDialogTitle>回滚到 v{targetVersion}？</AlertDialogTitle>
                 <AlertDialogDescription>
                   当前配置 v{currentVersion} 将被替换为 v{targetVersion}。需要在输入框输入 host slug{" "}
                   <code className="font-mono px-1 py-0.5 bg-muted rounded">{hostSlug}</code> 以确认。
                 </AlertDialogDescription>
               </AlertDialogHeader>
               <Input
                 value={input}
                 onChange={(e) => setInput(e.target.value)}
                 placeholder="输入 host slug"
                 className="font-mono"
               />
               <AlertDialogFooter>
                 <AlertDialogCancel>取消</AlertDialogCancel>
                 <AlertDialogAction
                   disabled={!matched || rollbackMutation.isPending}
                   className={matched ? "bg-destructive text-destructive-foreground hover:bg-destructive/90" : ""}
                   onClick={(e) => {
                     e.preventDefault();
                     rollbackMutation.mutate(targetSnapshotId, {
                       onSuccess: (resp) => {
                         toast.success(resp.message || "回滚请求已下发");
                         onSuccess?.();
                         onOpenChange(false);
                       },
                       onError: (err: any) => {
                         const { message } = parseBypassApiError(err?.message ?? "");
                         toast.error(message);
                       },
                     });
                   }}
                 >
                   执行回滚
                 </AlertDialogAction>
               </AlertDialogFooter>
             </AlertDialogContent>
           </AlertDialog>
         );
       }
       ```

    2. 修改 `web/admin/src/components/bypass/bypass-tab.tsx`，在 Plan 03 预留位置插入 PreviewSheet + ApplyProgressDialog：
       ```tsx
       import { PreviewSheet } from "./preview-sheet";
       import { ApplyProgressDialog } from "./apply-progress-dialog";

       export function BypassTab({ hostId }: BypassTabProps) {
         // ... Plan 03 已有的 useState / data / drawerOpen / editingRule

         const [previewOpen, setPreviewOpen]   = useState(false);
         const [applyOpen, setApplyOpen]       = useState(false);
         const [pendingRiskyCount, setPendingRiskyCount] = useState(0);

         return (
           <div className="space-y-6">
             <PresetGrid hostId={hostId} />
             <CustomRulesTable hostId={hostId} onCreate={...} onEdit={...} />

             <BypassRuleDrawer ... />

             {/* sticky 右下角 "查看生效预览" 按钮 */}
             <div className="sticky bottom-4 flex justify-end">
               <Button size="lg" onClick={() => setPreviewOpen(true)}>
                 查看生效预览
               </Button>
             </div>

             <PreviewSheet
               hostId={hostId}
               open={previewOpen}
               onOpenChange={setPreviewOpen}
               onApply={(preview) => {
                 setPendingRiskyCount(preview.risky_count);
                 setApplyOpen(true);
                 // 保持 PreviewSheet 开启（per UI-SPEC: Sheet 不关闭，用户看最终配置）
               }}
             />

             <ApplyProgressDialog
               hostId={hostId}
               open={applyOpen}
               onOpenChange={setApplyOpen}
               riskyCount={pendingRiskyCount}
             />
           </div>
         );
       }
       ```

    注意：`RollbackConfirmDialog` 是独立可复用组件，Phase 46 阶段先放代码但不强制接入 UI（rollback 入口可放在「应用历史」面板里，本 plan **不在 BypassTab 内集成 rollback 入口** —— 历史面板可作为 future enhancement；本 plan 只确保 RollbackConfirmDialog 可被未来的 SnapshotHistory 组件 import 使用）。
  </action>
  <verify>
    <automated>cd web/admin && pnpm typecheck && pnpm test:unit -- src/components/bypass/ && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RollbackConfirmDialog 在 input.trim() !== hostSlug 时按钮 disabled
    - 匹配后按钮变 destructive 色 className
    - BypassTab 含 sticky "查看生效预览" 按钮，点击打开 PreviewSheet
    - PreviewSheet onApply 触发 ApplyProgressDialog 打开
    - `grep -c "PreviewSheet\|ApplyProgressDialog" web/admin/src/components/bypass/bypass-tab.tsx` ≥ 2
    - `pnpm build` 生产构建通过
    - 所有 bypass/ 目录测试套件全部 PASS
  </acceptance_criteria>
  <done>v3.5 Phase 46 前端闭环：勾预设 → 加规则 → 预览 → 应用（5 阶段进度）→ 成功 toast。Rollback 组件就绪等待未来 SnapshotHistory 面板接入。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → preview/apply/rollback | adminGuard JWT |
| useSSE → /v1/admin/sse | 现有 SSE 通道已经做 JWT 鉴权（沿用 host 升级镜像同款） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | S (Spoofing) | apply / rollback API + SSE | mitigate | apiFetch 自动注入 JWT bearer token；SSE 通道复用同款 adminGuard 鉴权（沿用 $hostId.tsx 升级镜像模式），未登录请求 401 后跳转 /login |
| T-46-04 | I (Info Disclosure) | JSON Viewer 渲染 untrusted JSON | mitigate | 用 `<pre>` + `JSON.stringify` 而非 dangerouslySetInnerHTML；JSONViewer 不解析任何 HTML |
| T-46-04 | I (Info Disclosure) | NftDiff 渲染 untrusted text | mitigate | NftDiffViewer 用 `<pre>` + 文本拼接，React 默认转义；不解析 ANSI 或 HTML |
| T-46-13 | T (Tampering) | rollback 越权 | mitigate | RollbackConfirmDialog 强制要求输入 host slug 严格匹配；后端 Plan 02 二次校验 target_snapshot.host_id == hostID |
| T-46-14 | D (DoS) | 10000 行 JSON 卡死浏览器 | mitigate | JSONViewer 默认折叠 > 10000 行，用户点击才展开 |
| T-46-15 | I (Info Disclosure) | task error_summary 泄露内部错误 | mitigate | ApplyProgressDialog 优先用 bypassErrorMessage 翻译已知错误码；未知错误才显示 task.last_error_summary 原文（accept this trade-off：admin 用户才看得到，trust boundary 在登录） |
</threat_model>

<verification>
```bash
cd web/admin
pnpm typecheck
pnpm test:unit -- src/components/bypass/
pnpm build

# 文件完整性
ls src/components/bypass/                       # 至少 11 个 .tsx（Plan 03 5 个 + Plan 04 5 个 + __tests__/）
grep -c "preview\|apply\|rollback" src/hooks/use-bypass-snapshots.ts   # ≥ 5
grep -c "PreviewSheet\|ApplyProgressDialog" src/components/bypass/bypass-tab.tsx   # ≥ 2
grep -c "STAGES\|生成快照\|下发到 agent\|Reload 配置\|健康检查\|完成" src/components/bypass/apply-progress-dialog.tsx   # ≥ 5
```
</verification>

<success_criteria>
1. PreviewSheet 640px 宽，双 Tab (sing-box JSON / nft set diff) 切换
2. risky_count > 5 时主按钮 warning 色 + 文案变化「应用此配置（含 N 条高风险）」
3. JSONViewer 在 > 10000 行时折叠
4. NftDiffViewer 用 emerald-400 / red-400 / slate-500 三色渲染 unified diff
5. ApplyProgressDialog 渲染 5 个固定阶段（生成快照 / 下发到 agent / Reload 配置 / 健康检查 / 完成）
6. task.status="succeeded" 自动 500ms 关闭 + toast.success（中文）
7. task.status="failed" Dialog 保持开启 + 错误码中文文案 + 关闭按钮
8. RollbackConfirmDialog input.trim() 严格匹配 host slug 才能执行
9. BypassTab sticky "查看生效预览" 按钮 + PreviewSheet + ApplyProgressDialog 闭环
10. `pnpm typecheck` + `pnpm build` 通过，至少 10 个新测试 case PASS
</success_criteria>

<output>
完成后写 `.planning/phases/46-api-ui/46-04-SUMMARY.md`，必含：
- v3.5 Phase 46 完整用户旅程图（勾预设 → 加规则 → 预览 → 应用 → 5 阶段进度 → toast / 失败错误码）
- ApplyProgressDialog 5 阶段状态机表（pending/active/done/failed × 5 阶段）
- Phase 47 接管点说明：worker dispatch 真实执行后 task.progress_percent 如何驱动 stageStatuses 自然切换
- RollbackConfirmDialog 接口（Phase 47 或 v3.5 P1 接入 SnapshotHistory 时直接用）
</output>
