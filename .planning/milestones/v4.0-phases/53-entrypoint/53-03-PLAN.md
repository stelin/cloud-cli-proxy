---
phase: 53
plan: "03"
plan_name: 容器内手测 smoke 脚本 + fixture
created: 2026-05-16
requirements: []
decisions: [T-53-1, T-53-2, T-53-3, T-53-4, T-53-5, T-53-6]
depends_on: ["53-01", "53-02"]
estimated_effort: 1 day
---

# Plan 53-03: 容器内手测 smoke 脚本 + fixture

## Objective

提供 Phase 53 自测层 —— 不接 v3.6 e2e harness（Phase 55 才完整接入），但有一个本地一键脚本能跑通 T-53-1..6 六条 smoke 断言，让 Phase 53 ship 前每次镜像构建都能本地复现验证。

## Scope

| 断言 (CONTEXT.md) | 含义 | 实现 |
|---|---|---|
| T-53-1 | tun0 存在 + sing-box 跑在 uid=9000 | smoke 脚本 step 1 |
| T-53-2 | `cat config.json` 失败 | smoke 脚本 step 2 |
| T-53-3 | `curl ip.me` 返回受控出口 IP | smoke 脚本 step 3 |
| T-53-4 | user shell 空 cap | smoke 脚本 step 4 |
| T-53-5 | kill sing-box → 容器 ≤3s 死 | smoke 脚本 step 5 |
| T-53-6 | docker restart on-failure 触发 | smoke 脚本 step 6（依赖 Plan 54 控制面，53 阶段先跑手工 docker run --restart） |

## Files Created

- `tests/phase53/smoke.sh` — 主测试脚本（bash，本地一键跑）
- `tests/phase53/fixtures/test-singbox-config.json` — sing-box 测试 config（占位上游 proxy，仅用于 Phase 53 镜像自测）
- `tests/phase53/README.md` — 使用说明

## Tasks

### T1 — `tests/phase53/fixtures/test-singbox-config.json`

最小可用 sing-box config 用于 Phase 53 烟测。结构与 v3.6 gateway 输出一致：

```json
{
  "log": { "level": "info", "timestamp": true },
  "dns": {
    "servers": [
      { "type": "udp", "tag": "upstream-dns", "server": "1.1.1.1" }
    ],
    "rules": [],
    "final": "upstream-dns"
  },
  "inbounds": [
    {
      "type": "tun",
      "tag": "tun-in",
      "interface_name": "tun0",
      "address": ["172.19.0.1/30"],
      "mtu": 1500,
      "auto_route": true,
      "strict_route": true,
      "stack": "system",
      "endpoint_independent_nat": true
    }
  ],
  "outbounds": [
    {
      "type": "direct",
      "tag": "direct-out"
    }
  ],
  "route": {
    "rules": [
      { "action": "sniff" }
    ],
    "final": "direct-out",
    "auto_detect_interface": true
  }
}
```

**重要**：fixture 使用 `direct` outbound（直连，无上游代理），仅用于"sing-box 能起来 + tun 接管 + curl 走 tun"的烟测。**不验证出口 IP 是上游代理的 IP**（那需要真实 outbound 配置，Phase 55 e2e 才做）。

T-53-3 实际验证策略：容器内 `curl ip.me` 返回的 IP **应等于** host eth0 IP（因为 direct outbound 直接出网，没有上游代理）。这证明流量确实走 tun 而非容器 bridge 直出（如果走 bridge，IP 也是 host eth0，看起来一样）—— 所以 T-53-3 还要加一个独立 oracle：`ip route show table all` 看默认路由确实指向 tun0。

### T2 — `tests/phase53/smoke.sh`

```bash
#!/usr/bin/env bash
# Phase 53 烟测：本地 docker run managed-user:v4-dev 跑通 T-53-1..6。
# 仅用于开发期手测，CI 走 Phase 55 e2e（v3.6 harness 接入）。
#
# Usage:
#   bash tests/phase53/smoke.sh                       # 默认 image tag = managed-user:v4-dev
#   IMAGE=managed-user:v4-rc1 bash tests/phase53/smoke.sh
#
# Exit: 0 全绿 / 非 0 某条失败（打印失败步骤名）

set -euo pipefail

IMAGE="${IMAGE:-managed-user:v4-dev}"
CONTAINER_NAME="phase53-smoke-$$"
FIXTURE_DIR="$(cd "$(dirname "$0")" && pwd)/fixtures"
TMP_CONFIG="$(mktemp -d)/config.json"
trap 'cleanup' EXIT

cleanup() {
  docker rm -f "$CONTAINER_NAME" 2>/dev/null || true
  rm -rf "$(dirname "$TMP_CONFIG")"
}

log() { printf "\033[1;36m[smoke]\033[0m %s\n" "$*"; }
fail() { printf "\033[1;31m[FAIL]\033[0m %s\n" "$*" >&2; exit 1; }

# ===== 准备 =====
log "preparing fixture (writable copy, root:0600)"
cp "$FIXTURE_DIR/test-singbox-config.json" "$TMP_CONFIG"
chmod 600 "$TMP_CONFIG"
# config 必须由 root 拥有以模拟生产 host-agent 注入语义
sudo chown root:root "$TMP_CONFIG" 2>/dev/null || true

# ===== 启动容器 =====
log "starting container $CONTAINER_NAME from $IMAGE"
docker run -d --name "$CONTAINER_NAME" \
    --device /dev/net/tun \
    --cap-drop ALL --cap-add NET_ADMIN \
    -v "$TMP_CONFIG:/etc/sing-box/config.json" \
    -e CONTAINER_USER=workspace \
    -e CONTAINER_SSH_PASSWORD=phase53-smoke-pw \
    -e MODE=local \
    --restart=on-failure:3 \
    "$IMAGE" >/dev/null

# 等容器启动序列跑完（tun0 + nft + config rm）
sleep 6

# ===== T-53-1: tun0 ready + sing-box uid=9000 =====
log "[T-53-1] tun0 ready + sing-box uid=9000"
docker exec "$CONTAINER_NAME" ip link show tun0 >/dev/null \
    || fail "T-53-1 tun0 not ready"
uid=$(docker exec "$CONTAINER_NAME" sh -c 'ps -o uid= -p $(pidof sing-box) | tr -d " "')
[ "$uid" = "9000" ] || fail "T-53-1 sing-box uid=$uid, want 9000"

# ===== T-53-2: config 已 rm =====
log "[T-53-2] config 文件已删除"
if docker exec "$CONTAINER_NAME" test -f /etc/sing-box/config.json; then
  fail "T-53-2 config 仍存在"
fi

# ===== T-53-3: curl ip.me 走 tun =====
log "[T-53-3] tun 接管默认路由"
default_dev=$(docker exec "$CONTAINER_NAME" sh -c 'ip route show table all | grep "^default" | head -1 | awk "{print \$5}"')
case "$default_dev" in
  tun0) ;;
  *) fail "T-53-3 默认出口 dev=$default_dev, want tun0" ;;
esac

# curl 能 reach 外网（证明 tun 转发链路 OK；fixture direct outbound 直出）
if ! docker exec "$CONTAINER_NAME" curl -fsS --max-time 10 https://api.ipify.org >/dev/null 2>&1; then
  fail "T-53-3 容器内 curl ipify.org 失败（tun 链路异常）"
fi

# ===== T-53-4: workspace 用户空 cap + 不能 sudo =====
log "[T-53-4] workspace cap 集合空 + 不能 sudo"
caps=$(docker exec -u workspace "$CONTAINER_NAME" sh -c 'getpcaps $$ 2>&1' | head -1)
# expected: 形如 "Capabilities for `<pid>`: =" (空集)
if echo "$caps" | grep -qE 'cap_net_admin|cap_sys_admin|cap_net_raw'; then
  fail "T-53-4 workspace 持有特权 cap: $caps"
fi

if docker exec -u workspace "$CONTAINER_NAME" sudo -n true 2>/dev/null; then
  fail "T-53-4 workspace 居然能 sudo"
fi

# ===== T-53-5: kill sing-box → 容器 ≤3s 死 =====
log "[T-53-5] sing-box 死 → 容器死 fail-closed"
start_ts=$(date +%s)
# 必须用 root 杀（sing-box 是 uid=9000，workspace=1000 杀不了）
docker exec --user 0 "$CONTAINER_NAME" sh -c 'kill -9 $(pidof sing-box)' 2>/dev/null || true

# 等容器退出
deadline=$((start_ts + 5))
while [ "$(docker inspect -f '{{.State.Running}}' "$CONTAINER_NAME" 2>/dev/null)" = "true" ]; do
  if [ "$(date +%s)" -ge "$deadline" ]; then
    fail "T-53-5 sing-box 死 5s 后容器仍在跑（fail-closed 未生效）"
  fi
  sleep 0.2
done

end_ts=$(date +%s)
elapsed=$((end_ts - start_ts))
log "  container died in ${elapsed}s"
[ "$elapsed" -le 3 ] || fail "T-53-5 容器死亡耗时 ${elapsed}s > 3s"

# ===== T-53-6: docker restart on-failure 触发 =====
# 注：Phase 53 阶段控制面尚未实现 --restart=on-failure 配置（Phase 54 落地），
# 此处用 docker run 时手工设的 --restart=on-failure:3 验证 docker daemon 重启行为
log "[T-53-6] docker restart on-failure 触发"
sleep 3  # 给 docker daemon 时间触发 restart

restart_count=$(docker inspect -f '{{.RestartCount}}' "$CONTAINER_NAME" 2>/dev/null || echo 0)
[ "$restart_count" -ge 1 ] || fail "T-53-6 RestartCount=$restart_count, want >=1"

is_running=$(docker inspect -f '{{.State.Running}}' "$CONTAINER_NAME" 2>/dev/null)
[ "$is_running" = "true" ] || fail "T-53-6 容器未重新拉起 (Running=$is_running)"

# 重启后 sing-box 应该再次 uid=9000 跑起来
sleep 3
new_uid=$(docker exec "$CONTAINER_NAME" sh -c 'ps -o uid= -p $(pidof sing-box) | tr -d " "' 2>/dev/null || echo "missing")
[ "$new_uid" = "9000" ] || fail "T-53-6 restart 后 sing-box uid=$new_uid"

# ===== 全绿 =====
printf "\033[1;32m[OK]\033[0m Phase 53 smoke 全部通过 (T-53-1..6)\n"
exit 0
```

### T3 — `tests/phase53/README.md`

```markdown
# Phase 53 Smoke Tests

本地烟测 Phase 53 镜像与 entrypoint 启动序列。

## 跑测试

```bash
# 1. 构建镜像（Plan 53-01 + 53-02 改造完成后）
docker build -t managed-user:v4-dev -f deploy/docker/managed-user/Dockerfile .

# 2. 跑烟测
bash tests/phase53/smoke.sh
```

期望: 6 条 T-53-* 断言全部 OK。

## 不是 e2e

Phase 55 才完整接入 v3.6 e2e harness（testcontainers-go + testify/suite）。
本目录是 Phase 53 开发期的本地一键自测，不进 CI。

## 与 Plan 关系

| 断言 | 验收 PLAN |
|---|---|
| T-53-1 (tun0 + uid=9000) | 53-01 IMG-02 / 53-02 EP-02 |
| T-53-2 (config rm) | 53-02 D-V4-2 |
| T-53-3 (tun 接管) | 53-02 EP-03 / NET-01 |
| T-53-4 (空 cap + 无 sudo) | 53-01 IMG-04 / 53-02 NET-04 |
| T-53-5 (fail-closed ≤3s) | 53-02 EP-04 / NET-03 |
| T-53-6 (restart on-failure) | 53-02 + docker daemon 行为 |
```

### T4 — 给 Phase 53 自测加 Makefile 入口（可选）

```makefile
phase53-smoke: ## Run Phase 53 image smoke tests (requires managed-user:v4-dev)
	bash tests/phase53/smoke.sh
```

**Rationale**: 不强制（Phase 56 才统一 `make e2e`），但有 alias 方便日常开发。

## Verification

### V1 — fixture JSON 合法性

```bash
docker run --rm -v $(pwd)/tests/phase53/fixtures:/cfg managed-user:v4-dev sing-box check -c /cfg/test-singbox-config.json
```

期待: `Configuration OK`。

### V2 — smoke 脚本 shellcheck

```bash
shellcheck tests/phase53/smoke.sh
```

期待: 0 error。

### V3 — smoke 脚本本地端到端

需要 Plan 53-01 + 53-02 已完成，镜像构建成功。

```bash
make user-image  # 或 docker build ...
bash tests/phase53/smoke.sh
```

期待: 6 条 T-53-* 全绿，最后输出 `[OK] Phase 53 smoke 全部通过`。

## Out of Scope

- v3.6 e2e harness 接入（Phase 55）
- CI 配置（Phase 56）
- Pumba / netem / 真实上游代理（Phase 55）
- 多 OS / 多 arch 验证（Phase 53 仅 linux/amd64 烟测；arm64 由 Phase 55 真机 CI 覆盖）

## Risks

| Risk | Mitigation |
|---|---|
| sudo chown root 在 mac 上需要密码或 docker desktop 权限 | smoke 脚本对 `sudo chown` 失败容忍（`|| true`），fixture 文件权限主要靠 chmod 600 |
| `direct` outbound 出口 IP = host eth0 IP，看起来跟绕过 tun 一样 | T-53-3 用 `ip route show table all` 独立 oracle 断言默认路由 dev=tun0 |
| docker daemon 在 mac 上的 `--restart=on-failure` 行为可能与 linux 差异 | T-53-6 在 linux runner 跑更可靠，mac 上 best-effort |

## Acceptance

✅ V1-V3 通过
✅ smoke.sh 单文件 < 200 行，bash 4.0+ 兼容
✅ fixture config 合法且与 v3.6 gateway config schema 一致

## Commit Plan

单次 commit：
```
test(53-03): Phase 53 image smoke tests (T-53-1..6) + fixture
```

文件改动：
- `tests/phase53/smoke.sh` (新)
- `tests/phase53/fixtures/test-singbox-config.json` (新)
- `tests/phase53/README.md` (新)
- `Makefile` (一行 phase53-smoke target，可选)
