---
phase: 54
plan: "03"
plan_name: 双绑互斥契约保持 + 单元测试加固
created: 2026-05-16
requirements: [CTRL-04]
decisions: [D-54-11]
depends_on: []
estimated_effort: 0.5 day
wave: 1
---

# Plan 54-03: 双绑互斥契约保持 + 单元测试加固

## Objective

把 v3.6 Phase 51-09 落地的双绑互斥 API 契约（`ErrCodeEgressIPAlreadyBound` + 409 + 中英双语 message + `host_id`/`egress_ip_id` 字段回显）作为**单容器化重构的不变式锁**，新增一条显式以「Phase 54 契约保护」命名的单元测试，并验证 `internal/network/` Plan 54-01 / 54-02 改造**没有**在 binding 调用链上引入新依赖（gateway image / cloudproxy-net bridge 等）。

Phase 54 不修改 `internal/controlplane/http/admin_bindings.go` 任何业务代码，仅追加测试与契约文档。

## Scope（REQ → 实现位置 → 验收）

| REQ | 实现位置 | 验收手段 |
|---|---|---|
| CTRL-04 | `internal/controlplane/http/admin_bindings_test.go` 追加 `Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor` 测试用例 | go test 单一 t.Run 全 PASS，断言 5 项契约不变 |
| CTRL-04（无网络层依赖回归）| `internal/controlplane/http/admin_bindings.go` import 与 `Bind()` handler 调用链 grep | grep `network\.` / `docker` / `cloudproxy-` 在 handler 文件内 0 命中 |

## Files Touched

- `internal/controlplane/http/admin_bindings_test.go` （追加测试，不改既有用例）
- `internal/controlplane/http/admin_bindings.go` （**不改**，仅 grep 验证）

## Tasks

### T1 — 追加 `Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor` 测试

文件：`internal/controlplane/http/admin_bindings_test.go`

在文件末尾追加：

```go
// Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor
// Phase 54 / CTRL-04：双绑互斥契约保护测试。
//
// 单容器化重构（Plan 54-01 + 54-02）改动了 internal/network/container_proxy_provider.go
// 大幅删行 ≥ 300 行（删 gw 容器创建 / bridge 创建 / teardownGateway / host 端 nft 注入）；
// 本测试作为不变式锁，确认 admin_bindings.go 的 Bind() pre-check 链路：
//
//   1. HTTP status = 409
//   2. JSON body error_code = "egress_ip_already_bound"（机器可读稳定常量）
//   3. JSON body error 含中文「已绑定」+ 英文「already bound」（v3.6 51-09 双语契约）
//   4. JSON body host_id = 既有绑定的 host_id（不是请求 host_id）
//   5. JSON body egress_ip_id = 请求 egress_ip_id 回显
//
// 与 v3.6 TestAdminBindings_DoubleBind_ErrorCode 用例并存（不替代）：
//   - v3.6 用例：Phase 51 落地时的契约验证（功能新增）
//   - 本用例：Phase 54 重构后的契约保护（不变式锁）
//
// 若本测试在 Phase 54 / Phase 55 / 后续任何 milestone 失败，说明双绑契约
// 被无意打破，必须立即恢复。
func Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor(t *testing.T) {
    now := time.Now().Truncate(time.Second)
    stoppedHost := repository.Host{ID: "host-requesting", Status: "stopped", CreatedAt: now, UpdatedAt: now}
    occupierHostID := "host-already-occupying"
    requestEgressIPID := "eip-contested"

    store := &stubBindingStore{
        host:                 stoppedHost,
        existingEgressHostID: occupierHostID,
    }
    events := &stubEventRecorder{}
    router := adminTestRouter(t, Dependencies{
        Logger:        slog.Default(),
        AdminBindings: store,
        EventRecorder: events,
    })
    srv := httptest.NewServer(router)
    defer srv.Close()

    body, _ := json.Marshal(map[string]string{
        "host_id":      stoppedHost.ID,
        "egress_ip_id": requestEgressIPID,
    })
    req, _ := nethttp.NewRequest("POST", srv.URL+"/v1/admin/bindings", bytes.NewReader(body))
    req.Header.Set("Authorization", "Bearer "+validAdminToken(t))
    req.Header.Set("Content-Type", "application/json")

    resp, err := nethttp.DefaultClient.Do(req)
    if err != nil {
        t.Fatalf("request failed: %v", err)
    }
    defer resp.Body.Close()

    // 契约 1: HTTP 409
    if resp.StatusCode != nethttp.StatusConflict {
        t.Fatalf("[contract-1 status] = %d, want 409 (Phase 51-09 双绑互斥)", resp.StatusCode)
    }

    var respBody map[string]any
    if err := json.NewDecoder(resp.Body).Decode(&respBody); err != nil {
        t.Fatalf("decode response: %v", err)
    }

    // 契约 2: error_code 稳定字面量
    if code, _ := respBody["error_code"].(string); code != ErrCodeEgressIPAlreadyBound {
        t.Errorf("[contract-2 error_code] = %q, want %q (body=%v)",
            code, ErrCodeEgressIPAlreadyBound, respBody)
    }
    if ErrCodeEgressIPAlreadyBound != "egress_ip_already_bound" {
        t.Errorf("[contract-2 constant-value] ErrCodeEgressIPAlreadyBound = %q, want %q",
            ErrCodeEgressIPAlreadyBound, "egress_ip_already_bound")
    }

    // 契约 3: 中英双语 message
    msg, _ := respBody["error"].(string)
    if msg == "" {
        t.Errorf("[contract-3 message-nonempty] error message empty (body=%v)", respBody)
    }
    for _, sub := range []string{"已绑定", "already bound"} {
        if !strings.Contains(msg, sub) {
            t.Errorf("[contract-3 bilingual] message %q missing %q", msg, sub)
        }
    }

    // 契约 4: host_id = 既有占用者，不是请求者
    if hid, _ := respBody["host_id"].(string); hid != occupierHostID {
        t.Errorf("[contract-4 host_id] = %q, want %q (must be the occupier, not requester)",
            hid, occupierHostID)
    }

    // 契约 5: egress_ip_id 回显
    if eid, _ := respBody["egress_ip_id"].(string); eid != requestEgressIPID {
        t.Errorf("[contract-5 egress_ip_id] = %q, want %q", eid, requestEgressIPID)
    }
}
```

**为什么不复用既有 `TestAdminBindings_DoubleBind_ErrorCode`**：v3.6 用例可能被未来重构合并/拆分；新增独立用例 + Phase 54 显式命名让 reviewer 在删除时被名字提示"这是契约保护，不要删"。两个用例都跑、断言重复，是 acceptable 的成本（每次 < 10ms）。

### T2 — grep 验证 admin_bindings.go 无网络层依赖

文件：`internal/controlplane/http/admin_bindings.go`（**只读校验**）

新增 verifier 脚本/grep 命令（不进 Go 测试集，作为 verify 步骤手动跑）：

```bash
# 1. admin_bindings.go 不应 import internal/network
rg '^\s*"github\.com/.+/internal/network"' internal/controlplane/http/admin_bindings.go
# 期望：0 命中

# 2. admin_bindings.go 不应直接引用 docker / gateway / bridge 字面量
rg -i '\b(docker|gateway|cloudproxy-net|bridge)\b' internal/controlplane/http/admin_bindings.go
# 期望：0 命中（注释里如果有"gateway"字样需评估，预期当前文件 0 命中）

# 3. AdminBindingStore 接口签名稳定（5 个方法，无新增/删除）
rg '^\s*(GetHost|BindEgressIPToHost|UnbindEgressIPFromHost|GetBindingHostID|GetBindingHostIDByEgressIP)\(' \
   internal/controlplane/http/admin_bindings.go
# 期望：5 行命中（5 个接口方法签名）
```

这条 verify 不进 Go 测试，作为 Plan 54-03 V2 手动 step；通过即 sign-off。

### T3 — 在 PLAN frontmatter 与 SUMMARY 显式记录契约 ID

本 plan 的 SUMMARY（Plan 完成后 `gsd-executor` 写）必须包含一节 "契约保护"，列：

```
- 契约 ID: CTRL-04-CONTRACT-1..5
- 测试用例: Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor
- 契约源: Phase 51 Plan 09 (commit 在 .planning/milestones/v3.6-MILESTONE-AUDIT.md)
- 不变式锁定方式: Go unit test + ErrCodeEgressIPAlreadyBound 字面量值 grep 守护
```

## Verification

### V1 — 新增测试 PASS

```bash
go test ./internal/controlplane/http/ -run Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor -v -count=1
# 期望：PASS，断言 5 项契约不变
```

### V2 — grep 验证 admin_bindings.go 无网络层依赖（T2）

```bash
# 三条 grep 全部 0 命中（除 AdminBindingStore 接口 5 行命中）
rg '^\s*"github\.com/.+/internal/network"' internal/controlplane/http/admin_bindings.go
rg -i '\b(docker|gateway|cloudproxy-net|bridge)\b' internal/controlplane/http/admin_bindings.go
rg '^\s*(GetHost|BindEgressIPToHost|UnbindEgressIPFromHost|GetBindingHostID|GetBindingHostIDByEgressIP)\(' \
   internal/controlplane/http/admin_bindings.go | wc -l
# 期望：前两条 0 / 0；第三条 = 5（接口 5 方法签名）
```

### V3 — v3.6 既有用例不破

```bash
go test ./internal/controlplane/http/ -run 'TestAdminBindingsHandler|TestAdminBindings_DoubleBind_ErrorCode' -v -count=1
# 期望：v3.6 双绑 11 用例 + 新增 1 用例全 PASS
```

### V4 — 完整 controlplane test PASS

```bash
go test ./internal/controlplane/... -count=1
# 期望：全 PASS
```

### V5 — `ErrCodeEgressIPAlreadyBound` 字面量值未改

```bash
rg 'ErrCodeEgressIPAlreadyBound\s*=\s*"egress_ip_already_bound"' internal/controlplane/http/
# 期望：1 命中（admin_bindings.go L29 定义）
```

### V6 — 调用顺序无新增 store 方法

```bash
git diff internal/controlplane/http/admin_bindings.go
# 期望：0 diff（业务代码不改）
```

## Out of Scope

- 修改 `admin_bindings.go` 任何业务逻辑（业务不变是 CTRL-04 的核心要求）
- 修改 `AdminBindingStore` 接口签名（同上）
- E2E 双绑用例迁移到单容器拓扑（Phase 55 接手）
- 新增更细的双绑场景测试（同 host 重绑 / 同 IP 不同 host / 已 unbind 后再绑等）—— v3.6 已覆盖足够

## Risks

| Risk | Mitigation |
|---|---|
| 既有 `TestAdminBindings_DoubleBind_ErrorCode` 与新用例语义高度重叠，reviewer 可能想合并 | T3 SUMMARY 与测试 doc string 显式说明"两者并存是设计意图"；合并需走 Phase 56 / v4.1 显式 audit |
| `Bind()` handler 字面量 message 未来改中文措辞（如「已绑定」→「占用中」）| 本测试 5 个契约项里第 3 项硬断言中文子串「已绑定」+ 英文「already bound」；若 reviewer 想改文案必须同步改两个测试 + 改 commit message 显式标注，构成"破坏契约"红色信号 |
| 新用例与既有用例都跑后总测试时间增加 | 单个用例 < 10ms；总测试时间 < 100ms 增量；可忽略 |

## Acceptance

✅ V1-V6 全部通过
✅ 新增测试用例 `Test_Phase54_DoubleBindingContract_PreservedAfterSingleContainerRefactor` 存在且 PASS
✅ `admin_bindings.go` 业务代码 0 diff
✅ v3.6 既有双绑用例不破
✅ `ErrCodeEgressIPAlreadyBound` 字面量值 `"egress_ip_already_bound"` 未改

## Commit Plan

单一 commit：

```
test(54-03): 加 Phase 54 双绑互斥契约保护测试 (CTRL-04)
```

commit 内仅 1 文件改动（`internal/controlplane/http/admin_bindings_test.go`）；commit body 注明"业务代码 0 diff，本测试作为单容器化重构的不变式锁"。
