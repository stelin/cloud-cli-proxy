---
phase: 55
plan: "01"
plan_name: Scenario builder 单容器化（WithUser 合并 WithSingBoxGateway + 删除 cross-container 协调代码）
created: 2026-05-27
requirements: [E2E-V4-01, E2E-V4-05]
decisions: []
depends_on: []
estimated_effort: 1 day
wave: 1
---

# Plan 55-01: Scenario builder 单容器化

## Objective

重构 `tests/e2e/harness/scenario.go` 的 builder API，删除 `gatewaySpec`、`WithSingBoxGateway()`、`SingBoxGateway()` 访问器，合并到 `userSpec`/`WithUser()`/`User()`。同时删除 v3.6 cross-container 协调辅助代码（gw 启动同步、network connect 等待等），净删 ≥ 150 行。

## Scope

| REQ | 实现位置 | 验收 |
|---|---|---|
| E2E-V4-01 | `tests/e2e/harness/scenario.go` + 所有调用点 | `WithSingBoxGateway` + `SingBoxGateway` + `gatewaySpec` + `GatewayName` 0 命中 |
| E2E-V4-05 | `tests/e2e/harness/` + `tests/e2e/helpers*.go` | 删除 cross-container 协调代码 ≥ 150 行 |

## Files Touched

- `tests/e2e/harness/scenario.go` — 核心重构
- `tests/e2e/harness/suite.go` — 检查 gateway 引用
- `tests/e2e/helpers.go` + `tests/e2e/helpers_linux.go` — 删除 gw 协调辅助
- `tests/e2e/**/*.go` — 所有调用点迁移

## Tasks

### T1 — 删除 gatewaySpec + hostSpec.GatewayName

<read_first>
- tests/e2e/harness/scenario.go
- tests/e2e/harness/scenario.go (entire file)
</read_first>

<action>
删除 `gatewaySpec` 结构体（L63-67）、`hostSpec.GatewayName` 字段、`Scenario.gateways` map + `gatewayDeclOrder` slice。
</action>

<acceptance_criteria>
- grep `gatewaySpec` tests/e2e/harness/ 0 命中
- grep `GatewayName` tests/e2e/harness/scenario.go 0 命中
- `go build -tags=e2e ./tests/e2e/...` 通过
</acceptance_criteria>

### T2 — 替换 WithSingBoxGateway 为 WithUser 扩展字段

<read_first>
- tests/e2e/harness/scenario.go
- internal/network/container_proxy_provider.go (了解 v4.0 OutboundConfig 用法)
</read_first>

<action>
1. 删除 `WithSingBoxGateway()` 方法，在 `userSpec` 中添加 `OutboundConfig json.RawMessage` 字段
2. 修改 `WithUser(name, hostName)` → `WithUser(name, hostName, outboundConfig)` 或新增 `WithUserOutboundConfig(outbound json.RawMessage)` 
3. 删除 `WithHost` 中 "declare a gateway first" 的 guard（L244）
4. 删除 `s.gatewayDeclOrder` 相关逻辑
5. 更新 `startSteps()` 中 Step 4..6 的 gateway 循环为单容器路径
</action>

<acceptance_criteria>
- grep `WithSingBoxGateway` tests/ 0 命中
- grep `gatewayDeclOrder` tests/ 0 命中
- `WithUser` 方法接受或可设置 OutboundConfig
- `go build -tags=e2e ./tests/e2e/...` 通过
</acceptance_criteria>

### T3 — 删除 SingBoxGateway 访问器

<read_first>
- tests/e2e/harness/scenario.go
- tests/e2e/harness/suite.go
</read_first>

<action>
1. 删除 `SingBoxGateway()` 方法 + `GatewayHandle` 类型（如果仅为 gateway 使用）
2. 更新 `started` map 中 gateway 相关逻辑
3. 更新 `Stop()` 中 gateway 清理逻辑
</action>

<acceptance_criteria>
- grep `SingBoxGateway` tests/e2e/harness/ 0 命中
- grep `GatewayHandle` tests/e2e/harness/ 0 命中
- `go build -tags=e2e ./tests/e2e/...` 通过
</acceptance_criteria>

### T4 — 迁移所有调用点

<read_first>
- tests/e2e/harness/scenario.go (refactored builder)
- All files under tests/e2e/ containing `WithSingBoxGateway` or `SingBoxGateway`
</read_first>

<action>
迁移所有旧 API 调用点为新 API：
- `scenario.WithSingBoxGateway("primary", ob)` → 合并到 `scenario.WithUser(...)` 或通过 `WithUserOutboundConfig(ob)` 设置
- `scenario.SingBoxGateway("primary")` → 删除（v4.0 无独立 gateway）
- `hostSpec.GatewayName` 引用 → 删除
</action>

<acceptance_criteria>
- grep -rn `WithSingBoxGateway\|SingBoxGateway\|GatewayName` tests/ 0 命中（除注释）
- `go build -tags=e2e ./tests/e2e/...` 通过
</acceptance_criteria>

### T5 — 删除 cross-container 协调辅助代码

<read_first>
- tests/e2e/helpers.go
- tests/e2e/helpers_linux.go
- tests/e2e/harness/artifacts.go
</read_first>

<action>
删除 v3.6 期间为 cross-container 协调写的辅助代码：
- gw 启动同步等待逻辑
- `docker network connect` 等待
- gateway 容器相关的 docker label 检查
- gateway container name 构造辅助函数
目标：净删 ≥ 150 行
</action>

<acceptance_criteria>
- `git diff --stat` 显示 tests/ 目录净删 ≥ 150 行
- `go build -tags=e2e ./tests/e2e/...` 通过
</acceptance_criteria>
