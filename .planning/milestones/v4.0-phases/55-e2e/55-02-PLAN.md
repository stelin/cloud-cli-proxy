---
phase: 55
plan: "02"
plan_name: LEAK-01..08 用例迁移到单容器架构
created: 2026-05-27
requirements: [E2E-V4-02]
decisions: []
depends_on: ["55-01"]
estimated_effort: 1 day
wave: 2
---

# Plan 55-02: LEAK 用例迁移

## Objective

将 v3.6 LEAK-01..08 全部 8 条防泄漏用例迁移到 v4.0 单容器架构。核心变化：抓包视角从 cross-container pair 改为 host eth0 + 容器 veth pair。断言语义不变（DNS 明文不可见、DoT 不可见、ICMP 不通、IPv6 不通、IMDS 不通、raw socket 不通、link-local 不可达、capability 受限）。

## Scope

| REQ | 实现位置 | 验收 |
|---|---|---|
| E2E-V4-02 | `tests/e2e/leak/leak_01..08_*.go` + `tests/e2e/leak/helpers_test.go` | 8 条用例全绿 |

## Files Touched

- `tests/e2e/leak/leak_01_dns_plain_udp_test.go`
- `tests/e2e/leak/leak_02_dot_853_test.go`
- `tests/e2e/leak/leak_03_icmp_test.go`
- `tests/e2e/leak/leak_04_ipv6_test.go`
- `tests/e2e/leak/leak_05_imds_test.go`
- `tests/e2e/leak/leak_06_raw_socket_test.go`
- `tests/e2e/leak/leak_07_nft_linklocal_test.go`
- `tests/e2e/leak/leak_08_capability_test.go`
- `tests/e2e/leak/helpers_test.go`
- `tests/e2e/leak/suite_test.go`

## Tasks

### T1 — 迁移 leak helpers 到单容器模式

<read_first>
- tests/e2e/leak/helpers_test.go
- tests/e2e/leak/suite_test.go
- tests/e2e/harness/scenario.go (refactored)
</read_first>

<action>
1. 删除 `WithSingBoxGateway()` 调用，outboundConfig 通过 `WithUser()` 设置
2. 更新抓包逻辑：从 cross-container pair 改为 host eth0 + 容器 veth pair
3. 更新 `SingBoxGateway()` → 通过 `User()` 获取容器信息
4. 调整 `PrepareGateway` 引用为单容器路径
</action>

<acceptance_criteria>
- `go build -tags=e2e ./tests/e2e/leak/...` 通过
- grep `SingBoxGateway\|WithSingBoxGateway\|PrepareGateway` tests/e2e/leak/ 0 命中
</acceptance_criteria>

### T2..T9 — 逐条迁移 LEAK-01..08

<read_first>
- tests/e2e/leak/leak_{N}_*.go
- tests/e2e/leak/helpers_test.go (refactored)
</read_first>

<action>
对每条 LEAK-01..08 用例：
1. 迁移 Scenario builder 调用到新 API
2. 保持断言语义不变（检查的内容和预期结果不变）
3. 更新抓包视角注释
4. 确保 compile + test 通过
</action>

<acceptance_criteria>
- 8 条 LEAK 用例 `go test -tags=e2e -run "TestLeak" ./tests/e2e/leak/ -v` 全绿
</acceptance_criteria>
