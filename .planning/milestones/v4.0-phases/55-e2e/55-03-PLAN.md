---
phase: 55
plan: "03"
plan_name: KILL-01..04 用例迁移 + "PID 1 死 → 容器死 → 出网立即断" 新断言
created: 2026-05-27
requirements: [E2E-V4-03]
decisions: []
depends_on: ["55-01"]
estimated_effort: 1 day
wave: 2
---

# Plan 55-03: KILL 用例迁移

## Objective

将 v3.6 KILL-01..04 用例迁移到单容器架构。核心变化：`docker kill <gw>` 改为 `docker exec <user> kill -9 $(pidof sing-box)`。新增断言 "PID 1 死 → 容器死 → 出网立即断"。

## Scope

| REQ | 实现位置 | 验收 |
|---|---|---|
| E2E-V4-03 | `tests/e2e/killswitch_*.go` + `tests/e2e/killswitch_stress/` | 4+4 条 KILL 用例 + 新断言 |

## Files Touched

- `tests/e2e/killswitch_singbox_crash_test.go` — KILL 核心
- `tests/e2e/killswitch_resolvconf_tamper_test.go` — resolv.conf 篡改
- `tests/e2e/killswitch_stress/killswitch_01_sigkill_timing_test.go`
- `tests/e2e/killswitch_stress/killswitch_02_tun_down_test.go`
- `tests/e2e/killswitch_stress/killswitch_03_netem_delay_test.go`
- `tests/e2e/killswitch_stress/killswitch_04_disconnect_test.go`
- `tests/e2e/killswitch_stress/helpers_test.go`
- `tests/e2e/killswitch_stress/suite_test.go`

## Tasks

### T1 — 迁移 KILL 核心用例 (killswitch_singbox_crash)

<read_first>
- tests/e2e/killswitch_singbox_crash_test.go
- tests/e2e/harness/scenario.go (refactored)
</read_first>

<action>
1. 删除 `WithSingBoxGateway()` → 合并到 `WithUser()`
2. `docker kill <gw>` → `docker exec <user> kill -9 $(pidof sing-box)`
3. 新增断言：
   - sing-box 被 kill 后容器在 ≤3s 内退出（entrypoint fail-closed）
   - 容器退出后出网立即断（tcpdump 在新 ns 不可见）
4. 验证 docker `--restart=on-failure` 在 ≤5s 内拉起新容器
</action>

<acceptance_criteria>
- `go build -tags=e2e ./tests/e2e/...` 通过
- 断言覆盖：kill → exit ≤3s → network dead → restart ≤5s
</acceptance_criteria>

### T2 — 迁移 resolv.conf 篡改用例

<read_first>
- tests/e2e/killswitch_resolvconf_tamper_test.go
</read_first>

<action>
迁移到单容器 API，断言语义不变：篡改 resolv.conf 后 DNS 仍走 tun 或被拒绝。
</action>

<acceptance_criteria>
- 用例编译通过，断言与 v3.6 等价
</acceptance_criteria>

### T3 — 迁移 KILL 压力用例 (killswitch_stress 01..04)

<read_first>
- tests/e2e/killswitch_stress/*.go
</read_first>

<action>
逐条迁移 KILL 压力用例：
- 01: SIGKILL 时机测试 → `docker exec <user> kill -9 $(pidof sing-box)`
- 02: tun0 down 测试 → 容器内 `ip link set tun0 down`（但 user 无 NET_ADMIN，预期失败 → 改测试语义验证无权限）
- 03: netem delay 测试 → 保持不变（故障注入目标改为 user 容器）
- 04: disconnect 测试 → `docker network disconnect` 改为针对 user 容器
</action>

<acceptance_criteria>
- 4 条压力用例编译通过
- `go build -tags=e2e ./tests/e2e/killswitch_stress/...` 通过
</acceptance_criteria>
