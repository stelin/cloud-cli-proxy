---
phase: 55
plan: "04"
plan_name: GoldenPath + MVS-01..10 迁移到单容器
created: 2026-05-27
requirements: [E2E-V4-04]
decisions: []
depends_on: ["55-01"]
estimated_effort: 1 day
wave: 2
---

# Plan 55-04: GoldenPath + MVS 迁移

## Objective

将 v3.6 GoldenPath 与 MVS-01..10 全部用例迁移到单容器架构。所有出口 IP / DNS / default-deny / 错误码契约保持不变。

## Scope

| REQ | 实现位置 | 验收 |
|---|---|---|
| E2E-V4-04 | `tests/e2e/*.go`（bootstrap/egress/dns/default_deny/cli_error_codes/expiry/host_agent_recovery） | 全部 MVS + GoldenPath 用例编译并逻辑正确 |

## Files Touched

- `tests/e2e/bootstrap_test.go` — 黄金路径
- `tests/e2e/egress_ip_test.go` — 出口 IP 验证
- `tests/e2e/egress_ip_binding_test.go` — IP 绑定契约
- `tests/e2e/dns_test.go` — DNS 验证
- `tests/e2e/default_deny_test.go` — default-deny 验证
- `tests/e2e/cli_error_codes_test.go` — CLI 错误码契约
- `tests/e2e/expiry_test.go` — 到期治理
- `tests/e2e/host_agent_recovery_test.go` — host-agent 心跳恢复
- `tests/e2e/scenario_smoke_test.go` — smoke
- `tests/e2e/suite_test.go`
- `tests/e2e/helpers.go` + `tests/e2e/helpers_linux.go`

## Tasks

### T1 — 迁移 golden path bootstrap 用例

<read_first>
- tests/e2e/bootstrap_test.go
- tests/e2e/harness/scenario.go (refactored)
</read_first>

<action>
1. 删除 `WithSingBoxGateway()` 调用 → `WithUser()` 设置 outboundConfig
2. 删除 `SingBoxGateway()` 访问 → 通过 `User()` 获取容器信息
3. Bootstrap 流程保持：create host → wait ready → SSH connect → verify egress
4. 单容器：host ready = user 容器 running + sing-box tun up
</action>

<acceptance_criteria>
- `go build -tags=e2e ./tests/e2e/...` 通过
- Bootstrap 用例逻辑完整
</acceptance_criteria>

### T2 — 迁移 egress IP / DNS / default-deny 用例

<read_first>
- tests/e2e/egress_ip_test.go
- tests/e2e/egress_ip_binding_test.go
- tests/e2e/dns_test.go
- tests/e2e/default_deny_test.go
</read_first>

<action>
迁移所有验证用例到单容器 API：
- 出口 IP 断言不变（curl ip.me → 绑定出口 IP）
- DNS 断言不变（走 tun DNS stub）
- default-deny 断言不变（直连外网被拒）
- IP 双绑互斥契约不变（ErrCodeEgressIPAlreadyBound + 409）
</action>

<acceptance_criteria>
- 4 个测试文件编译通过
- 断言语义与 v3.6 等价
</acceptance_criteria>

### T3 — 迁移 CLI 错误码 / 到期 / 恢复用例

<read_first>
- tests/e2e/cli_error_codes_test.go
- tests/e2e/expiry_test.go
- tests/e2e/host_agent_recovery_test.go
</read_first>

<action>
迁移治理与错误码用例到单容器 API：
- CLI 错误码契约不变
- 到期容器自动停止不变
- host-agent 心跳恢复不变（心跳对象从 gw+user 变为仅 user 容器）
</action>

<acceptance_criteria>
- 3 个测试文件编译通过
- 断言语义与 v3.6 等价
</acceptance_criteria>

### T4 — 迁移 helpers + suite + smoke

<read_first>
- tests/e2e/helpers.go
- tests/e2e/helpers_linux.go
- tests/e2e/suite_test.go
- tests/e2e/scenario_smoke_test.go
</read_first>

<action>
1. helpers: 删除 `gatewayContainerName` / `gatewayImage` 等辅助函数
2. suite: 更新 `SetupSuite` 中 gateway 相关初始化
3. smoke: 更新 builder API 调用
</action>

<acceptance_criteria>
- `go build -tags=e2e ./tests/e2e/...` 全通过
- grep `SingBoxGateway\|WithSingBoxGateway\|gatewayContainer` tests/e2e/ 0 命中
</acceptance_criteria>
