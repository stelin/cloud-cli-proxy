---
phase: 56
plan: "01"
plan_name: CI paths 扩面 + Makefile e2e 入口
created: 2026-05-27
requirements: [CI-01, CI-02, CI-03]
decisions: []
depends_on: []
estimated_effort: 0.5 day
wave: 1
---

# Plan 56-01: CI paths 扩面 + Makefile e2e 入口

## Objective

堵塞 v3.6 CI 触发盲区，新增 `make e2e` 一条命令入口。

## Tasks

### T1 — CI paths filter 扩面 (CI-01)

<read_first>- .github/workflows/e2e.yml</read_first>
<action>
paths filter 新增：`internal/controlplane/http/**` / `internal/store/**` / `deploy/docker/**` / `Makefile` / `go.mod` / `go.sum`
</action>
<acceptance_criteria>
- grep "controlplane/http" .github/workflows/e2e.yml 命中
- grep "store/\*\*" .github/workflows/e2e.yml 命中
- grep "deploy/docker" .github/workflows/e2e.yml 命中
- grep "Makefile" .github/workflows/e2e.yml 命中
- grep "go.mod" .github/workflows/e2e.yml 命中
</acceptance_criteria>

### T2 — Makefile e2e target (CI-02)

<read_first>- Makefile</read_first>
<action>
新增 `e2e:` target：`go test -tags=e2e ./tests/e2e/... -count=1 -v -timeout=15m`
</action>
<acceptance_criteria>
- grep "^e2e:" Makefile 命中
- grep "go test -tags=e2e" Makefile 命中
</acceptance_criteria>

### T3 — e2e target 内 lint + vet (CI-03)

<read_first>- Makefile
- scripts/lint-no-bare-sleep.sh</read_first>
<action>
`make e2e` 内串入 `lint-no-bare-sleep` + `go vet -tags=e2e ./tests/e2e/...`
</action>
<acceptance_criteria>
- grep "lint-no-bare-sleep" Makefile 命中
- grep "go vet -tags=e2e" Makefile 命中
</acceptance_criteria>
