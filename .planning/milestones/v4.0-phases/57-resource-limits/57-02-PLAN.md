---
phase: 57-resource-limits
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - internal/agentapi/contracts.go
  - internal/runtime/runtime_service.go
  - internal/runtime/tasks/worker.go
  - internal/controlplane/http/admin_hosts.go
  - internal/controlplane/http/router.go
autonomous: true
requirements:
  - RES-02
  - RES-04

must_haves:
  truths:
    - "管理员可以通过 API 在创建主机时设置资源限制（不传=默认，0=无限制，正数=具体限制）"
    - "管理员可以通过 PATCH /v1/admin/hosts/:id/resources 在主机停止时修改资源限制"
    - "运行中主机调用 PATCH 返回 409 Conflict"
    - "Worker 创建容器时，DiskLimitGB > 0 则传 --storage-opt size=Xg"
    - "Worker 创建容器时，MemoryLimitMB/CPULimit/DiskLimitGB <= 0 时不传对应的 Docker 参数"
  artifacts:
    - path: "internal/agentapi/contracts.go"
      provides: "HostActionRequest.DiskLimitGB 字段"
      contains: "DiskLimitGB"
    - path: "internal/runtime/runtime_service.go"
      provides: "移除 defaultIntIfZero/defaultFloatIfZero，透传 DiskLimitGB 到 HostActionRequest"
      min_lines: 5
    - path: "internal/runtime/tasks/worker.go"
      provides: "buildCreateArgs 中新增 --storage-opt size=Xg 参数生成"
      contains: "storage-opt"
    - path: "internal/controlplane/http/admin_hosts.go"
      provides: "Create body 三态指针 + PatchResources handler + AdminHostStore 接口扩展"
      exports: ["PatchResources"]
    - path: "internal/controlplane/http/router.go"
      provides: "PATCH /v1/admin/hosts/{hostID}/resources 路由注册"
      contains: "PATCH /v1/admin/hosts/{hostID}/resources"
  key_links:
    - from: "admin_hosts.go PatchResources"
      to: "repository.UpdateHostResources"
      via: "h.store.UpdateHostResources"
      pattern: "UpdateHostResources"
    - from: "runtime_service.go QueueHostAction"
      to: "host.DiskLimitGB"
      via: "解引用 *int → int 赋值给 HostActionRequest.DiskLimitGB"
      pattern: "DiskLimitGB"
    - from: "worker.go buildCreateArgs"
      to: "request.DiskLimitGB"
      via: "if request.DiskLimitGB > 0 则追加 --storage-opt size=Xg"
      pattern: "storage-opt"
</must_haves>
---

<objective>
API 层 + Worker 层实现：在创建和 PATCH 端点实现三态指针语义（nil=默认 / 0=无限制 / 正值=限制），新增 PATCH 端点（仅允许 stopped 状态），HostActionRequest 新增 DiskLimitGB，Worker buildCreateArgs 支持 --storage-opt，移除 runtime_service 中的双层默认值兜底。

Purpose: 实现用户决策 D-02（三态指针）、D-03（PATCH 端点）、D-04（磁盘限制）、D-06（Worker 层）的完整后端逻辑。
Output: 更新后的 contracts.go、runtime_service.go、worker.go、admin_hosts.go、router.go
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-resource-limits/57-CONTEXT.md
@.planning/phases/57-resource-limits/57-RESEARCH.md

<interfaces>
<!-- 执行器需要直接使用的现有类型和契约。从代码库提取。 -->

来自 internal/agentapi/contracts.go L41-73（当前 HostActionRequest，需要新增 DiskLimitGB）:
```go
type HostActionRequest struct {
    // ...
    MemoryLimitMB int     `json:"memory_limit_mb,omitempty"`
    CPULimit      float64 `json:"cpu_limit,omitempty"`
    // DiskLimitGB 将在此新增
    // ...
}
```

来自 internal/runtime/runtime_service.go L164-188（QueueHostAction 中当前有默认值兜底）:
```go
request := agentapi.HostActionRequest{
    // ...
    MemoryLimitMB: defaultIntIfZero(host.MemoryLimitMB, 4096),
    CPULimit:      defaultFloatIfZero(host.CPULimit, 2.0),
    // DiskLimitGB 将在此新增，直接解引用 host.DiskLimitGB
}
```

来自 internal/runtime/runtime_service.go L331-343（将被移除的函数）:
```go
func defaultIntIfZero(value, fallback int) int { ... }
func defaultFloatIfZero(value, fallback float64) float64 { ... }
```

来自 internal/runtime/tasks/worker.go L268-273（当前内存/CPU 参数生成，磁盘参数将在此后追加）:
```go
if request.MemoryLimitMB > 0 {
    args = append(args, "--memory", fmt.Sprintf("%dm", request.MemoryLimitMB))
}
if request.CPULimit > 0 {
    args = append(args, "--cpus", fmt.Sprintf("%.1f", request.CPULimit))
}
```

来自 internal/controlplane/http/admin_hosts.go L31-43（AdminHostStore 接口，需要新增方法）:
```go
type AdminHostStore interface {
    ListHostsWithUsername(context.Context) ([]repository.HostWithUsername, error)
    GetHostDetail(context.Context, string) (repository.HostDetail, error)
    GetHost(context.Context, string) (repository.Host, error)
    UpsertHost(context.Context, repository.UpsertHostParams) (repository.Host, error)
    GetUser(context.Context, string) (repository.User, error)
    BindEgressIPToHost(context.Context, string, string) (repository.HostBinding, error)
    DeleteHost(context.Context, string) error
    ListHostsByUserID(context.Context, string) ([]repository.Host, error)
    ListRunningHosts(ctx context.Context) ([]repository.Host, error)
    GetHostWithClaudeAccount(ctx context.Context, hostID string) (repository.HostWithClaudeAccount, error)
    UpdateHostMounts(ctx context.Context, hostID string, mounts repository.HostMounts) error
}
```

来自 internal/controlplane/http/admin_hosts.go L147-157（Create body 当前结构，需要改为指针类型）:
```go
var body struct {
    UserID        string                `json:"user_id"`
    EgressIPID    string                `json:"egress_ip_id"`
    Timezone      string                `json:"timezone"`
    MemoryLimitMB int                   `json:"memory_limit_mb"`
    CPULimit      float64               `json:"cpu_limit"`
    DiskLimitGB   int                   `json:"disk_limit_gb"`
    HostMounts    repository.HostMounts `json:"host_mounts"`
}
```

来自 internal/controlplane/http/router.go L295-315（当前 host 路由注册，PATCH 将在此块内新增）:
```go
if deps.AdminHosts != nil {
    hostsHandler := NewAdminHostsHandler(...)
    mux.Handle("GET /v1/admin/hosts", adminGuard(hostsHandler.List()))
    mux.Handle("POST /v1/admin/hosts", adminGuard(hostsHandler.Create()))
    mux.Handle("GET /v1/admin/hosts/{hostID}", adminGuard(hostsHandler.Get()))
    // ... 其他路由
    mux.Handle("PUT /v1/admin/hosts/{hostID}/mounts", adminGuard(hostsHandler.UpdateMounts()))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contracts.go 新增 DiskLimitGB + runtime_service.go 移除默认值兜底 + 透传 DiskLimitGB</name>
  <files>internal/agentapi/contracts.go, internal/runtime/runtime_service.go</files>
  <read_first>
    internal/agentapi/contracts.go
    internal/runtime/runtime_service.go
    internal/store/repository/models.go
  </read_first>
  <action>
**一、internal/agentapi/contracts.go（约 L55 之后）**——在 `CPULimit` 字段之后新增 `DiskLimitGB` 字段：

```go
// HostActionRequest 结构体中，紧接 CPULimit float64 行之后新增：
DiskLimitGB   int     `json:"disk_limit_gb,omitempty"`  // >0 则传 --storage-opt size=Xg；<=0 不传参数
```

完整上下文（变更后 L54-56）：
```go
MemoryLimitMB int     `json:"memory_limit_mb,omitempty"`
CPULimit      float64 `json:"cpu_limit,omitempty"`
DiskLimitGB   int     `json:"disk_limit_gb,omitempty"`  // 新增
```

注意：`HostActionRequest` 的资源字段**保持非指针类型**（int/float64，不使用 *int/*float64）。Worker 层不需要三态语义——API 层已完成三态解析，Worker 只需知道"传参数(>0)还是不传参数(<=0)"。这避免了 RESEARCH.md 陷阱 3（`omitempty` 在 int 零值上会错误省略字段）。

**二、internal/runtime/runtime_service.go**——两处修改：

**修改点 1（约 L164-188）**：修改 QueueHostAction 中的 HostActionRequest 构造，将资源限制字段改为透传（移除 defaultIntIfZero/defaultFloatIfZero 兜底），新增 DiskLimitGB：

```go
// 变更前（L180-181）:
MemoryLimitMB: defaultIntIfZero(host.MemoryLimitMB, 4096),
CPULimit:      defaultFloatIfZero(host.CPULimit, 2.0),

// 变更后:
MemoryLimitMB: ptrToInt(host.MemoryLimitMB),
CPULimit:      ptrToFloat(host.CPULimit),
DiskLimitGB:   ptrToInt(host.DiskLimitGB),
```

并在同文件底部新增两个零值安全的解引用辅助函数（替换被移除的 defaultIntIfZero/defaultFloatIfZero）：

```go
// ptrToInt 将 *int 安全地解引用为 int。nil 返回 0（Worker 层：0 = 不传参数即无限制）。
func ptrToInt(v *int) int {
    if v == nil {
        return 0
    }
    return *v
}

// ptrToFloat 将 *float64 安全地解引用为 float64。nil 返回 0。
func ptrToFloat(v *float64) float64 {
    if v == nil {
        return 0
    }
    return *v
}
```

**修改点 2（约 L331-343）**：**删除** `defaultIntIfZero` 和 `defaultFloatIfZero` 两个函数（共约 13 行）。这两个函数的功能已被 API 层的三态解析 + ptrToInt/ptrToFloat 替代。
  </action>
  <verify>
    <automated>cd internal && go build ./agentapi/... ./runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - `contracts.go` 中 `HostActionRequest` 包含 `DiskLimitGB int \`json:"disk_limit_gb,omitempty"\`` 字段（grep: `DiskLimitGB`）
    - `runtime_service.go` 中 `QueueHostAction` 不再调用 `defaultIntIfZero` 或 `defaultFloatIfZero`
    - `runtime_service.go` 中存在 `ptrToInt` 和 `ptrToFloat` 辅助函数
    - `runtime_service.go` 中 `defaultIntIfZero` 和 `defaultFloatIfZero` 函数已删除
    - `go build ./internal/agentapi/... ./internal/runtime/...` 编译成功
  </acceptance_criteria>
  <done>HostActionRequest 已新增 DiskLimitGB，runtime_service 已移除默认值兜底并透传所有三个资源值到 Worker</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin_hosts.go Create 三态解析 + PatchResources handler + AdminHostStore 接口扩展</name>
  <files>internal/controlplane/http/admin_hosts.go</files>
  <read_first>
    internal/controlplane/http/admin_hosts.go
    internal/store/repository/models.go
    internal/store/repository/queries.go
  </read_first>
  <behavior>
    - Test 1: Create body 不传 memory_limit_mb 时（nil），API 层填入默认值 4096，UpsertHostParams.MemoryLimitMB 为 *int 指向 4096
    - Test 2: Create body 传 memory_limit_mb=0 时，UpsertHostParams.MemoryLimitMB 为 nil（NULL in DB = 无限制）
    - Test 3: Create body 传 memory_limit_mb=8192 时，UpsertHostParams.MemoryLimitMB 为 *int 指向 8192
    - Test 4: PATCH body 传 cpu_limit=0 且 host 状态为 stopped，返回 200
    - Test 5: PATCH 请求且 host 状态为 running，返回 409 + 错误消息 "主机正在运行中，请先停止"
  </behavior>
  <action>
**一、AdminHostStore 接口扩展（约 L31-43）**——在 `UpdateHostMounts` 之后新增一行：

```go
UpdateHostResources(ctx context.Context, hostID string, memoryLimitMB *int, cpuLimit *float64, diskLimitGB *int) error
```

完整接口新增项（加在 `UpdateHostMounts` 行之后）：
```go
type AdminHostStore interface {
    // ... 现有方法保持不变 ...
    UpdateHostMounts(ctx context.Context, hostID string, mounts repository.HostMounts) error
    UpdateHostResources(ctx context.Context, hostID string, memoryLimitMB *int, cpuLimit *float64, diskLimitGB *int) error  // Phase 57
}
```

**二、Create 端点 body 改为指针类型 + 三态解析（约 L149-157）**——修改 body struct 的资源字段为指针类型，并在构建 UpsertHostParams 前做三态转换：

```go
// 变更后的 body struct（L149-157）:
var body struct {
    UserID        string                `json:"user_id"`
    EgressIPID    string                `json:"egress_ip_id"`
    Timezone      string                `json:"timezone"`
    MemoryLimitMB *int                  `json:"memory_limit_mb"`   // nil=默认, 0=无限制, >0=具体限制
    CPULimit      *float64              `json:"cpu_limit"`         // nil=默认, 0=无限制, >0=具体限制
    DiskLimitGB   *int                  `json:"disk_limit_gb"`     // nil=默认, 0=无限制, >0=具体限制
    HostMounts    repository.HostMounts `json:"host_mounts"`
}
```

在构建 `UpsertHostParams` 之前（约 L218 之前），添加三态解析逻辑：

```go
// 三态解析：nil → 填入默认值 / 0 → NULL(无限制) / 正值 → 直接使用
resolveMemory := func(mb *int) *int {
    if mb == nil {
        def := 4096
        return &def
    }
    if *mb == 0 {
        return nil  // NULL in DB = 无限制
    }
    return mb
}
resolveCPU := func(cpu *float64) *float64 {
    if cpu == nil {
        def := 2.0
        return &def
    }
    if *cpu == 0 {
        return nil
    }
    return cpu
}
resolveDisk := func(gb *int) *int {
    if gb == nil {
        def := 20
        return &def
    }
    if *gb == 0 {
        return nil
    }
    return gb
}
```

然后将 UpsertHostParams 构造中的资源字段改为调用解析函数（~L227-229）：

```go
// 变更前:
MemoryLimitMB:    body.MemoryLimitMB,
CPULimit:         body.CPULimit,
DiskLimitGB:      body.DiskLimitGB,

// 变更后:
MemoryLimitMB:    resolveMemory(body.MemoryLimitMB),
CPULimit:         resolveCPU(body.CPULimit),
DiskLimitGB:      resolveDisk(body.DiskLimitGB),
```

**三、新增 PatchResources handler（约 L1042 的 UpdateMounts 方法之后）**——完整实现：

```go
// PatchResources 处理 PATCH /v1/admin/hosts/{hostID}/resources。
// 仅在 host.Status == "stopped" 时允许修改（D-03）。
// 请求体使用指针类型实现三态：nil=不修改该字段 / 0=无限制 / 正值=具体限制。
func (h *AdminHostsHandler) PatchResources() nethttp.Handler {
    return nethttp.HandlerFunc(func(w nethttp.ResponseWriter, r *nethttp.Request) {
        hostID := r.PathValue("hostID")

        var body struct {
            MemoryLimitMB *int     `json:"memory_limit_mb"` // nil=不修改, 0=无限制, >0=具体限制
            CPULimit      *float64 `json:"cpu_limit"`
            DiskLimitGB   *int     `json:"disk_limit_gb"`
        }
        if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
            writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "invalid request body"})
            return
        }

        // 验证范围：仅当字段非 nil 且 > 0 时做范围校验
        if body.MemoryLimitMB != nil && *body.MemoryLimitMB > 0 {
            if *body.MemoryLimitMB < 128 {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "memory_limit_mb 不能小于 128 MB"})
                return
            }
            if *body.MemoryLimitMB > 262144 { // 256 GB
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "memory_limit_mb 不能大于 262144 MB (256 GB)"})
                return
            }
        }
        if body.CPULimit != nil && *body.CPULimit > 0 {
            if *body.CPULimit < 0.1 {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "cpu_limit 不能小于 0.1 核"})
                return
            }
            if *body.CPULimit > 64 {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "cpu_limit 不能大于 64 核"})
                return
            }
        }
        if body.DiskLimitGB != nil && *body.DiskLimitGB > 0 {
            if *body.DiskLimitGB < 1 {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "disk_limit_gb 不能小于 1 GB"})
                return
            }
            if *body.DiskLimitGB > 2048 { // 2 TB
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "disk_limit_gb 不能大于 2048 GB (2 TB)"})
                return
            }
        }

        // 三态解析（与 Create 一致）：nil=不修改 / 0→NULL(无限制) / 正值→具体限制
        resolveMemory := func(mb *int) *int {
            if mb == nil { return nil }
            if *mb == 0 { return nil }
            return mb
        }
        resolveCPU := func(cpu *float64) *float64 {
            if cpu == nil { return nil }
            if *cpu == 0 { return nil }
            return cpu
        }
        resolveDisk := func(gb *int) *int {
            if gb == nil { return nil }
            if *gb == 0 { return nil }
            return gb
        }

        host, err := h.store.GetHost(r.Context(), hostID)
        if err != nil {
            if errors.Is(err, pgx.ErrNoRows) {
                writeJSON(w, nethttp.StatusNotFound, map[string]string{"error": "host not found"})
                return
            }
            h.logger.Error("get host for resource patch failed", "host_id", hostID, "error", err)
            writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "get host failed"})
            return
        }

        // D-03：仅允许 stopped 状态修改
        if host.Status != "stopped" {
            writeJSON(w, nethttp.StatusConflict, map[string]string{"error": "主机正在运行中，请先停止"})
            return
        }

        if err := h.store.UpdateHostResources(r.Context(), hostID,
            resolveMemory(body.MemoryLimitMB),
            resolveCPU(body.CPULimit),
            resolveDisk(body.DiskLimitGB),
        ); err != nil {
            h.logger.Error("update host resources failed", "host_id", hostID, "error", err)
            writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "update resources failed"})
            return
        }

        if h.events != nil {
            hid := hostID
            if _, err := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                HostID: &hid, Level: "info", Type: "admin.host.resources_updated",
                Message: "管理员更新主机资源限制",
                Metadata: map[string]any{"operator": "admin"},
            }); err != nil {
                h.logger.Error("record event failed", "type", "admin.host.resources_updated", "error", err)
            }
        }

        writeJSON(w, nethttp.StatusOK, map[string]string{"status": "ok"})
    })
}
```
  </action>
  <verify>
    <automated>cd internal/controlplane/http && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `AdminHostStore` 接口包含 `UpdateHostResources(ctx context.Context, hostID string, memoryLimitMB *int, cpuLimit *float64, diskLimitGB *int) error` 方法签名（grep: `UpdateHostResources`）
    - Create body 中 `MemoryLimitMB` 类型为 `*int`（grep: `MemoryLimitMB\s+\*int`）
    - Create body 中 `CPULimit` 类型为 `*float64`（grep: `CPULimit\s+\*float64`）
    - Create body 中 `DiskLimitGB` 类型为 `*int`（grep: `DiskLimitGB\s+\*int`）
    - 存在 `resolveMemory` / `resolveCPU` / `resolveDisk` 三态解析函数（Create 和 PatchResources 各一份或共用）
    - `PatchResources()` 方法存在且包含 `host.Status != "stopped"` 的 409 检查
    - PatchResources 中包含范围验证：内存 128-262144 MB、CPU 0.1-64 核、磁盘 1-2048 GB
    - PATCH 成功时写入 `admin.host.resources_updated` 事件
    - `go build ./internal/controlplane/http/...` 编译成功
  </acceptance_criteria>
  <done>Create 端点支持三态指针，PatchResources handler 已实现（含状态检查、范围验证、事件记录），AdminHostStore 接口已扩展</done>
</task>

<task type="auto">
  <name>Task 3: router.go PATCH 路由注册 + worker.go --storage-opt 磁盘限制参数</name>
  <files>internal/controlplane/http/router.go, internal/runtime/tasks/worker.go</files>
  <read_first>
    internal/controlplane/http/router.go
    internal/runtime/tasks/worker.go
    internal/agentapi/contracts.go
  </read_first>
  <action>
**一、internal/controlplane/http/router.go**——在 hosts 路由块中注册新 PATCH 端点。

位置：在 `router.go` 中 `mux.Handle("PUT /v1/admin/hosts/{hostID}/mounts", adminGuard(hostsHandler.UpdateMounts()))` 这一行（约 L315）之后新增：

```go
mux.Handle("PATCH /v1/admin/hosts/{hostID}/resources", adminGuard(hostsHandler.PatchResources()))
```

注意：
- 使用 Go 1.22+ 原生方法路由语法 `"PATCH /v1/admin/hosts/{hostID}/resources"`
- 与现有路由风格保持一致（adminGuard 包装）
- 放在 `UpdateMounts` 的 PUT 路由之后，逻辑上相邻（两者都是"修改现有主机配置"类操作）

**二、internal/runtime/tasks/worker.go**——在 `buildCreateArgs()` 中新增磁盘限制参数。

位置：在现有内存/CPU 参数块之后（约 L273 `if request.CPULimit > 0 { ... }` 块结束之后），新增磁盘限制参数生成：

```go
if request.DiskLimitGB > 0 {
    args = append(args, "--storage-opt", fmt.Sprintf("size=%dG", request.DiskLimitGB))
}
```

完整上下文（变更后 L268-278）：
```go
if request.MemoryLimitMB > 0 {
    args = append(args, "--memory", fmt.Sprintf("%dm", request.MemoryLimitMB))
}
if request.CPULimit > 0 {
    args = append(args, "--cpus", fmt.Sprintf("%.1f", request.CPULimit))
}
if request.DiskLimitGB > 0 {
    args = append(args, "--storage-opt", fmt.Sprintf("size=%dG", request.DiskLimitGB))
}
```

说明：
- `--storage-opt size=Xg` 需要宿主机存储驱动支持 overlay2 + xfs + pquota。不支持的宿主机上 Docker 会静默忽略该参数（不会阻塞 docker create）。
- DiskLimitGB 类型为 int（来自 HostActionRequest），`fmt.Sprintf("size=%dG", ...)` 生成如 `size=20G` 的格式。
  </action>
  <verify>
    <automated>grep -n 'PATCH.*resources' internal/controlplane/http/router.go && grep -n 'storage-opt' internal/runtime/tasks/worker.go</automated>
  </verify>
  <acceptance_criteria>
    - router.go 中存在 `mux.Handle("PATCH /v1/admin/hosts/{hostID}/resources", adminGuard(hostsHandler.PatchResources()))` 路由注册（grep: `PATCH /v1/admin/hosts/.*resources`）
    - worker.go 的 buildCreateArgs 中存在 `if request.DiskLimitGB > 0 { args = append(args, "--storage-opt", ...) }` 代码块（grep: `storage-opt`）
    - `--storage-opt` 参数格式为 `size=%dG`（如 `size=20G`）
    - `go build ./internal/controlplane/http/... ./internal/runtime/...` 编译成功
  </acceptance_criteria>
  <done>PATCH 路由已注册，worker buildCreateArgs 已支持 --storage-opt 磁盘限制参数，全后端编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API (PATCH) | 前端/HTTP 客户端发送资源限制修改请求，需验证权限和参数范围 |
| API -> DB (UpdateHostResources) | 指针参数直接写入 PostgreSQL，需防 SQL 注入（pgx 参数化查询自动防护） |
| API -> Worker (HostActionRequest) | 资源限制值从 DB 读取后传递给 Worker 生成 docker create 参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering | PatchResources body | mitigate | 资源值做范围校验：内存 128-262144 MB、CPU 0.1-64 核、磁盘 1-2048 GB。拒绝超出范围的值返回 400。 |
| T-57-04 | Elevation of Privilege | PatchResources status check | mitigate | 强制检查 `host.Status != "stopped"` 返回 409，防止绕过状态修改运行中容器的资源限制（`docker update` 未实现，修改不会生效）。 |
| T-57-05 | Denial of Service | PatchResources disk limit | mitigate | 磁盘上限 2048 GB (2 TB)，防止恶意设置超大值填满宿主机存储。 |
| T-57-06 | Tampering | Worker --storage-opt injection | accept | `fmt.Sprintf("size=%dG", request.DiskLimitGB)` 使用 %d 格式化 int，无字符串注入风险。Docker 在 overlay2 之外驱动上静默忽略该参数，不会导致容器创建失败。 |
| T-57-07 | Repudiation | 无审计日志 | mitigate | PatchResources 成功后写入 `admin.host.resources_updated` 事件，记录操作人（operator: admin）和目标主机 ID。 |
| T-57-08 | Spoofing | PATCH 端点 | accept | 路由套用 `adminGuard` 中间件，依赖现有 JWT 认证。非管理员无法访问。 |
</threat_model>

<verification>
## 整体验证

```bash
# 编译后端全部包
go build ./...

# 运行单元测试
go test ./internal/agentapi/... ./internal/runtime/... ./internal/controlplane/http/... -count=1 -short

# 确认 PATCH 路由存在
grep -c 'PATCH /v1/admin/hosts/{hostID}/resources' internal/controlplane/http/router.go

# 确认 worker 包含 --storage-opt
grep -c 'storage-opt' internal/runtime/tasks/worker.go

# 确认 contract 包含 DiskLimitGB
grep -c 'DiskLimitGB' internal/agentapi/contracts.go

# 确认 defaultIntIfZero 和 defaultFloatIfZero 已被移除
grep -c 'defaultIntIfZero\|defaultFloatIfZero' internal/runtime/runtime_service.go
```

预期：全部编译成功，grep 计数匹配预期，defaultIntIfZero/defaultFloatIfZero 的 grep 返回 0。
</verification>

<success_criteria>
- [ ] contracts.go 包含 `DiskLimitGB` 字段
- [ ] runtime_service.go 不再包含 `defaultIntIfZero` 和 `defaultFloatIfZero`
- [ ] runtime_service.go 中 QueueHostAction 透传 `DiskLimitGB`（通过 ptrToInt 解引用）
- [ ] Create body 资源字段已改为指针类型（*int / *float64）
- [ ] Create 端点包含三态解析逻辑（nil=默认 / 0=无限制 / 正值=限制）
- [ ] PatchResources handler 存在，包含 stopped 状态检查（409）和范围验证
- [ ] AdminHostStore 接口包含 `UpdateHostResources` 方法签名
- [ ] router.go 包含 `PATCH /v1/admin/hosts/{hostID}/resources` 路由
- [ ] worker.go buildCreateArgs 在 `DiskLimitGB > 0` 时传递 `--storage-opt size=Xg`
- [ ] `go build ./...` 成功
</success_criteria>

<output>
完成后创建 `.planning/phases/57-resource-limits/57-02-SUMMARY.md`
</output>
