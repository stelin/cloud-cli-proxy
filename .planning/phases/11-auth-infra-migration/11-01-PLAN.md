---
phase: 11-auth-infra-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/migrations/0007_auth_unification.sql
  - internal/store/repository/models.go
  - internal/store/repository/queries.go
  - internal/controlplane/http/auth.go
autonomous: true
requirements: [AUTH-01, CLAUDE-01]

must_haves:
  truths:
    - "users 表包含 role 列，默认值为 'user'"
    - "claude_accounts 表已创建，外键关联 users 和 hosts"
    - "User Go 结构体包含 Role 和 PasswordHash 字段"
    - "AuthClaims 结构体可用于签发带 user_id + role 的 JWT"
    - "GetUserByShortIDForAuth 查询返回包含 password_hash 和 role 的用户记录"
  artifacts:
    - path: "internal/store/migrations/0007_auth_unification.sql"
      provides: "role 列和 claude_accounts 表的 DDL"
      contains: "ALTER TABLE users ADD COLUMN"
    - path: "internal/store/repository/models.go"
      provides: "User.Role, ClaudeAccount 结构体"
      contains: "ClaudeAccount"
    - path: "internal/store/repository/queries.go"
      provides: "GetUserByShortIDForAuth, CreateUserWithRole 查询"
      contains: "GetUserByShortIDForAuth"
    - path: "internal/controlplane/http/auth.go"
      provides: "AuthClaims 结构体和 GenerateToken 函数"
      contains: "AuthClaims"
  key_links:
    - from: "internal/store/migrations/0007_auth_unification.sql"
      to: "internal/store/repository/queries.go"
      via: "SQL 列名与 Go Scan 字段对齐"
      pattern: "role.*Role"
---

<objective>
数据库迁移与认证基础类型：创建 0007 迁移文件（users.role 列 + claude_accounts 表），扩展 Go 模型和查询以支持角色和 Claude 账号，定义统一 JWT claims 结构体。

Purpose: 为 Plan 02/03 的统一登录端点、中间件和前端提供数据层和类型基础。
Output: 迁移 SQL、扩展后的 models.go/queries.go、auth.go（claims + token 工具）
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auth-infra-migration/11-CONTEXT.md
@.planning/phases/11-auth-infra-migration/11-RESEARCH.md

<interfaces>
<!-- 现有代码中需要了解的关键接口 -->

From internal/store/repository/models.go:
```go
type User struct {
    ID            string     `json:"id"`
    Username      string     `json:"username"`
    Status        string     `json:"status"`
    ShortID       string     `json:"short_id"`
    EntryPassword string     `json:"entry_password,omitempty"`
    ExpiresAt     *time.Time `json:"expires_at,omitempty"`
    CreatedAt     time.Time  `json:"created_at"`
    UpdatedAt     time.Time  `json:"updated_at"`
}

type AdminConfig struct {
    Username  string
    Password  string
    JWTSecret []byte
}

type CreateUserParams struct {
    Username      string
    PasswordHash  string
    ShortID       string
    EntryPassword string
}
```

From internal/controlplane/http/admin_auth.go:
```go
// 现有 JWT 签发 — 只用 RegisteredClaims，需要扩展为 AuthClaims
claims := jwt.RegisteredClaims{
    Subject:   "admin",
    Issuer:    "cloud-cli-proxy",
    IssuedAt:  jwt.NewNumericDate(now),
    ExpiresAt: jwt.NewNumericDate(now.Add(24 * time.Hour)),
}
```

From internal/store/repository/queries.go:
```go
// 现有 ListUsers SELECT — 需要扩展加入 role 列
SELECT id::text, username, status, COALESCE(short_id, ''), COALESCE(entry_password, ''), expires_at, created_at, updated_at
FROM users
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 数据库迁移 0007 + Go 模型扩展</name>
  <files>
    internal/store/migrations/0007_auth_unification.sql,
    internal/store/repository/models.go
  </files>
  <read_first>
    internal/store/migrations/0006_host_resource_limits.sql,
    internal/store/migrations/0001_initial.sql,
    internal/store/migrations/0005_host_env_and_user_entry.sql,
    internal/store/repository/models.go
  </read_first>
  <action>
1. 创建 `internal/store/migrations/0007_auth_unification.sql`，内容如下（per D-04, D-07, D-09, D-10）：

```sql
-- 1. users 表增加 role 列（per D-04）
ALTER TABLE users ADD COLUMN IF NOT EXISTS role TEXT NOT NULL DEFAULT 'user';

-- 2. 废弃 entry_password，保留列但标记将来删除（per D-07）
-- 注意：不在此迁移中 DROP COLUMN，避免破坏现有代码。entry.go 改造在 Plan 02 中完成后可安全删除。

-- 3. claude_accounts 表（per D-09, D-10）
CREATE TABLE IF NOT EXISTS claude_accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    host_id UUID REFERENCES hosts (id) ON DELETE SET NULL,
    email TEXT NOT NULL,
    display_name TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'active',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_claude_accounts_user_id ON claude_accounts (user_id);
CREATE INDEX IF NOT EXISTS idx_claude_accounts_host_id ON claude_accounts (host_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_claude_accounts_email ON claude_accounts (email);
```

2. 修改 `internal/store/repository/models.go`：
   - `User` 结构体增加 `Role string \`json:"role"\`` 字段（在 Status 之后）和 `PasswordHash string \`json:"password_hash,omitempty"\`` 字段（在 ShortID 之后）
   - 新增 `ClaudeAccount` 结构体：
     ```go
     type ClaudeAccount struct {
         ID          string    `json:"id"`
         UserID      string    `json:"user_id"`
         HostID      *string   `json:"host_id,omitempty"`
         Email       string    `json:"email"`
         DisplayName string    `json:"display_name"`
         Status      string    `json:"status"`
         CreatedAt   time.Time `json:"created_at"`
         UpdatedAt   time.Time `json:"updated_at"`
     }
     ```
   - 新增 `CreateUserWithRoleParams` 结构体：
     ```go
     type CreateUserWithRoleParams struct {
         Username     string
         PasswordHash string
         ShortID      string
         Role         string
     }
     ```
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy && go build ./internal/store/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "role TEXT NOT NULL DEFAULT 'user'" internal/store/migrations/0007_auth_unification.sql
    - grep -q "CREATE TABLE IF NOT EXISTS claude_accounts" internal/store/migrations/0007_auth_unification.sql
    - grep -q "idx_claude_accounts_user_id" internal/store/migrations/0007_auth_unification.sql
    - grep -q "idx_claude_accounts_email" internal/store/migrations/0007_auth_unification.sql
    - grep -q 'Role.*string.*json:"role"' internal/store/repository/models.go
    - grep -q 'PasswordHash.*string' internal/store/repository/models.go
    - grep -q "type ClaudeAccount struct" internal/store/repository/models.go
    - grep -q "type CreateUserWithRoleParams struct" internal/store/repository/models.go
    - go build ./internal/store/... 成功无报错
  </acceptance_criteria>
  <done>迁移文件包含 role 列、claude_accounts 表和索引；User 结构体包含 Role 和 PasswordHash；ClaudeAccount 模型已定义</done>
</task>

<task type="auto">
  <name>Task 2: 查询层扩展 + AuthClaims 类型定义</name>
  <files>
    internal/store/repository/queries.go,
    internal/controlplane/http/auth.go
  </files>
  <read_first>
    internal/store/repository/queries.go,
    internal/store/repository/models.go,
    internal/controlplane/http/admin_auth.go,
    internal/controlplane/http/bootstrap_auth.go
  </read_first>
  <action>
1. 修改 `internal/store/repository/queries.go`：
   - 所有 SELECT users 的查询（ListUsers、GetUser、CreateUser、UpdateUserStatus、UpdateUserExpiry 等）在 SELECT 列表中加入 `role` 列，在 Scan 中对应 `&item.Role`
   - 所有 SELECT users 的查询还需加入 `COALESCE(password_hash, '')` 列，Scan 对应 `&item.PasswordHash`
   - 注意列的顺序需要在现有列之后、created_at 之前添加（或紧跟 short_id 之后）
   - 新增查询函数 `GetUserByShortIDForAuth`：
     ```go
     func (r *Repository) GetUserByShortIDForAuth(ctx context.Context, shortID string) (User, error) {
         var item User
         if err := r.db.QueryRow(ctx, `
             SELECT id::text, username, status, COALESCE(short_id, ''), role,
                    COALESCE(password_hash, ''), expires_at, created_at, updated_at
             FROM users WHERE short_id = $1
         `, shortID).Scan(&item.ID, &item.Username, &item.Status, &item.ShortID, &item.Role,
             &item.PasswordHash, &item.ExpiresAt, &item.CreatedAt, &item.UpdatedAt); err != nil {
             return User{}, fmt.Errorf("get user by short_id for auth: %w", err)
         }
         return item, nil
     }
     ```
   - 新增 `CreateUserWithRole`：
     ```go
     func (r *Repository) CreateUserWithRole(ctx context.Context, params CreateUserWithRoleParams) (User, error) {
         var item User
         if err := r.db.QueryRow(ctx, `
             INSERT INTO users (username, password_hash, status, short_id, role)
             VALUES ($1, $2, 'active', $3, $4)
             RETURNING id::text, username, status, COALESCE(short_id, ''), role,
                       COALESCE(password_hash, ''), expires_at, created_at, updated_at
         `, params.Username, params.PasswordHash, params.ShortID, params.Role).Scan(
             &item.ID, &item.Username, &item.Status, &item.ShortID, &item.Role,
             &item.PasswordHash, &item.ExpiresAt, &item.CreatedAt, &item.UpdatedAt,
         ); err != nil {
             return User{}, fmt.Errorf("create user with role: %w", err)
         }
         return item, nil
     }
     ```

2. 创建 `internal/controlplane/http/auth.go`，定义认证核心类型（per D-01, D-02）：
   ```go
   package http

   import (
       "time"
       "github.com/golang-jwt/jwt/v5"
   )

   const BcryptCost = 10 // bcrypt.DefaultCost，统一项目级常量

   // AuthClaims 统一 JWT claims（per D-01）
   type AuthClaims struct {
       jwt.RegisteredClaims
       UserID string `json:"user_id"`
       Role   string `json:"role"` // "admin" | "user"
   }

   // GenerateAuthToken 签发带 user_id + role 的 JWT（per D-01）
   func GenerateAuthToken(secret []byte, userID, role string, expiry time.Duration) (string, error) {
       now := time.Now()
       claims := AuthClaims{
           RegisteredClaims: jwt.RegisteredClaims{
               Subject:   userID,
               Issuer:    "cloud-cli-proxy",
               IssuedAt:  jwt.NewNumericDate(now),
               ExpiresAt: jwt.NewNumericDate(now.Add(expiry)),
           },
           UserID: userID,
           Role:   role,
       }
       token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
       return token.SignedString(secret)
   }
   ```

注意：修改现有查询的 SELECT/Scan 列时，必须同步修改所有使用 User 结构体的查询函数（ListUsers、GetUser、CreateUser、UpdateUserStatus、DeleteUser 的 RETURNING、UpdateUserExpiry、GetBootstrapUserByUsername 保持不动因为它用独立结构体、GetUserByShortID 需要更新）。EntryPassword 列暂时保留在查询中避免破坏 entry.go。
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy && go build ./internal/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "GetUserByShortIDForAuth" internal/store/repository/queries.go
    - grep -q "CreateUserWithRole" internal/store/repository/queries.go
    - grep -q "type AuthClaims struct" internal/controlplane/http/auth.go
    - grep -q "GenerateAuthToken" internal/controlplane/http/auth.go
    - grep -q "BcryptCost" internal/controlplane/http/auth.go
    - grep -q "user_id.*role" internal/controlplane/http/auth.go
    - go build ./internal/... 成功无报错
  </acceptance_criteria>
  <done>所有 User 查询包含 role 和 password_hash 列；GetUserByShortIDForAuth 和 CreateUserWithRole 可用；AuthClaims + GenerateAuthToken 定义完成且项目编译通过</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` 编译通过
- 0007 迁移文件存在且包含 role 列和 claude_accounts 表
- AuthClaims 结构体和 GenerateAuthToken 函数在 auth.go 中定义
- 所有 User 查询包含 role 列
</verification>

<success_criteria>
- 迁移文件 0007_auth_unification.sql 定义了 role 列（DEFAULT 'user'）和 claude_accounts 表（含外键和索引）
- User 结构体包含 Role 和 PasswordHash 字段
- ClaudeAccount 结构体已定义
- AuthClaims 可用于签发带 user_id + role 的 JWT
- 整个项目 `go build ./internal/...` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/11-auth-infra-migration/11-01-SUMMARY.md`
</output>
