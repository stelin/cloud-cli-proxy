---
phase: 12-api
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - web/admin/src/lib/portal-api.ts
  - web/admin/src/hooks/use-portal-hosts.ts
  - web/admin/src/routes/_portal/index.tsx
  - web/admin/src/routes/_portal/hosts/$hostId.tsx
  - web/admin/src/components/layout/topbar.tsx
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03]

must_haves:
  truths:
    - "用户登录后在 /portal 看到自己的主机卡片列表，含主机名、状态、出口 IP、创建时间"
    - "用户点击主机卡片进入 /portal/hosts/{id} 详情页，看到出口 IP 和隧道类型"
    - "用户在详情页点击重建按钮后弹出确认对话框，确认后触发重建并显示进度"
    - "Topbar 在 portal 路由下显示正确标题，右侧显示用户角色"
  artifacts:
    - path: "web/admin/src/lib/portal-api.ts"
      provides: "portalApiFetch with /v1/user base URL"
      exports: ["portalApiFetch"]
    - path: "web/admin/src/hooks/use-portal-hosts.ts"
      provides: "React Query hooks for portal host data"
      exports: ["useMyHosts", "useMyHostDetail", "useRebuildHost"]
    - path: "web/admin/src/routes/_portal/index.tsx"
      provides: "Portal host list page with cards"
      contains: "PortalHostList"
    - path: "web/admin/src/routes/_portal/hosts/$hostId.tsx"
      provides: "Portal host detail page with rebuild"
      contains: "PortalHostDetail"
  key_links:
    - from: "web/admin/src/hooks/use-portal-hosts.ts"
      to: "web/admin/src/lib/portal-api.ts"
      via: "import portalApiFetch"
      pattern: "portalApiFetch.*\/hosts"
    - from: "web/admin/src/routes/_portal/index.tsx"
      to: "web/admin/src/hooks/use-portal-hosts.ts"
      via: "useMyHosts hook"
      pattern: "useMyHosts"
    - from: "web/admin/src/routes/_portal/hosts/$hostId.tsx"
      to: "web/admin/src/hooks/use-portal-hosts.ts"
      via: "useMyHostDetail + useRebuildHost hooks"
      pattern: "useMyHostDetail|useRebuildHost"
---

<objective>
用户自助面板前端：创建用户级 API 客户端、数据 hooks、主机列表页和详情页（含重建功能）。

Purpose: 让用户在浏览器中查看自己的主机状态、出口 IP 并触发重建操作。
Output: portal-api.ts + use-portal-hosts.ts + 主机列表页 + 主机详情页 + Topbar 适配
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-api/12-CONTEXT.md
@.planning/phases/12-api/12-RESEARCH.md
@.planning/phases/12-api/12-01-SUMMARY.md

<interfaces>
<!-- Backend API contracts from Plan 01 -->

GET /v1/user/hosts response:
```json
{
  "hosts": [
    {
      "id": "uuid",
      "hostname": "DESKTOP-XXXX",
      "status": "running",
      "egress_ip": "1.2.3.4",
      "created_at": "2026-01-01T00:00:00Z"
    }
  ]
}
```

GET /v1/user/hosts/{hostID} response:
```json
{
  "id": "uuid",
  "hostname": "DESKTOP-XXXX",
  "status": "running",
  "timezone": "America/Los_Angeles",
  "created_at": "2026-01-01T00:00:00Z",
  "updated_at": "2026-01-01T00:00:00Z",
  "egress_bindings": [
    {"ip_address": "1.2.3.4", "tunnel_type": "wireguard"}
  ]
}
```

POST /v1/user/hosts/{hostID}/rebuild response:
```json
{"task_id": "uuid", "status": "202 Accepted"}
```

From web/admin/src/lib/api.ts:
```typescript
export class ApiError extends Error {
  constructor(public status: number, message: string) { super(message); }
}
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
```

From web/admin/src/lib/auth.ts:
```typescript
export function getRole(): string | null
export function isAdmin(): boolean
export function isAuthenticated(): boolean
```

From web/admin/src/hooks/use-hosts.ts (admin pattern reference):
```typescript
export function useHosts() { return useQuery({ queryKey: ["hosts"], ... }) }
export function useHostDetail(hostId: string) { return useQuery({ queryKey: ["hosts", hostId], ... }) }
export function useHostAction() { return useMutation({ ... }) }
```

From web/admin/src/components/layout/topbar.tsx:
```typescript
const pageTitles: Record<string, string> = {
  "/": "仪表板",
  "/users": "用户管理",
  "/egress-ips": "出口 IP",
  "/hosts": "主机管理",
  "/tasks": "任务列表",
};
// 右侧硬编码 <span>管理员</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal API 客户端 + React Query Hooks</name>
  <files>
    web/admin/src/lib/portal-api.ts,
    web/admin/src/hooks/use-portal-hosts.ts
  </files>
  <read_first>
    web/admin/src/lib/api.ts,
    web/admin/src/hooks/use-hosts.ts,
    web/admin/src/lib/auth.ts
  </read_first>
  <action>
**1) 创建 `web/admin/src/lib/portal-api.ts`（per D-01 — 使用 /v1/user 前缀，不复用 admin 的 apiFetch）：**

```typescript
import { ApiError } from "./api";

const PORTAL_API_BASE = "/v1/user";

export async function portalApiFetch<T>(
  path: string,
  init?: RequestInit,
): Promise<T> {
  const token = localStorage.getItem("admin_token");
  const res = await fetch(`${PORTAL_API_BASE}${path}`, {
    ...init,
    headers: {
      "Content-Type": "application/json",
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
      ...init?.headers,
    },
  });

  if (res.status === 401) {
    localStorage.removeItem("admin_token");
    window.location.href = "/login";
    throw new ApiError(401, "Unauthorized");
  }

  if (!res.ok) {
    const body = await res.text();
    throw new ApiError(res.status, body);
  }

  if (res.status === 204) {
    return undefined as T;
  }

  return res.json();
}
```

**2) 创建 `web/admin/src/hooks/use-portal-hosts.ts`，定义 TS 类型和三个 hooks：**

TS 类型（对应后端 UserHostSummary / UserHostDetail）：
```typescript
export interface PortalHost {
  id: string;
  hostname: string;
  status: string;
  egress_ip: string;
  created_at: string;
}

export interface PortalEgressBinding {
  ip_address: string;
  tunnel_type: string;
}

export interface PortalHostDetail {
  id: string;
  hostname: string;
  status: string;
  timezone: string;
  created_at: string;
  updated_at: string;
  egress_bindings: PortalEgressBinding[];
}
```

Hooks（对标 use-hosts.ts 的 React Query 模式）：

```typescript
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { portalApiFetch } from "@/lib/portal-api";

export function useMyHosts() {
  return useQuery({
    queryKey: ["portal", "hosts"],
    queryFn: () => portalApiFetch<{ hosts: PortalHost[] }>("/hosts"),
  });
}

export function useMyHostDetail(hostId: string) {
  return useQuery({
    queryKey: ["portal", "hosts", hostId],
    queryFn: () => portalApiFetch<PortalHostDetail>(`/hosts/${hostId}`),
    enabled: !!hostId,
  });
}

export function useRebuildHost() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (hostId: string) =>
      portalApiFetch<{ task_id: string }>(`/hosts/${hostId}/rebuild`, { method: "POST" }),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["portal", "hosts"] });
    },
  });
}
```
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy/web/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/admin/src/lib/portal-api.ts 包含 `const PORTAL_API_BASE = "/v1/user"`
    - web/admin/src/lib/portal-api.ts 包含 `export async function portalApiFetch`
    - web/admin/src/hooks/use-portal-hosts.ts 包含 `export interface PortalHost`
    - web/admin/src/hooks/use-portal-hosts.ts 包含 `export function useMyHosts()`
    - web/admin/src/hooks/use-portal-hosts.ts 包含 `export function useMyHostDetail(`
    - web/admin/src/hooks/use-portal-hosts.ts 包含 `export function useRebuildHost()`
    - web/admin/src/hooks/use-portal-hosts.ts 包含 `queryKey: ["portal", "hosts"]`
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>portalApiFetch 使用 /v1/user 前缀，三个 hooks 类型正确，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 主机列表页 + 主机详情页 + 重建对话框 + Topbar 适配</name>
  <files>
    web/admin/src/routes/_portal/index.tsx,
    web/admin/src/routes/_portal/hosts/$hostId.tsx,
    web/admin/src/components/layout/topbar.tsx
  </files>
  <read_first>
    web/admin/src/routes/_portal/index.tsx,
    web/admin/src/routes/_portal.tsx,
    web/admin/src/routes/_dashboard/hosts/index.tsx,
    web/admin/src/components/layout/topbar.tsx,
    web/admin/src/lib/auth.ts,
    web/admin/src/hooks/use-portal-hosts.ts
  </read_first>
  <action>
**1) 替换 `web/admin/src/routes/_portal/index.tsx` 为主机列表页（per D-05, D-06, D-07, D-12）：**

使用 `createFileRoute("/_portal/portal")` 保持现有路由路径。

组件 `PortalHostList`：
- 调用 `useMyHosts()` 获取数据
- loading 状态：显示 "加载中..." 或骨架屏
- 空状态：显示 "暂无主机" 提示文案
- 正常状态：使用 shadcn/ui `Card` 组件以网格布局展示主机卡片
  - 网格：`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
  - 每张卡片显示：
    - `hostname` 作为标题
    - 状态 Badge：running=绿色(`bg-green-100 text-green-700`)、stopped=灰色(`bg-gray-100 text-gray-700`)、rebuilding=黄色(`bg-yellow-100 text-yellow-700`)、pending=蓝色(`bg-blue-100 text-blue-700`)、其他=灰色
    - `egress_ip` 前面加 Globe 图标（lucide-react）
    - `created_at` 格式化为相对时间或 `YYYY-MM-DD`
  - 卡片可点击，使用 TanStack Router `Link` 导航到 `/portal/hosts/${host.id}`

**2) 创建 `web/admin/src/routes/_portal/hosts/$hostId.tsx`（per D-07, D-11, D-08, D-09, D-10）：**

先确保目录存在：`web/admin/src/routes/_portal/hosts/`

使用 `createFileRoute("/_portal/portal/hosts/$hostId")`。

组件 `PortalHostDetail`：
- 从路由参数获取 `hostId`：`const { hostId } = Route.useParams()`
- 调用 `useMyHostDetail(hostId)` 获取详情
- 调用 `useRebuildHost()` 获取 mutation

布局：
- 顶部返回链接 `<Link to="/portal">` + 面包屑
- 主机基本信息卡片：hostname、status（Badge）、timezone、created_at、updated_at
- 出口 IP 信息区域（per D-11 只读）：遍历 `egress_bindings`，显示 `ip_address` 和 `tunnel_type`（wireguard/proxy）
- 操作区域：重建按钮

重建交互（per D-08, D-09, D-10）：
- 使用 shadcn/ui `AlertDialog` 组件
- 标题："确认重建主机？"
- 描述："重建将重置容器环境，home 目录数据保留。重建过程中主机将暂时不可访问。"
- 取消按钮 + 确认按钮
- 确认按钮 `disabled={rebuildMutation.isPending}`
- 确认按钮文字：`isPending ? "重建中..." : "确认重建"`
- 成功后显示 toast（使用 sonner `toast.success("重建任务已提交")`）
- 失败显示 toast.error

状态轮询（per D-10）：
- 当主机状态为 `rebuilding` 时，`useMyHostDetail` 的 `refetchInterval` 设为 3000ms
- 状态变为 `running` 后停止轮询

**3) 更新 `web/admin/src/components/layout/topbar.tsx`（Pitfall 4 修复）：**

扩展 `pageTitles` 映射：
```typescript
const pageTitles: Record<string, string> = {
  "/": "仪表板",
  "/users": "用户管理",
  "/egress-ips": "出口 IP",
  "/hosts": "主机管理",
  "/tasks": "任务列表",
  "/portal": "我的面板",
};
```

对于 `/portal/hosts/xxx` 这类动态路由，在 title 查找逻辑中增加前缀匹配：
```typescript
const title = pageTitles[pathname]
  ?? (pathname.startsWith("/portal/hosts/") ? "主机详情" : "管理后台");
```

右侧角色标签改为动态显示：
```typescript
import { getRole } from "@/lib/auth";
// ...
const role = getRole();
const roleLabel = role === "admin" ? "管理员" : "用户";
// ...
<span className="text-sm text-muted-foreground">{roleLabel}</span>
```
  </action>
  <verify>
    <automated>cd /workspace/Desktop/cloud-cli-proxy/web/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/admin/src/routes/_portal/index.tsx 包含 `useMyHosts`（调用 hook 获取数据）
    - web/admin/src/routes/_portal/index.tsx 包含 `Card`（使用 shadcn/ui 卡片）
    - web/admin/src/routes/_portal/index.tsx 包含 `/portal/hosts/`（卡片链接到详情页）
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 文件存在
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 包含 `useMyHostDetail`
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 包含 `useRebuildHost`
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 包含 `AlertDialog`（确认对话框）
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 包含 `重建将重置容器环境`（D-08 警告文案）
    - web/admin/src/routes/_portal/hosts/$hostId.tsx 包含 `refetchInterval`（轮询机制）
    - web/admin/src/components/layout/topbar.tsx 包含 `/portal`（在 pageTitles 中）
    - web/admin/src/components/layout/topbar.tsx 包含 `getRole`（动态角色显示）
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>主机列表以卡片形式展示（含状态 Badge 和出口 IP），详情页展示完整信息和出口 IP/隧道类型，重建按钮有确认对话框和进度轮询，Topbar 正确显示 portal 标题和用户角色</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证用户自助面板完整功能</name>
  <files>web/admin/src/routes/_portal/index.tsx, web/admin/src/routes/_portal/hosts/$hostId.tsx</files>
  <action>
人工验证完整的用户自助面板功能。验证步骤：
1. 使用普通用户账号登录系统，确认跳转到 /portal
2. 验证主机列表卡片显示：主机名、状态 Badge、出口 IP、创建时间
3. 点击卡片进入详情页，验证出口 IP 和隧道类型展示
4. 点击重建按钮，验证确认对话框和进度状态
5. 验证 Topbar 标题和角色显示
6. 验证无敏感字段泄露（检查浏览器网络面板）
  </action>
  <verify>人工验证</verify>
  <done>用户确认所有面板功能正常</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` 无类型错误
2. 用户登录后 /portal 展示主机卡片列表
3. 主机详情页展示出口 IP 和隧道类型，无敏感字段
4. 重建确认对话框文案正确，防重复提交有效
5. Topbar 标题和角色标签正确
</verification>

<success_criteria>
- 用户可在 /portal 看到自己的主机列表（卡片形式，含状态和出口 IP）
- 用户可在详情页看到出口 IP 和隧道类型
- 用户可触发主机重建并看到状态变化
- 前端不暴露敏感字段
- Topbar 适配 portal 路由
</success_criteria>

<output>
After completion, create `.planning/phases/12-api/12-02-SUMMARY.md`
</output>
