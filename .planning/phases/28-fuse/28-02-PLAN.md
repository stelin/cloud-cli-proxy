---
phase: 28-fuse
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - deploy/scripts/host-preflight.sh
  - docs/zh/guide/deployment.md
  - docs/en/guide/deployment.md
autonomous: true
requirements:
  - SRV-04

must_haves:
  truths:
    - "host-preflight.sh 在 FUSE 内核模块未加载时报错退出"
    - "中文部署文档包含 FUSE 前置条件章节"
    - "英文部署文档包含 FUSE prerequisites 章节"
  artifacts:
    - path: "deploy/scripts/host-preflight.sh"
      provides: "FUSE 内核模块前置检查"
      contains: "fuse"
    - path: "docs/zh/guide/deployment.md"
      provides: "FUSE 部署前置条件说明"
      contains: "FUSE"
    - path: "docs/en/guide/deployment.md"
      provides: "FUSE prerequisites section"
      contains: "FUSE"
  key_links:
    - from: "deploy/scripts/host-preflight.sh"
      to: "Linux 内核"
      via: "modprobe fuse + /dev/fuse 检测"
      pattern: "modprobe fuse"
    - from: "docs/zh/guide/deployment.md"
      to: "deploy/scripts/host-preflight.sh"
      via: "文档引用前置检查脚本"
      pattern: "host-preflight"
---

<objective>
在宿主机前置检查脚本中添加 FUSE 内核模块检测，并在中英文部署文档中补充 FUSE 相关前置条件和已知限制说明。

Purpose: 确保运维人员在部署前可以通过 host-preflight.sh 自动发现 FUSE 模块缺失问题，部署文档中清晰说明 FUSE/AppArmor 的前置要求和不同 Ubuntu 版本的差异。
Output: 增强后的 host-preflight.sh + 更新后的 zh/en 部署文档
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-fuse/28-CONTEXT.md
@.planning/phases/28-fuse/28-RESEARCH.md

<interfaces>
From deploy/scripts/host-preflight.sh — 现有结构（lines 1-40）:

```bash
#!/usr/bin/env bash
set -euo pipefail

require_cmd() {
  if ! command -v "$1" >/dev/null 2>&1; then
    echo "missing required command: $1" >&2
    exit 1
  fi
}

require_cmd docker
require_cmd ip
require_cmd systemctl

if ! command -v nft >/dev/null 2>&1 && ! command -v iptables >/dev/null 2>&1; then
  echo "missing required firewall tool: nft or iptables" >&2
  exit 1
fi

# Phase 2: WireGuard kernel module must be loadable
if ! modprobe -n wireguard 2>/dev/null; then
  if ip link add wg-test type wireguard 2>/dev/null; then
    ip link del wg-test 2>/dev/null || true
  else
    echo "missing wireguard kernel module" >&2
    exit 1
  fi
fi

# Phase 2: nsenter required for container namespace verification
require_cmd nsenter

# Phase 2: nft required for container firewall rules
require_cmd nft

# Phase 2: curl required for egress IP verification
require_cmd curl

mkdir -p /var/lib/cloud-cli-proxy
mkdir -p /run/cloud-cli-proxy
```

From docs/zh/guide/deployment.md — 依赖检查表（lines 14-35）:

```markdown
### 1.1 依赖检查

运行内置的依赖检查脚本：

```bash
sudo bash deploy/scripts/host-preflight.sh
```

该脚本会检查以下依赖是否就绪：

| 依赖 | 最低版本 | 用途 |
|------|----------|------|
| Docker Engine | 28.x+ | 容器运行时 |
| WireGuard | 内核模块 | 全隧道出网 |
...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: host-preflight.sh 添加 FUSE 内核模块检测</name>
  <files>deploy/scripts/host-preflight.sh</files>
  <read_first>
    - deploy/scripts/host-preflight.sh
    - .planning/phases/28-fuse/28-RESEARCH.md (Pitfall 3: FUSE 内核模块未加载)
  </read_first>
  <action>
在 `host-preflight.sh` 的 WireGuard 内核模块检测之后（即 `require_cmd nsenter` 之前），添加 FUSE 内核模块检测段：

```bash
# Phase 28: FUSE kernel module must be loadable (required for sshfs directory mapping)
if ! modprobe fuse 2>/dev/null; then
  if [ ! -c /dev/fuse ]; then
    echo "missing fuse kernel module (required for sshfs directory mapping)" >&2
    echo "try: modprobe fuse" >&2
    exit 1
  fi
fi

if [ ! -c /dev/fuse ]; then
  echo "missing /dev/fuse device (required for container FUSE mount)" >&2
  exit 1
fi
```

逻辑说明：
1. 先尝试 `modprobe fuse` 加载模块（某些系统需要显式加载）
2. 如果 modprobe 失败（可能没有 root 权限或模块不存在），检查 `/dev/fuse` 设备是否已存在（模块可能是内置而非可加载模块）
3. 最后确认 `/dev/fuse` 设备存在
  </action>
  <verify>
    <automated>grep -n 'fuse' deploy/scripts/host-preflight.sh && bash -n deploy/scripts/host-preflight.sh && echo "SYNTAX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - host-preflight.sh 包含 `modprobe fuse` 字符串
    - host-preflight.sh 包含 `/dev/fuse` 检测
    - host-preflight.sh 包含 "sshfs directory mapping" 或 "FUSE" 的错误提示
    - bash -n 语法检查通过
    - FUSE 检测段位于 WireGuard 检测之后、nsenter 检测之前
  </acceptance_criteria>
  <done>host-preflight.sh 在 FUSE 内核模块未加载时输出明确错误信息并退出</done>
</task>

<task type="auto">
  <name>Task 2: 中英文部署文档添加 FUSE 前置条件章节</name>
  <files>docs/zh/guide/deployment.md, docs/en/guide/deployment.md</files>
  <read_first>
    - docs/zh/guide/deployment.md
    - docs/en/guide/deployment.md
    - .planning/phases/28-fuse/28-RESEARCH.md (AppArmor deny mount 分析, Ubuntu 25.04+ fusermount3 profile)
    - .planning/phases/28-fuse/28-CONTEXT.md (D-08 产出物规格)
  </read_first>
  <action>
**中文文档 `docs/zh/guide/deployment.md`：**

1. 在「1.1 依赖检查」的依赖表格中追加一行：

```
| FUSE | 内核模块 | 容器内 sshfs 目录映射 |
```

2. 在「1.2 安装缺失依赖」章节的 nftables 部分之后，添加 FUSE 子章节：

```markdown
**FUSE 内核模块：**

```bash
modprobe fuse
echo fuse >> /etc/modules-load.d/fuse.conf
```

> 验证：`ls -la /dev/fuse` 应输出 `crw-rw-rw-` 权限的字符设备。

### 1.3 FUSE 与 AppArmor 兼容性

容器内的 sshfs 目录映射依赖 FUSE 文件系统。Docker 的默认 AppArmor profile（`docker-default`）包含 `deny mount` 规则，会阻断容器内的 FUSE 挂载操作。系统已在容器创建时自动添加 `--security-opt apparmor=unconfined` 解除此限制。

**已知限制：**

| 宿主机 OS | 影响 | 处理方式 |
|-----------|------|----------|
| Ubuntu 24.04 LTS | 默认 AppArmor 阻断 FUSE mount | 系统自动处理（apparmor=unconfined） |
| Ubuntu 25.04+ | 额外的 fusermount3 AppArmor profile 可能阻断 | 如遇问题，执行 `aa-disable /usr/bin/fusermount3` |
| Debian 12+ | 默认无 AppArmor | 通常无需额外配置 |
| 无 AppArmor 的发行版 | 无影响 | 无需额外配置 |

**验证 FUSE 兼容性：**

```bash
sudo bash scripts/verify-fuse-compat.sh
```

该脚本会自动检测宿主机安全模块状态、测试容器内 FUSE 挂载、验证与网络策略的共存性。
```

**英文文档 `docs/en/guide/deployment.md`：**

1. 在依赖表格中追加一行：

```
| FUSE | kernel module | Container sshfs directory mapping |
```

2. 在安装缺失依赖章节中添加 FUSE 子章节：

```markdown
**FUSE kernel module:**

```bash
modprobe fuse
echo fuse >> /etc/modules-load.d/fuse.conf
```

> Verify: `ls -la /dev/fuse` should show a character device with `crw-rw-rw-` permissions.

### 1.3 FUSE and AppArmor Compatibility

Container-based sshfs directory mapping requires FUSE. Docker's default AppArmor profile (`docker-default`) includes a `deny mount` rule that blocks FUSE mount operations inside containers. The system automatically adds `--security-opt apparmor=unconfined` when creating containers.

**Known limitations:**

| Host OS | Impact | Handling |
|---------|--------|----------|
| Ubuntu 24.04 LTS | Default AppArmor blocks FUSE mount | Handled automatically (apparmor=unconfined) |
| Ubuntu 25.04+ | Additional fusermount3 AppArmor profile may block | Run `aa-disable /usr/bin/fusermount3` if needed |
| Debian 12+ | No AppArmor by default | No extra configuration needed |
| Distros without AppArmor | No impact | No extra configuration needed |

**Verify FUSE compatibility:**

```bash
sudo bash scripts/verify-fuse-compat.sh
```
```
  </action>
  <verify>
    <automated>grep -c 'FUSE' docs/zh/guide/deployment.md && grep -c 'FUSE' docs/en/guide/deployment.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/zh/guide/deployment.md 包含 "FUSE" 字样（至少 3 处）
    - docs/zh/guide/deployment.md 包含 "modprobe fuse" 安装指令
    - docs/zh/guide/deployment.md 包含 "apparmor=unconfined" 说明
    - docs/zh/guide/deployment.md 包含 "fusermount3" Ubuntu 25.04+ 已知限制
    - docs/zh/guide/deployment.md 包含 "verify-fuse-compat.sh" 验证脚本引用
    - docs/en/guide/deployment.md 包含 "FUSE" 字样（至少 3 处）
    - docs/en/guide/deployment.md 包含 "modprobe fuse" 安装指令
    - docs/en/guide/deployment.md 包含 "AppArmor" 兼容性说明
    - 两份文档的 FUSE 章节结构对称（依赖表行 + 安装指令 + 兼容性章节 + 已知限制表 + 验证脚本引用）
  </acceptance_criteria>
  <done>中英文部署文档均包含 FUSE 前置条件、AppArmor 兼容性说明和已知限制表</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维人员 → 宿主机 | host-preflight.sh 以 root 权限执行内核模块加载 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Elevation of Privilege | host-preflight.sh — modprobe fuse | accept | modprobe 是标准的内核模块管理操作，脚本已要求 root/sudo 权限运行；FUSE 是内核内置模块，非第三方代码 |
| T-28-05 | Information Disclosure | 部署文档公开 AppArmor 配置细节 | accept | 文档描述的是公开已知的 Docker 默认行为，非敏感信息；文档面向运维人员 |
</threat_model>

<verification>
- `grep 'fuse' deploy/scripts/host-preflight.sh` 包含 FUSE 内核模块检测逻辑
- `bash -n deploy/scripts/host-preflight.sh` 语法检查通过
- `grep -c 'FUSE' docs/zh/guide/deployment.md` 返回 >= 3
- `grep -c 'FUSE' docs/en/guide/deployment.md` 返回 >= 3
</verification>

<success_criteria>
1. host-preflight.sh 在 FUSE 内核模块未加载且 /dev/fuse 不存在时报错退出
2. 中文部署文档包含 FUSE 前置条件、AppArmor 兼容性和已知限制说明
3. 英文部署文档包含对应的 FUSE prerequisites 章节
</success_criteria>

<output>
After completion, create `.planning/phases/28-fuse/28-02-SUMMARY.md`
</output>
