---
phase: "29.1"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/runtime_service.go
  - internal/runtime/tasks/worker.go
  - internal/runtime/tasks/worker_password_test.go
autonomous: true
requirements_addressed:
  - P0-HOTFIX-29.1

must_haves:
  truths:
    - "runtime_service.QueueHostAction 在 host.EntryPassword == \"\" 时直接返回 error，不再入队 + 不再落入 slog.Warn 静默继续分支"
    - "worker.buildCreateArgs 在 request.EntryPassword == \"\" 时返回 error，createHost 走既有 failed 路径，绝不再把 CONTAINER_SSH_PASSWORD 拼成 \"workspace\""
    - "worker.syncContainerCredentials 在 request.EntryPassword == \"\" 时记 runtime.entry_password_missing 事件后立即 return，绝不执行 docker exec chpasswd"
    - "worker 单测覆盖 buildCreateArgs 空密码 → error 分支与 syncContainerCredentials 空密码 → 事件无 chpasswd 分支"
  artifacts:
    - path: "internal/runtime/runtime_service.go"
      provides: "QueueHostAction 空密码 fail-fast + RecordEvent"
      contains: "runtime.entry_password_missing"
    - path: "internal/runtime/tasks/worker.go"
      provides: "buildCreateArgs / syncContainerCredentials 的 fail-fast 分支，无 \"workspace\" 字面量 fallback"
      contains: "entry_password is empty"
    - path: "internal/runtime/tasks/worker_password_test.go"
      provides: "TestBuildCreateArgs_EmptyEntryPassword_ReturnsError 与 TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd"
      contains: "runtime.entry_password_missing"
  key_links:
    - from: "internal/runtime/runtime_service.go::QueueHostAction"
      to: "repository.RecordEventParams{Type: \"runtime.entry_password_missing\"}"
      via: "Service 已持有的 repo/events 字段"
      pattern: "runtime\\.entry_password_missing"
    - from: "internal/runtime/tasks/worker.go::buildCreateArgs"
      to: "createHost err-path (taskStateFailed)"
      via: "return nil, fmt.Errorf(\"... entry_password is empty ...\")"
      pattern: "entry_password is empty"
    - from: "internal/runtime/tasks/worker.go::syncContainerCredentials"
      to: "w.repo.RecordEvent"
      via: "level=error, type=runtime.entry_password_missing, 然后 return"
      pattern: "runtime\\.entry_password_missing"

threat_model:
  boundaries:
    - "control-plane service ↔ task worker：空密码请求必须在进入队列前被拒"
    - "worker ↔ Docker daemon：绝不向 docker exec chpasswd 或 docker create 环境变量传入字符串 \"workspace\" 作为密码 fallback"
  threats:
    - id: T-29.1-01
      category: Tampering
      component: "worker.buildCreateArgs / worker.syncContainerCredentials"
      disposition: mitigate
      mitigation: "移除 firstNonEmpty(..., \"workspace\") 对密码字段的使用；改为显式空值检查 → error / RecordEvent；createHost 走既有 failed err-path"
    - id: T-29.1-02
      category: "Denial of Service / Tampering"
      component: "runtime_service.QueueHostAction"
      disposition: mitigate
      mitigation: "空 EntryPassword 直接 return error，避免给 worker 派发注定产出\"登不进去\"容器的任务"
    - id: T-29.1-05-log
      category: "Information disclosure"
      component: "RecordEvent metadata / slog output"
      disposition: mitigate
      mitigation: "事件与日志只记 host_id / container_name / source，**绝不**写入 request.EntryPassword 明文；Message 使用固定英文短句，不插入密码值"
---

<objective>
把 runtime 与 worker 链路上三处会把空密码 fallback 成 `"workspace"` 的静默分支改造成 fail-fast：
1. `runtime_service.QueueHostAction` 拒绝入队；
2. `worker.buildCreateArgs` 拒绝拼 docker create 参数；
3. `worker.syncContainerCredentials` 拒绝执行 chpasswd。

每一处都记录 `runtime.entry_password_missing` 审计事件以便运维追溯。

Purpose: 消灭 `CONTAINER_SSH_PASSWORD=workspace` 的运行时出现点，让"密码丢了"从"用户登不进容器"升级为"任务明确失败 + 可查事件"。
Output: runtime_service + worker 两处源文件 + 新增 worker_password_test.go；`go test ./internal/runtime/...` 全 PASS。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-CONTEXT.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md
@.planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md

<interfaces>
<!-- 来自 internal/store/repository/models.go —— RecordEventParams 结构 -->

```go
// internal/store/repository/models.go (既有接口签名，本 plan 不改)
type RecordEventParams struct {
    TaskID   *string
    HostID   *string
    Level    string            // "info" | "warn" | "error"
    Type     string            // 本 plan 新增 "runtime.entry_password_missing"
    Message  string
    Metadata map[string]any
}
```

<!-- 来自 internal/runtime/tasks/worker.go:673 —— execInContainer 已是 package-level var -->

```go
// internal/runtime/tasks/worker.go (已有)
var execInContainer = func(ctx context.Context, container, script, stdin string) ([]byte, error) { ... }
```

<!-- 来自 internal/runtime/tasks/ssh_inject_test.go:67-109 —— fakeWorkerRepo + setupInjectTest 模板 -->

```go
// internal/runtime/tasks/ssh_inject_test.go (既有测试脚手架，本 plan 复用)
type fakeWorkerRepo struct {
    events []repository.RecordEventParams
}
func (r *fakeWorkerRepo) RecordEvent(_ context.Context, params repository.RecordEventParams) (repository.Event, error) {
    r.events = append(r.events, params)
    return repository.Event{}, nil
}
func setupInjectTest(t *testing.T, proxyPub string) (*Worker, *fakeContainer, *fakeWorkerRepo) { ... }
func hasEventType(events []repository.RecordEventParams, eventType string) bool { ... }
```

<!-- 来自 internal/runtime/tasks/worker_volume_test.go:11-22 —— minimalCreateHostRequest 工厂 -->

```go
// internal/runtime/tasks/worker_volume_test.go (既有)
func minimalCreateHostRequest(hostID string) agentapi.HostActionRequest { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: runtime_service.QueueHostAction 空密码 fail-fast + RecordEvent</name>
  <files>internal/runtime/runtime_service.go</files>
  <read_first>
    - internal/runtime/runtime_service.go （必读全文：确认 Service 构造体当前是否已持有可写事件的 repo / events 字段、QueueHostAction 完整签名、错误返回类型）
    - internal/runtime/tasks/worker.go 的 waitForSSH RecordEvent 片段（行 ~392-399，作为 RecordEvent 调用模板）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md §2.1 / §4.2
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §3 / Shared Patterns A
  </read_first>
  <behavior>
    - 调用 `QueueHostAction(ctx, hostID, action)` 且该 host 的 `host.EntryPassword == ""` 时：
      1. 返回 non-nil error，error.Error() 包含 "entry_password is empty"；
      2. 记录一条 RecordEvent：Level="error"，Type="runtime.entry_password_missing"，HostID=&hostID，Metadata 至少包含 {"host_id": hostID, "action": action, "source": "queue"}；
      3. 不再调用任何 enqueue / taskSubmit 相关下游函数（即任务不入队）。
    - 当 `host.EntryPassword != ""` 时，保持原有流程完全不变（回归安全）。
  </behavior>
  <action>
    1. 阅读 `runtime_service.go` 当前的 `Service` 结构体，确认已有 `repo` 字段（或等价的事件记录能力）。如果尚未具备，**以最小侵入**方式追加一个能调用 `RecordEvent` 的字段（优先复用已持有的 repo/store；禁止引入新的事件接口类型）。

    2. 定位现有第 ~149-152 行 `slog.Warn("host entry_password is empty, container will use default password", ...)` 代码段（若行号漂移，以函数名为准）。

    3. 用以下逻辑**替换**该 warn 段落（保留 `slog.Info("queuing host action", ...)` 信息日志，注意它在 warn 之后）：

       ```go
       if request.EntryPassword == "" {
           hid := hostID
           if s.repo != nil {
               _, _ = s.repo.RecordEvent(ctx, repository.RecordEventParams{
                   HostID:  &hid,
                   Level:   "error",
                   Type:    "runtime.entry_password_missing",
                   Message: "host entry_password is empty; refusing to queue action",
                   Metadata: map[string]any{
                       "host_id": hostID,
                       "action":  string(action),
                       "source":  "queue",
                   },
               })
           }
           slog.Error("refusing to queue host action: entry_password empty",
               "host_id", hostID, "action", action)
           return repository.Task{}, fmt.Errorf("host %s entry_password is empty; refusing to queue %s", hostID, action)
       }
       ```

       注意：
       - 返回类型以当前函数签名为准（若返回的是 `Task` 零值 + error，对齐；如返回 `(string, error)` 则对应返回 ""，error）。
       - `string(action)` 视 action 类型；若已是字符串则直接用。
       - 只写 `host_id` 不写密码明文；Message 是固定短句不插值 EntryPassword。

    4. 严禁在本任务中：
       - 扩大 Service 构造签名到调用方无法感知的破坏性改动（若必须加字段，在内部 New 时填入已有 repo）；
       - 修改无关分支；
       - 引入新的 import 除 `fmt` / `repository` / `slog`（这些如果已 import 则复用）。
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'runtime\.entry_password_missing' internal/runtime/runtime_service.go 必须命中 ≥ 1 行
    - rg -n 'host %s entry_password is empty; refusing to queue' internal/runtime/runtime_service.go 必须命中 1 行
    - rg -n 'slog\.Warn.*host entry_password is empty, container will use default password' internal/runtime/runtime_service.go 必须命中 0 行（旧 warn 已被替换）
    - grep -F '"workspace"' internal/runtime/runtime_service.go 必须不出现（runtime_service 不应有此字面量，本 plan 也绝不引入）
    - go build ./... exit 0
    - go vet ./internal/runtime/... exit 0
    - 事件 Metadata 不包含 key 名为 "entry_password" / "password" / "pass" 的字段（grep -E '"(entry_password|password|pass)"' internal/runtime/runtime_service.go 输出不得新增此类行）
  </acceptance_criteria>
  <done>
    QueueHostAction 对空密码 host 返回 error + 写事件；非空密码主机流程零变化；包级构建与 vet 通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: worker.go buildCreateArgs / syncContainerCredentials 去除 "workspace" 密码 fallback</name>
  <files>internal/runtime/tasks/worker.go</files>
  <read_first>
    - internal/runtime/tasks/worker.go （必读全文至少 buildCreateArgs / syncContainerCredentials / waitForSSH / createHost 四处，确认当前 firstNonEmpty 调用位点与 err-path）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-RESEARCH.md §2.2 / §4
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §4 / §5 / Shared Patterns A
  </read_first>
  <behavior>
    - `buildCreateArgs(req, ...)` 中：
      - 若 `request.EntryPassword == ""` → 立即 `return nil, fmt.Errorf("host %s entry_password is empty; refusing to build create args", request.HostID)`；
      - 否则 `-e CONTAINER_SSH_PASSWORD=`+`request.EntryPassword` 直接拼接（不再走 `firstNonEmpty(..., "workspace")`）。
      - 保留 `linuxUser := firstNonEmpty(request.DefaultUser, "workspace")` 这一行 Linux 用户名 fallback 不动。
    - `syncContainerCredentials(ctx, request, containerName)` 中：
      - 若 `request.EntryPassword == ""` → 记一条 RecordEvent（Level=error, Type=runtime.entry_password_missing, HostID=&request.HostID, Metadata={"host_id": request.HostID, "container": containerName, "source": "sync"}），然后 `return`，**不执行** chpasswd；
      - 否则 `pass := request.EntryPassword`（直接取真值，不再走 firstNonEmpty）；`user := firstNonEmpty(request.DefaultUser, "workspace")` 保留不动。
  </behavior>
  <action>
    在 `internal/runtime/tasks/worker.go` 内做两处精确改动：

    **A) buildCreateArgs**

    在当前第 ~160 行 `linuxUser := firstNonEmpty(request.DefaultUser, "workspace")` 之**前**插入空密码守卫：

    ```go
    if request.EntryPassword == "" {
        return nil, fmt.Errorf("host %s entry_password is empty; refusing to build create args", request.HostID)
    }
    ```

    然后把现有第 ~167 行：

    ```go
    "-e", "CONTAINER_SSH_PASSWORD="+firstNonEmpty(request.EntryPassword, "workspace"),
    ```

    改为：

    ```go
    "-e", "CONTAINER_SSH_PASSWORD=" + request.EntryPassword,
    ```

    **B) syncContainerCredentials**

    把现有第 ~431-432 行：

    ```go
    user := firstNonEmpty(request.DefaultUser, "workspace")
    pass := firstNonEmpty(request.EntryPassword, "workspace")
    ```

    改为：

    ```go
    user := firstNonEmpty(request.DefaultUser, "workspace")
    if request.EntryPassword == "" {
        w.repo.RecordEvent(ctx, repository.RecordEventParams{
            HostID:  &request.HostID,
            Level:   "error",
            Type:    "runtime.entry_password_missing",
            Message: "host entry_password is empty; refusing to sync container credentials",
            Metadata: map[string]any{
                "host_id":   request.HostID,
                "container": containerName,
                "source":    "sync",
            },
        })
        return
    }
    pass := request.EntryPassword
    ```

    严禁：
    - 把 `user` 的 `firstNonEmpty(..., "workspace")` 改掉（Linux 用户名，不是密码，不受本 bug 影响）；
    - 修改 `buildCreateArgs` 内无关的 volume/mount/env 参数；
    - 修改 `chpasswd` 命令本身；
    - 修改 `execInContainer` 或其他 var；
    - 在 RecordEvent 的 Message/Metadata 里写入 `request.EntryPassword` 明文。
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/runtime/tasks/...</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'firstNonEmpty\(request\.EntryPassword' internal/runtime/tasks/worker.go 必须命中 0 行
    - rg -n '"CONTAINER_SSH_PASSWORD=" \+ request\.EntryPassword' internal/runtime/tasks/worker.go 必须命中 1 行
    - rg -n 'host %s entry_password is empty; refusing to build create args' internal/runtime/tasks/worker.go 必须命中 1 行
    - rg -n 'runtime\.entry_password_missing' internal/runtime/tasks/worker.go 必须命中 1 行（仅 syncContainerCredentials 里的那处）
    - rg -n 'firstNonEmpty\(request\.DefaultUser, "workspace"\)' internal/runtime/tasks/worker.go 必须仍然命中 ≥ 2 行（buildCreateArgs 与 syncContainerCredentials 各 1，Linux 用户名 fallback 保留）
    - go build ./... exit 0
    - go vet ./internal/runtime/tasks/... exit 0
    - rg -n '"password|"pass"' internal/runtime/tasks/worker.go 输出未包含将 request.EntryPassword 当 metadata value 的新代码行
  </acceptance_criteria>
  <done>
    worker 内两处密码 fallback 消除；编译通过；Linux 用户名 fallback 仍保留；RecordEvent 只记 host_id / container / source。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: 新增 worker_password_test.go 覆盖两条 fail-fast 分支</name>
  <files>internal/runtime/tasks/worker_password_test.go</files>
  <read_first>
    - internal/runtime/tasks/worker.go （Task 2.2 修改后的版本）
    - internal/runtime/tasks/ssh_inject_test.go （复用 fakeWorkerRepo / setupInjectTest / hasEventType / fakeContainer.log）
    - internal/runtime/tasks/worker_volume_test.go （minimalCreateHostRequest 工厂；TestBuildCreateArgs_InvalidVolumeMount 模板）
    - .planning/phases/29.1-gethost-entry-password-workspace/29.1-PATTERNS.md §6
  </read_first>
  <behavior>
    - `TestBuildCreateArgs_EmptyEntryPassword_ReturnsError`：构造 req 使 EntryPassword 为空，调用 `w.buildCreateArgs`，断言 err != nil 且 err.Error() 包含 "entry_password is empty"；返回的 args slice 为 nil 或零长。
    - `TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd`：用 setupInjectTest 装配 fake 容器与 repo；构造 req 使 EntryPassword 为空；调用 `w.syncContainerCredentials(ctx, req, "cx")`；断言：
      (a) `hasEventType(repo.events, "runtime.entry_password_missing")` == true；
      (b) fakeContainer.log 不包含字符串 "chpasswd"（即未执行密码同步）。
  </behavior>
  <action>
    新建文件 `internal/runtime/tasks/worker_password_test.go`，package 声明 `package tasks`。基础骨架：

    ```go
    package tasks

    import (
        "context"
        "strings"
        "testing"

        "github.com/zanel1u/cloud-cli-proxy/internal/agentapi"
    )

    // TestBuildCreateArgs_EmptyEntryPassword_ReturnsError 锁定 Phase 29.1：
    // worker 不得再对空 entry_password 进行 "workspace" fallback。
    func TestBuildCreateArgs_EmptyEntryPassword_ReturnsError(t *testing.T) {
        w := &Worker{}
        req := minimalCreateHostRequest("h-empty-pw")
        req.EntryPassword = ""
        args, err := w.buildCreateArgs(req, "c1", "c1")
        if err == nil {
            t.Fatal("expected error for empty EntryPassword, got nil")
        }
        if !strings.Contains(err.Error(), "entry_password is empty") {
            t.Errorf("error message should mention entry_password is empty; got: %v", err)
        }
        if len(args) != 0 {
            t.Errorf("expected empty args slice on error, got len=%d", len(args))
        }
    }

    // TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd：
    // 空 entry_password 必须只记事件、不执行 docker exec chpasswd。
    func TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd(t *testing.T) {
        w, fc, repo := setupInjectTest(t, "")
        req := minimalCreateHostRequest("h-sync-empty")
        req.EntryPassword = ""

        w.syncContainerCredentials(context.Background(), req, "cx")

        if !hasEventType(repo.events, "runtime.entry_password_missing") {
            t.Error("expected runtime.entry_password_missing event when EntryPassword is empty")
        }
        if strings.Contains(strings.Join(fc.log, "\n"), "chpasswd") {
            t.Error("syncContainerCredentials must not invoke chpasswd when EntryPassword is empty")
        }
        _ = agentapi.HostActionRequest{}
    }
    ```

    **注意事项：**
    1. 若 `syncContainerCredentials` 当前使用的是 `exec.CommandContext("docker", "exec", ...)`（非 `execInContainer` 包级 var），fakeContainer.log 捕获不到。此时 Task 2.3 **必须**先在 `worker.go` 内把 `syncContainerCredentials` 改为使用 `execInContainer` 包级 var 构造命令（与 §6 指出的既有 execInContainer 模板一致），作为"为可测性做的最小重构"；改完后 setupInjectTest 就能捕获调用流。
       - 若走这条路，只调整 chpasswd 这一行的执行方式，不改其他逻辑；
       - 若 Task 2.2 已实际改用 `execInContainer`（实施者判断最低成本路径），则此处复用即可。
    2. 若 minimalCreateHostRequest 工厂默认不设 EntryPassword，上述"req.EntryPassword = \"\""是冗余但无害；若工厂被他测依赖改为了非空默认，保持 override 语义即可。
    3. 绝不在断言中写真实密码样本；仅断言事件类型与 log 是否含 "chpasswd" 字符串。
  </action>
  <verify>
    <automated>go test ./internal/runtime/tasks/... -run 'TestBuildCreateArgs_EmptyEntryPassword_ReturnsError|TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 文件 internal/runtime/tasks/worker_password_test.go 存在
    - 文件 package 声明为 `package tasks`
    - grep -c 'TestBuildCreateArgs_EmptyEntryPassword_ReturnsError' internal/runtime/tasks/worker_password_test.go == 1
    - grep -c 'TestSyncContainerCredentials_EmptyEntryPassword_RecordsEventNoChpasswd' internal/runtime/tasks/worker_password_test.go == 1
    - go test ./internal/runtime/tasks/... -count=1 exit 0（整包回归未破坏）
    - 运行 `go test ./internal/runtime/tasks/... -run TestBuildCreateArgs_EmptyEntryPassword -v` 输出 PASS
    - 运行 `go test ./internal/runtime/tasks/... -run TestSyncContainerCredentials_EmptyEntryPassword -v` 输出 PASS
    - 测试文件中未出现任何形如 `EntryPassword = "[a-zA-Z0-9]{6,}"` 的真实密码字符串（仅空串或占位符）
  </acceptance_criteria>
  <done>
    两条 fail-fast 单测 PASS；整包回归 PASS；若为可测性改了 syncContainerCredentials 的执行方式，改动仅限 chpasswd 这一行。
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 成功
- `go vet ./internal/runtime/...` 成功
- `go test ./internal/runtime/... -count=1` 全部 PASS
- `rg -n '"workspace"' internal/runtime/tasks/worker.go` —— 只能看到 `firstNonEmpty(request.DefaultUser, "workspace")` 的 Linux 用户名 fallback（2 处），不得再出现密码 fallback
- 源码中不存在将 `request.EntryPassword` 写入任何 `Message` / `Metadata` 字段值或日志字段 value 的代码
</verification>

<success_criteria>
- runtime_service.QueueHostAction 拒绝空密码入队并记事件
- worker.buildCreateArgs 拒绝拼空密码参数并返回 error
- worker.syncContainerCredentials 拒绝对空密码执行 chpasswd 且写事件
- 新增两条 worker 单测稳定 PASS
- 联合 Plan 01 后，新建 host 的 CONTAINER_SSH_PASSWORD 必与 DB hosts.entry_password 一致；若未来某条路径回归漏列，worker 立即 fail 并抛事件而非静默注入 "workspace"
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-gethost-entry-password-workspace/29.1-02-SUMMARY.md` 汇报：
- runtime_service.go 实际改动行号与 Service 构造是否需要补字段
- worker.go 两处改动的最终形态、是否顺带做了 syncContainerCredentials 改用 execInContainer 的可测性重构
- 两条新测试的运行结果
- `rg '"workspace"' internal/runtime/tasks/worker.go` 的最终结果（确认仅剩 Linux 用户名 fallback）
</output>
