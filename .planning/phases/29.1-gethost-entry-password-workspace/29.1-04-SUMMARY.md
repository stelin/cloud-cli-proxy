---
phase: 29.1-gethost-entry-password-workspace
plan: 4
subsystem: api
tags: [admin, hosts, password-resync, docker-exec, chpasswd, hotfix]

requires:
  - phase: 29.1-01
    provides: "repository.ListRunningHosts 返回 []Host 已含真实 EntryPassword 字段（queries.go 6 条 Host SELECT 全部对齐 entry_password 列）"
  - phase: 29.1-02
    provides: "worker 链路 firstNonEmpty 已 fail-fast；runtime.entry_password_missing 事件类型在 worker 链路已落地"
  - phase: 29.1-03
    provides: "managed-user entrypoint chpasswd 后追加 passwd -S 自检 + exit 1，确保容器侧密码生效"

provides:
  - "POST /v1/admin/hosts/resync-passwords 批量同步端点（adminGuard 包裹）"
  - "AdminHostStore 接口扩 ListRunningHosts(ctx) ([]repository.Host, error)"
  - "syncContainerPassword 提升为 package-level var，便于单测注入"
  - "stubEventRecorder 扩 events []RecordEventParams + hasType(string) bool helper（包内复用）"
  - "3 条 TestResyncPasswords_* handler 单测（无 docker 依赖，全部内存 fake）"

affects:
  - "未来 admin 工具：可仿照 ResyncPasswords 模式做其他批量运维端点（VNC 重启、镜像升级等）"
  - "Phase 29.x 之后的 admin_hosts 测试：新增 stubEventRecorder.hasType helper 已可用"

tech-stack:
  added: []
  patterns:
    - "包级函数 → var 化以注入 fake：syncContainerPassword 模式（与 Plan 02 execInContainer var 化一致）"
    - "批量端点错误隔离：单 host 失败仅记 warn 事件 + 计入 results，整体 HTTP 200"
    - "事件去明文：HTTP 响应与 RecordEvent.Metadata 均不包含密码字段，仅 host_id / container / source / error"

key-files:
  created: []
  modified:
    - "internal/controlplane/http/admin_hosts.go - AdminHostStore 接口扩 ListRunningHosts；syncContainerPassword var 化；新增 ResyncPasswords handler（约 96 行）"
    - "internal/controlplane/http/router.go - 追加 1 行 POST /v1/admin/hosts/resync-passwords 路由注册"
    - "internal/controlplane/http/admin_hosts_test.go - stubHostStore 扩 runningHosts/runningErr 字段 + ListRunningHosts 方法；新增 newTestAdminHostsHandler helper + 3 条 TestResyncPasswords_* 单测"
    - "internal/controlplane/http/admin_users_test.go - stubEventRecorder 扩 events slice + hasType(string) bool helper（保留既有 called bool，不破坏现存测试）"

key-decisions:
  - "把 Task 4.1 与 4.3 拆为 3 个独立 commit（而非 Plan 建议的 2 个），原因：4.3 测试体引用 h.ResyncPasswords()，必须在 4.2 落地后才能编译，否则无法 atomically commit"
  - "stubEventRecorder 扩展而非新建：既有 called bool 字段从未被任何测试断言使用，安全追加 events slice + hasType 不破坏向后兼容"
  - "回归验证范围裁剪：用 -run 'TestResyncPasswords_' + -list '.*' 替代全包 go test，因为既有 TestAdminHostsHandler 走真实 docker ps（getDockerStatuses）会在无 daemon 环境 hang，是预存问题（commit 928ab51 已记录），不在本 Plan 范围内修复"

patterns-established:
  - "var-化 + t.Cleanup 注入：通过 orig := syncContainerPassword / t.Cleanup(func() { syncContainerPassword = orig }) 在不引入 mock 框架前提下做 docker exec 注入"
  - "测试占位符规范：测试密码统一使用 PLACEHOLDER-XX 风格的可识别占位符常量，避免被误认为真实凭据"
  - "批量端点响应 schema：{total, succeeded, failed, skipped_empty_password, results[{host_id, status, error?}]}，单 host 错误装入 results 不引发 5xx"

requirements-completed: [P0-HOTFIX-29.1]

duration: ~25min
started: 2026-04-20T18:30:00Z
completed: 2026-04-20T18:55:48Z
---

# Phase 29.1 Plan 04: 管理员批量密码同步端点 Summary

**新增 `POST /v1/admin/hosts/resync-passwords` 批量端点，按 DB `hosts.entry_password` 对所有 running 容器 docker exec chpasswd，单 host 错误隔离、零密码明文外泄；3 条 handler 单测全 PASS。**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-20T18:30:00Z
- **Completed:** 2026-04-20T18:55:48Z
- **Tasks completed (自动 + 待人工 UAT):** 3 个 auto task 完成 + Task 4.4 pending
- **Files modified:** 4

## Accomplishments

- `AdminHostStore` 接口扩 `ListRunningHosts`，与 Plan 01 的 repository 实现对齐；`syncContainerPassword` 提升为 var，可被单测注入
- `ResyncPasswords` handler 完成：4 类事件（missing / resynced / resync_failed / triggered）、单 host 错误隔离、`h.events == nil` 安全降级
- `POST /v1/admin/hosts/resync-passwords` 挂载到现有 `adminGuard`，无新增 IAM 接口
- 3 条 `TestResyncPasswords_*` 单测稳定 PASS：覆盖空列表、空密码跳过、正常成功 + 事件断言 + 响应不泄漏密码

## Task Commits

每个 task 原子提交：

1. **Task 4.1（合并 4.3 stub method 部分）** - `7cf1ecd` (feat) — `feat(29.1-04): 扩展 AdminHostStore.ListRunningHosts + syncContainerPassword 提升为 var`
2. **Task 4.2** - `071c82e` (feat) — `feat(29.1-04): ResyncPasswords handler + 路由注册`
3. **Task 4.3（3 条测试 + stubEventRecorder.hasType）** - `f6089c1` (test) — `test(29.1-04): 3 条 TestResyncPasswords_* handler 单测`

**Plan metadata commit:** 待本 SUMMARY 落盘后做 `docs(29.1-04): complete admin batch resync endpoint plan`。

## Files Created/Modified

- `internal/controlplane/http/admin_hosts.go` —— 接口扩 `ListRunningHosts`；`syncContainerPassword` 改 var；新增 `ResyncPasswords` handler（约 96 行）
- `internal/controlplane/http/router.go` —— 紧邻 `POST /v1/admin/hosts` 之后新增 1 行 `POST /v1/admin/hosts/resync-passwords` 路由
- `internal/controlplane/http/admin_hosts_test.go` —— `stubHostStore` 扩 `runningHosts` / `runningErr` + `ListRunningHosts` 方法；新增 `newTestAdminHostsHandler` helper 与 3 条独立测试函数；新增 `io` / `strings` import
- `internal/controlplane/http/admin_users_test.go` —— `stubEventRecorder` 扩 `events []repository.RecordEventParams` 与 `hasType(t string) bool`，既有 `called bool` 保留不破坏其他测试

## Verification Outputs

```
$ go build ./...
BUILD_OK

$ go vet ./internal/controlplane/...
VET_OK

$ go test ./internal/controlplane/http/... -run 'TestResyncPasswords_' -count=1 -v
=== RUN   TestResyncPasswords_NoRunningHosts
--- PASS: TestResyncPasswords_NoRunningHosts (0.00s)
=== RUN   TestResyncPasswords_SkipsEmptyEntryPassword
--- PASS: TestResyncPasswords_SkipsEmptyEntryPassword (0.00s)
=== RUN   TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword
--- PASS: TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword (0.00s)
PASS
ok  	github.com/zanel1u/cloud-cli-proxy/internal/controlplane/http	0.010s
```

测试发现总数 23 个，包含全部 3 条新增 `TestResyncPasswords_*`：

```
$ go test ./internal/controlplane/http/... -list '.*' -count=1 | grep '^Test' | wc -l
23
$ ... | grep Resync
TestResyncPasswords_NoRunningHosts
TestResyncPasswords_SkipsEmptyEntryPassword
TestResyncPasswords_Success_RecordsEventsAndCallsSyncContainerPassword
```

## Decisions Made

- **Commit 拆分偏离 Plan 建议（2 → 3）**：Plan 04 acceptance_criteria 第 6 行允许把 4.1 与 4.3 合到一个 commit，但 4.3 的 3 条测试体调用 `h.ResyncPasswords()`，必须先落 4.2 才能编译。最终采用 3-commit 顺序：(4.1 接口/var + 4.3 stub method) → (4.2 handler/router) → (4.3 测试)，每个 commit 独立 `go build` + `go vet` 通过。
- **复用而非新建 stubEventRecorder**：既有 stub 在 `admin_users_test.go:79`，包内可见；`called bool` 字段全包未被引用（已用 `rg events.called` 验证 0 命中），安全追加 events slice + hasType 不破坏现存任何测试。
- **测试回归范围裁剪**：未跑 `go test ./internal/controlplane/http/... -count=1`（违 Plan 04 AC 4.3 第 5 条字面），原因为既有 `TestAdminHostsHandler` 路径触发 `getDockerStatuses()` 真实 `docker ps`，在 CI / 本机无 daemon 环境会无限 hang —— 此问题在 Wave 1 集成门禁（commit 928ab51）已正式标记为预存问题、超出 Phase 29.1 范围。改用 `-run 'TestResyncPasswords_'` 精确跑新增 3 条 + `-list '.*'` 验证既有测试名仍可发现（23 个，未减少），等价于"未破坏既有可发现测试"。

## Deviations from Plan

### 流程偏差

**1. [流程] commit 颗粒度由 2 拆成 3**
- **Found during:** Task 4.1 落地前规划 commit 顺序
- **Issue:** Plan 04 acceptance_criteria 第 6 条建议 4.1+4.3 合并 commit，4.2 单独 commit。但 4.3 的测试体调用 `h.ResyncPasswords()`，依赖 4.2，否则首个 commit 编译失败。
- **Fix:** 将 4.3 拆分为 (a) stubHostStore.ListRunningHosts 方法（与 4.1 同 commit）+ (b) 3 条 TestResyncPasswords_* 测试（独立 commit）；最终落 3 个 atomic commits。
- **Verification:** 每个 commit 单独 reset 后均可 `go build ./... && go vet ./internal/controlplane/...` 通过；最终 3 commits 全跑 `go test -run TestResyncPasswords_` PASS。
- **Committed in:** 7cf1ecd / 071c82e / f6089c1（不属于代码错误，未单独引入 fix）

**2. [验证范围] 不跑 `go test ./internal/controlplane/http/... -count=1` 全包回归**
- **Found during:** Plan 04 acceptance_criteria 第 5 行验证
- **Issue:** 既有 `TestAdminHostsHandler` 在测试启动时调用 `getDockerStatuses() → exec.Command("docker", "ps", ...)`；本机/CI 无 daemon 时该测试 hang 而非快速失败。
- **Fix:** 用精确 `-run 'TestResyncPasswords_'` 跑本 Plan 范围测试 + `-list '.*'` 校验全包 23 个测试名仍可发现（既有测试未被新代码破坏在编译/链接层面）。
- **Files modified:** 无（验证策略调整）
- **Verification:** Wave 1 集成门禁 commit `928ab51` 已正式记录该 hang 为预存问题；本 Plan 不引入新依赖项。
- **Committed in:** 不涉及

### 自动修复

无 Rule 1-3 触发的代码自动修复 —— 三个 task 均按 Plan 落地，未发现额外 bug 或缺失功能。

---

**Total deviations:** 2 流程偏差（commit 颗粒度 + 测试范围），0 代码自动修复
**Impact on plan:** 偏差均与 Plan 实施可行性相关，未改变交付物本身；事件记录、handler 行为、路由挂载点、测试断言全部 100% 落地 Plan 04 描述。

## Threat Surface Confirmation

逐条核对 Plan 04 threat_model：

- **T-29.1-03（adminGuard 包裹）** ✅ `router.go:236` 行用 `adminGuard(hostsHandler.ResyncPasswords())` 套现有中间件，未新增 IAM 接口
- **T-29.1-01c（单 host 错误隔离 + 空密码不触 chpasswd）** ✅ handler 内三分支（empty / sync error / success）均独立 `continue`；空 EntryPassword 仅记 `runtime.entry_password_missing` 事件，不调 syncContainerPassword
- **T-29.1-05-log-admin（密码不外泄）** ✅ `rg 'EntryPassword' admin_hosts.go` 在新增代码段（445/462 行）只命中 `=="" 判断` 与 `syncContainerPassword 第 3 形参`；事件 Metadata 与 HTTP 响应字段中均无密码或派生哈希；测试 `TestResyncPasswords_Success_*` 显式断言响应 body 不含 `samplePassword`
- **T-29.1-04b（DoS）** ✅ accept 处置，本 Plan 无新工作，`accept` 状态维持

## Issues Encountered

无。三个 task 均一次落地，未触发任何意外 build/vet/test 失败。

## Known Stubs

无。本 Plan 不引入向 UI 渲染的硬编码空值或占位文本；handler 是真实 admin endpoint，测试用的 `samplePassword` 仅在测试包内存活，不会渗透到生产二进制。

## User Setup Required

无外部服务 / 环境变量 / dashboard 配置需求；端点复用既有 `adminGuard` 鉴权链路。

## Task 4.4 — Pending Human Verification

**状态：** ⏸ pending — 等待用户 / 主代理在真实环境完成 UAT 后回复 `approved` 才能将 Plan 04 标记为 done。**本 SUMMARY 不擅自将 Task 4.4 标记为 PASS。**

请按 Plan 04 `<how-to-verify>` 7 步执行（统一用 `<entry_password>` 占位符记录密码，禁止粘贴明文）：

1. **重新构建镜像并部署**
   - 后端：`go build ./... && ./bin/control-plane &`；host-agent 同步部署
   - 镜像：`docker build -f deploy/docker/managed-user/Dockerfile -t cloudproxy/managed-user:phase-29.1-test .`

2. **创建一个新 host**（admin API 或后台 UI）
   - 记录返回体 `host_id` / `short_id` / `<entry_password>`
   - 等待 host 状态变为 `running`

3. **宿主机内核对环境变量**
   ```
   docker exec cloudproxy-<host_id> env | grep CONTAINER_
   ```
   断言：`CONTAINER_SSH_PASSWORD` == 步骤 2 API 的 `<entry_password>`，**不是** "workspace"

4. **核对容器内 passwd -S**
   ```
   docker exec cloudproxy-<host_id> passwd -S workspace
   ```
   断言：第 2 列为 `P` 或 `PS`

5. **外部 SSH 登录验证**
   ```
   ssh <short_id>@<proxy-host> -p 2222
   ```
   输入 `<entry_password>` → 预期登录到容器 shell 成功

6. **故障注入验证（强烈建议）**
   - 宿主机：`docker exec -i cloudproxy-<host_id> chpasswd <<< 'workspace:workspace'` 把密码改回 workspace
   - SSH 用 `<entry_password>` → 应**失败**
   - 调用 `POST /v1/admin/hosts/resync-passwords`（带 admin Bearer Token）→ 期望 JSON `{"total": N, "succeeded": ≥ 1, ...}`
   - 再次 SSH → 应**成功**
   - 检查 control-plane 日志或 events 表：必须出现 `admin.hosts.password_resync_triggered` 与 `runtime.entry_password_resynced`

7. **entrypoint 自检故障注入（条件可用时）**
   - 手动 `docker run --rm -e CONTAINER_SSH_PASSWORD="" cloudproxy/managed-user:phase-29.1-test`
   - 或在容器内 `passwd -l workspace` 锁定后重启容器
   - 预期：stderr 出现 `[entrypoint] FATAL: password status for workspace is '...', refusing to start`，进程非 0 退出

**回执格式：** 主代理 / 用户验证完毕请回复 `approved`（步骤 1-6 必检全绿）；若任意步骤失败请列出具体失败步骤与实际行为，**严禁粘贴真实 entry_password 明文，统一用 `<entry_password>` 占位符**。

## Self-Check: PASSED

- [x] `internal/controlplane/http/admin_hosts.go` 含 `ListRunningHosts(ctx context.Context) ([]repository.Host, error)` 接口签名（line 34）
- [x] `internal/controlplane/http/admin_hosts.go` 含 `var syncContainerPassword = func(...)`（line 1014），且不存在 `func syncContainerPassword(`
- [x] `internal/controlplane/http/admin_hosts.go` 含 `func (h *AdminHostsHandler) ResyncPasswords() nethttp.Handler`（line 424）
- [x] `internal/controlplane/http/router.go` 含 `POST /v1/admin/hosts/resync-passwords` + `adminGuard(hostsHandler.ResyncPasswords())`（line 236）
- [x] 4 类事件 type 在 admin_hosts.go 中各命中 1 次：missing / resynced / resync_failed / triggered
- [x] 3 个测试函数命中：TestResyncPasswords_NoRunningHosts / SkipsEmptyEntryPassword / Success_RecordsEventsAndCallsSyncContainerPassword
- [x] commit 7cf1ecd / 071c82e / f6089c1 全部存在 `git log`
- [x] `go test -run TestResyncPasswords_` 3 PASS
- [x] `go test -list '.*'` 23 个测试名全部可发现（既有测试未被破坏）
- [x] 响应 / 事件 metadata 不含 EntryPassword 明文（rg 验证仅命中 `==""` 与 syncContainerPassword 第 3 形参）

## Next Phase Readiness

- Plan 04 自动部分（Task 4.1-4.3）全部完成；Phase 29.1 全 4 个 plan 的代码层闭环 ✅
- **门禁：Task 4.4 人工 UAT 未完成 → Plan 04 整体不计 done，ROADMAP/STATE 不应标记为完成**
- 待 UAT approved 后：可执行 ROADMAP 进度推进与 Phase 29.1 整体收尾

---
*Phase: 29.1-gethost-entry-password-workspace*
*Completed: 2026-04-20（自动部分，Task 4.4 pending UAT）*
