---
phase: 36-sshfs
plan: "06"
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03", "36-04", "36-05"]
files_modified:
  - internal/cloudclaude/doctor/mount.go
  - internal/cloudclaude/doctor/doctor.go
  - internal/cloudclaude/doctor/mount_test.go
autonomous: true
requirements:
  - REQ-MOUNT-V31-05

must_haves:
  truths:
    - "`cloud-claude doctor mount --json | jq '.checks | length'` 比 v3.0 多 5（从 ≥17 提升到 ≥22）"
    - "require_git_repo check：cwd 不是 git 仓库时 Fail + MOUNT_REQUIRE_GIT_REPO"
    - "oversized_files_count check：last-session.json 有 OversizedFiles 时 Warn + MOUNT_OVERSIZED_FILE_SKIPPED"
    - "sshfs_cache_args check：远端 mount 缺 cache 参数时 Fail + MOUNT_SSHFS_FAILED"
    - "git_proxy_enabled check：proxy_commands 不含 git 时 Warn + AUTH_CONFIG_MISSING"
    - "default_ignore_loaded check：CLOUD_CLAUDE_NO_DEFAULT_IGNORE=1 时 Warn + AUTH_CONFIG_MISSING"
    - "go test ./... 全 PASS，make ci-gate PASS（SC#6）"
  artifacts:
    - path: "internal/cloudclaude/doctor/mount.go"
      provides: "5 个新 check 函数 + gitRevParseTopLevel 私有 helper"
      contains: "checkRequireGitRepo"
    - path: "internal/cloudclaude/doctor/doctor.go"
      provides: "mount domain block 末尾 5 项 check 注册"
      contains: "checkOversizedFilesCount"
    - path: "internal/cloudclaude/doctor/mount_test.go"
      provides: "5 项 check 各 pass/warn/fail/skip 矩阵"
      contains: "TestCheckRequireGitRepo"
  key_links:
    - from: "internal/cloudclaude/doctor/mount.go"
      to: "internal/cloudclaude/errcodes/codes.go"
      via: "errcodes.MOUNT_REQUIRE_GIT_REPO + MOUNT_OVERSIZED_FILE_SKIPPED（Plan 01 提供）"
      pattern: "errcodes\\.MOUNT_REQUIRE_GIT_REPO"
    - from: "internal/cloudclaude/doctor/doctor.go"
      to: "internal/cloudclaude/doctor/mount.go"
      via: "mount domain block append 5 新 check 函数调用"
      pattern: "checkRequireGitRepo|checkOversizedFilesCount|checkSSHFSCacheArgs|checkGitProxyEnabled|checkDefaultIgnoreLoaded"
---

<objective>
在 doctor/mount.go 追加 5 个新 check 函数，在 doctor/doctor.go 的 mount domain block 末尾注册这 5 项，并在 mount_test.go 为每项添加 pass/warn/fail/skip 矩阵测试。本 plan 是 Phase 36 最后一个 Wave，所有前置数据结构和错误码已就绪。同时通过 `go test ./...` 和 `make ci-gate` 完成 SC#6 CI 闸门验证。

Purpose: 对应 SC#4——`cloud-claude doctor mount --json | jq '.checks | length'` 比 v3.0 多 5；同时作为整个 Phase 36 的集成验证门。
Output: doctor/mount.go +5 函数 + doctor/doctor.go 注册 + mount_test.go 矩阵测试 + go test ./... PASS + make ci-gate PASS
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-sshfs/36-CONTEXT.md
@.planning/phases/36-sshfs/36-RESEARCH.md
@.planning/phases/36-sshfs/36-PATTERNS.md

<interfaces>
<!-- Plan 01 产出（依赖项） -->
From internal/cloudclaude/errcodes/codes.go (Plan 01):
```go
MOUNT_REQUIRE_GIT_REPO       Code = "MOUNT_REQUIRE_GIT_REPO"
MOUNT_OVERSIZED_FILE_SKIPPED Code = "MOUNT_OVERSIZED_FILE_SKIPPED"
// 已有：MOUNT_SSHFS_FAILED, AUTH_CONFIG_MISSING
```

<!-- Plan 02/03 产出（依赖项） -->
From internal/cloudclaude/last_session.go (Plan 02):
```go
type OversizedFile struct { Path string; SizeBytes int64 }
// LastSessionSnapshot.OversizedFiles []OversizedFile `json:"oversized_files,omitempty"`
```

From internal/cloudclaude/config.go (Plan 02):
```go
func (c *Config) EffectiveProxyCommands() []string { ... }
```

<!-- doctor/mount.go 现有结构（RESEARCH.md F4 锚点） -->
From internal/cloudclaude/doctor/mount.go (精确模板):
```go
// checkMergerfsBranches — 远端 runner + want 列表 + missing join 模式（Lines 27-59）
func checkMergerfsBranches(ctx context.Context, runner RemoteRunner) Check {
    if runner == nil {
        return newSkip("mount", "mergerfs_branches", "未能连接远端容器，跳过")
    }
    mountOut, _, _ := runner.RunScript("mergerfs_mount", "mount | grep mergerfs | head -1")
    want := []string{"func.readdir=cor:4", "cache.attr=30", ...}
    var missing []string
    for _, w := range want {
        if !strings.Contains(mountOut, w) {
            missing = append(missing, w)
        }
    }
    if len(missing) > 0 {
        return newFail("mount", "mergerfs_branches", errcodes.MOUNT_MERGERFS_FAILED, ...)
    }
    return Check{Domain: "mount", Name: "mergerfs_branches", Status: StatusPass, ...}
}

// checkFUSEResidual — Details map + 直接构造 Check{} 绕过 newWarn 占位符（Lines 73-106）
func checkFUSEResidual(ctx context.Context) Check { ... }

// check.go helpers（lines 67-91）：
// newPass(domain, name, msg) Check
// newWarn(domain, name, code Code) Check
// newFail(domain, name, code Code, detail string) Check
// newSkip(domain, name, reason string) Check
```

<!-- doctor/doctor.go 注册点（RESEARCH.md F4 锚点） -->
From internal/cloudclaude/doctor/doctor.go (mount domain block, lines 226-234):
```go
if want("mount") {
    ensureRemote()
    report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "mergerfs_branches", timeout,
        func(c context.Context) Check { return checkMergerfsBranches(c, remoteRunner) }))
    report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "sshfs_mountpoint", timeout,
        func(c context.Context) Check { return checkSSHFSMountpoint(c, remoteRunner) }))
    report.Checks = append(report.Checks, checkFUSEResidual(ctx))
    report.Checks = append(report.Checks, checkAppArmorFusermount3(ctx))
    // ↑ v3.0: 4 checks — Phase 36 在此段末尾追加 5 项
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: doctor/mount.go 追加 5 个 check 函数 + gitRevParseTopLevel 私有 helper</name>
  <read_first>
    - internal/cloudclaude/doctor/mount.go（全文，约 140 行，精确记录现有 4 个函数的结构 + import 列表）
    - internal/cloudclaude/doctor/check.go（全文，约 80 行，确认 newPass/newWarn/newFail/newSkip/runWithTimeout/StatusPass/StatusWarn/StatusFail/StatusSkip 签名）
    - internal/cloudclaude/doctor/doctor.go（lines 226-234，确认 mount domain block 末尾位置）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F4 锚点" §"L2 doctor cwd" §"L6 LoadConfig 失败" §"L8 newWarn 占位符"
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/doctor/mount.go"（5 个函数精确代码块）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-13（5 项 check 规格 + 错误码映射表）
  </read_first>
  <action>
在 doctor/mount.go 文件末尾追加以下内容（不新建文件，D-13 决策）：

**Step 1 — 私有 git helper（doctor 包内，不能 import cmd/cloud-claude main package）：**

```go
// gitRevParseTopLevel 执行 git rev-parse --show-toplevel，cwd 不在 git 仓库时返回 error。
// doctor 包内私有实现（不能 import cmd/cloud-claude main package，D-13）。
func gitRevParseTopLevel(cwd string) error {
    cmd := exec.Command("git", "rev-parse", "--show-toplevel")
    cmd.Dir = cwd
    cmd.Stdout = io.Discard
    cmd.Stderr = io.Discard
    return cmd.Run()
}
```

**Step 2 — checkRequireGitRepo（本地 check，不需要 remoteRunner）：**

```go
func checkRequireGitRepo(ctx context.Context) Check {
    cwd, err := os.Getwd()
    if err != nil {
        return newSkip("mount", "require_git_repo", "无法获取当前目录: "+err.Error())
    }
    if err := gitRevParseTopLevel(cwd); err != nil {
        return newFail("mount", "require_git_repo", errcodes.MOUNT_REQUIRE_GIT_REPO,
            "当前目录不是 git 仓库: "+cwd)
    }
    return newPass("mount", "require_git_repo", "当前目录位于 git 仓库内: "+cwd)
}
```

**Step 3 — checkOversizedFilesCount（本地 check，直接构造 Check{}，绕过 L8 newWarn 占位符问题）：**

```go
func checkOversizedFilesCount(ctx context.Context) Check {
    snap, err := cloudclaude.LoadLastSession()
    if err != nil || snap == nil {
        return newSkip("mount", "oversized_files_count",
            "last-session.json 不存在，跳过（STATE_LAST_SESSION_MISSING）")
    }
    n := len(snap.OversizedFiles)
    if n == 0 {
        return newPass("mount", "oversized_files_count", "上次会话无超大文件跳过记录")
    }
    top5 := make([]string, 0, 5)
    for i, f := range snap.OversizedFiles {
        if i >= 5 {
            break
        }
        top5 = append(top5, fmt.Sprintf("%s (%dMB)", f.Path, f.SizeBytes/1024/1024))
    }
    entry, _ := errcodes.Lookup(errcodes.MOUNT_OVERSIZED_FILE_SKIPPED)
    return Check{
        Domain:     "mount",
        Name:       "oversized_files_count",
        Status:     StatusWarn,
        Code:       errcodes.MOUNT_OVERSIZED_FILE_SKIPPED,
        Message:    fmt.Sprintf("上次会话跳过了 %d 个超大文件，由 cold sshfs 兜底", n),
        NextAction: entry.NextAction,
        Details:    map[string]any{"oversized_count": n, "top5_files": top5},
    }
}
```

**Step 4 — checkSSHFSCacheArgs（远端 check，checkMergerfsBranches 的精确镜像）：**

```go
func checkSSHFSCacheArgs(ctx context.Context, runner RemoteRunner) Check {
    if runner == nil {
        return newSkip("mount", "sshfs_cache_args", "未能连接远端容器，跳过")
    }
    mountOut, _, _ := runner.RunScript("sshfs_mount", "mount | grep sshfs | head -1")
    want := []string{"cache=yes", "kernel_cache", "auto_cache", "cache_timeout=300"}
    var missing []string
    for _, w := range want {
        if !strings.Contains(mountOut, w) {
            missing = append(missing, w)
        }
    }
    if len(missing) > 0 {
        return newFail("mount", "sshfs_cache_args", errcodes.MOUNT_SSHFS_FAILED,
            "sshfs cache 参数缺少: "+strings.Join(missing, ","))
    }
    return Check{
        Domain:  "mount",
        Name:    "sshfs_cache_args",
        Status:  StatusPass,
        Message: "sshfs cache 参数完整（cache=yes,kernel_cache,auto_cache,cache_timeout=300）",
        Details: map[string]any{"mount": strings.TrimSpace(mountOut)},
    }
}
```

**Step 5 — checkGitProxyEnabled（本地 check）：**

```go
func checkGitProxyEnabled(ctx context.Context) Check {
    cfg, err := cloudclaude.LoadConfig()
    if err != nil {
        return newSkip("mount", "git_proxy_enabled", "配置未 init，跳过: "+err.Error())
    }
    for _, cmd := range cfg.EffectiveProxyCommands() {
        if cmd == "git" {
            return newPass("mount", "git_proxy_enabled", "proxy_commands 包含 git")
        }
    }
    return newWarn("mount", "git_proxy_enabled", errcodes.AUTH_CONFIG_MISSING)
}
```

**Step 6 — checkDefaultIgnoreLoaded（本地 check）：**

```go
func checkDefaultIgnoreLoaded(ctx context.Context) Check {
    if os.Getenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE") == "1" {
        return newWarn("mount", "default_ignore_loaded", errcodes.AUTH_CONFIG_MISSING)
    }
    return newPass("mount", "default_ignore_loaded", "默认二进制黑名单已加载")
}
```

**import 确认**（mount.go 若已有 os/exec/strings 则无需重复；否则追加）：
需要：`os`, `os/exec`, `io`, `fmt`, `strings`, `context`,
`github.com/…/cloudclaude`, `github.com/…/errcodes`
  </action>
  <acceptance_criteria>
    - `grep -c "^func check" internal/cloudclaude/doctor/mount.go` 输出比原有多 5（5 个新函数）
    - `grep "gitRevParseTopLevel" internal/cloudclaude/doctor/mount.go` 存在（私有 helper）
    - `grep "MOUNT_REQUIRE_GIT_REPO" internal/cloudclaude/doctor/mount.go` 存在
    - `grep "MOUNT_OVERSIZED_FILE_SKIPPED" internal/cloudclaude/doctor/mount.go` 存在
    - `grep "sshfs cache 参数缺少" internal/cloudclaude/doctor/mount.go` 存在
    - `go build ./internal/cloudclaude/doctor/...` 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/cloudclaude/doctor/... && grep -c "^func check" internal/cloudclaude/doctor/mount.go</automated>
  </verify>
  <done>mount.go 含 5 个新函数 + gitRevParseTopLevel helper，go build 干净</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: doctor.go 注册 5 项 check + mount_test.go 矩阵测试 + CI 闸门验证</name>
  <read_first>
    - internal/cloudclaude/doctor/doctor.go（lines 219-240，mount domain block，确认精确追加位置）
    - internal/cloudclaude/doctor/mount_test.go（全文，确认现有测试结构 + mock RemoteRunner 模式）
    - .planning/phases/36-sshfs/36-RESEARCH.md §"F4 doctor.go 注册点"（代码块）
    - .planning/phases/36-sshfs/36-PATTERNS.md §"internal/cloudclaude/doctor/mount_test.go"（5 项 check 矩阵模板）
    - .planning/phases/36-sshfs/36-CONTEXT.md §D-14（5 项 check 注册后总数 ≥22，SC#4 达标）
  </read_first>
  <behavior>
    - Test 1: checkRequireGitRepo pass（cwd = git 仓库）
    - Test 2: checkRequireGitRepo fail（cwd = 临时目录）
    - Test 3: checkSSHFSCacheArgs pass（mock runner 返回含全部 4 参数的 mount 输出）
    - Test 4: checkSSHFSCacheArgs fail（缺 kernel_cache）
    - Test 5: checkSSHFSCacheArgs skip（runner == nil）
    - Test 6: checkOversizedFilesCount pass（snap.OversizedFiles 空）
    - Test 7: checkOversizedFilesCount warn（snap.OversizedFiles 含 1 条）
    - Test 8: checkGitProxyEnabled pass（config proxy_commands 含 git）
    - Test 9: checkGitProxyEnabled warn（config proxy_commands 不含 git）
    - Test 10: checkDefaultIgnoreLoaded pass（未设 env）
    - Test 11: checkDefaultIgnoreLoaded warn（CLOUD_CLAUDE_NO_DEFAULT_IGNORE=1）
  </behavior>
  <action>
**Step 1 — doctor.go mount domain block 末尾追加 5 项注册：**

在 `report.Checks = append(report.Checks, checkAppArmorFusermount3(ctx))` 行之后追加：

```go
// [Phase 36] +5 项 mount check（REQ-MOUNT-V31-05，总计 ≥9 mount checks，SC#4 达标）
// 本地 check：不需要 ensureRemote()
report.Checks = append(report.Checks, checkRequireGitRepo(ctx))
report.Checks = append(report.Checks, checkOversizedFilesCount(ctx))
// 远端 check：需要 remoteRunner
report.Checks = append(report.Checks, runWithTimeout(ctx, "mount", "sshfs_cache_args", timeout,
    func(c context.Context) Check { return checkSSHFSCacheArgs(c, remoteRunner) }))
// 本地 check
report.Checks = append(report.Checks, checkGitProxyEnabled(ctx))
report.Checks = append(report.Checks, checkDefaultIgnoreLoaded(ctx))
```

注意：`checkRequireGitRepo`、`checkOversizedFilesCount`、`checkGitProxyEnabled`、`checkDefaultIgnoreLoaded` 均为本地 check，不调用 `ensureRemote()`。只有 `checkSSHFSCacheArgs` 走 `runWithTimeout` + `remoteRunner`。

**Step 2 — mount_test.go 追加 11 条矩阵测试：**

复用现有 mock RemoteRunner 模式，为每个新 check 函数覆盖 pass/warn/fail/skip 场景。示例：

```go
func TestCheckRequireGitRepo_Pass(t *testing.T) {
    // 使用当前 cwd（cloud-cli-proxy 本身是 git 仓库），os.Getwd() 返回 git 仓库
    result := checkRequireGitRepo(context.Background())
    if result.Status != StatusPass {
        t.Errorf("git 仓库内应 Pass，got %v: %v", result.Status, result.Message)
    }
}

func TestCheckRequireGitRepo_Fail(t *testing.T) {
    // 临时切换 cwd 到非 git 目录
    dir := t.TempDir()
    orig, _ := os.Getwd()
    os.Chdir(dir)
    defer os.Chdir(orig)

    result := checkRequireGitRepo(context.Background())
    if result.Status != StatusFail {
        t.Errorf("非 git 仓库应 Fail，got %v", result.Status)
    }
    if result.Code != errcodes.MOUNT_REQUIRE_GIT_REPO {
        t.Errorf("Code 应为 MOUNT_REQUIRE_GIT_REPO，got %v", result.Code)
    }
}

func TestCheckSSHFSCacheArgs_Pass(t *testing.T) {
    runner := &mockRemoteRunner{
        scripts: map[string]string{
            "sshfs_mount": "sshfs#root@127.0.0.1:/ /workspace-cold fuse.sshfs passive,reconnect,cache=yes,kernel_cache,auto_cache,cache_timeout=300 0 0",
        },
    }
    result := checkSSHFSCacheArgs(context.Background(), runner)
    if result.Status != StatusPass {
        t.Errorf("含全部 cache 参数应 Pass，got %v: %v", result.Status, result.Message)
    }
}

func TestCheckSSHFSCacheArgs_Fail_MissingKernelCache(t *testing.T) {
    runner := &mockRemoteRunner{
        scripts: map[string]string{
            "sshfs_mount": "sshfs#root@127.0.0.1:/ /workspace-cold fuse.sshfs cache=yes,auto_cache,cache_timeout=300 0 0",
        },
    }
    result := checkSSHFSCacheArgs(context.Background(), runner)
    if result.Status != StatusFail {
        t.Errorf("缺 kernel_cache 应 Fail，got %v", result.Status)
    }
    if !strings.Contains(result.Message, "kernel_cache") {
        t.Errorf("Fail 消息应含 kernel_cache，got %v", result.Message)
    }
}

func TestCheckSSHFSCacheArgs_Skip_NilRunner(t *testing.T) {
    result := checkSSHFSCacheArgs(context.Background(), nil)
    if result.Status != StatusSkip {
        t.Errorf("nil runner 应 Skip，got %v", result.Status)
    }
}

func TestCheckOversizedFilesCount_Pass(t *testing.T) {
    // 需要 mock LoadLastSession 或临时写入 last-session.json
    // executor 按实际 LoadLastSession 实现选择 mock 方式
    // 最简方案：在 t.TempDir() 写一个空 oversized_files 的 session 文件，
    // 再通过 env var 或 test helper 覆盖 lastSessionPath
}

func TestCheckOversizedFilesCount_Warn(t *testing.T) {
    // 同上，写入含 1 条 OversizedFiles 的 session 文件
}

func TestCheckGitProxyEnabled_Pass(t *testing.T) {
    // 需要 mock LoadConfig 返回含 git 的 ProxyCommands
}

func TestCheckGitProxyEnabled_Warn(t *testing.T) {
    // mock LoadConfig 返回不含 git 的 ProxyCommands
}

func TestCheckDefaultIgnoreLoaded_Pass(t *testing.T) {
    t.Setenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE", "")
    result := checkDefaultIgnoreLoaded(context.Background())
    if result.Status != StatusPass {
        t.Errorf("未设环境变量应 Pass，got %v", result.Status)
    }
}

func TestCheckDefaultIgnoreLoaded_Warn(t *testing.T) {
    t.Setenv("CLOUD_CLAUDE_NO_DEFAULT_IGNORE", "1")
    result := checkDefaultIgnoreLoaded(context.Background())
    if result.Status != StatusWarn {
        t.Errorf("CLOUD_CLAUDE_NO_DEFAULT_IGNORE=1 应 Warn，got %v", result.Status)
    }
    if result.Code != errcodes.AUTH_CONFIG_MISSING {
        t.Errorf("Code 应为 AUTH_CONFIG_MISSING，got %v", result.Code)
    }
}
```

对于 `checkOversizedFilesCount` 和 `checkGitProxyEnabled` 依赖外部状态（last-session.json / config.yaml）的测试，executor 选择最小 mock 方案（如包级 var 替换 LoadLastSession/LoadConfig，或直接在 t.TempDir() 中写临时文件后设置环境变量指向）。

**Step 3 — CI 闸门验证（本 plan 最终验收步骤）：**

```bash
go test ./... 2>&1 | tail -20   # 期望全 PASS，无 FAIL 行
make ci-gate                     # 期望 PASS（doctor JSON grep + errcodes + ≥200 字断言）
```
  </action>
  <acceptance_criteria>
    - `grep -c "checkRequireGitRepo\|checkOversizedFilesCount\|checkSSHFSCacheArgs\|checkGitProxyEnabled\|checkDefaultIgnoreLoaded" internal/cloudclaude/doctor/doctor.go` 输出 `5`（5 行注册）
    - `go test ./internal/cloudclaude/doctor/... -run TestCheck -v` 输出 ≥ 8 条 PASS（允许 mock 困难的部分 SKIP）
    - `go test ./... 2>&1 | grep -c "^FAIL"` 输出 `0`（无 FAIL 包）
    - `make ci-gate` 退出码 0（doctor JSON + errcodes + 长说明闸门全通过）
    - 运行 `cloud-claude doctor mount --json | jq '.checks | length'` 输出 ≥ 9（mount 维度 4+5=9 项）
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/cloudclaude/doctor/... -v && go test ./... 2>&1 | grep -E "^ok|^FAIL" | tail -20</automated>
  </verify>
  <done>doctor.go mount domain 含 5 项新注册，mount_test.go 覆盖 11 场景，go test ./... 全 PASS，make ci-gate PASS，SC#6 闸门通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 说明 |
|------|------|
| doctor check 本地执行 → 本地 FS | 5 项本地 check 仅读取 cwd/env/config，不写入 |
| doctor check 远端执行 → RemoteRunner | sshfs_cache_args 通过 RemoteRunner 执行 `mount \| grep sshfs`，输出只读不写 |
| doctor JSON 输出 → stdout | OversizedFiles top5 路径使用相对路径，不暴露绝对路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Information Disclosure | checkOversizedFilesCount.Details.top5_files | mitigate | 使用 OversizedFile.Path 相对路径（Plan 02 定义），不暴露家目录结构（ASVS L1 §7.3 文件路径安全） |
| T-36-06-02 | Tampering | gitRevParseTopLevel 可被伪造 git 命令影响 | accept | 与 T-36-04-01 同层；doctor 场景不是安全决策点（不作为权限检查），仅诊断提示 |
| T-36-06-03 | Elevation of Privilege | checkGitProxyEnabled 读取 config.yaml | accept | 只读操作；config.yaml 在用户家目录，已有文件系统权限控制 |
| T-36-06-04 | Information Disclosure | checkSSHFSCacheArgs 远端 mount 输出 | accept | mount 命令输出不含凭据；RemoteRunner 复用 v3.0 已有 SSH 通道 |
</threat_model>

<verification>
```bash
# Phase 36 完整验收
go test ./...
make ci-gate

# SC#1
cd /tmp && cloud-claude 2>&1 | head -5  # 期望含 MOUNT_REQUIRE_GIT_REPO，exit 4

# SC#4
cloud-claude doctor mount --json | jq '.checks | length'  # 期望 ≥ 9

# SC#5
cloud-claude explain MOUNT_REQUIRE_GIT_REPO  # exit 0，stdout ≥ 200 字
cloud-claude explain MOUNT_OVERSIZED_FILE_SKIPPED  # exit 0，stdout ≥ 200 字
```
</verification>

<success_criteria>
- doctor/mount.go 含 5 个新 check 函数（checkRequireGitRepo/checkOversizedFilesCount/checkSSHFSCacheArgs/checkGitProxyEnabled/checkDefaultIgnoreLoaded）+ gitRevParseTopLevel helper
- doctor/doctor.go mount domain block 末尾注册 5 项（4 本地 + 1 远端）
- mount_test.go 新增 ≥ 8 条测试覆盖主场景
- go test ./... 全 PASS（SC#6 前半）
- make ci-gate PASS（SC#6 后半）
- Phase 36 6 条 Success Criteria 全部达标
</success_criteria>

<output>
完成后创建 `.planning/phases/36-sshfs/36-06-SUMMARY.md`，记录：
- mount domain 最终 check 总数（期望 ≥ 9）
- make ci-gate 实际输出摘要
- go test ./... 通过包列表
- Phase 36 所有 SC 验收结果（SC#1~SC#6）
</output>
