---
phase: 260421-host-bind-mounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/migrations/0015_host_mounts.sql
  - internal/store/repository/models.go
  - internal/store/repository/queries.go
  - internal/agentapi/contracts.go
  - internal/runtime/runtime_service.go
  - internal/runtime/tasks/worker.go
  - internal/controlplane/http/admin_hosts.go
  - internal/controlplane/http/router.go
  - web/admin/src/hooks/use-hosts.ts
  - web/admin/src/components/hosts/mount-manager.tsx
  - web/admin/src/routes/_dashboard/hosts/$hostId.tsx
  - web/admin/src/components/hosts/create-host-dialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "创建主机时可传入 host_mounts 数组，数据写入 DB JSONB 列"
    - "GET 主机详情返回 host_mounts 字段"
    - "PUT /v1/admin/hosts/{hostID}/mounts 可独立更新挂载配置"
    - "重建容器时 host_mounts 自动转化为 docker create --mount type=bind 参数"
    - "source/target 必须为绝对路径，空值或非 / 开头被拒绝"
    - "前端详情页展示并可编辑挂载配置"
    - "前端创建对话框支持可选配置挂载路径"
  artifacts:
    - path: "internal/store/migrations/0015_host_mounts.sql"
      provides: "hosts 表 host_mounts JSONB 列"
      contains: "host_mounts"
    - path: "internal/store/repository/models.go"
      provides: "HostMount 结构体 + HostMounts 类型"
      contains: "HostMount"
    - path: "internal/agentapi/contracts.go"
      provides: "BindMount 结构体 + HostActionRequest.BindMounts"
      contains: "BindMount"
    - path: "internal/runtime/tasks/worker.go"
      provides: "bind mount --mount type=bind 参数生成"
      contains: "type=bind"
    - path: "internal/controlplane/http/admin_hosts.go"
      provides: "UpdateMounts handler"
      contains: "UpdateMounts"
    - path: "web/admin/src/components/hosts/mount-manager.tsx"
      provides: "挂载管理组件"
      contains: "MountManager"
  key_links:
    - from: "internal/controlplane/http/admin_hosts.go"
      to: "internal/store/repository/queries.go"
      via: "UpdateHostMounts call"
      pattern: "UpdateHostMounts"
    - from: "internal/runtime/runtime_service.go"
      to: "internal/agentapi/contracts.go"
      via: "host.HostMounts -> request.BindMounts mapping"
      pattern: "BindMounts"
    - from: "internal/runtime/tasks/worker.go"
      to: "internal/agentapi/contracts.go"
      via: "request.BindMounts iteration"
      pattern: "BindMount"
    - from: "web/admin/src/components/hosts/mount-manager.tsx"
      to: "PUT /v1/admin/hosts/{hostID}/mounts"
      via: "useUpdateHostMounts mutation"
      pattern: "updateHostMounts"
---

<objective>
实现宿主机路径挂载管理功能的全链路：DB 迁移、模型、仓储查询、Agent API 契约、Runtime 映射、Worker Docker 参数生成、Admin API 端点、前端类型 Hooks、挂载管理组件、详情页和创建对话框接入。

Purpose: 允许管理员通过管理界面为容器配置额外的宿主机目录 bind mount，且重建时不丢失。
Output: 完整后端 + 前端支持，可直接使用。
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- 关键类型和契约，executor 无需探索 codebase -->

From internal/store/repository/models.go:
```go
type Host struct {
    ID, UserID, Status, ShortID, EntryPassword, TemplateImageRef string
    HomeVolumeName, SlotKey, Timezone, Hostname string
    MemoryLimitMB int; CPULimit float64; DiskLimitGB int
    CreatedAt, UpdatedAt time.Time
}
type UpsertHostParams struct {
    UserID, Status, ShortID, EntryPassword, TemplateImageRef string
    HomeVolumeName, SlotKey, Timezone, Hostname string
    MemoryLimitMB int; CPULimit float64; DiskLimitGB int
}
```

From internal/agentapi/contracts.go:
```go
type VolumeMount struct {
    Name, Target string; ReadOnly bool; Labels map[string]string
}
type HostActionRequest struct {
    // ... existing fields ...
    Volumes []VolumeMount `json:"volumes,omitempty"`
}
```

From internal/controlplane/http/admin_hosts.go:
```go
type AdminHostStore interface {
    ListHostsWithUsername(ctx) ([]repository.HostWithUsername, error)
    GetHostDetail(ctx, string) (repository.HostDetail, error)
    GetHost(ctx, string) (repository.Host, error)
    UpsertHost(ctx, repository.UpsertHostParams) (repository.Host, error)
    // ... other methods ...
}
type adminHostDetailResponse struct {
    repository.HostDetail
    ConnectionInfo       *repository.ConnectionInfo `json:"connection_info,omitempty"`
    PersistentVolumeName string                     `json:"persistent_volume_name,omitempty"`
}
```

From internal/runtime/tasks/worker.go (buildCreateArgs L221-230):
```go
for _, vm := range request.Volumes {
    opts := fmt.Sprintf("type=volume,src=%s,dst=%s", vm.Name, vm.Target)
    if vm.ReadOnly { opts += ",readonly" }
    args = append(args, "--mount", opts)
}
```

From web/admin/src/hooks/use-hosts.ts:
```typescript
export interface HostDetail {
  host: { id, user_id, status, short_id, template_image_ref, home_volume_name, slot_key, timezone, hostname, created_at, updated_at };
  user: HostUser;
  bindings: HostBinding[];
  connection_info?: ConnectionInfo;
}
export function useCreateHost() {
  return useMutation({
    mutationFn: (data: { user_id: string; egress_ip_id: string; timezone?: string }) =>
      apiFetch<...>("/hosts", { method: "POST", body: JSON.stringify(data) }),
  });
}
```

From web/admin/src/routes/_dashboard/hosts/$hostId.tsx:
- 出口 IP 绑定 Card (L321-335) 使用 `<BindingManager>` 组件
- 生命周期 Card (L337+) 在其后
- 整体结构: 基本信息 Card -> 连接方式 Card -> 两列 Grid (出口 IP 绑定 | 生命周期)

From web/admin/src/components/hosts/binding-manager.tsx:
- Props: `{ hostId, hostStatus, bindings }`
- 使用 `useBindEgressIP()` / `useUnbindEgressIP()` mutations
- 运行中 (`isRunning`) 禁用操作
- 本地编辑 + 操作按钮模式

From web/admin/src/components/hosts/create-host-dialog.tsx:
- 表单 state: userId, egressIpId, timezone
- handleSubmit 调用 `createMutation.mutate({ user_id, egress_ip_id, timezone })`
- 使用 Select, Label, Button, Dialog 等 shadcn/ui 组件
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端全链路 — 迁移、模型、仓储、契约、Runtime、Worker、Admin API</name>
  <files>
    internal/store/migrations/0015_host_mounts.sql
    internal/store/repository/models.go
    internal/store/repository/queries.go
    internal/agentapi/contracts.go
    internal/runtime/runtime_service.go
    internal/runtime/tasks/worker.go
    internal/controlplane/http/admin_hosts.go
    internal/controlplane/http/router.go
  </files>
  <action>
**Step 1: DB Migration**

新建 `internal/store/migrations/0015_host_mounts.sql`:
```sql
ALTER TABLE hosts ADD COLUMN IF NOT EXISTS host_mounts JSONB NOT NULL DEFAULT '[]';
```
JSONB 数组格式: `[{"source":"/data/shared","target":"/mnt/shared","read_only":false}]`

**Step 2: Repository 模型**

修改 `internal/store/repository/models.go`:
- 在 Host 结构体 (L34) 末尾 `}` 之前新增字段 `HostMounts HostMounts \`json:"host_mounts"\``
- 新增类型（放在 Host 结构体之后）:
```go
type HostMount struct {
    Source   string `json:"source"`
    Target   string `json:"target"`
    ReadOnly bool   `json:"read_only,omitempty"`
}
type HostMounts []HostMount
```
- `UpsertHostParams` (L258) 末尾新增 `HostMounts HostMounts`

**Step 3: Repository 查询**

修改 `internal/store/repository/queries.go`:

1. **upsertHostSQL (L364)**: INSERT 列列表追加 `host_mounts`，VALUES 追加 `$13`（host_mounts 在 disk_limit_gb 之后），ON CONFLICT UPDATE SET 追加 `host_mounts = EXCLUDED.host_mounts`，RETURNING 追加 `host_mounts`。参数传递: 在现有 12 个参数之后追加 `mountsJSON`（先 `json.Marshal(params.HostMounts)` 得到 `[]byte`）。Scan 末尾追加一个 `json.RawMessage` 变量，函数末尾 `json.Unmarshal` 到 `item.HostMounts`。

2. **getHostSQL (L703)**: SELECT 追加 `, host_mounts`。GetHost (L709) Scan 末尾追加 `&rawMounts`（`var rawMounts json.RawMessage`），Scan 后 `json.Unmarshal(rawMounts, &item.HostMounts)`。

3. **listRunningHostsSQL (L957)**: SELECT 追加 `, host_mounts`。ListRunningHosts Scan 同上模式。

4. **listHostsWithUsernameSQL (L607)**: SELECT 追加 `, h.host_mounts`。ListHostsWithUsername 扫描 HostWithUsername 时同上模式（注意 HostWithUsername 嵌入 Host，扫描到 Host 字段）。

5. **新增 `UpdateHostMounts`**:
```go
func (r *Repository) UpdateHostMounts(ctx context.Context, hostID string, mounts HostMounts) error {
    data, err := json.Marshal(mounts)
    if err != nil {
        return fmt.Errorf("marshal host mounts: %w", err)
    }
    _, err = r.db.Exec(ctx, `UPDATE hosts SET host_mounts = $1, updated_at = NOW() WHERE id = $2`, data, hostID)
    return err
}
```
确保 `encoding/json` 在 import 中。

**Step 4: Agent API 契约**

修改 `internal/agentapi/contracts.go`:
- 在 VolumeMount 结构体 (L24) 之后新增:
```go
type BindMount struct {
    Source   string `json:"source"`
    Target   string `json:"target"`
    ReadOnly bool   `json:"read_only,omitempty"`
}
```
- HostActionRequest (L31) 末尾（ClaudeAccountID 之后）追加:
```go
BindMounts []BindMount `json:"bind_mounts,omitempty"`
```

**Step 5: Runtime Service 映射**

修改 `internal/runtime/runtime_service.go`:
- 在 QueueHostAction (L80) 中构建 `request` 之后（约 L163 之后），在 `request.EntryPassword == ""` 检查之前，插入:
```go
// 宿主机 bind mount 映射：repository.HostMounts -> agentapi.BindMounts
if len(host.HostMounts) > 0 {
    request.BindMounts = make([]agentapi.BindMount, 0, len(host.HostMounts))
    for _, hm := range host.HostMounts {
        request.BindMounts = append(request.BindMounts, agentapi.BindMount{
            Source:   hm.Source,
            Target:   hm.Target,
            ReadOnly: hm.ReadOnly,
        })
    }
}
```

**Step 6: Worker Docker 挂载**

修改 `internal/runtime/tasks/worker.go`:
- 在 `buildCreateArgs` 中，Volumes 循环 (L221-230) 之后、Labels 循环 (L232) 之前，新增:
```go
for _, bm := range request.BindMounts {
    if bm.Source == "" || bm.Target == "" {
        return nil, fmt.Errorf("invalid bind mount: source=%q target=%q", bm.Source, bm.Target)
    }
    if !strings.HasPrefix(bm.Source, "/") || !strings.HasPrefix(bm.Target, "/") {
        return nil, fmt.Errorf("bind mount paths must be absolute: source=%q target=%q", bm.Source, bm.Target)
    }
    if err := os.MkdirAll(bm.Source, 0o755); err != nil {
        return nil, fmt.Errorf("create bind mount source dir %s: %w", bm.Source, err)
    }
    opts := fmt.Sprintf("type=bind,src=%s,dst=%s", bm.Source, bm.Target)
    if bm.ReadOnly {
        opts += ",readonly"
    }
    args = append(args, "--mount", opts)
}
```
注意：确认 import 中有 `os` 和 `strings`（通常已有）。

**Step 7: Admin API**

修改 `internal/controlplane/http/admin_hosts.go`:
1. AdminHostStore 接口 (L26) 末尾追加:
```go
UpdateHostMounts(ctx context.Context, hostID string, mounts repository.HostMounts) error
```
2. **Create handler (L152)**: 请求体 struct 追加 `HostMounts repository.HostMounts \`json:"host_mounts"\``。UpsertHostParams (L193) 追加 `HostMounts: body.HostMounts`。
3. **Get handler (L104)**: `adminHostDetailResponse` (L98) 已嵌入 `repository.HostDetail`，HostDetail 包含 Host，Host 新增了 HostMounts 字段，JSON 序列化自动生效，无需改动。
4. **新增 UpdateMounts handler**（在文件末尾或 Delete handler 之后）:
```go
func (h *AdminHostsHandler) UpdateMounts() nethttp.Handler {
    return nethttp.HandlerFunc(func(w nethttp.ResponseWriter, r *nethttp.Request) {
        hostID := r.PathValue("hostID")
        var body struct {
            Mounts repository.HostMounts `json:"mounts"`
        }
        if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
            writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "invalid request body"})
            return
        }
        for _, m := range body.Mounts {
            if m.Source == "" || m.Target == "" {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "source and target are required"})
                return
            }
            if !strings.HasPrefix(m.Source, "/") || !strings.HasPrefix(m.Target, "/") {
                writeJSON(w, nethttp.StatusBadRequest, map[string]string{"error": "paths must be absolute (start with /)"})
                return
            }
        }
        if err := h.store.UpdateHostMounts(r.Context(), hostID, body.Mounts); err != nil {
            h.logger.Error("update host mounts failed", "host_id", hostID, "error", err)
            writeJSON(w, nethttp.StatusInternalServerError, map[string]string{"error": "update mounts failed"})
            return
        }
        if h.events != nil {
            hid := hostID
            if _, err := h.events.RecordEvent(r.Context(), repository.RecordEventParams{
                HostID: &hid, Level: "info", Type: "admin.host.update_mounts",
                Message: "管理员更新主机挂载配置",
                Metadata: map[string]any{"operator": "admin", "mount_count": len(body.Mounts)},
            }); err != nil {
                h.logger.Error("record event failed", "type", "admin.host.update_mounts", "error", err)
            }
        }
        writeJSON(w, nethttp.StatusOK, map[string]string{"status": "ok"})
    })
}
```

修改 `internal/controlplane/http/router.go`:
- 在 hosts 路由块（约 L253 之后，claude routes 之前）追加:
```go
mux.Handle("PUT /v1/admin/hosts/{hostID}/mounts", adminGuard(hostsHandler.UpdateMounts()))
```

**Step 8: 编译验证**

运行 `go build ./...` 确认无编译错误。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/cloud-cli-proxy && go build ./...</automated>
  </verify>
  <done>
    - `go build ./...` 通过
    - 0015_host_mounts.sql 存在且含 host_mounts JSONB 列
    - models.go 含 HostMount 结构体 + HostMounts 类型 + Host.HostMounts + UpsertHostParams.HostMounts
    - queries.go 含 UpdateHostMounts 方法，getHostSQL/listRunningHostsSQL/listHostsWithUsernameSQL 均 SELECT host_mounts
    - agentapi/contracts.go 含 BindMount 结构体 + HostActionRequest.BindMounts
    - runtime_service.go QueueHostAction 中 host.HostMounts 映射到 request.BindMounts
    - worker.go buildCreateArgs 中 BindMounts 循环生成 --mount type=bind 参数 + 绝对路径校验 + os.MkdirAll
    - admin_hosts.go 含 UpdateMounts handler + AdminHostStore 接口新增方法
    - router.go 注册 PUT /v1/admin/hosts/{hostID}/mounts
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端接入 — 类型 Hooks、挂载管理组件、详情页、创建对话框</name>
  <files>
    web/admin/src/hooks/use-hosts.ts
    web/admin/src/components/hosts/mount-manager.tsx
    web/admin/src/routes/_dashboard/hosts/$hostId.tsx
    web/admin/src/components/hosts/create-host-dialog.tsx
  </files>
  <action>
**Step 1: 类型和 Hooks**

修改 `web/admin/src/hooks/use-hosts.ts`:
1. 新增 `HostMount` 接口:
```typescript
export interface HostMount {
  source: string;
  target: string;
  read_only?: boolean;
}
```
2. `HostDetail.host` 类型 (L45) 追加 `host_mounts?: HostMount[]`。
3. `useCreateHost` mutationFn 的 data 类型追加 `host_mounts?: HostMount[]`。
4. 新增 `useUpdateHostMounts` hook:
```typescript
export function useUpdateHostMounts(hostId: string) {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (mounts: HostMount[]) =>
      apiFetch(`/hosts/${hostId}/mounts`, {
        method: "PUT",
        body: JSON.stringify({ mounts }),
      }),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["hosts", hostId] });
    },
  });
}
```

**Step 2: 挂载管理组件**

新建 `web/admin/src/components/hosts/mount-manager.tsx`:
参照 `binding-manager.tsx` (Props: `{ hostId, hostStatus, bindings }`) 模式:

```typescript
import { useState } from "react";
import { X, Plus, AlertTriangle } from "lucide-react";
import { toast } from "sonner";
import { useUpdateHostMounts, type HostMount } from "@/hooks/use-hosts";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Checkbox } from "@/components/ui/checkbox";

interface MountManagerProps {
  hostId: string;
  hostStatus: string;
  mounts: HostMount[];
}

const RESERVED_PATHS = ["/workspace", "/home/claude", "/var/lib/claude-persist"];
```

组件逻辑:
- Props: `{ hostId, hostStatus, mounts }`
- `isRunning = hostStatus === "running"`
- 本地 state: `localMounts` (从 props.mounts 初始化), `newSource`, `newTarget`, `newReadOnly`
- 添加行: source 输入框 onChange 时如果 target 为空或等于上一次的 source，自动同步 target = source（默认 source = target）
- target 为保留路径时显示黄色警告提示（AlertTriangle icon + "该路径为系统保留路径，可能影响容器正常运行"）
- source 和 target 都必须以 `/` 开头，否则 Input 旁边显示红色文字提示
- 每行右侧有删除按钮（X icon），运行中隐藏
- 底部"保存"按钮调用 `updateMountsMutation.mutate(localMounts)`，运行中隐藏
- 无挂载时显示"暂无挂载配置"占位文字
- 列表项显示: `source -> target` + 只读标记（如果 read_only）+ 删除按钮

UI 结构:
- 无 Card 包裹（嵌入详情页 Card 内部，参考 BindingManager）
- 列表: `divide-y divide-border/60` 分割线
- 每行: flex row, 左侧 `font-mono text-sm` 显示路径，右侧删除按钮
- 添加表单: flex row, source Input + target Input + readonly Checkbox + 添加 Button

**Step 3: 详情页接入**

修改 `web/admin/src/routes/_dashboard/hosts/$hostId.tsx`:
1. import 追加 `MountManager`:
```typescript
import { MountManager } from "@/components/hosts/mount-manager";
```
2. 在出口 IP 绑定 Card (L321-335) 之后、生命周期 Card (L337) 之前，新增挂载路径 Card:
```tsx
<Card className="rounded-xl border-border/80 shadow-sm">
  <CardHeader className="border-b bg-muted/30 pb-4">
    <CardTitle className="text-base">挂载路径</CardTitle>
    <CardDescription className="text-xs leading-relaxed">
      配置宿主机目录到容器的 bind mount。运行中主机需先停止后再编辑。
    </CardDescription>
  </CardHeader>
  <CardContent className="p-6 pt-5">
    <MountManager
      hostId={hostId}
      hostStatus={host.status}
      mounts={host.host_mounts ?? []}
    />
  </CardContent>
</Card>
```
注意：保持在 `grid grid-cols-1 gap-6 lg:grid-cols-2 lg:items-start` 容器内，与出口 IP 绑定 Card 并列。

**Step 4: 创建对话框接入**

修改 `web/admin/src/components/hosts/create-host-dialog.tsx`:
1. 新增 state: `const [hostMounts, setHostMounts] = useState<Array<{source: string; target: string; read_only?: boolean}>>([]);`
2. 在时区 Select (L213-233) 之后、`</div>` (L234) 之前，新增可折叠的"挂载路径"区域:
```tsx
<div className="space-y-2">
  <Label>挂载路径（可选）</Label>
  {hostMounts.map((m, i) => (
    <div key={i} className="flex items-center gap-2 text-sm">
      <span className="font-mono truncate" title={m.source}>{m.source}</span>
      <span className="text-muted-foreground">-&gt;</span>
      <span className="font-mono truncate" title={m.target}>{m.target}</span>
      {m.read_only && <span className="text-xs text-muted-foreground">(只读)</span>}
      <Button
        type="button"
        variant="ghost"
        size="sm"
        className="ml-auto h-6 w-6 p-0"
        onClick={() => setHostMounts(hostMounts.filter((_, j) => j !== i))}
      >
        <X className="h-3 w-3" />
      </Button>
    </div>
  ))}
  <div className="flex items-end gap-2">
    <div className="flex-1 space-y-1">
      <Input
        placeholder="宿主机路径 (例: /data/shared)"
        value={newMountSource}
        onChange={(e) => {
          setNewMountSource(e.target.value);
          if (!newMountTarget || newMountTarget === prevMountSource) {
            setNewMountTarget(e.target.value);
          }
          setPrevMountSource(e.target.value);
        }}
      />
    </div>
    <span className="text-muted-foreground pb-2">-&gt;</span>
    <div className="flex-1 space-y-1">
      <Input
        placeholder="容器路径 (默认同宿主机路径)"
        value={newMountTarget}
        onChange={(e) => setNewMountTarget(e.target.value)}
      />
    </div>
    <Button
      type="button"
      variant="outline"
      className="h-9"
      disabled={!newMountSource.startsWith("/") || !newMountTarget.startsWith("/")}
      onClick={() => {
        setHostMounts([...hostMounts, { source: newMountSource, target: newMountTarget }]);
        setNewMountSource("");
        setNewMountTarget("");
        setPrevMountSource("");
      }}
    >
      <Plus className="h-4 w-4" />
    </Button>
  </div>
  {newMountSource && !newMountSource.startsWith("/") && (
    <p className="text-xs text-destructive">宿主机路径必须以 / 开头</p>
  )}
</div>
```
需要新增 state: `newMountSource`, `newMountTarget`, `prevMountSource`。
3. `handleSubmit` 中 `createMutation.mutate` 的 payload 追加 `host_mounts: hostMounts.length > 0 ? hostMounts : undefined`。
4. `handleClose` 中重置: `setHostMounts([]); setNewMountSource(""); setNewMountTarget(""); setPrevMountSource("");`
5. import 追加 `X, Plus` from lucide-react（如尚未导入）。

**Step 5: 前端构建验证**

运行 `cd web/admin && npm run build` 确认无 TypeScript 或构建错误。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/cloud-cli-proxy/web/admin && npm run build</automated>
  </verify>
  <done>
    - `npm run build` 通过
    - use-hosts.ts 含 HostMount 接口 + useUpdateHostMounts hook + HostDetail.host 含 host_mounts
    - mount-manager.tsx 存在，导出 MountManager 组件，支持列表展示/添加/删除/保存
    - 详情页在出口 IP 绑定 Card 之后展示挂载路径 Card
    - 创建对话框含可折叠挂载路径区域，source 自动填充 target
    - 运行中状态禁用编辑
    - 保留路径显示警告提示
  </done>
</task>

</tasks>

<verification>
- `go build ./...` 编译通过
- `cd web/admin && npm run build` 构建通过
- `go test ./internal/store/repository/...` 无回归（host_mounts 有 DEFAULT '[]'，现有行不受影响）
- `go test ./internal/runtime/tasks/...` 无回归
- `go test ./internal/controlplane/http/...` 无回归
</verification>

<success_criteria>
- DB migration 文件存在且语法正确
- 所有 Host 读查询包含 host_mounts 列
- AdminHostStore 接口含 UpdateHostMounts 方法
- PUT /v1/admin/hosts/{hostID}/mounts 路由注册
- buildCreateArgs 为 BindMounts 生成 --mount type=bind 参数
- 绝对路径校验在 Worker 和 Admin API 双重执行
- 前端详情页展示并可编辑挂载配置
- 前端创建对话框支持可选配置挂载路径
- go build + npm run build 均通过
</success_criteria>

<output>
After completion, create `.planning/quick/260421-host-bind-mounts/260421-01-SUMMARY.md`
</output>
