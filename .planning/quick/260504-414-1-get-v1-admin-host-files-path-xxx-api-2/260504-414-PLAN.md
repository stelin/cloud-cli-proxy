---
phase: quick
plan: 260504-414
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controlplane/http/admin_host_files.go
  - internal/controlplane/http/router.go
  - internal/controlplane/http/admin_host_files_test.go
  - web/admin/src/hooks/use-host-files.ts
  - web/admin/src/components/hosts/path-autocomplete.tsx
  - web/admin/src/components/hosts/create-host-dialog.tsx
  - web/admin/src/components/hosts/mount-manager.tsx
autonomous: true
requirements:
  - QUICK-260504-414
must_haves:
  truths:
    - 管理员在新建主机对话框输入宿主机路径时能看到目录联想下拉列表
    - 管理员在挂载管理器添加挂载时输入宿主机路径时能看到目录联想下拉列表
    - API 拒绝路径遍历攻击（如 ../etc/passwd）
    - API 拒绝访问敏感系统目录（如 /proc、/sys、/dev）
  artifacts:
    - path: internal/controlplane/http/admin_host_files.go
      provides: GET /v1/admin/host-files?path=/xxx 端点实现
      exports: ["NewAdminHostFilesHandler"]
    - path: internal/controlplane/http/router.go
      provides: 路由注册
      contains: 'GET /v1/admin/host-files'
    - path: internal/controlplane/http/admin_host_files_test.go
      provides: handler 单测
      min_lines: 80
    - path: web/admin/src/hooks/use-host-files.ts
      provides: React Query hook 封装 API 调用
      exports: ["useHostFiles"]
    - path: web/admin/src/components/hosts/path-autocomplete.tsx
      provides: 可复用的路径自动补全输入组件
      exports: ["PathAutocomplete"]
    - path: web/admin/src/components/hosts/create-host-dialog.tsx
      provides: 新建主机对话框使用 PathAutocomplete
    - path: web/admin/src/components/hosts/mount-manager.tsx
      provides: 挂载管理器使用 PathAutocomplete
  key_links:
    - from: web/admin/src/components/hosts/path-autocomplete.tsx
      to: web/admin/src/hooks/use-host-files.ts
      via: useHostFiles hook
    - from: web/admin/src/hooks/use-host-files.ts
      to: GET /v1/admin/host-files
      via: apiFetch
    - from: internal/controlplane/http/router.go
      to: internal/controlplane/http/admin_host_files.go
      via: NewAdminHostFilesHandler
---

<objective>
为新建主机对话框和挂载管理器的宿主机路径输入框添加目录联想功能。

Purpose: 减少管理员手动输入宿主机绝对路径时的出错概率，提升挂载配置体验。
Output:
  - 后端 GET /v1/admin/host-files?path=/xxx API（带安全过滤）
  - 前端 PathAutocomplete 可复用组件
  - create-host-dialog.tsx 和 mount-manager.tsx 两处替换
</objective>

<execution_context>
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/Desktop/cloud-cli-proxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/controlplane/http/router.go
@internal/controlplane/http/admin_hosts.go
@web/admin/src/components/hosts/create-host-dialog.tsx
@web/admin/src/components/hosts/mount-manager.tsx
@web/admin/src/hooks/use-hosts.ts
@web/admin/src/lib/api.ts

<interfaces>
现有 API 模式（来自 router.go / admin_hosts.go）：
- 使用 `nethttp.NewServeMux()` + `mux.Handle("METHOD /path", handler)`
- admin 端点统一用 `adminGuard(handler)` 包裹
- handler 返回 `nethttp.Handler`，内部用 `writeJSON(w, status, payload)` 写响应
- 敏感操作记录 audit event（通过 `EventRecorder`）

现有前端模式（来自 use-hosts.ts / api.ts）：
- API 基地址 `/v1/admin`
- `apiFetch<T>(path, init?)` 封装 fetch，自动带 Bearer token
- React Query hook 模式：`useQuery({ queryKey: [...], queryFn: () => apiFetch<T>(path) })`
- 组件使用 shadcn/ui：Input、Button、Label、Dialog、Select 等

前端挂载数据结构：
```typescript
interface HostMount {
  source: string;   // 宿主机路径
  target: string;   // 容器路径
  read_only?: boolean;
}
```

create-host-dialog.tsx 中宿主机路径输入框当前实现：
- 第 263-272 行：纯 `<Input>` 组件，placeholder="宿主机路径 (例: /data/shared)"
- 状态：`newMountSource` / `setNewMountSource`
- 联动逻辑：输入 source 时若 target 为空或与上次 source 相同，自动同步 target

mount-manager.tsx 中宿主机路径输入框当前实现：
- 第 119-128 行：纯 `<Input>` 组件，placeholder="宿主机路径 (例: /data/shared)"
- 状态：`newSource` / `setNewSource`
- 联动逻辑同上
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端实现 GET /v1/admin/host-files API</name>
  <files>internal/controlplane/http/admin_host_files.go, internal/controlplane/http/admin_host_files_test.go, internal/controlplane/http/router.go</files>
  <behavior>
    - Test 1: 正常请求 path=/tmp 返回目录列表（子目录名数组），不含文件
    - Test 2: 路径遍历攻击 ../etc/passwd → 400 Bad Request
    - Test 3: 敏感目录 /proc → 403 Forbidden
    - Test 4: 不存在的路径 → 404 Not Found
    - Test 5: 非目录路径（如普通文件）→ 400 Bad Request
    - Test 6: 空 path 参数 → 400 Bad Request
    - Test 7: 相对路径 foo/bar → 400 Bad Request（必须以 / 开头）
  </behavior>
  <action>
    1. 创建 `internal/controlplane/http/admin_host_files.go`：
       - 定义敏感目录黑名单：`/proc`, `/sys`, `/dev`, `/boot`, `/etc/ssh`, `/root/.ssh`
       - 实现 `AdminHostFilesHandler` 结构体，含 `logger *slog.Logger`
       - 实现 `List() nethttp.Handler` 方法：
         - 从 query 参数读取 `path`
         - 校验 path 非空、以 `/` 开头、不包含 `..` 路径遍历片段（用 `strings.Contains(path, "..")` 或 `filepath.Clean` 比对）
         - 校验 path 不在敏感目录黑名单中（用 `strings.HasPrefix` 匹配）
         - 用 `os.Stat(path)` 确认路径存在且是目录
         - 用 `os.ReadDir(path)` 读取目录项，仅返回子目录名（过滤掉文件、symlink）
         - 返回 JSON：`{ "entries": ["dir1", "dir2"] }`
         - 错误时返回适当 HTTP 状态码 + `{ "error": "..." }`
       - 实现 `NewAdminHostFilesHandler(logger) *AdminHostFilesHandler`

    2. 修改 `internal/controlplane/http/router.go`：
       - 在 `Dependencies` 结构体中无需新增字段（handler 无外部依赖）
       - 在 adminGuard 区域内新增路由：`mux.Handle("GET /v1/admin/host-files", adminGuard(NewAdminHostFilesHandler(deps.Logger).List()))`

    3. 创建 `internal/controlplane/http/admin_host_files_test.go`：
       - 使用标准 `net/http/httptest` 测试 handler
       - 覆盖上述 7 个行为测试用例
       - 正常用例在临时目录下创建子目录结构，确保可移植
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/cloud-cli-proxy && go test ./internal/controlplane/http/ -run TestAdminHostFiles -v -count=1</automated>
  </verify>
  <done>
    - `go test` 7 条测试全部 PASS
    - `go build ./internal/controlplane/http/...` 无编译错误
    - 路由注册正确，API 可通过 `curl` 访问
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端实现 PathAutocomplete 组件和 useHostFiles hook</name>
  <files>web/admin/src/hooks/use-host-files.ts, web/admin/src/components/hosts/path-autocomplete.tsx</files>
  <action>
    1. 创建 `web/admin/src/hooks/use-host-files.ts`：
       - 导出 `useHostFiles(path: string)` hook
       - 使用 `useQuery`：
         - `queryKey: ["host-files", path]`
         - `queryFn: () => apiFetch<{ entries: string[] }>(\`/host-files?path=${encodeURIComponent(path)}\`)`
         - `enabled: path.length > 0 && path.startsWith("/")`
         - `staleTime: 30_000`（目录结构变化不频繁，30 秒缓存）

    2. 创建 `web/admin/src/components/hosts/path-autocomplete.tsx`：
       - Props 接口：
         ```typescript
         interface PathAutocompleteProps {
           value: string;
           onChange: (value: string) => void;
           placeholder?: string;
           disabled?: boolean;
           className?: string;
         }
         ```
       - 组件内部状态：
         - `open: boolean` — 下拉是否展开
         - `highlightedIndex: number` — 键盘高亮索引
       - 行为：
         - 使用 `useHostFiles(value)` 获取联想数据
         - 输入框获得焦点且 value 以 `/` 开头时展开下拉
         - 输入框失去焦点时延迟 150ms 关闭（允许点击选项）
         - 下拉显示 `entries` 列表，每个选项点击后 `onChange(value + "/" + entry)`（若 value 不以 `/` 结尾则补 `/`）
         - 键盘支持：↓/↑ 移动高亮，Enter 选中，Esc 关闭
         - 加载状态显示 `Loader2` 旋转图标
         - 空结果或无权限时显示 "无可用子目录" 提示
         - 若 value 不以 `/` 开头，不触发请求，显示提示文字
       - 样式：使用现有 shadcn/ui `Input` + 自定义下拉面板（绝对定位，z-50，白色背景，阴影边框，与现有设计一致）
       - 注意：组件高度不能撑破父容器，使用 `min-w-0` 和 `truncate` 处理长路径（继承 260504-2n4 修复）
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/cloud-cli-proxy/web/admin && npx tsc --noEmit --pretty 2>&1 | grep -c "path-autocomplete\|use-host-files" || echo "0 type errors in new files"</automated>
  </verify>
  <done>
    - TypeScript 编译无错误
    - `useHostFiles` 导出正确，类型完整
    - `PathAutocomplete` 导出正确，Props 接口完整
    - 组件支持键盘导航、点击选择、加载状态、空状态
  </done>
</task>

<task type="auto">
  <name>Task 3: 替换两处宿主机路径输入框为 PathAutocomplete</name>
  <files>web/admin/src/components/hosts/create-host-dialog.tsx, web/admin/src/components/hosts/mount-manager.tsx</files>
  <action>
    1. 修改 `create-host-dialog.tsx`：
       - 导入 `PathAutocomplete`
       - 将第 263-272 行的宿主机路径 `<Input>` 替换为 `<PathAutocomplete>`：
         - `value={newMountSource}`
         - `onChange={(v) => { setNewMountSource(v); if (!newMountTarget || newMountTarget === prevMountSource) { setNewMountTarget(v); } setPrevMountSource(v); }}`
         - `placeholder="宿主机路径 (例: /data/shared)"`
       - 保留原有的联动逻辑（target 自动同步）
       - 保留原有的验证提示（`!newMountSource.startsWith("/")` 时显示错误）
       - 确保 `min-w-0` 和 `truncate` 行为不变（PathAutocomplete 内部已处理）

    2. 修改 `mount-manager.tsx`：
       - 导入 `PathAutocomplete`
       - 将第 119-128 行的宿主机路径 `<Input>` 替换为 `<PathAutocomplete>`：
         - `value={newSource}`
         - `onChange={(v) => { setNewSource(v); if (!newTarget || newTarget === prevSource) { setNewTarget(v); } setPrevSource(v); }}`
         - `placeholder="宿主机路径 (例: /data/shared)"`
       - 保留原有的联动逻辑和验证提示
       - 保留 `RESERVED_PATHS` 警告逻辑（与 PathAutocomplete 不冲突）

    3. 两处修改的注意事项：
       - PathAutocomplete 的 `onChange` 回调在选中目录时会追加子目录名（如 `/data` → `/data/shared`），这与原有手动输入行为一致
       - 管理员可以继续手动输入完整路径，联想只是辅助
       - 确保添加按钮的 `disabled` 逻辑不受影响（仍检查 `startsWith("/")`）
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/cloud-cli-proxy/web/admin && npx tsc --noEmit --pretty 2>&1 | grep -c "create-host-dialog\|mount-manager" || echo "0 type errors in modified files"</automated>
  </verify>
  <done>
    - TypeScript 编译无错误
    - create-host-dialog.tsx 中宿主机路径输入已替换为 PathAutocomplete
    - mount-manager.tsx 中宿主机路径输入已替换为 PathAutocomplete
    - 原有联动逻辑（target 自动同步）和验证行为保持不变
    - 添加按钮的启用/禁用逻辑未被破坏
  </done>
</task>

</tasks>

<verification>
1. 后端：`go test ./internal/controlplane/http/ -run TestAdminHostFiles -v` 全部 PASS
2. 后端：`go build ./...` 无编译错误
3. 前端：`cd web/admin && npx tsc --noEmit` 无类型错误
4. 前端：`cd web/admin && npm run build` 构建成功
5. 端到端：启动后端后 `curl -H "Authorization: Bearer $TOKEN" "http://localhost:8080/v1/admin/host-files?path=/tmp"` 返回目录列表
6. 端到端：相同请求带 `path=../etc/passwd` 返回 400，`path=/proc` 返回 403
</verification>

<success_criteria>
- GET /v1/admin/host-files?path=/xxx 端点可用，返回目录名数组
- 路径遍历和敏感目录访问被正确拦截
- create-host-dialog.tsx 和 mount-manager.tsx 的宿主机路径输入框支持目录联想
- 管理员可以键盘或鼠标选择子目录，也可以继续手动输入
- 所有既有行为（target 联动、路径验证、添加按钮逻辑）保持完整
</success_criteria>

<output>
After completion, create `.planning/quick/260504-414-1-get-v1-admin-host-files-path-xxx-api-2/260504-414-SUMMARY.md`
</output>
